# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
*.egg-info/

# 노드
node_modules/
dist/
.vite/
*.tsbuildinfo

# 환경
.env
.env.*
!.env.example

# IaC
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# 도구
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.log

# 브라우저 자동화 산출물 — 스냅샷·스크린샷·화면 적재용 임시 사본이 쌓인다.
# 임시 사본에는 원천 데이터 바이트가 들어오므로 **추적하지 않는다**.
.playwright-mcp/

# 생성물 — 커밋 대상이다. 무시하지 않는다.
# (generated-up-to-date 게이트가 diff로 검증하므로 반드시 추적해야 한다)

# 워크트리는 레포에 들어가지 않는다 — git add -A 가 gitlink 로 집어넣는다
.claude/worktrees/
