네 단계 중 셋만 색을 받는다. 단계 3 규칙이 CSS에 없다.

**칩 클래스 생성 자리** — `CatalogTable.tsx:162`. 값이 null 이 아니면 `lvl lvl-${displayLevel(row)}` 로 그린다. 단계 값 원본은 `common/processingLevel` 의 `displayLevel` 이고 이 사본 디렉터리에는 없다. 단계가 0·1·2·3 네 값이라는 전제는 질문 문면을 따랐다.

**대응 규칙** — `catalog.css:130-133`

| 선택자 | 배경 | 글자색 |
|---|---|---|
| `.lvl` | 없음 (형태만: 높이·패딩·폰트) | 없음 |
| `.lvl-0` | gray-100 | gray-600 |
| `.lvl-1` | primary-50 | primary-700 |
| `.lvl-2` | primary-100 | primary-700 |
| `.lvl-3` | 규칙 부재 | 규칙 부재 |

공통 `.lvl` 은 형태 속성만 갖고 background·color 를 지정하지 않는다. 그래서 `lvl-3` 은 부모에서 상속한 글자색에 배경 투명으로 그려진다.

- 판정: 있음
- 빠진 규칙: `.lvl-3`
- 증상: 가공 단계 3 데이터셋의 「Lv3」 칩이 배경 없이 본문 글자색 그대로 표시되어 다른 단계 칩과 달리 pill 배경이 보이지 않는다.