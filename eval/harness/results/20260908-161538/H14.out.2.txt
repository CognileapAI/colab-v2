**결론 — 결함 있음.** 합격선 파일이 없으면 검사를 한 건도 하지 않고 「green · exit 0」을 찍는다. 실행으로 확인했다.

**흐름 (합격선 파일 부재 시)**

- `check-config.sh:5` — `COLAB_THRESHOLD_FILE` 미설정이면 `CONF` 가 빈 문자열.
- `check-config.sh:7` — `[ -z "$CONF" ]` 참 → 조건 성립. 경로가 있어도 파일이 없으면 `[ ! -f ]` 로 같은 분기.
- `check-config.sh:8` — 「합격선 파일이 없어 검사를 건너뜁니다.」 출력. 종료 없음.
- `check-config.sh:9`~`16` — `else` 블록(실제 검사 · `exit 1` 가능 자리)은 통째로 건너뜀.
- `check-config.sh:18`~`19` — 「check-config green」 출력 후 `exit 0`.

산출물 개수(`artifacts/`)는 한 번도 세지 않는다. 검사 0건 상태와 검사 통과 상태가 종료 코드·마지막 출력줄 모두 동일하다.

**규약 대조**

- 선언되면 검사한다 — 파일 존재 시에만 성립.
- 명시적 면제는 건수를 드러낸 채 넘어간다 — 면제 선언(별도 변수·플래그)이 없고, 건수(`N`)도 출력하지 않음.
- 아무 말도 없으면 실패한다 — 미충족. 「아무 말 없음」이 곧 green 이 됨.

「환경이 없어 못 돈 것」이므로 필요한 상태는 red(준비). 스크립트가 red(준비)로 판정할 자리는 8행 뒤에 비영 종료(관례상 `exit 2` 류 준비 코드, 러너 규약에 따름)를 두는 것이고, 면제를 허용하려면 명시적 면제 변수를 받아 그때만 `N` 을 출력하며 넘어간다.

- 판정: 결함 있음
- 모양: 설정 값이 없으면 조용히 건너뛰고 다음 줄에서 통과를 찍는 모양(silent skip → green-by-skip)
- 지금 나오는 결과: `check-config green` · exit 0
- 옳은 결과: red(준비) · exit 1 (비영 · 러너가 준비 코드를 따로 두면 그 코드)