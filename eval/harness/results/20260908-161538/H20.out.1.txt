대장(`work-items-excerpt.yaml`)을 먼저 고친다.

**두 순서의 대조**

- **별표 문단 `CLAUDE-6.md:3~5`** — 「상태 변경은 대장을 먼저 고친다. 항목 상태의 유일한 원본은 `dev-package/work-items.yaml` 이고 `03-HANDOFF §1` 은 그 반영본이다. 순서를 뒤집으면 산문이 다시 갈라진다.」 명시적 순서 규칙.
- **번호 목록 `CLAUDE-6.md:9~14`** — 1번이 `03-HANDOFF §1` 상태 갱신으로 시작. HANDOFF 가 먼저인 것처럼 읽힐 여지가 있음.

**판정 — 규칙은 별표 문단이다.**

- 별표 문단은 「원본 / 반영본」과 「순서를 뒤집으면 갈라진다」는 이유까지 적은 **순서 규정**이고, 2026-09-06 개정 표식(⭑)으로 목록 앞에 덧붙여진 상위 지침이다.
- 번호 목록은 「끝내기 전에 반드시」 해야 할 **누락 방지 체크리스트**다. 대장 항목이 목록에 없는 것은 별표 문단이 이미 선행 단계로 끌어올렸기 때문이고, 목록 번호는 항목 간 선후를 규정하지 않는다(3·4·5번 사이에도 실행 순서 의존이 없다).
- 인계 문서 발췌 자체도 같은 말을 한다 — `03-HANDOFF-excerpt.md` 상단 「상태의 원본은 `work-items.yaml`」. 판정은 게이트 `work-item-consistency` 가 대장 기준으로 하므로, HANDOFF 를 먼저 고치면 게이트 기준과 어긋난 중간 상태가 생긴다.

**원본과 반영본**

- 원본 = `work-items-excerpt.yaml`(대장). `WU-D6` 의 `status: open` → `done` 을 여기서 먼저 바꾼다.
- 반영본 = `03-HANDOFF-excerpt.md` §1 표의 상태 열(⬜/🟦 → ✅). 대장 값을 옮겨 적는 자리다.

- 먼저 고치는 파일: work-items-excerpt.yaml
- 두 번째: 03-HANDOFF-excerpt.md
- 원본/반영본: work-items-excerpt.yaml / 03-HANDOFF-excerpt.md
- 근거: CLAUDE-6.md:3