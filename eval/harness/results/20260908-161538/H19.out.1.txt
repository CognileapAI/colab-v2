`.verified--pending` 규칙은 이 디렉터리에 두 곳 있고, 둘 다 같은 토큰 쌍을 쓴다.

- `catalog.css:140` — `background: var(--color-gray-100); color: var(--color-gray-500)`
- `project.css:528-529` — `.project-detail .verified--pending` 이 같은 두 토큰을 그대로 반복

토큰 hex
- `--color-gray-500` = `#697077` (`tokens.css:10`)
- `--color-gray-100` = `#e8ecf2` (`catalog.css:6` · `project.css:10` 동일값)

WCAG 2.x 상대휘도 계산 결과 4.23:1 로 AA 합격선 4.5:1 미달. `catalog.css` 만 고치면 `project.css` 의 `.project-detail` 한정 규칙이 더 높은 특이도로 그대로 남으므로 두 곳을 함께 고쳐야 한다. `.lin--none`(`catalog.css:146`)도 같은 `gray-500` 글자색을 쓰지만 흰 면 위라 별도 계산 대상이고 이번 규칙 범위 밖이다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:529