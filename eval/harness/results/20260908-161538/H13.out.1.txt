**결과** — `.lin--none` 은 `--color-gray-400`(`#848c94`, `tokens.css:9`)을 글자색으로 쓰고, 흰 면 위 대비가 AA 기준 4.5:1 에 미달한다.

계산 근거 (WCAG 2.x 상대휘도, sRGB 선형화 후 `(L1+0.05)/(L2+0.05)`):

| 토큰 | hex | 흰 면 대비 | AA(4.5) |
|---|---|---|---|
| `--color-gray-400` (현재) | `#848c94` | 3.41:1 | 미달 |
| `--color-gray-500` | `#697077` | 5.02:1 | 통과 |
| `--color-text-muted` | `#565c63` | 6.76:1 | 통과 |

가장 작은 변경은 같은 회색 계열에서 한 단 진한 `--color-gray-500` 으로 토큰만 바꾸는 것이다. 이미 `tokens.css` 에 정의된 토큰이라 새 값 추가가 필요 없다. 파일은 변경하지 않았다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 `color: var(--color-gray-400)` 을 `var(--color-gray-500)`(`#697077`, 5.02:1)으로 바꾼다