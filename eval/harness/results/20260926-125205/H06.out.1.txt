카드 규칙에 그림자가 있습니다. 고칠 곳은 `catalog.css:31` 한 곳입니다.

- **고칠 곳:** `.catalog-page .card`(29–32행)의 31행에 `box-shadow: var(--shadow-sm);`가 있습니다. 정본은 카드에 그림자를 두지 않으므로 이 선언만 지우면 됩니다. 같은 줄의 `border-radius`와 `overflow: hidden`은 그대로 둡니다.
- **남겨둘 예외:** `.colmenu`(77–82행)는 열 메뉴 팝오버이고, 80행의 `box-shadow: var(--shadow-lg)`는 정본이 허용하는 예외입니다. 21행 `:root`의 `--shadow-lg` 토큰도 이 팝오버가 쓰고 있으니 함께 남겨야 합니다.
- **대상이 아닌 곳:** 50행과 52행의 `box-shadow`는 표 행 hover 때 왼쪽에 2px 강조선을 긋는 `inset` 선언입니다. 카드 그림자가 아니므로 지목 대상이 아닙니다.
- **따로 확인할 점:** 파일 안 어디에도 `--shadow-sm`이 정의되어 있지 않습니다. 31행을 지우면 이 토큰을 쓰는 곳이 이 파일에서 하나도 남지 않습니다.

- 판정: 있음
- 근거: catalog.css:31
- 유지할 예외: .colmenu