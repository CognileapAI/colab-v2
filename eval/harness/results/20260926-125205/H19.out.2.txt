대비 미달입니다. 이 디렉터리에는 `.verified--pending` 규칙이 두 개 있고, 둘 다 같은 토큰 조합이라 두 곳 모두 AA 4.5:1을 넘지 못합니다.

| 자리 | 글자색 | 배경색 |
|---|---|---|
| `catalog.css:140` | `var(--color-gray-500)` | `var(--color-gray-100)` |
| `project.css:528` (선언은 529행) | `var(--color-gray-500)` | `var(--color-gray-100)` |

**토큰 hex**
- `--color-gray-500` = `#697077` (`tokens.css:10`에만 정의)
- `--color-gray-100` = `#e8ecf2` (`catalog.css:6`과 `project.css:10`에 같은 값으로 각각 정의)

**계산 (WCAG 2.x 상대휘도)**
- L(#697077) = 0.1592
- L(#e8ecf2) = 0.8356
- 대비 = (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1** → 4.5:1 미달

`project.css:519-522` 주석에 따르면 `project.css`의 규칙은 `catalog.css`가 실리지 않는 화면을 위해 일부러 따로 적은 것입니다. 그래서 한 곳만 고치면 다른 한 곳은 미달로 남습니다.

같은 저대비 모양(gray-500 글자 또는 gray-100 배경)을 쓰는 다른 선택자도 있습니다(`catalog.css:46·61·124·146` 등). 이번 범위는 `.verified--pending`이라 셈에서 뺐고, 대비도 재지 않았습니다.

찾고 계산만 했고 파일은 고치지 않았습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528