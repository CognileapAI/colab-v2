`.lin--none` 배지는 흰 면 위에서 대비가 **3.41:1** 로, 합격선 4.5:1 에 못 미칩니다. 파일은 바꾸지 않았습니다.

- **글자색:** `catalog.css:135` 에서 `.lin--none { color: var(--color-gray-400); font-weight: 500; }` 이고, 토큰 값은 `tokens.css` 의 `--color-gray-400: #848c94` 입니다.
- **배경:** `.lin`(132행)과 `.lin--none` 어디에도 배경 지정이 없어서, 과제 조건대로 흰 면(`#ffffff`) 위에 놓입니다.
- **계산 (WCAG 2.x 상대휘도):**
  - `#848c94` 의 휘도는 0.2580, 흰색은 1.0000 입니다.
  - 대비는 (1.0 + 0.05) / (0.2580 + 0.05) = **3.41:1** 로, 기준 4.5:1 보다 낮습니다.
  - 글자가 11px, 굵기 500 이라서 큰 글자에 허용되는 3:1 예외는 적용되지 않습니다.
- **고칠 방향:** 같은 파일에 이미 있는 `--color-gray-500`(`#697077`)은 흰 면 위에서 **5.02:1** 입니다. 이 토큰은 `.verified--pending`·`.fchips` 버튼처럼 흐린 글자에 이미 쓰이고 있어 새 토큰이 필요 없습니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 `color` 를 `var(--color-gray-400)`(#848c94)에서 `var(--color-gray-500)`(#697077, 5.02:1)으로 바꾼다.