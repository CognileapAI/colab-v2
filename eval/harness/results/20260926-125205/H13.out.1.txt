「계보 없음」 배지의 글자 대비는 흰 면 위에서 3.41:1이라 합격선 4.5:1에 못 미칩니다. 파일은 바꾸지 않았습니다.

**찾은 값**
- `catalog.css:135` 규칙은 `.lin--none { color: var(--color-gray-400); font-weight: 500; }` 입니다. 배경을 따로 정하지 않아서 흰 면(`--color-surface` = `#ffffff`)이 그대로 비칩니다.
- `tokens.css` 에서 `--color-gray-400` 의 값은 `#848c94` 입니다.
- 글자 크기는 `.lin` 에서 물려받은 11px, 굵기는 500입니다. 큰 글자(18px 이상, 또는 굵게 14px 이상)가 아니므로 3:1 완화 기준은 쓸 수 없고 4.5:1이 그대로 적용됩니다.

**계산 (WCAG 2.x 상대휘도)**

| 색 | 상대휘도 L | 흰 면 대비 (1.05 / (L + 0.05)) |
|---|---|---|
| `#848c94` (`--color-gray-400`, 현재 값) | 0.2580 | **3.41:1** (미달) |
| `#697077` (`--color-gray-500`, 수정 후보) | 0.1592 | 5.02:1 (통과) |

**고치는 방법**
- `--color-gray-500` 은 이미 `tokens.css` 에 있는 토큰이라 새 값을 만들 필요가 없습니다. 글자색 참조만 바꾸면 5.02:1로 합격선을 넘습니다.
- fixture의 `SOURCE.md` 에도 같은 결함이 3.41:1로 적혀 있습니다. 위 수치는 그 기록과 별개로 직접 계산한 값이고, 결과가 같았습니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 글자색을 `var(--color-gray-400)`(#848c94)에서 `var(--color-gray-500)`(#697077, 5.02:1)으로 바꾼다