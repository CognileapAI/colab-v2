먼저 고칠 파일은 `work-items-excerpt.yaml` 이다. 근거는 `CLAUDE-6.md:3` 의 별표(⭑) 문단이다. 번호 목록은 `03-HANDOFF` 부터 적혀 있어 반대 순서처럼 보이지만, 규칙은 별표 문단 쪽이다.

**두 신호가 어떻게 다른가**
- **번호 목록 (`CLAUDE-6.md:9`)**: 1번 항목이 「`03-HANDOFF.md §1` 해당 WU 상태 갱신」이다. 이것만 읽으면 HANDOFF 를 먼저 고치는 것처럼 보인다.
- **별표 문단 (`CLAUDE-6.md:3`)**: 「상태 변경은 대장을 먼저 고친다」라고 순서를 직접 적는다.

**별표 문단이 규칙인 이유**
- **순서를 직접 말한다.** `:3` 에 「먼저」라는 말이 있다. 번호 목록은 순서를 말하지 않는다.
- **번호 목록은 순서를 정할 수 없다.** 1~6번 어디에도 대장(`work-items.yaml`)이 나오지 않는다. 대장과 HANDOFF 중 어느 쪽이 먼저인지 정할 근거가 목록 안에 없다. 목록은 「끝내기 전에 반드시」(`:7`) 로 시작하는, 세션을 닫기 전에 할 일을 적은 것이다.
- **순서를 뒤집으면 어떻게 되는지 적혀 있다.** `:4` 는 「순서를 뒤집으면 산문이 다시 갈라진다」고 한다. `:5` 는 판정을 게이트 `work-item-consistency` 가 한다고 한다. 즉 HANDOFF 는 대장과 맞는지 확인받는 쪽이다.
- **다른 파일도 같은 말을 한다.** `03-HANDOFF-excerpt.md:3` 에 「상태의 원본은 `work-items.yaml`」, 게이트가 읽는 것은 `WU`·`상태` 두 열이라고 적혀 있다.

**원본과 반영본**
- **원본**: `work-items-excerpt.yaml`. `WU-D6` 의 `status: open` 을 `done` 으로 바꾼다.
- **반영본**: `03-HANDOFF-excerpt.md` §1 표. 대장을 바꾼 뒤 해당 행의 상태를 ✅ 로 맞춘다. 번호 목록 1·2번(상태 갱신, 상단 요약 갱신)은 이때 한다.

- 먼저 고치는 파일: work-items-excerpt.yaml
- 두 번째: 03-HANDOFF-excerpt.md
- 원본/반영본: work-items-excerpt.yaml / 03-HANDOFF-excerpt.md
- 근거: CLAUDE-6.md:3