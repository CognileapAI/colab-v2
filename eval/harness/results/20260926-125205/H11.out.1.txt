`lineageGraph.css` 에서 죽은 스타일 두 모양을 모두 찾았습니다.

**⑴ 덮인 선언**
- `.detail-page .lin-way` 규칙(28–35행)에 `display` 가 두 번 나옵니다.
  - 29행: `display: inline-flex`
  - 33행: `display: inline-block`
- 뒤에 온 33행이 이기므로 29행의 `inline-flex` 는 적용되지 않습니다.
- 파일 안에서 `display` 가 두 번 선언된 규칙은 이것 하나뿐입니다.

**⑵ 미정의 토큰 참조: 11건**

두 `:root`(`tokens.css` 6–43행, `detail.css` 5–26행) 어디에도 정의되지 않은 토큰입니다. 11건 모두 폴백이 없어서 해당 선언은 무효입니다.

| 행 | 토큰 |
|---|---|
| 30 | `--color-accent-50`, `--color-accent-200` |
| 31 | `--color-accent-700` |
| 48 | `--color-gray-300` |
| 49 | `--color-gray-300` |
| 51 | `--color-primary-800` |
| 57 | `--color-primary-200`, `--color-primary-800` |
| 83 | `--color-accent-700`, `--color-accent-50` |
| 84 | `--color-ai` |

토큰 종류로는 7개(accent-50·200·700, gray-300, primary-200·800, ai)이고, 참조 건수로는 11건입니다.

- 덮인 선언: lineageGraph.css:33 이 lineageGraph.css:29 를 덮는다
- 미정의 토큰 참조: 11건
- 근거: 세 파일에서 주석을 공백으로 지운 뒤 Python 정규식으로 두 `:root` 블록의 `--x:` 정의를 모으고, lineageGraph.css 의 `var(--x` 참조를 행마다 대조해 폴백이 없는 미정의 참조를 셌다. 같은 방법으로 `{…}` 규칙마다 `display:` 선언이 몇 번 나오는지 셌다.