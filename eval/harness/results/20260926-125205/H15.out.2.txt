**결함이 있습니다.** 범위와 맞는 줄이 하나도 없으면 검사를 한 건도 하지 않고 green 으로 끝납니다.

**엉뚱한 값을 넘길 때** (예: `nope/`, 실행해서 확인함)
- `check-manifest.sh:9-12`: 매니페스트 4줄이 모두 `"$SCOPE"*` 에 걸리지 않습니다. 네 줄 모두 11행 `continue` 로 빠지고, 13행의 실재 검사는 한 번도 실행되지 않습니다.
- `check-manifest.sh:6`: `BAD=0` 이 그대로 남습니다.
- `check-manifest.sh:19`: `[ 0 -ne 0 ]` 이 거짓이라 오류 분기를 지나칩니다.
- `check-manifest.sh:24-25`: `check-manifest green — 불일치 0건` 을 찍고 `exit 0` 으로 끝납니다.
- 스크립트는 몇 줄을 검사했는지 세지 않습니다. 그래서 「0건을 검사해 불일치 0건」과 「N건을 검사해 불일치 0건」이 똑같이 green 으로 나옵니다. 저장소 규약이 금지하는 「대상이 없어 통과」가 바로 이 경우입니다.

**빈 값을 넘길 때** (`""`)
- `"$SCOPE"*` 가 `*` 이 되어 모든 줄에 걸립니다. 검사 범위가 조용히 매니페스트 전체(4건)로 넓어집니다.
- 이 fixture 에는 실물 파일이 없어서 `불일치 4건` · exit 1 이 나왔습니다. 하지만 파일이 다 있는 저장소라면 호출자가 요청하지 않은 범위를 검사하고 green 을 냅니다. 이것도 결함입니다.
- 인자를 아예 넘기지 않으면 `set -u` 때문에 5행에서 `$1: unbound variable` 로 exit 1 합니다. 이 경우는 우연히 실패로 끝납니다.

**고치는 방향** (파일은 고치지 않았습니다)
- 5행 직후에 `SCOPE` 가 비어 있으면 오류를 내고 exit 1 합니다.
- 범위에 걸린 줄을 세는 `MATCHED` 를 루프 안에서 올립니다. 19행보다 앞에 `MATCHED -eq 0` 이면 exit 1 하는 분기를 둡니다.
- green 줄에 검사한 건수를 함께 찍어서 0건 통과가 겉으로 드러나게 합니다.

- 판정: 결함 있음
- 검사 대상: 0건
- 지금 나오는 결과: check-manifest green — 불일치 0건 · exit 0
- 옳은 결과: `::error::범위 '<SCOPE>' 에 맞는 매니페스트 항목 0건` 을 내고 exit 1 (빈 값은 루프에 들어가기 전에 거부하고 exit 1)