`.verified--pending` 배지의 글자 대비는 **4.23:1**로, AA 합격선 4.5:1에 못 미칩니다. 이 디렉터리에서 같은 규칙이 두 곳에 있어서 둘 다 고쳐야 합니다.

**규칙이 있는 곳** (디렉터리 전체 grep 결과, CSS 파일 기준 2건)

| 파일:행 | 선언 | 배경 → hex | 글자 → hex |
|---|---|---|---|
| `catalog.css:140` | 한 줄 규칙 | `--color-gray-100` → `#e8ecf2` (`catalog.css:6`) | `--color-gray-500` → `#697077` (`tokens.css:10`) |
| `project.css:528` | `.project-detail .verified--pending` (색 선언은 529행) | `--color-gray-100` → `#e8ecf2` (`project.css:10`) | `--color-gray-500` → `#697077` (`tokens.css:10`) |

- `--color-gray-100`은 `catalog.css:6`과 `project.css:10`에 따로 선언돼 있지만 값이 같습니다. 그래서 두 규칙의 대비도 같습니다.
- `--color-gray-500`은 `tokens.css:10` 한 곳에만 선언돼 있습니다.

**계산 (WCAG 2.x 상대휘도)**
- 글자 `#697077`의 휘도는 0.1592, 배경 `#e8ecf2`의 휘도는 0.8356입니다.
- 대비 = (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1**. 4.5:1보다 낮아 불합격입니다.

**고치는 방법**
- 이미 있는 토큰으로 바꾸면 통과합니다. 같은 배경에서 `--color-gray-600`(`#565c63`)은 5.70:1, `--color-gray-700`(`#434950`)은 7.68:1입니다.
- 두 규칙의 `color`를 같은 토큰으로 함께 바꿔야 합니다. 한 곳만 바꾸면 다른 화면에는 미달이 그대로 남습니다.
- `tokens.css:10`의 `--color-gray-500` 값을 바꾸는 방법은 권하지 않습니다. `catalog.css:46·61·124·146` 같은 다른 규칙들까지 같이 바뀝니다.
- `--color-gray-600`은 `catalog.css`의 `:root`에만 선언돼 있습니다. `project.css`는 `catalog.css`를 싣지 않는 화면이라, 그대로 참조하면 값이 비게 됩니다. `project.css`의 `:root`에 `--color-gray-600: #565c63;`을 한 줄 추가하거나, `tokens.css`로 옮겨야 합니다.

파일은 고치지 않았습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528