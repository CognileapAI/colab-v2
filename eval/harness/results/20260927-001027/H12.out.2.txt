네 단계 중 3단계는 색을 받지 못합니다.

**칩 클래스를 만드는 자리**
- `CatalogTable.tsx:162`: `` className={`lvl lvl-${displayLevel(row)}`} ``
- `displayLevel(row)` 가 `null` 이면 칩을 그리지 않습니다(`:161`). 값이 0·1·2·3 이면 `lvl-0`~`lvl-3` 클래스가 붙습니다.

**`catalog.css` 에서 찾은 규칙** (`lvl` 로 파일 전체를 grep)

| 선택자 | 위치 | background | color |
|---|---|---|---|
| `.lvl` | `catalog.css:130` | 없음 (모양·글꼴만 지정) | 없음 |
| `.lvl-0` | `catalog.css:131` | `--color-gray-100` | `--color-gray-600` |
| `.lvl-1` | `catalog.css:132` | `--color-primary-50` | `--color-primary-700` |
| `.lvl-2` | `catalog.css:133` | `--color-primary-100` | `--color-primary-700` |
| `.lvl-3` | 없음 | — | — |

- `[class*="lvl-"]` 같은 넓은 선택자나 다른 대체 규칙은 이 파일에 없습니다.
- 공통 `.lvl` 도 배경·글자색을 정하지 않습니다. 그래서 3단계 칩은 알약 모양의 크기·여백·글꼴만 받습니다. 배경은 투명하고 글자색은 부모 요소의 색을 그대로 씁니다.

- 판정: 있음
- 빠진 규칙: `.lvl-3`
- 증상: 가공 단계 3 행의 `Lv3` 칩만 배경색 없이 부모 글자색으로 그려져, 색이 있는 0·1·2단계 칩과 달리 칩이 아닌 평문처럼 보인다.