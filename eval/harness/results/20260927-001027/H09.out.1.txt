상세 화면 정보 격자는 바깥 테두리와 칸 구분선이 같은 토큰을 써서, 요구한 두 층이 생기지 않습니다.

| 층 | 선언 위치 | 선언 | 토큰 |
|---|---|---|---|
| 컨테이너 선 | `detail.css:59` | `.detail-page .infogrid { border: 1px solid var(--color-border); … }` | `--color-border` |
| 칸 구분선 | `detail.css:62` | `.detail-page .infogrid .ig { … border-right: 1px solid var(--color-border); border-bottom: 1px solid var(--color-border); }` | `--color-border` |

- **값:** `tokens.css:20` 에서 `--color-border` 는 `#e8ecf2` 입니다. 두 선이 같은 색이어서 바깥 선과 칸 구분선이 구별되지 않습니다.
- **좁은 화면:** 폭 720px 이하에서 칸 구분선을 다시 지정하는 `detail.css:69` 도 같은 `--color-border` 를 씁니다.
- **다른 화면과의 비교:** 같은 코드베이스의 `catalog.css` 에서 표(`.tbl`)는 두 토큰을 나눠 씁니다. 머리글 아래 선(`:41`·`:45`)은 `--color-border-strong`, 행 사이 선(`:46`)은 `--color-border` 입니다. 상세 화면 정보 격자에는 이런 구분이 없습니다.

- 판정: 있음
- 근거: detail.css:59 · detail.css:62
- 토큰: --color-border / --color-border