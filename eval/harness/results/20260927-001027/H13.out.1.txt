「계보 없음」 배지의 글자 대비는 AA 기준 4.5:1에 못 미칩니다. 파일은 수정하지 않았습니다.

- **글자색 토큰:** `catalog.css:135` 의 `.lin--none { color: var(--color-gray-400); font-weight: 500; }` 가 이 배지의 글자색을 정합니다.
- **토큰 값:** `catalog.css` 의 `:root`(1~20행)는 `--color-gray-400` 을 다시 정의하지 않습니다. 그래서 값은 `tokens.css` 의 `--color-gray-400: #848c94` 입니다.
- **대비 계산 (WCAG 2.x 상대휘도):**
  - `#848c94` 의 상대휘도는 0.2580, 흰색(`#ffffff`)은 1.0 입니다.
  - (1.0 + 0.05) / (0.2580 + 0.05) = **3.41:1** 로, 합격선 4.5:1보다 낮습니다.
  - 굵기가 500이라 큰 글자 예외(3:1)도 적용되지 않습니다. 디자인 정본의 합격선도 4.5:1 하나입니다.
- **대안 토큰:**

| 토큰 | 값 | 흰 면 대비 |
|---|---|---|
| `--color-gray-400` (현재) | `#848c94` | 3.41:1 (미달) |
| `--color-gray-500` | `#697077` | 5.02:1 (합격) |
| `--color-gray-600` | `#565c63` | 6.76:1 (합격) |

회색 단계를 가장 작게 옮기면서 합격하는 토큰은 `--color-gray-500` 입니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 글자색 `var(--color-gray-400)`(#848c94)를 `var(--color-gray-500)`(#697077, 5.02:1)로 바꾼다.