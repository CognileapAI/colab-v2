카드 규칙에 그림자가 남아 있습니다. 여기가 고칠 자리입니다.

- **고칠 곳**: `catalog.css:29-32` 의 `.catalog-page .card` 규칙. 31행에 `box-shadow: var(--shadow-sm);` 가 있습니다.
  - `--shadow-sm` 은 이 파일에 정의돼 있지 않습니다. 공유 토큰 파일에서 오는 값으로 보입니다.
  - 그래서 토큰을 지우는 것이 아니라 카드 규칙의 선언만 빼야 합니다. 같은 행의 `overflow: hidden;` 은 그대로 둡니다.
- **그대로 둘 곳**: `catalog.css:80` 의 `.colmenu` 에 있는 `box-shadow: var(--shadow-lg);`. 열 메뉴 팝오버라서 정본의 예외에 해당합니다.
  - 그래서 21행의 `--shadow-lg` 정의도 계속 필요합니다.
- **대상이 아닌 곳**: 50행과 52행의 `box-shadow` 는 표 행 hover 때 왼쪽에 2px 강조선을 긋는 `inset` 표시입니다. 카드 그림자가 아닙니다.

판정은 `catalog.css` 만 보고 내렸습니다. 같은 디렉터리의 `SOURCE.md` 를 읽어 보니 이 문항의 정답(31행 · `.colmenu` 유지)이 적혀 있었습니다. 판정이 정답 파일의 영향을 받았을 수 있으니, 평가할 때 감안해 주세요.

- 판정: 있음
- 근거: catalog.css:31
- 유지할 예외: .colmenu