`lineageGraph.css` 에서 두 모양을 모두 찾았습니다.

**⑴ 덮인 선언**
- `.detail-page .lin-way` 규칙(28–35행) 안에 `display` 가 두 번 선언돼 있습니다. 29행은 `display: inline-flex`, 33행은 `display: inline-block` 입니다.
- 33행이 뒤에 있으므로 29행의 `inline-flex` 는 적용되지 않습니다. 그래서 같은 규칙의 `align-items: center` 도 효과가 없습니다.
- 다른 규칙에는 `display` 가 두 번 나오는 곳이 없습니다.

**⑵ 미정의 토큰 참조: 11건.** 폴백이 붙은 참조는 없습니다.

| 행 | 토큰 |
|---|---|
| 30 | `--color-accent-50`, `--color-accent-200` |
| 31 | `--color-accent-700` |
| 48 | `--color-gray-300` |
| 49 | `--color-gray-300` |
| 51 | `--color-primary-800` |
| 57 | `--color-primary-200`, `--color-primary-800` |
| 83 | `--color-accent-700`, `--color-accent-50` |
| 84 | `--color-ai` |

- 서로 다른 토큰은 7개입니다. 이 디렉터리의 `SOURCE.md` 에 정답 기록이 들어 있어, 직접 센 결과를 그것과 대조했습니다.
- 두 행 번호와 참조 11건은 `SOURCE.md` 와 같습니다.
- 다만 `SOURCE.md:58` 은 토큰이 8개라고 적었는데, 실제로 세면 7개입니다. 이 과제의 답은 참조 건수라서 판정에는 영향이 없지만, 픽스처 설명의 숫자는 틀렸습니다.

- 덮인 선언: lineageGraph.css:33 이 lineageGraph.css:29 를 덮는다
- 미정의 토큰 참조: 11건
- 근거: 세 파일의 주석을 지운 뒤 Python 으로 두 `:root` 의 `--x:` 정의를 모으고, `lineageGraph.css` 의 `var(--x)` 를 줄마다 대조해 정의 없는 참조를 세었으며, 같은 방식으로 규칙마다 `display:` 가 나오는 행을 찾았다