대장 `work-items.yaml`을 먼저 고칩니다. 근거는 번호 목록이 아니라 그 앞의 별표(⭑) 문단(`CLAUDE-6.md:3`)입니다.

**두 지시가 시사하는 순서**
- **별표(⭑) 문단(`CLAUDE-6.md:3-5`):** "상태 변경은 대장을 먼저 고친다"라고 순서를 직접 말합니다. 이유도 적혀 있습니다. 순서를 뒤집으면 산문이 다시 갈라진다는 것입니다(4행). 판정은 게이트 `work-item-consistency`가 한다고도 적혀 있습니다(5행).
- **번호 목록(`CLAUDE-6.md:9-14`):** 1번이 `03-HANDOFF.md §1` 상태 갱신이라서 HANDOFF를 먼저 고치라는 뜻으로 읽힐 수 있습니다. 하지만 목록 어디에도 `work-items.yaml`이 나오지 않습니다. 이 목록은 두 파일 사이의 순서를 정한 것이 아니라, 세션을 끝내기 전에 챙길 항목을 모은 것입니다.

**어느 쪽이 규칙인가**
- 별표 문단이 규칙입니다. 순서를 명시하고, 이유를 달고, 판정 주체까지 정합니다.
- 번호 목록의 1번은 "HANDOFF도 빠뜨리지 말라"는 뜻으로만 읽습니다. 따라서 HANDOFF §1 갱신은 대장을 고친 뒤 그 내용을 옮겨 적는 단계가 됩니다.

**원본과 반영본**
- **원본:** `dev-package/work-items.yaml`입니다. 별표 문단이 "유일한 원본"이라고 하고, HANDOFF 발췌 3행도 "상태의 원본은 `work-items.yaml`"이라고 적습니다.
- **반영본:** `03-HANDOFF §1`입니다. 같은 반영본으로 `WORK-UNITS §10~§11`도 있습니다. 게이트가 읽는 것은 HANDOFF 표의 `WU`·`상태` 두 열입니다.
- **작업 순서:** 대장 항목의 `status: open`을 `status: done`으로 바꿉니다. 그다음 HANDOFF §1 표의 해당 행을 ✅로 옮깁니다.

- 먼저 고치는 파일: dev-package/work-items.yaml
- 두 번째: dev-package/03-HANDOFF.md
- 원본/반영본: dev-package/work-items.yaml / dev-package/03-HANDOFF.md
- 근거: CLAUDE-6.md:3