「계보 없음」 배지는 흰 면 위 대비가 3.41:1 이라 합격선 4.5:1(AA)에 못 미친다. 파일은 읽기만 하고 바꾸지 않았다.

- **글자색**: `catalog.css:135` 의 `.lin--none` 은 `color: var(--color-gray-400)` 이다. `tokens.css` 에서 이 토큰의 값은 `#848c94` 이다.
- **배경**: `.lin--none` 에는 배경 지정이 없어서, 놓인 면인 흰색 `#ffffff` 이 그대로 보인다.
- **계산 (WCAG 2.x 상대휘도)**:
  - `#848c94` 의 휘도는 0.2580, `#ffffff` 는 1.0 이다.
  - 대비 = (1.0 + 0.05) / (0.2580 + 0.05) = **3.41:1**
- **완화 조건 없음**: 글자가 11px·굵기 500 이라 큰 글자 기준(3:1)을 쓸 수 없고, 4.5:1 을 넘어야 한다.
- **대안 토큰**: `tokens.css` 에 있는 `--color-gray-500` `#697077` 은 흰 면 위에서 5.02:1 이다. 값을 새로 만들지 않고 기존 토큰으로 합격선을 넘길 수 있다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 글자색 `var(--color-gray-400)`(#848c94)을 `var(--color-gray-500)`(#697077, 5.02:1)로 바꾼다.