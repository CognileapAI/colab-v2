v2 코드에는 이 지적이 해당하지 않습니다. Lv 칩 네 개 모두 4.5:1 을 넘고, 가장 낮은 값도 4.66:1 입니다.

칩 글자는 11px·굵기 600 이라 큰 글씨에 들어가지 않습니다(`detail.css:50`). 그래서 기준은 4.5:1 그대로입니다.

| 규칙 | 글자색 → hex | 배경색 → hex | 대비 |
|---|---|---|---|
| `.lvl-0` (`detail.css:51`) | `--color-gray-600` → `#565c63` (detail.css:7) | `--color-gray-100` → `#e8ecf2` (detail.css:6) | 5.70:1 |
| `.lvl-1` (`detail.css:52`) | `--color-primary-700` → `#0f62e0` (tokens.css:13) | `--color-primary-50` → `#edf4ff` (detail.css:11) | 4.94:1 |
| `.lvl-2`, `.lvl-3` (`detail.css:53`) | `--color-primary-700` → `#0f62e0` (tokens.css:13) | `--color-primary-100` → `#e2eeff` (detail.css:12) | 4.66:1 |

- 대비는 WCAG 2.x 상대휘도 공식(sRGB 선형화, 0.2126/0.7152/0.0722 가중)으로 계산했습니다.
- 목업의 3.71:1 은 이 코드의 토큰으로는 나오지 않습니다. 목업과 코드의 색 값이 다르다는 뜻입니다.
- 이 디렉터리의 `SOURCE.md` 에 정답(4.66:1, 「없음」)이 적혀 있습니다. 위 수치는 그 파일과 별개로 hex 에서 직접 계산했고, 결과가 같습니다.

- 판정: 없음
- 실측: 4.66:1
- 근거: detail.css:53