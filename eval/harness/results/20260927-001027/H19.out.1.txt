WCAG 대비 4.23:1로 AA 기준 4.5:1에 못 미칩니다. 이 디렉터리에서 `.verified--pending` 규칙은 두 곳에 있고, 둘 다 같은 색 조합을 씁니다.

| 자리 | 배경 | 글자 |
|---|---|---|
| `catalog.css:140` | `var(--color-gray-100)` | `var(--color-gray-500)` |
| `project.css:528` (선언은 529행) | `var(--color-gray-100)` | `var(--color-gray-500)` |

**토큰 값**
- `--color-gray-100` = `#e8ecf2`. `catalog.css:6`과 `project.css:10`의 `:root`에 같은 값으로 정의되어 있습니다.
- `--color-gray-500` = `#697077`. `tokens.css:10`에 정의되어 있습니다.

**계산 (WCAG 2.x 상대휘도)**
- `#697077`의 휘도는 0.1592, `#e8ecf2`의 휘도는 0.8356입니다.
- (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1**

**고칠 때 참고할 점**
- `project.css`의 규칙은 `catalog.css`를 복사한 것이 아니라 일부러 따로 둔 것입니다. `catalog.css`는 카탈로그 화면에서만 불러오고, 선택자를 `.project-detail` 안으로 묶어 두었기 때문입니다. 그래서 한 곳만 고치면 다른 화면에는 미달이 그대로 남습니다.
- 두 규칙 모두 토큰을 `var()`로 참조하므로 리터럴 hex만 계산하는 기존 대비 검사(`css_audit.py`)는 이 조합을 잡지 못합니다. 이 내용은 `SOURCE.md`에 적힌 설명이고, 제가 `css_audit.py`를 직접 확인하지는 않았습니다.
- 파일은 수정하지 않았습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528