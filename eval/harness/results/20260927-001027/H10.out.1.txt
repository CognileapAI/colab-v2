두 파일에서 음수 `margin` 선언은 2건입니다. 주석 안에는 음수 margin 값이 없습니다.

| 위치 | 선언 | 모양 |
|---|---|---|
| `detail.css:93` | `.detail-page .filelist { margin: -12px 0 var(--space-6); … border-top: none; }` | 세로 방향으로 12px 끌어올려 바로 위 `.infogrid`(60행 `margin-bottom: var(--space-6)`)에 붙입니다. `border-top: none` 과 짝을 이뤄 앞 요소에 이어 붙이는 전형적인 음수 상쇄입니다. |
| `shell.css:58` | `.detail-page .backlink, .project-detail .backlink { … padding: 3px 9px 3px 7px; margin-left: -7px; }` | 가로 방향으로 제 `padding-left` 7px 만큼 당깁니다. 호버 면이 생겨도 글자는 열 경계에 맞게 하려는 것입니다. 앞 요소에 붙이는 용도는 아니지만, 음수 값으로 여백을 상쇄하는 선언이라 셉니다. |

음수 값이지만 제외한 것:
- `shell.css:188` `bottom: -11px`: margin 이 아닌 위치 속성입니다.
- `shell.css:115` `letter-spacing: -0.02em`, `detail.css:33` `letter-spacing: -0.03em`: margin 이 아닙니다.
- 주석 속 `-` 표기(`S-05`, `E-06`, `BF-11`, `§1.3-1` 등): 코드가 아닙니다.

질문 범위 밖이지만, 모양 ⑵(자식이 `margin-top` 으로 제 여백을 지는 것)도 `detail.css` 36·37·41·42·43행에 보입니다. 이번 집계에는 넣지 않았습니다.

- 판정: 있음
- 음수 상쇄: 2건
- 근거: detail.css:93 · shell.css:58