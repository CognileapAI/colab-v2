상세 화면 정보 격자의 바깥 선과 칸 구분선이 같은 토큰 `--color-border`를 써서, 요구사항을 위반합니다.

- **컨테이너 선**: `detail.css:59`의 `.detail-page .infogrid`에 `border: 1px solid var(--color-border)`가 선언되어 있습니다.
- **칸 구분선**: `detail.css:62`의 `.detail-page .infogrid .ig`에 `border-right`와 `border-bottom`이 선언되어 있고, 둘 다 `1px solid var(--color-border)`입니다.
- **다른 규칙**: `detail.css:69`의 720px 이하 화면 규칙도 칸 구분선에 같은 `--color-border`를 씁니다.
- **토큰 정의**: `tokens.css:20`에서 `--color-border`는 `#e8ecf2`입니다. 같은 파일에 `--color-border-strong`(`:21`, `#dfe3e8`)이 따로 있지만, 정보 격자는 이 토큰을 쓰지 않습니다.
- **다른 파일**: `catalog.css`에는 `.detail-page .infogrid`를 덮어쓰는 규칙이 없습니다.

두 선의 색이 같아서 바깥 선과 칸 구분선이 구별되지 않고, 두 층으로 갈리지 않습니다.

- 판정: 있음
- 근거: detail.css:59 · detail.css:62
- 토큰: --color-border / --color-border