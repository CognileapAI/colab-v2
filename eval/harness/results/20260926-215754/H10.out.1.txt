두 파일에서 음수 margin 선언은 2건입니다. `grep`으로 전수 확인했습니다.

| 위치 | 선언 | 성격 |
|---|---|---|
| `detail.css:93` | `.detail-page .filelist { margin: -12px 0 var(--space-6); … }` | 세로 방향 음수 상쇄. 파일 목록을 바로 위 `.infogrid`에 붙이려고, 그 요소의 `margin-bottom: var(--space-6)`(detail.css:60) 중 12px을 음수로 되돌립니다. 대신 `border-top: none`과 아래쪽 모서리만 둥글린 `border-radius`로 두 박스를 하나처럼 이어 붙입니다. |
| `shell.css:58` | `.detail-page .backlink, .project-detail .backlink { margin-left: -7px; }` | 가로 방향 음수 상쇄. 자기 `padding-left: 7px`(shell.css:57)만큼 컨테이너 왼쪽 경계 밖으로 나가서, 글자 위치는 경계에 맞추고 hover 때 생기는 테두리와 배경은 바깥으로 삐져나오게 합니다. |

- **`shell.css:58`의 성격:** 앞 요소에 붙이는 모양은 아닙니다. 다만 자식이 음수로 컨테이너 경계를 넘으므로 「여백은 컨테이너가 소유한다」 위반으로 셌습니다.
- **대상에서 뺀 음수 값 (margin이 아님):**
  - `letter-spacing: -0.03em` (detail.css:33), `-0.02em` (shell.css:115)
  - `bottom: -11px` (shell.css:188)
  - `:nth-last-child(-n + 3)` (detail.css:64)
- **주석 안의 값:** 음수 margin 값은 없습니다. 잡힌 것은 `S-05`, `BF-11`, `E-06` 같은 식별자 속 하이픈과 줄표뿐입니다.
- **`SOURCE.md`:** 같은 디렉터리에 있는 이 파일에 정답 2건이 적혀 있었습니다. 위 결과는 CSS 원문을 직접 읽어 먼저 찾은 것이고, `SOURCE.md`와 일치합니다.
- **범위 밖이라 세지 않은 것:** 문제의 둘째 모양인 자식이 `margin-top`으로 제 여백을 지는 선언도 여럿 있습니다(`detail.css:36`, `37`, `43`, `96` 등).

- 판정: 있음
- 음수 상쇄: 2건
- 근거: detail.css:93 · shell.css:58