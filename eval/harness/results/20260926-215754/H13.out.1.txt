`.lin--none` 은 흰 면 위에서 대비가 3.41:1 이라 AA 합격선(4.5:1)에 미달합니다.

- **글자색:** `catalog.css:135` 의 `.lin--none` 은 `color: var(--color-gray-400)` 입니다. 이 토큰의 값은 `tokens.css:9` 에서 `#848c94` 입니다.
- **계산:** WCAG 2.x 식으로 구했습니다. `#848c94` 의 상대휘도는 0.2580, 흰색은 1.0 이라 (1.0 + 0.05) / (0.2580 + 0.05) ≈ **3.41:1** 입니다.
- **대안:** 같은 파일의 `--color-gray-500`(`#697077`)은 상대휘도 0.1592, 대비 **5.02:1** 로 AA 를 넘습니다.

이 fixture 의 `SOURCE.md` 에 정답(3.41:1, gray-500 로 수정)이 그대로 적혀 있습니다. 위 수치는 직접 다시 계산해 확인한 값이지만, 평가 과제로 쓴다면 이 파일이 답을 미리 알려 주는 셈입니다.

파일은 바꾸지 않았습니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 글자색 토큰 `--color-gray-400`(#848c94)을 `--color-gray-500`(#697077, 5.02:1)으로 바꾼다