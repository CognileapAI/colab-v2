`VENDORED-excerpt.md` 줄 번호 기준 판정 결과입니다.

- **표 시작:** 9행이 헤더, 10행이 구분선(`|---|…`)입니다.
- **표 안의 행:** 11행 `superpowers`, 12행 `mattpocock/skills`
- **13행:** 완전히 빈 줄입니다. `cat -A` 로 보면 `$` 만 있고 공백·탭도 없습니다. 표는 여기서 끝납니다.
- **표 밖으로 떨어진 행:** 두 줄 모두 표가 아니라 본문 문단으로 렌더링됩니다.
  - 14행 `emilkowalski/skills`
  - 15행 `vercel-labs/agent-browser`
- **계측 결과:** 9행부터 `|` 로 시작하는 줄이 몇 줄 이어지는지 셌더니 4줄이었습니다(헤더·구분선·데이터 2행). 13행을 지우면 6줄이 나와야 합니다.

- 판정: 있음
- 빈 줄: 13
- 표 밖으로 떨어진 행: 2건
- 확인 명령: `awk '/^\|/{n++; next} n{exit} END{print n}' VENDORED-excerpt.md` (고친 뒤 `6` 이면 정상, 지금은 `4`)