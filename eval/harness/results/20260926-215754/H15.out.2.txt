결함이 있습니다. 범위에 맞는 줄이 0건이어도 스크립트는 green(통과) · exit 0을 냅니다.

**줄별 추적 (엉뚱한 값, 예: `service/` 오타나 `nonexistent/`)**

- L5 `SCOPE="$1"` — 값이 있는지, 형식이 맞는지 검사하지 않습니다.
- L6 `BAD=0` — 이것이 유일한 카운터입니다. 실제로 몇 건을 검사했는지 세는 카운터는 어디에도 없습니다.
- L9–11 — 접두사가 어떤 줄과도 맞지 않으므로 4줄 모두 `*) continue`로 빠집니다. L13–16의 실재 검사는 한 번도 돌지 않습니다.
- L19 — `BAD`가 0 그대로라서 조건이 거짓입니다.
- L24–25 — `check-manifest green — 불일치 0건`을 찍고 exit 0으로 끝납니다. 하나도 검사하지 않았는데 이 결과가 나옵니다. 저장소 규약이 금지하는 「대상이 없어 통과」가 바로 이 경우입니다.

**다른 두 입력도 실제로 돌려 봤습니다**

| 호출 | 검사 대상 | 출력 · exit | 평가 |
|---|---|---|---|
| 인자 없음 | — | `line 5: $1: unbound variable` · exit 1 | `set -u` 덕분에 red가 나옵니다. 설계한 동작이 아니라 부수 효과이고, `::error::` 형식도 아닙니다. |
| `""` (빈 값) | 4건 (전체) | `::error::불일치 4건` · exit 1 | `"" *`가 모든 줄과 맞아서 범위가 조용히 매니페스트 전체로 넓어집니다. 이 픽스처에는 실물 파일이 없어 red가 나왔을 뿐이고, 실물이 다 있는 저장소라면 호출자가 뜻한 범위가 아닌 전체를 검사하고 green이 납니다. 이것도 결함입니다. |
| `service/`, `nonexistent/` | 0건 | `check-manifest green — 불일치 0건` · exit 0 | 핵심 결함입니다. |

**고칠 방향 (파일은 고치지 않았습니다)**

- L5 앞에 인자 검사를 둡니다. `[ -n "${1:-}" ] || { echo "::error::범위 접두사 인자가 비었다"; exit 1; }` 형태로, 빈 값과 인자 없음을 모두 명시적으로 거부합니다.
- L10의 `"$SCOPE"*)` 분기에서 `CHECKED=$((CHECKED + 1))`로 검사 건수를 셉니다.
- L19 앞에 `[ "$CHECKED" -eq 0 ]`이면 `::error::범위 '<SCOPE>' 에 맞는 매니페스트 줄 0건`을 찍고 exit 1로 끝냅니다.

- 판정: 결함 있음
- 검사 대상: 0건
- 지금 나오는 결과: check-manifest green — 불일치 0건 · exit 0
- 옳은 결과: `::error::범위 '<SCOPE>' 에 맞는 매니페스트 줄 0건`을 내고 exit 1로 끝낸다 (빈 인자도 인자 오류로 거부해 exit 1)