두 가지를 모두 찾았습니다. `display` 가 두 번 선언된 규칙은 하나이고, 두 `:root` 어디에도 없는 토큰을 참조하는 곳은 11건입니다.

**덮인 선언**
`lineageGraph.css` 에서 한 규칙에 `display` 가 두 번 나오는 곳은 `.detail-page .lin-way` 규칙 하나뿐입니다.
- 29행의 `display: inline-flex` 를 33행의 `display: inline-block` 이 덮습니다.
- 그래서 29행에 같이 적힌 `align-items: center` 도 실제로는 적용되지 않습니다. `align-items` 는 flex 컨테이너에서만 효과가 있기 때문입니다.

**미정의 토큰 참조**
주석을 빼고 세었고, 11건 모두 폴백이 없어 그 선언은 무효입니다.

| 행 | 토큰 |
|---|---|
| 30 | `--color-accent-50`, `--color-accent-200` |
| 31 | `--color-accent-700` |
| 48 | `--color-gray-300` |
| 49 | `--color-gray-300` |
| 51 | `--color-primary-800` |
| 57 | `--color-primary-200`, `--color-primary-800` |
| 83 | `--color-accent-700`, `--color-accent-50` |
| 84 | `--color-ai` |

토큰 종류로는 6개(`accent-50/200/700`, `gray-300`, `primary-200/800`, `ai`)입니다.

- 덮인 선언: lineageGraph.css:33 이 lineageGraph.css:29 을 덮는다
- 미정의 토큰 참조: 11건
- 근거: 세 파일에서 주석을 지운 뒤 두 `:root`(tokens.css·detail.css)의 `--x:` 정의를 모았고, lineageGraph.css 의 `var(--x)` 를 행 단위로 이 목록과 대조했으며, 규칙 블록마다 `display` 선언 수를 세는 python 스크립트를 돌렸다