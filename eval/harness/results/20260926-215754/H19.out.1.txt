AA 미달입니다. 같은 배지 규칙이 두 파일에 있고, 둘 다 같은 토큰 조합이라 두 곳 모두 고쳐야 합니다.

**`.verified--pending` 규칙 (디렉터리의 CSS 세 파일에서 `pending` 검색)**

| 자리 | 배경 | 글자 |
|---|---|---|
| `catalog.css:140` | `var(--color-gray-100)` | `var(--color-gray-500)` |
| `project.css:528` (배경·글자 선언은 529행) | `var(--color-gray-100)` | `var(--color-gray-500)` |

**토큰 hex**
- `--color-gray-100` = `#e8ecf2`. `catalog.css:6` 과 `project.css:10` 의 `:root` 에 같은 값으로 두 번 정의되어 있습니다.
- `--color-gray-500` = `#697077`. `tokens.css:10` 에만 있고, 다시 정의하는 곳은 없습니다.

**대비 계산 (WCAG 2.x 상대휘도)**
- 휘도는 `#697077` = 0.1592, `#e8ecf2` = 0.8356 입니다.
- (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1** 이라 기준 4.5:1 에 못 미칩니다.
- 두 배지 모두 11px·굵기 600 이라 큰 글자 예외(3:1)에 해당하지 않습니다.
- `SOURCE.md` 에 적힌 4.23:1 과 같지만, 그 값을 옮긴 게 아니라 직접 다시 계산했습니다.

**고치는 방법**
- 토큰 `--color-gray-500` 자체를 바꾸면 안 됩니다. `catalog.css` 의 다른 규칙 네 곳(46·61·124·146행)에서도 쓰고 있어서 영향이 번집니다.
- 두 규칙의 `color` 만 바꾸는 게 맞습니다. 예를 들어 `--color-text-muted`(`#565c63`)로 바꾸면 같은 배경에서 5.70:1 로 기준을 넘깁니다.
- 수정은 하지 않았습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528