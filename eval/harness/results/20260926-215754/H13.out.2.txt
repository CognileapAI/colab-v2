`.lin--none`은 흰 면 위에서 대비가 3.41:1로, AA 합격선 4.5:1에 못 미칩니다. 파일은 바꾸지 않았습니다.

**글자색 토큰**
- `catalog.css:135` 에 `.lin--none { color: var(--color-gray-400); font-weight: 500; }` 가 있습니다.
- `catalog.css` 의 `:root` 는 `--color-gray-400` 을 다시 정의하지 않습니다. 따라서 값은 `tokens.css:9` 의 `--color-gray-400: #848c94` 입니다.
- 이 배지에는 배경 선언이 없어 흰 면(`#ffffff`)이 그대로 보입니다.

**WCAG 2.x 대비 계산**

| 색 | 상대휘도 L | 흰색(L=1) 대비 `(1+0.05)/(L+0.05)` | AA 4.5:1 |
|---|---|---|---|
| `#848c94` (gray-400, 현재 값) | 0.2580 | **3.41:1** | 미달 |
| `#697077` (gray-500, 교체 후보) | 0.1592 | 5.02:1 | 충족 |

`--color-gray-500` 은 `tokens.css:10` 에 이미 있는 토큰입니다. 같은 파일의 비활성·흐린 글자(`catalog.css:40`, `:55`, `:118`)에도 이미 쓰이고 있어, 새 값을 만들지 않고 바로 쓸 수 있습니다.

같은 디렉터리의 `SOURCE.md` 에도 3.41:1과 gray-500 수정이 적혀 있습니다. 위 수치는 그 내용과 별개로 직접 계산했고, 결과가 같습니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 글자색 `var(--color-gray-400)`(#848c94)을 `var(--color-gray-500)`(#697077, 5.02:1)으로 바꾼다