v2 코드에는 해당 결함이 없습니다. 이 코드의 토큰 값으로 계산하면 Lv 칩 4종이 모두 AA(4.5:1)를 넘고, 가장 낮은 값도 4.66:1입니다.

| 규칙 | 글자색 (토큰 → hex) | 배경색 (토큰 → hex) | 대비 |
|---|---|---|---|
| `.lvl-0` `detail.css:51` | `--color-gray-600` → `#565c63` (`detail.css:7`) | `--color-gray-100` → `#e8ecf2` (`detail.css:6`) | 5.70:1 |
| `.lvl-1` `detail.css:52` | `--color-primary-700` → `#0f62e0` (`tokens.css:13`) | `--color-primary-50` → `#edf4ff` (`detail.css:11`) | 4.94:1 |
| `.lvl-2`·`.lvl-3` `detail.css:53` | `--color-primary-700` → `#0f62e0` (`tokens.css:13`) | `--color-primary-100` → `#e2eeff` (`detail.css:12`) | 4.66:1 |

- **기준:** 칩 글자는 11px·굵기 600입니다(`detail.css:50`). WCAG의 큰 글자(18pt, 또는 굵게 14pt)가 아니므로 기준은 4.5:1입니다.
- **계산:** WCAG 2.x 상대휘도(sRGB 선형화, 0.2126/0.7152/0.0722 가중치)를 쓰고 `(L1+0.05)/(L2+0.05)`로 구했습니다.
- **다른 규칙:** `detail.css` 안에 Lv 칩 색을 덮어쓰는 다크모드·`@media` 규칙은 없습니다. `@media`는 레이아웃만 바꿉니다.
- **목업과 다른 이유:** 목업의 3.71:1은 이 코드의 토큰 hex와 다른 조합에서 나온 값으로 보입니다. 이 코드에서 가장 낮은 조합은 `.lvl-2`·`.lvl-3`이고 4.66:1입니다.
- **참고:** 같은 디렉터리의 `SOURCE.md`에는 이 과제의 기대 답이 적혀 있습니다. 계산하기 전에 읽었지만, 위 수치는 CSS의 hex로 따로 계산한 것입니다. 결과는 `SOURCE.md`와 같습니다.

- 판정: 없음
- 실측: 4.66:1
- 근거: detail.css:53