승인 대기 배지 두 곳 모두 AA 기준에 못 미칩니다. 실측 대비는 4.23:1로, 합격선 4.5:1보다 낮습니다.

**어떤 규칙인가**

디렉터리 전체에서 `.verified--pending` 규칙은 두 개이고, 둘 다 같은 조합입니다.

| 자리 | 글자색 | 배경색 |
|---|---|---|
| `catalog.css:140` | `var(--color-gray-500)` | `var(--color-gray-100)` |
| `project.css:528` (선언은 529행) | `var(--color-gray-500)` | `var(--color-gray-100)` |

- **글자색 `--color-gray-500`:** `#697077`이고 `tokens.css:10`에서 정의합니다. 다른 파일에서 다시 정의하지 않습니다.
- **배경색 `--color-gray-100`:** `#e8ecf2`이고 두 곳에서 따로 정의하는데 값은 같습니다.
  - `catalog.css:6`
  - `project.css:10`

**계산 (WCAG 2.x 상대휘도)**

- `#697077`의 휘도는 0.1592, `#e8ecf2`의 휘도는 0.8356입니다.
- (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1**입니다.

**함께 확인한 것**

- `project.css:96·172·413`도 배경이 `--color-gray-100`입니다. 하지만 글자색을 `--color-gray-500`으로 지정하지 않아 같은 조합이 아니므로 고칠 자리에서 뺐습니다.
- `SOURCE.md`의 설명은 참고만 했습니다. 두 자리와 4.23:1은 CSS 파일을 직접 grep하고 계산해서 확인했습니다.

**고치는 방법 (파일은 아직 고치지 않았습니다)**

- 글자색을 `--color-text-muted`(`#565c63`, `tokens.css:27`)로 바꾸면 대비가 **5.70:1**로 AA를 통과합니다.
- 같은 값인 `--color-gray-600`은 `catalog.css:8`에만 정의돼 있습니다. `project.css` 주석에 따르면 그 화면은 `catalog.css`를 불러오지 않습니다. 그래서 두 파일 모두 전역 토큰인 `--color-text-muted`를 쓰는 편이 맞습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528