결론부터: 기준에 못 미칩니다. 대비는 4.23:1로 AA 기준 4.5:1보다 낮고, 같은 규칙이 두 곳에 있습니다. 파일은 고치지 않았습니다.

**규칙 위치 (이 디렉터리 전체 검색)**
- `catalog.css:140` — `.verified--pending { background: var(--color-gray-100); color: var(--color-gray-500); … }`
- `project.css:528` — `.project-detail .verified--pending {`이고, 색 선언은 다음 줄인 `project.css:529`에 있습니다. 주석에 따르면 이 화면은 `catalog.css`를 불러오지 않아서 규칙을 따로 두었습니다. 그래서 한 곳만 고치면 다른 한 곳은 그대로 남습니다.

**토큰 hex**

| 토큰 | 값 | 정의 위치 |
|---|---|---|
| `--color-gray-500` (글자) | `#697077` | `tokens.css:10` (정의는 여기 한 곳) |
| `--color-gray-100` (배경) | `#e8ecf2` | `catalog.css:6` · `project.css:10` |

**계산 (WCAG 2.x 상대휘도)**
- 글자 L = 0.1592, 배경 L = 0.8356
- 대비 = (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1**, 기준 4.5:1 미달
- `SOURCE.md`에 적힌 4.23:1과 직접 계산한 값이 같습니다.

**고칠 때 참고**
- 두 규칙의 글자색이나 배경색을 같은 방식으로 바꿔야 합니다.
- `--color-gray-500` 토큰 값을 바꾸면 이 토큰을 쓰는 다른 곳도 함께 바뀝니다. 흰 면 위 대비가 5.02:1인 곳 등이 여기에 해당합니다. 그러므로 두 규칙만 고치는 편이 안전합니다.
- `line-through`로 꺼진 상태처럼 보이게 한 표현도 WCAG에서 비활성 컨트롤이 대비 면제를 받는 요건에 맞지 않습니다. 이 배지는 조작하는 요소가 아니라 상태를 나타내는 글자입니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528