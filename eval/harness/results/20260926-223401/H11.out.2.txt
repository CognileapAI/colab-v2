`lineageGraph.css` 에서 덮인 선언 1곳과 미정의 토큰 참조 11건을 찾았습니다. 주석을 공백으로 지운 뒤 Python 스크립트로 셌습니다.

**덮인 선언**
- `.detail-page .lin-way` 규칙 하나에 `display` 가 두 번 있습니다. `lineageGraph.css:33` 의 `display: inline-block` 이 `lineageGraph.css:29` 의 `display: inline-flex` 를 덮습니다.
- 이 때문에 29행 옆에 적은 `align-items: center` 는 flex 전제인데 효과가 없습니다.
- 파일 전체에서 `display` 가 두 번 선언된 규칙은 여기 하나뿐입니다.

**미정의 토큰 참조**
- 두 `:root` 에 정의된 토큰은 45개입니다. `var()` 참조 중 폴백을 쓴 것은 한 건도 없습니다.

| 행 | 미정의 토큰 |
|---|---|
| 30 | `--color-accent-50`, `--color-accent-200` |
| 31 | `--color-accent-700` |
| 48 | `--color-gray-300` |
| 49 | `--color-gray-300` |
| 51 | `--color-primary-800` |
| 57 | `--color-primary-200`, `--color-primary-800` |
| 83 | `--color-accent-700`, `--color-accent-50` |
| 84 | `--color-ai` |

- 참조는 모두 11건이고, 서로 다른 토큰은 7가지입니다.
- 이 디렉터리의 `SOURCE.md` 에도 결과가 적혀 있습니다. 덮인 행(29·33)과 참조 11건은 제 측정과 같습니다. 다만 거기에는 토큰이 8가지라고 되어 있어 제가 센 7가지와 다릅니다. 답에 쓰는 참조 건수에는 영향이 없습니다.

- 덮인 선언: lineageGraph.css:33 이 lineageGraph.css:29 을 덮는다
- 미정의 토큰 참조: 11건
- 근거: 세 파일의 주석을 지우고 `tokens.css`·`detail.css` 두 `:root` 의 `--*:` 정의 45개를 모은 뒤, `lineageGraph.css` 의 `var(--…)` 참조를 줄마다 이 목록과 대조해 없는 것을 세고, 규칙 블록마다 `display:` 선언이 몇 번 나오는지 셌다