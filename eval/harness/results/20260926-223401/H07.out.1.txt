`lineageGraph.css` 에는 13px 미만인 `font-size` 선언이 13건 있고, 가장 작은 값은 10px 이다. 주석 안에는 `font-size` 가 없어서 제외한 것은 없다.

| 값 | 건수 | 위치 (줄) |
|---|---|---|
| 10px | 5 | 31 `.lin-way` · 72 `.lrow .stg` · 82 `.hist` · 83 `.aiflag` · 85 `.manflag` |
| 10.5px | 1 | 44 `.ln .n-role` |
| 11px | 5 | 10 `.dsec-h .hint` · 14 `.lin-stale .d` · 58 `.lin-use` · 81 `.lrow .ln-sub` · 95 `.lin-empty .muted` |
| 12px | 1 | 76 `.lrow .ln-name` |
| 12.5px | 1 | 45 `.ln .n-name` |

- 13px 이상인 `font-size` 는 26줄 `.lin-arw` 의 15px 하나다.
- `var()` 로 크기를 받는 선언 4건(9·92·93줄)은 세지 않았다. 사용 변수는 `--text-h2`·`--text-h3`·`--text-body-sm` 이다. 이 파일만으로는 몇 px 인지 알 수 없다.
- `font:` 축약형 선언은 없다.

- 판정: 있음
- 건수: 13
- 최소: 10px