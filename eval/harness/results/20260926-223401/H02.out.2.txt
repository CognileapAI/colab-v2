계측기는 주석 안의 값을 세지 않습니다. `<13px` 칸이 1건이고, 그 1건은 코드인 4행 `.a` 의 `11px` 입니다.

**계측기 출력**
- `sample.css` 의 `<13px` 칸은 1건입니다.
- `Detail` 목록에는 `sample.css:4` font-size 11.0px 한 줄만 있습니다.
- `contrast <4.5` 칸은 0건입니다.

**표본을 직접 센 결과**

| 행 | 위치 | font-size | 13px 미만 | 계측기 Detail |
|---|---|---|---|---|
| 2 | 주석 | 9px | 예 | 없음 |
| 4 | 코드 `.a` | 11px | 예 | 있음 (`:4` 11.0px) |
| 6 | 주석 | 10px | 예 | 없음 |
| 7 | 코드 `.b` | 14px | 아니오 | 없음 |
| 10 | 코드 `.c` | 13px | 아니오 (경계값, 미만 아님) | 없음 |

- 코드에서 13px 미만은 4행 하나뿐이고, 계측기 값 1과 같습니다.
- 13px 미만인 값 가운데 주석에 있는 것은 2행 `9px` 과 6행 `10px` 두 개이고, 둘 다 `Detail` 에 없습니다.
- 색 값도 확인했습니다. 주석에 있는 `#848c94` on `#ffffff` 조합은 3.41:1로 4.5 미만이라, 계측기가 셌다면 `contrast <4.5` 가 1 이상이어야 합니다. 출력은 0입니다.
- 코드에 쓰인 색 조합은 모두 4.5:1 이상입니다. 가장 밝은 `#697077` on `#ffffff` 를 직접 계산하면 약 5.0:1이므로 0건과 맞습니다.

- 판정: 주석분 0건
- 실측: 13px 미만 1건
- 주석 안에 있어 세지 않은 값: 2건
- 근거: `python3 "<repo>/eval/harness/H02-comment-false-positive/fixture/css_audit.py" --root "<repo>/eval/harness/H02-comment-false-positive/fixture"`