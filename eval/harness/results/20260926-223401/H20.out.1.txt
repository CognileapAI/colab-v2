**대장 `work-items.yaml` 을 먼저 고치고, `03-HANDOFF.md §1` 은 그다음에 고칩니다.** 근거는 `CLAUDE-6.md:3` 의 별표(⭑) 문단입니다.

**두 부분이 시사하는 순서**
- 번호 목록(`CLAUDE-6.md:9`): 1번 항목이 「`03-HANDOFF.md §1` 해당 WU 상태 갱신」입니다. 이것만 보면 HANDOFF 부터 고치는 것처럼 읽힙니다.
- 별표 문단(`CLAUDE-6.md:3-5`): 「**상태 변경은 대장을 먼저 고친다.**」라고 순서를 직접 적습니다.

**별표 문단이 규칙인 이유**
- 순서를 명시적으로 규정한 곳은 별표 문단(3행)뿐입니다. 번호 목록은 「끝내기 전에 반드시」 할 일의 점검 목록입니다(7행). 파일 사이의 편집 순서를 정하지 않습니다.
- 번호 목록에는 `work-items.yaml` 이 아예 나오지 않습니다. 대장과 HANDOFF 의 선후를 비교하는 근거가 될 수 없습니다.
- 별표 문단은 이유를 적습니다. 「순서를 뒤집으면 산문이 다시 갈라진다」(4행).
- 판정 주체도 적습니다. 사람 기억이 아니라 게이트 `work-item-consistency` 가 판정합니다(5행).
- 인계 문서 발췌 `03-HANDOFF-excerpt.md:3` 도 같은 내용입니다. 「상태의 원본은 `work-items.yaml`」이고, 게이트는 `WU`·`상태` 두 열을 읽습니다.

**원본과 반영본**
- 원본: `dev-package/work-items.yaml`. 항목 상태의 유일한 원본입니다(`CLAUDE-6.md:3`). 이 디렉터리의 발췌본에서는 `status: open` 을 `status: done` 으로 바꿉니다(`work-items-excerpt.yaml:3`).
- 반영본: `dev-package/03-HANDOFF.md §1`. 대장 상태를 옮겨 적는 곳입니다(`CLAUDE-6.md:4`). 상태 열을 ✅ 로 바꾸고, 이어서 번호 목록 2~6번을 진행합니다.

- 먼저 고치는 파일: work-items.yaml
- 두 번째: 03-HANDOFF.md
- 원본/반영본: work-items.yaml / 03-HANDOFF.md
- 근거: CLAUDE-6.md:3