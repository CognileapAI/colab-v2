**먼저 대장(`work-items.yaml`)을 고치고, 그다음 인계 문서(`03-HANDOFF.md §1`)를 고칩니다.** 근거는 `CLAUDE-6.md:3`의 ⭑ 문단입니다.

**두 문장이 가리키는 순서**
- `CLAUDE-6.md:3`의 ⭑ 문단은 「상태 변경은 대장을 먼저 고친다」고 적습니다. 이어서 `:3-4`에서 `work-items.yaml`이 유일한 원본이고 `03-HANDOFF §1`은 그 반영본이라고 밝힙니다.
- `CLAUDE-6.md:9`의 번호 목록 1번은 `03-HANDOFF.md §1` 상태 갱신입니다. 목록 어디에도 `work-items.yaml`이 나오지 않으니, 목록 순서만 따르면 인계 문서를 먼저 고치게 됩니다.

**⭑ 문단을 규칙으로 판정한 근거**
1. ⭑ 문단만 「먼저」라는 순서를 직접 말하고 이유도 붙입니다. 순서를 뒤집으면 산문이 다시 갈라진다는 것입니다. 번호 목록은 세션을 끝내기 전에 할 일을 모은 체크리스트이고, 원본과 반영본 사이의 우선순위는 말하지 않습니다.
2. 판정 주체도 ⭑ 문단에만 있습니다. `:5`에 「판정은 사람 기억이 아니라 게이트 `work-item-consistency`가 한다」고 적혀 있습니다. 목록 순서에는 이런 시스템 판정이 걸려 있지 않습니다.
3. 번호 목록 3번(`:11`)도 같은 원리를 씁니다. 값은 원본(`PLAN-SoT §9`)에 적고 HANDOFF에는 링크만 두라고 하고, 「두 곳에 적으면 갈라진다」는 이유를 답니다. ⭑ 문단은 이 원리를 상태값에 적용한 것이라서, 두 문장은 충돌이 아니라 ⭑ 문단이 목록 1번의 순서를 구체적으로 정하는 관계입니다.
4. 인계 문서 쪽도 같은 말을 합니다. `03-HANDOFF-excerpt.md:3`에 「상태의 원본은 `work-items.yaml`」이라고 적혀 있고, 게이트가 읽는 열은 `WU`와 `상태` 두 개라고 밝힙니다.

**원본과 반영본**
- 원본: `dev-package/work-items.yaml`. 여기서는 발췌본 `work-items-excerpt.yaml:4`의 `status: open` 필드를 `done`으로 바꿉니다.
- 반영본: `dev-package/03-HANDOFF.md §1` 진행도 표. 발췌본은 `03-HANDOFF-excerpt.md`이고, 원본을 바꾼 뒤 이 표의 `상태` 열을 ✅로 맞춥니다.

같은 디렉터리의 `SOURCE.md:37-38`에도 「규칙은 ⭑ 문단이다」라는 답이 적혀 있습니다. 위 판정은 `CLAUDE-6.md` 본문만으로도 성립하고, `SOURCE.md`는 그 결론과 일치할 뿐입니다.

- 먼저 고치는 파일: dev-package/work-items.yaml
- 두 번째: dev-package/03-HANDOFF.md
- 원본/반영본: dev-package/work-items.yaml / dev-package/03-HANDOFF.md
- 근거: CLAUDE-6.md:3