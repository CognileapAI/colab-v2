`lineageGraph.css`에서 13px 미만 `font-size` 선언은 **13건**이고, 가장 작은 값은 **10px**입니다. 주석(`/* … */`)을 모두 걷어낸 뒤 남은 `font-size` 선언 17개를 셌습니다.

| 행 | 선택자 | 값 |
|---|---|---|
| 10 | `.dsec-h .hint` | 11px |
| 14 | `.lin-stale .d` | 11px |
| 31 | 가공 방식 라벨 | **10px** |
| 44 | `.ln .n-role` | 10.5px |
| 45 | `.ln .n-name` | 12.5px |
| 58 | 활용 배지 | 11px |
| 72 | `.lrow .stg` | **10px** |
| 76 | `.lrow .ln-name` | 12px |
| 81 | `.lrow .ln-sub` | 11px |
| 82 | `.hist` | **10px** |
| 83 | `.aiflag` | **10px** |
| 85 | `.manflag` | **10px** |
| 95 | `.lin-empty .muted` | 11px |

- **값별 건수:** 10px 5건, 10.5px 1건, 11px 5건, 12px 1건, 12.5px 1건입니다. 소수점 값 2건(10.5px·12.5px)을 빼면 11건만 잡힙니다.
- **셈에서 뺀 선언:**
  - 13px 이상: 26행의 15px 1건입니다.
  - 토큰 참조: 9·92·93행의 `var(--text-h2)`·`var(--text-h3)`·`var(--text-body-sm)` 3건입니다. 이 파일 안에서는 값을 알 수 없어 제외했습니다. 토큰이 정의된 곳에서 13px 미만이면 건수가 늘어납니다.
- **주석 속 수치:** 주석 안에 `font-size` 값은 없습니다. 22행의 `820px`처럼 주석에 있는 수치는 `font-size`가 아니어서 영향이 없습니다.

직접 센 결과는 같은 디렉터리의 `SOURCE.md`에 적힌 기대값(13건, 최소 10px, 행 목록)과 일치합니다.

- 판정: 있음
- 건수: 13
- 최소: 10px