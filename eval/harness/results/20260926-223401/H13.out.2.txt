「계보 없음」 배지의 글자 대비는 흰 면 위에서 **3.41:1**로, AA 합격선 4.5:1에 못 미칩니다.

- **글자색 토큰:** `catalog.css:135`의 `.lin--none`은 `color: var(--color-gray-400)`입니다. `tokens.css`에서 이 토큰의 값은 `#848c94`입니다. 이 배지에는 배경색이 따로 없어서 흰 면 `#ffffff`에 그대로 놓입니다.
- **계산 (WCAG 2.x 상대휘도):**
  - `#848c94`의 상대휘도는 0.2580입니다.
  - 흰색의 상대휘도는 1.0000입니다.
  - 대비는 (1.0 + 0.05) / (0.2580 + 0.05) = **3.41:1**입니다.
  - 11px 글자(굵기 500)라서 큰 글자 기준 3:1은 적용되지 않고, 4.5:1을 넘어야 합니다.
- **제안 근거:** 같은 `tokens.css`에 한 단 진한 `--color-gray-500`(`#697077`)이 있고, 흰 면 위 대비가 **5.02:1**로 합격선을 넘습니다. 이 토큰은 이미 `.verified--pending`에서 꺼진 상태 글자색으로 쓰이고 있습니다.

파일은 바꾸지 않았습니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none`의 글자색을 `var(--color-gray-400)`(#848c94)에서 `var(--color-gray-500)`(#697077, 흰 면 위 5.02:1)으로 바꾼다.