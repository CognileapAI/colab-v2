카드 규칙에 `box-shadow`가 있습니다. 고칠 곳은 31행 하나입니다.

- **고칠 곳:** `.catalog-page .card`(29–32행)의 31행에 `box-shadow: var(--shadow-sm);`가 있습니다. 「카드에 그림자를 두지 않는다」 규칙에 어긋나는 곳은 이 선언뿐입니다.
- **예외(유지):** `.colmenu`(77–82행)는 열 메뉴 팝오버입니다. 80행의 `box-shadow: var(--shadow-lg)`는 예외라 그대로 둡니다. 21행 `:root`의 `--shadow-lg` 토큰도 이 팝오버만 쓰므로 함께 둡니다.
- **대상 아님:** 52행 `.tbl tr.clk:hover td:first-child`의 `box-shadow: inset 2px 0 0 …`는 행에 마우스를 올렸을 때 왼쪽에 생기는 2px 강조선입니다. 카드 그림자가 아니므로 지목하지 않습니다.

이 디렉터리의 `SOURCE.md`에 정답(「있음 · catalog.css:31 · .colmenu」)이 적혀 있습니다. 저는 CSS를 먼저 읽고 판정했고, 그 뒤에 이 파일을 봤습니다. 이 픽스처를 평가용으로 쓰신다면 정답 파일이 같은 디렉터리에 있으면 평가가 오염될 수 있습니다.

- 판정: 있음
- 근거: catalog.css:31
- 유지할 예외: .colmenu