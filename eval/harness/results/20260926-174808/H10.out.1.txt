두 파일의 코드에서 음수 margin 선언은 2건입니다. 주석 안에는 음수 값이 하나도 없습니다.

**음수 margin 선언**

| 위치 | 선언 | 하는 일 |
|---|---|---|
| `detail.css:93` | `.detail-page .filelist { margin: -12px 0 var(--space-6); … }` | 파일 목록을 위쪽 `.infogrid` 에 붙입니다. 같은 규칙에 `border-top: none` 과 아래쪽에만 둥근 모서리(`radius 0 0 …`)가 있어 앞 상자의 연장처럼 보이게 합니다. 정의 ⑴에 그대로 맞습니다. |
| `shell.css:58` | `.backlink { padding: 3px 9px 3px 7px; margin-left: -7px; … }` | 자기 왼쪽 padding 7px 를 음수 margin 으로 되돌립니다. 글자는 컨테이너 왼쪽 선에 맞고, hover 때 테두리와 배경만 밖으로 나옵니다. 앞 요소에 붙이는 것은 아니지만 자식이 컨테이너 밖으로 나가 제 위치를 스스로 맞추는 음수 상쇄입니다. |

**음수이지만 margin 이 아니어서 뺀 것**
- `shell.css:188` 의 `bottom: -11px`: 밑줄 `::after` 의 위치 값입니다.
- `shell.css:115` 와 `detail.css:33` 의 음수 `letter-spacing`: 자간입니다.
- `detail.css:64` 의 `:nth-last-child(-n + 3)`: 선택자 수식입니다.
- `--space-5`·`--space-6` 같은 토큰 이름 속 `-5`·`-6`: 값이 아닙니다.

「앞 요소에 붙이기」로 좁게 세면 `detail.css:93` 1건만 남습니다. 여기서는 음수 margin 을 전수로 찾으라는 요청에 따라 `shell.css:58` 도 넣었습니다.

- 판정: 있음
- 음수 상쇄: 2건
- 근거: detail.css:93 · shell.css:58