요약줄만 읽으면 **검사하지 않고 건너뛴 파일이 몇 개인지 알 수 없습니다.** 스크립트를 실제로 돌려 확인했습니다.

- 입력 파일은 3개입니다. `dataset.json`(226 B), `project.json`(106 B), `lineage.json`(0 B).
- `check-schemas.sh:10-11` — 빈 파일은 `SKIPPED` 에 1을 더하고 검사 없이 넘어갑니다. 이 입력에서는 `lineage.json` 이 여기에 해당합니다.
- `check-schemas.sh:22-25` — 실패 여부는 `BAD` 하나로만 정합니다. 그래서 건너뛴 파일이 있어도 green 으로 끝납니다.
- `check-schemas.sh:27` — 요약줄에는 `OK` 만 나오고 `SKIPPED` 는 나오지 않습니다.
- 실행 결과는 `check-schemas green — 검사 2건 전건 통과` 이고 종료코드는 0입니다. 입력 3건 중 2건만 검사했는데 요약줄은 「전건 통과」라고 말합니다. `lineage.json` 은 검사되지 않았는데도 요약줄에서 사라집니다.
- 2행 주석은 「전수로 검사한다」고 적혀 있어 실제 동작과 다릅니다.
- 요약줄만 고치면 건너뛴 건수는 보이게 되지만 green 판정은 그대로입니다. 「못 돌았음」을 통과로 세지 않는다는 규약을 판정까지 적용하려면 `SKIPPED` 가 1 이상일 때 green 을 막는 조건이 따로 필요합니다. 이번 답은 요약줄만 다룹니다.

- 판정: 결함 있음
- 숨은 계수: SKIPPED
- 근거: check-schemas.sh:27
- 고친 요약줄: echo "check-schemas green — 입력 $((OK + SKIPPED + BAD))건 중 통과 ${OK}건 · 건너뜀(빈 파일) ${SKIPPED}건 · 위반 ${BAD}건"