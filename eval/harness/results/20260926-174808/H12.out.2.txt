네 단계 중 3단계만 색을 받지 않습니다.

**칩 클래스를 만드는 자리**
- `CatalogTable.tsx:162`: `` className={`lvl lvl-${displayLevel(row)}`} ``
- `displayLevel(row)` 가 `null` 이면 칩을 그리지 않습니다(161행). 그 밖의 경우 `lvl-0` ~ `lvl-3` 이 나옵니다.

**`catalog.css` 에서 찾은 대응 규칙**

| 선택자 | 위치 | 배경 | 글자색 |
|---|---|---|---|
| `.lvl` | 130행 | 없음 | 없음 (모양·폰트만 정함) |
| `.lvl-0` | 131행 | `--color-gray-100` | `--color-gray-600` |
| `.lvl-1` | 132행 | `--color-primary-50` | `--color-primary-700` |
| `.lvl-2` | 133행 | `--color-primary-100` | `--color-primary-700` |
| `.lvl-3` | 없음 | 없음 | 없음 |

- 파일 전체에서 `lvl` 이 들어간 곳은 위 네 줄뿐입니다. `[class*=…]` 같은 속성 선택자도 없어서, 3단계에 색을 줄 다른 규칙이 없습니다.
- 공통 `.lvl` 은 배경과 글자색을 정하지 않습니다. 그래서 `lvl lvl-3` 칩은 알약 모양과 폰트만 받고, 배경은 투명하며 글자색은 부모 셀의 색을 그대로 씁니다.

`SOURCE.md` 를 보면 이 픽스처는 원본 `catalog.css` 의 134–136행(`.lvl-3` 규칙과 주석 두 줄)을 일부러 지운 사본입니다. 위 판정은 그 파일을 읽기 전에 grep 결과만으로 내렸습니다.

- 판정: 있음
- 빠진 규칙: .lvl-3
- 증상: 가공 단계 3인 행의 「Lv3」 칩만 배경 없이 셀 글자색으로 그려져, 다른 단계 칩과 달리 알약 모양이 보이지 않고 맨 글자처럼 보인다