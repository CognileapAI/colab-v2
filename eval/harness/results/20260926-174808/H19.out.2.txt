이 디렉터리에는 `.verified--pending` 규칙이 두 곳 있고, 두 곳 모두 대비가 4.23:1이라 AA 기준 4.5:1에 못 미칩니다. 파일은 고치지 않았습니다.

**규칙이 있는 자리**
| 자리 | 글자색 | 배경색 |
|---|---|---|
| `catalog.css:140` | `var(--color-gray-500)` | `var(--color-gray-100)` |
| `project.css:528` (선언은 529행) | `var(--color-gray-500)` | `var(--color-gray-100)` |

**토큰의 hex 값**
- `--color-gray-500`는 `#697077`입니다. `tokens.css:10`에만 정의되어 있고, 다른 파일에서 다시 정의하지 않습니다.
- `--color-gray-100`은 `#e8ecf2`입니다. `catalog.css:6`과 `project.css:10`에 같은 값으로 두 번 정의되어 있습니다.

**계산 (WCAG 2.x 상대휘도)**
- `#697077`의 휘도는 0.15924, `#e8ecf2`의 휘도는 0.83558입니다.
- 대비는 (0.83558 + 0.05) / (0.15924 + 0.05) = **4.23:1**입니다.

**고칠 자리**
- 한 곳만 고치면 나머지 화면의 배지가 그대로 남으니 두 규칙을 같이 고쳐야 합니다. `catalog.css`는 카탈로그 화면에서만 불러오고, 프로젝트 화면은 `project.css`에서 규칙을 따로 선언하기 때문입니다.
- 토큰 값은 바꾸지 않는 편이 맞습니다. `--color-gray-100`은 hover 배경과 `.chip--neutral` 등에도 쓰이고, `--color-gray-500`은 표 머리글 등에도 쓰여서 다른 화면까지 바뀝니다.
- 규칙에서 글자색만 `var(--color-text-muted)`(`#565c63`)로 바꾸면 같은 배경에서 5.70:1이 나와 기준을 넘습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528