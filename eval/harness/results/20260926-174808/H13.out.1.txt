`.lin--none` 는 AA 합격선을 넘지 못합니다. 흰 면 위 대비가 3.41:1 로 4.5:1 에 모자랍니다.

- **글자색 토큰:** `catalog.css:135` 는 `.lin--none { color: var(--color-gray-400); font-weight: 500; }` 이고, `tokens.css` 에서 `--color-gray-400` 의 값은 `#848c94` 입니다.
- **배경:** 이 클래스는 배경을 따로 정하지 않아 흰 면 `#ffffff` 위에 그대로 놓입니다.
- **계산 (WCAG 2.x 상대휘도):**
  - `#848c94` 의 상대휘도 L = 0.2580, 흰색은 1.0 입니다.
  - (1.0 + 0.05) / (0.2580 + 0.05) = **3.41:1**
- **큰 글자 기준 해당 없음:** `.lin` 은 11px 이고 굵기 500 입니다(`catalog.css:132`). 큰 글자로 치는 크기가 아니어서 3:1 완화 기준을 쓸 수 없고 4.5:1 이 그대로 적용됩니다.
- **대안 토큰:** 같은 파일의 `--color-gray-500` 인 `#697077` 은 5.02:1 로 합격선을 넘습니다.
- **토큰 값을 바꾸면 안 되는 이유:** `--color-gray-400` 는 표 머리 정렬 표시 `▾` 에도 쓰입니다(`catalog.css:65`). 그래서 토큰의 값은 두고, `.lin--none` 이 가리키는 토큰만 바꾸는 쪽이 적합합니다.

파일은 바꾸지 않았습니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 `color` 를 `var(--color-gray-400)` 에서 `var(--color-gray-500)`(#697077, 5.02:1)으로 바꾼다.