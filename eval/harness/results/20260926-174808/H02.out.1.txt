계측기는 주석 안의 값을 코드로 세지 않습니다. 13px 미만은 1건(`sample.css:4` 11px)만 나왔고, 표본을 직접 읽어 센 값과 같습니다.

**계측기 출력**
- `<13px` 칸: 1
- `Detail`: `sample.css:4` font-size 11.0px 한 줄
- `contrast <4.5` 칸: 0

**표본 대조** (`sample.css`의 `font-size` 5개)

| 줄 | 위치 | 값 | 13px 미만 | 계측기 |
|---|---|---|---|---|
| 2 | 주석 | 9px | 예 | 안 셈 |
| 4 | 코드 `.a` | 11px | 예 | 1건으로 셈 |
| 6 | 주석 | 10px | 예 | 안 셈 |
| 7 | 코드 `.b` | 14px | 아니오 | 해당 없음 |
| 10 | 코드 `.c` | 13px | 아니오 (기준이 `< 13`) | 해당 없음 |

- 코드 안의 13px 미만 값은 1건이고, 계측기 값 1과 같습니다.
- 주석 안의 13px 미만 값 2건(9px, 10px)은 출력 어디에도 없습니다.

**코드상 이유**
- `audit_file`이 파일을 읽자마자 `strip_comments()`로 `/* … */`를 공백으로 바꿉니다(`css_audit.py:82`). 이때 줄바꿈은 남기므로 줄 번호가 밀리지 않습니다.
- 토큰 정의를 모으는 `collect_defs`도 같은 방식으로 주석을 지웁니다(`css_audit.py:60`).

**색 값도 같습니다**
- 주석 안에 색 값이 4개 있습니다. 2번 줄의 `#848c94`·`#ffffff`, 9번 줄의 `#848c94 on #ffffff`(3.41:1)입니다.
- 이 값들은 대비 계산에 들어가지 않아서 `contrast <4.5` 칸이 0입니다.
- 아래 마지막 줄의 N은 질문 기준인 13px 미만 `font-size`만 센 수입니다. 주석 안의 색 4개는 포함하지 않았습니다.

- 판정: 주석분 0건
- 실측: 13px 미만 1건
- 주석 안에 있어 세지 않은 값: 2건
- 근거: `python3 "<repo>/eval/harness/H02-comment-false-positive/fixture/css_audit.py" --root "<repo>/eval/harness/H02-comment-false-positive/fixture"`