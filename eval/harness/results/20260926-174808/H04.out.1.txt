목업 검수의 지적은 v2 코드에는 해당하지 않습니다. v2 코드에서 대비가 가장 낮은 Lv 칩도 4.66:1 로 AA 기준 4.5:1 을 넘습니다.

| 규칙 | 위치 | 글자색 | 배경색 | 대비 |
|---|---|---|---|---|
| `.lvl-0` | `detail.css:51` | `--color-gray-600` `#565c63` | `--color-gray-100` `#e8ecf2` | 5.70:1 |
| `.lvl-1` | `detail.css:52` | `--color-primary-700` `#0f62e0` | `--color-primary-50` `#edf4ff` | 4.94:1 |
| `.lvl-2`, `.lvl-3` | `detail.css:53` | `--color-primary-700` `#0f62e0` | `--color-primary-100` `#e2eeff` | **4.66:1** |

- **토큰 출처:** `--color-primary-700` 은 `tokens.css:13` 에 있고, 나머지 네 토큰은 `detail.css` 의 `:root`(6·11·12행)에 있습니다.
- **계산:** WCAG 2.x 상대휘도 공식(sRGB 선형화, 0.2126/0.7152/0.0722 가중)으로 Python 을 써서 계산했습니다.
- **적용 기준:** Lv 칩 글자는 11px·굵기 600 이라 큰 글자 예외에 들지 않습니다. 그래서 4.5:1 기준을 적용했습니다.
- **다크 모드:** `prefers-color-scheme` 나 `data-theme` 로 색을 다시 지정하는 규칙이 두 파일 모두에 없습니다. 위 값이 곧 실제 적용되는 값입니다.
- **3.71:1 의 출처:** 이 코드의 토큰 조합으로는 3.71:1 이 나오지 않습니다. 목업에서 쓴 색 조합이 코드와 다른 것으로 보이지만, 목업 원본은 확인하지 않았습니다.
- **참고:** 픽스처에 있는 `SOURCE.md` 에 기대 정답(4.66:1, 「없음」)이 적혀 있습니다. 판정은 이 파일에 기대지 않고 위 계산만으로 냈고, 결과는 그 기대 정답과 같습니다.

- 판정: 없음
- 실측: 4.66:1
- 근거: detail.css:53