글자색과 배경색 조합이 AA 기준에 못 미칩니다. 대비는 4.23:1로, 합격선 4.5:1보다 낮습니다.

**`.verified--pending` 규칙이 있는 자리 (디렉터리 전체 grep)**
- `catalog.css:140`: 한 줄짜리 규칙입니다. `background: var(--color-gray-100); color: var(--color-gray-500);`
- `project.css:528`: 선택자는 `.project-detail .verified--pending`이고, 색 선언은 529행에 있습니다. `catalog.css`가 이 화면에 실리지 않아서 같은 규칙을 따로 한 번 더 적어 둔 곳입니다.

**토큰의 hex 값**
| 토큰 | hex | 정의 위치 |
|---|---|---|
| `--color-gray-500` (글자) | `#697077` | `tokens.css:10`, 이 한 곳에서만 정의 |
| `--color-gray-100` (배경) | `#e8ecf2` | `catalog.css:6` · `project.css:10`, 두 곳 모두 같은 값 |

**WCAG 2.x 대비 계산**
- 상대휘도: `#697077`은 0.1592, `#e8ecf2`는 0.8356입니다.
- 대비: (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1**이라 4.5:1에 미달합니다.

**고칠 자리**
두 규칙은 같은 토큰 조합을 씁니다. 그래서 `catalog.css:140` 한 곳만 고치면 상세 화면(`project.css:528`)의 배지는 계속 4.23:1로 남습니다. 두 곳 다 고쳐야 합니다.

반대로 토큰 hex를 바꿔서 해결할 수도 있습니다. 이 경우 `--color-gray-500`은 다른 규칙에서도 쓰이고(`catalog.css:46`·`61`·`124`·`146`), `--color-gray-100`은 두 파일 `:root`에 따로 선언되어 있습니다. 바꾸면 그 규칙들까지 함께 달라지니 따로 판단이 필요합니다. 파일은 수정하지 않았습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528