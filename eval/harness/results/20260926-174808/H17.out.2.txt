`check-schemas.sh:27` 의 요약줄만 읽으면 **건너뛴 파일이 있다는 사실과 그 수를 알 수 없다.** 빈 파일이 있어도 요약줄은 「전건 통과」라고 한다.

**스크립트 읽은 결과**
- `check-schemas.sh:10-12`: 빈 파일(`! -s`)은 `SKIPPED` 에 1을 더하고 `continue` 로 넘어간다. 검사를 하지 않는다.
- `check-schemas.sh:15`: `OK` 는 `$schema` 가 있는 파일만 센다.
- `check-schemas.sh:18`: `BAD` 는 `$schema` 가 없는 파일을 센다. `::error::` 줄로 파일마다 출력된다.
- `check-schemas.sh:27`: 요약줄은 `OK` 만 출력하고 「전건 통과」라고 쓴다. `SKIPPED` 는 어디에도 출력되지 않고, 종료 코드도 0 이다.
- `check-schemas.sh:23`: red 요약줄에도 `SKIPPED` 가 빠져 있다.

**직접 돌려 본 결과**
- `schemas/` 의 파일은 3개다: `dataset.json` 226B, `lineage.json` 0B, `project.json` 106B.
- 출력은 `check-schemas green — 검사 2건 전건 통과`, `exit=0` 이었다.
- `lineage.json` 은 검사하지 않았는데 출력 어디에도 나오지 않는다. 요약줄만 읽으면 스키마가 2개뿐이고 모두 통과한 것으로 읽힌다.
- 2행 주석은 「전수로 검사한다」고 하지만 실제로는 3건 중 2건만 검사했다.

**고칠 방향**
- 요약줄에 대상 전체 건수와 건너뛴 건수를 함께 적는다.
- 건너뛴 파일이 있으면 「전건 통과」라는 표현을 쓰지 않는다.
- 23행 red 요약줄에도 `SKIPPED` 를 넣는다.
- 파일은 고치지 않았다.

- 판정: 결함 있음
- 숨은 계수: SKIPPED
- 근거: check-schemas.sh:27
- 고친 요약줄: `echo "check-schemas green — 대상 $((OK + SKIPPED))건 중 통과 ${OK}건 · 건너뜀 ${SKIPPED}건(빈 파일, 미검사)"`