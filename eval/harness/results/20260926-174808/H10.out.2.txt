두 파일 모두에서 음수 margin 선언은 2건입니다.

| 위치 | 선언 | 하는 일 |
|---|---|---|
| `detail.css:93` | `.filelist { margin: -12px 0 var(--space-6); … border-top: none; }` | 바로 위 `.infogrid` 의 `margin-bottom: var(--space-6)`(`detail.css:60`) 중 12px 을 음수로 되돌려 파일 목록을 앞 요소에 붙입니다. 말씀하신 ⑴ 모양 그대로입니다. |
| `shell.css:58` | `.backlink { padding: 3px 9px 3px 7px; margin-left: -7px; }` | 자기 `padding-left` 7px 을 음수로 되돌려 글자를 컨테이너 왼쪽 선에 맞춥니다. 앞 요소에 붙이는 용도는 아니지만, 여백을 컨테이너가 아니라 자식이 음수로 조정한다는 점에서 음수 상쇄로 셌습니다. |

**음수이지만 margin 선언이 아니라 뺀 것**
- 위치 지정: `shell.css:188` `bottom: -11px`
- 글자 간격: `detail.css:33` `letter-spacing: -0.03em`, `shell.css:115` `letter-spacing: -0.02em`
- 선택자: `detail.css:64` `:nth-last-child(-n + 3)`
- 주석에는 음수 margin 값이 없습니다. 주석에 걸린 `-` 는 `E-06`, `§1.3-1`, `〈296〉-㉳` 같은 식별자 조각뿐입니다.

같은 디렉터리의 `SOURCE.md` 에 이 두 건이 정답으로 적혀 있는 것을 보았습니다. 이 파일은 grep 과 같은 명령으로 함께 출력됐고, 위 목록은 grep 결과와 두 CSS 파일의 행 번호를 직접 대조해 확인한 것입니다.

- 판정: 있음
- 음수 상쇄: 2건
- 근거: detail.css:93 · shell.css:58