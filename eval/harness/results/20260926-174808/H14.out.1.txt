**`check-config.sh` 는 합격선 파일이 없으면 검사를 건너뛰고도 green 을 찍고 exit 0 으로 끝난다. 결함이다.**

**흐름** (`COLAB_THRESHOLD_FILE` 을 unset 한 채 실행해 확인)

- `check-config.sh:5` — `CONF="${COLAB_THRESHOLD_FILE:-}"`. 변수가 없으면 빈 문자열이 되고, 실패 처리가 없다.
- `check-config.sh:7` — `[ -z "$CONF" ] || [ ! -f "$CONF" ]` 가 참이다. 변수가 없을 때와 변수가 가리키는 파일이 없을 때가 같은 갈래로 들어간다.
- `check-config.sh:8` — 「합격선 파일이 없어 검사를 건너뜁니다.」를 출력한다. 이 갈래에는 `exit` 이 없다.
- `check-config.sh:16` → `check-config.sh:18` — `fi` 를 지나 공통 꼬리로 떨어져 `check-config green` 을 출력한다.
- `check-config.sh:19` — `exit 0`.

실제 출력:
```
합격선 파일이 없어 검사를 건너뜁니다.
check-config green
exit=0
```

**규약 대조**

- 선언되면 검사한다: 해당하지 않는다. 합격선이 선언되지 않았다.
- 명시적으로 면제하면 건수를 드러내고 넘어간다: 해당하지 않는다. 스크립트에 면제 변수 같은 명시적 면제 수단이 없다. `artifacts/` 건수도 이 갈래에서는 세지 않는다(`check-config.sh:11` 은 else 쪽에만 있다).
- 아무 말도 없으면 실패한다: 이 경우다. 그런데 스크립트는 실패하지 않고 green 으로 끝난다.

8행의 안내문이 green 줄 바로 위에 찍혀 있어도 종료 코드는 0 이다. 종료 코드와 요약줄만 읽는 쪽(`gates/run.sh`·gate-runner)에는 green 으로 집계된다.

**옳은 결과**

- 합격선이 없는 것은 판정 재료가 없어 검사가 돌지 못한 것이다. 그래서 red(준비)다.
- 저장소 종료 코드 규약은 green 0 · red(판정) 1 · red(준비) 78 이다(`.agents/roles/measurement-lane.md:56`, `eval/harness/run.sh:20`, `infra/prod/tests/ship-gate.sh:10`).
- 따라서 8행 갈래는 red(준비)를 표식과 함께 출력하고 `exit 78` 로 끝나야 한다. 명시적 면제 경로를 둔다면 그 경로에서만 `artifacts/` 건수를 출력하고 넘어가야 한다.

- 판정: 결함 있음
- 모양: 설정 값이 없는 갈래가 `exit` 없이 공통 꼬리(18–19행)로 떨어져, 검사를 건너뛰고도 green · exit 0 을 찍는다(silent skip → green-by-skip)
- 지금 나오는 결과: `합격선 파일이 없어 검사를 건너뜁니다.` 다음 줄 `check-config green` · exit 0
- 옳은 결과: red(준비) · exit 78