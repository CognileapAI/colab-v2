대장(`work-items.yaml`)을 먼저 고치고, 그다음 인계 문서(`03-HANDOFF.md`)를 고칩니다.

**두 문단이 시사하는 순서**
- 번호 목록(`CLAUDE-6.md:9`)의 1번이 `03-HANDOFF.md §1` 상태 갱신이라, 인계 문서를 먼저 고치는 것처럼 읽힐 수 있습니다.
- 별표 문단(`CLAUDE-6.md:3`)은 「상태 변경은 대장을 먼저 고친다」고 적어, 대장이 먼저라고 명시합니다.

**어느 쪽이 규칙인가 — 별표 문단**
- 번호 목록은 「끝내기 전에 반드시」(7행) 해야 할 일을 모아 둔 점검표입니다. 대장 파일은 목록에 아예 나오지 않습니다. 그래서 목록 번호로는 대장과 인계 문서 사이의 순서를 정할 수 없습니다.
- 별표 문단은 순서 자체를 규정합니다. 「먼저」라고 쓰고, 「순서를 뒤집으면 산문이 다시 갈라진다」는 결과도 적습니다(4행).
- 별표 문단은 원본과 반영본도 정합니다. 원본은 `dev-package/work-items.yaml` 하나이고(3행), `03-HANDOFF §1`과 `WORK-UNITS §10~§11`은 「그 반영본」입니다(4행).
- 판정 주체도 적혀 있습니다. 사람 기억이 아니라 게이트 `work-item-consistency`가 판정합니다(5행).
- 인계 문서 발췌 `03-HANDOFF-excerpt.md:3`도 같은 내용입니다: 「상태의 원본은 `work-items.yaml`」, 게이트가 읽는 것은 `WU`·`상태` 두 열.

**실제로 바꿀 곳**
- 이 디렉터리의 `work-items-excerpt.yaml`이 원본 대장 `dev-package/work-items.yaml`의 발췌입니다. 여기서 `WU-D6`의 `status: open`(3행)을 `done`으로 바꿉니다.
- 그다음 `03-HANDOFF-excerpt.md`(원본 `dev-package/03-HANDOFF.md`)의 §1 표에서 해당 WU의 `상태` 열을 ✅로 바꿉니다.

- 먼저 고치는 파일: work-items.yaml
- 두 번째: 03-HANDOFF.md
- 원본/반영본: work-items.yaml / 03-HANDOFF.md
- 근거: CLAUDE-6.md:3