`.lin--none` 글자색은 `--color-gray-400`(`#848c94`) 이다. 흰 면 위 상대휘도 대비는 3.41:1 로 AA 합격선 4.5:1 미달이다. 같은 토큰 파일의 `--color-gray-500`(`#697077`)은 5.02:1, `--color-text-muted`(`#565c63`)은 6.76:1 로 통과한다. 파일은 수정하지 않았다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 `color: var(--color-gray-400)` 을 `var(--color-gray-500)`(#697077 · 5.02:1)으로 바꾼다