**결과**

⑴ 덮인 선언 — `.detail-page .lin-way` 규칙 안에 `display` 가 두 번 있다. 앞의 `display: inline-flex`(29행)를 뒤의 `display: inline-block`(33행)이 덮는다. 같은 규칙 안 후행 선언이 이긴다.

⑵ 미정의 토큰 참조 — 두 `:root`(`tokens.css:6` · `detail.css:5`) 어디에도 없고 폴백도 없는 토큰 6종, 참조 11건.

| 토큰 | 참조 행 |
|---|---|
| `--color-accent-50` | 30, 83 |
| `--color-accent-200` | 30 |
| `--color-accent-700` | 31, 83 |
| `--color-gray-300` | 48, 49 |
| `--color-primary-800` | 51, 57 |
| `--color-primary-200` | 57 |
| `--color-ai` | 84 |

주석 안의 값은 제외했다(1~5행 헤더, 22·27·32·47·53·87행 주석). 정의 45종 · 참조 58건 · 미정의 11건. 폴백 있는 참조는 0건.

- 덮인 선언: lineageGraph.css:33 이 lineageGraph.css:29 을 덮는다
- 미정의 토큰 참조: 11건
- 근거: 세 파일에서 `/* */` 주석을 제거한 뒤 `tokens.css`·`detail.css` 의 `--x:` 정의 집합과 `lineageGraph.css` 의 `var(--x` 참조를 정규식으로 대조하고, 미정의 토큰명을 grep 으로 두 파일에서 재확인(0건)했다