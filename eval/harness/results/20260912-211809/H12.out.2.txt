네 단계가 전부 색을 받지 않는다. 단계 3 만 빠진다.

- **칩 클래스 생성 자리** — `CatalogTable.tsx:162`. `lvl lvl-${displayLevel(row)}` 로 단계값을 그대로 붙인다. 단계 0·1·2·3 이 나오면 `lvl-0`~`lvl-3` 네 클래스가 생성된다.
- **CSS 대응 규칙** — `catalog.css:130~133`. 공통 `.lvl`(모양·크기만, 색 없음) 과 `.lvl-0`·`.lvl-1`·`.lvl-2` 세 규칙만 존재한다. `.lvl-3` 규칙은 파일 전체에 없다(grep 결과 0건).
- **결과** — 단계 3 칩은 공통 `.lvl` 로 둥근 테두리·크기·글꼴만 받고, `background`·`color` 는 상위 요소 값을 상속한다. 즉 배경 없는 투명 알약에 본문 글자색으로 `Lv3` 만 찍힌다.

- 판정: 있음
- 빠진 규칙: `.lvl-3`
- 증상: 가공 단계 3 데이터셋의 `Lv3` 칩이 배경색 없이 본문 글자색 그대로 그려져, 0·1·2 단계 칩과 달리 알약 배경이 보이지 않는다.