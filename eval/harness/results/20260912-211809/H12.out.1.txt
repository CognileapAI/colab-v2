**단계 3 이 색을 받지 못한다.**

- 칩 클래스 생성 자리 = `CatalogTable.tsx:162` — `lvl lvl-${displayLevel(row)}`. 단계 값 0·1·2·3 이 그대로 클래스 접미사가 된다.
- `catalog.css` 의 대응 규칙 = `.lvl`(공통 모양 · 130행) · `.lvl-0`(131) · `.lvl-1`(132) · `.lvl-2`(133). `.lvl-3` 규칙 없음.
- `.lvl` 공통 규칙에는 `background`·`color` 가 없으므로 단계 3 칩은 모양(둥근 알약·글꼴)만 받고 색은 상속값 그대로다.

- 판정: 있음
- 빠진 규칙: `.lvl-3`
- 증상: 가공 단계 3 데이터셋의 「Lv3」 칩이 배경 없이 투명한 알약 형태로, 글자색은 표 본문색 그대로 그려진다.