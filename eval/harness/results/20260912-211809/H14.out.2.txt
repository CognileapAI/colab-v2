**결론 — 결함 있음.** 합격선 파일이 없으면 검사 본체(10~15줄)를 한 번도 타지 않고 18~19줄에서 `green` · `exit 0` 을 찍는다. 「아무 말도 없으면 실패한다」 규약 위반이고, 「환경이 없어 못 돈 것」이 통과로 보고되는 green-by-skip 모양이다.

**흐름 추적 (합격선 파일 부재 시)**

- `check-config.sh:5` — `COLAB_THRESHOLD_FILE` 미설정 → `CONF=""`.
- `check-config.sh:7` — `[ -z "$CONF" ]` 참 → then 분기 진입.
- `check-config.sh:8` — 「합격선 파일이 없어 검사를 건너뜁니다.」 출력. **exit 없음.**
- `check-config.sh:10~15` — 합격선 읽기 · 산출물 계수 · 비교 전부 미실행.
- `check-config.sh:18~19` — 「check-config green」 출력 · `exit 0`.

실측(파일 없이 실행) —

```
합격선 파일이 없어 검사를 건너뜁니다.
check-config green
exit=0
```

**규약 3종 대조**

- 선언되면 검사한다 — 파일이 있을 때만 성립(9~16줄). 해당 없음.
- 명시적으로 면제하면 건수를 드러낸 채 넘어간다 — 면제 선언 변수·플래그가 존재하지 않고, 건너뛸 때 산출물 건수도 출력하지 않는다. 미충족.
- 아무 말도 없으면 실패한다 — 아무 말도 없는 상태(변수 미설정)가 8줄에서 green 으로 귀결. 위반.

**옳은 형태** — 7줄 분기에서 「합격선 파일 없음 · 경로=<값>」을 찍고 red(준비) 로 종료(exit 비0). 면제는 별도 명시 변수로만 허용하고 그때도 `N` 건수를 출력.

- 판정: 결함 있음
- 모양: 설정 값이 없으면 8줄에서 「건너뜁니다」만 찍고 exit 없이 18~19줄로 흘러 통과를 찍는 green-by-skip
- 지금 나오는 결과: `합격선 파일이 없어 검사를 건너뜁니다.` → `check-config green` · exit 0
- 옳은 결과: red(준비) · exit 1