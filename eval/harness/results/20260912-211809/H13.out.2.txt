`.lin--none` 은 `--color-gray-400`(`#848c94`)을 글자색으로 쓴다. 흰 면 위 대비는 3.41:1 로 AA 합격선 4.5:1 미달이다. 같은 토큰 파일의 `--color-gray-500`(`#697077`)은 5.02:1 로 통과하고, `--color-text-muted`(`#565c63`)는 6.76:1 이다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 `color: var(--color-gray-400)` 을 `var(--color-gray-500)` 으로 바꾸면 5.02:1 로 AA 통과