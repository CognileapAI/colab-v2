대장 `work-items-excerpt.yaml`을 먼저 고칩니다. 번호 목록이 아니라 CLAUDE-6.md:3의 ⭑ 문단이 규칙입니다.

**두 쪽이 시사하는 순서**
- **⭑ 문단 (CLAUDE-6.md:3–5)**: 3행에 「**상태 변경은 대장을 먼저 고친다.** 항목 상태의 유일한 원본은 `dev-package/work-items.yaml`」이라고 적혀 있습니다. 4행은 `03-HANDOFF §1`을 「그 반영본」으로 정하고, 순서를 뒤집으면 산문이 다시 갈라진다는 이유를 댑니다. 5행은 판정을 게이트 `work-item-consistency`가 한다고 적습니다.
- **번호 목록 (CLAUDE-6.md:9)**: 1번이 「`03-HANDOFF.md §1` 해당 WU 상태 갱신」이라서 목록 순서만 보면 HANDOFF를 먼저 고치는 것처럼 읽힙니다.

**⭑ 문단이 규칙인 이유**
1. 두 파일 사이의 순서를 직접 말하는 곳은 ⭑ 문단뿐입니다. 번호 목록에는 `work-items.yaml`이 아예 나오지 않습니다. 목록은 세션을 끝내기 전에 할 일을 적은 점검표이고, 대장과 반영본 중 무엇을 먼저 고칠지는 정하지 않습니다.
2. ⭑ 문단은 순서를 어겼을 때 생기는 결과(산문이 갈라짐)와 판정 주체(게이트)를 함께 적어 규칙으로 내세웁니다. 목록은 순서에 대해 아무 근거도 주지 않습니다.
3. 03-HANDOFF-excerpt.md:3도 「상태의 원본은 `work-items.yaml`」이라고 적어 같은 쪽을 가리킵니다.

**원본과 반영본**
- **원본**은 `work-items.yaml`입니다(발췌본 `work-items-excerpt.yaml`). 게이트가 기준으로 읽는 파일이라 여기서 `status: open` → `status: done`으로 먼저 바꿉니다.
- **반영본**은 `03-HANDOFF.md §1` 진행도 표입니다(발췌본 `03-HANDOFF-excerpt.md`). 대장을 고친 뒤 해당 WU 행의 `상태` 열을 ✅로 바꿉니다. 이어서 번호 목록 2번에 따라 같은 파일 상단의 최종 갱신·현재 단계·다음 WU도 갱신합니다.

- 먼저 고치는 파일: work-items-excerpt.yaml
- 두 번째: 03-HANDOFF-excerpt.md
- 원본/반영본: work-items-excerpt.yaml / 03-HANDOFF-excerpt.md
- 근거: CLAUDE-6.md:3