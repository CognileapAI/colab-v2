「계보 없음」 배지의 대비는 3.41:1이라 AA 합격선 4.5:1에 못 미칩니다. 파일은 고치지 않았습니다.

- **글자색**: `catalog.css:135`에서 `.lin--none { color: var(--color-gray-400); font-weight: 500; }`로 정해져 있습니다.
- **hex**: `--color-gray-400`는 `tokens.css`에 `#848c94`로 정의되어 있습니다. `catalog.css`의 `:root` 블록이 이 토큰을 덮어쓰지 않는 것도 확인했습니다.
- **배경**: `.lin--none`은 배경을 따로 지정하지 않으므로 흰 면(`#ffffff`) 위에 놓입니다.
- **글자 크기**: 11px, weight 500이라 큰 글자 예외(3:1)에 해당하지 않습니다. 그래서 4.5:1이 합격선입니다.

**WCAG 2.x 상대휘도 계산**
- `#848c94`의 상대휘도 L = 0.2580, 흰색의 L = 1.0
- 대비 = (1.0 + 0.05) / (0.2580 + 0.05) = **3.41:1**

**고칠 후보** (흰 면 기준, 같은 방식으로 계산)
- `--color-gray-500` `#697077`: 5.02:1로 합격합니다.
- `--color-gray-600` `#565c63`: 6.76:1로 합격합니다.

한 단계만 올려도 합격하므로 `gray-500`을 제안합니다. 이 파일의 다른 보조 글자(`.verified--pending`, 표 머리)도 `gray-500`을 쓰고 있어 서로 맞춰집니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none`의 `color`를 `var(--color-gray-400)`(#848c94)에서 `var(--color-gray-500)`(#697077, 5.02:1)로 바꾼다