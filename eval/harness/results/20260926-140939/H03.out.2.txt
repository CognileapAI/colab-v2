`VENDORED-excerpt.md` 13행이 빈 줄이고, 그 뒤 2행이 표 밖으로 떨어졌습니다.

- **표 범위:** 9행 머리글, 10행 구분 행, 11–12행 데이터 행 2개(superpowers, mattpocock/skills)입니다.
- **빈 줄:** 13행이 빈 줄이라 표가 여기서 끝납니다.
- **떨어진 행:** 14행 `emilkowalski/skills`와 15행 `vercel-labs/agent-browser`는 표 행이 아니라 `|`로 시작하는 본문 문단으로 렌더됩니다.
- **실측:** 아래 확인 명령으로 연속된 `|` 줄 묶음을 세면 지금은 `4`와 `2`, 두 묶음이 나옵니다. 13행을 지우면 `6` 한 묶음만 나와야 합니다.

- 판정: 있음
- 빈 줄: 13
- 표 밖으로 떨어진 행: 2건
- 확인 명령: `awk '/^\|/{c++} !/^\|/{if(c)print c; c=0} END{if(c)print c}' VENDORED-excerpt.md`