13행이 빈 줄이라 표가 12행에서 끝납니다. 그 아래 14·15행 두 줄은 표 밖으로 떨어졌습니다.

- **표 안에 남은 줄 (9–12행):** 머리행(9), 구분행(10), 데이터 행 `superpowers`(11)와 `mattpocock/skills`(12).
- **13행:** `cat -A` 결과가 `$` 하나뿐인 완전한 빈 줄입니다. 공백도 없습니다.
- **표 밖으로 떨어진 줄 (14–15행):** `emilkowalski/skills`와 `vercel-labs/agent-browser`. 14행 바로 다음인 15행이 `|---|` 구분행이 아니어서 새 표로도 읽히지 않습니다. 그래서 두 줄은 본문 문단이 됩니다.
- **실측:** `|`로 시작하는 줄이 연속으로 이어진 묶음의 길이를 세면 지금은 `4`와 `2`, 두 묶음으로 나옵니다. 고친 뒤에는 `6` 하나만 나와야 합니다(머리행 1 + 구분행 1 + 데이터 4).

- 판정: 있음
- 빈 줄: 13
- 표 밖으로 떨어진 행: 2건
- 확인 명령: `awk '/^\|/{n++; next} n{print n; n=0} END{if(n)print n}' VENDORED-excerpt.md`