카드 규칙에 그림자가 있습니다. `catalog.css:31` 한 곳만 고치면 됩니다.

- **고칠 곳:** 카드 규칙 `.catalog-page .card`(29–32행) 안의 31행입니다. 여기에 `box-shadow: var(--shadow-sm);` 가 선언되어 있어 「카드에 그림자를 두지 않는다」에 어긋납니다. 이 선언만 빼면 되고, 같은 줄의 `border-radius`·`overflow` 는 그대로 둡니다.
- **다른 곳에서 다시 선언되지 않음:** 좁은 화면용 규칙(158–168행)에는 `.card` 가 없습니다. 파일 전체에서 `card` 가 나오는 곳도 29행뿐입니다.
- **예외라서 두는 것:**
  - `.colmenu` 80행 `box-shadow: var(--shadow-lg)`: 팝오버이므로 떠 있는 면이 맞습니다.
  - 52행 `.tbl tr.clk:hover td:first-child { box-shadow: inset 2px 0 0 … }`: 카드 그림자가 아니고, 표 행에 마우스를 올렸을 때 왼쪽에 생기는 2px 강조선입니다. 50행은 그 전환 효과를 정한 줄입니다.

같은 디렉터리의 `SOURCE.md` 에 이 문항의 정답(`catalog.css:31`, `.colmenu`)이 적혀 있고, 저는 그 파일도 읽었습니다. 판정은 CSS 원문을 직접 확인해서 냈지만, 채점할 때 그 파일이 정답을 미리 보여 주는 셈이라는 점은 알아 두셔야 합니다.

- 판정: 있음
- 근거: catalog.css:31
- 유지할 예외: .colmenu