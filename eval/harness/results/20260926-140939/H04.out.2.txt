이 지적은 v2 코드에는 해당하지 않습니다. Lv 칩 네 단계 모두 4.5:1 을 넘고, 가장 낮은 값은 `.lvl-2`·`.lvl-3` 의 4.66:1 입니다.

토큰 hex 는 `detail.css` 의 `:root` 와 `tokens.css` 에서 가져왔습니다. `--color-primary-700` 만 `tokens.css:13` 에 있습니다.

| 규칙 | 행 | 글자색 | 배경색 | 대비 |
|---|---|---|---|---|
| `.lvl-0` | detail.css:51 | `--color-gray-600` #565c63 | `--color-gray-100` #e8ecf2 | 5.70:1 |
| `.lvl-1` | detail.css:52 | `--color-primary-700` #0f62e0 | `--color-primary-50` #edf4ff | 4.94:1 |
| `.lvl-2`, `.lvl-3` | detail.css:53 | `--color-primary-700` #0f62e0 | `--color-primary-100` #e2eeff | 4.66:1 |

- **적용 기준:** 칩 글자는 11px·600 입니다(detail.css:50). 큰 글자 기준(18.66px 굵게)에 못 미치므로 3:1 이 아니라 4.5:1 을 적용했습니다.
- **목업과의 차이:** 목업의 3.71:1 은 이 코드의 어떤 글자색·배경 조합에서도 나오지 않습니다. 목업 쪽 글자색이나 배경 값이 이 코드와 다른 것으로 봅니다.
- **SOURCE.md 에 정답이 들어 있음:** 픽스처 안의 `SOURCE.md` 에 정답(「4.66:1 → 없음」)이 그대로 적혀 있습니다. 위 수치는 그 파일과 상관없이 직접 계산했고 결과는 같습니다. 다만 이 파일을 읽을 수 있는 상태에서는 에이전트가 계산 없이 답을 베낄 수 있어서, 이 평가로는 계산 능력을 제대로 잴 수 없습니다. 평가 실행 때는 이 파일을 픽스처 밖으로 옮기기를 권합니다.

- 판정: 없음
- 실측: 4.66:1
- 근거: detail.css:53