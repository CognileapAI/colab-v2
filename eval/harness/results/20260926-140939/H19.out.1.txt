`.verified--pending` 배지의 글자 대비는 **4.23:1** 로 AA 합격선 4.5:1 에 못 미칩니다. 같은 규칙이 두 파일에 따로 선언되어 있어 두 곳을 다 고쳐야 합니다.

**`.verified--pending` 규칙 (디렉터리 전체 grep 결과 2건)**

| 위치 | 배경 | 글자 |
|---|---|---|
| `catalog.css:140` | `--color-gray-100` | `--color-gray-500` |
| `project.css:528` (선언은 529행) | `--color-gray-100` | `--color-gray-500` |

**토큰 값**
- `--color-gray-100` = `#e8ecf2`. `catalog.css:6` 과 `project.css:10` 에 같은 값으로 따로 선언되어 있습니다.
- `--color-gray-500` = `#697077`. `tokens.css:10` 에서 옵니다.

**WCAG 2.x 계산**
- 상대휘도: `#697077` → 0.1592, `#e8ecf2` → 0.8356
- 대비: (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1**

**두 곳이 따로 있는 이유**
- `project.css` 는 520~522행 주석에 이 규칙을 다시 적은 이유를 밝혀 두었습니다. `catalog.css` 는 카탈로그 화면에서만 실리고, 이 화면(project)은 그 파일을 싣지 않기 때문입니다.
- 그래서 `catalog.css:140` 만 고치면 프로젝트 상세 화면의 배지는 4.23:1 그대로 남습니다.

**고칠 때 주의할 점**
- `tokens.css:10` 의 `--color-gray-500` 은 다른 자리에서도 씁니다. 예를 들어 `catalog.css:46` · `61` · `124` · `146` 입니다.
- 그래서 토큰 값을 바꾸기보다 두 규칙의 `color` 를 더 진한 토큰으로 바꾸는 것이 영향 범위가 작습니다.
- 후보는 `catalog.css:8` 의 `--color-gray-600` `#565c63` 입니다. 이 조합의 대비는 따로 재지 않았습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528