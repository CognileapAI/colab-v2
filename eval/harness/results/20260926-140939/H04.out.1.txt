v2 코드에는 이 지적이 해당하지 않습니다. 네 칩 모두 4.5:1을 넘고, 가장 낮은 값도 4.66:1입니다.

| 칩 | 규칙 위치 | 글자색 | 배경색 | 대비 |
|---|---|---|---|---|
| `.lvl-0` | `detail.css:51` | `--color-gray-600` `#565c63` (`detail.css:7`) | `--color-gray-100` `#e8ecf2` (`detail.css:6`) | 5.70:1 |
| `.lvl-1` | `detail.css:52` | `--color-primary-700` `#0f62e0` (`tokens.css:13`) | `--color-primary-50` `#edf4ff` (`detail.css:11`) | 4.94:1 |
| `.lvl-2`, `.lvl-3` | `detail.css:53` | `--color-primary-700` `#0f62e0` (`tokens.css:13`) | `--color-primary-100` `#e2eeff` (`detail.css:12`) | 4.66:1 |

- 대비는 WCAG 2.x 상대휘도(sRGB 선형화 후 0.2126·0.7152·0.0722 가중)로 계산했습니다.
- 칩 글자는 11px·600(`detail.css:50`)이라 큰 글자 기준(3:1)이 아니라 일반 글자 기준(4.5:1)으로 봤습니다.
- 목업의 3.71:1은 이 코드의 어느 조합에서도 나오지 않습니다. 목업과 v2가 쓰는 토큰 hex가 다른 것으로 보입니다.

한 가지 문제가 있습니다. 같은 디렉터리의 `SOURCE.md`가 정답(`#0f62e0` on `#e2eeff`, 4.66:1, 판정 「없음」)을 적어 두고 있습니다. 이번 값은 CSS만으로 따로 계산했고 결과가 같았습니다. 그래도 픽스처 디렉터리를 읽을 수 있는 모델은 계산하지 않고 이 파일에서 답을 가져올 수 있습니다. H04가 판정 능력을 제대로 재려면 `SOURCE.md`를 픽스처 밖으로 옮겨야 합니다.

- 판정: 없음
- 실측: 4.66:1
- 근거: detail.css:53