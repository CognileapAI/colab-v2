승인 대기 배지(`.verified--pending`)는 AA 기준에 미달하고, 고칠 자리는 두 곳입니다. 파일은 고치지 않았습니다.

**`.verified--pending` 규칙 전수 (3개 파일 grep)**

| 자리 | 배경 | 글자 |
|---|---|---|
| `catalog.css:140` | `var(--color-gray-100)` | `var(--color-gray-500)` |
| `project.css:528` (선언은 529행) | `var(--color-gray-100)` | `var(--color-gray-500)` |

`tokens.css`에는 이 규칙이 없습니다.

**토큰 hex**
- `--color-gray-500` = `#697077`. 정의는 `tokens.css:10` 한 곳뿐입니다.
- `--color-gray-100` = `#e8ecf2`. `catalog.css:6`과 `project.css:10` 두 곳에 같은 값으로 정의되어 있습니다.

**대비 (WCAG 2.x 상대휘도)**
- 글자 L = 0.1592, 배경 L = 0.8356
- 대비 = (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1**로, 4.5:1에 못 미칩니다.

**고칠 때 주의할 점**
- **토큰 값을 바꾸면 안 됩니다.** `--color-gray-500`은 흰 면 위 `.lin--none`(`catalog.css:146`, 5.02:1로 합격)에도 쓰입니다. 두 규칙의 `color` 선언을 바꿔야 합니다.
- **`--color-gray-600`은 `project.css`에서 쓰면 안 됩니다.** 이 토큰(`#565c63`, 5.70:1)은 `catalog.css`의 `:root`에만 있습니다. `project.css` 주석에 따르면 그 화면은 `catalog.css`를 불러오지 않으므로, 쓰면 정의되지 않은 값이 됩니다.
- **대안은 `--color-text-muted`입니다.** 값이 같은 `#565c63`(5.70:1)이고 셸 토큰(`tokens.css:27`)이라 두 화면 모두에서 정의됩니다. 두 자리에 모두 이 토큰을 쓰면 됩니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528