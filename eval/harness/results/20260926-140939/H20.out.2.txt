대장 파일 `work-items-excerpt.yaml`을 먼저 고칩니다. 규칙은 CLAUDE-6.md 3~5행의 ⭑ 문단입니다. 번호 목록 1번이 HANDOFF를 먼저 적었지만 그것은 순서 규칙이 아닙니다.

**두 쪽이 시사하는 순서**
- 번호 목록(9행): 1번이 `03-HANDOFF.md §1` 상태 갱신입니다. 목록에 대장(`work-items.yaml`)은 아예 없어서, 번호 순서만 보면 HANDOFF를 먼저 고치는 것처럼 읽힙니다.
- ⭑ 문단(3행): "**상태 변경은 대장을 먼저 고친다.**" 순서를 직접 정한 문장이고, 4행에 "순서를 뒤집으면 산문이 다시 갈라진다"는 이유가 붙어 있습니다.

**⭑ 문단이 규칙인 이유**
- 번호 목록은 7행 "끝내기 전에 반드시:" 아래에 세션을 끝내기 전에 고칠 곳을 적어 둔 체크리스트입니다. 번호는 항목을 세는 용도일 뿐, 대장과 HANDOFF 사이의 순서를 정하지 않습니다. 대장이 목록에 없으니 둘의 순서를 정할 수도 없습니다.
- ⭑ 문단만 "먼저"라는 말로 순서를 명시하고 이유(4행)를 붙였습니다. 또 5행에서 판정 주체를 사람 기억이 아닌 게이트 `work-item-consistency`로 정합니다.
- 인계 문서도 같은 말을 합니다. `03-HANDOFF-excerpt.md:3`에 "**상태의 원본은 `work-items.yaml`**"이라고 적혀 있고, 표는 상태 표기와 포인터만 남긴 반영본이라고 밝힙니다.

**원본과 반영본**
- 원본: `work-items.yaml`(여기서는 `work-items-excerpt.yaml`). 1~3행의 `WU-D6` 항목에서 `status: open`을 `done`으로 바꿉니다.
- 반영본: `03-HANDOFF.md §1`(여기서는 `03-HANDOFF-excerpt.md`). 원본을 고친 뒤, 해당 WU 행의 상태 칸을 ✅(닫힘)로 맞춥니다. 게이트가 읽는 열은 `WU`와 `상태` 두 개입니다.

물음은 순서와 근거였으므로 파일은 고치지 않았습니다.

- 먼저 고치는 파일: work-items-excerpt.yaml
- 두 번째: 03-HANDOFF-excerpt.md
- 원본/반영본: work-items-excerpt.yaml / 03-HANDOFF-excerpt.md
- 근거: CLAUDE-6.md:3