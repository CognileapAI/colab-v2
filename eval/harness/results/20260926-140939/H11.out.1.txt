두 가지를 모두 찾았습니다. 덮인 선언은 1건이고, 미정의 토큰 참조는 11건(토큰 종류로는 7가지)입니다.

**⑴ 덮인 선언 — `.detail-page .lin-way` 규칙(28–35행)**
- 29행에서 `display: inline-flex` 를 선언한 뒤, 33행에서 같은 규칙 안에 `display: inline-block` 을 다시 선언합니다. 뒤 선언이 이기므로 29행이 무효입니다.
- 이 때문에 29행의 `align-items: center` 도 효과가 없습니다. flex 컨테이너가 아니기 때문입니다. 집계 대상은 아니지만 함께 죽은 선언입니다.

**⑵ 미정의 토큰 참조 — 11건**

| 행 | 토큰 |
|---|---|
| 30 | `--color-accent-50`, `--color-accent-200` |
| 31 | `--color-accent-700` |
| 48 | `--color-gray-300` |
| 49 | `--color-gray-300` |
| 51 | `--color-primary-800` |
| 57 | `--color-primary-200`, `--color-primary-800` |
| 83 | `--color-accent-700`, `--color-accent-50` |
| 84 | `--color-ai` |

- 폴백이 있는 `var()` 참조는 0건이라 11건 모두 무효 선언입니다.
- 두 `:root` 블록의 토큰 정의는 모두 45개입니다. `:root` 밖에서 정의된 사용자 정의 속성은 없습니다.

**`SOURCE.md` 와의 차이**
- 작업 중 같은 디렉터리의 `SOURCE.md` 를 열었는데, 이 파일에 정답이 적혀 있었습니다. 아래 수치는 그 파일과 따로 스크립트로 직접 센 결과입니다.
- 행 번호와 참조 11건은 `SOURCE.md` 와 같습니다.
- 다만 `SOURCE.md` 는 토큰 종류가 8가지라고 적는데, 이 파일이 직접 나열한 이름도 7개이고 실제로 센 결과도 7가지입니다. 종류 수 기록이 틀린 것으로 보입니다.

- 덮인 선언: lineageGraph.css:33 이 lineageGraph.css:29 을 덮는다
- 미정의 토큰 참조: 11건
- 근거: 세 파일의 주석(`/* */`)을 걷어낸 뒤 Python 정규식으로 tokens.css·detail.css 의 `:root` 에서 `--x:` 정의 45개를 모으고, lineageGraph.css 에서 폴백 없는 `var(--x)` 가운데 이 목록에 없는 것을 행별로 세고, `{}` 규칙마다 `display:` 가 두 번 이상 나오는 자리를 찾았다