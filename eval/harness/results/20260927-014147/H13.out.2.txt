「계보 없음」 배지는 흰 면 위에서 3.41:1 이라 합격선 4.5:1(AA)에 못 미칩니다.

**글자색 토큰**
- `catalog.css:135`: `.lin--none { color: var(--color-gray-400); font-weight: 500; }`
- 이 규칙에는 배경이 없어서 흰 면(`--color-surface` = `#ffffff`)이 그대로 비칩니다.
- `--color-gray-400` 는 `catalog.css` 에서 다시 정의하지 않습니다. 따라서 `tokens.css:9` 의 `#848c94` 가 그대로 적용됩니다.

**대비 계산 (WCAG 2.x 상대휘도)**
- 텍스트 크기는 `.lin` 기준 11px 이고 굵기는 500입니다. 큰 텍스트 예외(18pt, 또는 굵게 14pt)에 해당하지 않으므로 합격선은 4.5:1 입니다.
- `#848c94` 의 상대휘도 L 은 0.2580, `#ffffff` 는 1.0 입니다.
- (1.0 + 0.05) / (0.2580 + 0.05) = **3.41:1** 로 AA 미달입니다.

**고칠 후보**

| 토큰 | hex | 흰 면 대비 | 판정 |
|---|---|---|---|
| `--color-gray-400` (현재) | `#848c94` | 3.41:1 | 미달 |
| `--color-gray-500` | `#697077` | 5.02:1 | 통과 |
| `--color-gray-600` | `#565c63` | 6.76:1 | 통과 |

`--color-gray-500` 는 기준을 넘는 회색 중 가장 옅어서, 원래 의도인 '흐린 상태' 느낌을 가장 많이 남깁니다. 이미 `tokens.css:10` 에 있는 토큰이고 같은 파일의 `.verified--pending` 도 쓰고 있어 새 토큰을 만들 필요가 없습니다.

과제 지시대로 파일은 고치지 않았습니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 `color: var(--color-gray-400)`(#848c94)를 `var(--color-gray-500)`(#697077, 5.02:1)로 바꾼다