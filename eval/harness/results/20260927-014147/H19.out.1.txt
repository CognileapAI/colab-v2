승인 대기 배지는 대비 4.23:1로 AA 기준 4.5:1에 못 미칩니다. 같은 규칙이 두 파일에 있어 두 곳을 모두 고쳐야 합니다.

**규칙 위치 (디렉터리 전체 grep 결과)**
- `catalog.css:140`: `.verified--pending { background: var(--color-gray-100); color: var(--color-gray-500); … }`
- `project.css:528`: `.project-detail .verified--pending {`이고, 색 선언은 529행입니다. `catalog.css`를 싣지 않는 화면이라 규칙을 따로 다시 적어 둔 자리입니다(528행 앞 주석).

**토큰 hex**
- `--color-gray-500` = `#697077`: `tokens.css:10`에만 정의되어 있습니다.
- `--color-gray-100` = `#e8ecf2`: `catalog.css:6`과 `project.css:10` 두 곳에 같은 값으로 정의되어 있습니다.

**대비 계산 (WCAG 2.x 상대휘도)**
- 글자 `#697077`의 휘도는 0.1592, 배경 `#e8ecf2`는 0.8356입니다.
- 대비는 (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1**입니다.
- 배지 글자는 11px·600이라 큰 글자 예외(3:1)가 적용되지 않고, 4.5:1을 넘어야 합니다.

**고칠 때 참고**
- 두 규칙이 같은 두 토큰을 씁니다. 한 파일만 고치면 다른 화면의 배지는 그대로 4.23:1로 남습니다.
- `--color-gray-500` 토큰 값을 바꾸면 이 토큰을 쓰는 다른 자리도 함께 바뀝니다. 예를 들어 `catalog.css`의 46·61·124·146행입니다. 그래서 두 배지 규칙의 `color`를 더 진한 토큰으로 바꾸는 편이 범위가 좁습니다. `--color-text-muted`(`#565c63`)가 `#e8ecf2` 위에서 약 5.7:1이라 기준을 넘습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528