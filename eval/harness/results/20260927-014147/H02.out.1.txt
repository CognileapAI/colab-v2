**답: 아니다. 계측기는 주석 안의 값을 코드로 세지 않는다.** `<13px` 칸은 1이고, `Detail` 목록에는 코드 줄인 `sample.css:4`의 11px 하나만 나온다.

**눈으로 센 `sample.css`의 font-size**

| 줄 | 위치 | 값 | 13px 미만 | 계측기에 나옴 |
|---|---|---|---|---|
| 2 | 주석 | 9px | 예 | 아니오 |
| 4 | 코드 `.a` | 11px | 예 | 예 (`sample.css:4 font-size 11.0px`) |
| 6 | 주석 | 10px | 예 | 아니오 |
| 7 | 코드 `.b` | 14px | 아니오 | — |
| 10 | 코드 `.c` | 13px | 아니오 (기준이 13 미만) | — |

- 코드에 있는 13px 미만 값은 1건이고, 계측기 값 1과 같다.
- 주석에 있는 13px 미만 값은 2건(9px, 10px)이고, 둘 다 세지 않았다.
- 주석에는 색 값도 4개 있다(2줄의 `#848c94`·`#ffffff`, 9줄의 `#848c94`·`#ffffff`). 대비 칸 `contrast <4.5`가 0이므로 이 값들도 세지 않았다.

**주석을 빼는 방식**
- `css_audit.py:76-78`의 `strip_comments`가 `/* … */` 안의 글자를 공백으로 바꾸고 줄바꿈은 그대로 둔다. 그래서 주석은 사라지고 줄 번호는 맞게 남는다(`:4`와 일치).
- 이 처리는 파일을 검사하는 `audit_file`(`:82`)과 토큰 정의를 모으는 `collect_defs`(`:60`) 모두에 쓰인다.

- 판정: 주석분 0건
- 실측: 13px 미만 1건
- 주석 안에 있어 세지 않은 값: 2건
- 근거: `python3 "<repo>/eval/harness/H02-comment-false-positive/fixture/css_audit.py" --root "<repo>/eval/harness/H02-comment-false-positive/fixture"`