승인 대기 배지의 글자 대비는 4.23:1로, 합격선 4.5:1(AA)에 못 미칩니다. 같은 규칙이 두 파일에 있어서 두 곳 모두 고쳐야 합니다.

**규칙 위치 (디렉터리 전체 grep 결과)**
- `catalog.css:140`: `.verified--pending { background: var(--color-gray-100); color: var(--color-gray-500); … }`
- `project.css:528`: `.project-detail .verified--pending { … }`. 색 선언은 529행에 있습니다.
- 다른 파일에는 이 규칙이 없습니다.

**토큰 값**

| 토큰 | 값 | 정의 위치 |
|---|---|---|
| `--color-gray-500` (글자) | `#697077` | `tokens.css:10` (여기에만 있음) |
| `--color-gray-100` (배경) | `#e8ecf2` | `catalog.css:6` · `project.css:10` (두 곳 모두 같은 값) |

**대비 계산 (WCAG 2.x 상대휘도)**
- `#697077` on `#e8ecf2` = **4.232:1** → 미달입니다.
- 두 규칙은 토큰 조합이 같고, 토큰 값도 두 파일에서 같습니다. 그래서 두 곳 모두 4.23:1입니다.

**고칠 때 참고할 점**
- 토큰 값을 바꾸는 대신, 두 규칙의 `color`를 각각 바꾸는 것을 권합니다.
  - `--color-gray-500`을 어둡게 하면 이 토큰을 쓰는 다른 곳까지 바뀝니다. `catalog.css`에서만 46·61·124·146행이 이 토큰을 씁니다.
  - `--color-gray-100`은 두 파일이 각자 정의하고 있어서, 값을 바꾸면 두 정의를 모두 고쳐야 합니다.
- 바꿀 값으로는 `--color-text-muted` (`#565c63`, `tokens.css:27`)가 맞습니다. 같은 배경에서 5.70:1이 나옵니다.
- 이 fixture 안의 `SOURCE.md`에 따르면, 이 미달을 잡는 검사는 지금 어디에도 없습니다. `css_audit.py`의 대비 계산은 색이 리터럴 hex일 때만 돌고, 두 선언은 `var()` 참조라 계산에서 빠집니다. 두 곳을 고친 뒤 같은 모양이 다시 생기지 않게 하려면 검사를 따로 추가해야 합니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:528