계측기는 주석 안의 값을 코드로 세지 않습니다. `<13px` 칸은 1건이고, 그 1건은 코드에 있는 값입니다.

**계측기 출력**
- `<13px` 칸은 1이고 `contrast <4.5` 칸은 0입니다.
- `Detail` 목록에는 `sample.css:4 font-size 11.0px` 한 줄만 있습니다.

**`sample.css` 실물 대조**

| 줄 | 위치 | 값 | 13px 미만 여부 |
|---|---|---|---|
| 4 | 코드 `.a` | 11px | 해당. 계측기 값과 일치 |
| 7 | 코드 `.b` | 14px | 해당 없음 |
| 10 | 코드 `.c` | 13px | 해당 없음 (기준이 13px 이상) |
| 2 | 주석 | 9px, `#848c94`, `#ffffff` | 9px가 미만이지만 세지 않음 |
| 6 | 주석 | 10px | 미만이지만 세지 않음 |
| 9 | 주석 | `#848c94`, `#ffffff` | 색 값이므로 해당 없음 |

- **글자 크기:** 코드에서 13px 미만은 11px 하나뿐입니다. 주석에 있는 9px와 10px가 세어졌다면 칸이 3이 되었을 텐데, 실제 값은 1입니다.
- **색 대비:** 계측기와 별개로 직접 계산했습니다. 코드의 글자색은 흰 배경 대비 5.02, 18.19, 6.76:1로 모두 4.5:1 이상입니다. 주석에 있는 `#848c94`는 3.41:1이라서 세어졌다면 `contrast <4.5` 칸이 1 이상이어야 하는데 0입니다.
- **계측기 코드:** `css_audit.py:76`의 `strip_comments`가 `/* … */` 안의 글자를 공백으로 바꾼 뒤에 셉니다. 줄바꿈은 남겨서 줄 번호는 그대로 유지합니다.

**주석 안에 있어 세지 않은 값 6건**
- 글자 크기 2건: 9px, 10px. 둘 다 13px 미만입니다.
- 색 4건: `#848c94` 2건, `#ffffff` 2건.
- 9번 줄의 `3.41:1`은 대비 비율이지 CSS 값이 아니라서 넣지 않았습니다.

- 판정: 주석분 0건
- 실측: 13px 미만 1건
- 주석 안에 있어 세지 않은 값: 6건
- 근거: `python3 "<repo>/eval/harness/H02-comment-false-positive/fixture/css_audit.py" --root "<repo>/eval/harness/H02-comment-false-positive/fixture"`