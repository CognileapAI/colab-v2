두 가지 모두 찾았습니다. 둘 다 스크립트로 직접 계측한 결과입니다.

**⑴ 덮인 선언**
- `.detail-page .lin-way` 한 규칙 안에 `display` 가 두 번 선언돼 있습니다.
  - 29행 `display: inline-flex`
  - 33행 `display: inline-block`
- 뒤에 오는 33행이 이기므로 29행은 적용되지 않습니다. 같은 줄 앞쪽의 `align-items: center` 도 flex 가 아닌 inline-block 에서는 효과가 없습니다.

**⑵ 미정의 토큰 참조** (두 `:root` 에 정의된 토큰 45개와 대조, 폴백 있는 참조는 0건)

| 행 | 토큰 |
|---|---|
| 30 | `--color-accent-50`, `--color-accent-200` |
| 31 | `--color-accent-700` |
| 48 | `--color-gray-300` |
| 49 | `--color-gray-300` |
| 51 | `--color-primary-800` |
| 57 | `--color-primary-200`, `--color-primary-800` |
| 83 | `--color-accent-700`, `--color-accent-50` |
| 84 | `--color-ai` |

- 참조는 11건이고, 서로 다른 토큰은 7가지입니다.
- 주의할 점: 같은 디렉터리의 `SOURCE.md` 에는 가짓수가 8 로 적혀 있습니다. 표에 나온 토큰은 accent-50, accent-200, accent-700, gray-300, primary-200, primary-800, ai 이렇게 7개뿐입니다. 이번 과제는 참조 건수를 묻기 때문에 답(11건)에는 영향이 없지만, `SOURCE.md` 의 8 은 틀린 값입니다.
- `SOURCE.md` 에 정답이 이미 적혀 있었지만, 두 값은 그 파일과 따로 계측했습니다.

- 덮인 선언: lineageGraph.css:33 이 lineageGraph.css:29 를 덮는다
- 미정의 토큰 참조: 11건
- 근거: 세 파일에서 주석을 지운 뒤, `tokens.css`·`detail.css` 의 `:root` 에서 `--*:` 정의 45개를 모으고, `lineageGraph.css` 의 폴백 없는 `var(--…)` 중 정의에 없는 것을 줄 단위로 셌다. 같은 스크립트로 규칙 본문마다 `display:` 선언 수도 셌다.