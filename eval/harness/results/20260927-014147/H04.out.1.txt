v2 코드에는 이 지적이 해당하지 않습니다. Lv 칩 4개(`.lvl-0`~`.lvl-3`) 모두 4.5:1 이상이고, 가장 낮은 값은 4.66:1입니다.

| 칩 | 규칙 | 글자색 | 배경색 | 대비 |
|---|---|---|---|---|
| `.lvl-0` | `detail.css:51` | `--color-gray-600` `#565c63` (`detail.css:7`) | `--color-gray-100` `#e8ecf2` (`detail.css:6`) | 5.70:1 |
| `.lvl-1` | `detail.css:52` | `--color-primary-700` `#0f62e0` (`tokens.css:13`) | `--color-primary-50` `#edf4ff` (`detail.css:11`) | 4.94:1 |
| `.lvl-2`·`.lvl-3` | `detail.css:53` | `--color-primary-700` `#0f62e0` (`tokens.css:13`) | `--color-primary-100` `#e2eeff` (`detail.css:12`) | 4.66:1 |

- **기준:** 칩 글자가 11px·600(`detail.css:50`)이라 큰 텍스트가 아닙니다. 그래서 AA 기준 4.5:1을 적용했습니다.
- **계산:** 파이썬으로 WCAG 2.x 상대휘도(sRGB 선형화, 0.2126/0.7152/0.0722)를 적용해 직접 계산했습니다.
- **토큰:** 두 파일 어디에서도 이 토큰들을 다른 값으로 다시 정의하지 않습니다.
- **목업의 3.71:1:** 코드의 어느 Lv 칩 조합과도 맞지 않습니다. 목업과 v2 코드의 값이 다른 것으로 보입니다.
- **하네스 문제:** 같은 디렉터리의 `SOURCE.md`에 정답(4.66:1·「없음」)이 적혀 있습니다. 위 수치는 CSS 값으로 따로 계산했고 결과는 그 정답과 같습니다. 다만 이 과제를 평가에 쓴다면 `SOURCE.md`를 픽스처 디렉터리 밖으로 옮겨야 합니다. 지금은 답을 미리 볼 수 있는 상태입니다.

- 판정: 없음
- 실측: 4.66:1
- 근거: detail.css:53