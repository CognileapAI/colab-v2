네 단계 중 3단계는 색을 받지 못합니다. 0·1·2단계만 CSS 규칙이 있고 `.lvl-3` 규칙이 없습니다.

- **칩 클래스를 만드는 곳:** `CatalogTable.tsx:162`에서 `` className={`lvl lvl-${displayLevel(row)}`} ``로 만듭니다. `displayLevel`이 `null`이면 칩을 그리지 않습니다(161행).
- **대응 CSS 규칙:** `catalog.css`에서 칩에 적용되는 규칙은 아래 네 줄뿐입니다. 클래스 이름 일부로 거는 선택자나 미디어 쿼리 안에 둔 규칙은 없습니다.

| 단계 | 규칙 | 배경 | 글자색 |
|---|---|---|---|
| 모든 칩 공통 | `.lvl` (130행) | 없음 | 없음 (모양·글꼴만 정함) |
| 0 | `.lvl-0` (131행) | `--color-gray-100` | `--color-gray-600` |
| 1 | `.lvl-1` (132행) | `--color-primary-50` | `--color-primary-700` |
| 2 | `.lvl-2` (133행) | `--color-primary-100` | `--color-primary-700` |
| 3 | 없음 | — | — |

- **3단계 칩이 그려지는 모습:** 공통 `.lvl` 규칙에는 배경과 글자색이 없습니다. 그래서 3단계 칩은 둥근 모양·안쪽 여백·글꼴만 적용되고, 배경은 투명하며, 글자는 표 칸의 기본색을 그대로 씁니다.
- **확인하지 못한 부분:** `displayLevel`은 `../common/processingLevel`에서 가져오는데 이 폴더에는 그 파일이 없습니다. 값이 0~3이라는 점은 주신 설명을 따랐습니다.
- **참고:** 폴더의 `SOURCE.md`에는 이 사본을 만들 때 `.lvl-3` 규칙과 그 주석을 일부러 지웠다고 적혀 있습니다. 위 판정은 두 파일을 직접 검색해서 나온 결과와 같습니다.

- 판정: 있음
- 빠진 규칙: .lvl-3
- 증상: 가공 단계 3인 행의 "Lv3" 칩만 배경이 비어 있고 글자는 기본색이라, 색이 들어간 Lv0~Lv2 칩과 달리 테두리 없는 글자로만 보인다.