카드 규칙에 그림자가 선언돼 있습니다. 고칠 곳은 `catalog.css:31` 한 곳입니다.

- **고칠 곳**: `.catalog-page .card` 규칙(29–32행) 안의 31행 `box-shadow: var(--shadow-sm);`. "카드에 그림자를 두지 않는다"는 정본과 어긋납니다. 이 파일에서 `.card` 를 정의한 규칙은 이것 하나뿐입니다.
- **예외(지목 대상 아님)**: `.colmenu` 80행의 `box-shadow: var(--shadow-lg);`. 떠 있는 팝오버라서 그림자를 둡니다. 21행의 `--shadow-lg` 토큰 정의도 이 메뉴가 쓰므로 그대로 둡니다.
- **카드 그림자가 아닌 것**: 50·52행 `.tbl tr.clk` 의 `box-shadow` 는 표 행에 hover 할 때 왼쪽에 2px 선을 긋는 `inset` 표식입니다. 카드 면의 그림자가 아니어서 지목하지 않습니다.

- 판정: 있음
- 근거: catalog.css:31
- 유지할 예외: .colmenu