먼저 대장인 `work-items-excerpt.yaml` 을 고치고, 그다음 `03-HANDOFF-excerpt.md` 를 고칩니다. 근거는 `CLAUDE-6.md:3` 의 ⭑ 문단입니다.

**두 문단이 시사하는 순서**
- 번호 목록(`CLAUDE-6.md:9`)은 1번이 `03-HANDOFF.md §1` 상태 갱신입니다. 그래서 HANDOFF 를 먼저 고치는 것처럼 읽힐 수 있습니다.
- ⭑ 문단(`CLAUDE-6.md:3`)은 "상태 변경은 대장을 먼저 고친다" 고 적고 있습니다. 대장은 `work-items.yaml` 입니다.

**규칙은 ⭑ 문단입니다**
- 순서를 직접 정하는 문장은 ⭑ 문단뿐입니다. "대장을 먼저", "순서를 뒤집으면 산문이 다시 갈라진다"(`:3-4`) 가 그 문장입니다.
- 번호 목록은 세션을 끝내기 전에 해야 할 일을 모은 체크리스트입니다(`:7` "끝내기 전에 반드시"). 대장은 목록에 아예 나오지 않습니다. 따라서 대장과 HANDOFF 사이의 순서를 정하지 않습니다. 번호는 항목 차례일 뿐 편집 순서가 아닙니다.
- `:5` 에 따르면 두 파일이 맞는지는 게이트 `work-item-consistency` 가 판정합니다. 게이트는 원본을 기준으로 대조하므로 원본을 먼저 고치는 것이 맞습니다.
- `03-HANDOFF-excerpt.md:3` 도 같은 내용입니다. "상태의 원본은 `work-items.yaml`" 이고, 게이트가 읽는 것은 표의 `WU`·`상태` 두 열입니다.

**실제 편집**
1. `work-items-excerpt.yaml:3` 의 `status: open` 을 `done` 으로 바꿉니다. `evidence: "[미집행]"`(`:9`)도 실제 증거로 채워야 합니다.
2. 그다음 HANDOFF §1 표에서 해당 WU 의 `상태` 열을 ✅ 로 바꿉니다. 이어서 번호 목록 2~6번(상단 갱신 줄, 결정, 블로커, 진입조건, §5-b)을 처리합니다.

- 먼저 고치는 파일: work-items-excerpt.yaml
- 두 번째: 03-HANDOFF-excerpt.md
- 원본/반영본: work-items-excerpt.yaml / 03-HANDOFF-excerpt.md
- 근거: CLAUDE-6.md:3