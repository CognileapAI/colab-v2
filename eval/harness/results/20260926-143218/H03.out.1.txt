`VENDORED-excerpt.md` 의 표에 빈 줄이 하나 있습니다. 13행이 길이 0인 빈 줄이고, 이 때문에 뒤의 두 행이 표에서 빠집니다.

- **9~12행이 표입니다.** 9행이 헤더, 10행이 구분선, 11행이 `superpowers`, 12행이 `mattpocock/skills` 입니다.
- **13행에서 표가 끝납니다.** 공백 문자도 없는 완전한 빈 줄입니다.
- **14~15행은 표 밖 문단이 됩니다.** 14행은 `emilkowalski/skills`, 15행은 `vercel-labs/agent-browser` 입니다. 앞에 헤더와 구분선이 없어 새 표로도 인식되지 않고 파이프가 든 일반 텍스트로 렌더링됩니다.
- **파일은 15행에서 끝납니다.** 그래서 표 밖으로 떨어진 행은 2개입니다.

아래 확인 명령은 9행부터 파일 끝까지 `|` 로 시작하지 않는 줄을 출력합니다. 고친 뒤에는 아무것도 출력되지 않아야 합니다. 지금 파일에 돌리면 빈 13행이 걸려 `13:` 이 출력됩니다.

- 판정: 있음
- 빈 줄: 13
- 표 밖으로 떨어진 행: 2건
- 확인 명령: `awk 'NR>=9 && !/^\|/ {print NR": "$0}' VENDORED-excerpt.md`