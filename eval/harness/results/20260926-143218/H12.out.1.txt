네 단계 중 3단계만 색을 받지 못합니다.

- **칩 클래스를 만드는 곳:** `CatalogTable.tsx:162` 의 `` className={`lvl lvl-${displayLevel(row)}`} `` 입니다. 0·1·2·3 에 대해 각각 `lvl-0`~`lvl-3` 이 붙습니다. `displayLevel(row)` 가 `null` 이면 칩을 그리지 않습니다(161행).
- **`catalog.css` 에서 `lvl` 이 들어간 규칙 전체:**

| 선택자 | 행 | background | color |
|---|---|---|---|
| `.lvl` | 130 | 없음 | 없음 |
| `.lvl-0` | 131 | `--color-gray-100` | `--color-gray-600` |
| `.lvl-1` | 132 | `--color-primary-50` | `--color-primary-700` |
| `.lvl-2` | 133 | `--color-primary-100` | `--color-primary-700` |
| `.lvl-3` | 없음 | — | — |

`.lvl` 은 크기, 둥근 모서리, 여백, 글꼴만 정합니다. 배경과 글자색은 없습니다. `lvl` 이 들어간 선택자는 이 네 줄이 전부이고, 속성 선택자나 부모 선택자로 3단계를 따로 칠하는 규칙도 없습니다. 그래서 3단계 칩은 모양은 갖추지만 배경이 없고 글자색은 부모에게서 물려받습니다.

- 판정: 있음
- 빠진 규칙: `.lvl-3`
- 증상: 3단계 행의 "Lv3" 칩만 배경색 없이 주변 글자색으로 그려져, 색이 있는 Lv0~Lv2 칩과 달리 칩으로 보이지 않고 글자만 떠 있다.