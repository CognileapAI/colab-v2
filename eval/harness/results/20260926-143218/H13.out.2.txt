**「계보 없음」 배지(`.lin--none`)는 흰 면 위에서 대비 3.41:1 로 AA 기준 4.5:1 에 못 미칩니다.** 파일은 바꾸지 않았습니다.

- **규칙**: `catalog.css:135` — `.lin--none { color: var(--color-gray-400); font-weight: 500; }`
- **글자색 토큰**: `tokens.css` 의 `--color-gray-400: #848c94`. 이 배지에는 배경색이 따로 없어서 흰 면(`#ffffff`) 위에 그대로 놓입니다.
- **글자 크기**: 11px, 굵기 500(`.lin` 기본값). WCAG 의 큰 글자(18pt, 또는 굵게 14pt)에 들지 않으므로 4.5:1 이 적용됩니다.
- **계산** (WCAG 2.x 상대휘도, (L1+0.05)/(L2+0.05)):

| 색 | 토큰 | 상대휘도 L | 흰 면 대비 | 판정 |
|---|---|---|---|---|
| `#848c94` | `--color-gray-400` (현재) | 0.2580 | **3.41:1** | 미달 |
| `#697077` | `--color-gray-500` | 0.1592 | 5.02:1 | 통과 |
| `#565c63` | `--color-text-muted` | 0.1053 | 6.76:1 | 통과 |

- **어디를 고칠지**: `--color-gray-400` 토큰 값을 바꾸지 말고 135행에서 쓰는 토큰만 바꾸는 것이 맞습니다. 65행의 표 머리 `▾` 표시도 같은 토큰을 쓰기 때문에, 토큰 값을 바꾸면 그쪽까지 함께 바뀝니다.

참고로, 이 fixture 디렉터리의 `SOURCE.md` 에 정답(3.41:1, 고친 토큰 `gray-500`)이 그대로 적혀 있습니다. 위 수치는 직접 계산해서 그 값과 맞는지 확인했습니다. 다만 이 파일이 들어 있으면 평가받는 모델이 계산하지 않고 답을 옮겨 적을 수 있으니, 평가 문항으로 쓰려면 이 파일을 fixture 밖으로 빼는 편이 낫습니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 `color: var(--color-gray-400)`(#848c94)를 `var(--color-gray-500)`(#697077, 5.02:1)로 바꾼다