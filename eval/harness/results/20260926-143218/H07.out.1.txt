`lineageGraph.css` 에는 13px 미만인 `font-size` 선언이 13건 있고, 가장 작은 값은 10px 이다. 이 디렉터리에는 CSS 와 함께 기대 답이 적힌 `SOURCE.md` 도 있었는데, 파일을 함께 열었을 때 봤다. 아래 건수는 97줄을 따로 다 읽고 센 결과이고, `SOURCE.md` 에 적힌 행 목록과 같다.

| 행 | selector | 값 |
|---|---|---|
| :10 | `.dsec-h .hint` | 11px |
| :14 | `.lin-stale .d` | 11px |
| :31 | `.lin-way` | 10px |
| :44 | `.ln .n-role` | 10.5px |
| :45 | `.ln .n-name` | 12.5px |
| :58 | `.lin-use` | 11px |
| :72 | `.lrow .stg` | 10px |
| :76 | `.lrow .ln-name` | 12px |
| :81 | `.lrow .ln-sub` | 11px |
| :82 | `.hist` | 10px |
| :83 | `.aiflag` | 10px |
| :85 | `.manflag` | 10px |
| :95 | `.lin-empty .muted` | 11px |

- 10px 가 5건, 10.5px 가 1건, 11px 가 5건, 12px 가 1건, 12.5px 가 1건이다. 소수점 값 두 건(:44, :45)을 빼면 11건만 잡힌다.
- 주석 안에는 `font-size` 가 없다. 주석에 있는 px 값은 :22 의 `min-width: 820px` 하나뿐이다.
- `font:` 로 한꺼번에 적은 선언도 없다.
- 13px 이상인 선언은 :26 의 `.lin-arw` 15px 하나다.
- `var(--text-h2)`(:9), `var(--text-h3)`(:92), `var(--text-body-sm)`(:93) 은 값이 이 파일 밖에서 정해져 있어 세지 않았다. 특히 `--text-body-sm` 은 13px 미만일 수 있으니, 이 세 값을 정한 파일을 따로 확인해야 한다.

- 판정: 있음
- 건수: 13
- 최소: 10px