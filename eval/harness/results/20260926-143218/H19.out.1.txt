승인 대기 배지는 4.5:1 기준에 못 미칩니다. 대비는 4.23:1이고, 같은 규칙이 두 파일에 있어 두 곳을 모두 고쳐야 합니다.

**`.verified--pending` 규칙 위치** (이 디렉터리의 `.css` 3개와 `SOURCE.md` 전체 검색)

| 파일:행 | 선택자 | 글자색 | 배경색 |
|---|---|---|---|
| `catalog.css:140` | `.verified--pending` | `var(--color-gray-500)` | `var(--color-gray-100)` |
| `project.css:529` (규칙은 528행에서 시작) | `.project-detail .verified--pending` | `var(--color-gray-500)` | `var(--color-gray-100)` |

- `tokens.css`에는 `.verified--pending` 규칙이 없습니다.

**토큰 값**
- `--color-gray-500`는 `tokens.css:10`에서만 정의됩니다: `#697077`. 다른 파일에서 덮어쓰지 않습니다.
- `--color-gray-100`은 `catalog.css:6`과 `project.css:10`에 따로 정의되어 있고, 값은 둘 다 `#e8ecf2`입니다. 따라서 두 화면의 대비는 같습니다.

**대비 계산 (WCAG 2.x 상대휘도)**
- `#697077`의 휘도는 0.1592, `#e8ecf2`의 휘도는 0.8356입니다.
- (0.8356 + 0.05) / (0.1592 + 0.05) = **4.23:1**이므로 AA 4.5:1에 못 미칩니다.
- `SOURCE.md:12`에 적힌 4.23:1과도 같습니다.

**고치는 방법**
- 두 곳 모두 글자색을 `var(--color-text-muted)`로 바꾸면 됩니다.
  - 이 값은 `tokens.css:27`의 `#565c63`이고, 같은 배경에서 5.70:1로 기준을 넘습니다.
- `catalog.css:131`의 `.lvl-0`이 이미 같은 색 조합(`#565c63` 글자, `gray-100` 배경)을 쓰고 있습니다.
- `project.css`에서는 `--color-gray-600`을 쓰면 안 됩니다. 이 토큰은 `catalog.css:8`에만 있는데, 프로젝트 화면은 `catalog.css`를 불러오지 않습니다(`project.css:520-522` 주석). 그래서 두 화면 모두에 적용되는 `--color-text-muted`를 권합니다.
- `tokens.css:10`의 토큰 값 자체를 바꾸는 것은 권하지 않습니다. `gray-500`은 `catalog.css:46`, `:61`, `:124`, `:146`에서도 쓰여서 영향이 이 배지 밖으로 번집니다.
- 파일은 수정하지 않았습니다. 요청은 위치를 찾는 것까지였습니다.

- 판정: 있음
- 실측: 4.23:1
- 고칠 자리: 2곳
- 근거: catalog.css:140 · project.css:529