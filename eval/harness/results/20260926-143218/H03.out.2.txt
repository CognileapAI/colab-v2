`VENDORED-excerpt.md` 의 표에 빈 줄이 하나 끼어 있고, 그 때문에 마지막 두 행이 표 밖으로 떨어졌습니다. `cat -A` 로 파일을 줄 번호와 함께 읽고, 공백·탭만 있는 줄이 섞여 있지 않은지도 함께 봤습니다.

**표 구조 (9–15행)**

| 행 | 내용 | 표 소속 |
|---|---|---|
| 9 | 머리글 `\| 출처 \| URL \| 라이선스 \| 판본 \| 내려받은 날 \|` | 표 |
| 10 | 구분자 `\|---\|---\|---\|---\|---\|` | 표 |
| 11 | superpowers | 표 |
| 12 | mattpocock/skills | 표 (여기서 표가 끝남) |
| 13 | 빈 줄. `cat -A` 로 `$` 만 찍히고 공백이나 탭은 없음 | 표를 끊음 |
| 14 | emilkowalski/skills | 표 밖의 본문 문단 |
| 15 | vercel-labs/agent-browser | 표 밖의 본문 문단 (파일 마지막 줄) |

- 14–15행은 `|` 로 시작하지만 새 표가 되지 못합니다. 14행을 머리글로 치더라도 바로 다음 15행이 구분자 행(`|---|…`)이 아니기 때문입니다. 그래서 두 줄은 한 문단으로 합쳐져 원문 그대로 보이게 됩니다.
- 2·6·8행도 빈 줄이지만 모두 표가 시작되기 전(9행 앞)에 있어 이 문제와 관계없습니다.
- 13행을 지우면 표는 9–14행이 되고 파일은 14행에서 끝납니다. 확인 명령은 이 상태를 기준으로 합니다. 출력이 없으면 정상입니다.

- 판정: 있음
- 빈 줄: 13
- 표 밖으로 떨어진 행: 2건
- 확인 명령: `awk 'NR>=9 && !/^\|/ {print NR": "$0}' VENDORED-excerpt.md`