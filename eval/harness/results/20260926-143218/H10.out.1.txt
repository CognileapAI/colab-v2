두 파일의 `margin` 선언을 모두 확인했습니다. 음수 값을 쓴 margin 선언은 2건이고, 둘 다 주석 밖의 실제 코드입니다.

| 위치 | 선언 | 규칙 |
|---|---|---|
| `detail.css:93` | `margin: -12px 0 var(--space-6);` | `.detail-page .filelist`: 위쪽 음수로 앞의 infogrid에 붙입니다. 같은 규칙에 `border-top: none`이 함께 있습니다. |
| `shell.css:58` | `margin-left: -7px;` | `.detail-page .backlink` / `.project-detail .backlink`: `padding-left: 7px`만큼 음수로 되돌립니다. |

**빼고 센 것:**
- 음수 값이지만 margin이 아닌 것: `letter-spacing`(`detail.css:33` · `shell.css:115`), `bottom: -11px`(`shell.css:188`).
- 주석 안에 음수 margin 값은 없습니다. 두 건 모두 앞선 주석(`detail.css:84–87`, `shell.css:36–42`)이 끝난 뒤의 선언입니다.
- ⑵ 자식이 `margin-top`으로 제 여백을 지는 경우(예: `detail.css:36`·`37`·`43`)는 이번 집계에 넣지 않았습니다.

- 판정: 있음
- 음수 상쇄: 2건
- 근거: detail.css:93 · shell.css:58