두 파일에서 음수 margin 선언은 2건입니다. 둘 다 주석 밖의 실제 코드 줄입니다.

| 위치 | 선언 | 내용 |
|---|---|---|
| `detail.css:93` | `margin: -12px 0 var(--space-6)` | `.filelist` 를 위로 12px 당겨 바로 앞 요소 `.infogrid` 에 붙입니다. 그래서 `border-top: none` 과 아래쪽만 둥근 모서리를 함께 씁니다. ⑴ 모양 그대로입니다. |
| `shell.css:58` | `margin-left: -7px` | `.backlink` 가 자기 `padding-left: 7px` 를 음수로 되돌려, 글자 시작점을 줄의 가장자리에 맞춥니다. |

- **`shell.css:58` 의 차이**: 앞 요소에 붙이는 것이 아니라 자기 안쪽 여백을 가로로 되돌리는 경우입니다. 그래도 자식이 음수로 제 위치를 조정한다는 점은 같아서 건수에 넣었습니다.
- **빼고 센 것**: 음수 값이지만 margin 이 아닌 선언 3건은 셈에 넣지 않았습니다. `detail.css:33` 과 `shell.css:115` 의 `letter-spacing`, `shell.css:188` 의 `bottom: -11px` 입니다.
- **주석 확인**: 두 선언 모두 바로 앞 주석 블록(`detail.css:84–87`, `shell.css:36–42`)이 끝난 뒤에 있습니다. 주석 안에서는 margin 값이 나오지 않습니다.
- **`SOURCE.md` 와의 관계**: 이 디렉터리의 `SOURCE.md` 에 정답(2건, 같은 두 행)이 적혀 있습니다. 위 결과는 그 파일에 기대지 않고 두 CSS 파일을 직접 grep 하고 행 주변을 읽어 얻었고, 결과가 일치합니다.

- 판정: 있음
- 음수 상쇄: 2건
- 근거: detail.css:93 · shell.css:58