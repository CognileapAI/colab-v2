「계보 없음」 배지의 글자 대비가 기준(4.5:1)에 못 미칩니다. 흰 면 위에서 3.41:1입니다.

**글자색**
- `catalog.css:135`: `.lin--none { color: var(--color-gray-400); font-weight: 500; }`
- `tokens.css`에서 `--color-gray-400`의 값은 `#848c94`입니다.
- 배경은 흰 면(`#ffffff`) 그대로입니다. `.lin`(`catalog.css:132`)과 `.lin--none` 모두 `background`를 지정하지 않습니다.

**대비 계산 (WCAG 2.x 상대휘도)**

| 색 | 상대휘도 L | 흰 면 대비 (1.05 / (L + 0.05)) |
|---|---|---|
| `#848c94` (gray-400, 현재) | 0.2580 | **3.41:1** |
| `#697077` (gray-500, 대안) | 0.1592 | 5.02:1 |

- 흰 면의 상대휘도는 1.0이므로 대비는 1.05 / (L + 0.05)입니다.
- 글자는 11px·굵기 500이어서 WCAG의 큰 글자 예외(3:1)에 해당하지 않습니다. 그래서 기준은 4.5:1이고, 3.41:1은 미달입니다.
- 이미 있는 토큰 `--color-gray-500`으로 바꾸면 5.02:1로 통과합니다. 새 토큰을 만들 필요는 없습니다.

파일은 고치지 않았습니다.

- 판정: 있음
- 실측: 3.41:1
- 근거: catalog.css:135
- 제안: `.lin--none` 의 `color: var(--color-gray-400)`(#848c94)를 `var(--color-gray-500)`(#697077, 5.02:1)로 바꾼다.