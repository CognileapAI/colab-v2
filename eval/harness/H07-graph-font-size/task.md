디자인 정본의 접근성 합격선은 **「글자 13px 이상」** 이다.

이 디렉터리에 계보 그래프의 `lineageGraph.css` 사본이 있다.

- 이 파일 안의 `font-size` 선언 중 **13px 미만인 것을 전수로** 센다.
- 소수점 값(`10.5px`·`12.5px`)도 센다.
- 가장 작은 값이 얼마인지 적는다.
- 주석 안의 값은 코드가 아니다.

답 끝에 다음 세 줄을 이 형식 그대로 적는다.

- 판정: 있음 | 없음
- 건수: <N>
- 최소: <N>px
