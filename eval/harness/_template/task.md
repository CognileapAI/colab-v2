⛔ 아래 두 줄은 새 과제를 만들 때 지운다. 이 파일의 **모든 글자가 프롬프트로 전달된다** —
   설명·형식 규칙을 여기 남기면 그것까지 과제의 일부가 된다. 규칙은 `eval/harness/README.md` 에 있다.

<여기부터 판정받는 에이전트가 받는 지시문. 로스터 표의 「과제 한 줄」을 지시문으로 편다.>

이 디렉터리에 있는 파일만 근거로 삼는다.

- <무엇을 판정하라는 요구 1>
- <무엇을 판정하라는 요구 2>

답에 다음을 그대로 적는다.

- 판정: 있음 | 없음
- 근거: <파일명>:<행> 한 줄
- 실측값: <숫자나 건수>
