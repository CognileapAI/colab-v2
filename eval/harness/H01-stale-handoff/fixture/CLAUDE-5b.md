## 5-b. Claude 가 반복해서 틀리는 것 (2회째면 여기에 적는다)

증상 → 예방. **같은 실수가 두 번 났으면 한 줄로 추가한다.**

- **낡은 HANDOFF·메모리를 최신 값으로 믿고 「다음 단계」를 오판** → 착수 전 `git log --oneline -8` 로 최근 회차를 먼저 본다 (2026-09-08 · R-C 마감 뒤 「다음 = R-C intent」라 적음)
- **정적 계측기가 CSS 주석을 코드로 읽어 오탐 3건** → 계측기는 주석을 제거한 뒤 잰다. 표본 실물 대조 없이 수치를 채택하지 않는다 (2026-09-08 · `css_audit.py`)
- **마크다운 표 사이에 빈 줄이 끼어 행이 표 밖으로 떨어짐** → 표를 고친 뒤 `sed -n` 으로 행이 붙어 있는지 본다 (2026-09-08 · `VENDORED.md` 2회)
- **아래 셋은 이미 규칙에 있다 — 여기서 중복하지 않고 그 자리를 본다**:
  「지시가 실물과 어긋나면 멈춘다」·「행 번호 대신 앵커」·「미병합 값을 사실로 적지 않는다」
  → `.claude/skills/colab-v2-work/SKILL.md §1` · `.claude/rules/colab-rules.md §1`
