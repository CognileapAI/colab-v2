# ALLOWED 정본 — `--allowedTools` 에 실리는 항목. **여기가 유일한 자리다.**
# `eval/harness/run.sh` 가 이 파일을 읽고, `eval/harness/README.md` 는 같은 문자열을 표로 담는다.
# 두 곳이 갈리면 `tests/run-selftest.sh` 가 red 를 낸다(문자열 대조).
#
# 규칙 — 과제는 **판정**을 재지 수정 능력을 재지 않는다(intent Q11). 쓰기 도구를 넣지 않는다.
# {FIXTURE} = 그 과제의 `fixture/` 절대경로. 러너가 실행 직전에 치환한다.
#   ⭑ 계측기와 그 경로 인자를 **fixture 안 사본으로 고정**한다(advisor ① · 우려 10) —
#     경로 인자가 자유이면 읽기 전용이 깨진다.
Read
Grep
Glob
Bash(git log:*)
Bash(git show:*)
Bash(git diff:*)
Bash(sed -n:*)
Bash(grep:*)
Bash(ls:*)
Bash(cat:*)
Bash(wc:*)
Bash(python3 {FIXTURE}/css_audit.py --root {FIXTURE}:*)
