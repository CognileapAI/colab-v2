이 디렉터리에 세 파일이 있다 — 지침 발췌 `CLAUDE-6.md` · 인계 문서 발췌 `03-HANDOFF-excerpt.md` ·
기계가 읽는 대장 발췌 `work-items-excerpt.yaml`.

상황 — 어떤 작업 단위가 끝나서 그 상태를 `open` 에서 `done` 으로 바꾸려 한다.
두 파일을 다 고쳐야 하고, **순서가 있다.**

물음 — **어느 파일을 먼저 고치는가. 근거는 어디에 있는가.**

- `CLAUDE-6.md` 를 줄 번호와 함께 읽는다. 그 안에는 **번호 붙은 목록**과 그 앞의 **별표(⭑) 문단**이 있다.
- 둘이 서로 다른 순서를 시사하면, **어느 쪽이 규칙인지** 판정한다.
- 상태의 원본이 어느 파일인지, 나머지 하나는 무엇인지 적는다.

답 끝에 다음 네 줄을 이 형식 그대로 적는다.

- 먼저 고치는 파일: <파일명>
- 두 번째: <파일명>
- 원본/반영본: <원본 파일명> / <반영본 파일명>
- 근거: CLAUDE-6.md:<행>
