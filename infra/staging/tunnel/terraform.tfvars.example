# 이 파일을 terraform.tfvars 로 복사하지 않는다. 실제 값은 홈 0600 파일에서
# TF_VAR_* 환경변수로 주입한다 (README 참고). 이 예시는 변수 모양만 보여준다.

cloudflare_api_token  = "<API_TOKEN>"
cloudflare_account_id = "<ACCOUNT_ID>"
tunnel_id             = "<TUNNEL_ID>"
