# Intent: R-C — 미리보기 5건(축 ①) ＋ R-B 후속 19건(축 ②) · 계약 동결 해제 21차 후보

> ⛔ **초안 · 승인 미승인 · 발의 Ted 2026-09-08 · grill-me 2026-09-08 완료(프론티어 공집합).**
> 초안까지가 에이전트의 역할이다. 교정·커밋은 Ted 가 한다(커밋이 곧 승인). 이 파일을 승인된 것처럼 인용하지 않는다.

메타 — 발의자: Ted(축 ① 구두 발의 5건) ＋ agent(축 ② R-B 후속 묶음) · 작성 2026-09-08 · 승인 **미승인**

- 축 ① 원본 intent: `dev-package/intent/2026-09-08-preview-slot.md`(측정·설계 트리·16 판정 축자 — 여기서 재측정하지 않는다).
- 축 ② 원본: `dev-package/sessions/R-B-ROUND-20260908.md` §5(판정 A 확정값) · §6(후속 목록).
- 묶음 방식 = **R-C 한 intent · 두 축 · 21차 한 패키지**(Ted 2026-09-08 · `## 확인`).

## 문제

**축 ① 미리보기 5건**(요약 · 축자는 원본 intent)
- 미리보기 자리가 그리기 완료 뒤에 생겨 레이아웃이 뛴다(`.mapstage` 고정 크기 0 · 상세는 컨테이너만 선다).
- 데이터셋 상세가 세로 단일 컬럼이라 이미지 좌·정보 우 배치가 아니다(`detail.css:27`).
- 500MB 초과는 `TOO_LARGE_MESSAGE` 로 거절되고 복구 경로(`RenderTarget.fileIds`)를 화면이 쓰지 않는다.
- 그릴 파일·변수·시각을 고르는 UI 가 없다 — 서버는 `fileIds`·`instant` 를 받을 준비가 돼 있는데 FE 가 아무것도 싣지 않는다.
- 축척 기준이 없다(컨테이너 폭 = 배율) · 배경 지도 0 · 확대/축소는 상세에만.

**축 ② R-B 후속 19건**(`R-B-ROUND-20260908.md` §6 축자 · 괄호 = §5 질의 번호)
- 게이트 승격 3종 묶음: `db/platform/tests/*-drift.sh` 오라클 · 적용 DB `alembic upgrade head` 준비 단계 · `alembic` requirements 등재(질의 3·4·17·30).
- `LabInfoPanel` VISIBILITIES 3값(16) · 홈 데이터 맵 `LabDataMap.byCategory`(33 · 21차 후보) · `UploadModal.submit` 400 표면화(31) · `replace_variables` statement-level 트리거(36) · 변수명 UNIQUE(11) · 프로젝트 이름 UNIQUE(R-A′ 이월).
- Lv 표시 규칙 통일(27·41) · `.lvl-3` CSS(45) · `.lin--none` 대비(46) · `.dt-gridact` 음수(47) · 여백 컨테이너 이관 잔여 9곳＋`.dsec`(49) · 코랄 액센트 복원(48).
- 사후 「기록 없음」 선언 · 기존 엣지 `method` 수정 op(42 · 계약 필요) · AI 제안에 부모 Lv(23) · `ProjectDatasetRow` 사람 값(24).
- 배포 창: 마이그레이션 5건 실적용 전 staging/prod 실측 — 0019 백필 손실(35) · 판정식 전환 행(40) · 공개 범위 매핑(20).
- 판정 상태: §5 61건 전부 2026-09-08 「권고대로!」 확정(`intent/2026-09-07-r-b.md ## 확인` A·C·D). §5 에서 「R-C」 를 명시한 판정 = 9·14·19·20·28·41·43·48. **여기서 다시 열지 않는다.**

## 원한 결과 (proposed outcome)

**축 ①** — 원본 intent `## 원한 결과` 1~10 을 그대로 승계(자리 선점 4상태 무점프 · 진행 3단계 · 못그림 문면 · 완성 삽입 · 상세 좌우 · 500MB 도달 · 파일·변수·시각 선택 · 축척 기준 · 세 화면 확대/축소 · 배경 지도). 판정으로 좁혀진 값:
1. 틀 = `aspect-ratio` 4:3 · 업로드 장면2 즉시 · 상세 열자마자.
2. 상세 = 좌 미리보기(sticky) · 우 기본 정보＋파일 · 계보·활용 아래 전폭 · 960px 미만 한 열(미리보기 먼저).
3. 500MB = 상한 유지 · 초과 시 첫 renderable 조각 자동 재요청(`fileIds`) · 선택 UI 로 다른 조각.
4. 선택 = 파일(기존 files 조회)·변수·시각(기존 `instant`) · 변수＋시각 목록 「대상 기술」 조회 1건 신설(21차) · 기본값은 서버 선택값을 화면에 표시.
5. 축척 = 데이터 `bounds` 중심 ＋ 표준 축척 사다리 스냅 · 세 화면 공유 `useZoomPan` · 작은 유역은 외곽선＋더블클릭 맞춤.
6. 배경 = 자립형 Natural Earth 1:110m 해안선＋국경 · 외부 요청 0 · 500KB 상한.

**축 ②** — 4묶음
- **(G) 게이트 승격 3종**(3·4·17·30): `*-drift.sh` 오라클 7벌을 게이트로 · 적용 DB `alembic upgrade head` 를 게이트 준비 단계로 · `alembic` 을 requirements 에. 셋을 한 WU.
- **(S) 서버·FE 소형**: `LabInfoPanel` VISIBILITIES 3값(16) · `UploadModal.submit` 400 ↔ 그 외 갈라 노출(31) · `replace_variables` statement-level 트리거(36) · 프로젝트 이름 UNIQUE(R-A′ 이월 C-2) · 기간 인라인 칸 철거(14) · `ordinal` 0행 읽기 퇴행 제거(9) · 공개 범위 내림 소유자 한정(19) · 이관 빈틈 보정 마이그레이션(20 · 배포 전 실측 선행) · Lv0 출처 안내 사람 Lv 기준(28) · 빈 상태 3문면 권한 무관(43).
- **(L) Lv 표시·CSS**: Lv 표시 사람 값 우선 통일(27·41 · 계약 21차) · `.lvl-3` 4단 색 = Lv2 보다 한 단 진한 같은 계열(45) · `.lin--none` 대비 AA(46) · `.dt-gridact` 음수 상쇄 제거(47) · 여백 컨테이너 이관 잔여 9곳＋`.dsec`(49) · 코랄 액센트 복원(48 · 목업 `:root` 회수 조건부).
- **(K) 계약 필요 4**: `LabDataMap.byCategory`(33) · 사후 「기록 없음」 선언 ＋ 기존 엣지 `method` 수정 op(42) · AI 계보 제안에 부모 Lv(23) · `ProjectDatasetRow` 사람 값 열쇠(24).
- ⚠ §6 의 「변수명 UNIQUE(11)」 은 §5-11 판정 A 「변수명 중복 허용 유지」 와 충돌 → **원한 결과에서 제외** · `## 우려`(아래 설계 트리 Q6).

## 영향 범위

- 사용자 / 화면: 업로드 모달 장면2 좌측 · 데이터셋 상세 전체 레이아웃·미리보기·계보 빈 상태·상세 편집 · 연구실 정보 패널 · 홈 데이터 맵 · 프로젝트 표 · 계보 그래프 노드.
- 서비스 · 스키마 · 계약: viz-render(대상 기술 조회 · 변경은 viz-render 만) · core-api(`byCategory` · 기록 없음 선언·method op · 제안 Lv · 프로젝트 이름 UNIQUE · 공개 범위 내림 권한 · 이관 빈틈 마이그레이션 1) · 계약 `core-viz.yaml` 첨가 1 · `fe-core.yaml`·`common.json` 첨가(21차 패키지 6건 · 설계 트리 Q2) · 마이그레이션 = 보정 1 ＋ 프로젝트 이름 UNIQUE 1(전망 · spec 이 확정) · 게이트 신설 1~3.
- 계약 파괴 여부: **전망 아니오(㉮ 첨가)** · Lv 통일이 기존 열쇠 의미를 바꾸면 ㉯ — `contract-breaking` 출력으로 병합 직전 확정 · Ted 서명 자리 = `## 확인`.

## 제약

- POL-021 「타일 서버도 바탕 지도도 쓰지 않는다」 — **부분 반전**: 타일 서버·외부 CDN 금지는 유지, 자립형 벡터 배경만 허용. 반전은 〈N〉 에 축자 기록(PLAN-SoT ㉴ 「B-2 해안선 오버레이 미채택」 반전 포함). 원문은 지우지 않고 덧붙인다.
- core-api 에 geo 라이브러리 금지(`CLAUDE.md §3-4`) — 서버측 미리보기 변경은 viz-render 에만.
- `Policy_데이터셋_상세 §1.3-5` 「한 번에 값 하나만 그린다」 — 선택 UI 는 바꿔 그리기 · 겹쳐 보기 아님.
- 존치 규칙(`R-B-3-frontend.md:71`) — 2단 등록 게이트·기준 격자 파일 흐름·이어올리기 배너·확장보기·격자 업로드 블록은 걷지 않는다.
- 타일 서빙 강제 없음(`〈240〉` 기본값 「한 장」).
- 승인 없이 `contracts/` 를 고치지 않는다 · 21차 승인이 `contracts/` 첫 수정보다 먼저(커밋 순서).
- 결정 번호 〈N〉 은 병합 직전 재실측(현 최대 374 · `dev-package/prd/tools/max-decision.sh`).

## 설계트리 (grill-me 결과)

- Q1 축 ①·② 를 어떻게 묶는가 → A **R-C 한 intent · 두 축 · 21차 한 묶음**(권장안 수용). 대안(축 ①＋21차만 / intent 둘) 기각 — 21차는 한 번만 열리고 게이트 승격은 배포 창보다 먼저 서야 한다.
- Q2 21차 패키지 구성 → A ⑴ core-viz 「대상 기술(describe)」 조회 신설(변수＋시각 목록) ⑵ Lv 표시 사람 값 통일 열쇠(계보 노드·프로젝트 표) ⑶ `LabDataMap.byCategory` ⑷ 사후 「기록 없음」 선언 ＋ `method` 수정 op ⑸ AI 제안 부모 Lv ⑹ `ProjectDatasetRow` 사람 값. 등급 = `contract-breaking` 출력으로 병합 직전 확정(전망 ㉮ 첨가 · ⑵ 가 기존 열쇠 의미를 바꾸면 ㉯). 회차 번호 발급처 = `PLAN-SoT §9`(`X2-FREEZE-PROTOCOL.md §5`).
- Q3 배포 창 실측 3건(35·40·20)을 R-C 에 넣는가 → A **R-C 밖 · 배포 창 항목**. 단 20 의 보정 마이그레이션 자체는 R-C 안(S 묶음) · 실행 순서 = 실측 → 마이그레이션.
- Q4 WU 번호 → A **`WU-C1…`** — 대장에 `WU-C*` 0건(실측 `grep -E '^  - id: (WU-)?C'` = `C1~C4`·`CR-1`·`CR-2`·`CT-1` 뿐 · 접두 `WU-` 로 구분).
- Q5 예상 WU 수 → A **10~12**(축 ① 5 ＋ G 1 ＋ S 2~3 ＋ L 1~2 ＋ K 1). 확정은 spec `## 산출 계획`.
- Q6 §6 「변수명 UNIQUE(11)」 → A **제외** — §5-11 판정 A 「중복 허용 유지」 가 확정값. §6 문면이 판정과 어긋난 것이므로 §6 을 고치지 않고 여기 기록.
- Q7 `d9_topic_synonym` 4값→5값 이관(§5-5 · `intent/2026-09-07-r-b.md` D 에 언급) → A **R-C 안 · S 묶음 후보**. 소유 WU 는 spec 이 정한다.

## 미해결 질문

- 없음 — 프론티어 공집합(2026-09-08). 레인 실측 위임 3건(판정 아님):
  - 표준 축척 사다리 단 값(시스템 상수 한 자리) `[미측정]`.
  - Natural Earth 1:110m 해안선＋국경 파일 실크기 · 500KB 초과 시 간략화 수준 `[미측정]`.
  - 21차 등급 ㉮/㉯ — `contract-breaking` 출력 축자로 병합 직전 확정.

## 범위 밖 (명시 제외)

- 배포 창 실측 3건(35·40·20 실측) · `COLAB_VIZ_WORK_MAX_BYTES` 배포값 실측 — 배포 창 항목.
- 미리보기 렌더 성능 · 겹쳐 보기(overlay) · 타일 서빙 · 배경 지도 도시 표기 · 새 시각화 종류.
- 변수명 UNIQUE(판정 A 로 종결).
- `w9-dev-deploy-rebased` 처리(HANDOFF 다음 ⑷ · 별건).

## 확인

⭑ **승인 2026-09-08 — Ted 원문 「잔여단계 수행하자」**(이 세션 · intent 2건 승인 ＋ 레인 착수 지시로 읽음). 21차 계약 승인은 별도 문장이 필요하다(아래). main 반영은 Ted 커밋.

- 프론티어 공집합 확인: 2026-09-08 — 축 ① grill-me 3라운드(원본 intent `## 확인` 축자) · 축 ② 판정은 §5 61건 종결(재개봉 0).
- **묶음 방식** → Ted 확인 문장(원문 그대로 · 선택지 라벨): "R-C 한 intent · 두 축 ＋ 21차 한 묶음"
- **21차 대상 ⑴ 대상 기술 조회** → Ted 확인 문장(원문 그대로): "파일·변수·시각 셋 · 변수 목록 조회 신설 = 21차"
- **21차 대상 ⑵~⑹**(Lv 통일·byCategory·기록 없음·method op·제안 Lv·ProjectDatasetRow) → 근거 = 2026-09-08 「권고대로!」(`intent/2026-09-07-r-b.md ## 확인` D · §5-41 「계약 21차」 · §5-33·42 「21차 후보」).
- **계약 동결 해제 21차 승인** → Ted 승인 문장(원문 그대로): "<...>" — 등급·값·소비자 수는 승인 요청 패키지 `dev-package/sessions/R-C-C21-REQUEST-<날짜>.md`(spec 뒤 작성)에서.
- 재개봉 금지: **예** — 2026-09-05 확정 16 · 2026-09-06 확정 12 · 2026-09-08 R-B 61건 판정 · 축 ① 16 판정 · 위 Q1~Q7.
  - 다르게 구현할 사유를 찾으면 **고치지 말고 보고한다.**

## 참조

- 축 ① intent: `dev-package/intent/2026-09-08-preview-slot.md`
- R-B 판정·후속: `dev-package/sessions/R-B-ROUND-20260908.md` §5·§6 · `dev-package/intent/2026-09-07-r-b.md ## 확인`
- R-A′ 이월: `dev-package/sessions/R-A2-ROUND-20260907.md` §4·§5
- 동결 해제 절차: `dev-package/sessions/X2-FREEZE-PROTOCOL.md §5` · 20차 선례 `dev-package/sessions/R-B-C20-REQUEST-20260907.md`
- POL-021 원문: `dev-package/PLAN-SoT.md:591` · `dev-package/WORK-UNITS.md:425` · `CLAUDE.md:19`(`〈240〉`)
- 계약: `contracts/seams/core-viz.yaml:386-431` · `contracts/seams/fe-core.yaml:574,1070`
- spec: `dev-package/prd/specs/R-C.md`(작성 예정)
- 라운드 파일: `dev-package/prd/rounds/R-C.md`(작성 예정)
- 결정: 〈N〉 (병합 시 기입 · 현 최대 374)
