용도 — `/grill-me` 종료 시 산출되는 발의 아티팩트의 집. 에이전트가 초안, 승인자(develop 리뷰 권한자)가 교정한다. 양식은 `TEMPLATE.md`.
명명 — `<YYYY-MM-DD>-<주제>.md`. 승인 = 메타 줄 `승인: @<handle> <날짜> "<원문>"` 을 담은 커밋의 병합 · 판별 = `scripts/harness/intent_ref.py`(legacy 표기는 `scripts/harness/intent_legacy_approved.txt` 동결분만).
개정 금지 — 승인된 intent 는 고치지 않고 신규 intent 를 발행한다 [추론 · 스펙 K 미검증 11].
