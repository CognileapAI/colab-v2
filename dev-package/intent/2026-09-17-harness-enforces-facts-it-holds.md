# Intent: 실행기가 아는 사실은 무의미하거나 판정이거나 — 둘 중 하나다
메타 — 발의자: Ted · 방향 결정: Ted · 작성 2026-09-17 · 승인 2026-09-18

## 문제
2026-09-17 세션에서 이슈 3건을 조사·명세·구현하며, **안전 속성이 거의 전부 「에이전트가
지시문을 읽고 정직하게 보고해서」 지켜졌다.** 기계가 막은 것이 아니다. 관측된 7건이다.

1. 게이트 레인 단독 실행은 오케스트레이터가 프롬프트에 그 문장을 넣어서 지켜졌다. `gates/run.sh` 에 프로세스 간 조율이 없다.
2. `gates/tools/_pg.sh` 가 잠금을 얻지 못한 세 갈래에서 **성공을 반환한다**(`:73`·`:74`·`:78`).
3. 병렬 안전성 미선언 게이트가 10건까지 누적됐고 실행기는 매 전수에 **경고만** 찍었다.
4. spec 산출물을 만든 서브에이전트가 `dev-package/prd/specs/` 가 `WATCH` 밖이라 **증거로 선언조차 못 하고** `--mode read-only` 로 닫았다(`scripts/harness/hooks/lifecycle_contract.py:23`).
5. 신설 역할에 `SubagentStop` 매처가 없어 완료가 자동 검증되지 않는다(`.claude/settings.json:31`·`:40` 에 `researcher`·`lane-worker` 뿐).
6. 한 서브에이전트가 **산출물을 쓴 뒤에** lifecycle task 를 등록해 「수정 전 시작 기록」을 어겼다. **아무것도 탐지하지 못했고 자진신고로만 드러났다.**
7. 여러 서브에이전트가 막힌 도구를 다른 도구로 우회하고 그 사실을 밝혔다.

### 이것이 ADR-0005 의 반증이 아닌 이유
`docs/decisions/0005-harness-controls-are-declarative.md:21` 은 「기계가 실제로 강제하는 것은
게이트 종료코드와 작업 증거 계약뿐이다」를 **의도적으로** 정했다. 위 7건 중 **1·2·3·4 는 그 둘의
바깥이 아니라 안쪽이다.** 즉 경계를 넓혀 달라는 요구가 아니라, **ADR-0005 가 이미 보증한 두
기계가 자기 약속을 못 지키고 있다**는 보고다.

가장 선명한 실물 — `_pg.sh:61` 의 **자기 헤더**가 이렇게 적혀 있다.
> ⚠ 슬롯을 못 얻으면 **red 다.** 기다렸다 건너뛰거나 재시도해서 green 을 만드는 경로는 없다

그런데 같은 파일 `:69` 의 함수 시그니처가 `→ 0=획득 / 1=red` 인데 `:73`(`flock` 부재) ·
`:74`(슬롯 디렉터리 생성 실패) · `:78`(슬롯 파일 열기 실패)이 **`return 0`** 을 낸다.
**아무것도 얻지 못했는데 「획득」이라고 답한다.** 상한 초과 갈래(`:82-86`)는 `pg_readiness_report`
＋ `PG_READINESS_EXIT` 로 제대로 간다 — 설계는 옳고 **degradation 갈래가 거짓말을 한다.**

### 「규율이 작동했다」는 반대 독법과, 그것이 깨지는 지점
7건 전부 **그 빈틈에 부딪힌 에이전트가 스스로 신고**해서 드러났다. ADR-0005 의 목적이
「무엇을 검증했다고 보고해도 되는가의 상한」(`:43`)이라면 오늘은 성공이다 — 아무것도 과대보고되지
않았다. 그러나 이 독법은 **생존 편향**에 걸린다. 셀 수 있는 것은 **드러난** 위반뿐이다.
`_pg.sh` 의 세 갈래는 **어떤 에이전트도 알 수 없으므로 영원히 신고되지 않는다.**

## 원한 결과 (proposed outcome)
하네스가 **이미 알고 있는 사실을 경고로 낮추지 않고 판정으로 낸다.** 게이트 실행기가 잠금을
얻지 못했거나 같은 호스트에서 `serial` 선언 게이트가 이미 돌고 있으면 **종료코드 78**(red·준비)이
나오고 그 사유가 readiness 줄로 남는다. 병렬 안전성 미선언은 **red(판정)·종료코드 1** 이다 —
판정 대상이 선언표 자체이므로 준비가 아니라 판정이다(`gates/README.md:82-90`). 작업 증거 계약은
연구 산출물의 정당한 자리인 `dev-package/prd/specs/` 를 볼 수 있다.

경계를 정하는 규칙 한 문장이 선다 — **「실행기가 아는 사실은 무의미하거나 판정이거나 — 둘 중
하나다.」** 이 문장과 재검토 조건 한 줄이 ADR-0005 에 **2026-09-18 추가**로 들어간다.
ADR-0005 는 **대체되지 않고 개정된다.**

### 채택된 규칙 문장 — 무엇을 거르는가
실행기가 이미 쥔 사실은 **무의미하거나**(그러면 그 검사를 지운다) **판정이거나**(ADR-0004 의 세
상태 중 하나로 낸다) 둘 중 하나다. 경고는 그 사이에 있는 제3의 자리인데, 그 자리는 없다.
병렬 안전성 경고가 정확히 그 실패 사례다 — **기계가 알면서 속삭였고, 매 전수에 찍히다가 아무도
듣지 않게 됐다.** 이 문장은 각 관측을 **한쪽으로 강제로 몰기 때문에** 판정 도구가 된다.

## 영향 범위
- `gates/tools/_pg.sh:73`·`:74`·`:78` · `gates/tools/_lock.sh:19` · `gates/run.sh`(호스트 잠금)
- `scripts/harness/hooks/lifecycle_contract.py:23`(`WATCH`) · `scripts/harness/hooks/lane-gate-summary.sh:5`
- `docs/decisions/0005-harness-controls-are-declarative.md` 결과 절 · 재검토 조건(2026-09-18 개정)
- `docs/decisions/0002-gate-running-lanes-run-alone.md` — superseded
- 계약 파괴 여부: 아니오. 게이트 판정 로직·종료코드 규약·검사 대상을 바꾸지 않는다.
  `colab-gate-summary/1` 스키마와 `counts` 키 집합도 그대로다.

## 제약
- **승격 대상은 「침묵하는 위반 ∧ 실행기가 이미 사실을 쥐고 있음」을 동시에 만족하는 것뿐이다.**
  잠금 호출은 이미 실패를 반환했고, 선언표는 이미 파싱돼 있다. 비용은 **아는 사실을 종료코드로
  옮기는 것**이다. ⚠ 이 제약은 **새 감지 장치를 금지하지 않는다** — 금지로 읽으면 이 intent 자신의
  승격 3건(호스트 잠금 · `WATCH` 확대 · 측정 레인 훅)이 전부 자기 규칙에 걸린다(Q7 참조).
  거르는 것은 「아는 사실을 속삭이는 것」이지 「새로 아는 것」이 아니다.
- 의도를 알아야 하는 것(왜 도구를 바꿨는가 · 요약이 정직한가)은 사실이 아니므로 규율로 남는다.
- ADR-0005 가 기각한 수단을 되살리지 않는다 — 문자열 가로채기 훅(`:35`) · 매 턴 도는 `Stop` 훅(`:36`).
- `0005:45`(동시 쓰기를 막는 장치는 없다)·`:53` 은 **문자 그대로 유효하다.** 게이트 뮤텍스는
  체크아웃 쓰기 잠금이 아니다.
- **이 intent 는 형제 intent 5건의 선행 조건이 아니다.** 형제 각각은 **독립 결정**이며 이 규칙
  문장에 자동 정렬되지 않는다. 선행 조건으로 만들면 오늘 사용자가 지적한 그 지연을 다시 만든다.
  규칙과 ADR 개정, 그리고 `_pg.sh`·`_lock.sh` 만 이 intent 가 소유한다.

## 설계트리 (grill-me 결과)
- Q1 ADR-0005 를 대체하는가 → A **아니다. 개정한다.** ADR 의 핵심 주장(훅은 신뢰 경계가 아니다 ·
  OS 강제를 주장하지 않는다)은 오늘 반증되지 않았다. 반증된 것은 **이미 강제한다고 한 두 기계의
  구멍**이다. ADR-0003 이 `:35` 에서 한 것과 같은 형태로 추가 줄을 넣는다.
- Q2 정직한 보고를 기계로 강제할 수 있는가 → A **일반적으로 불가능하다.** 가능한 것은
  **정직이 불필요해지게 만드는 것**이다. 레인 완료 검사(`lifecycle_contract.py:301-303`)가 이미
  「이 트리에서 게이트가 green 이었다」에 대해 그렇게 한다. 전수 실행 **횟수**도 같은 형태로
  요약 파일이 들고 있으면 에이전트를 믿지 않아도 된다.
- Q3 「수정 전 시작 기록」을 `begin()` 의 존재 거부로 잡을까 → A **기각(2026-09-18 결정).**
  초안은 「선언한 산출물 경로가 등록 시점에 이미 존재하면 거부한다」를 싼 수정으로 봤으나
  **성립하지 않는다.** 비-legacy `begin()` 은 `run_id=uuid.uuid4().hex` 로 매번 새 디렉터리를
  만들고(`lifecycle_contract.py:176`) 산출물은 그 아래에 묶인다
  (`scripts/harness/task_state.py:58-63`·`:77`). 선언은 `runtime:artifacts/<이름>` 형식만 받으므로
  (`lifecycle_contract.py:173-174` · `task_state.py:66-72`) **등록 시점에 존재할 수가 없다** —
  거부 조건이 영원히 안 걸린다. 관측 6(쓰고 나서 등록)은 승격 4(`WATCH` 확대)가 대신 잡는다.
- Q4 신설 역할의 `SubagentStop` 은 → A **승격한다(2026-09-18 결정).** 초안은 미뤘으나 형태가
  정해졌다 — 새 훅을 만들지 않고 `scripts/harness/hooks/lane-gate-summary.sh:5` 의 고정
  `--role lane-worker` 를 **역할 인자로 일반화**하고 `.claude/settings.json` 의 `SubagentStop`
  배열에 매처 1건을 더한다. `task_state.py:12` 의 `GATE_ROLES` 가 이미 `measurement-lane` 을
  포함하고 `:81` 이 `gate-summary.json` 을 묶으므로 「보고할 자리가 없다」는 전제가 해소됐다.
- Q5 `WATCH` 를 넓히면 부작용은 → A `WATCH` 가 **researcher 쓰기 범위 화이트리스트를 겸한다**
  (`lifecycle_contract.py:291` 이 그 밖의 변경을 거절한다). 넓히면 researcher 가 만질 수 있는
  범위가 함께 넓어진다. spec 은 정의상 researcher 산출물이므로 **수용 가능**하다.
- Q6 오늘 세션을 얼마나 느리게 했겠는가 → A 거의 전부 **0** 이다. `_pg.sh` 수정은 그 갈래가
  불발했다면 green 이 78 이 됐을 뿐이고 그건 옳은 판정이다. 호스트 잠금도 이미 프롬프트로 직렬화돼
  있었으므로 0 이다. `WATCH` 확대는 **음의 비용** — spec 에이전트가 read-only 로 닫지 않아도 됐다.
  미선언 처분은 선언 10건을 넣는 일회 비용(10~20분)뿐이다. **전수 3회 요구**만이 오늘의 지연에
  실제로 해당하며, 그래서 그것은 **과제별 선언 조건으로 남기고 장치로 만들지 않는다.**

### 2026-09-18 결정 — Ted 판정으로 추가된 항목
- Q7 규칙 문장을 초안의 「기계는 이미 쥔 사실을 강제하고, 의도는 사람과 에이전트가 지킨다」로
  둘까 → A **기각. 따름정리를 규칙으로 승격한다.** 기각 사유 둘이다.
  ⑴ **아무것도 결정하지 못한다.** 호스트 뮤텍스의 Q1(전체 배타 잠금)과 Q2(`serial` 선언 게이트만)가
  **둘 다 이 문장을 근거로 인용할 수 있다.** 양쪽을 모두 정당화하는 문장은 판정 도구가 아니다.
  ⑵ **새 감지 장치를 금지한다.** 「이미 쥔 사실」만 강제하라는 문면은 자기 승격 3건과 모순된다 —
  호스트 잠금은 지금 없는 잠금을 **새로 만들고**, `WATCH` 확대는 지금 안 보는 경로를 **새로 보고**,
  측정 레인 훅은 지금 안 거는 매처를 **새로 건다.** 채택 문장은 「무의미하거나 판정이거나」이며,
  이것은 **경고라는 중간 상태를 없애는** 규칙이지 감지 범위를 동결하는 규칙이 아니다.
- Q8 승격 범위는 → A **5건 승격 · 1건 기각 · 1건 이미 완료.**
  ① `_pg.sh:74`·`:78` 의 `return 0` → 78(준비). **지금.**
  ② `_pg.sh:73`(`command -v flock … || return 0`) → 78. `gates/tools/_lock.sh:19` 과 **묶어서**
     같은 PR 에 낸다 — 한 디렉터리가 `flock` 부재에 대해 두 가지 정책을 들고 있으면 안 된다.
  ③ 호스트 잠금 — 승격하되 **소유자는 `2026-09-17-gate-host-mutex-enforcement.md`** 다.
  ④ `WATCH` 확대(`lifecycle_contract.py:23`) — 승격. 근거는
     `2026-09-17-spec-artifacts-outside-lifecycle-watch.md` 의 Q0 이 **YES** 로 결정된 것이다.
  ⑤ 측정 레인 훅 — 승격(Q4).
  ⑥ `begin()` 존재 거부 — **기각**(Q3).
  ⑦ 병렬 안전성 미선언 — **이미 develop 에 있다.** `scripts/harness/check.py` 의
     `check_gate_parallelism()`(`:25`·`:91`)이 red(판정)·**종료코드 1** 로 막는다
     (`gates/README.md:82-90`, 이슈 #56 · 승인 2026-09-17). 이 intent 가 「78」이라 적었던 것은
     오기이며 본문에서 정정했다.
- Q9 `flock` 부재에 면제 변수를 둘까 → A **두지 않는다.** 함대 전 호스트에 `flock` 이 있다 —
  이 호스트는 util-linux 2.39.3(`/usr/bin/flock`), CI 는 `.github/workflows/ci.yml` 의 `runs-on`
  15건이 **전부 `ubuntu-latest`** 다(`:18`·`:48`·`:137`·`:176`·`:269`·`:328` 외 9건). 실재하지 않는
  환경을 위한 면제 변수는 그 자체가 우회로다. macOS 가 함대에 들어오는 시점에 **그때** 3상태
  변수를 넣는다 — 선례는 `COLAB_VISUAL_EXEMPT=1`·`COLAB_HARNESS_EVAL_EXEMPT=1`
  (`gates/README.md:39`·`:43`, `AGENTS.md:46`)이다. `gates/run.sh:580` 의 `SELFTEST_EXEMPT` 는
  **하드코딩 표**라 선례가 아니다.
- Q10 PR #115(되돌리기 superuser 승격)는 이 규칙의 사례인가 → A **아니다.** #115 는 RLS 때문에
  **하네스가 못 보던 행을 보게** 만든 것이다 — 시야를 넓혔지 쥔 사실을 강제한 것이 아니다.
  이 규칙의 사례로 인용하지 않는다(`dev-package/intent/2026-09-18-rollback-purge-as-superuser.md`).

## 미해결 질문
- 없음. 초안의 3건은 모두 실측으로 닫혔다 — ⑴ 미선언 처분은 `check_gate_parallelism()` 으로
  이미 red(판정)·1 이다(`gates/README.md:82-90`) ⑵ `flock` 은 함대 전 호스트에 있다(Q9)
  ⑶ `WATCH` 는 **수정까지** 본다 — `lifecycle_contract.py:304` 가 baseline 과 현재 스냅숏의
  다이제스트 차이를 `changed` 로 잡고 `:309` 가 그 전부에 `WATCH` 접두사를 요구한다. 따라서
  `prd/specs/**` 기존 파일 편집에도 소급 비용이 붙으며, 그 비용은 수용한다.

## 범위 밖 (명시 제외)
- 정직한 보고 자체를 기계로 강제하려는 시도. 도구 우회를 문자열 가로채기로 막는 훅. 매 응답마다
  도는 `Stop` 훅. 체크아웃 단위 쓰기 잠금. 역할 경계의 OS 접근 통제 승격.
- 검증 실행 **횟수**를 장치로 만드는 것. 요약 파일에 기록할 수는 있으나 「3회」 요구는 과제별
  선언 조건으로 남긴다.
- `begin()` 의 산출물 존재 거부. 기각됐다(Q3).
- 테스트 위생 1건(프런트 고정 벽시계 예산). 이 규칙의 사례가 아니며 독립이다.
- PR #115(되돌리기 superuser 승격). 이 규칙의 사례가 아니다(Q10).
- 커밋·push·PR 게시·배포·이슈 댓글·이슈 종결.

## 실행 순서 (PR 4단 · 각 단계는 전수 게이트로 검증한다)
1. `_pg.sh:73`·`:74`·`:78` 3줄 ＋ `gates/tools/_lock.sh:19` ＋ ADR-0005 개정 ＋ ADR-0002 대체.
2. 호스트 잠금(`2026-09-17-gate-host-mutex-enforcement.md` 소유).
3. `WATCH` 확대(`2026-09-17-spec-artifacts-outside-lifecycle-watch.md` 소유).
4. 측정 레인 훅(`2026-09-17-measurement-lane-subagent-stop-hook.md` 소유).

## 확인
- Ted 확인 문장(원문 그대로): "좋아 그렇게해"
- Ted 지적(원문 그대로): "시스템적으로 명확하게 해야하는거아님? 에이전트 그냥 도는게 아니라"
- 앞선 Ted 지적(같은 계열, 원문 그대로): "이건 에이전트가 돌리는게 아니라 시스템으로 엄격하게 무조건되엉야하잖아"
- 승인 2026-09-18. **남은 승인 필요 지점: 없음.** 아래 6건이 이 날짜에 전부 결정됐다.
  1. 규칙 문장 — 따름정리를 채택(Q7). 초안 문장은 기각.
  2. ADR-0005 는 **개정**한다. 동시에 ADR-0002 를 **대체(superseded)** 한다 —
     ADR-0002:32 가 「`gates/run.sh` 에 프로세스 간 뮤텍스가 들어갈 때」를 스스로 재검토 조건으로
     적었고, 승격 ③ 이 그 조건을 충족시킨다. 대체 사실은 ADR-0005 개정문에 기록한다.
  3. 승격 범위 — Q8 의 5건.
  4. `flock` 부재 — 78. 면제 변수 없음(Q9).
  5. 착수 시점 — 위 「실행 순서」 4단.
  6. GitHub 이슈 — **올리지 않는다.** intent 와 ADR 로 간다.
- 재개봉 금지: 예.

## 참조
- ADR: `docs/decisions/0005-harness-controls-are-declarative.md:21`·`:35`·`:36`·`:43`·`:45`·`:49-53`(개정 대상),
  `0002-gate-running-lanes-run-alone.md:32`(대체 대상 · 자기 재검토 조건),
  `0003-human-approval-machine-checks-form.md:35`, `0004-gate-verdict-three-states.md`
- 코드: `gates/tools/_pg.sh:61`·`:69`·`:73`·`:74`·`:78`·`:82-86`, `gates/tools/_lock.sh:19`,
  `scripts/harness/hooks/lifecycle_contract.py:23`·`:173-176`·`:304`·`:309`,
  `scripts/harness/hooks/lane-gate-summary.sh:5`,
  `scripts/harness/task_state.py:12`·`:58-63`·`:66-72`·`:77`·`:81`,
  `scripts/harness/check.py:25`·`:91`, `.claude/settings.json:31`·`:40`,
  `gates/run.sh:580`(`SELFTEST_EXEMPT` — 선례 아님)·`:740`(자식 재진입)
- 문서: `gates/README.md:39`·`:43`(면제 변수 선례)·`:82-90`(미선언 = red(판정)·1), `AGENTS.md:46`
- 형제 intent(각각 **독립 결정** · 이 규칙에 자동 정렬되지 않는다):
  `2026-09-17-gate-host-mutex-enforcement.md`(승격 ③ 소유),
  `2026-09-17-spec-artifacts-outside-lifecycle-watch.md`(승격 ④ 소유),
  `2026-09-17-measurement-lane-subagent-stop-hook.md`(승격 ⑤ 소유),
  `2026-09-17-gate-input-env-vars-block-merge-gate.md`
- 무관 intent(이 규칙의 사례가 아님): `2026-09-17-frontend-test-wall-clock-budget-convention.md`
- 대체된 intent: `2026-09-17-tests-writing-outside-rollback-scope.md` 는
  `dev-package/intent/2026-09-18-rollback-purge-as-superuser.md`(승인 2026-09-18 · develop 에 병합)가
  **대체**했으므로 2026-09-18 에 삭제했다.
- 판단 근거: Fable 어드바이저 심의 2026-09-17. `_pg.sh:55-95`·`_lock.sh:14-21` 을 develop 에서
  직접 재확인했다(2026-09-18).
