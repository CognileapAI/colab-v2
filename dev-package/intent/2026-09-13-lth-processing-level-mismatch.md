# Intent: 가공 단계 불일치를 이행분·미구현분·재판정분으로 갈라 닫는다
메타 — 발의자: 이태헌(1차 검증 피드백 · 기획자 분류 긴급) · 작성 2026-09-13 · 승인 미승인

## 문제
- 데이터셋 상세에서 고른 가공 단계가 Lv2, 연결 데이터로 계산한 값이 Lv0 인데 「그대로 두어도 등록돼요」 한 줄만 뜨고 저장된다(확인 사례 = 기획자 보고의 시험 데이터셋 1건).
- 그 한 줄은 두 값만 말하고 **왜 갈렸는지는 말하지 않는다** — 판정문이 요구한 「사유 한 줄」이 화면에 없다.
- 부모를 한 건도 연결하지 않은 등록에서는 등록 화면에 경고가 아예 뜨지 않는다.
- 목록·검색·프로젝트 상세·검토 화면에 불일치가 전혀 보이지 않는다.
- 등록을 마친 뒤 사람이 고른 가공 단계를 화면에서 고치는 칸이 없다.

## 원한 결과 (proposed outcome)
- 검증 가능 문장 ⑴ — 불일치 안내 한 줄이 두 값 ＋ 각 값의 근거(사람이 고른 값 / 부모 최대 Lv＋1 · 부모 0건이면 0)를 함께 말한다. 상세와 등록 두 자리가 같은 문장을 쓴다.
- 검증 가능 문장 ⑵ — 데이터셋 목록의 가공 단계 칸이 불일치 행에 표식을 그린다(응답 열쇠는 이미 내려온다).
- 검증 가능 문장 ⑶ — 상세 편집에 가공 단계 칸이 있고, 저장 후 상세 표시값이 바뀐다.
- 검증 가능 문장 ⑷ — 위 셋의 시험이 각 1건 이상 green 이고, 불일치 상태에서 등록·저장이 계속 성공한다(기존 판정 유지 확인).
- 판정 = 새 판정 없이 닫히는 분(⑴·목록 표식·편집 칸)이 green 이고, 판정 반전이 필요한 분은 Ted 판정 대기로 분리 등재된다.

## 영향 범위
- 사용자 / 화면: 데이터셋 상세 헤더 · 등록 ③ 연결 단계 · 데이터셋 목록 · (선택) 프로젝트 상세 표·계보 노드·할 일 함 · 상세 편집 폼.
- 서비스 · 스키마 · 계약: 이행분은 `frontend/src/components/detail/DetailHeader.tsx` · `frontend/src/components/lineage/LineageStep.tsx` · `frontend/src/components/catalog/CatalogTable.tsx` · `frontend/src/components/detail/editFields.ts`. 목록 확대 시 `services/core-api/.../routes/project.py`·`routes/lineage.py` 의 두 값 조립을 세 값 조립으로 올린다(열쇠 추가).
- 계약 파괴 여부: 아니오 — 편집 칸은 기존 계약 열쇠를 쓴다(`contracts/seams/fe-core.yaml` 의 `DatasetUpdate.processingLevelUserSet` 실재). 프로젝트 표·계보 노드 확대는 열쇠 추가형.

## 제약
- 현재 동작은 2026-09-05 Ted 확정 판정 그대로다 — 판정 축자
  > ⓐ 경고만 — 사람이 Lv0~Lv3 을 고르고, 파생값과 달라도 등록을 막지 않고 화면에 사유 한 줄을 낸다.
  (중간 생략 = 열 재신설·원장 기재 지시 부분) 같은 문서 기본값 축자 — 「- 기본 선택값 = **Lv2**(rev1 `<option selected>`)」
- 수용 기준 축자 — 「Given 사람 Lv 와 파생 Lv 가 어긋남, When `createDataset`, Then **성공하고 경고만 뜬다**(400 이 아니다 …)」
- 판정 재개봉은 Ted 만 가능하다(`.claude/rules/colab-rules.md §8`). 아래 ⑵·⑶·⑷′ 는 요청이고 결정이 아니다.
- 불일치는 저장되지 않는다 — 사람 값 1칸(`db/platform/schema.sql` 앵커 `processing_level_user_set text`) ＋ 계보에서 매 요청 계산. 파생값·불일치 칸은 테이블에 없다.
- 사유를 담을 저장 칸이 없다 — 사유 입력을 요구하면 마이그레이션 1건과 계약 열쇠 추가가 선다.
- 사례 데이터셋 `TEST Stage12 Preview HDF5 20260911` 의 실제 저장값(사람 Lv · 부모 건수) `[미확인]` — dev DB 조회로 닫힌다.

## 설계트리 (grill-me 결과)
- Q1 기획자 권고 ⑴(차이 이유 ＋ 두 값 근거 표시) → A **판정 이행 미달**이다. 판정문이 「화면에 사유 한 줄을 낸다」를 요구했고 실물 문장은 두 값만 말한다. 새 판정 없이 문장을 보강한다. 크기 **S**.
- Q2 권고 ⑵(기본값을 계산값으로) → A **판정 반전** — 판정문의 「기본 선택값 = Lv2」를 바꾼다. Ted 필요. 크기 **M**(`RegisterArea.tsx`·`UploadModal.tsx` ＋ 시험).
- Q3 권고 ⑶(사유 입력 ＋ 명시 확인) → A **판정 반전** — 「막지 않는다」의 반전이고 사유 저장 칸이 필요하다. 계약 열쇠 추가 ＋ `db/platform` 마이그레이션 1건 ＋ 라우트·도메인·화면. Ted 필요. 크기 **L**. AI 없이도 완결되는 성질은 영향받지 않는다.
- Q4 권고 ⑷(목록·검토 표시) → A **미구현**이고 판정 저촉 없음. 목록은 화면만 고치면 된다(불일치 열쇠가 이미 응답에 실린다 · 프런트에서 그 열쇠를 읽는 자리는 상세 헤더 하나뿐). 프로젝트 표·계보 노드는 열쇠를 아직 받지 않는다. 크기 **S**(목록만) / **M**(표·노드·할 일 함).
- Q5 조사가 더한 ⑸(등록 뒤 수정 칸 부재) → A **미구현**. 프런트에서 사람 값을 전송하는 자리는 업로드 흐름 둘뿐이고 나머지는 모두 읽기다(조사 C 추가 실측). 계약 열쇠가 이미 있어 화면 칸 신설만으로 닫힌다. 크기 **S~M**.
- Q6 ⑷′(부모 0건 등록에서 경고 부재) → A **설계 차이**이고 ⑵ 와 **같은 판정에 매인다** — 등록 화면의 미리보기는 부모 Lv 를 모르면 만들지 않는 규칙이고(`LineageStep.tsx` 독스트링 축자 「부모 Lv 를 하나라도 모르면 미리보기를 만들지 않는다.」), 부모 0건을 0 으로 볼지가 기본값 판정과 한 건이다. 크기 **S**(advisor 게이트 ② 반영 — 결함으로 분류하지 않는다).
- Q7 같은 계열의 선례 → A 안내 전용으로 더한 사례가 있다 — 사람 Lv0 ＋ 파생≠0 행의 출처 안내(구현 자리 `frontend/src/components/detail/BasicInfoGrid.tsx` · 주석 축자 「⛔ 안내로만이다 — 저장을 막지 않고 재입력을 강제하지 않는다.」). 차단 없이 안내를 더하는 방향은 기존 판정과 정합한다.

## 미해결 질문
- Ted 판정 ① — 등록 화면의 가공 단계 기본값을 고정 Lv2 에서 계산값 추종으로 바꾸는가(부모 0건이면 Lv0).
  - 사용자가 보는 것 = 등록 화면을 열면 가공 단계 칸에 미리 들어 있는 값이 달라진다.
  - 대가 = ⓐ 바꾼다 — 차이가 생기는 건수가 줄고 잘못된 단계가 확정되는 길이 좁아진다. 확정 판정문 한 줄을 뒤집고 등록 시험을 정정한다. 부모 0건 등록에서도 경고가 서려면 같은 판정에서 함께 정한다. ⓑ 유지한다 — 작업 0, 오판정 경로가 남는다.
  - 권고 = ⓐ. 기본값만 바꿔도 불일치 발생 자체가 줄고 차단은 늘지 않는다.
- Ted 판정 ② — 다른 단계를 유지할 때 사유 입력과 한 번 더 확인을 요구하는가.
  - 사용자가 보는 것 = 등록 마지막에 한 단계가 늘고, 왜 그 단계를 골랐는지 적는 칸이 생긴다.
  - 대가 = ⓐ 요구한다 — 잘못된 단계 확정이 좁아지고 선택 이유가 기록에 남는다. 사유 저장 칸과 마이그레이션이 생기고, 부모를 나중에 붙이는 흐름에서는 같은 확인을 두 번 받는다. ⓑ 요구하지 않는다 — 부모 후붙임 흐름이 지금처럼 한 번에 끝난다.
  - 권고 = 이번 회차 밖. 사유 칸 설계와 함께 다음 회차 판정으로 두고 항목으로 등재한다.
- 사례 데이터셋의 저장값 `[미확인]`(dev DB) · 할 일 함 모수에 불일치를 더할 자리 `[미확인]`(`frontend/src/components/dashboard/dashboardSource.ts` 확인으로 닫힌다).

## 범위 밖 (명시 제외)
- 불일치 상태에서 등록·저장을 차단하는 동작(판정이 금지).
- 파생값·불일치 결과를 테이블에 저장하는 구조 변경.
- 분류 3축 편집 칸 신설(같은 편집 폼의 별 항목).
- 검색 결과 카드의 불일치 표기.
- 「CoLAB에서 가공됨」 표식·계보 삭제 시 부모 이름 보존(별 항목 · `stage: after_stage2`).

## 확인
- 프론티어 공집합 확인: `[미확인]` — 이 초안에서 미실시.
- Ted 확인 문장(원문 그대로): "없음 — 미승인"
- 재개봉 금지: 아니오 — 이행분·미구현분은 재개봉 없이 닫고, 반전 2건은 이 문서가 판정을 요청한다.

## 참조
- 기획 원본: `40 COLAB-기획/10_적용전/`(무수정)
- 피드백 원문: `dev-package/reports/issues/2026-09-13-lth-review-1-raw.md` `I-1`
- 조사: `dev-package/reports/issues/2026-09-13-lth-survey-C.md` `I-1` ＋ 「추가 실측」
- 취합: `dev-package/reports/issues/2026-09-13-lth-review-1-SUMMARY.md`
- 기존 판정: `dev-package/prd/PRD-260905-적용전기획.md` 미결-2 ⓐ(Ted 2026-09-05) · 미결-7 ⓐ(상한 Lv3) · `PRD-10` · 반영본 `dev-package/prd/개발계획서-260905.md` `WU-B5` · 원장 반전 기재 `PLAN-SoT §9 〈194〉`
- 관련 항목: `LV-3` · `LV-4`(둘 다 `stage: after_stage2` · 이 건을 덮지 않음)
- 결정: 〈N〉 (병합 시 기입)
