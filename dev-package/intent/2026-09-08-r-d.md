# Intent: R-D — 브랜치 전략(축 ①) ＋ 하네스 eval 20건(축 ②) · 제품 코드 0 · 계약 0

> ⛔ **초안 · 승인 미승인 · 발의 Ted 2026-09-08 · grill-me 2026-09-08 완료(프론티어 공집합).**
> 초안까지가 에이전트의 역할이다. 교정·커밋은 Ted 가 한다(커밋이 곧 승인). 이 파일을 승인된 것처럼 인용하지 않는다.

메타 — 발의자: Ted(축 ① 「브랜치 전략이 없네? 어떻게 하는게 좋을까?」 → 권고안에 「좋아 intent에 얹어」 · 축 ② 「이게 검증결과로 나왔는데, 개선준비해보자」) ＋ agent(초안) · 작성 2026-09-08 · 승인 **미승인**

- 축 ① 근거: `dev-package/sessions/R-C-ROUND-20260908.md` §9(창 9 발견 · 〈378〉 ⑧) · `03-HANDOFF.md` 「⑷ `w9-dev-deploy-rebased` 처리」.
- 축 ② 원본 intent: `dev-package/intent/2026-09-08-harness-evals.md`(설계 트리 Q1~Q12 · 로스터 20건 — 여기서 재측정하지 않는다).
- 순서 합의(Ted 2026-09-08 「모전부 권고대로 계획」 뒤): **하네스 강화 → 배포(R-D) → 재검사(강화 스킬 · staging 실화면) → 리스트업 → R-E 디자인 fix.** R-C 후속 9건과 디자인 판정 25건은 아래 「이월 → R-E」.
- 결정 번호 〈N〉 은 병합 직전 재실측(현 최대 378).

## 문제

**축 ① 브랜치 전략이 문서에 없다**
- 관행만 있다 — `main` 보호·ff-only, 라운드마다 `integration/r-*`, WU 마다 `lane/wu-*` 워크트리, 레인 → 통합은 rebase＋ff. 어느 문서도 이것을 규칙으로 적지 않았다(`grep -rn "브랜치 전략\|branching" CLAUDE.md docs/` = 0건).
- 그 빈자리에서 사고가 났다 — 창 9(2026-09-06)가 `main` 밖 레인 sha `20b3715`(`integration/w9-dev-deploy`)를 dev 에 배포했고, 그 브랜치의 ai 마이그레이션 `0006_topic_vocab_six` 가 dev 에만 적용된 채 남았다. R-C 배포 창에서 형제 `0006_rc7_synonym_category` 와 부딪혀 `migrate-ai` 직전 STOP → WU-C13 `0007_merge` 로 해소(〈378〉 ⑧).
- 배포 도구가 브랜치를 보지 않는다 — `infra/dev/ship.sh:14` 은 `dist/colab-v2-dev.sha` 만 읽고, `infra/dev/build.sh` 는 「그 워킹트리의 HEAD」를 굽는다. EC2 에는 git 이 없어 호스트 쪽 검사도 없다.
- 레인 브랜치가 정리되지 않는다 — 실측 `git branch --list`: `integration/r-a2` · `r-b` · `r-c` · `w9-dev-deploy` · `lane/wu-c1·c2·c3·c4·c5·c7·c8·c9·c10·c11·c13` · `plan/r-c-0908` · `plan/r-d-0908` · `w9-rebase` = **18개**. 살아남은 브랜치가 「책에 없는 층」의 씨앗이다.

**축 ② 하네스 품질을 재는 자리가 없다**(요약 · 축자는 원본 intent)
- 지침·스킬·훅·에이전트를 고쳐도 행동이 바뀌었는지 재는 장치가 0. 2026-09-08 하루에 재발 사고 3건이 `CLAUDE.md §5-b` 산문으로만 남았다.
- 기존 `eval/` 은 D10 품질(K3·K4)만 판정한다.

## 원한 결과 (proposed outcome)

**축 ① 규칙 6개**(Ted 「좋아」 · 문면은 권고안 축자)
1. `main` 은 **유일한 배포 원천**. dev·prod 에 올라가는 sha 는 반드시 `origin/main` 의 조상. `ship.sh`·`up.sh`·staging `deploy.sh --target dev` 가 `git merge-base --is-ancestor` 로 검사하고 아니면 거절.
2. staging 은 예외 — `integration/*` HEAD 를 굽는다(리허설). 단 원장 행에 **브랜치 이름**을 같이 적는다.
3. `integration/r-N` 은 `main` tip 에서 따고, `main` 으로는 **ff-only 한 줄**. 병합 뒤 브랜치 삭제.
4. `lane/wu-*` 는 `integration` 에서 따고 rebase＋ff 로 돌아온다. 통합에 얹힌 즉시 삭제.
5. 마이그레이션은 **한 라운드 = 한 체인 구간**. 형제가 생기면 `00NN_merge` ＋ 두 순서 drift 오라클(0007 선례) 의무.
6. 릴리스 = 태그. dev 실적용 때 `dev-YYYYMMDD-N`, prod 는 기존 `prod-YYYYMMDD`. 〈N〉 행이 태그를 가리킨다.

**축 ① 산출물**
- `docs/BRANCHING.md` 신설(규칙 6 ＋ 브랜치 수명 표 ＋ 「하지 말 것」).
- `CLAUDE.md` **§10** 한 절(현 마지막 절 = `§9 업로드(S3)·배포` · `CLAUDE.md:161`) — 규칙 6 요지 ＋ `docs/BRANCHING.md` 링크.
- `infra/dev/ship.sh` 앞단 게이트 1 — `SHA` 를 읽는 `:14` 직후·scp 전에 로컬에서 `git -C "$REPO" fetch origin main && git merge-base --is-ancestor "$SHA" origin/main` 실패 시 `exit 65` ＋ 사유. 같은 반입에 `MAIN_SHA`(`origin/main` sha) 파일을 함께 올린다.
- `infra/staging/deploy.sh:266` `ledger_append deploy …` 비고에 `브랜치=<git branch --show-current>` 필드 추가(`rollback.sh:135` 동형).
- `deploy_doctor` **15번째 항목** 「실행 sha ∈ main」 — `MARKS`(`deploy_doctor.py:62`) 확장 · `/opt/colab-v2/CURRENT_SHA` 와 `MAIN_SHA` 대조.
- 브랜치 정리 — 18개 중 `main` 조상 확인분 즉시 삭제 · 창 9 계열 3개(`w9-rebase`·`integration/w9-dev-deploy`·`origin/integration/w9-dev-deploy-rebased`)는 잔여 2커밋 실측 보고 → Ted 한 줄 → 삭제.

**축 ② 하네스 eval**(원본 intent `## 원한 결과` 승계) — 러너 `eval/harness/run.sh` · 과제 20건(`<번호-이름>/{task.md,fixture/,expect.sh}`) · 2회 2/2 green · 첫 실측으로 상한 확정 · 승격 스텁.

## 영향 범위
- 사용자 / 화면: 없음.
- 서비스 · 스키마 · 계약: `deploy_doctor.py`(15항목) · `infra/dev/ship.sh` · `infra/staging/deploy.sh` · `docs/` · `CLAUDE.md` · `eval/harness/` 신설 · `gates/run.sh` 승격 스텁(면제 선언). 제품 코드 0 · 계약 0 · 스키마 0 · 마이그레이션 0.
- 계약 파괴 여부: 아니오.

## 제약
- `main` push·ff 병합은 Ted(규칙 3 자체가 그것을 정본화한다). 에이전트는 `integration/*`·`lane/*` 까지.
- EC2 에 git 없음(`infra/dev/README.md`) — 호스트 쪽 「main 조상」 검사는 파일(`MAIN_SHA`)로만 가능.
- `deploy.sh:65` 워킹트리 0건 착수 조건과 충돌하는 산출물 금지(배포가 레포 파일을 고치지 않는다 · 〈297〉 선례).
- 원문 삭제 금지 — CLAUDE.md §9 · HANDOFF 원문은 덧붙인다.
- 하네스 eval 제약은 원본 intent `## 제약` 그대로(제품 코드 0 · 외부 호출 0 · 기계 판정 우선 · 대상 0건 red · `--max-turns` 부재).
- 결정 번호 〈N〉 은 병합 직전 재실측.

## 설계트리 (grill-me 결과)
- Q1 doctor 15항목 「실행 sha ∈ main」의 근거 → A **ⓐ `ship.sh` 가 `origin/main` sha 를 `/opt/colab-v2/MAIN_SHA` 로 함께 실어 doctor 가 `CURRENT_SHA` 와 대조**(권장안 수용). EC2 에 git 이 없어 파일 대조가 유일하고, 게이트를 우회한 경우를 사후에 잡는다.
- Q2 규칙 1 게이트의 위치 → A **`ship.sh` 한 곳**(반입 자체를 막는다) ＋ doctor 사후 대조. `up.sh` 이중 검사는 두지 않는다.
- Q3 태그 명명 → A **`dev-YYYYMMDD-N` · `prod-YYYYMMDD`**(권장안 수용). dev 에 원장이 없어 「안전 복구 세대」가 이미지 잔존에만 의존하던 것을 태그가 닫는다. 태그는 doctor 전건 뒤에 찍는다.
- Q4 브랜치 정리 시점 → A **이번 라운드 첫 WU**(권장안 수용). 살아 있는 브랜치가 곧 다음 사고다.
  - Q4a 창 9 잔여 2커밋(`80aeb00` 트리거 스풀·매니페스트 원천 표기 · `de1a5a2`·`97f1d99` 원장 사본) → A **WU-D4 안에서 실측 보고 → Ted 한 줄 → 삭제**(intent 판정 아님 · 흡수/폐기는 실측 뒤).
- Q5 R-D 묶음 → A **축 ① 브랜치 전략 4 WU ＋ 축 ② 하네스 eval 3 WU**(하네스 eval 라운드 Q12 · 권장안 수용). 디자인 fix 16 은 재검사 뒤 R-E · R-C 후속 9건은 R-E 이월.
- Q6~Q12 하네스 eval 세부 → 원본 intent `2026-09-08-harness-evals.md` 설계 트리 Q5~Q12 참조(배분 3·10·5·2 · 저장 형식 · 상한 180s＋$0.50→p95×2 · 2회 2/2 · 로컬 러너 먼저 · 승격 3회 연속 뒤 세 상태 · 권한 read-only · R-D 합류).

**WU 스케치(확정은 spec)** — WU-D1 `BRANCHING.md`＋`CLAUDE.md §10` · WU-D2 `ship.sh:14` 직후 반입 게이트＋`MAIN_SHA` 반입＋`deploy.sh:266` 원장 브랜치 필드 · WU-D3 doctor 15항목 · WU-D4 브랜치 정리 18(창 9 2커밋 실측 선행) · WU-D5 eval 러너＋과제 형식＋`eval/README.md` 한 행 · WU-D6 과제 20건＋첫 실측(p95·예산) · WU-D7 승격 준비(`gates/run.sh` 등록 스텁 세 상태 · CI paths-filter 출력 · 시크릿은 Ted). **예상 7 · 제품 코드 0 · 계약 0 · 마이그레이션 0.**

## 이월 → R-E (판정 없이 이월 · 소유 WU 미지정)

| # | 항목 | 근거 |
|---|---|---|
| 1 | alembic 로거 — `db/ai/alembic.ini:37-39`·`db/platform/alembic.ini` `[logger_alembic] handlers =` 비어 축자 로그 0 | staging-deploy-rc2.log 실측 |
| 2 | `project.css:436` 음수 상쇄(4건째) | `R-C-VERIFY-20260908.md` · 디자인 판정표 #26 즉시 수정 후보 |
| 3 | `upload.css` 자식 `margin-top` 5곳(`:348·357·384·388·424`) | 디자인 판정 Q17(판정 13) 대상 — ⓐ면 변경 0 |
| 4 | AI 계보 제안 부모 Lv 생산자 0(`d10_suggestion.py:61,88-91,119` 직렬화만) | C9 `[미상]` |
| 5 | 축척 사다리 5단 staging 실물 `bounds` 분포 재검 | C4 `[미상]` |
| 6 | `describe` 스모크를 `STAGING-WINDOW-RUNBOOK.md §2` 목록에 추가 | 예행 2회 미실시 |
| 7 | 시각 후보 처음·마지막 둘 — `core-viz.yaml:487-495 InstantRange` · 목록 첨가는 계약 22차 후보 | C3 ⓐ |
| 8 | `granularity: null` 죽은 가지(`RegisterArea.tsx:277,303,327`) | C8 초과 |
| 9 | 창 9 잔여 2커밋 흡수/폐기 — **R-D WU-D4 로 이동**(위 Q4a) | — |

## 디자인 판정 25건 — 값 확정 · 집행은 R-E(재검사 뒤)

Ted 2026-09-08 「모전부 권고대로 계획」. 원 판정표 = `dev-package/sessions/design-review-20260908.md`(Ted 판정 21 · 후보 WU 16). 선택지 라벨 축자.

- Q1 R-D 범위 → ⓑ R-D = 브랜치 전략 ＋ (하네스 eval) · R-C 후속 5 는 R-E · **후속 합의로 디자인 fix 16 도 재검사 뒤 R-E**
- Q2 배포 점검 15번째 항목 → ⓐ `MAIN_SHA` 파일 대조 · Q3 태그 → ⓐ `dev-YYYYMMDD-N`/`prod-YYYYMMDD` · Q4 브랜치 정리 → ⓐ 첫 WU
- Q5 인증 대기 뱃지 대비 → ⓐ 글자색 같은 회색 한 단 진하게(5.70:1) · Q6 구역 메뉴 선택 상태 → ⓐ 파랑 한 단(4.94:1) · Q7 뱃지·캡션 11px → ⓑ 정본 기준선을 뱃지류에 대해 개정(개정값 제안 = 뱃지·캡션 11px 허용 · 본문·라벨 13px 유지) · Q8 글자 크기 승격 81건 → ⓐ 유형별 순차
- Q9 복제 토큰 12개 → ⓐ 공통 승격 · Q10 미리보기 색 이름 → ⓐ 공통 정본으로 치환 · Q11 대시보드 7종 → ⓑ 기존 공통 이름 치환(중립 강조 1 승격) · Q12 미정의 19건 → ⓐ 업로드·계보 공유분 먼저 · 랩 위험색 보류 · Q13 검색 폴백 → ⓐ 실제 값으로 정정
- Q14 상단바 그림자 → ⓑ 유지 · Q15 모달 그림자 → ⓐ 팝오버 허용 범주에 포함 · Q16 대시보드 테두리 → ⓑ 두 층 체계 · Q17 자식 위쪽 간격 12곳 → ⓐ 반복 형제만 · 단일 자리 무접촉
- Q18 누름 피드백 → ⓐ 신설(값은 R-E 라운드) · Q19 모션 축소 → ⓑ 파일마다 · Q20 모달 열기·닫기 → ⓐ 중단 가능 전환 ＋ 닫기 대칭 · Q21 팝오버 기준점 → ⓐ 달력 팝오버만 · Q22 드래그 → ⓐ 포인터 이벤트＋시작 임계(값은 R-E) · Q23 드롭 영역 → ⓐ 상태 신설(색은 R-E) · Q24 글자 간격 → ⓐ 구간 토큰(값은 R-E) · Q25 속도 계승 → ⓑ 현행

## 미해결 질문
- 없음 — 프론티어 공집합(2026-09-08). 레인 실측 위임: 창 9 2커밋 실측(WU-D4) · eval 상한 p95·예산(WU-D6).

## 범위 밖 (명시 제외)
- git-flow 식 `develop`·`release/*` 계층 — 사람 하나＋에이전트 레인 구조에서 층만 늘고, 사고 원인(「책에 없는 층」)은 층수와 무관.
- prod 배포 재개(⏸ 유지 · `〈334〉`-㉳).
- 마이그레이션 되돌림 정책 변경(forward-only 유지).
- staging 원장 형식 전면 개정(필드 1개 추가만).
- 디자인 fix 16 WU 집행 · R-C 후속 9건 · 계약 22차 — 전부 R-E.
- CI 시크릿 발급(Ted 몫).

## 확인
⛔ Ted 가 채워 커밋하기 전에는 정본이 아니다(커밋이 곧 승인).
- 프론티어 공집합 확인: 2026-09-08 — 라운드 1(브랜치 전략 ＋ 디자인 판정 25문항) · 라운드 1(하네스 eval 8문항)
- 발의 원문(2026-09-08): 「브랜치 전략이 없네? 어떻게 하는게 좋을까? 브랜치전략」 → 권고안 6규칙 → Ted 확인 문장(원문 그대로): "좋아 intent에 얹어"
- 디자인 판정 25문항 → Ted 확인 문장(원문 그대로): "모전부 권고대로 계획" · 이어진 순서 재편 원문: "디자인스킬(하네스가)를 개선하고 있으니까 이거 강화 끝내고, 배포하고, 그다음에 다시 디자인적으로 검토하고 항목을 리스트업해보자 어때?"
- 하네스 eval 8문항 → Ted 확인 문장(원문 그대로): "전부권고대로"
- 계약 동결 해제 21차/22차: 해당 없음(계약 0)
- 재개봉 금지: 예 — 규칙 6 · Q1~Q5(＋Q4a) · 하네스 eval Q1~Q12 · 디자인 판정 25건(값) · R-C 판정 전건.

## 참조
- 원본 intent(축 ②): `dev-package/intent/2026-09-08-harness-evals.md`
- `dev-package/sessions/R-C-ROUND-20260908.md` §7·§9 · `R-C-VERIFY-20260908.md` · `WU-C13-ai-merge-20260908.md` · `design-review-20260908.md`
- `dev-package/03-HANDOFF.md` R-C 마감 증보(「남은 것」) · `PLAN-SoT §9 〈378〉`
- 코드: `infra/dev/ship.sh:14` · `infra/dev/build.sh` · `infra/staging/deploy.sh:266` · `infra/staging/rollback.sh:135` · `services/core-api/ops/deploy_doctor.py:62` · `CLAUDE.md:161`(§9) · `db/ai/alembic.ini:37-39`
- 브랜치 실측: `git branch --list` 18개 · `git log main..w9-rebase` 3커밋
- ELI5 브리프(로컬 · `eli5/` 미추적): `eli5/2026-09-08/w9-ai-merge-eli5-20260908.html`
- spec: `dev-package/prd/specs/R-D.md`(작성 예정) · 라운드 파일: `dev-package/prd/rounds/R-D-*.md`(작성 예정) · 결정: 〈N〉(병합 시 기입 · 현 최대 378)
