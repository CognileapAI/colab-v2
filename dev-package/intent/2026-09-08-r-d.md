# Intent: R-D — 브랜치 전략(축 ①) ＋ R-C 후속 9건(축 ②) · 계약 22차 여부 미결

> ⛔ **초안 · 승인 미승인 · 발의 Ted 2026-09-08 · grill-me 미실시(프론티어 열림).**
> 초안까지가 에이전트의 역할이다. 교정·커밋은 Ted 가 한다(커밋이 곧 승인). 이 파일을 승인된 것처럼 인용하지 않는다.

메타 — 발의자: Ted(축 ① 「브랜치 전략이 없네? 어떻게 하는게 좋을까?」 → 권고안에 「좋아 intent에 얹어」) ＋ agent(축 ② R-C 후속 묶음) · 작성 2026-09-08 · 승인 **미승인**

- 축 ① 근거: `dev-package/sessions/R-C-ROUND-20260908.md` §9(창 9 발견 · 〈378〉 ⑧) · `03-HANDOFF.md` 「⑷ `w9-dev-deploy-rebased` 처리」.
- 축 ② 근거: `R-C-ROUND-20260908.md` §7·§9 · `R-C-VERIFY-20260908.md`(미달 3 · 초과 2).
- 결정 번호 〈N〉 은 병합 직전 재실측(현 최대 378).

## 문제

**축 ① 브랜치 전략이 문서에 없다**
- 관행만 있다 — `main` 보호·ff-only, 라운드마다 `integration/r-*`, WU 마다 `lane/wu-*` 워크트리, 레인 → 통합은 rebase＋ff. 어느 문서도 이것을 규칙으로 적지 않았다(`grep -rn "브랜치 전략\|branching" CLAUDE.md docs/` = 0건).
- 그 빈자리에서 사고가 났다 — 창 9(2026-09-06)가 `main` 밖 레인 sha `20b3715`(`integration/w9-dev-deploy`)를 dev 에 배포했고, 그 브랜치의 ai 마이그레이션 `0006_topic_vocab_six` 가 dev 에만 적용된 채 남았다. R-C 배포 창에서 형제 `0006_rc7_synonym_category` 와 부딪혀 `migrate-ai` 직전 STOP → WU-C13 `0007_merge` 로 해소(〈378〉 ⑧).
- 배포 도구가 브랜치를 보지 않는다 — `infra/dev/ship.sh:16` 은 `dist/colab-v2-dev.sha` 만 읽고, `infra/dev/build.sh` 는 「그 워킹트리의 HEAD」를 굽는다. EC2 에는 git 이 없어 호스트 쪽 검사도 없다.
- 레인 브랜치가 정리되지 않는다 — 실측 `git branch --list`: `integration/r-a2` · `r-b` · `r-c` · `w9-dev-deploy` · `lane/wu-c1·c2·c3·c4·c5·c7·c8·c9·c10·c11·c13` · `plan/r-c-0908` · `w9-rebase` = **18개**(전부 `main` 에 들어갔거나 판정 대기). 살아남은 브랜치가 「책에 없는 층」의 씨앗이다.

**축 ② R-C 가 남긴 후속 9건**(소유 WU 없음)
- 마이그레이션 축자 로그 0 — `db/ai/alembic.ini:37-39`·`db/platform/alembic.ini:37-39` `[logger_alembic] level = INFO` 이나 `handlers =` 가 비어 있어 `Running upgrade` 줄이 어디에도 안 남는다(staging-deploy-rc2.log 실측 · 사후 DB 델타로 대신했다).
- `frontend/src/components/project/project.css:436` `margin: -6px 0 14px` — 음수 상쇄 4건째(R-B §5-47 판정 밖 · C11 잔여).
- `frontend/src/components/upload/upload.css` 자식 `margin-top` 5곳(`:348 .partrow` · `:357 .up-analyze` · `:384 .fieldnote` · `:388 .ad-more` · `:424 .dr-foot`) — R-B §5-49 지목 밖(후속 WU 유래).
- AI 계보 제안 부모 Lv — 계약 `parentProcessingLevel` ＋ `d10_suggestion.py:61,88-91,119` 직렬화는 있으나 **값을 채우는 생산자 0**(C9 `[미상]`).
- 축척 사다리 5단(150·400·1,200·3,000·10,000 km)은 문서 bbox ＋ 픽스처 11건으로 정했다 — staging 실물 `bounds` 분포 미측(C4 `[미상]` · dev 데이터 14건이라 실증 아님).
- 창 9 나머지 2커밋 — `git log main..w9-rebase` = `80aeb00`(트리거 스풀 ＋ 매니페스트 원천 표기 · ai 6값은 C13 이 흡수) · `de1a5a2`(창 9 원장·보고) · `97f1d99`(결정 번호 재번호). 흡수/폐기 판정 없이는 브랜치를 못 지운다.
- `describe` 스모크가 `dev-package/notes/STAGING-WINDOW-RUNBOOK.md` §2 목록에 없다(예행 2회 모두 미실시).
- 시각 후보가 처음·마지막 둘뿐 — `contracts/seams/core-viz.yaml:487-495 InstantRange` 가 「목록을 통째로 내리지 않는다」로 설계됨(C3 ⓐ). 셋 이상 시각 중 하나를 고르려면 계약 개정(22차 후보).
- `granularity: null` 이 화면에서 도달 불가한 죽은 가지 — `RegisterArea.tsx:277,303,327`(C8 초과 · 최소 단위 셀렉트 철거의 결과).

## 원한 결과 (proposed outcome)

**축 ① 규칙 6개**(Ted 「좋아」 · 문면은 권고안 축자)
1. `main` 은 **유일한 배포 원천**. dev·prod 에 올라가는 sha 는 반드시 `origin/main` 의 조상. `ship.sh`·`up.sh`·staging `deploy.sh --target dev` 가 `git merge-base --is-ancestor` 로 검사하고 아니면 거절.
2. staging 은 예외 — `integration/*` HEAD 를 굽는다(리허설). 단 원장 행에 **브랜치 이름**을 같이 적는다.
3. `integration/r-N` 은 `main` tip 에서 따고, `main` 으로는 **ff-only 한 줄**. 병합 뒤 브랜치 삭제.
4. `lane/wu-*` 는 `integration` 에서 따고 rebase＋ff 로 돌아온다. 통합에 얹힌 즉시 삭제.
5. 마이그레이션은 **한 라운드 = 한 체인 구간**. 형제가 생기면 `00NN_merge` ＋ 두 순서 drift 오라클(0007 선례) 의무.
6. 릴리스 = 태그. dev 실적용 때 `dev-YYYYMMDD-N`, prod 는 기존 `prod-YYYYMMDD`. 〈N〉 행이 태그를 가리킨다.

**축 ① 산출물**
- `docs/BRANCHING.md` 신설(규칙 6 ＋ 브랜치 수명 표 ＋ 「하지 말 것」).
- `CLAUDE.md` **§10** 한 절(현 마지막 절 = `§9 업로드(S3)·배포` · `CLAUDE.md:149`) — 규칙 6 요지 ＋ `docs/BRANCHING.md` 링크.
- `infra/dev/ship.sh` 앞단 게이트 1 — `SHA` 를 읽는 `:16` 직후·scp 전에 로컬에서 `git -C "$REPO" fetch origin main && git merge-base --is-ancestor "$SHA" origin/main` 실패 시 `exit 65` ＋ 사유. EC2 에 git 이 없으므로 검사는 로컬.
- `infra/staging/deploy.sh:266` `ledger_append deploy … green "…"` 비고에 `브랜치=<git branch --show-current>` 필드 추가(`rollback.sh:135` 동형).
- `deploy_doctor` **15번째 항목** 「실행 sha ∈ main」(`MARKS` `deploy_doctor.py:62` 확장) — 설계 갈래는 아래 트리.
- 브랜치 정리 — 삭제 목록 18개 중 `main` 조상 확인된 것 즉시 삭제, `w9-rebase`·`integration/w9-dev-deploy`·`w9-dev-deploy-rebased` 는 축 ② 창 9 판정 뒤.

**축 ② 후속 9건**
- (L) alembic 로거 — 두 `alembic.ini` `[logger_alembic] handlers = console` · 마이그레이터 컨테이너 stdout 이 `deploy.sh`/`up.sh` 로그에 남는다(예행 로그에 `Running upgrade` 줄 ≥1).
- (C) CSS 잔여 — `project.css:436` 음수 제거 · `upload.css` 5곳 컨테이너 gap 이관 · 대비·여백 시험 확장(C11 `css-residual-rc11.test.ts` 재사용).
- (A) AI 제안 생산자 — `d10_suggestion` 이 부모 후보의 사람 Lv(`processingLevelUserSet ?? processingLevel`)를 `parentProcessingLevel` 에 싣는다 · FE 충돌 경고가 실제로 뜬다(C9 시험의 「열쇠 없음 → 조용」 갈래가 「열쇠 있음」으로).
- (S) 사다리 재검 — staging 실물 `bounds` 분포(`d3_dataset` 전량) 실측 → 단 값 유지/조정 판정 · 사다리 상수 한 자리만 고친다.
- (W) 창 9 판정 — `80aeb00` 트리거 스풀·매니페스트 원천 표기의 흡수/폐기 · `de1a5a2`·`97f1d99` 는 원장 사본이라 폐기 후보 · 판정 뒤 브랜치 3개 삭제.
- (R) 런북 — STAGING-WINDOW-RUNBOOK §2 스모크에 `POST /preview-target-descriptions` 1건 추가.
- (I) 시각 목록 — `InstantRange` 유지(ⓐ) vs 목록 첨가(ⓑ · 22차) — 미결.
- (G) `granularity` 죽은 가지 — `RegisterArea.tsx` 의 `granularity` 분기 정리 또는 존치 근거 기록.

## 영향 범위
- 사용자 / 화면: (C) CSS · (A) 등록 ③ 충돌 경고 · (G) 등록 기간 칸. 나머지는 운영·문서.
- 서비스 · 스키마 · 계약: ai-service `d10_suggestion` 생산자(A) · `deploy_doctor.py`(15항목) · `ship.sh`·`deploy.sh` · `alembic.ini` ×2 · 계약은 (I)ⓑ 채택 시에만 `core-viz.yaml` 첨가 1.
- 계약 파괴 여부: **미결** — (I)ⓐ 면 아니오(계약 0) · (I)ⓑ 면 22차 ㉮ 첨가 전망(Ted 서명 필요).
- 스키마·마이그레이션: 0 전망((L) 은 로거 설정뿐).

## 제약
- `main` push·ff 병합은 Ted(규칙 3 자체가 그것을 정본화한다). 에이전트는 `integration/*`·`lane/*` 까지.
- 계약 동결 유지 — (I)ⓑ 를 고르면 22차 승인 요청 패키지(`sessions/R-D-C22-REQUEST-<날짜>.md`)가 `contracts/` 첫 수정보다 먼저.
- EC2 에 git 없음(`infra/dev/README.md`) — 호스트 쪽 「main 조상」 검사는 파일(`MAIN_SHA`)로만 가능.
- `deploy.sh:65` 워킹트리 0건 착수 조건과 충돌하는 산출물 금지(배포가 레포 파일을 고치지 않는다 · 〈297〉 선례).
- 원문 삭제 금지 — CLAUDE.md §9 · HANDOFF 원문은 덧붙인다.
- 결정 번호 〈N〉 은 병합 직전 재실측.

## 설계트리 (grill-me 전 — 갈래만 · A 는 비움)
- Q1 doctor 15항목 「실행 sha ∈ main」의 근거 파일 → ⓐ `ship.sh` 가 `origin/main` sha 를 `/opt/colab-v2/MAIN_SHA` 로 함께 실어 doctor 가 `CURRENT_SHA` 와 대조(「배포 시점 main」 · 사후 main 이 자라도 판정 불변) / ⓑ doctor 가 `--main-sha` 인자를 받아 로컬 실행자가 `git rev-parse origin/main` 을 넘김(항상 최신 · 실행자 책임) → A <미정>
- Q2 규칙 1 게이트의 위치 → ⓐ `ship.sh` 한 곳(반입 자체를 막는다) / ⓑ `ship.sh` ＋ `up.sh`(호스트가 `MAIN_SHA` 대조 · 이중) → A <미정>
- Q3 태그 명명 → ⓐ `dev-YYYYMMDD-N` · `prod-YYYYMMDD`(권고) / ⓑ `dev-<sha12>` 만(이미지 태그와 동형) → A <미정>
- Q4 창 9 `80aeb00` 잔여(트리거 스풀·매니페스트 원천 표기) → ⓐ 흡수(별 WU) / ⓑ 폐기(dev 실적용분은 남지만 정본에 안 넣음 — 드리프트 존치) / ⓒ 실측 후 판정 → A <미정>
- Q5 (I) 시각 목록 → ⓐ `InstantRange` 유지 · 처음·마지막 둘 / ⓑ `instants: Timestamp[]`(상한 N) 첨가 · 22차 / ⓒ 서버 기본값＋자유 입력 → A <미정>
- Q6 R-D 크기 → ⓐ 축 ①＋② 한 라운드(WU 8) / ⓑ 축 ① 먼저(WU-D1~D4 · 계약 0) 후 축 ② 를 R-E / ⓒ 축 ① 만 → A <미정>
- Q7 브랜치 삭제 시점 → ⓐ 즉시(main 조상 확인분 15개) / ⓑ R-D 병합 시 일괄 → A <미정>
- Q8 (G) `granularity` → ⓐ 분기 제거 / ⓑ 존치(근거 기록) → A <미정>

**WU 스케치(확정은 spec)** — WU-D1 `BRANCHING.md`＋`CLAUDE.md §10` · WU-D2 `ship.sh` 게이트＋`deploy.sh` 원장 브랜치 필드(＋Q2ⓑ 면 `up.sh`) · WU-D3 doctor 15항목 · WU-D4 브랜치 정리(Q4 뒤) · WU-D5 alembic 로거 · WU-D6 CSS 잔여 2 · WU-D7 사다리 재검＋describe 스모크 · WU-D8 AI 제안 생산자(＋Q5ⓑ 면 22차). 예상 8.

## 미해결 질문
1. Q1 doctor 근거 파일 ⓐ/ⓑ.
2. Q3 태그 명명.
3. Q4 창 9 `80aeb00` 잔여 흡수/폐기.
4. Q5 시각 목록 — 계약 22차를 열 것인가.
5. Q6 R-D 크기 — 축 ① 만 먼저 갈지.
6. Q7 브랜치 삭제 시점 · Q8 `granularity`.
7. 규칙 6 태그를 「dev 실적용마다」 찍을지 「doctor 14/14 뒤」에만 찍을지.

## 범위 밖 (명시 제외)
- git-flow 식 `develop`·`release/*` 계층 — 사람 하나＋에이전트 레인 구조에서 층만 늘고, 이번 사고 원인(「책에 없는 층」)은 층수와 무관.
- prod 배포 재개(⏸ 유지 · `〈334〉`-㉳).
- 마이그레이션 되돌림 정책 변경(forward-only 유지).
- staging 원장 형식 전면 개정(필드 1개 추가만).

## 확인
⛔ Ted 가 채워 커밋하기 전에는 정본이 아니다(커밋이 곧 승인).
- 프론티어 공집합 확인: <미실시 · grill-me 대기 · Q1~Q8>
- 발의 원문(2026-09-08): 「브랜치 전략이 없네? 어떻게 하는게 좋을까? 브랜치전략」 → 권고안 6규칙 → 「좋아 intent에 얹어」
- Ted 확인 문장(원문 그대로): "<...>"
- 계약 동결 해제 22차: <Q5 판정 뒤 · ⓑ 면 요청 패키지 선행>
- 재개봉 금지: 예 — R-C 판정 전건(축 ① 16 · R-B 61 · 21차) · 규칙 6 문면은 Ted 「좋아」로 수용됐으나 세부(Q1~Q8)는 열림.

## 참조
- `dev-package/sessions/R-C-ROUND-20260908.md` §7·§9 · `R-C-VERIFY-20260908.md` · `WU-C13-ai-merge-20260908.md`
- `dev-package/03-HANDOFF.md` R-C 마감 증보(「남은 것」) · `PLAN-SoT §9 〈378〉`
- 코드: `infra/dev/ship.sh:16-25` · `infra/dev/build.sh` · `infra/staging/deploy.sh:266` · `infra/staging/rollback.sh:135` · `services/core-api/ops/deploy_doctor.py:62,736` · `db/ai/alembic.ini:37-39` · `db/platform/alembic.ini:37-39` · `contracts/seams/core-viz.yaml:487-495` · `services/ai-service/src/colab_ai/domains/d10_suggestion.py:61,88-91,119` · `frontend/src/components/project/project.css:436` · `frontend/src/components/upload/upload.css:348,357,384,388,424` · `frontend/src/components/upload/RegisterArea.tsx:277,303,327`
- 브랜치 실측: `git branch --list` 18개 · `git log main..w9-rebase` 3커밋
- ELI5 브리프: `eli5/2026-09-08/w9-ai-merge-eli5-20260908.html`
- spec: `dev-package/prd/specs/R-D.md`(작성 예정) · 라운드 파일: `dev-package/prd/rounds/R-D-*.md`(작성 예정) · 결정: 〈N〉(병합 시 기입 · 현 최대 378)
