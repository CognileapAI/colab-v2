# Ted 판정 대기 전수 목록 (2026-08-27 · 읽기 전용 스윕)

> ## ⚠ **2026-08-27 (2 차) — 「열린 것은 `I3` 7 건뿐」이라던 앞 머리말은 사실과 달랐다.**
> **이 스윕이 놓친 판정이 둘 더 있었다.**
> ⓐ **`§1.5` 정의의 여섯**(`P6`·`P7`·`P8`·`K3`·`K5`·자동 무효화)**이 순서표에 없던 것** — `sessions/STAGE2-READY.md §6-B1` 이 잡았다. **stage 2 착수를 막는 판정이었다.**
> ⓑ **블로커 `#22`**(D2·D8 잔존 고아 행) — 행 본문이 「판정 필요」로 열려 있는데 **이 문서가 세지 않았다.**
> **둘 다 2026-08-27 에 Ted 가 판정했다 — `PLAN-SoT §9 〈167〉`**(아래 `§A` 에 행으로 세웠다).
>
> **지금 열려 있는 것 = `§A` 의 `I3` `[정본 무근거]` 7 건 하나다.** ⚠ **그리고 그 하나의 성격이 바뀌었다** —
> `I3`·`I4` 가 stage 2 경로에 들어오면서(`〈167〉-㉲`) **7 건이 stage 2 차단 요인**이 됐다. 종전에는 「stage 2 목록 밖」이었다.
> ⚠ **`§B`·`§C` 에 취소선 없이 남은 줄들은 「열린 판정」이 아니라 「소유 WU 가 정해진 이연」이거나 「낡은 표기」다** — 다시 열지 않는다.
>
> ／ 이전 머리말 — ~~이 문서에서 아직 열려 있는 것은 `§A` 의 `I3` `[정본 무근거]` 7 건 하나뿐이다.~~
> `§A` 다섯 = `〈163〉` · `§B` 넷 = `〈164〉` · `§B` 나머지 넷과 `§C` 여섯 · `§D` 여덟 = `〈165〉` 회차에 집행됐다.

> **판정과 근거는 `PLAN-SoT §9` 소관이다. 여기는 「무엇이 아직 안 정해졌나」의 목록뿐이다.**
> 스윕 범위 = `03-HANDOFF §4·§4.5·§5.5` · `PLAN-SoT §9 확정/열린 것` · `WORK-UNITS §10~§10.3` ·
> `sessions/{X2-FREEZE-PROTOCOL-DRAFT, R1-RESTORE-DRAFT, C-GRID-ONLY-REVIEW, STAGE2-PREP, I3}.md` ·
> 전체 grep(`[정본 무근거]`·`Ted 판정`·`미판정`·`판정 사안`·`유보`·`보류`·`[미확인]`).
> 최근 마감분(`〈112〉`·`〈118〉`·`〈137〉`·`〈138〉`·`〈145〉`·`〈151〉`~`〈161〉`)은 **마지막 기재 기준**으로 대조했다.

---

## A. stage 2 착수를 실제로 막는 판정

> **2026-08-27 — 다섯이 닫혔다** (`PLAN-SoT §9 〈163〉`). **남은 것은 `I3` `[정본 무근거]` 7건 하나다.**

| 항목 | 어디에 있나 | 무엇을 정해야 하나 | 정하지 않으면 막히는 것 | 기본값 (무응답 시) | 긴급도 |
|---|---|---|---|---|---|
| ~~**X-2 ㉮ 자동 허용 등급을 여는가**~~ | `sessions/X2-FREEZE-PROTOCOL-DRAFT.md:149` | 「㉮ 자동 허용을 여는가 — 열지 않으면 모든 회차가 ㉯ 다(현행 유지). 열면 산문·승인 대기 비용이 사라지는 대신 「누가 언제 열었나」가 §9 등재에만 남는다」 | `X-2` 를 닫을 수 없고, `X-2` 는 `WORK-UNITS §10.1` 의 stage 2 진입 전 처리 넷 중 하나 | ✅ **판정 = 연다** — 3등급제 채택 · 「해제 횟수 상한」 폐기 (`〈163〉-㉮`) | ✅ **판정 완료** |
| ~~**X-2 회차 번호 중복 정정 방식**~~ | `sessions/X2-FREEZE-PROTOCOL-DRAFT.md:150` | 「회차 번호 중복(§1) 정정 방식 — `〈107〉` 을 6차로 재번호할 것인가, 「번호 갈라짐」을 사실로 남길 것인가」 | 같은 문서(`X-2`)의 §5-㉲ 「회차 번호는 §9 가 유일한 발급처」 규칙을 확정 못 함 | ✅ **판정 = `〈107〉` 을 6차로 재번호** — `〈151〉` 은 7차 · **§9 발급 항목 번호는 불변** (`〈163〉-㉯`) | ✅ **판정 완료** |
| ~~**X-2 ㉰-4 소급 전수 확인을 WU 로 여는가**~~ | `sessions/X2-FREEZE-PROTOCOL-DRAFT.md:151` | 「㉰-4(집행 없는 신설 금지)의 소급 — 지금 계약에 남은 미집행 선언이 더 있는지 전수 확인을 WU 로 열 것인가」 | 새 WU 1건의 존폐. `〈151〉`(`202` 철회)이 같은 계열의 마지막 사례였다 | ✅ **판정 = 연다 — 새 WU `X-5`** (`〈163〉-㉰`) | ✅ **판정 완료** |
| ~~**X-2 `seam-consistency` G-a·G-c·G-d 구현 시점**~~ | `sessions/X2-FREEZE-PROTOCOL-DRAFT.md:152` | 「stage 2 안인가, 이후인가」 | stage 2 WU 목록(`WORK-UNITS §10.2`)의 확정. 게이트가 못 잡는 6종이 계속 사람 판정에 남는다 | ✅ **판정 = stage 2 이후** — `§10.2` 목록 밖 (`〈163〉-㉱`) | ✅ **판정 완료** |
| ~~**`R-1` 비밀 7종의 백업·보관 정책**~~ | `sessions/R1-RESTORE-DRAFT.md:261` | 비밀 7종(`credentials.json`·`subjects.json`·`*_DB_URL_FILE` 5·env)을 백업할 것인가 — 백업하면 「비밀의 사본이 하나 더」다. 문서가 **정본 판정 사안**으로 표기 | `R-1` 완료 정의. `R-1` 은 `X-1`·`X-4` 재기동의 전제조건(`PLAN-SoT §9 〈158〉-㉯`)이고 그 셋이 `WORK-UNITS §10.3` 1단 전부 | ✅ **판정 = 백업하지 않는다** — 재발급 절차를 `R-1` 완료 정의에 명시 (`〈163〉-㉲`) | ✅ **판정 완료** |
| **I3 `[정본 무근거]` 7건** | `sessions/I3.md:84·197·215·229·249·261·277` | ① push 알림 채널(메일·메신저) ② 폴링 주기 값 · 워처를 systemd 인가 cron 인가 ③ 레지스트리 사용 여부 · 이미지 보존 개수 ④ 배포 원장 파일 형식 · 보존 개수 ⑤ 「이미지 롤백 시 스키마는 그대로 둔다」를 규칙으로 삼는가 ⑥ 배포 실패 시 직전 green 릴리스로 **자동** 롤백하는가 ⑦ 승인 기록의 보관 위치·형식·승인자 표기 | `I3`(배포 자동화) 착수. `03-HANDOFF:101` 이 `I3 ⬜` 이고 `DR-4`·`DR-5`·`DR-6`(SHA 재현성·직전 이미지 소실·헬스 없는 `exit 0`)의 소유 WU 가 전부 `I3` 다 | 세션이 값을 정하고 근거를 적는다(`I3.md:303` — 정본이 값을 안 주면 만들지 않고 보고) — **즉 착수 시점에 다시 물어야 한다** | **막힘** |
| ~~**`§1.5` 의 여섯을 stage 2 순서표에 넣는가**~~ | `WORK-UNITS §1.5` vs `§10.2`·`§10.3` · `sessions/STAGE2-READY.md §6-B1` | `P6`·`P7`·`P8`·`K3`·`K5`·자동 무효화가 stage 2 정의에는 있는데 순서표에 없다 — **stage 2 의 끝이 정의되지 않는다** | stage 2 계획 전부 | ✅ **판정 = 넣는다**(`〈167〉-㉮`) — `§1.5` 는 고치지 않고 순서표를 고쳤다 | ✅ **판정 완료** |
| ~~**블로커 `#22` D2·D8 잔존 고아 행**~~ | `03-HANDOFF §4` `#22` | 픽스처 잔재인 D2·D8 고아 행을 어떻게 할 것인가 | `R-1` 1단의 기준선 정합 | ✅ **판정 = 지운다**(`〈167〉-㉰`) — 전범위 백업 직후 · `X-1` 이전 · 사후 판정은 **「고아 0 실측」** | ✅ **판정 완료 · 실행 대기** |
| ~~**`PA-G` 를 지금 여는가**~~ | `WORK-UNITS §7 PA-G` | 교체 기한 「stage 1 완료 판정과 동시」가 발동했는가 | 정본이 적은 유일 로그인 수단의 부재 | ✅ **판정 = 연다**(`〈167〉-㉯`) — ⚠ **발동 근거는 약하다**고 판정문에 적었다 | ✅ **판정 완료** |

---

## B. 막지는 않지만 미루면 비싸지는 판정

> **2026-08-27 — 넷이 닫혔다** (`PLAN-SoT §9 〈164〉`). 남은 넷은 **소유 WU 가 정해진 이연**이고 열린 판정이 아니다.

| 항목 | 어디에 있나 | 무엇을 정해야 하나 | 정하지 않으면 막히는 것 | 기본값 | 긴급도 |
|---|---|---|---|---|---|
| ~~**온톨로지 그래프의 범위·충분성**~~ | `03-HANDOFF.md:225`(블로커 `#12`) | `d9_concept_edge` 19행으로 stage 1 의 「온톨로지 기반」을 충족한 것으로 볼 것인가, 어휘·관계를 더 정할 것인가 | 검색 품질(`T-1` 과 맞물린다). 착수는 안 막는다 — `〈157〉` 로 stage 2 몫이 됐다 | ✅ **판정 = `T-1` 과 묶어 한 번에 확장한다** (`〈164〉-㉮`) — 재생성이 1 회로 끝난다. 블로커 `#12` 해소 | ✅ **판정 완료** |
| ~~**검색 평가셋 작성 소유자**~~ | `03-HANDOFF.md:226`(블로커 `#13`) | 15~20 질의를 원천 문서에서 유래시켜 **검색 레인이 아닌 소유자**가 쓰게 할 것인가 | `W5`(검색) **완료 판정**만 막는다. 착수는 안 막는다 | ✅ **판정 = 검색 레인이 원천 문서에서 초안 · Ted 검수** (`〈164〉-㉯`) — 블로커 `#13` 해소 · `W5` 완료 판정 차단 해제 | ✅ **판정 완료** |
| **접근 승인 「회수」 규칙** | `PLAN-SoT.md:473`(열린 것 ⑭) | 만료 6개월 외에 조기 회수 경로를 둘 것인가 | `P6` | 만료만 — 조기 회수 없음 | 안 막힘 |
| **계보 그래프 한계선(B-07) · 보조입력 부모 표기(B-09)** | `PLAN-SoT.md:474`(열린 것 ⑮) | 그래프 깊이·보조입력 부모의 표기 규칙 | `P3` — 실데이터가 있어야 정할 수 있다 | 미정 — `P3` 착수 때 다시 묻는다 | 안 막힘 |
| ~~**`〈72〉` 「주제 빈 상태」 증명 수단**~~ | `PLAN-SoT.md:475`(열린 것 ⑳-b) | ⓐ 픽스처 / ⓑ stage 2 재판정 / ⓒ 실사용자 업로드 중 하나 | `〈72〉` 완료 정의 1항 · 평가셋 「주제 NULL」 오라클 | ✅ **판정 = ⓐ 픽스처** (`〈164〉-㉰`) — 주제 `NULL` 데이터셋 픽스처 1 건을 시험으로 고정한다 | ✅ **판정 완료** |
| **AWS 토폴로지·리전·계정 구조** | `PLAN-SoT.md:470`(열린 것 ⑪) | prod 를 열 때의 계정 구조 | `I1` 이후 prod 전부 | `㊻` 로 **출시 결정에 종속** — staging 은 이 결정 없이 완결된다 | 안 막힘(보류 확정) |
| ~~**`DR-16`·`DR-18` 계약 산문 개정 창**~~ | `03-HANDOFF.md:281`·`:282` | 「기준 격자 0~1건」 산문(`core-pipeline.json:39`)과 `FileKind.description` 의 `grid_axis` 표기를 **어느 회차에** 고칠 것인가 | 없음 — 구조 제약이 아니라 산문. 이벤트 계약 개정 창이 드물어 놓치면 다음 해제까지 못 싣는다 | ✅ **판정 = ㉮ 자동 허용 · 다음 회차에 즉시 싣는다** (`〈164〉-㉱`) — **등급제 첫 집행 사례** | ✅ **판정 완료** |
| **`DR-17` 격자 축 판별 방식** | `03-HANDOFF.md:283` | 서버가 파일에서 축을 판별하는 안을 채택할 것인가(채택 조건 넷 충족 시) | `P2`(`P2-EXEC` W0-1) — 이미 ✅ 로 지나갔으므로 지금은 stage 2 `D5` 로 넘어간다 | 채택 조건 넷을 못 채우면 수동 지정 폴백 | 안 막힘 |

---

## C. 문서 정합성 판정 (번호 정정 · 기재 오류 · `[미확인]`)

> **2026-08-27 — 여섯 전부 집행됐다** (`PLAN-SoT §9 〈165〉` 회차). `§11` 완주 체크리스트 정정 · `§10` 표 갱신 · 블로커 `#18` 해소 ·
> `R-1` `public` 밖 객체 **해소·없음** · 터널·DNS·terraform state **범위 밖 확정** · **이미지 digest 대장 신설**(`reference/IMAGE-DIGESTS.md`).
> **남은 열린 항목 0.**

| 항목 | 어디에 있나 | 무엇을 정해야 하나 | 정하지 않으면 막히는 것 | 기본값 | 긴급도 |
|---|---|---|---|---|---|
| **`WORK-UNITS §11` 완주 체크리스트가 전부 ✅ 다** | `WORK-UNITS.md:416`·`:417`·`:419` | `P3`~`P8`·`K3`~`K5`·`I3`·`I4` 가 ✅ 로 적혀 있는데 `03-HANDOFF §1` 은 `I3 ⬜`(`:101`)·`I4 ⬜`(`:102`) 다. 어느 쪽이 값인가 | 없음 — 그러나 「v2 가 끝났다」를 문서가 잘못 말한다 | 그대로 두면 다음 세션이 완주로 오독한다 | 안 막힘(정정 필요) |
| **`WORK-UNITS §10` 의 `D2c`·`I3`·`IS4`·`C1` 행이 낡았다** | `WORK-UNITS.md:352`·`:354`·`:356`·`:361` | `D2c` 는 `DR-7` 이 ✅ 이행됐고(`03-HANDOFF:294`) `I3` 행의 「Ted 답 대기」는 A 표로 옮겨야 한다 — 표를 갱신할 것인가 | 없음 | 그대로 두면 「다음 WU = D2c」로 읽힌다(실제 다음 WU 는 `R-1`) | 안 막힘(정정 필요) |
| **블로커 `#18` staging 실배포 `[미확인]`** | `03-HANDOFF.md:231` | 표기를 해소로 바꿀 것인가 — **정할 체크** = `docker ps` 로 `colab_v2_staging_*` 8개 ＋ `/healthz/<unit>` 5개 200 ＋ 데이터셋 12·파일 129 실측(`§4.5` 4·5번). `〈149〉`·`〈159〉` 가 이미 그 값들을 실측했다 | 없음 | `[미확인]` 유지 | 안 막힘 |
| **`R-1` `public` 밖 객체 유무 `[미확인]`** | `sessions/R1-RESTORE-DRAFT.md:184`·`:263` | **정할 체크** = staging pg 에 `\dn` ＋ `SELECT * FROM pg_ts_config WHERE cfgname NOT IN (기본)` 1회 실행 | `R-1` 리허설 1회차의 첫 측정 항목 | 리허설 때 잰다 | 안 막힘 |
| **`R-1` 터널·DNS·terraform state 소실 시나리오 `[미확인]`** | `sessions/R1-RESTORE-DRAFT.md:86` | 호스트 소실 시 복원 범위에 넣을 것인가 — **정할 체크** = `IS2` 의 `terraform plan` 을 빈 state 에서 재현(이미 `IS4` 가 부분 리허설했다) | 없음 — `R-1` 범위 밖으로 명시돼 있다 | 범위 밖 유지 | 안 막힘 |
| **`R-1` 이미지 digest 대장 부재** | `sessions/R1-RESTORE-DRAFT.md:262` | `:i2` 가 움직이는 태그인데 digest 기록 파일이 레포에 없다 — 대장을 만들 것인가 | `WORK-UNITS §10.3` 1단의 검증 항목 「이미지 digest 일치」 | 대장 없이 재기동 직전에 한 번 기록 | 안 막힘 |

---

## D. 이미 판정됐는데 「열림」으로 적혀 있는 것 (= 지울 것)

> **2026-08-27 — 여덟 전부 걷었다** (`PLAN-SoT §9 〈165〉` 회차). ⭑ 그중 **`〈120〉` 3 건은 앞 회차가 이미 고쳐 둔 상태였다**(`03-HANDOFF §3.5` · `WORK-UNITS §10.3` 둘 다 ✅).
> **남은 열린 항목 0.**

| 항목 | 어디에 있나 | 무엇이 이미 정해졌나 | 막는 것 | 기본값 | 긴급도 |
|---|---|---|---|---|---|
| **`〈120〉` 격자 전용 업로드 3건 ⬜ 미이행** | `03-HANDOFF.md:196` · `WORK-UNITS.md:401`(2단 주석) | **`〈145〉` 가 ⓒ 로 닫았다** — 「그대로 둬」(Ted 2026-08-27). 할 일이 없다 | 없음 | 행을 ✅ 로 고친다 | 지울 것 |
| **`§4.5` 진입조건 6 「Ted 판정 5건이 도착했는가」** | `03-HANDOFF.md:269` | 다섯 전부 판정됨 — ①`Q-01`·`Q-04` = `〈118〉`·`〈137〉` ②검색 비결정성 = `〈112〉` ③`D-16` 이름 = `〈111〉` ④간선 `D-09`→`D-16` = `〈159〉`(이미 그어져 있었다) ⑤`#35` = `〈138〉`. 대조표 = `sessions/STAGE2-PREP.md:16`~`:19` | 없음 | 진입조건 행을 삭제한다 | 지울 것 |
| **블로커 `#8` 터널 ID 잔존** | `03-HANDOFF.md:222` | **`〈156〉-㉲`** — 「그대로 둔다」(Ted 2026-08-27). 판정 완료 | 없음 | ⛔ 표기 잔재 정리 | 지울 것 |
| **블로커 `#33` 검색 red** | `03-HANDOFF.md:246` | `〈136〉`·`〈137〉`·`〈142〉`·`〈143〉` 로 **판정 완료 · 의도적 red 유지** | 없음 | 🟧 → ✅(판정 완료)로 표기 | 지울 것 |
| **`STAGE2-PREP §1` 8·9번 · `§2` 2단 B·C** | `sessions/STAGE2-PREP.md:19`·`:20`·`:62`·`:66` | `〈159〉`·`〈160〉` 으로 **이미 집행돼 있었다** — 잴 것이 없다. 문서에 이미 취소선이 있으나 `WORK-UNITS §10.3` 의 순서표에도 같은 주석이 남아 있다 | 없음 | 그대로 두면 다음 회차가 같은 것을 다시 확인한다 | 지울 것 |
| **`WORK-UNITS §10.1` X-3** | `WORK-UNITS.md:375` | `〈155〉` 로 닫힘(12/12 · 204 · `d6_project_dataset` 0→12) | 없음 | 취소선 유지 · 표에서 내린다 | 지울 것 |
| **`PLAN-SoT` 열린 것 ⑧⑨⑩⑫⑬⑯⑰⑱⑲-b⑳** | `PLAN-SoT.md:467`~`:480` | 열 항목 전부 **해소**로 본문에 적혀 있는데 「열린 것」 표에 남아 있다 | 없음 | 표에서 내리고 확정 로그로 옮긴다 | 지울 것 |
| **블로커 `#21` `202` 저장 자리** | `03-HANDOFF.md:213`(머리말) | `〈151〉` 로 **Ted 판정 = 철회**(6차 동결 해제). 머리말이 아직 「Ted 의 판정이 있어야 움직인다」로 남아 있다 | 없음 | 머리말 문장을 정정한다 | 지울 것 |

---

*작성 2026-08-27. 읽기 전용 스윕의 산출이고 어떤 파일도 고치지 않았다 — 판정은 `PLAN-SoT §9`.*
