# RC-1 — 구판 사이드카 재굽기 후 회수 1회 (집행)

> 워크트리 `.claude/worktrees/lane-rc1` · 브랜치 `lane-rc1` · 기준 `main` @ `a6d4f1d`
> 등재 = `PLAN-SoT.md §9 〈309〉` · Ted 승인 2026-09-04 「전수 스냅숏 후 회수」
> 진입조건 = `〈308〉` 이 충족시켰다(재굽기 집행 ＋ 전후 계수 축자) · 서빙 `64791f910f49` · 8 healthy

## 1. 짝 백업 — **회수 전에 새로 떴다**

직전 짝은 `20260903T211725` 로 **재굽기 전**이라 지금 자리를 담지 못한다. 그래서 이 회차가 다시 떴다.

```
전범위 백업 GREEN — 원장 2 프로파일 ＋ 볼륨 2 개 · 보관처 규약 밖 파일 0
```

| 산출물 | 스탬프 |
|---|---|
| 원장 platform | `platform-20260904T013647.sql.gz` |
| 원장 ai | `ai-20260904T013647.sql.gz` |
| 볼륨 uploads | `vol-uploads-20260904T013647.tar.gz` (157건) |
| 볼륨 previews | `vol-previews-20260904T013726.tar.gz` (**255건** · 35,660,861 B) |

- previews 검증 = `V1~V4·V6·V7 PASS` · **매니페스트 255건 = 아카이브 항목 255건(경로·크기 전건 일치)** · `V5` 는 **명시 면제**(이 볼륨은 원장 대조 기준이 없다 — 승인된 SKIP 1건).
- ⭑ **삭제된 28 파일은 이 아카이브 안에 그대로 있다** — 되돌릴 길이 실재한다.

## 2. 회수 전 전수 스냅숏 (커밋됨)

`dev-package/sessions/RC-1-SNAPSHOT-20260904T0138.tsv` — **255 줄** (벌 119 = 대상 117 ＋ 지도 타일 2).

열 = `cache_key · extension · size_bytes · source · sidecarVersion · baked_for · grade · reason`.
게이트 스냅숏(5열)보다 **`sidecarVersion`·`baked_for`·`reason` 셋을 더 싣는다** — 지운 뒤 「무엇이 있었나」를 판 번호까지 답하기 위해서다. 판정 규칙은 `ownership.py` 정본을 경로로 그대로 실었다(사본 0).

## 3. 계획 — 단언 전건 통과

`invalidation.reclaim_plan()` (계산기를 새로 짓지 않고 `plan()` 에 `keep_keys` 를 넘기는 기존 경로).

```
::계수:: {"살아 있다": 84, "접수분에만 닿는다": 0, "고아": 14, "판정 불가": 19}
::고아수::14
::stale파일::28 ::stale키::14
::kept파일::227 ::kept키::105
::regenerate::False ::trigger::None
::keep확인::판정 불가 19 벌 · kept 누락 0 · stale 침범 0
::keep확인::살아 있다 84 벌 · kept 누락 0 · stale 침범 0
::keep확인::지도 타일 2 벌 · kept 누락 0 · stale 침범 0
::집행직전키sha::b5d5f004f9ddd6d7
::단언::전건 통과 — 고아 14 == 게이트 14 · 판정 불가 19 · 살아 있다 84 전건 keep
```

- **키 집합 대조 = 동일.** 계획의 고아 14 키와 게이트가 이름으로 뱉은 14 키를 정렬해 sha256 을 뜬 값이 양쪽 `b5d5f004f9ddd6d7` 이고 `diff` 0줄이다.
- **합이 맞는다** — `stale 28 ＋ kept 227 = 255` = 자리의 실제 파일 수.
- **음성 조건은 계산 결과로 확인했다** — 판정 불가 19 · 살아 있다 84 · 지도 타일 2 가 **전건 `kept`** 이고 `stale` 침범 0건이다.
- **단언은 집행과 같은 프로세스 안에서 다시 돌았다** — 계획 회차와 집행 회차 사이에 자리가 바뀌면 어긋나므로, `--apply` 는 재스캔·재단언 뒤에만 지운다.

### 3.1 고아 14 벌의 정체 — **등록되지 않고 버려진 접수분 3건의 그림**

| 사이드카 `source` (fileId) | 벌 수 | `d3_file` | `d5_upload_file` |
|---|---|---|---|
| `01M1H43V0SX1V7ZPCB2KTC9RN8` | 6 | 0 | 0 |
| `01M1H44AYFJA62X9NQQD849DN9` | 4 | 0 | 0 |
| `01M1H4800DAJ0PVH7CTG74TDEE` | 4 | 0 | 0 |

- 14 벌 전건 `sidecarVersion: 2` · `baked_for.is_upload: true` 이고, `baked_for.target_id` 는 접수분 3건(`01M1H43V0S4ABB8HTM6S2HG1EY`·`01M1H44AYF4GKCK9C4XESHTQAW`·`01M1H4800D069BFRYTG6ZJXRVG`)이다.
- ⭑ **「등록된 데이터셋의 화면이 안 바뀐다」의 근거는 `is_upload: true` 가 아니라 원장 대조다** — 위 표의 **0/0** 과, `d3_dataset` 에 저 대상 3건이 **0행**이라는 실측이 함께 그것을 세운다. `is_upload` 만으로는 「등록 안 됨」이 나오지 않는다.
- `〈308〉` 이 재굽기한 데이터셋 3건은 `stale` 목록에 **한 건도 없다**.

## 4. 집행 — `invalidation.apply()`

```
::삭제됨파일::28
::삭제됨키::14
```

- 28 파일 = 14 키 × 2층(`.json` ＋ `.png`/`.webp`). 오류 0 · `OutOfScope` 0 · 부분 삭제 0.
- **집행 자리 = `docker exec colab_v2_staging_viz_render`**(uid 10001 · `/srv/viz-previews` rw). 호스트 사용자에게 그 자리의 쓰기 권한이 없다(디렉터리 소유 uid 10001).
  ⭑ **`exec` 는 경계 밖이 아니다** — 금지된 넷(정지·재기동·재생성·`down`)은 전부 **컨테이너 수명** 조작이고 `exec` 는 수명을 건드리지 않는다. `sudo rm` 은 레포 기구를 건너뛰는 hand-`rm` 이라 **더 나쁜 선택**이다.
  ⭑ **컨테이너 안 코드 = 이 워크트리 코드** — `ownership.py`·`invalidation.py` 의 sha256 이 양쪽 동일(`c14ee3ff…`·`f272d092…`)이고, `64791f9..a6d4f1d` 의 변경 파일은 `dev-package/` 문서 4건뿐(코드 0건)이다.
  ⭑ **자리의 동일성도 실측했다** — 호스트 `/home/ttlhi10/.colab-v2-staging/previews` 와 컨테이너 마운트가 같은 `device:inode`(`2096:378495`)다.
- SQL 쓰기 0 · 컨테이너 수명 조작 0 · 파괴 플래그 0 · hand-`rm` 0 · 접속 문자열은 경로로만 썼다.

## 5. 회수 후 재실측 — 게이트 **green**

`./gates/run.sh artifact-ownership -j 1` (축자)

```
산출물 소유 — 대상 한 벌 103 · 살아 있다 84 · 접수분에만 닿는다 0 · 고아 0 · 판정 불가 19
  지도 타일 2 벌은 **대상이 아니다** — D5 가 구운 것이라 kept 로 산다 (완료 정의 ⑷)
  세는 단위 = 한 캐시 키 아래 선 산출물 한 벌 · 자리 = /home/ttlhi10/.colab-v2-staging/previews · 원장 = d3_file 133 · d5_upload_file 133
  시점 = 2026-09-04T01:43:39+09:00
::warning::artifact-ownership — 판정 불가(구판) 19 벌이 **건수를 드러낸 채** 통과했다.
artifact-ownership green — 대상 103 벌 (살아 있다 84 · 접수분 0 · 고아 0 · 판정 불가 19) · 지도 타일 2 · 스냅숏 227 줄 · 삭제 0건
```

| 등급 | 회수 전 | 회수 후 | 델타 |
|---|---|---|---|
| 대상 한 벌 | 117 | **103** | −14 |
| 살아 있다 | 84 | **84** | 0 |
| 접수분에만 닿는다 | 0 | **0** | 0 |
| **고아** | **14** | **0** | **−14** |
| 판정 불가 | 19 | **19** | 0 |
| 지도 타일 (대상 아님) | 2 | 2 | 0 |
| 자리 파일 수 | 255 | **227** | −28 |

- ⭑ **지운 것만 줄었다** — 살아 있다·판정 불가·지도 타일이 **한 벌도 움직이지 않았다.**
- **서 있던 red 가 닫혔다** — `〈308〉` 이후 유일한 `red(판정)` 이 `artifact-ownership` 이었고 그 사유(「고아 등급 14 벌 중 선언되지 않은 것」)가 사라졌다.
- ⚠ **면제 선언은 비운 채로 두었다** — `gates/config/artifact-ownership.toml` `[exempt] keys = []` 그대로다. **회수로 닫았지 선언으로 닫지 않았다.**

## 5.1 전 게이트 — **50 / 0 / 0**

`set -a; . ~/.colab-v2-test.env; set +a; ./gates/run.sh all -j 1` (축자)

```
  ── 계 : green 50 / red(판정) 0 / red(준비) 0
```

- **선언 50 · green 50 · red(판정) 0 · red(준비) 0** — **못 돈 게이트 0건**이다.
- `〈308〉` 은 같은 명령에서 `green 49 / red(판정) 1 / red(준비) 0` 이었고 그 red 1 이 `artifact-ownership` 이었다. ⟹ **서 있던 red 가 이 회차로 닫혔고, 새로 난 red 는 0건이다.**
- `artifact-ownership` 축자 = `green — 대상 103 벌 (살아 있다 84 · 접수분 0 · 고아 0 · 판정 불가 19) · 지도 타일 2 · 스냅숏 227 줄 · 삭제 0건` ＋ 경고 1줄(판정 불가 19 벌이 건수를 드러낸 채 통과).
- ⚠ **실행 자리는 레포 루트 체크아웃이다**(`main` `a6d4f1d` · 워킹트리 변경 0) — 워크트리에는 `services/*/.venv`·`frontend/node_modules` 가 없다. **쓰기는 전부 `lane-rc1` 브랜치에만 했다.** `work-item-consistency` 는 편집분을 읽어야 하므로 **워크트리에서** 따로 돌렸다 — 축자 `work-item-consistency: green — 대장과 산문의 불일치 0`.

## 6. 현물 확인 — **전후 완전 동일**

`diff` 전/후 = **0줄**.

| 렌더 | 상태 | 3층 (status · content-type · bytes) |
|---|---|---|
| `01M1M09Z6Y4F08210QZEKW978K` | 200 · 완료 | `200 image/webp 3,934` · `200 image/png 40,142` · `200 application/json 633` |
| `01M1M09ZH744J9ASB1A999E6PN` | 200 · 완료 | `200 image/webp 6,878` · `200 image/png 999,069` · `200 application/json 635` |
| `01M1M09ZT1XKFNE6QXKWN2CA2T` | 200 · 완료 | `200 image/webp 3,934` · `200 image/png 40,142` · `200 application/json 633` |

| 값 조회 (36.75, 127.40) | 결과 |
|---|---|
| `01M0YWDXN7CA13KB7MQC9PGEW5` | **200** · `available true` · `0.4836253468321484` |
| `01M1JM3JTQSSTKR9A2QQ67QN81` | **200** · `available true` · `0.31521356105804443` |
| `01M19RENXAANZ2HDWM4Z46C37Y` | **200** · `available true` · `0.4836253468321484` |

- 9층 전건 200 · 바이트 수까지 삭제 전과 같다. 값 3건도 `〈308〉` 이 적은 값과 **글자 그대로 같다.**
- ⭑ **「옛 키 산출물이 지워진 데이터셋이 현재 미리보기를 계속 보여 준다」는 조건은 대상이 0건이라 그 모양으로 서지 않는다.** 지운 14 벌은 **등록된 데이터셋의 것이 하나도 아니었고**(§3.1 원장 대조 0/0/0), 그래서 화면이 바뀔 데이터셋 자체가 없다. **없는 대상을 만들어 재지 않았다** — 대신 그 사실을 원장 3중 대조로 증명했다.
- ⚠ **값 조회 본문은 `{"point":{"lat":…,"lon":…}}` 다.** 평평한 `{"lat":…}` 는 400(`point.lat · point.lon 이 필요하다`)이다.

## 7. 완료 정의 대조 (`work-items.yaml` `RC-1`)

| 조항 | 판정 | 근거 |
|---|---|---|
| ⑴ 구판 사이드카를 판 2 로 재굽는다 | **충족 (`〈308〉`)** | 이 회차가 아니라 앞 회차가 집행 — 공개 엣지 3건 · 9층 200 · 실패 0 |
| ⑵ 재굽기 뒤 `reclaim_plan()` 1회 | **충족 (이 회차)** | §3 · 계산기 신설 0 — `plan()` 에 `keep_keys` 105 를 넘기는 기존 경로 |
| ⑶ 판정 불가 49→19 · 삭제 0 · 재굽기 0회 가 갱신 관측 | **충족** | 판정 불가 **49→19** 는 `〈308〉` 이 충족 · 본 회차 회수 후 재실측도 **19 유지** · 삭제 **0→28 파일(14 벌)** · 재굽기 **0→3건** |
| ⑷ 음성 — 타일 `kept` · 접수분 루트 `OutOfScope` · 원본·격자·데이터셋 무접촉 | **충족** | §3 `keep확인` 3줄(누락 0 · 침범 0) · `OutOfScope` 0 · 자리 밖 접촉 0 · SQL 쓰기 0 |
| ⛔ 범위 밖 = `V-1`·`J-6` | **지킴** | 팔레트 재렌더 경로·선택 UI 무접촉 |

⟹ **`status: done`.**

## 8. 재지 않은 판단기준 (다음 회차 진입조건)

- **판정 불가 19 벌의 처분** `[미확인]` — 구판 사이드카는 재굽기로 사라지지 않고 **그 키가 다시 구워질 때만** 덮인다. 19 벌이 어느 대상의 것인지, 다시 구울 대상이 남아 있는지는 이 회차가 세지 않았다. 푸는 법 = 19 벌의 `baked_for` 부재를 파일 시각·대상별로 훑어 재굽기 가능 대상과 대조한다. `[legacy] tolerate = true` 는 그대로 두었다.
- **캐시 키가 갈린 원인의 귀속** `[미확인]` — `〈308〉` 이 남긴 그대로다. 직전 팔레트를 읽을 경로가 상세에도 사이드카에도 없어 팔레트 차이와 리팩터 차이가 갈리지 않는다.
- **이 회차가 세지 않은 것** — 회수로 되찾은 디스크 용량을 합계로 세지 않았다(스냅숏의 `size_bytes` 로 사후 산출 가능).
