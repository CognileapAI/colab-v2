# DR-4 — dev 무인 재생성 실행 기록 (2026-09-14)

- 성격 = `dev-package/tools/dev-reseed/reseed.sh` 1회 실행의 **생성물**. 값의 원본은 `result.json` 이다.
- 승인 = dev 한정 상시 승인(`.claude/rules/deploy.md` 11번 증보 문단). 회차별 GO 불요 · 승인 기록 = `approval-record.json`.
- 실행 자리 = `dev-package/reports/dev-reseed-runs/20260914T020005Z` · 실행 식별자 `20260914T020005Z` · 배포 대상 sha `b955bece7384`.
- 결과 = **failed**(멈춘 단계 `s3`) · 총 소요 50초.

## 1. 단계별 소요

| 단계 | 상태 | 종료코드 | 소요(초) | 로그 |
|---|---|---|---|---|
| preflight | ok | 0 | 3 | `logs/preflight.log` |
| rehearse | skipped | — | — | `—` |
| deploy | skipped | — | — | `—` |
| reset | ok | 0 | 14 | `logs/reset.log` |
| bootstrap | ok | 0 | 12 | `logs/bootstrap.log` |
| up | ok | 0 | 20 | `logs/up.log` |
| s3 | failed | 1 | 1 | `logs/s3.log` |
| prelude | skipped | — | — | `—` |
| seed | skipped | — | — | `—` |
| verify | skipped | — | — | `—` |
| report | skipped | — | — | `—` |

## 2. 계수

- 계수 없음 — verify 단계를 돌지 않았다.

- `deploy_doctor` 요약줄 축자 = `항목 15 — ✓ 15 · ✗ 0 · ─ 0`

## 3. 미리보기 판정 표 (0 행 · 성립 0 · 판정불가 0)

| 순번 | 데이터셋 | 가공 단계 | 판정 | ms |
|---|---|---|---|---|
| — | — | — | — | — |

## 4. 차단 (1 건)

- `s3` · **s3-plan** — 계획 검토 미달

## 4-1. 자동 복구 (0 건)

- 0 건 — 정지 뒤 실패가 없었다.

## 5. 사람이 채울 자리

- 원장 등재문 · 대장 `DR-4` 상태 · `03-HANDOFF §1` 갱신은 오케스트레이터가 한다(레인·도구가 하지 않는다).
- 판정이 필요한 미성립 항목의 원인 분류(도구 결함 / 미리보기 뒷단 `PV-2`)를 여기에 적는다.
- **「판정불가」가 1건이라도 있으면 그 회차는 미리보기를 잰 것이 아니다** — 원인(브라우저 무응답 · 선택자 변경)을 먼저 적는다.

## 6. 실행 창 (KST) — 3회차 · 사람이 채운 자리 (`WU-C4` · 작업 사본 브랜치 `lane/wu-c4-run3` · 기점 `cd25c62e`)

| 구간 | 시각 | 소요 | 결과 |
|---|---|---|---|
| 0 · 자격·접속 확인 | 10:57 ~ 10:58 | — | `aws sts` 신원 = `colab-platform-s3-uploader-dev` · ssh `docker ps` 앱 4종 `:dev-b955bece7384` healthy · `agent-browser doctor` 13 pass / 0 fail · 러너 자격 파일 `install -m 600` |
| 1 · `--preflight-only --target-ref b955bece` | 10:59:16 ~ 10:59:21 | 5초 | **exit 0 · `preflight — 통과 10 · 미달 0`**(git · dev-sha · aws · qemu · leftovers · ref-root · agent-browser · secrets 9건 0600 · resources 18293 MiB/859 GiB · build-plan 28/18) |
| 1 · `--rehearse --target-ref b955bece` | 10:59:28 ~ 10:59:40 | 12초 | **exit 0 · `리허설 — 원시동작 10 · 통과 10 · 어긋남 0`** — psql_master_query `quoted\|0` · ssh_script 따옴표·`$`·백틱 되받기 · compose_ps 4 단위 · migrator platform `0031_search_evidence (head)` · ai `0007_merge_vocab_and_category (head) (mergepoint)` · s3-plan 키 1526 sha256 `2eb35e5d…` · psql_owner_url `1` · doctor `항목 15 — ✓ 15 · ✗ 0 · ─ 0` · runner report · agent-browser 제목 `Co-Lab` |
| 1 · `--dry-run --from reset --target-ref b955bece` | 10:59:40 | 1초 | exit 0 · 8단계 전건 code=0 |
| 2 · 실행 `--from reset --target-ref b955bece`(단일 프로세스 · `nohup setsid` · `--accounts-file` 없음) | 11:00:05 ~ 11:00:55 | **50초** | preflight ✓ 3초 → **reset ✓ 14초**(① 계수 · ①ᵃ 0 · ①′ 정지 · ①″ 질의 결과 0 · ② 두 체인 DROP/CREATE COMMIT) → **bootstrap ✓ 12초**(extensions · 두 체인 head · grants · 롤 접속 3/3) → **up ✓ 20초**(4 단위 healthy · 도구 안 `deploy_doctor` 1회 15/15) → **`s3` ✗ 1초 · exit 1** — §7 |
| 2″ · `deploy_doctor` 1회(도구 밖 · 정지 뒤) | 11:02 | — | `항목 15 — ✓ 15 · ✗ 0 · ─ 0` · `전 항목 통과 (─ 0 — 15 항목이 실제로 돌았다)` · ⑮ `b955bece7384 ∈ main` · 원문 `doctor-final.out`(실행 자리 · 추적 제외) · 비밀 패턴 0건 |
| 3 · 계정 단계 / 4 · UI 실사 | — | — | **미실행**(prelude·seed·verify 가 돌지 않았다 — 연구실·계정이 없어 로그인 자체가 성립하지 않는다) |

- 사람 입력 = **0회**(대화형 프롬프트 0 · BatchMode ssh · 러너 표준입력 대기 0). 도구 밖 사람 조작 = **0회**(앱 재기동은 `up` 단계가 했고 정지 뒤 복구 필요 없음 · `recovery.jsonl` 0건).
- 비밀값 검사 = 실행 자리 전 파일 ＋ 이 기록 `grep -E 'postgresql://…:…@|AKIA|PASSWORD='` **0건** · 러너 자격 파일은 정지 확정 뒤 `shred -u`(작업 자리 잔존 0).
- `--from` 재개 0회 · `s3` 재시도 0회 · 2차 실행 0회(지시 규칙).

## 7. 정지 지점 — `s3` ② 「계획 검토」 · 원인 = 도구 결함 1건(실물 결함 아님 · S3 삭제 0건)

- 순서 = ① `s3-plan` ✓(키 1526 건 · 멀티파트 0 · sha256 `2eb35e5db6c3ad6805bd6c42b1635a4e7bfc5d4ffbeefa8de89b37de1b19293a`) → **② 계획 검토 ✗** → ③ `s3-apply` **미실행** → ④ 실행 후 계수 미실행.
- 축자(`logs/s3.log`) = `AssertionError: 계획 파일 소유자가 실행자가 아니다`(`File "<stdin>", line 5`).
- 원인 = `dev-package/tools/dev-reseed/stages.sh` `reset_docker_cmd` 가 초기화 도구 컨테이너를 **`--user 0`** 으로 돌려(소유자 URL 파일이 uid 10001 0600 이라 필요 · DR-2 정정 ㉡) 호스트 `/tmp/colab-reseed-out/plan.json` 이 **uid 0 · 0600** 으로 써지는데, `stage_s3` ② 의 검토 `python3` 는 ssh 사용자(**uid 1000** · sudo 없음)로 돌며 `assert st.st_uid == os.getuid()` 를 건다. 두 uid 가 다르므로 **이 검사는 실모드에서 통과할 수 없다.** 실측 = `ls -ln /tmp/colab-reseed-out/` 4 파일 전건 `0 0` · `id -u` = 1000.
- 같은 검사가 도구 안에도 있다(`services/core-api/ops/reset_dev_environment.py` `st_uid != os.getuid()` · `s3-apply` 가 계획을 읽을 때) — 그쪽은 같은 `--user 0` 컨테이너 안이라 uid 0 = 0 으로 성립한다. 호스트 쪽 사본만 어긋난다.
- 이 경로는 **실모드로 돈 적이 없다** — `--rehearse` ⑸ 는 `s3-plan` 을 임시 폴더에 내고 지울 뿐 ② 검토 본문을 돌리지 않고, `--dry-run` 은 `DRY ②` 한 줄을 찍는다. `tests/`(`doctor-parse` · `preflight-red` · `preflight-secrets` · `remote-transport`) 에 `s3:review` 픽스처 **0건**. 2026-09-13 초기화(`DR-2-run-2026-09-13.md` §4)는 사람이 `s3-plan` 뒤 곧장 `s3-apply` 를 냈고 호스트 쪽 소유자 검토는 없었다.
- 고침 후보(코드 변경 · 이 회차 밖 · 후속 레인) = ⑴ ② 검토를 컨테이너 안(같은 `--user 0`)에서 돌리거나 `sudo python3` 로 내고 기대 소유자를 「도구가 쓴 uid(0)」로 명시한다 — 실행자 uid 와의 일치는 이 자리의 불변식이 아니다 ⑵ 또는 호스트 쪽 소유자 단언을 빼고 `_ops/` 0건·접두사·0600 만 남긴다(소유자 검사는 `s3-apply` 가 이미 한다) ⑶ `--rehearse` ⑸ 에 ② 검토 본문을 **같은 경로**로 얹는다(임시 폴더의 계획으로) ⑷ `tests/remote-transport.sh` 류에 픽스처 — uid 0 소유 0600 계획이 검토를 통과(green) · 0644 · `_ops/` 키는 red. `dev-reseed-selftest` 게이트에 얹는다.

## 8. 현재 dev 상태 (정지 뒤 · 2026-09-14 11:03 KST 기준)

- DB = **초기화됨**(`reset` ② COMMIT · `bootstrap` 완료). 실행 전 계수(연구실 경계 안 · 도구 축자 `count-before.json`) = platform 스키마 `['account_admin','public']` · 연구실 1 · `d3_dataset` **26** · `d3_file` **565** · `d6_project` **4** · `d4_lineage_edge` **18** · ai 스키마 `['public']`. 지금 = 두 체인 `DROP … CASCADE` → `CREATE SCHEMA public` → 마이그레이션 head(platform `0031_search_evidence` · ai `0007_merge_vocab_and_category`) · `pg_trgm` · 앱 롤 GRANT · `account_admin` 롤 ok · **연구실·계정·자격·서비스 운영자 0**(prelude 미실행). 행 계수 재측정 = `[미확인]`(푸는 법 = 다음 회차 `reset` ① 또는 `reset:count` 본문 읽기 전용 재실행).
- S3 = **무변**(`s3-apply` 미실행) — `uploads/` 1008 · `previews/` 518 객체 · 멀티파트 0 이 DB 참조 없이 남아 있다. `_ops/` 무접촉. 계획 파일 = 원격 `/tmp/colab-reseed-out/plan.json`(uid 0 · 0600 · 이 실행의 sha256) 잔존.
- 앱 = `up` 단계가 4 단위를 같은 compose·env 로 올렸다 → `docker ps` 4종 `Up (healthy)` · 이미지 `:dev-b955bece7384` 그대로 · `volume_init` Exited(0).
- `deploy_doctor` = 정지 뒤 도구 밖 **1회** `항목 15 — ✓ 15 · ✗ 0 · ─ 0`(§6). 도구 안 `up` 단계 1회까지 합쳐 이 회차 2회 실행 · 둘 다 15/15.
- 배포·태그 = 무변(`dev-20260914-2` · 1회차 실적용 그대로). staging·prod·`_ops/` 무접촉.
- 화면 = 로그인할 계정이 없다(연구실·계정 미제공). 이태헌·Ted 계정 포함 **모든 계정 부재** — 다음 회차 `prelude` 가 첫 계정을 심고 `accounts` 단계가 운영자 계정을 되만든다.

## 9. 이탈 — 지시 §4 ⑴~⑷

1. 배포 = 1회차에 release executor 로 선행 — 이 회차는 `--from reset` 이라 `deploy` skipped. 무변.
2. 계정 단계 = 별도 명령(`stage_seed` 가 `--accounts-password-file` 미전달) — **미도달**(seed 미실행). 운영자 계정 재생성 0건.
3. 사람 입력 = **0회**(§6). 도구 밖 사람 조작 0회(실패 경로 복구 불요 · 앱은 `up` 이 올린 채).
4. 「한 번의 실행으로」 = **불성립**. `reset → report` 가 한 프로세스에서 끝나지 않았다(`s3` 에서 exit 1). `--from` 재개 0회.

## 10. 다음 회차 진입조건

1. `stages.sh` `stage_s3` ② 소유자 단언 고침 ＋ 리허설 ⑸ 에 검토 본문 편입 ＋ 픽스처(§7 고침 후보 ⑴~⑷) — 별도 레인 · `dev-reseed-selftest` green.
2. 위 뒤 `--preflight-only` 10/10 · `--rehearse` 10/10 → **`--from reset --target-ref b955bece`**(dev 는 그 sha 그대로). `--from s3` 재개는 규칙상 하지 않는다 — DB 는 이미 비었고 S3 만 남았으나, 파괴 단계 재개는 사람 판정 자리다(오케스트레이터).
3. 재실행 전 dev 는 「빈 DB ＋ 옛 S3 객체 1526」 상태라 화면 로그인이 불가하다 — 이 상태를 두는 기간은 Ted 판정.
4. 계정 단계·UI 실사·계수표는 seed·verify 가 돈 회차에서 처음 나온다.
