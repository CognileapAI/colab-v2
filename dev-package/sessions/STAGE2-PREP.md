# stage 2 진입 준비 — Ted 컨펌 9건과 재검증 (2026-08-27)

> **판정과 근거는 `PLAN-SoT §9 〈155〉`·`〈156〉` 에 있다. 여기는 착수 순서다.**
> 두 곳에 같은 값을 적지 않는다.

---

## 1. 컨펌 9건 — 무엇이 정해졌나

| # | 항목 | 판정 | 집행 |
|---|---|---|---|
| 1 | `X-1` 슈퍼유저 `POSTGRES_PASSWORD` 회전 | **㈏ `X-4` 재기동에 동봉** | ⬜ |
| 2 | `X-2` 계약 동결 해제 프로토콜 | **㈎ 문서로 못 박는다** | ⬜ |
| 3 | `X-3` 프로젝트↔데이터셋 실연결 | **㈎ 정문으로** | ✅ `〈155〉` |
| 4 | `X-4` `_FILE` 배선 재기동 검증 | **㈎ 계획된 재기동** | ⬜ |
| 5 | `Q-01`·`Q-04` 되돌림 · `#35` | 이미 판정됨 (`〈118〉`·`〈137〉`·`〈138〉`) | — |
| 6 | 검색 비결정성 | 이미 판정됨 (`〈112〉` 4건) | — |
| 7 | `D-16` 이름 | 확정됨 (`〈111〉`) | — |
| 8 | 계보 간선 `D-09` → `D-16` | **㈎ 긋는다** | ✅ **이미 되어 있었다** (`〈159〉`) — 잴 것이 없었다 |
| 9 | 격자 전용 업로드 `upload.failed` | **㈎ 워커가 감지 대상에서 제외** | ✅ **이미 집행됨** (`〈114〉`·`4a8e41b`) — 지시하려던 한 줄은 **틀린 수정**이었다 (`〈160〉`) |

**미집행은 셋이다** — `X-1`·`X-2`·`X-4`(3 번 `X-3` 은 `〈155〉` 로 닫혔다). `X-1` 은 `X-4` 에 묶여 있다.
**5~9 번은 전부 판정·집행이 끝난 기록이다** — `§4.5` 진입조건이 이 표를 대조 근거로 가리킨다(`03-HANDOFF §4.5`).

## 2. 착수 순서 — **재기동이 맨 앞이다** (2026-08-27 개정 · `§9 〈158〉`)

> ⚠ **종전 초안은 재기동을 맨 뒤에 뒀다. 뒤집혔다.** 뒤에 두면 A·B·C 변경 **위에** 재기동 위험이
> 얹혀 조용한 롤백이 나면 **원인이 셋 중 무엇인지 뒤섞인다.** 단일 변수로 겪는 편이 싸다.

**1단 — 재기동 (단일 변수)**

1. **`R-1` 복원 절차** — 세우고 리허설한다. ⚠ **이것 없이 재기동하지 않는다.** 「백업은 있는데
   되돌리는 절차가 없다」가 그대로 모순이고, 미루면 그 사이 모든 재기동이 무보험이다
2. **전범위 백업** — `platform` DB **＋ `ai` DB(온톨로지 사전) ＋ 볼륨**. ⚠ **지난 사고가 정확히 `ai` 쪽이었다**
3. **`X-1` 회전 ＋ `X-4` 재기동**
4. **검증 = 헬스가 아니라 실측** (2026-08-27 재측정 · `§9 〈159〉-㉲`)

   | 값 | 기준선 |
   |---|---|
   | 데이터셋 | **12** |
   | 파일 | **129** |
   | 계보 간선 | **6** — 전건 확정. ⚠ 여러 문서의 「5」는 **틀린 값이다** |
   | `d6_project_dataset` | **12** |
   | 온톨로지 사전 | **non-`None`** (헬스 200 이 이것을 보증하지 않는다) |
   | 이미지 | **digest** 일치 — 대장 = `dev-package/reference/IMAGE-DIGESTS.md` (`§9 〈165〉-㉱`) |

**2단 — 남은 것은 `A` 와 `E` 둘이다**

> ⭑ **2026-08-27 — 「병렬 셋」이 아니다.** `B`·`C` 는 **이미 집행돼 있었다**(`§9 〈159〉`·`〈160〉`)고,
> `〈120〉` 3 건은 `〈145〉` 로 **닫혔다.** 아래 6·7 은 **집행 기록이지 할 일이 아니다** — 다시 재지 않는다.

5. **`A` 동결 해제 프로토콜** — 코드 무변경
6. ~~**`B` 계보 간선 `D-09` → `D-16`**~~ ✅ **할 것이 없다 — 이미 그어져 있다** (`§9 〈159〉`).
   실측 = `confirmed_at` 2026-08-26 12:14Z · `confirmed_by` 전창현(교수) · `D-16` 이 **Lv1 · 확정**.
   ⚠ **집행됐는데 `§9` 에 등재가 없었다.** 「할 일」로 적혀 있던 것이 이미 된 일이었다 — **잴 것이 없다**
7. ~~**`C` 격자 전용 업로드**~~ ✅ **할 것이 없다 — 이미 집행돼 있다** (`§9 〈160〉`).
   실물 = `d5_ingestion.py:167` **`if not bodies and grids:`** early return · 커밋 `4a8e41b` · `origin/main` 포함.
   ⚠ **여기 적혀 있던 `if bodies and not readable:` 는 틀린 수정이었다** — 빈 업로드까지 통과시키고,
   `stage1=False` 에서는 「형식 인식 실패」가 「내부 오류」로 갈아탈 뿐이다. **깨지는 시험도 0건이었다.**
   ⚠ **남은 것 하나** — ⓐ ~~`〈120〉` 3건~~ **착수 후보가 아니다.** `〈145〉` 로 **닫혔다**(Ted 「그대로 둬」) —
   「미실행」이 아니라 **「하지 않기로 한 것」**이다. ⭑ **2026-08-27 — `03-HANDOFF.md` 의 ⬜ 표기도 걷었다.** 이 줄은 이제 이력이다.
   ⓑ **stage 2 에서 `:167` 분기를 뒤로 밀면 「내부 오류」로 부활한다** — 가공을 켤 때 위치를 옮기지 않는다
8. **`E` `main` 병합** — **게이트 green 확인을 건너뛰지 않는다**(`CLAUDE.md §7`)

**3단 — stage 2 본체**

`T-1` → `D5`(파싱 → 좌표계 → COG) → `P3`(타일·확대·스크린샷) ∥ `F-1` → `V-1`·`V-2` → `S3` → `J-1`.
⚠ **타일은 COG 없이 못 낸다** — `D5` → `P3` 는 선호가 아니라 의존이다.

## 3. 재기동에서 반드시 지킬 것

- **이미지 재빌드를 빠뜨리지 않는다.** `〈153〉` 때 배선만 바꾸고 옛 이미지로 올렸더니
  **ai-service 만 healthy** 였고 그게 제일 나빴다 — **사전 DB 가 `None` 으로 조용히 비어** 온톨로지가
  통째로 사라진 채 **헬스 200** 을 냈다. **죽은 쪽은 바로 보이고 살아 있는 쪽이 속인다**
- `RESTART.md §2` 의 `COLAB_STAGING_*_DB_URL_FILE` **5키**가 없으면 `:?` 로 죽는다
- **`-f compose.i2.yml` 을 빼면 조용히 I2 이전으로 롤백된다.** 그 상태에서도 루트 헬스는 200 이다
- 회전 음성 확인은 **네트워크 경유로** 잰다 — pg 컨테이너 안 루프백은 `pg_hba` 가 `trust` 라
  **엉터리 비밀번호도 통과한다**

## 4. `admin` 계정 — 무엇이고 무엇이 아닌가

- 계정 `admin` · 연구실 `00000000000000000000HYMETS` · 계정 ID `000000000000000000HYMETSA1` · 역할 **`교수`**
- ⚠ **시스템 권한이 아니다.** 역할 enum 은 `교수`·`연구원` 둘뿐이고 `admin` 은 **일반 교수-역할 계정**이다.
  이 이름을 특수 취급하는 코드를 만들지 않는다
- ⚠ **비밀번호가 약하고 staging 은 외부에 열려 있다. prod 를 열기 전 회전이 필수다**
- 계정을 더 심으려면 — `ops/set-password.py` 로 `credentials.json` ＋ `d1_account`·`d2_member_role` **2행**.
  **자격 파일만 채우면 로그인은 되는데 권한이 0 이다**(스코프는 토큰이, 권한은 DB 역할이 정한다)
- 마운트가 읽기 전용이라 **호스트 파일 제자리 덮어쓰기(inode 유지) ＋ core-api 재기동**이다

## 5. 블로커 표를 믿지 마라 — 넷이 틀려 있었다

`§9 〈156〉`. **「열려 있다」는 최근에 잰 값이 아니라 마지막으로 적은 값이다.**
`#14` 는 green 인데 red 로, `#24` 는 없는 버그가 있는 것으로, `#12` 는 있는 그래프가 없는 것으로,
`#27` 은 살아 있는 태그가 죽은 것으로 적혀 있었다. **반대로 `#8` 은 실물이 더 나빴다**(레포 `PUBLIC`).

**착수 전에 해당 항목을 한 번 재고 시작한다.**

## 6. 복원 절차 — **`R-1` 로 세웠다**

**살아 있는 staging 을 제자리 복원하는 절차가 레포에 없다.** 복원 리허설은 **일회용 인스턴스 전용**이고,
실사고 시 복원 명령은 사람이 조립해야 한다. **`WORK-UNITS §10.2` 의 `R-1` 이 이것이고,
`X-1`·`X-4` 재기동의 전제조건이다**(`§9 〈158〉-㉯`).

## 7. stage 2 범위 판정 넷 (2026-08-27 Ted · `§9 〈157〉`)

| 물음 | 판정 |
|---|---|
| 값 조회 소유 도메인 | **`D7 Visualization`** — 사용자 관점에서 미리보기·타일과 같은 행위다 |
| pgvector | **stage 2 범위 밖** — 검색은 키워드 ＋ 온톨로지 사전으로 간다 |
| `ts_config` 한국어 재작성 | **stage 2 초반**(`T-1`) — 재생성 비용이 행 수에 비례하고 지금이 가장 싸다 |
| 편의 기능 `§J` 10건 | **stage 2 마지막 묶음**(`J-1`) |

⚠ **앞의 둘은 맞물린다** — 임베딩을 안 쓰기로 한 이상 **키워드 매칭이 검색 품질의 전부**이고,
형태소 사전이 바로 그 품질이다. 따로 내린 판정이지만 같은 방향이다.

---

*작성 2026-08-27. 인계 전용이고 판정을 담지 않는다 — 판정은 `PLAN-SoT §9`.*
