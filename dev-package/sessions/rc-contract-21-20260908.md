# 레인 `rc-contract-21` — WU-C10 · 계약 동결 해제 21차 집행 (2026-09-08)

기반 = `integration/r-c` `ab3eb7e`(Ted 21차 승인 커밋). 브랜치 `lane/wu-c10`. push 안 함.

## 커밋 (승인 → 계약 순서가 커밋 순서로 보인다)

| 해시 | 무엇 |
|---|---|
| `ab3eb7e` | (선행) Ted 21차 승인 — `contracts/` 첫 수정보다 **먼저** |
| `37a78c3` | 계약 첨가 6건 (`contracts/` 만) |
| `91ff210` | 서버 집행 (신설 op 4 전부 · 501 표 4 그대로) |
| `97585f6` | 시험 ＋ 생성물 재생성 |

## 첨가 6건 — 실물

- ⑴ `core-viz.yaml` **`describeTarget`**(`POST /target-descriptions`) ＋ 스키마 3
  (`TargetDescription`·`InstantRange`·`TargetDefault`) · `fe-core.yaml` 중계
  **`POST /preview-target-descriptions`**(경로를 일부러 달리했다 — oasdiff 합성 비교가
  같은 경로를 한 엔드포인트로 합친다 · `listPalettes` 선례).
- ⑵ `processingLevelUserSet` optional — `LineageNode`·`ProjectDatasetRow`.
  **`processingLevel` 은 required·파생값 그대로다**(덮어 쓰면 ㉯ · spec 구현 결정 ⑵).
- ⑶ `DataMap.byCategory` optional — 5값 전부 · 0이어도 줄 유지 · `byTopic` 무변.
- ⑷ `declareLineageUnknown`(400 with parent) · `updateLineageParentMethod`
  (`confirmedAt`·`confirmedBy` 무접촉) — 둘 다 **사람이 부른다**.
- ⑸ `core-ai.yaml` `ParentCandidateSuggestion.parentProcessingLevel` optional
  (모르면 **열쇠 자체를 만들지 않는다**).

## 게이트 — 축자

```
contract-lint green — seam 3건, 룰 위반 0.
contract-breaking green — 기준 ab3eb7e (3건) 대비 파괴적 변경 없음.
generated-up-to-date green — 등기부 10건 전부 재생성 일치, 등기부 밖 자칭 생성물 0건.
seam-consistency green — G-e 460건 · G-b 10건 · ㉠ 0건 · ㉡ 18건.
ai-no-lineage-write green — 계약·코드·체인 세 층 모두에서 쓰기 경로가 없다.
service-tests-core-api green — 실행 956건 전부 통과 (skipped 0 · deselected 6 …).
service-tests-viz-render green — 실행 379건 전부 통과 (skipped 0 · deselected 40 …).
service-tests-ai-service green — 실행 137건 전부 통과 (skipped 0 · deselected 26 …).
frontend-typecheck green — tsc --noEmit(…) 오류 0건.
work-item-consistency: green — 대장과 산문의 불일치 0
exec-bit green — `.sh` 131건 전부 인덱스 모드 100755 (100644 = 0건).
```

## 수치

- 소비자 수(REQUEST §7 ⑤ 측정법 축자) = **88 → 92**.
- 계약 op(`test_route_table`) **66 → 69** · core-viz **6 → 7** · **501 표는 4 그대로**
  (`C21_REAL` 이 신설 3건의 실동작 시험을 붙든다).
- 시험 수 전/후 — viz-render 379(+11) · core-api 956(+26) · ai-service 137(+4).
- 마이그레이션 **0건**.

## 못 잰 축 (지어내지 않는다)

- 시각 수천 건 파일에서의 `describeTarget` 지연 `[미측정]`.
- `describe` 응답 캐시 여부 `[미결]` — 이 회차는 캐시를 안 만든다.
- 조각이 여럿일 때 **읽을 수 있는 첫 조각**이 답한다(합집합·교집합 아님) — 레인 판단.
