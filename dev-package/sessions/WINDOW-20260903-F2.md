# staging 단독 창 2 — 단계 F(롤백 왕복) 집행 ＋ 창 종료 기록 (2026-09-03 12:39~ KST)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md §6`(단계 F) · `§10-⑤`(go/no-go). 등재 = `PLAN-SoT §9 〈301〉`.
> 이 파일은 **이 회차에 실제로 잰 값**만 적는다. 재지 않은 것은 `[미확인]` 으로 남긴다.
> 집행 범위 = **⑤ 자체점검 → 짝 백업 → F-1 롤백 → F-2 되돌아오는 배포 → 배포 후 점검 → 대장·인계 갱신**.
> 단계 G 는 **Ted 판정으로 건너뛰었다**(2026-09-03) — 아래 §6.

## 0. 창 진입 전 상태 (실측 · 읽기 전용 · 2026-09-03 12:35~12:39 KST)

| 축 | 실측값 | 재는 방법 |
|---|---|---|
| 커밋 | `d9e89a3a074ba859a27ffc4270579bc180317c81` · 브랜치 `main` | `git rev-parse HEAD` |
| 워킹트리 | **변경 0건** | `git status --porcelain \| grep -c .` |
| 서빙 릴리스 | **`72408173be50`** — 앱 5종 이미지 태그 동일 | `docker inspect -f '{{.Config.Image}}'` 5종 |
| 컨테이너 | staging **8개 전부 healthy** | `docker ps` |
| 체인 head | platform `0011_lv1_drop_level_user_set` · ai `0005_k2b_concept_graph_seed` | `verify/verify-chains.sh` → GREEN(통과 2 · SKIP 0) |
| 엣지 | `https://www.colab-hydro.com/healthz` **200** | `curl` |
| E 적재분 | 데이터셋 `01M1JM3JTQSSTKR9A2QQ67QN81` 상세 **200**(베어러) | `GET /api/v1/datasets/{id}` |

⚠ staging 스택 밖 컨테이너 2건(`a2_pg`·`ai_pg`)은 8개 계수에 넣지 않았다. 이 회차 무접촉.

## 1. go/no-go ⑤ 자체점검 (`RUNBOOK §10-⑤` 축자 = 「원장 green 2행 ＋ 그 태그 이미지 6종 실재 ＋ D·E 측정 완료」)

| # | 조건 | 실측 |
|---|---|---|
| ⓐ | 원장 green `deploy` 최근 2행 | `2026-09-02T23:44:32 · 77813d391f94` · `2026-09-03T11:35:49 · 72408173be50` — **둘 다 green** |
| ⓑ | 롤백 대상 = 직전 green | **`77813d391f94`** (현재 서빙 `72408173be50` 제외 후 최신) |
| ⓒ | 그 태그 이미지 **6종 실재** | `core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`·`migrator` **6/6** (`docker images` 실측) |
| ⓓ | `:prev` 6종 | 6/6 존재 · 이미지 ID 가 `77813d391f94` 6종과 **전건 동일** |
| ⓔ | 서빙 · 헬스 | 서빙 `72408173be50` · 8 healthy |
| ⓕ | D·E 측정 완료 | D = `WINDOW-20260903-D4.md` · E = `WINDOW-20260903-E2.md`(`〈300〉`) |
| ⓖ | 마이그레이션 델타 | `git diff --name-status 77813d391f94..d9e89a3 -- '*/versions/*'` = **0줄** ⟹ 되돌릴 스키마가 없다 |

⭑ **advisor 게이트 ③ = GO.** 지적 3건을 그대로 반영했다 — ⑴ 롤백 창 동안 적재·변환 요청 0건 유지 ⑵ 77813↔72408 사이 다른 green `deploy` 행 없음을 원장에서 눈으로 확인 ⑶ 롤백 중 엣지 일부가 red 로 나오는 것은 **정상**(옛 이미지)이며 롤백 실패로 읽지 않는다.

⛔ **되돌아오는 배포는 `72408173be50` 복귀가 아니라 `main` HEAD `d9e89a3a074b` 의 신규 릴리스다.** 「같은 것으로 돌아왔다」로 적지 않는다.

## 2. 짝 백업 (`RUNBOOK §1` — F 가 서빙 릴리스를 두 번 바꾸므로 파괴 단계 앞에 짝을 둔다)

`infra/staging/backup/backup-full.sh` · **전범위 백업 GREEN** (12:39:26~12:40:07)

| 산출물 | 스탬프 · 크기 | 판정 |
|---|---|---|
| `platform-20260903T123926.sql.gz` | 16,845B(해제) · 데이터 행 91 · CREATE TABLE 6 | GREEN (통과 7 · SKIP 0) |
| `ai-20260903T123926.sql.gz` | 5,158B | GREEN |
| `vol-uploads-20260903T123927.tar.gz` | 393,734,963B · 항목 157 = 매니페스트 157 · 원장 133 전건 포함 | GREEN (통과 8 · SKIP 0) |
| `vol-previews-20260903T124005.tar.gz` | 29,384,245B · 항목 192 = 매니페스트 192 | GREEN (통과 6 · **승인된 SKIP 1** = V5 오라클 `none`) |

보관처 규약 밖 파일 **0건**(0단 위생 통과).

## 3. F-1 — 롤백 (`infra/staging/rollback.sh --to-last-green`)

**넘긴 플래그는 `--to-last-green` 하나다.** 스크립트가 그 플래그를 받는 줄 —

```
    --to-last-green)  MODE=lastgreen; shift ;;
```

파괴 플래그(`--skip-alias-reattach`·`--to-placeholder`)는 **넘기지 않았다.**

| 시각 | 사건 |
|---|---|
| 12:41:36 | 착수 |
| 12:41:48 | `롤백 판정 — 헬스 6종 + 본문 대조` 착수 |
| 12:42:06 | **서빙 태그 대조 GREEN** |
| 12:42:08 | 별칭 `:i2` 재부착 GREEN(6/6) · digest 이력 6종 · 원장 행 |

**증거**

- 판정 = **`배포 판정: GREEN (통과 15건 · SKIP 0 — 모든 항목이 실제로 돌았다)`** — 헬스 6종 200 ＋ 본문 단위명 대조 ＋ 컨테이너 8종 healthy ＋ `0.0.0.0` 0건.
- **오라클(헬스가 아니다) — 축자:**

```
2026-09-03T12:42:06 서빙 태그 대조 GREEN — 서빙 중인 이미지가 실제로 77813d391f94 다
```

- 서빙 이미지 5종 실측 = `colab-v2/{core-api,pipeline-worker,viz-render,ai-service,frontend}:77813d391f94`.
- 별칭 재부착 = **6/6종의 `:i2` 가 `77813d391f94` 를 가리킨다**(이미지 ID 대조 · `c958298d2722`·`db139f9ab9f0`·`d726afc293b6`·`765d3fc65db5`·`f3d116b40033`·`c4be7a6c88dd`).
- 체인 **무변경** — platform `0011_lv1_drop_level_user_set` · ai `0005_k2b_concept_graph_seed` (롤백 후 `verify-chains.sh` GREEN 통과 2 · SKIP 0).
- 컨테이너 **8 healthy**.
- 원장 행 축자 —

```
2026-09-03T12:42:08+0900	rollback	-	77813d391f94	green	직전 green 릴리스로 되돌림 (직전 배포=72408173be50) 별칭재부착GREEN digest이력=6종
```

**데이터 무접촉 증명** — 롤백은 서빙 상태만 되돌린다(`rollback.sh` 머리말 「pgdata 볼륨도 건드리지 않는다」). 그 주장을 **본문으로** 쟀다: 단계 E 가 **`72408173be50` 릴리스에서 적재한** 데이터셋 `01M1JM3JTQSSTKR9A2QQ67QN81` 의 상세가 **옛 릴리스 `77813d391f94` 아래에서도 200 이고 본문이 같다**(`datasetId` · `fileName=Prediction_20230501.npy`). 즉 롤백은 이미지만 되돌렸고 E 의 적재분은 그대로다.

⭑ **스키마는 롤백이 되돌리지 않는다**(forward-only · `〈168〉-㉲`). **다만 이 창에는 마이그레이션이 없었다** — `77813d391f94..d9e89a3` 의 `*/versions/*` diff 가 0줄이다. ⟹ **이번 롤백은 이미지·서빙 상태를 전부 되돌렸고 남은 격차가 없다.** 마이그레이션이 낀 창이었다면 그렇지 않다.

## 4. F-2 — 되돌아오는 배포 (`infra/staging/deploy.sh --target staging`)

승인 행을 **배포 행보다 먼저** 넣었다(`DEPLOY-269:10` 형식 · B-0 과 같은 형태) —

```
2026-09-03T12:42:54+0900	approve	d9e89a3a074b	-	-	승인자=Ted 본것=단계 F 승인(롤백 왕복) · 결정 〈301〉 · go/no-go ⑤ GO — …
```

| 시각 | 사건 |
|---|---|
| 12:43:10 | 착수 |
| 12:43:35 | ⑩-b 엣지 설정 바이트 대조 **일치**(`56cc63f04dad443a`) |
| 12:43:54 | ⑪ 배포 판정 **GREEN (통과 15건 · SKIP 0)** · ⑪-b 체인 GREEN(2 · SKIP 0) |
| 12:43:55 | ⑫-a 별칭 `:i2` 재부착 GREEN 6/6 |
| 12:43:56 | 원장 green 행 · 이미지 보존 정리 |

- **신규 릴리스 id = `d9e89a3a074b`**(`main` HEAD `d9e89a3a074ba859…` 의 12자리). 플래그 없음.
- 서빙 이미지 5종 실측 = `colab-v2/*:d9e89a3a074b` · 컨테이너 **8 healthy**.
- 체인 **무변경**(0011 / 0005) — 이 배포는 스키마를 건드리지 않았다.
- 원장 행 축자 —

```
2026-09-03T12:43:56+0900	deploy	d9e89a3a074b	d9e89a3a074b	green	별칭재부착GREEN digest이력=6종 배포전백업GREEN 워킹트리변경=0
```

- 보존 정리로 **`e935db203b07` 6종이 삭제됐다**(보존 = 릴리스 3 ＋ 별칭 2 · `deploy.sh` 설계 동작). 감추지 않고 적는다. 남은 롤백 세대 = `72408173be50` · `77813d391f94`.
- 엣지 재확인 — `/healthz` **200** · 데이터셋 `01M1JM3JTQSSTKR9A2QQ67QN81` 상세 **200**.

### 4.1 중단 구간

| 구간 | 창 | 근거 |
|---|---|---|
| 롤백(72408→77813) | 12:41:36 → 12:41:48 = **약 12초** | 스크립트 로그(착수 ~ 헬스 판정 착수) |
| 되돌아오는 배포(77813→d9e89a3) | 12:43:10 → 12:43:54 = **약 44초** | 스크립트 로그(착수 ~ 판정 GREEN) |

⚠ **이것은 「컨테이너 교체~판정 GREEN」 구간이지 5xx 지속 시간이 아니다.** 외부 관측기를 붙이지 않았으므로 **실제 사용자 오류 구간은 `[미확인]`** 이다. 다음 회차의 진입조건으로 남긴다.

## 5. 배포 후 점검

### 5.1 게이트 — `./gates/run.sh all -j 1`

| 회 | 조건 | 결과 |
|---|---|---|
| 1회 | 게이트 입력 env **미주입** | green 36 · red(판정) 0 · **red(준비) 7** |
| 2회 | 홈 0600 게이트 입력 env 주입 후 재실행 | **green 42 · red(판정) 0 · red(준비) 1** |
| 3회 | `rls-effect-selftest` **단독** 재실행 | **green** — 축자 `rls-effect-selftest green — 보호 장치를 하나씩 떼면 실제로 red 가 난다. 틀린 롤도 red 다.` (케이스 18건 · 동시 4) |

- **red(판정) 은 0 건이다 — 이 왕복이 만든 red 는 없다.**
- 1회의 red(준비) 7건은 전부 「입력 미선언 4 · 환경대기 3」(`COLAB_APPLIED_DB_URL_*`·`COLAB_AUTOMETA_STAGING_DB_URL`·`COLAB_PREVIEW_TILE_DB_URL`·`COLAB_ARTIFACT_OWNER_DB_URL`·`COLAB_REFERENCE_DATA` 미주입). 2회에서 6건이 green 으로 돌았다.
- ⚠ **43/43 을 한 번의 `all` 실행에서 얻지는 못했다.** 2회의 잔여 1건은 `rls-effect-selftest` **red(준비)** 이고, 원인은 **일회용 postgres 준비 시간**(판정부가 아니라 환경 · `CLAUDE.md §1-b-⑸`)이다. **단독 재실행에서 green** 이므로 **계수 기준을 밝혀 43 green** 으로 적는다 — 상한 연장·건너뛰기·병렬도 조작으로 만든 green 이 아니다.
- 선례 = `WINDOW-20260903-B2.md §3` 도 같은 게이트 1건이 `all` 에서 red(준비)였다. **이번 왕복이 만든 것이 아니다.**

### 5.2 `artifact-ownership` 계수

`./gates/run.sh artifact-ownership` (배포 후 실측 · 시점 `2026-09-03T13:10:42+09:00`)

```
산출물 소유 — 대상 한 벌 90 · 살아 있다 57 · 접수분에만 닿는다 14 · 고아 0 · 판정 불가 19
  세는 단위 = 한 캐시 키 아래 선 산출물 한 벌 · 자리 = <홈>/.colab-v2-staging/previews
  원장 = d3_file 133 · d5_upload_file 146 · 전수 스냅숏 192 줄 · 삭제 0건
  경계 대조 — 관리자 롤 (d3_file,d5_upload_file)=(133,146) ↔ 경계 롤 'colab_app' (0,0) (갈렸다)
```

- **고아 = 0.** 왕복이 고아를 만들지 않았다.
- 계수가 `WINDOW-20260903-B2.md §3.1`(대상 75 · 살아 있다 42 · 원장 130/143)보다 늘었다 — **기준 차이가 아니라 단계 E 의 적재분**(데이터셋 1 ＋ 파일 3 ＋ 그 산출물)이 사이에 들어왔다. 세는 단위·자리는 같다.
- 판정 불가 19 벌은 **구판 사이드카**(`sidecarVersion`·`baked_for` 부재)로 **건수를 드러낸 채** 통과했다 — 고아가 아니라 보류(`A-1` 완료 정의 ⑴).

### 5.3 복원 preflight P8 — 새 릴리스를 **자동으로** 따라간다 (`〈297〉` 검증)

읽기 전용 실행(`preflight.sh --stamp 20260903T123926 --record-digests …`) 축자 —

```
── 자체 빌드 — 기대값 출처: `§5` 원장 회차 `d9e89a3a074b` (…/image-digest-ledger.tsv)
digest 대조 GREEN — 10 건 전건 일치 · 면제 0 (자체=원장 회차 d9e89a3a074b · 외부=대장 §3 · 배포되는 이미지가 전부 실제로 돌았다)
```

**손으로 `IMAGE-DIGESTS.md §3` 을 고치지 않았는데 회차가 `d9e89a3a074b` 로 따라왔다.** 롤백도 같은 원장에 남았다(12:42:08 · `77813d391f94` 6줄) — 이력에 구멍이 없다.
⚠ 같은 실행의 P6·P7·P9 는 FAIL 이다 — **복원용 환경변수를 주지 않은 읽기 전용 실행**이기 때문이고, **복원은 시작하지 않았다**(「사전조건 RED — 복원을 시작하지 않는다」). P8 만 이 회차의 관심사다.

## 6. 단계 G — 건너뜀 (Ted 판정 2026-09-03 · 등재 `〈301〉`)

- **판정 = G 를 집행하지 않는다.** `terraform plan` = `No changes.` 를 `IS4 ①` 의 오라클로 받는다. `apply` 는 **2026-08-23 에 이미 실행됐다**(`tunnel/README §0` 「상태 — 적용 완료 (2026-08-23)」 ＋ 대장 `IS4.evidence` 「실디렉터리 `plan` = `No changes.`」).
- **오늘 실측(읽기 전용 · `apply` 0회)** — `infra/staging/tunnel` 에서 `terraform plan` 축자 —

```
No changes. Your infrastructure matches the configuration.
```

- **`terraform` 은 호스트에 설치돼 있다 — `v1.9.8`.** ⟹ 런북 `§11 ⛔6`(「미설치라고 적혀 있다 · `[미확인]`」)은 **낡았다.** 실측으로 정정한다.
- **`IS4` 상태는 `partial` 그대로다** — 미달 ②(맨몸 호스트 조건 미실험)가 남는다. `R-1` 산출물에 terraform state 가 들어 있지 않다는 사실은 이 회차가 뒤집지 않았다.
- 레포·터널 무변경 — `git status --porcelain` 0건(state 파일은 `.gitignore` 대상).

## 7. 창 종료 — 타임라인 B → E → (G 건너뜀) → F

| 단계 | 회차 기록 | 릴리스 | 결과 |
|---|---|---|---|
| A(짝 백업) | `WINDOW-20260903-A2.md` | — | GREEN · 스탬프 `20260903T103411` |
| B(배포) | `WINDOW-20260903-B2.md` | **`72408173be50`** | GREEN · 게이트 green 42 / red(준비) 1 |
| C(크론) | `WINDOW-20260902-ABC2.md` | — | 설치 완료 |
| D(복원) | `WINDOW-20260903-D.md` ~ `D4.md` | — | 복원 완주 · 원상복구 |
| E(`S3`) | `WINDOW-20260903-E2.md` · `〈300〉` | `72408173be50` | NumPy 본체 적재 ＋ 5종 엣지 왕복 5/5 · `S3` done |
| **G** | (이 파일 §6) | — | **건너뜀** — `plan` = `No changes.` 를 오라클로 수용 |
| **F** | **이 파일** | `77813d391f94` → **`d9e89a3a074b`** | 롤백 GREEN · 되돌아오는 배포 GREEN |

### 7.1 종료 상태 8축 (창 닫는 시점 실측)

| # | 축 | 값 |
|---|---|---|
| ① | 체인 | platform `0011_lv1_drop_level_user_set` · ai `0005_k2b_concept_graph_seed` (창 내내 무변경) |
| ② | 서빙 릴리스 | **`d9e89a3a074b`** — 앱 5종 이미지 태그 동일 |
| ③ | 컨테이너 | `colab_v2_staging_*` **8개 전부 healthy** · 호스트 노출 `0.0.0.0` 0건 |
| ④ | digest | preflight P8 = **10건 전건 일치 · 면제 0**(자체 6 = 원장 회차 `d9e89a3a074b` · 외부 4 = 대장 `§3`) |
| ⑤ | `verify-deploy` | **GREEN · 통과 15건 · SKIP 0** |
| ⑥ | 게이트 | `all -j 1` = **green 42 · red(판정) 0 · red(준비) 1** ＋ 그 1건(`rls-effect-selftest`) **단독 재실행 green** ⟹ **43 green · red(판정) 0** |
| ⑦ | `artifact-ownership` | 대상 90 벌 · 살아 있다 57 · 접수분 14 · **고아 0** · 판정 불가 19 · 삭제 0건 |
| ⑧ | 원장 꼬리 | `approve d9e89a3a074b` → `deploy d9e89a3a074b … green` (그 앞 `rollback … 77813d391f94 … green`) |

### 7.2 단계 E 의 되돌림 목록 — **그대로 둔다**

`WINDOW-20260903-E2.md` 가 적은 되돌림 대상(데이터셋 `01M1JM3JTQSSTKR9A2QQ67QN81` ＋ 그 파일 3건 · 정비 12건)은 **이 회차가 되돌리지 않았다.** 근거 = `S3` 가 `done` 으로 닫혔고(`〈300〉`), F 의 롤백은 **데이터를 건드리지 않았다**(§3 본문 대조로 증명). 되돌림이 필요해지면 그 목록이 정본이다.

## 8. 이 회차가 재지 않은 것

| # | 항목 | 무엇을 하면 풀리나 |
|---|---|---|
| 1 | 롤백·배포의 **실제 5xx 지속 시간** | 창 밖에서 초 단위 폴링 관측기를 붙이고 다시 왕복한다 |
| 2 | `I3 ⑴` 자동 트리거 완주 | 배포 트리거(워처·크론)를 설치하고 사람이 명령을 부르지 않은 회차를 1건 남긴다 |
| 3 | `I3 ⑾` 전후 `/healthz` 200→200 중 **롤백 직전 `/healthz` 값** | 이 회차는 롤백 직전에 **상세 200**(인증)만 쟀다. 다음 회차는 `/healthz` 를 먼저 찍는다 |
| 4 | 롤백 중 **엣지 5종 본문 왕복** | 롤백 창을 늘려 옛 릴리스에서 5종을 다시 잰다(이번엔 창을 짧게 두는 쪽을 골랐다) |
| 5 | `IS4 ②` 맨몸 호스트 조건 | `R-1` 산출물 밖의 state 보관 재료가 필요하다 |
