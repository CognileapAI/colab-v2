# CT-1 정본 대조 판정 · F-2 문구 정합 정지 (2026-08-31 · 워크트리 `lane-fe-copy`)

> **산출은 대장 `evidence` 2건 ＋ 이 문서다. 코드·계약·DB·게이트·운영 스택 무접촉 · `status` 0건 변경 · `depends_on` 0건 변경 · 배포 0 · push 0.**
> 기점 `main` = `273bbd6`. 게이트 **green 35 / red 0** (전후 동일) · 프런트엔드 시험 **383 pass** (전후 동일) · `tsc --noEmit` **0**.

---

## 1. `F-2` — 문구 불일치는 실재한다. 그러나 고치지 않고 멈춘다

### 1-1. 실측 (대장이 이번 회차에 양방향으로 틀렸던 자리라 전건 다시 쟀다)

| 완료 정의 | 실물 | 판정 |
|---|---|---|
| ⓑ 카탈로그에서 키워드 입력을 걷어내고 필터로만 거른다 | `components/catalog/**` 의 `<input>` **0건** — `AppliedConditions.tsx` 0 · `CatalogTable.tsx` 0 · `ColumnMenu.tsx` 0 | **충족** |
| ⓒ 검색 진입점 하나 | `SearchHero` 의 유일한 사용처가 `routes/LabPage.tsx:11` | 진입점은 하나 — 다만 **ⓒ 는 3회차 판정으로 stage 3 동행** |
| ＋ 문구 정합 | **불일치 실재** (아래) | ⛔ **정지** |

**불일치 축자.**

- `routes/DatasetsPage.tsx:60` — 「찾을 것이 정해져 있으면 **홈의 AI 검색**이 빨라요.」 (＋ `:62` `<Link to="/lab">검색으로</Link>`)
- `components/search/SearchHero.tsx:37-38` — 「이 검색은 데이터에 **적혀 있는 낱말 그대로** 찾아요. 「강수량」으로는 「강수」가 적힌 데이터가 나오지 않으니 …」

한 화면은 상대를 **AI 검색**이라 부르고, 상대 화면은 스스로를 **낱말 그대로 맞추는 검색**이라 밝힌다. **정반대다.**

### 1-2. ⛔ 왜 고치지 않았나 — 정본에 고를 문면이 없다

| 갈래 | 정본 근거 | 왜 막히나 |
|---|---|---|
| 그대로 둔다 | — | 링크가 **이번 릴리즈에 없는 화면**(검색 화면 S-01·S-06 = stage 3)으로 사용자를 보낸다 |
| 「AI」 두 글자만 지운다 | `Policy_데이터_찾기.md:40` 축자 「AI ｜ **쓴다**(코랄 표시) ｜ 안 쓴다」 | 정본은 검색을 **AI 쓰는 길로** 적는다. 지우는 것이 정본 축자에 어긋난다 |
| 상호 안내를 걷어낸다 | `:155` 축자 「상호 안내 ｜ 결과·카탈로그 하단 ｜ **점선 박스로 반대 길을 권한다**」 · `:52`(§1.3-8) 「두 길은 서로를 가리킨다」 | 정본이 **두라고** 적은 것이라 걷는 것도 위반이다 |
| 다른 문구로 갈아 쓴다 | **없음** | 정본은 **두 길이 다 있는 세계만 기술한다.** 검색 화면 없는 릴리즈의 하단 안내를 어느 조항도 적지 않는다 |

⟹ **네 갈래 전부 정본 근거 0건 또는 정본 위반.** `CLAUDE.md §4`(기획이 애매하면 멈추고 보고) · `§5`(지어내지 않는다 · 부분 완료로 닫지 않는다)에 따라 **문구를 쓰지 않았다.**

⭑ 겸하여 **정본 자신이 이 물음을 열어 둔 채로 있다** — `_재검토_E02-E03_260826.md:101` 축자 「**E-02의 자연어 검색은 이번 릴리즈에 있나, 없나.** … 그대로면 E-02의 S-01·S-06이 통째로 미뤄지고 **남는 것은 카탈로그뿐이다**」. 3회차 Ted 판정이 「미뤄진다」 쪽으로 닫았고, **그 뒤 하단 안내를 무엇으로 대체하는지는 정본이 아직 적지 않았다.**

### 1-3. `F-2` 는 닫지 않는다

완료 정의 셋 중 **ⓑ 만 충족**이고 문구 정합 1건이 `[미확인]` 이다. **부분 완료로 닫지 않는다.**

⭑ **Ted 판정 대기 1건 (기존 미결의 재확인 — 새 물음이 아니다)** — 「검색 화면 없는 릴리즈에서 카탈로그 하단 상호 안내(`:155`)와 0건 안내를 무엇으로 대체하는가」. **정본 개정 자리다.**

---

## 2. `CT-1` — 정본 대조 판정. 다섯 조항 전건 충족, 고친 것 0건

**판정만 했고 새로 짓지 않았다.** 전거 = `Policy_데이터_찾기.md`(2026-08-18) ＋ `_재검토_E02-E03_260826.md`(`〈129〉` 지정 정본).

| 조항 | 정본 축자 | 실물 | 판정 |
|---|---|---|---|
| ⑴ 열 구성 8열 | `:111` 「`데이터셋 · 주제 · Level(가공 단계) · 프로젝트 · 업로더 · 수정일 · 계보 · Verified` + 오른쪽 끝 빠른 작업 자리」 · `:156` 「**8열 표 + 빠른 작업 자리**. 헤더는 세로 스크롤에도 위에 고정 … 스크롤은 표 래퍼 한 곳」 | `columns.ts:5-14` 여덟 글자 그대로·순서 그대로 · `CatalogTable.tsx:128` 아홉째 `빠른 작업` 칸 · 래퍼 1건(시험 `catalog.test.tsx:271`) | **충족** |
| ⑵ 조건 5열 | `:115` 「조건을 걸 수 있는 열은 **주제 · Level · 업로더 · 계보 · Verified 다섯**이고, 나머지 열은 정렬만」 · `:116` 「값별 건수 — **다른 열에 걸린 조건을 먼저 적용한 뒤에 센다.** 눌러도 0건인 값은 미리 흐리게」 ／ `_재검토:49` 「**다섯 열 유지가 정답**」 | `columns.ts:17` 축자 `FILTERABLE = ['주제','Level','업로더','계보','Verified']` · `CatalogTable.tsx:118` 이 비(非)조건 열에 `facets={null}` 을 넘겨 정렬만 남긴다 · 선적용 `localEngine.ts:104` · 흐리게 `ColumnMenu.tsx:50-52`(`is-zero`)·`catalog.css:97-98` | **충족** |
| ⑶ 기본 정렬 | `:117` 「기본 정렬 ｜ 카탈로그: **수정일 최신순** · 검색: Verified 우선 + 관련도」 · `:147` 「순서를 고르는 조작은 **카탈로그에만** 있다」 | `types.ts:24` 축자 `DEFAULT_SORT = { column: '수정일', order: '내림' }` | **충족** |
| ⑷ 잠긴 행 | `:152` 「자물쇠 + 잠김 칩 · **행에는 접근 요청 버튼을 두지 않는다** · 행을 누르면 잠긴 상세로 가고 요청은 거기서」 · `:130` 「카탈로그 행은 자물쇠 + `잠김` 칩」 · `:162` 「행 빠른 작업 — … **잠긴 행에는 나타나지 않는다**」 | `CatalogTable.tsx:146-150` 자물쇠 · `:180` `잠김` 칩 · `:183-184` 잠긴 행은 빠른 작업 칸이 비고 접근 요청 버튼도 없다 · `:143` 행 클릭 → `onOpen` → 상세 | **충족** |
| ⑸ 0건 문구 | `:170` 「"**조건에 맞는 데이터가 없어요. 조건을 하나 풀어 보세요.**" ｜ 조건 칩 줄의 전체 해제」 | `CatalogTable.tsx:135` 축자 일치 · 전체 해제 `AppliedConditions` | **충족** |

### 2-1. ⚠ 「Verified 우선 정렬」은 카탈로그 조항이 아니다

정본 `:117` 은 한 줄 안에서 둘을 **가른다** — 「**카탈로그: 수정일 최신순** · **검색**: Verified 우선 + 관련도」. `〈224〉`-㉯ 가 「정본이 요구한 Verified 우선 정렬」이라 적은 것도 **검색 실행 경로**의 이야기이고 그 소유를 승인 처리로 적었다. ⟹ **카탈로그의 기본 정렬에 Verified 우선을 넣으면 정본 위반이다. 넣지 않았다.**

### 2-2. 앞선 `[미확인]` 3건은 이미 측정돼 있었다 — 값을 지우지 않고 시점을 붙인다

대장 `CT-1.evidence` 가 「미실측 = 잠긴 행의 접근 요청 버튼 부재 · 행 클릭 시 잠긴 상세 이동 · 빠른 작업 잠긴 행 미표시 `[미확인]`, 푸는 법 = **잠금 픽스처 1건**」이라 적었다. **그 픽스처는 이미 있다.**

- `components/catalog/fixture.ts:104` — `bodyAccessible: false` 행 1건
- `frontend/test/catalog.test.tsx:244` 「잠긴 행에는 빠른 작업도 접근 요청 버튼도 두지 않는다」 · `:253` 「열린 행에는 빠른 작업 두 가지가 있다」 · `:261` 「잠긴 행을 눌러도 상세로 간다 (요청은 거기서 한다)」 — **383건 전건 green**

⟹ **세 건 다 측정됐다. `[미확인]` 은 지난 값이다.**

---

## 3. ⛔ 대장 모순 1건 — `CT-1` 의 `depends_on: [P1, P6]` 과 진입조건이 어긋난다. **고치지 않고 보고한다**

### 3-1. 어긋남의 축자

| 자리 | 축자 |
|---|---|
| `work-items.yaml` `CT-1.entry_conditions` | 「**`P1` 닫힘**(카탈로그 화면 실물 존재 · ✅ 2026-08-23) — 이 항목은 화면을 새로 짓지 않고 **이미 선 화면이 정본 세목을 지키는지**를 판정한다」 — `P1` **하나** |
| `work-items.yaml` `CT-1.depends_on` | `[P1, P6]` — **둘** |
| `CT-1.note` 의 사유 | 「**`P6`(승인 처리) = 조항 ⑵ 와 ⑷ 의 판정에 필요하다** — `accessRequestPending`·`canApproveVerification` 이 항상 `false` 라 **잠긴 행도 `Verified` 값도 지금은 화면에서 참이 되는 행이 0건**이고, 그 상태로는 두 조항을 판정할 수 없다」 |

### 3-2. 정본에서 찾은 근거 — **진입조건 쪽(`P1` 만)이 맞다.** `P6` 는 정본 요구가 아니다

**⒜ 정본은 다섯 조항 어디에도 승인 처리를 선행으로 걸지 않는다.** `:111`·`:115`·`:117`·`:152`·`:170` 은 전부 **표의 표시·조건·정렬 규칙**이다. 정본이 승인 처리를 가리키는 자리는 `:130` 의 「(범위는 승인 처리 E-06 적용 지점 표)」 **하나뿐**이고, 그 괄호가 정하는 것은 **접근 요청의 범위**이지 카탈로그 행의 표시 규칙이 아니다. 그 행의 표시는 같은 줄이 스스로 못 박는다 — 「카탈로그 행은 자물쇠 + `잠김` 칩」.

**⒝ `note` 의 전제(「참이 되는 행이 0건」)가 실물과 다르다.** 판정 대상은 **표가 그런 행을 만났을 때 무엇을 그리는가**이고, 그 대조는 **픽스처 1건으로 끝난다 — 그리고 이미 있다**: `fixture.ts:104` 의 `bodyAccessible: false`, `fixture.ts:23`·`:80` 의 `verified: true`. 두 조항 다 **오늘 판정됐다**(§2 · 시험 `catalog.test.tsx:236`~`:268`). ⟹ **`P6` 없이 판정 가능함이 실측으로 증명됐다.**

**⒞ 대장 자신이 같은 곳에서 반대로 적는다.** `CT-1.completion_def` 축자 「**판정 방법** = 위 다섯을 **화면 실물과 1:1 대조한다**」. 화면 실물 대조에 승인 처리 런타임은 들어가지 않는다.

### 3-3. 무늬 판정 — 「타일 서빙」(`〈240〉`)과 같은 자리다

`〈240〉` 은 **정본이 배제한 것을 대장 문구만 보고 구현한** 회차였다. 여기는 방향이 하나 더 얕다 — **정본에 없는 선행을 대장이 스스로 얹었다.** 실해(實害)가 같다: `P6` 는 `depends_on: [P1..P5]` 를 지는 뒤쪽 항목이라, 이 간선이 서 있는 동안 **오늘 이미 전건 충족인 `CT-1` 이 「선행 대기」로 잠긴다.**

⭑ **그래도 `depends_on` 을 바꾸지 않았다 — Ted 판정 자리다.** 물음 한 줄:

> **`CT-1` 의 `depends_on` 에서 `P6` 를 뺀다** (근거 = 정본 다섯 조항이 승인 처리를 걸지 않고, `note` 가 든 「참이 되는 행 0건」이 픽스처로 이미 해소돼 오늘 전건 판정됐다). **뺀다면 `CT-1` 은 `depends_on: [P1]` ＝ 진입조건과 일치하고, 다섯 조항 전건 충족이므로 그 자리에서 닫을 수 있다.**

⚠ **`P1` 쪽은 어긋나지 않는다** — 양쪽 다 `P1` 을 적고 `P1` 은 ✅ done 이다. 어긋나는 것은 **`P6` 한 간선**뿐이다.

---

## 4. 경계

- 만진 파일 = `dev-package/work-items.yaml`(`F-2`·`CT-1` 의 `evidence` **추가만** — 기존 문장 삭제 0건) ＋ 이 문서.
- **`frontend/**` 무수정** · `status` 0건 · `depends_on` 0건 · 계약·생성물·게이트 무접촉 · staging 무접촉 · push 0 · 배포 0.
- 다른 레인 소유 파일(`infra/**`·`contracts/**`·`components/preview/**`·`upload/**`) **접촉 0건**.
