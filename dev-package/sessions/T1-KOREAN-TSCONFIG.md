# T1-KOREAN-TSCONFIG — `ts_config` 한국어 재작성 착수 회차 기록

／ 작성 2026-08-28 · 워크트리 `worktree-agent-aa043eb7e0beb3cfb` · 기저 `8ec41e8`
／ 대상 = 대장 `dev-package/work-items.yaml` `T-1` · 완료 정의 정본 = `WORK-UNITS §10.2` 「↳ T-1 완료 정의 (2026-08-27 확장 · `〈167〉-㉱`)」

---

## 0. 결론 — **착수 불가. 생산 코드 변경 0 · 마이그레이션 0.**

**완료 정의 ⓐ 가 이 레포의 실물로 실행 불가능하다.** 근거는 이번 회차 실측 둘이다(`§2`).
ⓑ·ⓒ·ⓓ 는 정본이 스스로 **ⓐ 와 같은 회차에 묶는다**고 적었으므로 ⓐ 가 서지 않으면 함께 서지 않는다(`§3`).

**지어내고 진행하지 않았다.** 정본에 없는 대체 설계(동의어 사전 기반 `ts_config` 신설 등)를 완료 정의로
승격시키지 않았다 — 그것은 완료 정의 ⓐ 의 「형태소」가 아니고, 지어낸 완료 정의는 이 WU 를 영영 못 닫게 만든다.

---

## 1. 완료 정의 축자 (정본 `WORK-UNITS §10.2:390`)

> ⓐ `ts_config` 한국어 재작성 ＋ 전 행 재생성
> ⓑ **온톨로지 어휘·관계 확장을 같은 회차에**(`〈164〉-㉮` — 재생성 1회로 끝난다)
> ⓒ **`public` 밖 객체 재측정**(`〈165〉-㉰` 의 목록이 한국어 `ts_config` 신설로 즉시 낡는다)
> ⓓ **검색 평가셋 A층 실패 8건 재측정·보수**(`S2b-SEARCH-EVALSET` · 기대값은 정본·판정에서만 도출하고 검색을 돌려 역산하지 않는다)

같은 표 `WORK-UNITS §10.2:389` 이 ⓐ 의 뜻을 못 박는다 — **「검색 사전을 `simple` 에서 형태소로」**.

---

## 2. 실측 — ⓐ 를 막는 것 둘

### 2.1 실측 ① — 이미지에 한국어 `ts_config` 도, 한국어 형태소 확장도 없다

／ 수단 = 일회용 `postgres:16-alpine` (`docker run --rm` ＋ `--tmpfs /pgdata` ＋ `PGDATA=/pgdata/db` ＋ **호스트 포트 미공개**,
　`gates/tools/_pg.sh` 와 같은 형태). 질의는 `docker exec` 로 컨테이너 안에서만 돌렸다. **운영 스택 `colab_v2_staging_*` 에 접촉 0.**
／ 시점 = 2026-08-28. 단위 = `pg_ts_config` 행 수 · `pg_available_extensions` 행 수.

| 잰 것 | 값 |
|---|---|
| `SELECT count(*) FROM pg_ts_config` | **29** |
| `cfgname` 전량 | `arabic armenian basque catalan danish dutch english finnish french german greek hindi hungarian indonesian irish italian lithuanian nepali norwegian portuguese romanian russian serbian simple spanish swedish tamil turkish yiddish` |
| 한국어 후보 | **0건** — 위 29 에 한국어가 없다 |
| `pg_available_extensions` 중 `ko\|mecab\|textsearch\|nori\|hunspell\|dict` | **`dict_xsyn` · `dict_int` 둘뿐** — 둘 다 한국어 형태소 분석기가 아니다(각각 확장 동의어 · 정수 토큰) |
| 대조군(질의가 틀린 것이 아님을 보임) | `pg_trgm` · `unaccent` 는 나온다 · `vector` 는 안 나온다 |

**`0005_s1_search_index.py` 서두의 종전 실측과 같은 값이다** — 「`pg_ts_config` 29개에 한국어가 **없다**」. 낡지 않았다.

### 2.2 실측 ② — 정본에 「어떤 한국어 `ts_config` 인가」가 없다

`db/platform/versions/0005_s1_search_index.py:39-43` 이 그 자리를 **`[정본 무근거]`** 로 명시한다 —
「한국어 `ts_config` 를 정한 정본 줄이 없다([검색준비도] D-3-3 · `S1-PLAN §7-㉨`). **지어내지 않는다**(`㊴-②`)」.

**ⓐ 는 목표 값이 정본에 없는 채로 「재작성하라」만 있다.** 값을 이 회차가 고르면 그것이 곧 정본 날조다.

### 2.3 두 실측이 합쳐 뜻하는 것

- 형태소 `ts_config` 를 세우려면 **이미지에 형태소 분석기를 넣어야 한다** → `infra/staging/compose.i2.yml` · 이미지 빌드.
- 그 면은 **이 항목의 배타 파일 면 밖**이고(`db/platform/**`·`db/ai/**`·`d3_catalog.py`·`d9_ontology.py`·`eval/k4-search/**`·본 문서),
  `0005` 서두가 같은 판단을 이미 적어 두었다 — 「**이미지를 조용히 바꾸지도 않는다 — 그 판단과 `compose.i2.yml` 은 이 레인 소유가 아니다.**」

---

## 3. ⓑ·ⓒ·ⓓ 를 따로 닫지 않은 이유 — 정본이 묶었다

| 항 | 정본이 묶은 근거 | 따로 하면 생기는 것 |
|---|---|---|
| **ⓑ** 온톨로지 어휘·관계 확장 | `〈164〉-㉮` — 「**같은 회차에** … **재생성 1회로 끝난다**」 | ⓐ 가 뒤에 오면 전 행 재생성이 **2회**가 된다. 묶은 이유 자체를 없앤다 |
| **ⓒ** `public` 밖 객체 재측정 | `〈165〉-㉰` 의 목록이 **「한국어 `ts_config` 신설로 즉시 낡는다」** | 신설이 없으면 잴 변화가 없다. 지금 재면 **다음 회차에 곧바로 낡는 값**을 정본에 얹는 것 |
| **ⓓ** 평가셋 A층 실패 8건 재측정·보수 | 순서표 `WORK-UNITS §10.3:444` — 「3단 `T-1`(＋…＋평가셋 A층 8건 재측정·보수)」 | 사전이 그대로면 같은 값이 다시 나온다(`§4` 참조). 「쟀다」만 늘고 판정은 안 는다 |

---

## 4. ⓓ 대상 8건의 현재 귀속 — **이 회차가 새로 재지 않았다.** 마지막 실측은 2026-08-26 이다

／ 출처 = `S2b-SEARCH-EVALSET §2.10.2`(A층 재판정 2회차 · 통과 8 · 실패 8 · 층 밖 2). **아래는 그 문서의 값을 옮긴 것이지 이번 회차 측정값이 아니다.**

| # | 원인 갈래 | ⓐ(형태소 `ts_config`)로 움직이는가 | 실제로 푸는 것 |
|---|---|---|---|
| `Q-04`·`Q-05`·`Q-07` | ① 확장 경계 | **아니다** | 온톨로지 엣지 판정(ⓑ) — 단 `Q-04` 를 고치는 엣지 제거가 `Q-05`·`Q-06` 을 건드린다(`§2.9.4`). **Ted 판정 사안** |
| `Q-12`·`Q-13` | ② 설명 적재 결손 | **아니다** | `D-01`·`D-02` 의 `summary` 가 `NULL` — 적재는 **화면 경로로만**(`㊾-③`) 일어난다. 이 회차의 배타 파일 면 밖이고 운영 스택은 읽기 전용이다 |
| `Q-01` | ④ 매칭 규칙 | **아니다** | `〈89〉` 검색어 OR 결합 개정 — 평가셋 문서가 **「개정하지 않는다」**로 못 박고 `PLAN-SoT §9-〈113〉` 으로 올린 판정 사안 |
| `Q-11` | ④ 매칭 규칙 | **부분적으로 가능성 있음 · `[미확인]`** | 원인이 `D-08` **설명**의 「강수량」 토큰이다. 형태소 분해가 들어오면 값이 바뀔 수 있으나 **어느 쪽으로 바뀌는지는 사전이 정해져야 잰다** |
| `Q-14` | ⑤ 기지 한계 | **부분적으로 가능성 있음 · `[미확인]`** | 붙여쓰기 질의 `HSR레이더합성반사도` — `simple` 이 한 덩어리를 낸다. 형태소 분해가 이 자리를 바꿀 수 있다. `§4-②` Ted 확정 ⓐ 로 이미 기지 한계 처리 |

**즉 실패 8 중 형태소 `ts_config` 로 움직일 여지가 있는 것은 최대 2건(`Q-11`·`Q-14`)이고 둘 다 `[미확인]` 이다.**
나머지 6 은 **ⓐ 로 안 풀린다** — 온톨로지 판정 · 설명 적재 · 매칭 규칙 개정에 걸려 있다.

> ⚠ **이 표는 「ⓐ 를 할 값어치가 없다」는 뜻이 아니다.** ⓐ 는 색인 규칙 자체를 바꾸므로 **통과 8건까지 전부 재판정 대상**이 된다.
> 여기서 말한 것은 「ⓐ 만으로 실패 8 이 닫히지 않는다」 하나뿐이다.

---

## 5. 검사기 결과 — 축자

이 회차의 변경은 **본 문서 1개 추가**뿐이다(생산 코드 · 마이그레이션 · 계약 변경 0).
그 경로를 검사 대상으로 읽는 게이트는 없다. 가장 가까운 `dev-package` 게이트를 **기준선으로** 돌렸다.

```
$ ./gates/run.sh work-item-consistency
work-item-consistency: 대장 84건 · ㈐ 진실원 대조 48행 · ㈏ 체크리스트 대조 41건 · ㈑ 착수 후보 33행 · ㈒ 기한 5건 · ㈓ conflict 12건
  ── 검사 대상 밖 9건 (파싱하지 못해 **안 본** 자리다 — 통과의 근거가 아니다)
     · ㈒ `PA-G`: 기한 발동 여부가 `unknown` — 사람이 판정해야 한다
::error::work-item-consistency — 불일치 13건
  · ㈑ `I0`: … · ㈓ `S2b`·`I1`·`I3`·`X-2`·`R-1`·`J-1`·`P2`·`P6`·`S1`·`PA`·`S2`·`2단-BC120`
종료코드 = 1 (RED)
```

／ **RED 이고, 이 회차가 만든 RED 가 아니다.** `gates/README.md` 가 이 게이트를 「red 로 태어나는 것이 설계」로 적는다.
／ 전체 출력은 위 요약이 아니라 실행 원문이 정본이다 — 요약하며 줄인 자리가 있으므로 재현은 위 한 줄 명령으로 한다.
／ **RED 를 green 으로 만들려고 검사 대상을 줄이지 않았다.**

⚠ 그중 `㈓ S2b` 항목이 **이 회차와 직접 맞물린다** — 게이트 출력 축자:
「`WORK-UNITS §8.5` 는 🟧(A층 재판정 = 통과 8 · 실패 8 · 층 밖 2, **실패 8건 재측정은 `T-1` 로 이관**)」.
**그 이관 대상이 이번에 닫히지 않았다.** `S2b` 의 갈라짐은 그대로 남는다.

### 5.1 실패 픽스처 증거 — **없다. 만들 수 없었다**

지시는 「생산 변경을 되돌려 시험이 red 로 가는 것을 보이라」였다.
**생산 변경이 0 이므로 되돌릴 것이 없고, 따라서 red 픽스처 증거도 없다.**
없는 것을 있는 것처럼 적지 않는다. 이 칸은 **ⓐ 가 실제로 실행되는 회차의 진입 산출물**이다.

---

## 6. 이 회차가 하지 않은 것

- **`contracts/seams/` 를 열지 않았다.** 계약 표면 변경이 필요해지는 지점에 도달하지 않았다.
- **운영 스택 `colab_v2_staging_*` 에 접촉하지 않았다** — 정지·재기동·`down` 0 · `DELETE`/`UPDATE`/DDL 0 · 파괴 플래그 0.
  일회용 인스턴스만 `--rm` ＋ tmpfs ＋ `PGDATA` 지정 ＋ 호스트 포트 미공개로 띄웠고 종료 시 지웠다.
- **접속 문자열·비밀번호를 이 문서·로그 어디에도 남기지 않았다.**
- **평가셋 기대값을 한 줄도 고치지 않았다.** 검색을 돌려 기대값을 역산하지 않았다 — 애초에 평가셋을 돌리지 않았다.
- **완료 정의를 지어내지 않았다.** ⓐ 를 「동의어 사전 기반 `ts_config`」 로 바꿔 읽지 않았다.
- **`PLAN-SoT §9` 에 쓰지 않았다** — 이 회차의 배타 파일 면 밖이다. `§2` 실측 둘은 §7 에 등재 대상으로 올린다.

---

## 7. `[미확인]` 과 그것을 푸는 것

| 자리 | 무엇이 미확인인가 | 무엇을 하면 풀리나 |
|---|---|---|
| **ⓐ 목표 값** | 한국어 `ts_config` 를 **무엇으로** 할 것인가 — 정본에 줄이 없다(`[정본 무근거]`, `0005:39-43`) | **Ted 판정 1건.** 형태소 분석기를 도입할 것인가 · 도입한다면 무엇인가(`mecab-ko` 계열 등) |
| **ⓐ 실행 수단** | 이미지에 형태소 분석기를 넣는 일의 소유자 | `infra/staging/compose.i2.yml` · 이미지 빌드 소유 레인 지정. **`T-1` 파일 면에 그 자리가 없다** |
| **ⓐ 비용** | 확장 도입이 `postgres:16-alpine` 에서 되는가(alpine 은 빌드 의존이 다르다) | 후보 확장을 정한 뒤 일회용 컨테이너 1회 빌드 시험 |
| **`Q-11`·`Q-14`** | 형태소 도입이 이 둘을 어느 쪽으로 움직이는가 | 사전이 확정된 뒤 A층 재측정 1회 |
| **ⓒ 대상 목록** | `〈165〉-㉰` 의 `public` 밖 객체 목록이 지금 몇 건인가 | 이 회차가 재지 않았다. ⓐ 뒤 staging 카탈로그 **읽기 전용** 조회 |
| **`K4`·`P4` 완료 판정** | `T-1` 뒤 재측정에 걸려 있다(`§10.2:390` ⭑) | `T-1` 이 실제로 도는 회차 |

**이번에 세지 않은 판단기준(다음 회차 진입조건)** —
ⓐ 형태소 도입 시 `0005`·`0006` 오라클(`0005-assertions.sql` D절 · `0006-assertions.sql`)의 개정 범위
ⓑ 전 행 재생성의 실제 대상 행 수(생성 열이므로 `ALTER` 로 자동 재생성되나 **소요와 잠금은 미측정**)
ⓒ `d3_catalog.py` `_PREFIX_TSQUERY`·`_SEARCH_TRGM` 이 새 사전에서 그대로 유효한가

---

## 8. 다음 수 — 가장 값싼 것 하나

**`T-1` 을 여는 데 필요한 것은 코드가 아니라 판정 1건이다** — 「한국어 `ts_config` 를 무엇으로 세울 것인가」.
그 줄이 정본에 서기 전에는 ⓐ 가 목표 없이 열려 있고, ⓑ·ⓒ·ⓓ 는 정본이 ⓐ 에 묶어 두었다.
그 판정이 서면 이 문서 `§7` 표가 그대로 착수 점검표가 된다.
