# DR-4 — dev 무인 재생성 실행 기록 · 5회차 (2026-09-14 · 배포 `d5cd6ca9` ＋ 가뭄 `.gpkg` 2건 등록 ＋ verify)

- 성격 = 4회차가 남긴 마지막 2건(seq 13·14 `.gpkg`)을 **제품 고침 배포 뒤 화면으로 등록**하고 verify 로 28/28 을 재는 회차. `reseed.sh` 는 **`--from verify` 1회**만 돌았다(생성물 = 이 파일의 뼈대 ＋ `result.json`). 값의 원본은 실행 자리 `dev-package/reports/dev-reseed-runs/20260914T050442Z/`(추적 제외).
- 승인 = dev 한정 상시 승인(`.claude/rules/deploy.md` 11번 증보 문단). 회차별 GO 불요 · `reset`·`bootstrap`·`s3` 단계 **미실행**(dev 데이터 26건 보존).
- 기점 = `main` `d5cd6ca9`(등록 카드 단계 이동 `analyzing = !(ready || failure)` 고침 병합 커밋) · 작업 사본 브랜치 `lane/wu-c4-run5` · 배포 대상 sha `d5cd6ca971df`(1회차 `b955bece7384` → 이번 `d5cd6ca971df`).
- 실행 식별자 = `20260914T050442Z`(`--from verify`). 같은 회차의 다른 프로세스 = release executor 2건(`c4-run5-dev-d5cd6ca971df` · `c4-run5-dev-d5cd6ca971df-web`) ＋ 러너 직접 호출 5건(`login`·`projects`·`datasets --only-seq 13,14`·`verify`·`report`).
- 결과 = **배포 green(`deploy_doctor` 15/15 · 한 번의 실행 · 2회)** · **seed 28/28**(seq 13·14 `registered_no_preview`) · **verify green**(계수 판정 전건 일치 · 차단 0) · 사람 입력 **0회** · **「한 번의 실행」 판정 = 불성립**(§8).

## 1. 실행 창 (KST) · 프로세스별 소요

| 시각 | 프로세스 | 상태 | 종료코드 | 소요 | 비고 |
|---|---|---|---|---|---|
| 13:44~13:50 | 자격·접속 확인 | ok | — | — | Arn `user/colab-platform-s3-uploader-dev` · ssh `docker ps` 4종 `:dev-b955bece7384` healthy · `agent-browser doctor` 13 pass · `df -h /` 6.6G free |
| 13:51:12 | executor `c4-run5-dev-d5cd6ca971df`(d0~d11 ＋ v0~v8) | **failed** | 1 | ≈3분 | d0~d10 ✓(build arm64 · ship · `up: ok (sha d5cd6ca971df)` · repo 동기화 · npm ci·build) → **d11 `deploy_web.py` 정지**(§2-1) · verify 미도달 |
| 13:53~13:54 | executor `c4-run5-dev-d5cd6ca971df-web`(복구 계획 · d = build ＋ web · v0~v8) | **verified** | 0 | ≈1분 | 「올렸다: 96 파일 · 6,639,084 B」 · `deploy_doctor` **`항목 15 — ✓ 15 · ✗ 0 · ─ 0`** · `MAIN_SHA` `candidate=d5cd6ca971df ancestor=yes` · `CURRENT_SHA` `d5cd6ca971df` · `index.html` md5 local=public `369555ea…` · `tagged_units=4` |
| 13:55 | 태그 `dev-20260914-3` 로컬 생성(`d5cd6ca9`) | ok | — | — | **미push**(원격 push 는 오케스트레이터 몫) |
| 13:56:20 | 러너 `--phase login --account pi@hymets.invalid` | done | 0 | 2s | 「이미 로그인 상태 — login 단계 건너뜀」(세션 `colab-dev` 재사용 · 비밀번호 파일 **읽지 않음**) |
| 13:56:26 | 러너 `--phase projects` | done | 0 | 8s | 4건 「목록에 이미 있다」 · **`id=None`**(§7-2) → 화면 목록 재구성으로 id 4건 채움(§4) |
| 13:57~13:59 | 상태 파일 재구성(읽기 전용 · §4) | ok | — | — | 26행 `dataset_id` 회수 · 프로젝트 id 4건 |
| 13:59:30 | 러너 `--phase datasets --only-seq 13,14` | **done 28** | 0 | 45s | seq 13 22.8s · seq 14 21.4s(§5) |
| 14:01:01 | 러너 `--phase verify` | partial | 0 | 198s | 데이터셋 화면 **28/28** · 간선 **18/18** · 미리보기 5종 grib=그려짐 · nc·bin·tif·hdf4=안 그려짐(§8) |
| 14:04:25 | 러너 `--phase report` | ok | 0 | 0s | 완료 28/28 · 계획 총량 5,176,422,065 B |
| 14:04:42 | `reseed.sh --from verify --target-ref d5cd6ca9` | **ok** | 0 | 50s | preflight **10/10** · verify 47s 「계수 판정 — 전건 일치」 · report ok · 차단 0 |
| 14:05~14:06 | `deploy_doctor` 1회(도구 밖 · `doctor-final.out` · 비밀 패턴 0건) | ok | 0 | ≈40s | **`항목 15 — ✓ 15 · ✗ 0 · ─ 0`** · `전 항목 통과 (─ 0 — 15 항목이 실제로 돌았다)` · ⑮ `d5cd6ca971df ∈ main` |
| 14:06~14:09 | 화면 실사(읽기 전용 · `shots/`·`shots-2nd/`) | ok | — | — | §6 |

## 2. dev 배포 — release executor

- 계획 = `dist/release-c4-run5-dv/release.json`(gitignore · 비밀값 0) — 1회차 계획 `c4-run-dev-b955bece7384` 의 sha 치환본. 고정 입력 8건 sha256 = 1회차와 **전건 동일**(실측). `git diff --stat b955bece7384..d5cd6ca9 -- db services infra scripts` = **0**(마이그레이션 신규 0건 · 프런트 `RegisterArea.tsx` 변경 반영).
- 되돌리기 사본 = 웹 버킷 배포 전 **152 객체 · 68,773,526 B** → `dist/web-backup-b955bece/`(gitignore).
- EC2 디스크 = 배포 전 `6.6G free(68%)` → ship 전 7,046,926,336 B(정지선 2.0GiB ✓) → load 후 6,739,169,280 B(정지선 1.5GiB ✓) → up 후 `6.3G(69%)` → 회차 끝 `6.1G(70%)`. prune 0회(실행 태그·이전 태그 `dev-b955bece7384`·`dev-e9ca26b15bd1` 보존).

### 2-1. 정지 1건 — d11 `deploy_web.py` 자격 부재 (도구 밖 사람 실수 · 제품 결함 아님)

- 축자 = `RuntimeError: AWS 자격증명을 찾지 못했다 — AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY 환경변수, ECS 컨테이너 자격증명, EC2 IMDSv2 를 순서대로 시도했다.`
- 원인 = `deploy_web.py` 의 SigV4 자작 클라이언트(`colab_core/kernel/aws_credentials.py`)는 **`AWS_PROFILE` 을 읽지 않는다.** 1회차 기록 §3 「자격 = 실행 셸 한정 `aws configure export-credentials --profile … --format env`」를 이 회차 실행 셸이 빠뜨렸다.
- 실측(정지 시점) = 버킷 152 객체 · 공개 `index.html` md5 `7371a284…` = 되돌리기 사본 = **미변경**(부분 업로드 0). 컨테이너 4종은 이미 `:dev-d5cd6ca971df` healthy · `CURRENT_SHA`·`MAIN_SHA` 갱신됨.
- 처리 = `infra/releases/README.md` 「같은 id 로 무조건 재배포하지 않는다 · 별도 검토한 복구 계획」에 따라 **복구 계획** `…-web`(deploy = `npm run build` ＋ `deploy_web.py` 둘 · 검증 9건 동일 · 입력 8건 동일) 을 `--check` 뒤 실행. 자격은 실행 셸 한정 환경변수(값 기록 0).
- 배포 기록 = git common dir `deploy-releases/c4-run5-dev-d5cd6ca971df/`(`deployment: failed`) ＋ `…-web/`(`deployment: verified` · `notification: queued` · `verified_at` 2026-09-14T04:54:31Z).

## 3. 계수 (유효 측정 = `20260914T050442Z` verify · 러너 `verify.json` · 화면 실사)

| 항목 | 기대 | 실측 | 판정 |
|---|---|---|---|
| 데이터셋 | 28 | **28**(화면 `.hcnt` 「28건」 · 러너 상태 28) | 성립 |
| 프로젝트 | 4 (`drought` 2) | 4 · `by_project` 4 · **`drought` 상세에 `SPI-4weeks`·`SPEI-4weeks` 표시** | 성립 |
| 계보 간선 | 18 | **18/18** · 누락 0 | 성립 |
| 「미지정」 가공 단계 | 0 | **0** | 성립 |
| 프로젝트 미연결 | 0 | **0**(`usage-card` 28행 전건 1) | 성립 |
| 가공 단계 분포 | Lv0 10 · Lv1 16 · Lv2 2 | **Lv0 10 · Lv1 16 · Lv2 2**(상세 순회 28행 · 등재표 대조 mismatch 0) | 성립 |
| 미리보기 판정(28행) | 판정불가 0 | 성립 **28** · 미성립 0 · 판정불가 0 — ⚠ **gpkg 2행의 「성립」은 측정 시점 결함**(§8-1) | 계수는 성립 · gpkg 2행 판독은 §8-1 로 정정 |
| 계정 | 2 | **2**(4회차 §10 값 · 이 회차 DB 계수 **미측정** · 계정 단계 미실행 · 변경 조작 0) | `[미확인 — 이 회차 재측정 없음]` |

- `deploy_doctor --env dev` = 2회 각각 **`항목 15 — ✓ 15 · ✗ 0 · ─ 0`**(executor v1 · 도구 밖 1회) · ⑮ `d5cd6ca971df ∈ main`.

## 4. 상태 파일 재구성 — 지시의 전제와 실물이 어긋난 자리

- 지시 전제 = 「4회차 러너 상태 파일이 seq 13·14 를 blocked 로 표시 · PI 비밀번호는 `<work-dir>/new-password.txt`(유일 사본)」.
- 실물 = **둘 다 부재.** 4회차 작업 사본(`lane/wu-c4-run4` 워크트리)이 병합 뒤 삭제되면서 `.work/`(gitignore)·`dev-reseed-runs/`(gitignore)가 함께 사라졌다. 탐색 범위 = ext4 체크아웃·워크트리 · 옛 `/mnt/f` 체크아웃(워크트리 2개 · 09-13 것) · `/tmp` · 홈 전체 — `new-password.txt`·`state.json`(러너) **0건**. 4회차 §10 「러너 자격 파일 = `shred -u`」와도 부합.
- 등록·verify 가 성립한 근거 = 브라우저 세션 `colab-dev`(agent-browser 데몬 · 활성 세션 목록에 있음)가 **로그인 상태를 유지**(`/lab` 착지 · `login-submit` 0 · 서버 세션 TTL 12h · core-api 재생성 뒤에도 유지). 러너 login 단계는 「이미 로그인 상태」로 비밀번호 파일을 읽지 않는다.
- 재구성 방법(읽기 전용 · dev 쓰기 0) = 로그인된 페이지 안에서 `fetch('/api/v1/projects')`·`fetch('/api/v1/datasets')`(`nextCursor` 페이지 2장)로 **이름→id** 만 회수(`c4r5-helper/list_ids.js` · 토큰은 페이지 밖으로 내지 않음) → `rebuild_state.py` 가 seq 1~12·15~28 행을 `status: done · source: 화면 목록 재구성(5회차)` 로, 프로젝트 id 4건을 채움. 계획(`upload-plan.json`)은 `build_plan.py` 재생성(28/4/18 · `plan-manifest.yaml` diff 0).
- 로그인 계정 = **`[미확인]`** — 세션이 어느 계정인지 화면에 표시 자리가 없고 `/api/v1/me` 는 SPA 토큰 없이는 401. 4회차 §6 「첫 계정(PI) 로그인」 세션이 그대로 이어졌다는 것이 근거의 전부다.

## 5. seq 13·14 등록 (제품 고침 `d5cd6ca9` 검증)

| 순번 | 이름 | 파일 | 바이트 | 분석 | 배너 축자 | 등록 카드 | 가공 단계 | 프로젝트 | 상태 | id | 소요 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 13 | SPI-4weeks | `SPI_4weeks_sig_wide.gpkg` | 24,797,184 | 실패 | 「형식 인식 실패 — 지도로 못 그려요 · 등록은 됩니다」 | `reg-open` → ① `Lv1` → ② 이름·설명 → ③ 연결 `drought` → `reg-done` | Lv1 | drought | `registered_no_preview` | `01M2F4JCQNBVFCS9HATS24E8NQ` | 22.8s |
| 14 | SPEI-4weeks | `SPEI_4weeks_sig_wide.gpkg` | 24,797,184 | 실패 | 〃 | 〃 | Lv1 | drought | `registered_no_preview` | `01M2F4K0YYD9C1JSEPK9BR8QFH` | 21.4s |

- 4회차 정지 자리(「단계 이동 실패: ③ 연결」)는 **재현되지 않았다** — 「다음 →」가 분석 실패 뒤에도 활성. 갈무리 `fail/13-analysis.png`·`14-analysis.png`(분석 실패 배너) · `shots/13.png`·`14.png`(등록 직후).
- `grid-skip` 지목점 미해소 로그 2건은 선택 단계라 계속(4회차와 같음 · 격자 없는 형식).

## 6. 화면 실사 (읽기 전용 · 세션 `colab-dev` · `dev-reseed-runs/20260914T050442Z/shots/`·`shots-2nd/`)

- 카탈로그 `/datasets` 머리 「**28건**」 — `catalog-28.png`.
- 프로젝트 `drought` 상세 — `SPI-4weeks`·`SPEI-4weeks` 2건 표시(4회차 「연결된 데이터셋이 없어요」 → 해소) — `project-drought.png`.
- gpkg 상세(seq 13 · 1차 · `gpkg-detail-13.png`·`.txt`) — `basic-info` 1 · `dataset-preview` 1 · **`preview-unavailable` 1** · 미리보기 자리 축자 「**그리는 서버에 연결하지 못했다: timed out** · 다운로드·계보 확인은 그대로 할 수 있어요.」 · 「이 데이터를 쓴 과제·논문 1건 drought」.
- gpkg 상세(seq 14 · 2차 · `shots-2nd/`) — `preview-unavailable` **0** · 축자 「**이 형식은 아직 지도로 못 그려요.** 지금 그릴 수 있는 형식은 NetCDF · Binary · HDF4 · GeoTIFF · NumPy · GRIB · HDF5 예요.」(`datasetPreviewSource.ts` 문면).
- 같은 상세를 25초 폴링(1초 간격)한 3회(seq 13·14·대조 seq 3)에서는 `preview-unavailable` 0 유지 — **문면과 testid 가 시점에 따라 갈린다**(§8-1).

## 7. 정지·이탈 (도구 결함 2건 · 사람 실수 1건 · 제품 결함 0건)

| # | 자리 | 축자 | 원인 | 처리 |
|---|---|---|---|---|
| 7-1 | executor d11 | `AWS 자격증명을 찾지 못했다` | 사람 — 실행 셸에 정적 자격 미주입(§2-1) | 복구 계획 `-web` 로 완료 · 코드 무수정 |
| 7-2 | 러너 `projects` | 「목록에 이미 있다. id=None」 뒤 `status: done` | **도구 결함(green-by-skip)** — `project_index()` 는 `<a href="/projects/<id>">` 를 찾는데 목록은 `onClick` 이동이라 앵커 0 · id 없이 done 기록 | 이 회차 = 화면 목록 재구성으로 채움(§4) · 고침은 이 레인 밖(`dev-seed` 후속) |
| 7-3 | 지시 §2 전제 | 상태 파일·`new-password.txt` 부재 | 4회차 워크트리 삭제로 gitignore 산출물 유실 | §4 재구성 · **비밀 재발급·API·DB 우회 0** |

## 8. 새 관측 (판정 없음 · 후속 항목 입력)

### 8-1. verify 순회기의 gpkg 2행 「성립」은 측정 시점 결함이다 (도구 결함 · `dev-reseed` 후속)

- 순회기 판정 = `dataset-preview` 등장 직후(436·442 ms) `preview-unavailable` 계수 0 → 「성립」. 그러나 같은 상세를 `networkidle` 뒤 읽으면 1(§6 1차) — 미리보기 요청이 **중계 타임아웃(10초)** 뒤에야 문면을 세운다. 4회차 §3 각주 「이 회차의 「성립」은 「미리보기 없음 문면이 없다」이지 「그려졌다」가 아니다」의 형제 결함.
- 갈래 둘 = ⓐ 타임아웃 문면 「그리는 서버에 연결하지 못했다」(`preview-unavailable` testid 있음) ⓑ 형식 문면 「이 형식은 아직 지도로 못 그려요」(testid 없음 · 계수 0). 어느 쪽이 서는지가 시점·서버 상태에 따라 갈린다.
- 지시의 기대 「gpkg 2행 = 미성립/판정불가 · 포맷 명시」는 **현 도구로는 나오지 않는다** — 순회기가 형식 문면을 셀 testid 가 없고, 타임아웃 문면은 10초 뒤에 온다. 고치는 자리 = 순회기 대기 조건(`preview-unavailable` **또는** 렌더 이미지 등장까지) ＋ 형식 문면 testid(프런트).

### 8-2. viz-render 재기동 (`PV-2` 입력)

- 컨테이너 재생성(13:52 KST) 뒤 `RestartCount` **0 → 1**(14:04:18 KST · 러너 hdf4 미리보기 검사 14:03:58~14:04:18 과 겹침) **→ 2**(14:07:20 KST · gpkg 상세 폴링 중) · `OOMKilled=false` · `exit=0`. 4회차 §8(5회)과 같은 모양 · 원인 `[미확인]`.
- 러너 자체 미리보기 5종 = grib **그려짐** · nc·bin·tif·hdf4 **안 그려짐**(4회차 5종 전건 안 그려짐 → 1종 개선 · 판정 아님).

### 8-3. 기타

- 카탈로그 목록 API 는 `limit` 을 20 으로 고정하고 `offset` 을 무시한다 — 페이지는 `nextCursor` 뿐(`catalog.py` `PAGE_SIZE`). 러너 `catalog_total()` 의 `.hcnt` 총계 방식은 유효.
- 세션 뼈대(`report.py` 산출)에 실행 자리 **절대경로**가 찍힌다(`CLAUDE.md §3-8` 위반 · 이 파일에서 상대경로로 바꿔 적음) — `report.py` 후속.

## 9. 판정 — 「한 번의 실행」

- **불성립.** 회차 5개(1~5회차) · 이 회차 안에서도 프로세스 8개(executor 2 ＋ 러너 5 ＋ `reseed.sh` 1). `reseed.sh` 전 10단계를 한 프로세스로 밟은 회차는 아직 없다.
- 이 회차가 검증한 것 = ⑴ 제품 고침 `d5cd6ca9` 로 분석 실패 업로드가 등록 카드를 끝까지 걷는다(2/2) ⑵ dev 28/28 · 간선 18/18 · `drought` 2건 ⑶ `deploy_doctor` 15/15 한 번의 실행(2회).
- 이 회차가 검증하지 않은 것 = `reset`~`seed` 무인 완주(다음 회차) · 계정 계수 재측정 · 로그인 계정 신원 · gpkg 미리보기 판정(§8-1 도구 결함으로 미판정).

## 10. 현재 dev 상태 (14:10 KST 기준)

- 앱 4종 `:dev-d5cd6ca971df` healthy · `CURRENT_SHA`·`MAIN_SHA` `d5cd6ca971df ancestor=yes` · 웹 번들 `index.html` md5 `369555ea…`(local=public) · 태그 `dev-20260914-3` 로컬 · 디스크 `6.1G free(70%)`.
- DB = 프로젝트 4 · 데이터셋 **28** · 간선 18 · 계정 2(4회차 값 · 이 회차 조작 0). S3 = 이번 등록분 2건 추가(≈49.6MB) · `_ops/` 무접촉 · staging·prod 무접촉.
- 작업 사본 `.work/`(재구성 상태 파일 · 갈무리) · `dev-reseed-runs/20260914T050442Z/` · `dist/` = 전부 추적 제외. 비밀 파일 신규 0(러너 자격 파일 생성 0 · shred 대상 0).

## 11. 다음 회차 진입조건

1. `reseed.sh` 전 10단계 **한 프로세스** 완주(6회차) — 이 회차의 도구 결함 2건(§7-2 `project_index` 앵커 의존 · §8-1 순회기 대기 조건)을 먼저 고치고 `dev-reseed-selftest` red→green 픽스처를 단다.
2. `FMT-GPKG` 판정 입력 = §6 문면 2종(타임아웃 / 형식) ＋ 등록 성립 사실. `PV-2` 입력 = §8-2.
3. 러너 작업 자리(`.work/`)는 워크트리와 함께 사라진다 — 회차 산출물 보존 규약(복사 자리) 을 `dev-seed/README.md §3` 에 정한다.
