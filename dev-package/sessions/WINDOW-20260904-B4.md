# 단독 창 4 — 단계 B(배포) ＋ 첫 접촉 채집 · 재굽기 · 소유 재실측 · 현물 (2026-09-04 21:53~22:15 KST)

> 워크트리 `.claude/worktrees/lane-window-b4` · 브랜치 `lane-window-b4` · 기준 `main` @ `f465d83`
> 앞 회차 = `WINDOW-20260904-A4.md`(짝 백업 ＋ go/no-go ① 증거) · 양식 = `WINDOW-20260903-B3.md`
> Ted GO(2026-09-04 · go/no-go ①) = 「GO — 배포 → 첫 접촉 채집 → 재굽기 → 재실측 연속」
> 등재 = `PLAN-SoT §9 〈312〉`

## 0. 원장 2행 — 축자

```
2026-09-04T21:53:06+0900	approve	f465d83	-	-	승인자=Ted 본것=go/no-go ① GO · 짝 백업 platform-20260904T211840/vol-uploads-20260904T211841/vol-previews-20260904T211919 GREEN · latest-check GREEN · 마이그레이션 델타 0줄(0012 부재 · 덤프 alembic_version_platform=0011) · 배포전 게이트 8건 green(contract-lint·generated-up-to-date·contract-breaking·migration-single-head·frontend-typecheck·frontend-test·service-tests-core-api·service-tests-viz-render) · 롤백 대상 64791f910f49 이미지 6/6 실재 · 배포 직후 값 조회 4건 Server-Timing 채취 승인 포함 · 사후 재굽기(POST /api/v1/previews 3건 · staging WRITE) 승인 포함 · sha=f465d83(=513013c+A4문서) · WINDOW-20260904-A4
2026-09-04T21:54:09+0900	deploy	f465d83a2ef1	f465d83a2ef1	green	별칭재부착GREEN digest이력=6종 배포전백업GREEN 워킹트리변경=0
```

**승인 행이 배포 행보다 먼저다** — 63초 앞선다(`DEPLOY-269:10` 규약).

## 1. 단계 B — 배포 판정 6항

| # | 축 | 실측 |
|---|---|---|
| 1 | 실행 이미지 태그 | 앱 5종 전부 `colab-v2/<unit>:f465d83a2ef1` (`docker ps`) · 별칭 `:i2` **6/6** 이 이미지 ID 로 대조 완료(`core-api 77d675a7752d`·`pipeline-worker 7ffcb30c6f14`·`viz-render 6c628cd7ee0c`·`ai-service 27d21caee12e`·`frontend b5604978809f`·`migrator e4986ae2bb2a`) |
| 2 | 체인 head | platform **`0011_lv1_drop_level_user_set`** · ai **`0005_k2b_concept_graph_seed`** — **A4 §1.1 예측대로 움직이지 않았다** |
| 3 | 헬스 본문 대조 | `/healthz` `^ok$` ＋ 단위 5종 `"unit"` 값 일치 — **6/6 PASS** |
| 4 | 컨테이너 | **8개 healthy** · 호스트 노출 `0.0.0.0` **0건** |
| 5 | ⑩-b 엣지 설정 바이트 대조 | 레포 판 ＝ 도는 판 (`56cc63f04dad443a`) |
| 6 | 원장 마지막 줄 | `deploy f465d83a2ef1 … green … 배포전백업GREEN 워킹트리변경=0` |

- **`verify-deploy.sh` = GREEN (통과 15건 · SKIP 0)** · `verify-chains.sh` = GREEN (통과 2건 · SKIP 0).
- 필수 설정 프리플라이트 GREEN(통과 17건 · SKIP 0) · ⑤ 배포 전 백업 platform GREEN(7·SKIP 0) · ai GREEN(7·SKIP 0).
- digest 이력 원장 **6종 append** (`image-digest-ledger.tsv` `2026-09-04T21:54:08~09`).
- 보존 = 릴리스 3개(`f465d83a2ef1`·`64791f910f49`·`d9e89a3a074b`) ＋ 별칭 2개. `72408173be50` 6종 삭제(보존 규약대로).
- **red 0 · SKIP 0.**

**다운 시간** — 착수 `21:53:18` → 배포 GREEN 판정 `21:54:09` = **51초**.
⚠ **이 값은 착수~판정 구간이고 5xx 지속 시간이 아니다** — 5xx 를 재는 계측이 이 경로에 없다(`B3 §1` 과 같은 `[미확인]`).

## 2. ⒤ 첫 접촉 채집 — `〈304〉` 갈래 ⓓ(재기동 첫 접촉) · **배포당 한 번뿐인 창**

원값 = `sessions/WINDOW-20260904-FIRSTTOUCH.tsv`(13열 · 4행). 요청 = 공개 엣지 `POST /api/v1/datasets/{id}/value-lookup` · 점 `36.75,127.40` · **데이터셋당 1회씩** · UA `colab-v2-window-a4/1.0 (VL-1 branch d probe)`.

| 순 | 데이터셋 | wall ms | connect / appconnect / starttransfer / total (s) | http | available | `Server-Timing` 축자 |
|---|---|---:|---|---|---|---|
| 1 | GK-2A NDVI 월평균 | **315** | 0.038473 / 0.082538 / 0.309361 / 0.309450 | 200 | true | `coreAccess;dur=9.425, coreRelay;dur=97.846, coreTotal;dur=107.271, vizResolve;dur=0.152, vizFindTile;dur=9.151, vizReadPoint;dur=79.135, vizTotal;dur=88.448` |
| 2 | U-Net 예측 | **327** | 0.064159 / 0.147534 / 0.290211 / 0.290316 | 200 | true | `coreAccess;dur=3.060, coreRelay;dur=27.195, coreTotal;dur=30.255, vizResolve;dur=0.332, vizFindTile;dur=20.273, vizReadPoint;dur=3.962, vizTotal;dur=24.584` |
| 3 | PV-1 업로드 | **205** | 0.041526 / 0.089386 / 0.198516 / 0.198610 | 200 | true | `coreAccess;dur=2.441, coreRelay;dur=11.217, coreTotal;dur=13.658, vizResolve;dur=0.214, vizFindTile;dur=7.333, vizReadPoint;dur=1.630, vizTotal;dur=9.181` |
| 4 | DEM(대조군) | **203** | 0.038604 / 0.085298 / 0.196617 / 0.196700 | 200 | false | `coreAccess;dur=2.363, coreRelay;dur=9.171, coreTotal;dur=11.533, vizResolve;dur=0.204, vizFindTile;dur=6.954, vizReadPoint;dur=0.000, vizTotal;dur=7.167` |

### 2.1 이 채집이 닫은 것 · 닫지 못한 것

- ⭑ **`Server-Timing` 이 공개 엣지를 통과한다 — `〈311〉` 이 `[미확인]` 으로 남긴 그 자리가 닫혔다.** Cloudflare(`server: cloudflare` · `cf-ray a35d2435caf6f9ad-HKG`)가 헤더를 **떼지 않는다**. 배포 전 같은 스크립트로 잰 대조(21:52:51 · 서빙 `64791f910f49`)에서는 4건 전부 **헤더 미도착**이었다 — 계측이 실제로 실린 것이 이 차이다.
- ⭑ **`〈304〉` 의 1,164 / 2,111 ms 스파이크는 재현되지 않았다.** 재기동 첫 접촉 4건의 관측 최대는 **327 ms** 이고, `〈311〉` 의 따뜻한 상태 p95(329·287·277·267 ms)와 같은 자리다. ⟹ **후보 ⓓ(재기동 첫 접촉)로는 그 스파이크가 설명되지 않는다.** 원인은 `[미확인]` 그대로다 — **후보가 하나 줄었다**는 것이 이 회차의 산출이다.
- ⭑ **서버 안의 첫 접촉 비용은 실재한다 — 다만 사용자 시간을 지배하지 않는다.** 1번 호출의 `vizReadPoint` **79.1 ms** ＋ `coreRelay` **97.8 ms** 는 3번 호출(1.6 / 11.2 ms)의 수십 배다. 그런데 `time_total` 은 0.309 s 대 0.199 s 로 **110 ms 차이**뿐이다 — `〈311〉`-㉮ 의 「엣지가 262 ms 를 먹는다」와 같은 그림이다.
- ⚠ **사람이 부르는 첫 요청이라는 조건은 완전히 서지 않았다.** `deploy.sh ⑪` 이 `verify-deploy.sh` 를 스스로 부르고 그것이 `/healthz` 6종을 친다. 이 채집은 **`deploy.sh` 가 끝난 직후 3초 안**(21:54:12)에 나갔다. **값 조회 경로(`core-api` 데이터셋 라우트 → `viz-render` 자리 탐색·점 읽기)는 헬스가 밟지 않는 코드다** — 그래서 그 경로에 대해서는 첫 접촉이 맞다. **감추지 않고 적는다.**
- ⚠ **A4 §6⑴ 의 예측이 틀렸다** — 「재굽기 전이라 1~3 은 `available:false` 가 정상」이라고 적었으나 **실측은 3건 전부 `available:true`** 였다. ⟹ **값 조회는 ③지도형 캐시 키로 자리를 찾지 않는다.** 무엇으로 찾는지는 이 회차가 가르지 않았다 — `[미확인]`.

## 3. 게이트 전수 — 재굽기 **전**

`set -a; . ~/.colab-v2-test.env; set +a; ./gates/run.sh all -j 1` (로그 `gates-b4.log` · 단독 8 · 병렬 42 · 미선언 0 = **선언 50**)

```
  ── 계 : green 50 / red(판정) 0 / red(준비) 0
```

- **50/0/0.** `service-tests-core-api`·`rls-effect-selftest` 둘 다 **1회차 실행에서 green** — 재실행 조건이 서지 않았다. ⛔ **「고쳐서 green」이 아니다** — `〈311〉`-㉵ 가 적은 간헐성의 원인은 `[미확인]` 그대로이고, 이번 표본에서 재현되지 않았을 뿐이다.
- **A4 §1.2 가 남긴 「이번에 세지 않은 판정기준」이 닫혔다** — `gates/run.sh all` 을 배포 커밋에서 돌지 않았던 자리를, 배포 후 전수 50/0/0 이 덮었다.

### 3.1 `artifact-ownership` 계수 — **재굽기 전** (축자)

```
산출물 소유 — 대상 한 벌 104 · 살아 있다 85 · 접수분에만 닿는다 0 · 고아 0 · 판정 불가 19
  세는 단위 = 한 캐시 키 아래 선 산출물 한 벌 · 자리 = /home/ttlhi10/.colab-v2-staging/previews · 원장 = d3_file 133 · d5_upload_file 133
  회수 전 전수 스냅숏 230 줄
artifact-ownership green — 대상 104 벌 (살아 있다 85 · 접수분 0 · 고아 0 · 판정 불가 19) · 지도 타일 2 · 스냅숏 230 줄 · 삭제 0건
```

⚠ **`〈309〉`(RC-1 회수 후) 의 `103 / 84 / 19 · 자리 파일 227` 에서 +1 벌 · +3 파일 늘어 있다.** 원인은 이 회차가 안다 — **§5 의 화면 확인이 재굽기보다 먼저 돌면서 GK-2A ③지도형을 요청했고, 자리에 새 키가 없어 viz-render 가 **그 자리에서 구웠다**(`50dc2d4d…` · `21:59:37` · 3파일). **감추지 않는다** — 이것은 `POST /api/v1/previews` 가 아니라 **상세 화면의 평상 경로(GET)가 낸 staging WRITE** 다. 사용자가 화면을 열면 똑같이 일어나는 동작이고, Ted 승인 범위(같은 3 데이터셋 · ③층 재굽기)와 대상이 같다. **순서를 잘못 잡은 것은 이 창이다.**

## 4. 재굽기 — 공개 엣지 `POST /api/v1/previews` 3건 · 팔레트 `단색-파랑`

경로 = **공개 엣지 하나뿐**(A4 §6⑶). 베어러는 **경로로만**(`~/.colab-v2-staging-subjects.tokens.json`) · UA 매 요청 부착. SQL 쓰기 0 · 컨테이너 조작 0 · 파괴 플래그 0.

| 데이터셋 | renderId | 상태 | ①썸네일 | ②비지도형 | ③지도형 |
|---|---|---|---|---|---|
| `01M0YWDXN7CA13KB7MQC9PGEW5` GK-2A | `01M1P8MMM5G81K7KW754MXSYA8` | 202 → **완료** | `200 image/webp 3,934` | `200 image/png 40,142` | `200 image/png 36,635` |
| `01M1JM3JTQSSTKR9A2QQ67QN81` U-Net | `01M1P8MN2S2A300F9RG212PMZB` | 202 → **완료** | `200 image/webp 6,878` | `200 image/png 999,069` | `200 image/png 1,109,335` |
| `01M19RENXAANZ2HDWM4Z46C37Y` PV-1 | `01M1P8MNCV92X3AFB56K713D1Z` | 202 → **완료** | `200 image/webp 3,934` | `200 image/png 40,142` | `200 image/png 36,635` |

- **9층 전건 `200`** · `text/html` 0건 · 실패 0건. ①이 `image/webp` 인 것은 정본이다(`〈74〉`-㉮).
- `precisionBadge` = GK-2A·PV-1 **`투영 계산 격자`** · U-Net **`동봉 격자 적용`**. 색범위 단계 3건 모두 **`확정`**. 팔레트 3건 모두 **`단색-파랑`**.

### 4.1 캐시 키 전후 — GK-2A 3층 (⭑ **A4 §3 의 `[미확인]` 이 닫혔다**)

| 층 | 전 (`〈308〉` 재굽기 · `2026-09-04T01:05`) | 후 (이 창) | 갈렸나 |
|---|---|---|---|
| ①썸네일 | `ad863d60de2ff8e77e7cf87cb0fb63f69b18cfe49e45000eabde88325b680c34` | **같은 키** | ✗ |
| ②비지도형 | `f48dc45c5c11a81c01396ee629f3bf96772b5c5042703bbb4a83018aa2dc8647` | **같은 키** | ✗ |
| ③지도형 | `4e0ac3e3819227c04f718d2a6fb31a6107f6c5deb24047c7275db4be7f4e50f8` | **`50dc2d4da52c0f6f1d7e3478f7107ce2ad1a85ebb4b78c279c28646216b8d569`** | ✓ |

⟹ **갈린 층은 ③지도형 하나뿐이다.** A4 §3 이 「③ 한 층뿐인가 = `[미확인]`」으로 남긴 자리를 **실측이 닫는다** — 세 데이터셋 전부 ③만 새 키가 섰다(U-Net `a306fb2b…` → `d4a00edc…` · PV-1 `f7b007c5…` → `4bf57c39…`). `〈308〉` 회차에서 3층이 다 갈렸던 것은 **팔레트 입력이 섞여 있었기 때문**이고, 팔레트를 같게 둔 이번에는 **`downsample` 라벨이 물린 ③층만** 갈린다.

⚠ **`warp+blockavg` → `warp+nearest` 라벨 자체는 화면·사이드카 어디에도 드러나지 않는다.** 사이드카(`sidecarVersion: 2`)가 담는 것은 `layer`·`source(s)`·`baked_for`·`crs`·`bbox`·`width/height`·`pixel_size_m`·`created` 뿐이다. **라벨이 물렸다는 증거는 키 해시가 갈렸다는 사실 하나뿐**이고, 그것이 §5 의 채움률 변화로 뒷받침된다.

## 5. 소유 재실측 — 재굽기 **후** (축자)

```
산출물 소유 — 대상 한 벌 106 · 살아 있다 87 · 접수분에만 닿는다 0 · 고아 0 · 판정 불가 19
::warning::artifact-ownership — 판정 불가(구판) 19 벌이 **건수를 드러낸 채** 통과했다.
   구판 = sidecarVersion·baked_for 가 없는 사이드카. **고아가 아니라 보류다** (A-1 완료 정의 ⑴).
artifact-ownership green — 대상 106 벌 (살아 있다 87 · 접수분 0 · 고아 0 · 판정 불가 19) · 지도 타일 2 · 스냅숏 236 줄 · 삭제 0건
```

| 등급 | `〈309〉` RC-1 후 | 이 창 재굽기 전 | 이 창 재굽기 후 | 이 창 전체 차 |
|---|---:|---:|---:|---:|
| 대상 한 벌 | 103 | 104 | **106** | **+3** |
| 살아 있다 | 84 | 85 | **87** | **+3** |
| 접수분에만 닿는다 | 0 | 0 | 0 | 0 |
| 고아 | 0 | 0 | **0** | 0 |
| 판정 불가 | 19 | 19 | **19** | 0 |
| 자리 파일 | 227 | 230 | **236** | **+9** |

**⛔ 기대치 `+9 살아 있다` 는 서지 않았다. 실측은 `+3` 이다 — 그 차이를 통째로 설명한다.**

- 늘어난 벌 셋 = **③지도형 3개뿐**(GK-2A `50dc2d4d…` 21:59 · U-Net `d4a00edc…` 22:10 · PV-1 `4bf57c39…` 22:10).
- **①②층은 6개 전부 기존 키를 되돌려받았다** — 재굽기가 새 벌을 만들지 않았다(§4.1). A4 §6⑷ 의 「3 데이터셋 × 3층 = +9」는 **3층이 다 갈린다는 전제**에서 나온 수이고, 그 전제가 §4.1 로 무너졌다.
- 자리 파일 +9 = ③층 3벌 × 3파일(`.png`·`.pgw`·`.json`).
- ⛔ **「움직이지 않으면 재굽기가 안 일어난 것」(A4 §6⑷)** 이라는 판정 규약은 지켜졌다 — 계수가 **움직였고**, 그 움직임이 §4 의 renderId 3건·§6 의 채움률 변화와 세 갈래로 맞물린다.

**게이트가 `〈308〉`-era 옛 9층에 대해 말하는 것 — 축자대로 옮긴다.**

- **고아 = 0.** 옛 ③층 3벌(`4e0ac3e3…`·`a306fb2b…`·`f7b007c5…`)은 **고아가 되지 않았다.** 사이드카의 `baked_for.target_id` 가 살아 있는 데이터셋을 가리키므로 게이트는 그것들을 여전히 **`살아 있다`** 로 센다. **stale 이지만 소유자가 있다** — 게이트에 「stale」 등급이 없다.
- **판정 불가 = 19, 무변.** 구판 사이드카는 재굽기로 사라지지 않는다.
- **삭제 0건.** 게이트도 이 창도 자리에서 한 파일도 지우지 않았다.
- ⟹ **회수(`invalidation.reclaim_plan()` → `apply()`)가 필요한 대상이 이 창에서 새로 3벌 늘었지만, 게이트는 그것을 지목하지 못한다.** 이것이 `CR-2` ⑶ 이 지고 갈 자리다.

## 6. 현물 확인 — BF-1~BF-6 (사용자 눈에 보이는 것)

방법 = 실제 브라우저(Chromium 1440×1100)로 공개 엣지에 붙어 화면을 열고 DOM·계산 스타일·이미지 픽셀을 읽었다. 세션 토큰은 **경로로만** 읽어 `localStorage`(`colab.session.token`)에 주입했고 **값은 이 문서·로그 어디에도 남기지 않았다**. **쓰기 조작 0건**(업로드·등록·승인·삭제·수정 미실행).

| id | 무엇을 보는가 | 관찰 | 판정 |
|---|---|---|---|
| **BF-1** | 계보 그래프에 빈 칸이 없고 화살표가 선 칸 사이에만 있는가 | GK-2A 상세 — 렌더 칸 **2** · **빈 칸 0** · 화살표 **1**(= 칸 − 1) · `data-col` = `1`,`2` 오름차순 · 칸 폭 195 / 324 px(고정 178 px 아님) | **충족** |
| **BF-2** | ③지도형에 흰 줄·성긴 채움이 사라졌는가 | §6.1 표 — 채움률 3건 전부 **100.00 %** · 전결측 행 **0** · 전결측 열 **0** | **충족** |
| **BF-3** | 한계 상태가 처음부터 보이고 버튼이 무동작으로 안 보이는가 | 3 데이터셋 전부 — 「확대」 `disabled=true` ＋ `aria-disabled="true"` · 「원본 해상도까지 봤어요」가 **로드 시점부터** 표시 · 「축소」·「기본 배율로」는 활성 | **충족** |
| **BF-4** | 미리보기 머리에 데이터셋 이름·파일명·원본 격자, 범례에 변수명이 있는가 | 머리 = `미리보기 — GK-2A NDVI 100 m 경기남부·충청권 월평균 (2023-05)` 등 **3건 전부 이름 표기** · 파일명 `GK2A_NDVI_mean_202305.tif`·`Prediction_20230501.npy`·`pv1b_upload.tif` **3건 전부** · 격자 캡션 `격자 1280x1280` **U-Net·PV-1 2건**(GK-2A 는 자리째 빠짐) · 범례에 **`변수`** 칸 실재 | **조건부 충족** — 아래 ⚠ 둘 |
| **BF-5** | 프로젝트 화면 뿌리 여백과 설정 버튼 간격 | `.project-page` `padding: 24px 20px 40px` · `.project-detail` `padding: 32px 24px 96px` · `.gnb-settings` `gap: 6px` | **충족** |
| **BF-6** | 설명문 `／` 가 표시 단계에서 줄 나눔 되는가 | `dh-sum` = 이끔 문장 1(`자료명 GK2A_NDVI_mean_yyyymm.tif.`) ＋ 목록 **5개 항목** · 화면 텍스트에 `／` **0건** | **충족** (⚠ 정본 §8 문구 확인은 여전히 Ted 대기) |
| **BF-8** | (미수정) `/lab` 뿌리 여백 결함이 남아 있는가 | `/lab` 뿌리 = `appmain` · `padding: 0px` · 제목 「찾을 것이 정해져 있으면 물어보세요」 | **결함 그대로 — 기대대로** |

⚠ **BF-4 잔여 둘 — 감추지 않는다.**
⑴ **GK-2A 는 격자 캡션 자리가 통째로 빠졌다.** `formatSourceGrid` 가 「둘 다 없으면 자리째 뺀다」는 구현 의도대로 도는 것이고 결함이 아니지만, **Ted Q2(「상세에서 원본 픽셀 크기를 알 수 있게」)는 이 데이터셋에서 아직 안 선다.**
⑵ **U-Net 의 범례 변수명이 사람 말이 아니다** — 화면에 `변수` / `01M1JM2P8PX4XWWAKTQMN936AW`(원천 ULID)로 나온다. 서버가 `legend.variable` 에 그 값을 싣기 때문이고(`GET /api/v1/previews/{id}` 응답 실측), **프론트는 받은 값을 그대로 그린다.** GK-2A·PV-1 은 `band1` — 이것도 사람 말은 아니다. **`BF-4` 완료 정의(「범례에 변수명」)는 자리 기준으로는 섰고 값 기준으로는 안 섰다.**

### 6.1 BF-2 — ③지도형 픽셀 실측 (전 · 후)

`~/.colab-v2-staging/previews` 의 PNG 알파 채널을 직접 셌다(읽기 전용).

| 데이터셋 | 구판 (`warp+blockavg`) | 신판 (`warp+nearest`) |
|---|---|---|
| GK-2A | 821×1024 · 채움 **99.80 %** · 전결측 행 **2** · 열 0 | 821×1024 · 채움 **100.00 %** · 전결측 행 **0** · 열 0 |
| U-Net | 821×1024 · 채움 **48.72 %** · 전결측 행 **384** · 열 **181** | 821×1024 · 채움 **100.00 %** · 전결측 행 **0** · 열 **0** |
| PV-1 | 821×1024 · 채움 **99.80 %** · 전결측 행 **2** · 열 0 | 821×1024 · 채움 **100.00 %** · 전결측 행 **0** · 열 0 |

⟹ **Ted 4(성긴 채움)와 Ted 10·11 스크린샷의 가로 흰 줄이 실물에서 사라졌다.** 「전결측 행 2줄」은 `〈310〉`-㉰ 가 적은 그 두 줄이고, U-Net 의 **48.72 %** 는 같은 조항의 「채움률이 떨어진다」가 이 데이터셋에서 얼마였는지를 **처음으로 잰 값**이다.

## 7. 그 밖의 현물 (읽기 전용 · 베어러 경로 · UA 부착)

| 점검 | 결과 |
|---|---|
| `GET /api/v1/me` | **200** · `전창현` · `role 교수` · `labId 00000000000000000000HYMETS` |
| 값 조회 GK-2A (재굽기 뒤) | **200** · `available true` · **`0.4836253468321484`** · `exactness 원본과 같은 칸` |
| 값 조회 U-Net | **200** · `available true` · **`0.31521356105804443`** · `exactness 가장 가까운 칸` |
| 값 조회 PV-1 | **200** · `available true` · **`0.4836253468321484`** · `exactness 원본과 같은 칸` |
| 값 조회 DEM(대조군) | **200** · `available false` · `unavailableReason 자리에 산출물이 없다` — 재굽기 대상이 아니므로 정상 |
| `POST /api/v1/dataset-searches` `{"query":"강수","verified":true}` | **200** · **0행** · `degraded false` · `searchedCount 14` — 대조군(`verified` 없이)은 **3행** ⟹ 걸름이 실제로 걸린다 |
| `GET /api/v1/lab` | **200** · `고려대학교 수문학연구실` · `memberCount 3` |
| `GET /api/v1/datasets/01M0YWDXN7CA13KB7MQC9PGEW6` (없는 것) | **404** · `{"code":"NOT_FOUND","message":"없거나 연구실 경계 밖이다."}` — 중립 문면 |

- ⛔ **자기 연구실 묘비 410 은 이 창도 재지 못했다** — staging 에 `deleted_at` 행 0건이고 만들려면 삭제(파괴적 쓰기)가 필요하다. `〈308〉`·`〈310〉` 과 같은 자리다.
- ⚠ 브라우저 콘솔 오류는 `static.cloudflareinsights.com` beacon **연결 거부 4건**뿐 — `QA-SCREENS-20260903 §1` 이 「환경 귀책」으로 분류한 그것과 같다. 앱 귀책 오류 0건.

## 8. 끝 상태 8축

| # | 축 | 값 |
|---|---|---|
| ① | 서빙 회차 | **`f465d83a2ef1`** — 앱 5종 이미지 태그 실측 일치 · `:i2` 6/6 |
| ② | 배포 판정 | **GREEN (통과 15건 · SKIP 0)** ＋ 체인 GREEN (2건 · SKIP 0) |
| ③ | 컨테이너 | **8 healthy** · 호스트 노출 `0.0.0.0` **0건** |
| ④ | 게이트 (재굽기 전) | 선언 50 · **green 50 / red(판정) 0 / red(준비) 0** |
| ⑤ | 산출물 소유 | 대상 **106** · 살아 있다 **87** · 접수분 **0** · 고아 **0** · 판정 불가 **19** · 자리 파일 **236** |
| ⑥ | 재굽기 | 3 데이터셋 · renderId 3 · 9층 전건 200 · 실패 0 · **새 벌 3(③층만)** |
| ⑦ | 현물 | BF-1·2·3·5·6 충족 · BF-4 조건부 · BF-8 결함 유지(기대대로) · API 8건 전건 기대대로 |
| ⑧ | 삭제 | **0건** — 자리에서 한 파일도 지우지 않았다 |

## 9. 이 창이 재지 않은 것 · 못 닫은 것

1. **`〈304〉` 스파이크의 원인** — 후보 ⓓ 를 재고 **재현되지 않았다**. 후보가 하나 줄었을 뿐 원인은 `[미확인]`.
2. **값 조회가 무엇으로 자리를 찾는가** — ③층 키가 갈린 직후에도 `available:true` 였다(§2.1). 캐시 키가 아니다. `[미확인]`.
3. **BF-4 의 변수명 값** — 자리는 섰고 값이 사람 말이 아니다(§6 ⚠⑵). **새 결함으로 등록할지는 Ted 가 연다.**
4. **`BF-6` 정본 §8 「한 줄 요약」 문구 충돌** — 여전히 Ted 확인 대기. 위반이면 `git revert 02738e4`.
5. **옛 ③층 3벌의 회수** — 게이트가 `살아 있다` 로 세므로 지목되지 않는다. `CR-2` ⑶.
6. **410 묘비** — 도달 불가(파괴적 쓰기 필요).
7. **5xx 지속 시간** — 재는 계측이 없다.
8. **`review-pr1` 워크트리** — 이 창은 **무접촉**. staging 접촉 여부는 확인하지 않았다(A4 §7 ㉣ 의 그 판단이 실측으로 바뀌지 않았다).

## 10. 이 회차가 만진 것 · 만지지 않은 것

- **staging 쓰기 = 셋** ⑴ `deploy.sh --target staging` 1회(승인됨) ⑵ `POST /api/v1/previews` 3건(승인됨) ⑶ ⚠ **상세 화면 GET 이 낸 온디맨드 굽기 1벌**(§3.1 · 승인 대상과 같은 3 데이터셋 ③층이지만 **경로가 다르다** — 순서를 잘못 잡은 것은 이 창이다).
- **컨테이너 수명 조작 0**(`deploy.sh` 안에서 도는 것 제외) · **파괴 플래그 0** · **DDL 0** · **SQL 쓰기 0** · **삭제 0** · **push 0** · **병합 0**.
- `a2_pg`·`ai_pg` **무접촉**. `review-pr1` 워크트리 **무접촉**.
- 자격증명·접속 문자열 **미출력**.
