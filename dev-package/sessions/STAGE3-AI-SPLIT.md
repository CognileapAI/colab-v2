# STAGE3-AI-SPLIT — 「AI 는 전부 다음 단계」를 대장에 반영한다

- 회차 = 2026-08-30 · Claude · 워크트리 `lane1-measure` · 브랜치 `wt/lane1-measure`
- `main` 병합 = 성공(충돌 0) · 병합 후 기점 커밋 `c5d27ea`
- 접촉 범위 = `dev-package/work-items.yaml` 3항목의 `stage`·`note` 만. **코드·계약·DB·운영 스택 무접촉.**
- ⚠ **`PLAN-SoT.md §9` 에 직접 쓰지 않았다** — 번호 발급·등재는 오케스트레이터 소관(`SKILL.md §1-b-⑶`). 아래 §5 가 등재문이고 번호는 비워 두었다.

---

## 1. 판정 근거 — Ted 축자

- **「AI 는 전부 다음 단계」** (2026-08-30). 계보 제안이 여기서 말하는 AI 에 드는가를 직접 물어 **「예」**로 확인.
- **stage 3 = AI 전부, 그것만.** 자연어 검색 · AI 검색 · 계보 제안.
- **stage 2 = 그 밖 전부**, **편의 기능 묶음 포함**. 편의 기능 묶음이 stage 2 의 마지막 묶음이다.
- **승인 처리는 stage 2 에 남는다** — 260826 결정이 뺐으나 이 판정이 되돌린다. AI 가 아니다. 후속(연구실 대시보드 · E-01 적용 지점 표)도 같다.

이 판정이 **최신**이고, 아래 §2 의 모든 앞선 진술보다 뒤에 선다.

## 2. 연표 — 범위 진술을 날짜순으로

값을 지우지 않고 시점을 붙인다(`SKILL.md §5`).

| # | 날짜 | 출처 | 축자 | 뒤엣것이 뒤집었나 |
|:--:|---|---|---|---|
| ⑴ | 2026-08-23 | `PLAN-SoT §9 〈51〉` | 「GRIB 을 v2 범위에서 뺀다」 | **뒤집힘** — 260826 §2-3 |
| ⑵ | 2026-08-26 | `_결정_…260826.md` §2-5 | 「이번 릴리즈: 표·필터·정렬 기반 **조건 검색만**. **자연어 검색은 넣지 않는다**」 ／ 「단, 이건 "빼는 결정"이 아니라 **"순서 결정"**이다」 | **조건 검색 부분은 살아 있다** · 자연어 부분은 §2-15 가 보정 |
| ⑶ | 2026-08-26 | 같은 문서 §2-12 | 「**E-06 승인 처리 — 이번 릴리즈 제외**」 ／ 「업로드하면 즉시 등록되고 누구도 검토하지 않는다」 | ⭑ **뒤집힘 — 2026-08-30 Ted 판정(§1)이 stage 2 로 되돌렸다** |
| ⑷ | 2026-08-26 | 같은 문서 §2-13 | 「E-01 역할과 권한 — **이번 범위 밖으로 두되 문서는 보존한다**」 | ⭑ **뒤집힘 — 후속(E-01 적용 지점 표)이 승인 처리를 따라 stage 2 에 남는다** |
| ⑸ | 2026-08-26 | 같은 문서 `:192` | 「AI 계보 제안 카드가 화면 절반을 차지하나 **AI는 이번 릴리즈에서 전부 제외됐다**」 | ⚠ **문맥 = 핵심시나리오 목업 1건의 폐기 사유**다. 릴리즈 전체 판정으로 읽지 않았다. **다만 2026-08-30 판정과 방향이 같다** |
| ⑹ | 2026-08-26 | 같은 문서 §2-15 | 「**결정 2-5 는 낡은 정보 위에서 내려졌다**」 ／ 「이미 만들어져 있고, **품질이 확인될 때까지 켜지 않는다**」 ／ 「**끄는 비용이 0이다**」 | §2-5 의 자연어 부분을 **같은 날 보정** |
| ⑺ | 2026-08-26 | `PLAN-SoT §9 〈129〉` | 「`40 COLAB-기획/` 2026-08-26 문서 13건을 **정본으로 지정**한다」 ／ 「기존 에픽과 충돌하면 **이 13건이 이긴다**」 | 유효 |
| ⑻ | 2026-08-26 | `_작업계획서_P2라운드_260826.md` `R-4`·`R-5` | 삭제 목록에 「자연어 검색」·「E-06 승인 처리 전체」 ／ **그러나 §3.4 가 곧바로 「⑵ 이미 만들어졌다」·「⑶ 부분적으로 이미 만들어졌다 … 5번 절 판정 대상」**으로 되돌려 놓았다 | **그 자리에서 이미 미결로 돌아갔다** |
| ⑼ | 2026-08-28 | `PLAN-SoT §9 〈182〉` | 「**검색 진입점은 AI 검색 하나**」 ／ 「`ts_config` 한국어화는 `deferred` — **하지 않기로 한 것**」 | ⭑ **자연어 검색을 되살렸다** — 2026-08-30 판정이 다시 stage 3 으로 민다 |
| ⑽ | 2026-08-29 | `PLAN-SoT §9 〈197〉` | 「**이것은 「빼는 것」이 아니라 「되살리는 것」이다**」 ／ 「사람이 직접 고르는 것이 기본, **AI 추천은 버튼으로 받는 보조**」 ／ 「`K3` 는 그대로 `stage 2` 다」 | ⭑ **계보 제안을 되살렸다** — 2026-08-30 판정이 stage 3 으로 민다. **위상·발동 방식은 그대로 물려받는다** |
| ⑾ | 2026-08-29 | `PLAN-SoT §9 〈203〉` | 「`LV-2` 의 stage 는 `stage 1`」 ／ 「**새 기능이 아니라 배포된 표면 안의 정정**이다」 | ⭑ **유효 — 이번 이동에도 `stage1` 그대로다** |
| ⑿ | 2026-08-29 | `〈208〉`·`〈217〉` | 「합격선은 퍼센트가 아니다 — **미판정 0건 ＋ 판정 후 전건 통과**」 | 유효 — stage 3 완료 규칙으로 인계 |
| ⒀ | 2026-08-30 | `PLAN-SoT §9 〈224〉` | 「정본이 요구한 **Verified 우선 정렬이 실행 경로에 없고, 그 소유가 승인 처리다**」 | ⭑ **승인 처리 존치를 뒷받침한다** — §3 참조 |
| ⒁ | **2026-08-30** | **Ted 판정 (§1)** | **「AI 는 전부 다음 단계」** | ⭑ **최신 — 이 회차가 반영한다** |

## 3. 실측 하나 — 승인 처리 존치의 독립 근거

260826 §2-12 가 제외 사유로 든 축자는 「신규 스펙 전체(…62건)에 `Verified`·잠금·접근 요청·할 일 함이 **한 번도 등장하지 않는다**」였다.

이 회차가 직접 쟀다 —

- `Co-Lab_ver2_업로드_계보_260825_이태헌.html`(260825 판) — `Verified` **0건** · `접근 요청` **0건** · `잠김` **0건**. **§2-12 의 진술은 이 문서에 한해 맞다.**
- `…/에픽/E-02_데이터_찾기/documents/Policy_데이터_찾기.md`(260818 정본) — `Verified` **8곳**. 축자 셋:
  - `:117` 「기본 정렬 | 카탈로그: **수정일 최신순** · 검색: **Verified 우선 + 관련도**」
  - `:111` 「카탈로그 열 구성 | `데이터셋 · 주제 · Level(가공 단계) · 프로젝트 · 업로더 · 수정일 · 계보 · **Verified**`」
  - `:115` 「조건을 걸 수 있는 열은 주제 · Level · 업로더 · 계보 · **Verified** 다섯」

⭑ **이것이 판정의 무게를 바꾼다** — `Verified` 는 **조건 검색의 열이자 조건 열**이다. 그리고 조건 검색은 260826 §2-5 가 **이번 릴리즈에 남긴 것**이다(축자 「표·필터·정렬 기반 조건 검색만」). 즉 **승인 처리를 빼면 이번 릴리즈에 남기로 한 조건 검색의 열 하나가 값을 못 갖는다.** 2026-08-30 Ted 판정과 같은 결론에 실측으로 닿는다.

⚠ **두 문서가 갈린다 — 어느 쪽도 고르지 않는다.** 260825 판에 없고 260818 정본에 있다. **Ted 판정 대기**(§6-㈎).

## 4. 대장에 무엇을 고쳤나

**고친 것 = 3항목의 `stage` 와 `note` 뿐이다. `status` 는 한 건도 건드리지 않았다** — 그래서 산문(`03-HANDOFF §1`·`WORK-UNITS §11`)의 상태 표기와 어긋날 자리가 없다.

| 항목 | 종전 | 지금 | 근거 |
|---|:--:|:--:|---|
| 계보 제안 서비스 (`K3`) | `stage2` | `after_stage2` | 「AI 는 전부 다음 단계」 · 계보 제안 = AI 확인 |
| 자연어 검색 서비스 (`K4`) | **`stage1`** | `after_stage2` | 항목 이름 자체가 자연어 검색 |
| 제안 원장 (`K5`) | `stage2` | `after_stage2` | `K5 ⊇ K3` 엄격 직렬의 귀결 |

⚠ **`stage3` 을 쓰지 않고 `after_stage2` 를 썼다.** 사유 = 검사기의 허용값이 `{stage1, stage2, after_stage2, out_of_scope, unknown}` 5종이고(`gates/tools/work_item_consistency.py:87`), **검사기를 고치지 않는다**는 지시를 지켰다. `after_stage2` 는 「stage 2 뒤」라 Ted 가 이름 붙인 **stage 3** 과 뜻이 같다. **이름을 정식 값으로 세울지는 Ted 판정 대기**(§6-㈏).

**보존한 것 (`SKILL.md §7` — 지우지 말고 개정 표시)**

- `〈197〉`·`〈203〉`·`〈211〉`·`〈182〉`·`〈208〉`·`〈217〉`·`〈220〉`·`〈222〉` 는 **원문을 한 글자도 지우지 않았다.** 각 항목 `note` 에 「본문 그대로 유효하고 stage 3 이 물려받는다 · **바뀐 것은 시점 하나이지 내용이 아니다**」로 적었다.
- 종전 `stage` 값은 각 `note` 말미에 `／ 이전 ~~stage1~~` · `~~stage2~~` 로 남겼다.
- ⭑ **이미 지은 것은 폐기가 아니라 stage 3 진입 자산이다** — 각 `note` 에 실물 경로로 박았다:
  - 계보 제안 = `services/ai-service/src/colab_ai/domains/d10_suggestion.py` · `relay.py` 의 `honest_empty_suggestions` 네 갈래 · 시험 3종
  - 자연어 검색 = 평가셋 `eval/s2b-alayer/`(18건 · `run.py` · `baseline.json`) · 2세대 평가셋 · `LlmQueryInterpreter`／`LiteralInterpreter` 자동 폴백
  - **되돌리지 않는다.** 끄는 비용이 0 이라 되살리는 일이 아니다(260826 §2-15 축자 「되돌리기 쉽다」).

**옮기지 않은 것 — 이름으로 적는다**

- **승인 처리 · 연구실 대시보드 · E-01 적용 지점 표** — stage 2 유지. AI 가 아니다. §3 이 독립 근거.
- **편의 기능 묶음** — stage 2 유지, 마지막 묶음.
- **`LV-2`(자동 호출을 버튼으로)** — `stage1` 유지(`〈203〉`-㉯). 걷어낼 자동 호출이 stage 1 배포 표면 안에 있다.
- **검색 쪽 3건은 자연어로 깨끗이 부를 수 없어 남겼다** — §6-㈐.

## 5. `PLAN-SoT §9` 등재문 (번호 미발급 — 오케스트레이터가 발급한다)

```
| 〈  〉 | **stage 3 을 「AI 전부」로 가른다 — 계보 제안·자연어 검색·제안 원장 셋이 stage 2 를 떠나고, 승인 처리는 되돌아온다** | **판정 (2026-08-30 Ted) ＋ 반영 (Claude · 워크트리 `lane1-measure`).** 근거 산출 = `sessions/STAGE3-AI-SPLIT.md`. **코드·계약·DB·운영 스택 무접촉 — 산출은 대장 3항목의 `stage`·`note` 뿐이고 `status` 는 0건 변경이다.** **㉮ 확정 = 「AI 는 전부 다음 단계」** — 계보 제안이 여기 드는가를 직접 물어 **「예」**로 확인. **stage 3 = AI 전부, 그것만**(자연어 검색 · AI 검색 · 계보 제안) · **stage 2 = 그 밖 전부, 편의 기능 묶음 포함**(그것이 stage 2 의 마지막 묶음). **㉯ 이동 3건** = 계보 제안 서비스 `stage2`→`after_stage2` · **자연어 검색 서비스 `stage1`→`after_stage2`** · 제안 원장 `stage2`→`after_stage2`(`K5 ⊇ K3` 의 귀결). ⚠ **`stage3` 이라는 값은 쓰지 않았다** — 검사기 허용값 5종에 없고(`gates/tools/work_item_consistency.py:87`) **검사기를 고치지 않았다.** `after_stage2` 가 뜻으로 같다. **정식 값 신설 여부 = 판정 대기.** **㉰ ⭑ 승인 처리는 되돌아온다** — 260826 `§2-12` 축자 「**E-06 승인 처리 — 이번 릴리즈 제외**」를 이 판정이 뒤집는다. 사유 = **AI 가 아니다.** 후속 둘(연구실 대시보드 · E-01 적용 지점 표)도 같다. **㉱ ⭑ 실측이 ㉰ 를 독립으로 뒷받침한다** — `§2-12` 의 제외 사유는 「신규 스펙 62건에 `Verified` 가 **한 번도 등장하지 않는다**」였는데, **260825 판 HTML 에서는 `Verified` 0건이 맞고**(실측) **260818 정본 `Policy_데이터_찾기.md` 에서는 8곳**이다 — `:117` 축자 「검색: **Verified 우선 + 관련도**」 · `:115` 축자 「조건을 걸 수 있는 열은 … **Verified** 다섯」. **`Verified` 는 조건 검색의 조건 열이고, 조건 검색은 `§2-5` 가 이번 릴리즈에 남긴 것이다.** ⚠ **두 문서가 갈린 채로 적는다 — 고르지 않는다.** **㉲ 지우지 않았다** — `〈197〉`(계보 제안을 되살린 판정)·`〈203〉`·`〈211〉`·`〈182〉`·`〈208〉`·`〈217〉`·`〈220〉`·`〈222〉` **원문 무삭제.** 그 판정들이 정한 **위상·발동 방식·완료 정의·합격선은 stage 3 이 그대로 물려받는다 — 바뀐 것은 시점 하나이지 내용이 아니다.** 종전 `stage` 값은 `／ 이전 ~~…~~` 로 각 항목에 남겼다. **㉳ ⭑ 지은 것은 자산으로 인계한다** — 계보 제안 `d10_suggestion.py`·`relay.py` `honest_empty_suggestions` 네 갈래·시험 3종 / 자연어 검색 평가셋 `eval/s2b-alayer/`(18건)·2세대 평가셋·`LlmQueryInterpreter`／`LiteralInterpreter` 자동 폴백. **되돌리지 않는다** — 260826 `§2-15` 축자 「**끄는 비용이 0이다**」·「**되돌리기 쉽다**」. **㉴ `LV-2` 는 `stage1` 그대로다**(`〈203〉`-㉯) — 걷어낼 자동 호출이 stage 1 배포 표면 안에 있어 이 이동이 그 정정을 미루지 않는다. **㉵ 계수 (기준 = 대장 `stage` 필드 · 2026-08-30 실측)** — **stage 2 총 34 · done 13 · deferred 1 · 잔여 20**(open 16 · partial 3 · in_progress 1). 차단 유형별 잔여 = **지금 착수 가능 8** · **선행 대기 12** · 판정 대기 0 · 자격 필요 0. stage 1 open 이 **3 → 2** 로 준다(자연어 검색 서비스가 나갔다). **㉶ ⚠ 임계 경로가 stage 2 를 벗어난다** — 대장 `depends_on` 실측 최장 사슬은 **`T-1 → K4 → P4 → P6 → P7 → P8` 6홉**인데 `K4` 가 이제 stage 3 이다. **검색 화면(`P4`)이 stage 3 항목을 선행으로 걸고 있고, 그 뒤에 승인 처리 사슬 전체가 달려 있다.** `K4` 를 빼고 세면 stage 2 내부 최장은 **`T-1 → P4 → P6 → P7 → P8` 5홉**. **`P4` 의 `K4` 종속을 끊을 것인가 = 판정 대기.** **㉷ 검사기** — `work-item-consistency` **green**(대장 95건 · 진실원 대조 70행 · 체크리스트 49건 · 착수 후보 33행 · conflict 0) · `planning-freshness` **green**(임베드 15건 전건 MATCH). **㉸ 경계** — 운영 스택 무접촉 · `main` 병합·push 0 · `§9` 직접 기재 0(번호 발급은 오케스트레이터). *2026-08-30 Ted* |
```

## 6. Ted 판정 대기 — 애매해서 **남긴** 것

**전부 stage 2 에 그대로 두었다.** 덜 빼는 것은 되돌릴 수 있고, 더 빼면 거짓 완료가 된다.

**㈎ `Verified` 를 두 정본이 갈라 적는다.** 260825 판 0건 · 260818 정본 8곳. **어느 쪽도 고르지 않았다.** 갈림 그대로 기록.

**㈏ `stage3` 을 정식 값으로 세울 것인가.** 지금은 `after_stage2` 로 적었다. 세우려면 검사기 허용값과 실패 픽스처를 함께 늘려야 한다.

**㈐ 검색 3건 — 자연어인가, 조건 검색인가.** 260826 §2-5 가 **조건 검색은 남긴다**고 했으므로 건별로 갈랐다. **셋 다 남겼다.**

| 항목 | 판정 | 사유 |
|---|:--:|---|
| `ts_config` 한국어 재작성 (`T-1`) | **stage 2 유지** | 형태소 검색 설정이지 자연어 생성이 아니다. **완료 정의 ⓐⓒ 는 `〈182〉` 로 이미 `deferred`** 이고 남은 것은 ⓓ 뿐 — `[미확인]` |
| 카탈로그 키워드 입력 제거 (`F-2`) | **stage 2 유지** | 실제 잔여가 **문구 정합 1건**이다(2026-08-29 실측). 자연어 기능이 아니다 |
| 검색 히어로 · 검색 결과 (`P4`) | **stage 2 유지** | **화면**이다. 순위는 `ts_rank_cd` 단독이고 `〈182〉`-⑶ 이 「staging 에서 LLM 이 한 번도 돌지 않는다」로 실측 |

⚠ **㈐ 가 ㉶ 을 낳는다** — `P4` 는 stage 2 인데 `depends_on` 에 `K4`(stage 3)가 있다. **끊을 것인가 = 판정 대기.** 끊지 않으면 승인 처리·연구실 대시보드·E-01 적용 지점 표까지 stage 3 뒤로 밀린다.

**㈑ 검색 품질 합격선의 거처.** `〈217〉` 이 `K4`·`P4` 에 걸었는데 지금 둘이 다른 단에 있다.

**㈒ 완료 정의 미작성** — 이 회차에서 새로 발견한 것 0건. `R0`·`R1`·`R2` 의 `완료 정의 미작성` 은 종전 그대로이며 **지어내지 않았다.**
