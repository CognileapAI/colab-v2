# 밤샘 진행 계획 — 2026-08-23

> 이 문서가 이번 밤의 **작업 정본**이다. 각 레인의 에이전트는 자기 칸만 채우고, `03-HANDOFF.md` 는 메인 세션이 마지막에 한 번만 갱신한다.
> 승인 = Ted, 2026-08-23. **4레인 전부 · A3 staging 배포까지 자율**(`PLAN-SoT §9-㊵` 이 staging 을 자율로 두고 AWS 만 승인으로 묶었다).

---

## 0. 전제 — 사람이 막는 것이 하나도 없다

`㊷`(모델)·`㊸`(온톨로지)·`㊶`(검색 엔진)·`㊴`(D1)이 닫히면서 **열린 결정 ⑧⑨⑩이 전부 해소**됐다.
남은 사람 의존은 **AWS 계정(I0)** 과 **OpenAI API 키(K3·K4)** 둘뿐이고, 이 밤의 어느 레인도 그 둘을 필요로 하지 않는다.

## 1. 레인

```
레인 A  플랫폼 임계경로 (순차 — 이 밤의 성패)
  A1 core-api 골격 — 34 라우트 · 실질의 5 · 501 두 종
  A2 스코프 커널 음성 4종 + body_access 실효 2종     →  P0 닫힘
  A3 I2 walking skeleton — 5개 배포 단위를 staging 에 빈 상태로  →  분수령
  A4 P1 착수

레인 B  게이트                      레인 C  지식
  B1 D2b 이벤트 계약 게이트           C1 K1 온톨로지 스키마 (시드 테이블 3개)
  B2 generated-up-to-date (11번째)    C2 K2 시드 적재
  B3 D3b RLS 실효 증명                (K3·K4 는 OpenAI 키 대기 — 이 밤에 못 연다)

레인 D  인프라
  D1 IS3 staging 백업 + 복원 리허설
  D2 IS4 terraform state 복구 절차
```

의존: A2←A1 · A3←A2 · A4←A3 · B3←A2 · C2←C1. 그 밖은 전부 병렬.

## 2. 멈춤 규칙 (사람이 없는 동안 미리 못 박는다)

1. **게이트 red 는 우회하지 않는다.** red 면 그 WU 를 🟧 로 남기고 다음으로 넘어간다. 검사 대상을 줄여 green 을 만드는 것은 금지다 (`CLAUDE.md §4`)
2. **정본이 값을 주지 않는 자리는 만들어 채우지 않는다.** `[정본 무근거]` 로 남기고 보고한다 (`§9-㊴-②`)
3. **경계를 넘어야 하면 멈추고 기록한다** — Port 추가 / 도메인 재분할 / 기획 확인 중 어느 것인지 (`CLAUDE.md §4`)
4. **`contracts/` 는 동결이다.** 계약을 고쳐야 green 이 되는 상황은 **보고 대상**이지 작업이 아니다
5. **AWS 는 건드리지 않는다.** staging 까지가 자율선이다 (`㊵`)
6. 커밋은 메인 세션이 WU 단위로 한다 — 레인 에이전트는 커밋하지 않는다(같은 작업트리 병렬 충돌 회피)
7. `03-HANDOFF.md` · `PLAN-SoT.md` 는 **메인 세션만** 쓴다. 레인은 자기 `sessions/`·`reports/` 문서에 적는다

## 3. A1 core-api — 확정된 설계 판단

`sessions/P0.md §4 산출물 #4`. advisor 검토(2026-08-23)로 초안 3곳을 고쳤다.

| 항목 | 확정 | 근거 |
|---|---|---|
| 라우트 | fe-core.yaml **34개 전부** 등록 | 계약 동결분. op 수는 34 다 (27 이라 적은 중간 보고가 있었으나 yaml 발췌 절단이었다) |
| 실질의 | **5개만** — `getCurrentAccount` `getLab` `listDatasets` `listDatasetFiles` `createProject` | 이 5개가 음성 4종(읽기·자식·미스코프·쓰기 WITH CHECK)을 HTTP 층에서도 덮는다. 더 붙이면 P1 을 앞당겨 먹는다 |
| 나머지 29개 | **501** + `ErrorEnvelope.code` 두 종 — `NOT_IMPLEMENTED_NO_STORE`(저장처 자체가 없다: access-request 4 · verification-request 2 · downloadDataset) / `NOT_IMPLEMENTED_P1`(로직 미착수) | "저장처 없는 7건만 501" 로 하면 **나머지 27건이 200 으로 거짓말**한다. P1 이 하나 구현할 때마다 501 목록이 줄어드는 것이 진척 계측이 된다 |
| 404 | **쓰지 않는다** | `§9-㊱` 이 404 를 「경계 밖」으로 못 박았다. 미구현에 404 를 쓰면 거짓말이다 |
| 계약 수정 | **없다** | 501 은 계약이 아니라 **배포 상태**다. 계약에 박으면 P6 이 구현한 뒤 지울 때 oasdiff 가 breaking 을 낸다. `contract-lint` 는 yaml 만 보므로 충돌하지 않는다 |
| 스코프 커널 | 트랜잭션 스코프 **`SET LOCAL`** + 요청 경계 commit/rollback | 풀 커넥션에 비-LOCAL `SET` 을 쓰면 다음 요청으로 lab_id 가 샌다 |
| 앱 DB 롤 | **NOBYPASSRLS · 비소유자** | 앱 롤이 superuser/BYPASSRLS 면 FORCE RLS 가 무력이고 음성 테스트가 **거짓 green** 이 된다 |
| 인증 | 계약이 이미 답했다 — `sessionSubject` bearer, 「개발자가 계정을 심어 제공한다」(P-17), 실제 수단은 P1 | `labId` 를 요청 파라미터로 받지 않는다. 헤더로 lab_id 를 주입하는 임시 경로를 만들지 않는다 |
| 정규 ID 코드측 | `colab_core` 의 `Ulid` 타입을 여기서 만든다 | DB DOMAIN 만 있고 파이썬 측이 없다 (`산출물 #3` 의 코드 쪽) |
| 스택 | Python 3.12 + FastAPI + SQLAlchemy 2.x | `PLAN-SoT §9-㊳` |

## 4. 정정 — 낡은 기록 2건

| 문서 | 낡은 서술 | 실측 |
|---|---|---|
| `sessions/P0-schema.md §7-①` | "schema-diff 는 `COLAB_APPLIED_DB_URL` 하나만 받아 두 체인을 동시에 green 으로 만들 수 없다 — P0 의 열린 블로커" | **이미 고쳐져 있다.** `gates/tools/schema-diff.sh` 가 `COLAB_APPLIED_DB_URL_PLATFORM`·`_AI` 를 받고 구 변수 단독은 red 를 낸다. 실증 = `reports/p0-schema-verify-20260823.md` |
| advisor 지적 "`unlinkProjectDataset` 만 있고 link 가 없다 = 계약 결손" | — | **결손이 아니다.** 계약이 명시한다 — 「담는 동작은 이 seam 에 없다」, 업로드 화면(E-04)이 맡는다 |

## 5. 진행 기록 (레인이 채운다)

| WU | 상태 | 산출 | 비고 |
|---|---|---|---|
| **선행 실증** schema-diff | ✅ | `reports/p0-schema-verify-20260823.md` | 단일 실행에서 **두 체인 모두 green**. `P0-schema §7-①` 정정 완료 → **P0 완료판정 #1 충족** |
| A1 core-api 골격 | ✅ | `sessions/P0-core-api.md` · `services/core-api/` | 34 라우트 전부 등록(오라클 diff) · 실질의 5 · 501 두 종 29. 경계 게이트 3종 red→green · pytest 86 green. 열린 것 2건: 잠긴 데이터셋 `fileCount`(스키마 개정 필요) · 계보 상태 `원천`/`기록 없음` 판정 [정본 무근거] |
| A2 음성·실효 테스트 | ✅ | `sessions/P0-rls-proof.md` · `services/core-api/tests/` | 음성 4종(읽기·자식·미스코프·쓰기 WITH CHECK) + `body_access` 실효 2종(음성 2방향·양성 3종) **전부 red→green**. **red 증명 9회 자동화**(`tests/fixtures/red-proof.sh`) — 보호 장치를 하나씩 떼어 red 를 실제로 만들었다. 앱 롤 실측 = `rolsuper=f·rolbypassrls=f`, 테이블 20개 전부 `colab_owner` 소유(앱 소유 0), FORCE RLS 18개 → **거짓 green 아님**. 같은 묶음을 superuser 로 돌리면 13 failed 로 무너지는 것까지 확인. `SET LOCAL` 누수 없음(커넥션 1개 풀·`pg_backend_pid` 동일 확인 + 비-LOCAL 대조군이 실제로 새는 것으로 탐지력 증명). pytest **106 green**. A1 이 남기지 않은 시드를 `tests/fixtures/seed.sql` 로 레포에 들였다(A1 86건 무수정 green). **→ P0 완료판정 #4 충족 · `§9-㉖` 닫힘.** 남은 것 = **게이트 승격은 D3b(B3)** — `gates/` 금지 범위라 손대지 않았다 |
| A3 I2 walking skeleton | ✅ | `sessions/I2.md` · `infra/staging/` (compose.i2 · nginx.i2 · deploy/rollback · db-bootstrap · scope-proof · migrator) · `services/*/Dockerfile` · `frontend/Dockerfile` | **5개 단위 전부 공개 주소에서 헬스 green** (`/healthz/<unit>` 이 각자 `unit` 이름으로 응답 · 컨테이너 헬스체크 7/7 healthy). **배포 1회 · 롤백 1회 성공** — 04:29:07 자리표시 → 04:34:26 skeleton → 04:36:45 자리표시 복귀 → 04:37:39 skeleton(최종). 네 시점 모두 `/healthz` **200**, 530 구간 없음(엣지 컨테이너 교체 방식이라 무중단). 코드만으로는 판정 불가라 **본문까지 대조**했다. **postgres 를 올렸다** — 두 체인을 **두 데이터베이스**로 분리(platform head `0001_p0_platform` 테이블 20 / ai head `0003_k2_ontology_seed` 시드 22행). 앱 롤 실측 `rolsuper=f·rolbypassrls=f`·소유 테이블 0·FORCE RLS 18 — **P0-rls-proof 와 숫자 동일**. staging 실물 대상 스코프 증명 5종(음성 3 · 양성 1 · 잔여물 0). **터널 선언 무수정**, `terraform plan` = `No changes.` — 라우팅은 오리진 안쪽 nginx 가 가른다. 호스트 노출 `127.0.0.1:3000` 하나, `0.0.0.0` 0건. 열린 것 2건: **주체 표 미배치 → `/api/v1/me` 401**(P1) · platform 시드 0행. **IS3 진입조건 개방** — 붙일 설정 4줄은 `I2.md §6`(실행은 IS3 몫). 이 호스트 특성 1건: docker root 가 9p 라 named volume 불가 → WSL ext4 바인드(경로는 홈 0600 env) |
| A4 P1 착수 | ⬜ | | |
| B1 D2b | ⬜ | | |
| B2 generated-up-to-date | ⬜ | | |
| B3 D3b | ✅ | `sessions/D3b.md` · `gates/tools/rls-effect.sh` · `gates/tools/rls-effect-selftest.sh` · `gates/run.sh` · `gates/README.md` · `.github/workflows/ci.yml` | **① `db-selftest` red 원인 해소** — 픽스처 케이스가 레포 `rls-allowlist.toml` 을 읽어, K1 의 **정당한** `d9_*` 3줄 추가가 「낡은 면제」로 기준 케이스를 red 로 만들었다. 게이트가 옳고 selftest 배선이 틀린 경우 → 픽스처가 **자기 allow-list** 를 들도록 고쳤다. 「낡은 면제」 검사·`d9_*` 3줄 **둘 다 그대로 둔다**. 실 레포 `rls-coverage` = green(21건). **② 게이트 승격 `rls-effect`** — 오라클 3종(본체 음성 0행 / 잠긴 메타 양성 `P-13` / cross-tenant `lab_id` 보유 18표 전수 0행 + 미스코프 0행) 전부 green, red fixture **8종**(경계 `USING(true)`·자식만 누락·미스코프 기본값·`body_access` 제거·만료검사만 제거·둘째 층 PERMISSIVE·메타에 RESTRICTIVE·메타에 정책 추가) 전부 red. **③ 잘못된 롤은 red** — `rolsuper`·`rolbypassrls`·소유 테이블 3중 확인 + 세션 안 재확인. superuser 주입·`BYPASSRLS` 부여·소유자 이전·없는 롤 **4방향 red**. 인프라 red 5종(스키마·시드·앱롤 부재 / 적용 불가 스키마 / 도커 부재). `rls-effect-selftest` **18/18**, `selftest` 묶음에 편입 · CI `schema-gates` 에 배선. 전 게이트 스윕 **red 0** (증명 5셋 개별 green — contract 15·event 33·boundary 30·db 43·rls-effect 18). 일회용 컨테이너는 `b3_rlseffect_*`(포트 publish 0) 뿐 — `colab_v2_*` 무접촉. `gates/`·`.github/` 외 무수정, 커밋 안 함. **여전히 못 잡는 것 = HTTP 층**(순수 SQL 판정이라 403/404 구분은 아직 pytest 에만 있다) — `D3b.md §⑤` |
| C1 K1 | ✅ | `db/ai/schema.sql` 시드 테이블 3개 · `versions/0002_k1_ontology.py` · `sessions/K1.md` | 게이트 4종 green(schema-diff·migration-single-head·rls-coverage·ai-no-lineage-write) · 예시 인스턴스 왕복 확인. 세 표 **RLS 면제**(연구실 공통 지식 — 근거 `K1.md §3`). `rls-allowlist.toml [ai]` 에 이름 3개 추가(파일 주석이 지정한 흐름) |
| C2 K2 | ✅ | `db/ai/seed/k2-coverage-standard.tsv`(기준 21항목) · `seed/k2_ontology_seed.sql` · `versions/0003_k2_ontology_seed.py` · `tools/k2-coverage-check.sh` · `sessions/K2.md` | **커버리지 미커버 0건**(어휘 13 + 주제 4 + 지명 4 = 21, 항목마다 정본 인용 위치 기재). 22행 적재 · `source_note` 빈 행 0. 시드는 **체인 안**(모든 환경이 같아야 하고 K3·K4 가 전제한다) · `ON CONFLICT` 멱등. 체크 **red 5방향 실증**(행 삭제 2종 · 빈 DB · 접속처 없음 · 기준 축소). 게이트 4종 green(migration-single-head `db/ai` 리비전 3 · head 1 / rls-coverage / schema-diff / ai-no-lineage-write). `gates/` 무수정 — 체크는 `db/ai/tools/` 에 뒀다. **[정본 무근거] 5건** = `K2.md §5`, 특히 **주제 동의어는 정본이 `강우데이터` 하나만 줬고 지명 별칭은 0건**이다 |
| D1 IS3 | ✅ | `sessions/IS3.md` (§7~12 추가) · `infra/staging/backup/` | **실 staging 대상으로 닫았다.** I2 가 postgres 를 올려 진입조건이 열렸고, 붙이는 일은 「설정 한 줄」이 아니라 **두 체인을 두 백업으로 가르는 일**이었다 — 백업을 프로파일 목록(`platform`·`ai`)으로 바꿨다. **실백업 GREEN** — platform 5,635B(테이블 20 · 행 1) · ai 2,427B(테이블 4 · 행 23), C1~C6 전부 PASS. **복원 리허설 GREEN** — 일회용 `is3_pg_*` 에 복원해 테이블 수 · 테이블별 행 수 · **내용 md5** 전부 원본과 일치, K2 시드 **22행** 별도 확인(살아 있는 DB 는 읽기만). **스케줄 설치함**(03:30 백업 · 월 04:10 재검사, cron 데몬 가동 확인). 실패 가시성 = 로그 + `BACKUP-FAILED.txt` 표식(다음 성공에서만 삭제) + `LAST-SUCCESS.txt` + 주간 신선도 재검사 → **침묵 상한 1주**. push 알림은 여전히 없다(레포 밖 결정). **fixture 11건 전부 red** — F9 신규(두 체인 중 한쪽만 백업된 부분 성공 = RED). `/healthz` 200 → 200, 컨테이너 8개 무손상. 남은 것: `gates/run.sh` 편입(다른 레인이 `gates/` 점유 중 — 완료 오라클 밖이라 판정을 막지 않는다) · 알림 채널 · 오프호스트 사본 |
| D2 IS4 | 🟧 | `sessions/IS4.md` · `infra/staging/tunnel/README §5-1` | 복구 절차 저작 + **리허설 실행**(scratch 에 `.tf` 만 복사 → `init→import→plan`, 실자격증명). 원본 state md5 동일 · 실디렉터리 `plan` = `No changes.` · staging 내내 200. **미달 = 마지막 `apply` 미실행**(실 터널 건드림, 범위 밖) **· 맨몸 호스트 조건(terraform 미설치·자격증명 파일 없음) 미실험.** 원격 백엔드는 I0 에 걸었다 |


---

## 6. 밤샘 결산 (2026-08-23)

**닫힌 WU 8개** — P0 · I2 · IS3 · K1 · K2 · D2b · D3b · D3
**🟧 1개** — IS4(마지막 `apply` 미실행 · 맨몸 호스트 조건 미실험)

| 무엇이 달라졌나 | 전 | 후 |
|---|---|---|
| 게이트 | 10종 구현 · 자기증명 83 케이스 | **12종 구현 · 139 케이스** (`generated-up-to-date` 만 미구현, 설계대로 red) |
| 코드 | 없음 | core-api 34 라우트(pytest 106) · FE 셸 · 빈 도메인 자리 3개 |
| staging | 자리표시 페이지 하나 | **5개 배포 단위 + postgres 2체인** · 백업 스케줄 |
| T-D | D3 🟧 | **전부 닫힘** |
| 사람 대기 | AWS · OpenAI 키 | 그대로 (둘뿐) |

**밤새 지킨 선**
- 게이트를 우회하거나 검사 대상을 줄여 green 을 만든 적이 없다. `db-selftest` 가 red 였을 때도 **게이트가 옳고 배선이 틀렸다**로 고쳤다
- 정본이 값을 주지 않는 자리를 만들어 채우지 않았다. K2 는 정본이 준 동의어가 `강우데이터` 하나뿐이라 하나만 넣었다
- 절반을 완료로 세지 않았다. IS4 는 리허설이 사실상 성공했지만 🟧 다
- AWS 를 건드리지 않았다 (`㊵`)

**아침에 사람이 볼 것**
1. `㊹`·`㊺` 두 결정 — Claude 판단이고 **비토 가능**하다
2. IS3 의 정직한 한계 — 백업 실패가 **가서 봐야 보이는 자리**에만 남는다. push 알림 채널은 레포 밖 결정이다
3. `ai-no-lineage-write ⑧` 이 무뎌졌다 — ai-service 에 빈 도메인 자리가 생기면서 「AI 가 계보에 쓰지 않는다」의 증명이 다시 공허해졌다. **K3 가 실제 코드를 넣을 때 조여야 한다**
4. 먹통 `oasdiff` 컨테이너 2개가 남아 있다(권한 문제로 정리하지 못함). staging 에 영향 없음
5. `P0-core-api §5` 의 미해결 — 잠긴 데이터셋의 `fileCount` 가 계약상 `minimum: 1` 인데 `body_access` 가 막아 실제 0 이다. P1 이 만난다
