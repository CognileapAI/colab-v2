# Ted 판정 셋 집행 회차 — `〈230〉`·`〈231〉`·`〈232〉` (2026-08-30 · 워크트리 `lane-canon`)

> **성격 = 문서·계약 회차다.** 화면 기능은 짓지 않았다 — 계약을 동결한 자리까지가 이 회차이고,
> 그 위에 화면을 세우는 것은 `P3` 화면 레인 몫이다.
> **값과 근거는 `PLAN-SoT §9 〈230〉`·`〈231〉`·`〈232〉` 에 있다. 여기는 그 회차의 실행 기록이다.**
> **증거(EVIDENCE)와 해석(INTERPRETATION)을 갈라 적는다** · **재지 않은 수는 적지 않는다.**

---

## 0. 한 줄 결론

`P5` 를 **닫았다**(완료 정의의 순환을 끊고 셋을 재서 전건 충족) · 스크린샷 중계 op 을 **11차 동결 해제로 열고 같은 회차에 구현했다** ·
확대(줌)의 **정본 근거와 닫는 조건 여섯**을 `Policy_데이터셋_상세 v2.5` 에 적었다.
게이트는 **before green 31 / red 2 → after green 31 / red 2** 로 같고, 이번 변경이 만든 red 는 **0건**이다.

---

## 1. `〈230〉` — `P5` 완료 정의의 순환 제거

**무엇이 순환이었나 (EVIDENCE).**
대장 `P5.completion_def` 종전 문언 = 「「각 P 의 완료 판정」 4항 (`§7` 말미) ＋ **P6(승인)**·**`§1.5` 완료 조건**」.
`P6.depends_on` = `[P1, P2, P3, P5]`. **`P5` 가 `P6` 를 완료 정의로 들고 `P6` 가 `P5` 를 선행으로 들면 둘 다 정의상 닫힐 수 없다.**

**`§1.5` 는 인용 착오였다 (EVIDENCE).** `WORK-UNITS §1.5` 는 「v2 가 끝났다」의 조건 **8항**이고 한 P 의 완료 정의가 아니다.
⚠ 게다가 `03-HANDOFF` 가 그 절을 가리키는데 **그 문서에는 `§1.5` 라는 절이 없다.**

**새 완료 정의 = 구현 ＋ 게이트 green ＋ staging 배포 green.**
⚠ **「목업 대비 화면 검증」을 `P5` 에 다시 걸지 않는다** — 그 축은 전 화면 대상 `G1b`(⬜)가 따로 진다.

**셋을 건별로 쟀다 (EVIDENCE) → 전건 충족 → `partial` → `done`.**

| # | 조건 | 실측 |
|---|---|---|
| 1 | 구현 | op 셋(`deleteProject`·`setProjectStatus`·`unlinkProjectDataset`) ＋ 모달 셋(F-03·F-04·F-05) 전건 착지 · **501 표 19 → 16** · 잔여 0건 (`〈229〉`-㉲) |
| 2 | 게이트 green | `gates/run.sh all` **green 31 / red(판정) 1(`autometa-loss`) / red(준비) 1(`preview-tile-slot`)** — 둘 다 stage 2 미가동에서 오는 구조적 red (`RESTART §④-㉯`) |
| 3 | staging 배포 green | 2026-08-30T18:39:07+0900 · 릴리스 `eb61556de245` · 배포 판정 **GREEN**(통과 15 · SKIP 0) |

**같은 오류가 다른 `P` 항목에 있는가 = 없다 (전수 실측).**
`P0`~`P8` 아홉의 `completion_def` 를 전건 읽었다. 「P6(승인)」 또는 「`§1.5`」를 완료 정의로 든 항목은 **`P5` 하나뿐**이다.
`P0` 의 「완료 판정 8행」은 `§1.5` 가 아니라 **`§7` 의 8행**이고 순환이 아니다. `P4` 의 긴 문언에 `P6` 이라는 낱말이 나오지만
그것은 **정렬 소유를 적은 서술**이지 완료 조건이 아니다. **고칠 것 0건.**

**정직하게 (INTERPRETATION).** 목업 대비의 실측 기록은 **반쪽**이다 — 모달 셋과 데이터셋 표에는 목업 인용이 코드 머리글에 있고
(`ProjectFormModal`·`ProjectCloseModal`·`ProjectDatasetTable`), **목록·상세 골격(`ProjectCards`·`ProjectTable`·`ProjectToolbar`)에는 없다.**
그 축을 `G1b` 로 보낸 것이 이번 판정이고, **축이 사라진 것이 아니다.**

---

## 2. `〈231〉` — 11차 동결 해제 · 스크린샷 중계 op

**밟은 절차 = `sessions/X2-FREEZE-PROTOCOL.md` (2026-08-27 확정 프로토콜).**
등급 판정 `§5` → 증거 `§5-㉱` 7항 → 기록 `§5-㉲` 8필드 → 회차 번호 등재(`§1`).

**등급 = ㉯ (Ted 승인 필수).** 게이트 계수로는 순수 가산(`contract-breaking` ERR 0)이지만
`§5-㉯` 가 **「op 신설 — 501 표·op 총계가 움직인다」**를 사람 판정으로 못 박는다. **등급은 게이트 수가 아니라 성질로 정한다.**

**신설한 것 = `createPreviewScreenshot` (`POST /preview-screenshots`) 1 op.**
요청 스키마는 `core-viz.yaml#ScreenshotRequest` 를 **참조만** 하고 재선언하지 않는다.
응답 = `200 image/png` · `400`·`401`·**`403`**·`404`·`409`·`503`·`500`.

> ⚠ **경로를 `core-viz` 의 `/screenshots` 와 일부러 다르게 뒀다** — 같은 경로면 `contract-breaking` 의 합성 비교가
> 두 표면을 한 엔드포인트로 합쳐 버린다(`listPalettes` 가 `/preview-palettes` 로 갈린 것과 같은 이유).

**증거 (`§5-㉱` 7항 · EVIDENCE).**

| # | 증거 | 출력 |
|---|---|---|
| 1 | `contract-breaking` | green — 「No breaking changes to report, but the specs are different」 (ERR 0) |
| 2 | `contract-lint` · `generated-up-to-date` · `seam-consistency` | 전건 green — lint(seam 3 · 위반 0) · generated(등기부 4건 재생성 일치) · seam(G-e 348 · G-b 7 · **㉠ 신설 1건 대조 통과** · ㉡ 18) |
| 3 | 소비자 수 | **0건** — `grep -rn 'createPreviewScreenshot\|preview-screenshots' frontend/src services`(생성물·본 회차 산출 제외) → 0 |
| 4 | 마이그레이션 0 | `git diff --stat -- db/` → **0행** |
| 5 | 집행 증명 | **모의가 아니다** — 실 HTTP 서버(가짜 viz)를 세워 PNG 바이트가 화면까지 손대지 않은 채 오는 것을 쟀다(`r.content == PNG_BYTES` · POST 왕복 1 · 경계 헤더 대조). **오라클임도 증명했다** — 권한 판정 줄을 걷어 내니 `test_the_screenshot_needs_the_upload_edit_switch` 가 **red**, 되돌리니 green |
| 6 | 회귀 수치 | op 총계 **53 → 54** · **501 표 불변** · core-api **500 → 505 passed / 0 failed** · `fe-core.ts` **+138 줄**(명령 재생성) |
| 7 | 이번에 세지 않은 축 | ㈎ **실서버 왕복**(staging 의 진짜 viz-render 로는 안 쟀다) ㈏ **화면 층**(버튼·권한 표시) ㈐ **PNG 실물**(바이트를 지나 보내는 것만 쟀다) |

**`§5-㉰-4`(집행 없는 신설 금지)를 지켰다.** 신설과 **같은 회차에** core-api 중계를 구현했다 —
`routes/preview.py::create_preview_screenshot` ＋ `app/relay.py::HttpPreviewRelay.screenshot`
(바이너리 전송 `_request_binary` 신설 — `image/png` 는 `json.loads` 로 못 받는다) ＋ `ports/relay.py` 표면.

**도메인 경계 (`CLAUDE.md §3`).** core-api 가 하는 판정은 **둘뿐**이다 —
⑴ **편집 권한**(`업로드·편집`. `core-viz.yaml` 이 그 자리를 여기에 넘겼으므로 없으면 **아무도 안 하는 판정**이 된다)
⑵ **연구실 경계**(**404 로 답한다 — 경계 밖은 존재를 알리지 않는다**).
**geo 라이브러리 0줄** · `banned-import` green · **생성물 손수정 0**(등기부 명령으로만 재생성).

⚠ **화면의 버튼은 아직 없다.** FE 주석의 낡은 서술(「FE 도달 계약 표면이 없다」)만 정정했고,
버튼을 세우는 것은 `P3` 화면 레인 몫이다.

---

## 3. `〈232〉` — 확대(줌)의 정본 근거와 완료 기준

**개정 전 실측 (EVIDENCE).** 정본 `Policy_데이터셋_상세.md` 전문에 `확대`·`줌` **0건**.
지시는 대장 `P3.completion_def` 의 「타일 서빙·**확대**」 한 낱말뿐이었고 **무엇을 지을지가 어디에도 없었다.**

**백업을 먼저 떴다** — 정본은 git 저장소가 아니라 **사본이 유일한 롤백**이다(`〈67〉` 회차와 같은 관행).
자리 = `<작업공간>/40 COLAB-기획/_backup_260830_ZOOM/` (md 1 · 패키지 html 1).

**개정한 것 = `Policy_데이터셋_상세` v2.4 → v2.5 (`updated: 2026-08-30`) · 네 자리.**

| 자리 | 무엇을 |
|---|---|
| `§2` 업무 흐름 표 | 「지도를 확대·축소하거나 끌어 옮긴다」 행 추가 |
| `§8` 화면 동작 표 | **`확대·이동`** 행 추가 — 보기 권한 · 렌더 재요청 없음 · 데이터 해상도가 한계 · 층 공통 · 상태 미저장 |
| `§8` | **「확대(줌) — 왜 넣는가, 무엇이 되면 된 것인가」** 블록 신설 (근거 ＋ 조건 6) |
| `§8` 스크린샷 행 | 「확대한 자리와 배율도 "지금 장면"에 포함된다」 한 문장 |
| `§12` 변경 이력 | `2.5 / 2026-08-30` 행 |

**근거는 지어내지 않고 정본 안에서 끌어냈다 (INTERPRETATION).**
목적이 「이 데이터가 **어떻게 생겼는지**를 판단한다」(`§1.1`·`§4`)인데 **격자 칸이 화면 픽셀보다 촘촘하면 전체 한 장에서는 뭉개진다.**
서버 렌더(`§8`)가 맡는 것은 **전체를 보여주는 데까지**이고, 한 걸음 더 들어가 보는 일이 확대다.

**완료 기준 = 조건 여섯 (전부 충족해야 닫는다).**
⑴ 그린 뒤 확대·축소·이동이 된다 ⑵ 확대해도 **값·팔레트·구간 수 설정과 범례가 안 바뀐다**
⑶ **렌더를 다시 걸지 않는다**(확대 조작 중 새 렌더 작업 생성 0건) ⑷ **데이터가 가진 해상도가 한계**이고 한계에서 안내를 낸다 —
**없는 값을 만들어 그리지 않는다** ⑸ 층이 둘 이상이면 **모든 층에 함께** 적용된다
⑹ **보기 권한만 있어도 확대되고 확대 상태는 저장하지 않는다**.

**수치는 적지 않았다** — 재지 않은 수라서다. 정본에 그 사실을 한 줄로 못 박았다.

**임베드 재동기 · 게이트 (EVIDENCE).** `package/데이터셋_상세_260817.html` 의 `md-policy` 블록을
**검사기 자신의 `normalize()`** 로 재동기했다. **검사기·정규화 규칙·검사 범위에 한 글자도 대지 않았다.**
`planning-freshness` **green**(15블록 전부 MATCH · E-03 `md-policy` **VER 2.5** · SHA8 `3ed8c3a9`) ·
검사기 `--selftest` **green**(fail-closed 3건 증명).

**`V-2`(값 조회)는 함께 판정받지 않았다.** `P3.note` ㈏ 가 「같은 자리이므로 같이 판정받아야 한다」고 적었으나
**Ted 판정은 확대 하나**였다. `〈213〉` 의 사유가 그대로 살아 있어 `V-2.completion_def` 는 `완료 정의 미작성` 그대로다.

---

## 4. 하지 않은 것 — 따로 적는다

| 안 한 것 | 왜 |
|---|---|
| **렌더 성능·상한의 합격선 기재** | **판정 요청으로 올렸다**(`03-HANDOFF §4` 블로커 **#47**). 몇 초·몇 픽셀을 뒷받침할 실측이 레포·정본 어디에도 없다. 비슷해 보이는 둘은 합격선이 아니다 — 계약 `Viewport` 의 `maximum: 4096`(**요청 상한**) · PoC 「한 파일 해석 60초」(**업로드·디코드 한도**). **지어내지 않는다** |
| **스크린샷 버튼·확대 UI 구현** | 이 회차는 계약·중계·정본까지다. 화면은 `P3` 화면 레인 몫이고, **범위를 늘리지 않는다** |
| **`V-2` 완료 정의 기재** | Ted 판정 대상이 아니었다 |
| **`P3` 닫기** | 남은 차단 둘(㈐·㈒)이 살아 있다. **부분 완료로 닫지 않는다** |
| **커밋 외 조작** | push·병합·`git stash` **0건.** 운영 스택 무접촉 · DB·마이그레이션 무접촉 |
| **staging 실서버 왕복 측정** | `〈231〉`-⑧ 에 「이번에 세지 않은 축」으로 적었다 |
