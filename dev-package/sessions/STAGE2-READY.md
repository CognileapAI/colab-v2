# stage 2 완주 준비 — 전수 목록 (2026-08-27 · 읽기 전용 스윕)

> **판정과 값의 정본은 `PLAN-SoT §9` 이고, 진행 상태의 진실원은 `03-HANDOFF §1` 이다.**
> 이 문서는 **「stage 2 를 끝내려면 무엇이 남았는가」의 목록**이고 값을 다시 적지 않는다 — 링크로만 가리킨다.
> 스윕 범위 = `03-HANDOFF §1·§3.5·§4·§4.5·§5.5` · `WORK-UNITS §1.5·§3~§11` · `PLAN-SoT §9`(확정 `〈151〉`~`〈166〉` · 열린 것 · 확정으로 내려간 것) ·
> `sessions/{STAGE2-PREP, TED-DECISIONS-OPEN, R1-RESTORE-DRAFT, X2-FREEZE-PROTOCOL-DRAFT, I3, STAGE1-CLOSE, S1-PLAN-REFOUND §J, S2-PLAN-DRAFT}.md` · `DOMAINS §6` · `RESTART.md`.
> **표기** — ✅ 닫힘 · 🟧 부분 · ⬜ 착수 후보 · ⏸ 열지 않기로 한 것 · ⛔ 차단 (`WORK-UNITS §12`·`§11`).
> **[추론]** 표시가 붙은 줄은 문서가 직접 말한 것이 아니라 두 문서를 대조해 내가 세운 것이다.

> ## ⭑ **개정 2026-08-27 — 이 문서가 연 물음 중 여섯이 판정으로 닫혔고, 이 문서 자신의 결함 셋이 잡혔다.**
> **판정 = `PLAN-SoT §9 〈167〉`**(Ted). 값과 근거는 거기 있다 — 여기서는 무엇이 닫혔는지만 적는다.
> **닫힌 것** — `§6-B1`(여섯의 순서표 편입) · `§6-B2`(`PA-G` 를 연다) · `§6-B3`(`V-1` ↔ `§J-6` 범위) · `§6-B5`(`J-1` 을 WU 로 둔다 ＋ `J-7` 적출) · `§6-C3`(`R2` 표기) · `§6-C4`(`#22`).
> **이 문서의 결함 셋 (어드바이저 지적)** —
> ⓐ **`§6-C5` 가 틀렸다** — `#25` 는 **2026-08-26 에 이미 판정됐다**(`S2b-SEARCH-EVALSET §4-확정`: 기대값 0건 확정 · `source_label` 부여 금지 · `pg_trgm` 0.3 유지). **열린 충돌이 아니라 낡은 행 표기였다.**
> ⓑ **판정 필요 계수가 부풀려져 있었다** — 「A 7 ＋ B 9 ＋ C 8 = 24 · 막힘 8」로 셌는데, **막힘은 `I3` 7건 ＋ `§6-B1` 1건이고 그 여덟이 8 로 「늘어난」 것이 아니다.** `TED-DECISIONS-OPEN` 의 「7건 하나」에 **B1 하나가 더해진 것**이다.
> ⓒ **`§6-B3`·`§6-B5` 는 과다 개방이었다** — 둘 다 **이미 판정·이연된 것**(`〈78〉-㉲` · `〈157〉-㉱`)을 다시 열린 판정으로 세웠다. **낡은 표기와 열린 판정을 가르지 못한 자리다.**
> ⚠ **아래 본문은 무수정 보존한다** — 취소선·개정 표시만 붙인다. 지우면 같은 물음이 근거 없이 다시 열린다.

---

## 1. 1단 — stage 2 를 열기 전에 닫아야 하는 것

### 1.0 이미 닫힌 둘 — 실물 확인

| WU | 상태 | 확인한 자리 |
|---|---|---|
| **X-2** 계약 동결 해제 프로토콜 | ✅ **닫힘** | `PLAN-SoT §9 〈163〉-㉮`(3등급제 확정 · 횟수 상한 폐기) · `sessions/X2-FREEZE-PROTOCOL-DRAFT.md §7`(Ted 판정 4항 전건 완료 · 「남은 판정 0건」) · 문서 제목이 「확정 · 2026-08-27」 · `WORK-UNITS §10.1` 하단 주석 · `03-HANDOFF §1 T-X` |
| **X-3** 프로젝트↔데이터셋 실연결 | ✅ **닫힘** | `PLAN-SoT §9 〈155〉` — 정문(화면·API)으로 12/12 · 204 · `d6_project_dataset` 0 → 12 · 시드 INSERT 0. `WORK-UNITS §10.1` 에서 표에서 내렸다 |

⚠ **`sessions/STAGE2-PREP.md §1` 2번은 `X-2` 를 아직 ⬜ 로 적는다** — `〈163〉` 이후 갱신되지 않은 자리다(§6-C1).

### 1.1 남은 넷

| WU | 완료 정의(요약) | 진입조건 | 지금 막는 것 | 예상 크기 | 판정 필요 |
|---|---|---|---|---|---|
| **R-1** 복원 절차 | 살아 있는 staging 을 **제자리 복원**하는 절차를 세우고 리허설한다. 정본 초안 = `sessions/R1-RESTORE-DRAFT.md`. 잔여 항목은 아래 `1.2` 표 | 없음 | `X-1`·`X-4` 재기동 전부(`〈158〉-㉯`) | **큼** — 볼륨 백업 기구가 아직 없다(`R1-RESTORE-DRAFT §6` #1) ＋ 리허설 1회차 | **없음**(비밀 7종 정책은 `〈163〉-㉲` 로 확정) · ⚠ `[미확인]` 둘 존속 |
| **X-1** 슈퍼유저 `POSTGRES_PASSWORD` 회전 | pg 컨테이너 env 의 슈퍼유저 값 회전. `〈153〉` 이 앱·소유자 3롤만 회전했고 슈퍼유저 값은 `docker inspect` 로 보인다 | `R-1` · `X-4` 에 동봉(`〈158〉-㈏` · `STAGE2-PREP §1` 1번) | 없음(재기동 회차 안) | **작음** | 없음 |
| **X-4** `_FILE` 배선 재기동 실전 검증 | `RESTART.md §2` 의 `COLAB_STAGING_*_DB_URL_FILE` **5키**로 재기동이 실제로 서는지. 표는 갱신됐고 **다음 재기동이 첫 실전**이다 | **`R-1` 선행**(`〈158〉-㉯`) | 1단 전체의 종점 | **중간** — 8컨테이너 정지·기동 왕복 | 없음 |
| **X-5** ㉰-4 소급 전수확인 | 계약(`contracts/`)에 **선언만 되고 구현이 없는 op·필드**가 더 있는지 전수 확인 → 철회 또는 집행으로 가른다 | **없음** | 없음 — 1단의 다른 셋과 병렬 | **중간** — seam 3종 전수 대조 | 없음(`〈163〉-㉰` 로 WU 화 확정) |

- **정본** — `X-1`·`X-4` = `WORK-UNITS §10.1` · `R-1`·`X-5` = `§10.2` · 현황판 = `03-HANDOFF §1 T-X`.
- **X-5 는 1단 순서표(`§10.3`)에 이름이 없다** — 진입조건이 없어 어디에나 붙을 수 있으나, **순서상 자리가 문서에 지정돼 있지 않다**(§6-C2 판정 필요).

### 1.2 `R-1` 완료 정의 — 항목별 산출물 유무

| # | 완료 정의 항목 | 근거 | 레포에 산출물이 있는가 |
|---|---|---|---|
| 1 | **uploads·previews 볼륨 백업 절차** | `R1-RESTORE-DRAFT §6` #1 · `§2` #3·#4 · `〈161〉-㉳` | **없다** — 기구 자체가 없다. **`R-1` 의 진짜 선행 결손** |
| 2 | **비밀 7종 재발급 절차**(완-비1) | `〈163〉-㉲` · `R1-RESTORE-DRAFT §7.1`·`§7.2` | **부분** — `credentials.json`·`*_DB_URL_FILE` 5종은 값이 있고, **`subjects.json`·env 파일 둘은 `[미확인]`** |
| 3 | **재발급본으로 기동이 선다**(완-비2) | `R1-RESTORE-DRAFT §7.1` | **없다** — 리허설 미실시 |
| 4 | **원장과 짝이 맞는다**(완-비3 · `d1_account`·`d2_member_role`) | 〃 | **없다** — 리허설 미실시 |
| 5 | **리허설 1회차**(`§5` 의 1·3·6 을 일회용 인스턴스에서 한 묶음) | `R1-RESTORE-DRAFT §5`·`§6` #6 | **없다** — 문서가 스스로 「초안이고 리허설 전」이라 적는다 |
| 6 | **이미지 digest 대장** | `〈165〉-㉱` | **있다** — `dev-package/reference/IMAGE-DIGESTS.md`(8건 = 자체 5 ＋ 외부 3) |
| 7 | **`public` 밖 객체 목록** | `〈165〉-㉰` | **있다(결과 = 없음)** — `public` 하나뿐 · 사용자 정의 `ts_config` 0. ⚠ **`T-1` 이 한국어 `ts_config` 를 만들면 즉시 낡는다** — `T-1` 완료 정의에 재측정이 든다 |
| 8 | **터널·DNS·terraform state 소실 시나리오** | `〈165〉` · `R1-RESTORE-DRAFT §2` #12 | **범위 밖으로 확정** — `R-1` 이 지지 않는다 |
| 9 | **`sha256` 대조 · `--skip-age` 를 스크립트에 넣기** | `R1-RESTORE-DRAFT §6` #5 | **없다** — 런북이 손으로 채운다. 「기구화는 별건」이라 적혀 있고 **소유 WU 표기가 없다**(§5 후보) |
| 10 | **런북 본체**(사전조건 P1~P9 · 정지 순서 · 복원 순서 4단 · 검증 5) | `R1-RESTORE-DRAFT §4` | **있다(초안)** — 명령 집행 0 · 미리허설 |

- **`R-1` 은 볼륨 백업 절차 없이 닫지 않는다**(`〈161〉-㉳`) — 지금 되돌리면 **DB 만 과거로 가고 파일은 현재에 남는다**.

---

## 2. 1단 실행 순서와 그 근거

```
R-1 복원 절차(＋리허설)
   → 전범위 백업 (platform DB ＋ ai DB ＋ 볼륨)
      → X-1 회전 ＋ X-4 재기동  (단일 변수로 겪는다)
   ∥ X-5 소급 전수확인 (진입조건 없음 · 계약 레인)
```

- **순서의 정본** = `WORK-UNITS §10.3` · 근거 = `PLAN-SoT §9 〈158〉`.
- **재기동을 맨 앞에 둔 근거**(`〈158〉-㉮`) — 뒤에 두면 다른 변경 **위에** 재기동 위험이 얹혀, 조용한 롤백이 나면 원인이 뒤섞인다.
- **복원 절차를 전제조건으로 둔 근거**(`〈158〉-㉯`) — 「백업은 있는데 되돌리는 절차가 없다」가 그대로 모순이고, 미루면 그 사이 모든 재기동이 무보험이다.
- **백업 범위에 `ai` DB·볼륨을 넣은 근거**(`〈158〉-㉰`) — 지난 사고가 정확히 `ai` 쪽이었다(`〈153〉`).

### 2.1 검증 기준선 — 헬스가 아니라 실측

| 축 | 기준선 | 2026-08-27 실측 여부 |
|---|---|---|
| 데이터셋 | **12** | ✅ 실측(`〈165〉-㉮`) |
| 파일 | **129** | ✅ 실측(`〈165〉-㉮`) |
| 계보 간선 | **6** — 측정법 = `d4_lineage_edge` 의 **`confirmed_at IS NOT NULL`**(`status` 열 없음) | ✅ 실측(`〈165〉-㉮`·`㉯`). ⚠ 여러 문서의 「5」는 틀린 값(`〈159〉-㉲`) |
| `d6_project_dataset` | **12** | ✅ 실측(`〈165〉-㉮`) |
| 온톨로지 사전 | **non-`None`** — `d9_concept_edge` 19 · `d9_concept` 49 | ✅ 실측(`〈165〉-㉮`). ⚠ 헬스 200 이 이것을 보증하지 않는다(`R1-RESTORE-DRAFT §3-㉲`) |
| 이미지 digest | `reference/IMAGE-DIGESTS.md` 와 **한 줄씩 대조** | ✅ 대장 신설(`〈165〉-㉱`) · **대조 실행은 재기동 회차** |
| **`d4_lineage_edge` 전체 행 수**(`SELECT COUNT(*)`) | **미확정** — 전체가 6 이 아니면 아무도 안 본 미확정 행이 있다는 뜻 | ⛔ **재지 않았다** — `〈166〉-㉰` 이 이름으로 남기고 `WORK-UNITS §10.3` 1단 검증에 걸었다 |

- **컨테이너 8/8 up·healthy · `/healthz` 5/5 = 200** 도 같은 회차에 실측됐다(`〈165〉-㉮`) — 블로커 `#18` 해소.
- **digest 불일치면 재기동을 계속하지 않는다**(`〈165〉-㉱` · `WORK-UNITS §10.3`).

### 2.2 재기동에서 반드시 지킬 것 (`STAGE2-PREP §3`)

1. **이미지 재빌드를 빠뜨리지 않는다** — `〈153〉` 때 배선만 바꾸고 옛 이미지로 올려 **ai-service 만 healthy** 였고 **사전 DB 가 `None` 으로 조용히 비었다**. 헬스는 200 이었다.
2. **`COLAB_STAGING_*_DB_URL_FILE` 5키**가 없으면 `:?` 로 컨테이너가 아예 안 뜬다(`RESTART.md §2`).
3. **`-f compose.i2.yml` 을 빼면 조용히 I2 이전으로 롤백된다** — 그 상태에서도 루트 헬스는 200 이다(`RESTART.md §2-②` · `R1-RESTORE-DRAFT §3-㉮`).
4. **회전 음성 확인은 네트워크 경유로 잰다** — pg 컨테이너 안 루프백은 `pg_hba` 가 `trust` 라 엉터리 비밀번호도 통과한다.
5. **비밀 파일은 제자리 덮어쓰기 · `mv` 금지** — `:ro` 파일 바인드라 inode 가 바뀌면 옛 파일을 계속 읽는다(`〈93〉-㉰`·`〈109〉-㉳`).

---

## 3. 2단 — 병합 전 처리

### 3.1 `A` 동결 해제 프로토콜 — 남은 것

| 항목 | 상태 |
|---|---|
| 프로토콜 본문(㉮/㉯/㉰ 3등급 · ㉱ 증거 7항 · ㉲ 기록 8필드) | ✅ 확정 — `X2-FREEZE-PROTOCOL-DRAFT.md §5` |
| Ted 판정 4항 | ✅ 전건 완료 — 같은 문서 `§7`, 「남은 판정 0건」 |
| 회차 번호 확정(`〈94〉` 5차 · `〈107〉` 6차 · `〈151〉` 7차 · 다음 **8차**) | ✅ `〈163〉-㉯` · `〈166〉-㉮` |
| **파일명이 아직 `X2-FREEZE-PROTOCOL-DRAFT.md`** | ⬜ **남은 것** — 내용은 「확정 프로토콜」인데 이름이 초안이다(`〈163〉-㉮` ⓓ). §5-A1 |
| `seam-consistency` G-a·G-c·G-d | ⏸ **stage 2 이후 확정**(`〈163〉-㉱`) — 그때까지 게이트 미탐지 6종은 ㉯ 사람 판정이 받는다 |

**즉 `A` 의 실질 잔여는 파일명 개명 하나다.** [추론 — 문서가 「남은 판정 0건」이라 적고 파일명만 초안 표기로 남아 있다]

### 3.2 `E` `main` 병합

- **게이트 green 확인을 건너뛰지 않는다**(`CLAUDE.md §7` · `STAGE2-PREP §2` 8번).
- 갈래 상태 = `main` = `worktree-p2-exec` fast-forward(`STAGE1-CLOSE §0`). **로컬 `main` 체크아웃은 뒤처져 있을 수 있다.**
- 게이트 기준선 = **23종 전부 green**(2026-08-25 실측 · `03-HANDOFF §2`) · 마감 회차 재측정 = **red 0**(`WORK-UNITS §1.5` 조건 5).

### 3.3 다음 회차 ㉮ 자동 허용 묶음 — 한 목적으로 함께 싣는다

**근거** — `〈164〉-㉱`(`DR-16`·`DR-18` = ㉮ 자동 허용 · 등급제 첫 집행) ＋ `〈166〉-㉱`(`fe-core.yaml` 산문도 **같은 ㉮ 묶음**). **셋을 쪼개면 `㉰-6`(묶음 쪼개기로 자동 허용 만들기 금지)에 걸린다.**

| # | 바뀌는 파일 | 무엇 |
|---|---|---|
| 1 | `contracts/events/core-pipeline.json`(`UploadAcceptedPayload.files` 설명) | 「기준 격자 파일 **0~1건**」 산문을 `〈58〉` 의 **0~2건**으로. 구조 제약은 무변경(`minItems: 1` 뿐) — `DR-16` |
| 2 | `contracts/schemas/common.json`(`FileKind.description`) | DB 열 이름을 앞질러 못박은 `grid_axis` 표기 정정 — `DR-18` |
| 3 | `contracts/seams/fe-core.yaml`(산문 1줄) | 「여섯 번째 동결 해제」 → **일곱 번째** — `〈166〉-㉱` |
| 4 | **생성물** | 위 3의 반영분. **손수정 금지**(`CLAUDE.md §3-7`) — 재생성으로만 |

- **재생성 필요 = 예**(`〈166〉-㉱` 이 「생성물 재생성 동반」이라 명시).
- **돌려야 하는 게이트 넷** — `contract-lint` · `contract-breaking`(**ERR 0 이어야 ㉮ 자격**) · `generated-up-to-date` · `seam-consistency`.
- **㉮ 5조건 대조**(`X2 §5-㉮`) — `contract-breaking` ERR 0 · 마이그레이션 0 · 소비자 0 · 설계 판단 0 · 정본 무개정. **소비자 0 은 주장이 아니라 grep 출력으로 남긴다**(`㉱-3`).
- **등재** = `PLAN-SoT §9` 한 항목 · 필드 8개(`㉲`). **회차 번호 = 8차.**

---

## 4. 3단 — stage 2 본체 WU 전수

### 4.0 의존 그래프

```
[1단 완료]
   │
   ├─ T-1  ts_config 한국어 재작성 ＋ 온톨로지 어휘·관계 확장   (재생성 1회)
   │
   ├─ D5 stage 2 파트 : 헤더 파싱 → 좌표계 변환 → COG
   │        │
   │        ▼  ⚠ 타일은 COG 없이 못 낸다 — 선호가 아니라 의존 (〈158〉-㉳)
   │      P3  계보 그래프 · 타일/확대 · createScreenshot · 2D 렌더 3종 · 렌더 표현 확장
   │        │        │
   │        │        ├─ V-1 팔레트 선택 재렌더
   │        │        └─ V-2 값 조회
   │        │
   │        └────────────────────────► S3  실데이터 4종 E2E   (S2 도 선행)
   │
   ├─ F-1  S-08 화면      (P3 와 병렬 가능)
   │
   └─ J-1  편의 기능 §J 10건   ← stage 2 마지막
```

⭑ **개정 2026-08-27 (`〈167〉`)** — 위 그래프는 **여섯을 빠뜨린 판**이다. **정본은 `WORK-UNITS §10.3` 3단**이고 거기에
`PA-G` ∥ `D5` ∥ `K3`→`K5` 병렬 · `P3`＋`D5`→`Y-1` · `S3`→`P6`→`P7`→`P8` · `I3`→`I4`(본체와 병렬)가 들어갔다.

- **순서 정본** = `WORK-UNITS §10.3` 3단 · `STAGE2-PREP §2`.
- **`D5` 안의 순서** = 파싱 → 좌표계 → COG. 근거 = **헤더 파싱이 첫 체감 가치**(메타 자동 채움)라 조기 가시화한다(`〈158〉-㉳`).

### 4.1 WU 별

| WU | 완료 정의(요약) | 소유 도메인 / 배포 단위 | 진입조건 | 선행 | 아직 안 정해진 것 |
|---|---|---|---|---|---|
| **T-1** `ts_config` 한국어 재작성 | 검색 사전을 `simple` 에서 형태소로. **전 행 재생성 수반.** ＋ **온톨로지 어휘·관계 확장을 같은 회차에 묶는다**(`〈164〉-㉮`) ＋ **`public` 밖 객체 재측정**(`〈165〉-㉰`) | D3 / core-api (사전 = `db/ai`) | **stage 2 초반** — 데이터셋 12개인 지금이 가장 싸다 | 1단 | 확장할 **어휘·관계의 목록과 분량**이 값으로 없다. `d9_concept_edge` 19행을 얼마나 늘리는지 미기재 |
| **D5** stage 2 파트 | ⑶ **헤더 파싱 = 자동 추출**(포맷·변수·기간·좌표계·격자·용량 · `DR-14`) ⑷ **좌표계 변환**(기준 격자 0~2건 · **못 읽으면 `[미상]` 이고 실패 — 지어내지 않는다** · `DR-9`) ⑸ **COG 변환**(오버뷰 리샘플링 범주형/연속형 분기 `DR-12` · **입력 tif ↔ 산출 COG 층 분리** `DR-2`) | **D5** / `pipeline-worker` | 같은 WU 의 stage 1 파트 뒤(`WORK-UNITS §10.2` 의존) | 1단 | `DR-17`(격자 축 판별 방식) 채택 조건 넷의 최종 판정이 **`P2` 에서 stage 2 `D5` 로 넘어왔다**(`TED-DECISIONS-OPEN §B`) |
| **P3** 계보 그래프 · 2D 시각화 | 계보 그래프 · **데이터셋 상세 2D 렌더 3종**(격자·경계·점) · `createScreenshot` · 렌더 표현 확장 · **타일 서빙·확대**(`〈78〉` 각주에서 완료 정의로 올렸다 · `§10.2`) · 렌더 성능·상한 조정 | D4 · **D7** (둘 다 P3 에 흡수 · WU 아님) / `viz-render` ＋ core-api 중계 | `P2` | **D5**(COG) | ⑮ **계보 그래프 한계선(B-07) · 보조입력 부모 표기(B-09)** — `PLAN-SoT` 열린 것 ⑮, 「실데이터가 있어야 정할 수 있다」 |
| **F-1** S-08 화면 | 미등록 미리보기 화면 자체 | frontend | `P3` 와 **병렬 가능** | 1단 | 화면의 완료 정의가 **WU 표에 한 줄뿐**이다 — 목업 대비 검증 항목이 별도로 없다 |
| **V-1** 팔레트 선택 재렌더 | 사용자가 고른 컬러맵으로 다시 그린다 | **D7** / `viz-render` | `P3` | P3 | ⚠ **`§J-6`(팔레트 선택 UI)와 충돌** — `J-6` 은 「편의이자 범위 밖」으로 적혀 있다(`S2-PLAN-DRAFT §A-4`). §6-B3 |
| **V-2** 값 조회 | 지도에서 한 점을 찍으면 그 값이 뜬다 | **D7** / `viz-render` (`〈157〉-㉮`) | `P3` | P3 | 없음 — 소유가 `〈157〉` 로 확정됐다 |
| **S3** 실데이터 4종 E2E | **지원 4종**(NetCDF·Binary·HDF4·GeoTIFF)이 업로드→파싱→COG→시각화→계보 확정까지 완주. **4종 각각 최소 1건**이 시각화 화면에 그려지고 계보가 확정 상태로 남는다. **GeoTIFF 를 가장 먼저** | 여러 도메인 횡단 | **`S2` · `P3`** | S2·P3 | ⚠ **`〈134〉` 로 GRIB 이 지원 포맷에 돌아왔고 E2E green** 인데(`WORK-UNITS §1.5` 조건 7 재판정) **`S3` 완료 정의는 4종 그대로**다. §6-B4 |
| **J-1** 편의 기능 묶음 | `§J` **10건**(아래 4.2) | 혼합 | **stage 2 마지막**(`〈157〉-㉱`) | 앞 전부 | ⚠ **`〈78〉-㉰`(「물결·레인·완료 정의 어디에도 넣지 않는다」)와 WU 화가 충돌**(`S2-PLAN-DRAFT §A-9`). §6-B5 |

- **`P3` 의 범위는 이미 작아졌다** — `createRender`·`getRender`·`getRenderTile`·`listPalettes` ＋ 중계 2 op 은 `〈63〉-㉮` 로 `P2` 로 빠져나갔다. **P3 착수 시 그것을 다시 자기 범위로 세지 않는다**(`WORK-UNITS §7` 주석).
- **advisor 반대 의견이 살아 있다** — 「렌더 4포맷＋타일＋팔레트는 사실상 P3 의 심장」이라 별도 WU 절단을 권고했고, `sessions/P2-EXEC.md §8` 위험 1번으로 보존돼 있다.

### 4.2 `J-1` 의 `§J` 10건 — 실제 목록

**정본 = `sessions/S1-PLAN-REFOUND.md §J`**(표에 `J-1`~`J-10` 이 값으로 있다).
⚠ **이름이 겹친다** — WU 이름 `J-1`(묶음 전체)과 항목 `J-1`(격자 가져오기)이 다른 것이다.

| # | 항목 | 무엇 | 딸린 비용 |
|:--:|---|---|---|
| J-1 | **다른 데이터셋의 격자 가져오기** | 형상 일치 격자를 `[가져오기]` 로 복제 | — |
| J-2 | **연구실 기본 격자 지정** | 연구실 기본 격자를 한 번 정하면 자동 제시 | ⚠ **새 테이블 필요 — 마이그레이션 1건이 따라온다** |
| J-3 | **격자 불일치 경고** | 형식·해시가 다른 격자에 「약 612 m 차이」 알림 | ⚠ **`R-4` 의 유일한 완화** · 빼는 대가는 `§F` 16-ⓔ |
| J-4 | **격자 적용 전 예상 영역 미리 보여주기** | 적용 시 예상 bbox 표시 | — |
| J-5 | **카탈로그 「지도 없음」 배지·필터** | 목록에서 격자 대기 데이터셋 식별 | — |
| J-6 | **팔레트 선택 UI** | `listPalettes` 소비 | ⚠ **`V-1` 과 충돌**(§6-B3) |
| J-7 | **자동 무효화 파이프라인** | 사건 감지 → 무효화 범위 계산 → 자동 재생성 | ⚠ **「다시 만들기」 버튼은 이것의 1단계가 아니다** — `J-7` 을 꺼낼 때 버튼이 흡수된다 |
| J-8 | **진행바 남은 시간 예측** | 「약 3초 남음」 | ⚠ **근거가 `[미측정]`** — 정직성 문제라 지금 넣으면 안 된다 |
| J-9 | **첫 파일 도착 즉시 동기 렌더** | 전량 대기 없이 1장 먼저 | 지배 구간은 업로드(tif 12:1) — 이득이 작다 |
| J-10 | **격자 파일 폴더째 드롭** | 디렉터리 드롭 | **폴더 드롭만 편의** — 두 장 함께 받고 서버가 판별하는 것은 기능 |

- **이 표의 규칙 셋** — ① 버리지 않는다 ② 일정에 넣지 않는다 ③ **하나씩 꺼내 쓰지 않는다**.
- **`J-3` 을 빼는 것이 안전한지는 `[미측정]`** 이라고 표 자신이 적는다.

---

## 5. stage 2 완주에 필요한데 WU 가 없거나 순서에 없는 것

> `CLAUDE.md §5` 가 「나중에로 남기기」를 금한다. 아래는 **소유가 비었거나, 소유는 있는데 stage 2 순서표(`WORK-UNITS §10.2`·`§10.3`)에 이름이 없는 것**이다.

### 5.A stage 2 정의에는 들어 있는데 순서표에 없는 것 — **가장 큰 구멍**

`WORK-UNITS §1.5` 는 stage 2 를 **「가공 · 타일 · `createScreenshot` · 팔레트 · S-08 · 값 조회 · 자동 무효화 · `P3`·`P6`·`P7`·`P8`·`S3`·`K3`·`K5` · `〈78〉` 편의 묶음」** 으로 정의한다.
~~`§10.2` WU 표와 `§10.3` 3단 순서에는 **`P6`·`P7`·`P8`·`K3`·`K5` 와 「자동 무효화」가 없다.**~~
✅ **닫혔다 (2026-08-27 · `〈167〉-㉮`)** — 여섯 전부 **`WORK-UNITS §10.2-b` WU 표와 `§10.3` 3단 순서**에 들어갔다.
**`§1.5` 는 고치지 않았다** — 정의는 옳았고 개정 대상이 순서표였다. 자동 무효화는 **`Y-1`** 로 서고 `§J-7` 에서 적출됐다.

| 항목 | 상태 | stage 2 범위인가 | 근거 |
|---|:--:|---|---|
| **P6** 승인 처리 | ⬜ | **stage 2 범위** — `§1.5` 가 이름으로 적었다. **순서표에 없다** | `WORK-UNITS §1.5` vs `§10.2`·`§10.3` |
| **P7** 연구실 대시보드 | ⬜ | 〃. **D8 은 「도메인 10개 전부」를 stage 2 까지 미결로 두는 유일한 항목**(`§1.5` 조건 3 재판정) | 〃 |
| **P8** E-01 적용 지점 표 | ⬜ | 〃 | 〃 |
| **K3** 계보 제안 서비스 | ⬜ | 〃. 완료 판정 = 평가셋 대비 ＋ **D4 쓰기 경로 부재 음성 테스트 green** | `WORK-UNITS §8` |
| **K5** 제안 원장 | ⬜ | 〃. 진입조건 `K3` | 〃 |
| **자동 무효화** | ⬜ | `§1.5` 는 stage 2 로 적고, `§J-7` 은 **편의 기능**으로 적는다 — **같은 것이 두 층에 있다** | `WORK-UNITS §1.5` vs `S1-PLAN-REFOUND §J-7`. **판정 필요**(§6-B5) |
| **K4** 자연어 검색 | ⬜ | 서비스는 섰고 **완료 판정만 남았다** — 오라클 = 평가셋 대비 재현율. `〈164〉-㉯` 로 차단은 풀렸으나 **평가셋 초안 작성·Ted 검수가 WU 로 안 서 있다** | `03-HANDOFF §1 T-K` · `〈164〉-㉯` |
| **P4** 검색 히어로·결과 | ⬜ | `K4` 와 같은 사유로 열려 있다(오라클이 평가셋) | `03-HANDOFF §1 T-P` |
| **P5** 프로젝트 | 🟧 | 잔여 = **프로젝트 op 넷이 501**(`updateProject`는 `〈150〉` 으로 열렸다) ＋ **F-03~F-05 모달 3종 부재** ＋ `P6` 대기 | 〃 |
| **P2** 업로드·계보 | 🟧 | 잔여 = 계보 확정까지의 완주(`〈70〉-㉮` 로 stage 2) | `WORK-UNITS §1.5` 조건 7 |

~~**[추론]** 이 열 줄은 stage 2 완주에 필요하고, 3단 순서표에 자리가 없다. **순서 편입 여부가 판정 대상이다**(§6-B1).~~
✅ **판정 완료 — 편입한다**(`〈167〉-㉮`). `K4`·`P4` 는 **`T-1` 완료 정의에 묶인 평가셋 재측정**으로 판정 회차를 얻었다(`〈167〉-㉱`).

### 5.B 소유 WU 는 있는데 stage 2 목록 밖 — v2 완주에는 든다

| 항목 | 상태 | 분류 | 근거 |
|---|:--:|---|---|
| **I3** 배포 자동화 | ⬜ ⛔ | **→ stage 2 경로에 편입됐다**(`〈167〉-㉲`) — 그 결과 **`[정본 무근거]` 7건이 stage 2 차단 요인**이 됐다. ／ 이전 **stage 2 목록 밖 · v2 완주 필요** | `WORK-UNITS §11`. **착수 차단 = `[정본 무근거]` 7건**(§6-A). 소유 드리프트 = `DR-4`(SHA 재현성) · `DR-5`(직전 이미지 소실) · `DR-6`(헬스 없는 `exit 0` = fail-open) |
| **I4** 운영 준비 | ⬜ | **→ stage 2 경로에 편입됐다**(`〈167〉-㉲`) · 진입조건 = `I3` | `WORK-UNITS §4`. 완료 판정 = 복원 리허설 성공 — **`R-1` 과 재료가 겹친다** [추론] |
| **G1b** 목업·화면 최신성 | ⬜ | 〃 · 진입조건 = `P2`·`P4` | `WORK-UNITS §5`. **`planning-freshness` 게이트가 판정하지 않는 축**이다 |
| **C1**~**C4** v1 이관·지식 추출 | ⬜ | **stage 2 에서도 열지 않는다**(`§10` 명시) · **완주에는 든다** | `WORK-UNITS §10`·`§11`. ⚠ **`C3` 은 `D5` 의 입력**(`HARVEST.md`)이나 **차단은 아니다** — `§6 D5` 행이 「있으면 쓰고 없어도 진행한다」 |
| **IS4** terraform state 보관 | 🟧 | 잔여 2건 = **마지막 `apply`(실 터널)** ＋ **맨몸 호스트 조건 리허설**. ⚠ **맨몸 리허설은 `R-1` 과 재료가 겹쳐 `R-1` 뒤에 붙이는 것이 싸다** | `WORK-UNITS §10` · `03-HANDOFF §1 IS4` |
| **S2** 초기 데이터 적재 | 🟧 | 잔여 = 완료 조건 전항 재판정. `〈149〉`~`〈153〉` 으로 결손이 0 이 됐으나 **행 표기는 🟧 유지** | `03-HANDOFF §1 T-S` · `WORK-UNITS §11` |
| **R2** v1 레포 5종 archive | ✅ **정정됨** — `WORK-UNITS §11` 을 ⬜ 로 맞췄다(`〈167〉-㉴`) ／ 이전 **표기 충돌** | `03-HANDOFF §1 T-R` = **⬜**(C4 후) / `WORK-UNITS §11` = **✅** | §6-C3 **판정 필요** |
| **PA-G** 구글 IdP 어댑터 | ⬜ **→ 편성됨**(`〈167〉-㉯` · `WORK-UNITS §10.2-b`) | **진입조건 = stage 1 완료 · 교체 기한 = stage 1 완료 판정과 동시에 이 WU 를 연다**(`〈122〉-㉯`). **stage 1 이 닫힌 지금 열려 있어야 하는데 `§10.2` 에 없다** | `WORK-UNITS §7 PA-G` · `03-HANDOFF §3.5 〈122〉` = **⬜ 미이행 · 자격 발급 0** |
| **I0 · I1 · I5** | ⏸ | **prod — 열지 않기로 한 것**(`㊻`). 착수 후보에 올리지 않는다 | `WORK-UNITS §11` |
| **seam-consistency G-a·G-c·G-d** | ⏸ | **stage 2 이후로 확정**(`〈163〉-㉱`) | `X2 §6` |
| **`X2-FREEZE-PROTOCOL-DRAFT.md` 파일명 개명** | ⬜ | **stage 2 범위 · 소유 WU 없음** — 내용은 확정 프로토콜인데 이름이 초안이다 | `〈163〉-㉮` ⓓ. §3.1 |
| **`〈128〉` 백업 최소 행 수 상향** | ⬜ **미이행** | **소유 WU 표기 없음** — `config.example.env` 의 `_platform=1` 이 그대로다. `R-1` 에 붙일 후보 [추론] | `03-HANDOFF §3.5 〈128〉` |
| **`R-1` 의 `sha256` 대조·`--skip-age` 기구화** | ⬜ | 「기구화는 별건」이라 적혀 있고 **소유가 비었다** | `R1-RESTORE-DRAFT §6` #5 |
| **실데이터 4종 E2E** | ⬜ | **= `S3` 그 자체**(`WORK-UNITS §11` 정정문) — 별도 항목이 아니다 | `WORK-UNITS §11` |

### 5.C 분류할 수 없는 것 — **판정 필요**

| 항목 | 왜 분류가 안 되는가 |
|---|---|
| ~~**`P6`·`P7`·`P8`·`K3`·`K5`·자동 무효화의 3단 순서 자리**~~ | ✅ **닫힘**(`〈167〉-㉮`) — `§10.2-b`·`§10.3` 에 편입됐다 |
| ~~**`X-5` 의 1단 안 자리**~~ | ✅ **닫힘**(`〈167〉`) — `§10.3` 1단에 **무의존 병렬**로 이름이 들어갔다 |
| ~~**`K4`·`P4` 완료 판정용 평가셋 작성 회차**~~ | ✅ **닫힘**(`〈167〉-㉱`) — **`T-1` 완료 정의에 A층 8건 재측정·보수가 묶였다.** 그 뒤 한 단계로 `K4`·`P4` 판정 |
| **`I4` 와 `R-1` 의 중복 범위** (⬜ 미판정 — `〈167〉` 이 열어 둔 채 남겼다) | 둘 다 「복원 리허설」을 완료 판정으로 든다. 한쪽이 다른 쪽을 흡수하는지 미기재 [추론] |

---

## 6. Ted 판정이 필요한 것 — 전수

> `sessions/TED-DECISIONS-OPEN.md` 는 **「이 문서에서 아직 열려 있는 것은 `§A` 의 `I3` 7건 하나뿐」** 이라고 적는다.
> **독립 검증 결과 — 그 주장은 「stage 2 착수를 막는 것」에 한해 맞고, 「열린 판정 전체」로는 성립하지 않는다.** 같은 문서 `§B` 에 취소선 없는 네 줄이 남아 있고, 그 문서가 놓친 것이 아래 B2·B3·B4·B5·C 계열이다.

### A. 착수를 막는 것 — `I3` `[정본 무근거]` 7건

**출처 = `sessions/I3.md`**(각 항목의 실제 문장). **정하지 않으면 막히는 것 = `I3` 착수 전부**, 그리고 `I3` 소유인 `DR-4`·`DR-5`·`DR-6`.

| # | 무엇을 정해야 하나 (문서 원문) | 문서상 기본값 | 긴급도 |
|:--:|---|---|---|
| 1 | **push 알림 채널(메일·메신저)** — 「`IS3 §12-2` 와 같은 이유로 **레포 밖 결정**이다. 정본 확인 필요」 | 없음 — 세션이 정하고 근거를 적는다 | **막힘** |
| 2 | **폴링 주기 값 · 워처를 systemd 로 걸지 cron 으로 걸지** | 〃 | **막힘** |
| 3 | **레지스트리 사용 여부 · 이미지 보존 개수** | 〃 | **막힘** |
| 4 | **배포 원장 파일 형식 · 보존 개수** — `IS3 §10` 의 표식 파일 3종이 참고 | 〃 | **막힘** |
| 5 | **「이미지 롤백 시 스키마는 그대로 둔다」를 규칙으로 삼는가** | 〃 | **막힘** |
| 6 | **배포 실패 시 직전 green 릴리스로 자동 롤백하는가** | 〃 | **막힘** |
| 7 | **승인 기록의 보관 위치·형식·승인자 표기** | 〃 | **막힘** |

- **공통 기본값** = `I3.md:303` 「정본이 값을 주지 않으면 만들지 않는다 — `[정본 무근거]` 로 남기고 보고한다」. **즉 착수 시점에 다시 물어야 한다.**
- **`I3` 진입조건 자체는 열려 있다** — `I2` ✅ 하나뿐(`I3.md` 머리말).

### B. `TED-DECISIONS-OPEN` 이 「열림」으로 남겨 둔 것 · 놓친 것

| # | 무엇을 정해야 하나 | 정하지 않으면 막히는 것 | 문서상 기본값 | 긴급도 |
|:--:|---|---|---|---|
| ~~**B1**~~ ✅ **닫힘 (`〈167〉-㉮`)** — **넣는다.** `§1.5` 는 고치지 않고 `§10.2-b`·`§10.3` 을 고쳤다. ／ 이전 **`P6`·`P7`·`P8`·`K3`·`K5`·자동 무효화를 stage 2 순서표에 넣는가** — `§1.5` 는 stage 2 라 적고 `§10.2`·`§10.3` 은 목록에 없다 | stage 2 의 **끝이 어디인지** 가 정의되지 않는다 | 없음 — 두 문서가 갈린 채다 | **막힘**(계획 수립) |
| ~~**B2**~~ ✅ **닫힘 (`〈167〉-㉯`)** — **연다.** ⚠ **기한 발동 근거는 약하다고 판정문에 적었다** — stage 1 완료 조건 4·7 이 한 점으로 찍혀 있지 않다. ／ 이전 **`PA-G` 를 지금 여는가** — `〈122〉-㉯` 가 「교체 기한 = stage 1 완료 판정과 동시」로 못 박았고 stage 1 은 닫혔다. 실물 = 자격 발급 0 | 정본이 적은 **유일 로그인 수단**이 계속 없다. 비밀번호 어댑터가 임시 수단으로 남는다 | 「stage 1 완료 시 연다」 — **조건은 이미 성립** | **막힘 아님 · 기한 초과** |
| ~~**B3**~~ ✅ **닫힘 (`〈167〉-㉵`)** — `V-1` = **서버측 재렌더 경로까지** · **선택 UI 는 `§J-6` 으로 `J-1` 묶음에 남는다.** ⚠ **애초에 과다 개방이었다** — `J-6` 은 `〈78〉` 로 이미 이연된 것이다. ／ 이전 **`V-1`(팔레트 재렌더)과 `§J-6`(팔레트 UI)의 관계** — `J-6` 은 「편의이자 범위 밖」이고 `V-1` 은 stage 2 WU 다 | `V-1` 의 범위. 화면 없는 재렌더인지 UI 포함인지 | 미정 — `S2-PLAN-DRAFT §3.3` 갈림 ① | 막힘 아님 |
| **B4** | **`S3` 완료 정의에 GRIB 을 넣는가** — `〈134〉` 로 GRIB 이 지원 포맷에 돌아왔고 E2E green 인데 `S3` 는 **4종** 그대로 | `S3` 완료 판정의 대상 집합 | 4종 유지(`〈51〉`) | 막힘 아님 |
| ~~**B5**~~ ✅ **닫힘 (`〈167〉-㉳`)** — `J-1` 은 WU 로 둔다(`〈157〉-㉱` 가 `〈78〉` 보다 나중이라 이미 유효했다). **새로 정해진 것은 `J-7` 적출 하나**이고 그 예외는 `〈167〉-㉳` 에 명문화됐다(묶음 10 → 9). ⚠ **애초에 과다 개방이었다.** ／ 이전 **`J-1`(편의 묶음)을 WU 로 두는가** — `〈78〉-㉰` 은 「물결·레인·완료 정의 어디에도 넣지 않는다」, `§10.2` 는 WU 로 세웠다 | stage 2 의 마지막 구간이 일정에 드는지 | `〈157〉-㉱` = stage 2 마지막 묶음 | 막힘 아님 |
| **B6** | **⑭ 접근 승인 「회수」 규칙** — 만료 6개월 외 조기 회수 경로를 둘 것인가 | `P6` | 만료만 — 조기 회수 없음 | 막힘 아님 |
| **B7** | **⑮ 계보 그래프 한계선(B-07) · 보조입력 부모 표기(B-09)** | `P3` | 미정 — `P3` 착수 때 다시 묻는다 | 막힘 아님 |
| **B8** | **`DR-17` 격자 축 판별 방식 채택 여부**(채택 조건 넷) | stage 2 `D5` | 조건 넷 미충족 시 수동 지정 폴백 | 막힘 아님 |
| **B9** | **⑪ AWS 토폴로지·리전·계정 구조** | `I1` 이후 prod 전부 | `㊻` 로 출시 결정에 종속 | ⏸ 보류 확정 |

### C. 문서-실물 충돌 — 판정으로만 닫힌다

| # | 충돌 | 어디 |
|:--:|---|---|
| **C1** | **`X-2` 상태가 두 문서에서 다르다** — `STAGE2-PREP §1` 2번 = ⬜ / `03-HANDOFF §1 T-X`·`WORK-UNITS §10.1`·`〈163〉` = ✅ | 표기 정정 |
| **C2** | **`X-5` 의 착수 순서 자리가 없다** — `§10.3` 1단·2단·3단 어디에도 이름이 없다 | 순서표 |
| ~~**C3**~~ ✅ **닫힘 (`〈167〉-㉴`)** — `WORK-UNITS §11` 을 **⬜** 로 맞췄다(진실원 = `03-HANDOFF §1`). ／ 이전 **`R2` 표기 충돌** — `03-HANDOFF §1 T-R` ⬜ / `WORK-UNITS §11` ✅ | `§11` 이 3차 정정을 받아야 한다 |
| ~~**C4**~~ ✅ **판정 완료 (`〈167〉-㉰`)** — **D2·D8 고아 행을 지운다**(`R-1` 백업 직후 · `X-1` 이전 · 절차 = `WORK-UNITS §10.3` 1단-㉮). 실 `DELETE` 는 `R-1` 실행 회차. ⚠ **이 줄의 「`§4` 머리말이 닫힌 것으로 적는다」는 실물과 다르다** — `03-HANDOFF §4` 머리말은 `#22` 를 **존속**으로 적고 있었다. ／ 이전 **블로커 `#22` 가 열린 채다** — 행 본문이 「판정 필요 = **D1·D2·D6·D8 잔존 픽스처를 어떻게 할 것인가**」로 바뀌어 있는데, `§4` 머리말은 `#22` 를 닫힌 것으로 적고 `TED-DECISIONS-OPEN` 은 이 항목을 세지 않았다 | `03-HANDOFF §4` #22 |
| ~~**C5**~~ ⛔ **이 줄이 틀렸다 (2026-08-27 · 어드바이저 지적)** — `#25` 는 **2026-08-26 에 이미 판정됐다**: `sessions/S2b-SEARCH-EVALSET.md §4-확정` 에서 두 건 다 선택지 **ⓐ** — 「한국수자원학회」 **기대값 0건 확정 ＋ `source_label` 부여 금지** · **`pg_trgm` 0.3 현행 유지**. **열린 충돌이 아니라 낡은 행 표기였고**, `03-HANDOFF §4` `#25` 행만 이번에 정정했다. ／ 이전 **블로커 `#25` 가 열린 채다** — ⓐ **「한국수자원학회」가 어느 데이터셋에도 `source_label` 로 배정돼 있지 않다**(원인 `[미확인]`) ⓑ **`pg_trgm` 임계 0.3 을 이름 전체에 걸어 짧은 오타가 안 걸린다**. 둘 다 **평가셋 승인 전에 판정해야 기대값이 흔들리지 않는다**고 적혀 있고, 평가셋은 `〈164〉-㉯` 로 다시 살아났다 | `03-HANDOFF §4` #25 |
| **C6** | **`〈128〉`(백업 최소 행 수 상향)의 소유 WU 가 없다** | `03-HANDOFF §3.5` |
| **C7** | **`R-1` `[미확인]` 둘** — `subjects.json` 재생성 수단 · env 파일 키 목록 정본. **값을 지어내지 않는다 — 리허설 1회차의 측정 항목** | `R1-RESTORE-DRAFT §7.2` |
| **C8** | **`〈83〉-㉮`(`RenderResult.bounds` ②비지도형) 잔여 표기** — `03-HANDOFF §1 T-S` 의 `S1-preview` 행이 아직 「`§4` #19 · Ted 판정 필요」를 달고 있는데, `#19` 는 `〈85〉` 로 해소됐고 같은 표에 ✅ 로 적혀 있다 | 표기 드리프트 |

~~**판정 필요 합계 — A 7건 ＋ B 9건 ＋ C 8건 = 24건.** 그중 **막힘은 A 7건 ＋ B1 = 8건.**~~

⭑ **정정 (2026-08-27 · 어드바이저 지적)** — **이 합계는 부풀려져 있었다.** 「24건」은 **판정 대기 · 이미 판정된 낡은 표기 · 문서 드리프트**를 한 수로 섞은 값이다.
**막힘의 실제 = `I3` 7건 ＋ `B1` 1건.** `TED-DECISIONS-OPEN` 의 「열린 것은 `I3` 7건 하나」에 **하나가 더해진 것**이지 **여덟으로 늘어난 것이 아니다.**
**2026-08-27 이후 남는 막힘 = `I3` 7건 하나**(`B1` 은 `〈167〉-㉮` 로 닫혔다). 나머지는 **표기 정정 작업**이고 판정이 아니다.

---

## 7. 예상 규모와 위험

### 7.1 순서 — 무엇이 병렬이고 무엇이 직렬인가

| 구간 | 성격 |
|---|---|
| `R-1` → 전범위 백업 → `X-1`＋`X-4` | **엄격 직렬** — `〈158〉-㉯` 가 전제조건으로 못 박았고, 단일 변수 원칙(`㉮`)이 묶음을 금한다 |
| `X-5` | **1단과 병렬** — 진입조건 없음 · 계약 레인 |
| `A` 파일명 개명 · `E` 병합 | **직렬**(A → E) · 1단 뒤 |
| ㉮ 자동 허용 묶음(3파일 ＋ 생성물) | **한 목적 = 한 회차** — 쪼개면 `㉰-6` 위반 |
| `T-1` | 3단 **맨 앞** — 재생성 비용이 행 수에 비례 |
| `D5`(파싱 → 좌표계 → COG) | **엄격 직렬** — 안에서도 순서가 있다 |
| `D5` → `P3` | **엄격 직렬 · 의존** — **타일은 COG 없이 못 낸다**(`〈158〉-㉳`) |
| `F-1` | `P3` 와 **병렬** |
| `V-1`·`V-2` | `P3` 뒤 · 둘은 서로 병렬 [추론] |
| `S3` | `S2` ＋ `P3` 뒤 — **합류점** |
| `J-1` | **stage 2 마지막** · 10건을 한 묶음으로 |
| `I3` → `I4` | **stage 2 와 병렬 가능**(진입조건 = `I2` ✅) — 단 `I3` 7건 판정이 먼저 |

### 7.2 위험 — 근거가 있는 것만

| # | 위험 | 근거 |
|:--:|---|---|
| **1** | **이미지 재빌드 누락 — 살아 있는 쪽이 속인다** | `〈153〉` 실사고: 배선만 바꾸고 옛 이미지로 올려 **ai-service 만 healthy** 였고 **사전 DB 가 `None` 으로 조용히 비었다**. 헬스 200 이 이것을 못 잡는다(`R1-RESTORE-DRAFT §3-㉲`). 대응 = digest 대장 대조(`〈165〉-㉱`) |
| **2** | **`-f compose.i2.yml` 누락 → 조용한 I2 이전 롤백** | `RESTART.md §2-②` — 그 상태에서도 **루트 헬스는 200** |
| **3** | **볼륨 백업 부재 — 원장과 바이트의 세대가 갈린다** | `〈161〉-㉲` ⓐ·`㉳`. `R-1` 을 이 절차 없이 닫으면 복원이 반쪽이다 |
| **4** | **문서가 실물보다 낡는다** | `〈156〉` 넷 ＋ `〈159〉` ＋ `〈160〉` ＋ `〈161〉` ＋ `§11` ＋ `〈120〉` = **한 회차에 아홉**(`〈162〉-㉱`). 「열려 있다」는 **최근에 잰 값이 아니라 마지막으로 적은 값**이다. 대응 = **착수 전 해당 항목 실측**(`STAGE2-PREP §5`) |
| **5** | **완주 체크리스트 오독** | `〈162〉-㉯`·`WORK-UNITS §11` 2차 정정. **부분 정정은 남은 거짓 ✅ 를 더 믿게 만든다** — 이 문서 §5.B 의 `R2` 충돌이 그 잔재다 |
| **6** | **stage 2 의 끝이 정의돼 있지 않다** | `§1.5` 와 `§10.2`·`§10.3` 이 다른 목록을 말한다(§6-B1). **범위가 안 정해진 채 착수하면 `P2` 가 겪은 「실행 중 범위 재정의로 멈춤」이 반복된다** |
| **7** | **`P3` 이 예상보다 커진다** | advisor 권고(별도 WU 절단) ＋ `P2-EXEC §8` 위험 1번. **커지면 범위를 줄이지 말고 멈추고 절단을 재론한다** |
| **8** | **`I3` 미착수로 배포가 fail-open 인 채 stage 2 를 돈다** | `DR-6` — `deploy.sh` 가 앱 5종 헬스 대기 없이 `exit 0` 을 낸다. `DR-4`(워킹트리를 굽는다 · SHA 로 특정 불가) · `DR-5`(직전 이미지 태그 소실)가 같은 뿌리다 |
| **9** | **좌표를 지어내는 경로** | `DR-9` — PoC 는 좌표를 못 찾으면 임의 정규격자를 만들고 「성공」을 반환했다. **`D5` stage 2 파트가 정확히 그 자리를 켠다.** 못 읽으면 `[미상]` 이고 실패다 |
| **10** | **`d5_ingestion.py:167` 분기를 뒤로 밀면 「내부 오류」로 부활한다** | `〈160〉-㉳` — 가공을 켤 때 이 분기의 위치를 옮기지 않는다 |

---

*작성 2026-08-27. 읽기 전용 스윕의 산출이고 어떤 파일도 고치지 않았다 — 판정은 `PLAN-SoT §9`.*
