# `F-1` 닫음 회차 — S-08 미등록 파일 미리보기 화면

／ 2026-08-31 · 워크트리 `lane-f1-close` · 등재 `PLAN-SoT §9 〈241〉` · 대장 `work-items.yaml` `F-1` `in_progress` → `done`
／ 완료 정의의 출처 = `PLAN-SoT §9 〈183〉`(3행) ＋ `〈210〉`(⑵ 개정) · 근거 산출 `sessions/F1-SCOPE.md`

---

## 1. 착수 전 실측 — 무엇이 이미 서 있었나

**대장이 뒤처져 있었다.** 상태는 `in_progress` 였고 근거란은 「격리 워크트리에서 구현 중 · 완료가 아니다」였는데, 실물은 셋 다 서 있었다.

| 완료 정의 | 착수 전 실물 | 잠근 시험 |
|---|---|---|
| ⑴ 라우트 연결 | `app/routes.tsx` 가 `PREVIEW_ROUTE_PATH` 로 `UnregisteredPreviewPage` 를 세운다 | `test/preview.test.tsx` 「완료 정의 ① — 앱 라우팅 표가 S-08 을 세운다」 |
| ⑴ 모달이 실제로 이동 | `UploadModal.tsx` `viewOnly()` 가 `previewNavigation()` 으로 이동한다 · `createDataset` 을 부르지 않는다 | `test/upload.test.tsx` 「§7.2 전이 — `보기만 할게요` 는 S-08 로 보낸다」 2건 |
| ⑵ 구간 수 컨트롤 · 팔레트 없음 | `PreviewControls.tsx` 3~9 · 기본 6 · 팔레트 후보 0개 | `test/preview.test.tsx` 「컨트롤은 팔레트와 구간 수 둘뿐이다」 3건 |

⚠ **`F1-SCOPE §3` 이 「없다」고 적은 셋 중 둘(라우트 · 모달 전이)은 그 뒤 회차가 이미 메웠다.** 문서를 믿지 않고 코드를 읽어 판정했다.

## 2. red 로 세운 시험 — 2건

**이 회차가 더한 것은 「명시적 범위 밖」을 잠그는 음성 시험뿐이다.**

- 「타일 조각을 세우지 않고 그림 한 장을 그린다」 — `preview-mosaic`·`preview-tile` 부재 · 한 장의 `src` 가 0/0/0
- 「확대 컨트롤도 확대 조작 자리도 없다」 — `data-zoomable`·`data-zoom-scale`·`preview-zoom` 부재

**오라클 출처 (추정 아님)**
- `〈183〉` 「**명시적 범위 밖** = 확대 · 타일 · 지도 값 조회 · 스크린샷 · 팔레트 컨트롤 · 등록 복귀 경로」
- `〈240〉`-㉱ 가 정본 `Policy_데이터셋_상세` v2.7 에 적은 경계 축자 「**켜도 미등록 업로드는 한 장**」 — 같은 성질을 viz-render 쪽에서 음성으로 잠근 그 줄의 프런트 짝이다
- `components/preview/tiles.ts` 주석(`〈238〉`) 「타일 갈래의 주 화면은 이 함수가 아니다」

**red 확인 방법** — `UnregisteredPreviewPage` 에 `useZoomPan()` 을 물려 `PreviewMap` 에 `zoom` 을 넘기는 변형으로 **2건 모두 red**(`× 타일 조각을…` · `× 확대 컨트롤도…`)를 실측한 뒤 변형을 되돌렸다. **green 으로 시작한 시험을 오라클로 쓰지 않았다.**

## 3. 손댄 것

| 파일 | 무엇을 |
|---|---|
| `frontend/test/preview.test.tsx` | 음성 시험 2건 신설 (＋30 행) |
| `frontend/src/components/preview/PreviewControls.tsx` | **낡은 주석 정정** — `〈183〉`-⑵ 의 「`정본 무근거`」를 `〈210〉` 이 거짓으로 판정했는데 주석이 그대로 있었다. 정본 축자를 근거로 적고 **종전 문면은 취소선으로 보존** · `〈210〉`-㉱ 의 `[미확인]`(이 화면의 권한·노출 규칙)도 지어내지 않고 옮겨 적었다. **동작 변경 0건** |
| `dev-package/work-items.yaml` · `WORK-UNITS.md` · `03-HANDOFF.md` · `PLAN-SoT.md` | 대장 먼저 · 산문은 반영본 |

**손대지 않은 것** — `components/preview/tiles.ts` · `DatasetPreviewSection.tsx`(타일·배포 레인 소유) · `catalog/**`·`DatasetsPage.tsx`·`SearchHero.tsx`(문구 레인 소유). 음성 시험은 `tiles.ts` 의 `tileUrl` 을 **읽어 쓰기만** 한다.

## 4. 완료 조건 건별 충족표

| # | 조건 | 판정 |
|---|---|---|
| ⑴ | 도달 경로 = 라우트 ＋ 모달이 실제로 이동 (등록 복귀는 범위 밖) | **충족** — 양쪽 절반이 각각 시험으로 잠겨 있다 |
| ⑵ | 구간 수 컨트롤 남김 · 팔레트는 범위 밖 (`〈210〉`) | **충족** — 3~9·기본 6 · 팔레트 후보 0개 |
| ⑶ | 화면 시험 통과 ＋ 관련 게이트 green (staging 실배포는 조건 아님) | **충족** — frontend **385 / 0** · `tsc --noEmit` **0** · 게이트 `all` **green 35 / red 0** |

**⇒ 부분 완료가 아니다. `F-1` 을 닫는다.**

⚠ **닫히지 않은 것을 함께 적는다** — `F1-SCOPE §5` 의 결정 9항 중 답해진 것은 **1·2·7 뿐**이다. 나머지 여섯(정본 §8.1 판정 행 범위 · 목업 대비 파일명 제목·`등록 안 됨` 칩 · **수명·만료 실서버 시험** · 권한 · 타인 접근 · 팔레트 목록 실호출)은 **`〈183〉` 이 완료 조건으로 삼지 않았으므로 미충족이 아니라 범위 밖**이고 `[미확인]` 그대로다. 요구하려면 **완료 정의를 먼저 고친다.**

## 5. 계수

- frontend 시험 **383 → 385 passed / 0 failed** (신설 2 · 둘 다 red 선행) · 시험 파일 19
- `tsc --noEmit` 오류 **0**
- 게이트 `gates/run.sh all` — **before green 35 / red 0 → after green 35 / red 0**. 이번 변경이 만든 red **0건** · 우회·비활성화·면제 선언·검사 축소 **0건**
- 계약 개정 0건 · 생성물 재생성·손수정 0건 · 서버 코드 무접촉 · staging 무접촉 · push 0건
