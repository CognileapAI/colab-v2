# R3 — `[미확인]` 마감 회차 (자격·운영 접촉 없이 닫을 수 있는 것만)

／ 워크트리 `lane2-search` · 브랜치 `wt/lane2-search` · 2026-08-30 (KST)
／ 진입 = `main` `f86e908` 병합(fast-forward · 충돌 0)
／ 규율 = 재지 않은 것을 잰 것처럼 쓰지 않는다. **못 돈 것은 통과가 아니다.** 값이 갈리면 지우지 않고 시점을 붙인다.

---

## 1. 이 회차가 실제로 잰 것 — 세 축

| 축 | 직전 기재 | **이번 실측** | 시점(KST) | 판정 |
|---|---|---|---|---|
| 적용 스키마 드리프트 | 2026-08-27 이후 재측정 없음 | `db/ai` **green** · `db/platform` **red — 드리프트 1곳** | 2026-08-30 02:12 | **CLOSED (값 있음. green 아님)** |
| 설명 적재 시각 | 「2026-08-27」 ↔ 「2026-08-26T11:14:04+00」 | **둘 다 맞다 — 기준이 다르다** | 2026-08-30 02:15 | **CLOSED** |
| 휴면 시험 실패 3건의 성격 | 「레포 결함인지 사본 문제인지 못 갈랐다」 | **사본 venv 문제** — 배포 이미지에서 34/34 통과 | 2026-08-30 02:12 | **CLOSED** |

### 1-1. 적용 스키마 드리프트 — 읽기 전용으로 **잴 수 있었고, 쟀다**

- 읽기 = `pg_dump --schema-only` 뿐 · 비교판 = `--rm` 일회용 postgres(tmpfs · 호스트 포트 미공개).
  운영 스택에 `DELETE`·`UPDATE`·DDL·정지·재기동 **0건**. `a2_pg`·`ai_pg` 무접촉.
- 명령 = 체인별 적용 DB 를 붙여 `./gates/run.sh schema-diff` 1회. **접속 문자열은 어디에도 남기지 않았다.**
- **`db/ai` = 드리프트 없음.**
- **`db/platform` = 드리프트 1곳** — `d4_lineage_edge_origin_check` 의 허용값이
  선언 `('ai','manual','processed')` ↔ 적용 `('AI 제안을 사람이 확인','사람이 직접 연결')`.
- **원인 특정 · 새 결함 아님** — `db/platform/versions/0008_lineage_origin_labels.py` 가 staging 에 **미적용**이다
  (`03-HANDOFF` 축자 「전진 전용 · staging 미적용 · 배포 경로가 적용한다」와 정확히 일치).
- **푸는 법** = 배포 회차가 `0008` 을 적용한 뒤 이 게이트를 재실행한다. **적용 전에 green 으로 적지 않는다.**
- 등재 자리 = `STAGE2-ROADMAP §` 게이트 표(이번 회차가 원문 아래에 붙였다).

### 1-2. 설명 적재 시각 — 값 둘 다 살린다

| 값 | 무엇을 잰 것인가 | 실측 |
|---|---|---|
| **2026-08-26T11:14:04.581286+00** | `d3_dataset_description.updated_at` 최댓값 | 12행 전건 2026-08-26 |
| **2026-08-27** | 설명 결손 2건을 채운 시각 | `d3_dataset.last_modified_at` = **2026-08-26T18:40:06+00 = 2026-08-27T03:40 KST** |

근거 셋 — ⑴ 설명 표에 **비내부 트리거 0개** · 열 기본값은 `now()` 뿐이라 `UPDATE` 로 안 움직인다.
⑵ 갱신 경로(`services/core-api/src/colab_core/domains/d3_catalog.py`)는 설명 표의 `name`·`topic`·`summary` 만 고치고
시각은 **`d3_dataset.last_modified_at` 에만** 박는다. ⑶ 결손 2건은 본문이 채워져 있으면서
`updated_at` 이 12행 중 **가장 이른 두 값**이고 그 둘만 `last_modified_at` 이 앞서 있다.
⟹ **설명 표의 시각은 「행 삽입 시각」이지 「본문 수정 시각」이 아니다.** 어느 문서도 틀리지 않았다.
등재 자리 = `sessions/K4-EVALSET-G2.md §1`(원문 유지 · 아래에 해소 블록).

### 1-3. 휴면 시험 실패 3건 — 레포가 아니라 사본

| 어디서 | 수집 | 통과 | 실패 | 오류 |
|---|---:|---:|---:|---:|
| 호스트 venv (python 3.12) | 34 | 31 | **3** | 0 |
| **배포 이미지 `colab-v2/pipeline-worker:30b3e0a7b3f3`** | 34 | **34** | **0** | 0 |

`--rm` · `--network none` · 레포 `:ro` 마운트 · 호스트 포트 미공개.
⟹ `pydantic-core` 2건 · `test_hsr_with_reference_grid_completes` 1건은 **이 사본의 venv 구성 문제**다.
⚠ 레포 루트를 마운트하지 않으면 `contracts/events/envelope.json` 미발견으로 error 1건이 난다 — 마운트 범위 문제이지 시험 결함이 아니다.
등재 자리 = `sessions/I3-DEPLOY-AUTOMATION-PREP.md §5.2`.
**세지 않은 판단기준** = `stage2-markers.sh` 를 이미지 안에서 돌게 바꿀지(배선 변경 · 이 회차 범위 밖).

---

## 2. 이번에 닫지 **않은** 것 — 무엇이 있어야 닫히나

| 마커 | 어디 | 닫히려면 |
|---|---|---|
| 정문 `POST /searches` 1회 · `listDatasets` 1회 | `sessions/DRIFT3-MEASURE.md` | **staging 앱 자격.** 없이는 못 잰다 |
| `Q-19`(ai-service 정지 후 503) | 같은 곳 | **컨테이너 정지** — 경계 밖. 직전 통과를 이번 통과로 세지 않는다 |
| `Q-20`(타 연구실 계정 질의) | 같은 곳 | **앱 롤 자격** |
| 복원 리허설 · 별칭 결함 둘 | 같은 곳 | 운영 회차(비가역) — Ted 승인 |
| `LV-4` 저장 자리 설계 · `〈196〉` 경고 문구 · `LV-3` 화면 문구 | `work-items.yaml` · `WORK-UNITS §` | **상품 판정(Ted)** — 지어내지 않는다 |
| `S-07`(연구실 설정) 소유 에픽 | `work-items.yaml` | **에픽 배정(Ted)** |
| `COLAB_AI_TEST_DICT_DB_URL` 값 출처 | `STAGE2-ROADMAP §` | 설정 출처가 Ted 홈의 `0600` 파일 — 자격 |
| NDVI·강수 임계 테이블 | `DATA-REFERENCE §` | v1 PoC 에 값이 **없다** — 신설은 상품 판정 |

---

## 3. 게이트 — `./gates/run.sh all -j 4` (2026-08-30 · 이 워크트리)

**green 26 · red 3 / 29종.** 우회·비활성화·범위 축소 **0건**.

| red | 사유(실측) | 성질 |
|---|---|---|
| `schema-diff` | 전 종 실행에는 적용 DB 변수를 주지 않았다 | **환경 부재 red.** 드리프트 자체는 §1-1 에서 따로 쟀다 |
| `autometa-loss` | `COLAB_APPLIED_DB_URL_PLATFORM` 미지정 | 환경 부재 red — 설계상 정상 |
| `stage2-markers` | 호스트 venv 에서 실패 3건 | **사본 구성**(§1-3). 배포 이미지에서는 34/34 |

⚠ 부수 관측 — `gates/config/parallelism.toml` 에 **`autometa-loss`·`autometa-loss-selftest` 2종이 미선언**이다.
실행기는 안전한 쪽(단독)으로 돌리고 건수를 출력에 적었다 — **설계대로**이며 green-by-skip 아니다. 선언 보충은 게이트 소관 레인에 넘긴다.

---

## 4. `PLAN-SoT §9` 등재용 원문 (**번호는 오케스트레이터가 발급한다**)

> | 〈번호〉 | **닫을 수 있던 `[미확인]` 셋을 실측으로 닫았다 — 스키마 드리프트는 미적용 마이그레이션 1건 · 설명 시각은 기준 차이 · 휴면 시험 실패는 사본 문제** | **실측 (2026-08-30 · 워크트리 `lane2-search` · 정본 `sessions/R3-UNKNOWNS-CLOSE.md`).** **㉮ 적용 스키마 축을 읽기 전용으로 쟀다** — `db/ai` 드리프트 없음 · `db/platform` 드리프트 **1곳**(`d4_lineage_edge_origin_check` 허용값). 원인 = `0008_lineage_origin_labels.py` **staging 미적용**이고 문서가 이미 그렇게 적어 둔 것과 일치한다. **새 결함 아니고, 적용 전에는 green 이 될 수 없다.** **㉯ 설명 적재 시각의 갈림은 오기가 아니라 기준 차이다** — `2026-08-26T11:14:04+00` = 설명 행 **삽입** 시각(최댓값) · `2026-08-27` = 결손 2건 **본문 채움** 시각(`d3_dataset.last_modified_at` = `2026-08-26T18:40:06+00` = KST 2026-08-27 03:40). 근거 = 설명 표에 비내부 트리거 0개 · 갱신 경로가 그 열을 건드리지 않음 · 결손 2건만 두 시각이 역전. **두 값 다 남긴다.** **㉰ 휴면 시험 실패 3건은 레포 결함이 아니다** — 배포 이미지 `colab-v2/pipeline-worker:30b3e0a7b3f3` 에서 **34건 전건 통과**(호스트 venv 는 34 중 31). ⚠ **못 닫은 것은 전부 자격·운영 접촉·상품 판정에 걸린다**(정문 호출 2 · 컨테이너 정지 1 · 타 연구실 자격 1 · 복원 리허설 1 · 상품 판정 4). **게이트 = green 26 · red 3**(환경 부재 2 · 사본 구성 1) · 우회 0. |
