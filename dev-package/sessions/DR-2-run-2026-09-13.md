# DR-2 — 실행 기록 (2026-09-13)

- 승인 = `PLAN-SoT §9 〈396〉`-㉲ Ted GO · 1회 소진 · advisor 게이트 ③ 조건(자체 점검 15/15) 충족(`dev-package/sessions/DR-2-deploy-2026-09-13.md` 3차).
- 절차 정본 = `dev-package/sessions/DR-2-runbook.md`. 실행 sha `81784897ec74`(= `origin/main` tip).
- 비밀 취급 = 접속 문자열·비밀번호·키 미기재. 호스트 경로(`/opt/…`·`/etc/colab`)는 EC2 운영 식별자이고 레포 경로는 루트 기준 상대경로다.

---

## A 단계

### 결과 요약

- 진행 = 사전 점검 → 계수 → 앱 4단위 정지 → 두 체인 스키마 재생성 → 확장 → 마이그레이션 2체인 → 앱 롤 GRANT.
- 정지 지점 = 3절 ⑤ 의 **`account-admin`** · 종료코드 **3** · 축자 「`ERROR:  permission denied to alter role` / `DETAIL:  Only roles with the SUPERUSER attribute may change the SUPERUSER attribute.`」
- 미실행 = ⑤′(`up.sh` 전체) · 4절 `deploy_doctor` · ⑥(`s3-plan`) · ⑦ · ⑧(`s3-apply`) · 5절 SQL 선행 4단계.
- S3 무접촉 = 계획 생성·적용 0회. `_ops/` 접촉 0.

### 사전 점검 (실측)

| # | 항목 | 값 |
|---|---|---|
| 1 | `deploy_release.py status dr2-dev-81784897ec74-3` | `deployment=verified` · 단계 16건 전건 exit 0 |
| 2 | 다른 작업 사본 진행 중 배포 | 0 (사본 3개 · 상태 `verified`·`failed` 뿐) |
| 3 | EC2 `colab-ops-*` 컨테이너 | 0 |
| 4 | 원격 초기화 도구 존재 | `/opt/colab-repo/services/core-api/ops/reset_dev_environment.py` 27099 B |
| 5 | `deploy_doctor.py` md5 | 개발 기계 = EC2 = `a2d5651512ff786f0a71eeb36e51ed9c` |
| 6 | `/opt/colab-repo/db/platform/versions` 최신 | `0031_search_evidence.py` |
| 7 | `/etc/colab/master.url` | 존재 |
| 8 | `required-env` 이름 4건 | `COLAB_OWNER_PASSWORD`·`COLAB_APP_PASSWORD`·`COLAB_AI_APP_PASSWORD`·`COLAB_ACCOUNT_ADMIN_PASSWORD` — 출처 URL 파일 4건 전부 존재·0600·base64url 문자 집합 충족 |
| 9 | 버킷·호스트 식별자 | `COLAB_CORE_S3_BUCKET=colab-platform-data-dev` 정확 일치 · 두 소유자 URL 호스트에 `-dev` 포함 |
| 10 | 실행 sha | `/opt/colab-v2/CURRENT_SHA` = `81784897ec74` · `MAIN_SHA` = `ancestor=yes` |

### ① 계수 (실행 전 · 연구실 경계를 건 상태)

- 보고서 = 호스트 `/tmp/out/count-before.json`(0600) · 회수본에 URL·키 0건.
- platform 스키마 `['account_admin','public']` · 연구실 2.
- 행수 = `d3_dataset` 27 · `d3_file` 450 · `d6_project` 6 · `d4_lineage_edge` 9.
- S3 = `uploads/` 840 · `previews/` 437 · 진행 중 멀티파트 0.

### ①′ 앱 4단위 정지

- `docker compose stop core-api pipeline-worker viz-render ai-service` exit 0 · 4건 `Stopped`.
- `pg_stat_activity`(마스터 롤 · `colab_platform`·`colab_ai`) 활성 0 · 전체 백엔드 0 · `state is null` 0 → NULL 가림 문제 미발생.

### ② 스키마 재생성

- exit 0 · 축자 「`── 두 체인 스키마 재생성 COMMIT.`」 ＋ 다음 명령 4줄.
- DDL 8건 실행 = 체인별 `DROP SCHEMA … CASCADE` · `CREATE SCHEMA public AUTHORIZATION colab_owner` · `REVOKE CREATE … FROM PUBLIC` · `COMMENT ON SCHEMA public`.

### ③ `extensions`

- exit 0 · `CREATE EXTENSION` 2건 · 축자 「`extensions: ok`」.

### ④ 마이그레이션 2체인

- `migrate-platform`·`migrate-ai` 각각 exit 0 · 출력 0줄(체인 `alembic.ini` 의 로깅 수준이 WARN 이라 정상 동작이 무출력이다).
- 살아 있는 DB head 실측 = `alembic_version_platform` = `0031_search_evidence` · `alembic_version_ai` = `0007_merge_vocab_and_category`.
- ⚠ 기록 = `migrate-platform` 을 2회 냈다. 1회차 직후 검증 질의가 **표 이름을 틀려**(`alembic_version` · 정본은 체인별 `alembic_version_platform`) 미적용으로 오판한 것이 원인이다. 2회차는 `upgrade head` 멱등 재실행이라 상태 변화 0. 파괴적 단계 재시도 아님.

### ⑤ 앱 롤 GRANT — `app-grants` 통과 · `account-admin` 실패

- `app-grants` exit 0 · 축자 「`app role grants: ok (colab_app@platform · colab_ai_app@ai · SELECT only)`」.
- `account-admin` **exit 3** — 실패 문장 = `services/core-api/ops/account-admin-role.sql` 의 `ALTER ROLE %I NOSUPERUSER NOCREATEDB NOCREATEROLE NOINHERIT BYPASSRLS PASSWORD %L`.
- 원인 = RDS 마스터가 `rds_superuser` 이고 진짜 SUPERUSER 가 아니다. PostgreSQL 은 `NOSUPERUSER` 를 **명시한 것만으로** SUPERUSER 속성 변경으로 판정해 슈퍼유저를 요구한다(현재 값이 이미 `NOSUPERUSER` 여도 같다).
- 성격 = 이번 초기화가 만든 결함이 아니라 **RDS 경로에서 이 스크립트가 통과할 수 없는 구조**다. `infra/dev/db-bootstrap.sh` 의 `account-admin` 은 `infra/staging/db-bootstrap.sh` 로 위임되고 원격 갈래는 마스터 URL 로만 붙는다.
- 실패 위치가 GRANT 앞이라 **GRANT 문 전건 미실행**이다.

### 정지 시점의 dev 실물 (읽기 전용 실측)

| 대상 | 값 |
|---|---|
| `colab_account_admin` 롤 | 존재 · `super=false` `createdb=false` `createrole=false` `inherit=false` `bypassrls=true` `login=true` (롤은 클러스터 수준이라 스키마 재생성으로 사라지지 않았다) |
| 같은 롤의 표 GRANT | **0건** |
| 같은 롤의 스키마 USAGE | `public` false · `account_admin` false |
| `colab_app`·`colab_ai_app` | GRANT 재적용 완료 |
| 두 체인 | 스키마 재생성 ＋ head 적용 완료 |
| 앱 4단위 | **정지 상태 유지** |
| S3 | 무변 — `uploads/` 840 · `previews/` 437 · 멀티파트 0 |

- 귀결 = `account_admin` 스키마의 표에 접근권이 없어 **로그인 경로가 동작하지 않는다**. ⑤′ 를 먼저 내도 로그인과 `deploy_doctor` 판정이 갈린다.

### 판정 대기 항목

1. `account-admin-role.sql` 의 `ALTER ROLE` 에서 `NOSUPERUSER`·`NOCREATEDB`·`NOCREATEROLE` 를 빼고 `NOINHERIT BYPASSRLS PASSWORD` 만 남기는 갈래 — 신설 경로의 `CREATE ROLE` 은 무변, 기존 롤 재적용 경로만 RDS 에서 성립.
2. `ALTER ROLE` 을 현재 속성과 다를 때만 내도록 조건화하는 갈래.
3. 어느 갈래든 레포 수정 ＋ 배포 트리 동기화가 선행이고 `DR-2` 잔여 단계(⑤′·4절·⑥~⑧·5절)는 그 뒤에 잇는다.

- ⛔ 승인 1회 소진 규약 — 잔여 단계를 잇는 데 새 GO 가 필요한지 Ted 판정 대상이다. 이 창에서 파괴적 단계 재시도 0.

### 산출물 자리

- 계수 보고서 = 호스트 `/tmp/out/count-before.json` · 스키마 보고서 = 호스트 `/tmp/out/schema.json` · 둘 다 0600.
- 계획 파일(`s3-plan`) 미생성.

---

## A 단계 재개 (같은 날 2회차)

### 전제

- 재개 근거 = `services/core-api/ops/account-admin-role.sql` 의 `ALTER ROLE` 이 둘로 갈라지고 `NOSUPERUSER`·`NOCREATEDB`·`NOCREATEROLE` 가 빠진 판이 `main` 에 있음.
- 실행 sha 무변 = `81784897ec74`. 이미지 재배포 0 · 배포 트리만 `main` 기준으로 동기화.
- 승인 = 새 GO 없음. 재개 범위는 A 단계 잔여분까지이고 `s3-apply`·SQL 선행은 미착수.

### 1. 배포 트리 동기화

- `main` 아카이브(`db`·`gates`·`services/core-api/ops`·`infra`) 생성 → scp → `sudo tar xzf … -C /opt/colab-repo --overwrite` · 3건 exit 0.
- md5 대조 = `services/core-api/ops/account-admin-role.sql` 개발 기계 = EC2 `7b868431a1b83562dc5aa2f8337bc130`.
- `services/core-api/ops/deploy_doctor.py` md5 `a2d5651512ff786f0a71eeb36e51ed9c` 무변 · `reset_dev_environment.py` 27099 B.

### 2. ⑤ `account-admin`

- 1회차 exit 1 · 사유 = 환경변수 이름 부족. `infra/staging/db-bootstrap.sh` 가 머리에서 `COLAB_OWNER_PASSWORD`·`COLAB_APP_PASSWORD` 를 **단계와 무관하게** 요구한다. DB 접촉 0 · 파괴 단계 미진입.
- 2회차 exit 0 — 네 이름 전부 0600 URL 파일에서 추출해 주입. 축자 「`account admin role: ok (운영자 등기 자동 변경 없음)`」.
- 사후 확인 = `core-database`·`ai-db`·`account-admin-database` 각각 `select 1` exit 0 · ai 시드 `d9_method_term` 13 · `d9_topic_synonym` 18 · `d9_place_alias` 4.
- `\dp account_admin.login_credential` = `colab_account_admin=arw/colab_owner`(SELECT·INSERT·UPDATE) · 소유자 `colab_owner=arwdDxt`.
- ⚠ 런북 정정거리 ㉠ = ⑤ 사후 확인의 `psql "$(cat …url)"` 는 그대로 동작하지 않는다. URL 파일 셋 다 스킴이 `postgresql+psycopg` 라 psql 이 URI 로 읽지 않고 로컬 소켓으로 붙어 exit 2. 스킴을 `postgresql` 로 바꾼 뒤 exit 0.
- ⚠ 런북 정정거리 ㉡ = URL 파일이 uid 10001 소유 0600 이라 psql 컨테이너에 `--user 0` 이 필요하다.

### 3. ⑤′ `up.sh` 전체 ＋ `deploy_doctor`

- `up.sh` exit 0 · 4단위 healthy · 저장 모드 `s3`(pipeline-worker·viz-render) · 마지막 줄 「`up: ok (sha 81784897ec74)`」.
- `deploy-verification.sh` 1회 = 「`항목 15 — ✓ 15 · ✗ 0 · ─ 0`」 · ⑮ `81784897ec74 ∈ main`. 재시도 0 · 부분 실행 합산 0.
- 외부 진입 `/api/v1/me` = 401(API 생존).

### 4. ⑥ `s3-plan`

- 1회차 exit 2 · 사유 = `COLAB_CORE_S3_BUCKET` 빈 값. 버킷·리전은 `dev.env` 가 아니라 `compose.yml` 의 리터럴이다(런북 정정거리 ㉢). 도구가 아무것도 하지 않았다.
- 2회차 exit 0 · 계획 키 **1277 건** = `uploads/` 840 ＋ `previews/` 437 — ① 실행 전 계수와 일치.
- 두 접두사 밖 키 **0** · `_ops/` **0** · 진행 중 멀티파트 **0**.
- 계획 sha256 = `658884b2740e4497fafcedab16a5c7c249bfc71b85288434c0f7bfe1a3d42dae`.
- 계획 본문 필드 = `bucket`·`keys`(문자열 배열)·`multipartUploads`·`schema`·`sha256` — 접속 문자열·비밀 0건.
- 자리 = 호스트 `/tmp/out/plan.json`(0600) · 보고서 `/tmp/out/s3-plan.json`(0600).

### 5. 정지점

- `s3-apply` 미실행 · 5절 SQL 선행 4단계 미실행 · 이 창의 S3 객체 삭제 **0**.
- 다음 착수 조건 = 계획 적용에 대한 Ted 판정. 계획 검토 ⓐ~ⓓ 는 위 4 의 실측으로 충족.

---

## B 단계 (같은 날 3회차 · 런북 3절 ⑦~⑧ ＋ 4절 ＋ 5절)

### 전제 재확인 (실물)

- 실행 sha 무변 = `81784897ec74`. 앱 4단위 전부 `healthy`. 이미지 재배포 0.
- 계획 파일 = 호스트 `/tmp/out/plan.json` 0600 · 내부 `sha256` = `658884b2740e4497…` · `keys` 1277 · `multipartUploads` 0.
- 실행 전 `_ops/` 객체 = **66**(버킷 전건 대조용 기준선).
- 런북 정정 3건 반영 = 스킴 `postgresql+psycopg` → psql 용 `postgresql` 치환 · URL 파일이 uid 10001 0600 이라 psql 컨테이너 `--user 0` · 버킷·리전은 `compose.yml` 리터럴(`colab-platform-data-dev` · `ap-northeast-2`).

### ⑦ `s3-apply`

- exit **0** · 축자 「`── s3-apply — 키 1277 건 · 멀티파트 0 건`」.
- 보고서 `/tmp/out/s3-apply.json` — `planned.keys` 1277 · `planned.multipartUploads` 0 · `failures` **[] (0건)** · `after.objects` `uploads/` 0 · `previews/` 0 · `after.multipartUploads` 0.
- 미삭제 키 **0**. 재시도 0 · 부분 실행 0.

### ⑧ 사후 확인

| 대상 | 값 |
|---|---|
| `uploads/` 객체 | 0 |
| `previews/` 객체 | 0 |
| 진행 중 멀티파트 | 0 |
| 버킷 전체 객체 | 66 (＝ `_ops/` 66 · 그 밖 0) |
| `_ops/` 객체 | **66 — 실행 전과 무변** |

- `--phase count` exit 0 · 보고서 `/tmp/out/count-after.json`(0600) · 회수본에 접속 문자열·키 0건.
- 계수(연구실 경계를 건 경로) = `d3_dataset` 0 · `d3_file` 0 · `d6_project` 0 · `d4_lineage_edge` 0 · 연구실 0 · S3 `uploads/` 0 · `previews/` 0 · 멀티파트 0.
- 스키마 = platform `['account_admin','public']` · ai `['public']`.

### 5절 SQL 선행

- 실행 형태 = `postgres:16-alpine` · `--network host --user 0` · URL 파일 읽기 전용 마운트 · 컨테이너 안에서 스킴 치환. core-api 이미지에 `psql` 부재 실측.
- 원격 SQL 2건 md5 = 개발 기계와 일치(`provision-lab.sql` `c0e8ad19…` · `provision-service-operator.sql` `ad86e583…`).

**① 연구실 — `infra/staging/provision-lab.sql`**

- exit 0 · `INSERT 0 1` 4건 · 계수표 = `d1_account` 1 · `d1_lab` 1 · `d1_lab_profile` 1 · `d2_member_role` 1 · `d2_permission_switch` 0.
- 생성물 = 연구실 「고려대학교 수문학연구실」 · 계정 「전창현」 · 역할 `교수` · 로그인 아이디 `pi@hymets.invalid`.

**② 첫 계정 — `services/core-api/ops/provision-account.sql` 미실행(건너뜀)**

- 사유 = ① 이 교수 계정 1건(`d1_account` ＋ `d2_member_role`)을 이미 심는다. ② 를 그대로 내면 같은 연구실에 두 번째 계정이 생긴다.
- 런북 5절 ③·④ 가 요구하는 `account_id` 는 ① 이 심은 값을 그대로 썼다. 교수는 네 스위치가 항상 켜진 것으로 판정되므로 `d2_permission_switch` 0행이 정상이다.

**③ 첫 로그인 자격 — `account_admin.login_credential` INSERT 1건**

- 접속 = `account-admin-database.url`(`colab_account_admin` 롤). 같은 트랜잭션 첫 문장으로 `pg_advisory_xact_lock(1131379081)`.
- 값 생성 = 제품과 같은 두 함수 그대로 — `colab_core.kernel.password.hash_password` · `colab_core.kernel.db_credentials.normalize_login_name`.
- 열 목록 = 앞 여덟만(`account_id`·`login_name`·`kdf`·`salt`·`password_hash`·`n`·`r`·`p`) · 뒤 넷은 DB 기본값 수령.
- 결과 = `login_credential` **1행** · `login_name` `pi@hymets.invalid` · `kdf` `scrypt` · `n`/`r`/`p` = `16384`/`8`/`1` · `must_change_password` **true** · `session_version` **1**.
- 초기 비밀번호 = 24자 무작위. **표준입력 한 줄로만 전달**했고 argv·환경변수·로그·이 문서에 0건. 자리 = 세션 스크래치 `initial-password.txt`(0600 · 레포 밖). 원격 임시 스크립트는 실행 직후 `shred -u` 로 삭제 확인.

**④ 서비스 운영자 — `services/core-api/ops/provision-service-operator.sql`**

- FORCE RLS 대응 = 같은 psql 세션에서 `SET app.current_lab` 을 먼저 건 뒤 파일 실행(이 파일은 경계를 스스로 걸지 않는다).
- exit 0 · `INSERT 0 1` · `account_admin.service_operator` **1행**.

### 4절 재확인

- `deploy_doctor`(`deploy-verification.sh`) **1회** = 축자 「`항목 15 — ✓ 15 · ✗ 0 · ─ 0`」 ＋ 「`전 항목 통과 (─ 0 — 15 항목이 실제로 돌았다)`」 · exit 0. 재시도 0 · 부분 실행 합산 0.
- ⑮ = `81784897ec74 ∈ main`. ⑭ 백업 = 0.9시간 전 · 객체 63건. ⑬ `/api/v1/me` 401.
- 외부 진입 실측 = CloudFront `/api/v1/me` **401**(미인증 거절 · API 생존).

### B 단계 정지 상태

- dev 데이터 = 데이터셋·파일·프로젝트·계보 전건 0 · S3 사용자 객체 0 · `_ops/` 무변.
- 로그인 경로 = 연구실 1 · 계정 1(교수) · 자격 1(첫 로그인 비밀번호 변경 강제) · 서비스 운영자 1.
- 미실행 = 런북 7절 기록 4건(대장·HANDOFF·원장·태그) · 시나리오 §2 이후(프로젝트·데이터셋·계보·미리보기).
- 이 창의 파괴적 단계 재시도 0 · staging 무접촉 · `_ops/` 무접촉 · 다른 버킷 무접촉.
