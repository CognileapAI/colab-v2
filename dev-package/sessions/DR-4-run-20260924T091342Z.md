# DR-4 — dev 무인 재생성 실행 기록 (2026-09-24)

- 성격 = `dev-package/tools/dev-reseed/reseed.sh` 1회 실행의 **생성물**. 값의 원본은 `result.json` 이다.
- 승인 = dev 한정 상시 승인(`.claude/rules/deploy.md` 11번 증보 문단). 회차별 GO 불요 · 승인 기록 = `approval-record.json`.
- 실행 자리 = `<RUN_DIR>` (`--run-dir` 입력으로 지정한 실행 폴더; `result.json` 포함) · 실행 식별자 `20260924T091342Z` · 배포 대상 sha `ea21d8c2aa54`.
- 단계 로그와 승인 기록의 상대경로 기준은 실행 폴더다. `result.json`의 `runDir`는 JSON 파일 위치 기준이다.
- 결과 = **failed**(멈춘 단계 `seed`) · 총 소요 78초.

## 1. 단계별 소요

| 단계 | 상태 | 종료코드 | 소요(초) | 로그 |
|---|---|---|---|---|
| preflight | ok | 0 | 4 | `logs/preflight.log` |
| rehearse | skipped | — | — | `—` |
| deploy | skipped | — | — | `—` |
| reset | ok | 0 | 14 | `logs/reset.log` |
| bootstrap | ok | 0 | 12 | `logs/bootstrap.log` |
| up | ok | 0 | 18 | `logs/up.log` |
| s3 | ok | 0 | 7 | `logs/s3.log` |
| prelude | ok | 0 | 5 | `logs/prelude.log` |
| seed | failed | 1 | 18 | `logs/seed.log` |
| verify | skipped | — | — | `—` |
| report | ok | 0 | 0 | `logs/report.log` |

## 2. 계수

- 계수 없음 — verify 단계를 돌지 않았다.

- `deploy_doctor` 요약줄 축자 = `항목 15 — ✓ 15 · ✗ 0 · ─ 0`

## 3. 미리보기 판정 표 (0 행 · 성립 0 · 판정불가 0)

| 순번 | 데이터셋 | 가공 단계 | 판정 | ms |
|---|---|---|---|---|
| — | — | — | — | — |

## 4. 차단 (1 건)

- `preflight` · **preflight:git** — 작업 트리가 깨끗하지 않다 — 4 건

## 4-1. 자동 복구 (0 건)

- 0 건 — 정지 뒤 실패가 없었다.

## 4-2. 이 회차가 실제로 무엇이었나 (레인 기입)

- **중단 회차(`20260924T083322Z`)의 재개**다. 실행 자리(`--run-dir`)와 러너 상태(`.work`)를 그대로 이었다.
  그래서 위 단계표의 `reset`~`prelude` 는 **08:33~08:34Z 의 값**이고, 이 회차가 실제로 돈 것은
  `preflight`(4초) · `seed`(18초) · `report` 뿐이다.
- §4 의 차단 `preflight:git` 도 **앞 시도(09:12:47)의 것**이다 — `blocked.jsonl` 이 실행 자리에 누적된다.
  그 시도는 작업 트리가 깨끗하지 않아 preflight 가 막은 것이고 `seed` 에 들어가지 않았다.
- §2 의 `deploy_doctor` 15/15 는 **08:34Z `up` 단계의 값**이다. 이 회차는 doctor 를 다시 재지 않았다.
- `seed` 가 멈춘 자리 = `projects` 국면. 원인은 **자격이 바뀐 세션을 제품이 거절**한 것이고
  (`kernel/session_token.py:59-61` · `kernel/login_sessions.py:192-206`), 화면은 프로젝트 목록이 아니라
  로그인 화면이었다(`.work/fail/projects-fail.txt`). 도구 결함이며 미리보기 뒷단(`PV-2`)과 무관하다.
- 판정 경위·수선·계수는 `dev-package/reports/corpus-expansion/wu2-reseed-2026-09-24.md` 「재개 (㈏)」 절에 있다.

## 5. 사람이 채울 자리

- 원장 등재문 · 대장 `DR-4` 상태 · `03-HANDOFF §1` 갱신은 오케스트레이터가 한다(레인·도구가 하지 않는다).
- 판정이 필요한 미성립 항목의 원인 분류(도구 결함 / 미리보기 뒷단 `PV-2`)를 여기에 적는다.
- **「판정불가」가 1건이라도 있으면 그 회차는 미리보기를 잰 것이 아니다** — 원인(브라우저 무응답 · 선택자 변경)을 먼저 적는다.
