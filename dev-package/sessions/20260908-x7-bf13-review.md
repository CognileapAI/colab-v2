# X-7 / BF-13 수용 검토 — 2026-09-08

For: X-7은 시험 목적과 무관한 DB 물리순서 의존을 제거하고, 열린/잠긴 fixture를 별도로 검증한다. BF-13은 실제 CSS 원문을 읽으며 제품 값과 위치를 바꾸지 않아 범위에 맞는다.
Against: BF-13 선언 파서가 유효한 CSS를 검사에서 누락하므로 현재 모든 공통 토큰 값 일치를 보장한다는 수용은 불가. X-7 변경을 폐기할 근거는 발견하지 않았다.
Verdict: X-7 approve(전체 게이트/새 원격 결과 별도); BF-13 approve-with-changes.

## P2 — 마지막 선언의 세미콜론 생략 시 검사 누락

- `frontend/test/shared-css-tokens.test.ts` declarations의 `(--[a-z0-9-]+)\s*:\s*([^;]+);`는 끝 세미콜론을 필수로 요구한다.
- CSS 블록 마지막 선언은 세미콜론 없이 유효하다. 한 화면에서 공통 토큰을 마지막에 다른 값으로 두고 세미콜론을 생략하면 해당 선언이 추출되지 않아 다른 화면과의 불일치가 보고되지 않는다.
- 남은 공통 토큰들이 존재하므로 raw/공통 선언 비어있음 방지는 이 경우를 막지 못한다. 현재 변이 시험도 모든 변이에 세미콜론을 붙이므로 같은 구멍을 검사하지 않는다.
- 수정: 블록 끝으로 종료되는 선언도 수용하고, 동일 원문에서 마지막 공통 선언의 값을 바꾼 뒤 끝 세미콜론을 생략하는 실패 fixture를 추가한다. 정적 파서 범위를 늘리지 않아도 이 정상 CSS 형태는 잡아야 한다.

## X-7 검토

- `conftest.py:32-33`의 DS_A1은 열림, DS_A2는 잠김으로 선언되어 있다. 변경은 해당 상수를 사용해 기대202/403을 분리한다.
- 정상202 경로의 fake viz는 여전히 service bearer를 검사하므로 기존 인증 중계 오라클을 유지한다.
- 잠김403은 제품 권한 규칙을 약화하지 않고 음성 조건을 더한다. 임의 LIMIT1 query는 제거됐다.
- 새 시험/기존bodyaccess10pass는 메인 전달값이며 리뷰에서 재실행하지 않았다. 전체core gate는 진행 중 전달 상태, 새 원격CI 성공은 미측정.

## BF-13 적정 범위

- vite 설정은 catalog.css?raw만 추가 허용하고 기존 CSS 적용을 유지한다.
- CSS 제품 값/위치 변경 없음. 실제 ?raw 원문, 세 파일 각각 선언 존재, 실제 공통 선언 존재를 확인한다.
- 현재16개 값 일치는 이전 조사 실측이며 이번 리뷰의 재실행 수치가 아니다. 메인의7파일 회귀 완료 전 전체회귀 통과로 기록하지 않는다.
- 정책/계약/마이그레이션 변경 없음. 전체 제품 완료 또는 BF-11 선행 수용 완료를 이 리뷰가 대신하지 않는다.

코드/대장 편집0, 테스트 실행0. 지정 노트만 작성했다.

## P2 수정 재확인

- 격리 worktree의 `shared-css-tokens.test.ts`를 다시 읽었다. 선언 종결자가 `(?:;|$)`로 바뀌어 블록 끝에서 끝나는 마지막 선언을 수집한다.
- 마지막 선언 세미콜론을 생략한 서로 다른 색값 fixture가 추가되어 conflicts 길이1을 단언한다. 보고했던 P2 경로를 직접 덮는다.
- 메인 전달 증거: fixture 추가 후1fail3pass RED → 파서 수정 후4pass; 기존7파일120pass; TypeScript typecheck pass. X-7 전체 core-api981pass/6deselected/0skip/0fail/0error. 리뷰는 해당 시험을 재실행하지 않았다.
- 갱신 Verdict: X-7 approve, BF-13 approve. 이번 검토 대상에서 남은 수용 차단 문제 없음. 원격 CI는 미실행이며 로컬 검증을 원격 성공으로 바꾸어 기록하지 않는다. 전체 제품 완료/배포 수용은 별도다.
