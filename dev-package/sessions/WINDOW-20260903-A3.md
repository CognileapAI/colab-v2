# staging 단독 창 3 — 단계 A(짝 백업) 집행 ＋ go/no-go ① 증거 (2026-09-03 21:05~21:20 KST)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md §1`·`§10-①`. 이 파일은 **이 회차에 실제로 잰 값**만 적는다.
> 집행 범위 = **A 만**. B(배포) 미착수 — go/no-go ① 은 Ted 판정 자리이고 이 회차는 증거만 세운다.
> 선행 회차 = `WINDOW-20260903-A2.md`(A 양식) · `WINDOW-20260903-F2.md`(창 2 종료 상태 · 서빙 `d9e89a3a074b`).
> 이번 배포 후보 = **`main` @ `56eac76`**.

## 0. 창 진입 전 상태 (실측 · 읽기 전용 · 2026-09-03 21:05~21:12 KST)

| 축 | 실측값 | 재는 방법 |
|---|---|---|
| 커밋 | `56eac76` · 브랜치 `main` | `git log -1` |
| 워킹트리 | **변경 0건** | `git status --porcelain` = 0줄 (`deploy.sh:62`) |
| 워크트리 | 측정 시점 **`main` 하나뿐** — ⚠ 아래 §0.1 참조 | `git worktree list` |
| 서빙 릴리스 | **`d9e89a3a074b`** — 앱 5종 이미지 태그 동일(`core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`) | `docker ps` ＋ `LAST-SUCCESS.txt` 마지막 줄 `2026-09-03T12:43:56+0900 deploy OK d9e89a3a074b` |
| 컨테이너 | staging **8개 전부 healthy** (`nginx`·`frontend`·`core_api`·`pipeline_worker`·`viz_render`·`ai_service`·`cloudflared`·`pg`) | `docker ps` |
| 체인 head (platform) | **`0011_lv1_drop_level_user_set`** · 리비전 11건 · head 1개 | `gates/run.sh migration-single-head` |
| 체인 head (ai) | **`0005_k2b_concept_graph_seed`** · 리비전 5건 · head 1개 | 〃 |
| 원장 green `deploy` 행 | **≥2** (`d9e89a3a074b` · `72408173be50` · `77813d391f94`) | `release-ledger.tsv` |
| 창 시각 충돌 | **없음** — 21:05 착수, 크론은 03:30 · 04:10(월) · 04:40 | `0.1-ⓔ` |
| `COLAB_STAGING_ENV` | 셸 기본 환경에 **미설정** — B 착수 시 홈 0600 env 파일을 export 해야 한다 | 종전 회차와 같음 |

⚠ **staging 스택 밖 컨테이너 2건**(`a2_pg`·`ai_pg` · 5일 가동)은 8개 계수에 넣지 않았다. 이 회차 무접촉.

### 0.1 ⛔ **레인 전제가 깨졌다 — 감추지 않고 적는다**

- 창 진입 측정(21:05)에서 `git worktree list` 는 **`main` 한 줄**이었다.
- **21:11:56 에 워크트리 `review-pr1` 이 새로 등록됐다**(`.git/worktrees/review-pr1/gitdir` mtime 실측 · detached `0e294e5`「PR 본문을 지금 브랜치 실물에 맞춘다」).
- ⟹ **단계 A 집행(21:16)은 다른 체크아웃이 존재하는 상태에서 돌았다.** 선행 조건 `0.1-ⓓ`(다른 레인 0개)가 그 시점에 성립하지 않았다.
- **A 의 판정에 미치는 영향 = 없음으로 판단한다** — ⑴ `review-pr1` 은 레포 체크아웃일 뿐 staging 스택·백업 보관처를 만지지 않았고 ⑵ `main` 워킹트리 변경은 여전히 0건이며 ⑶ 백업은 additive 라 다른 레인이 오염시킬 대상이 없다. **다만 판단이지 실측이 아니다 — B 착수 전에 이 워크트리가 무엇을 하는지 확인하고, staging 을 만지는 레인이면 창을 닫는다.**
- 등록되지 않은 잔여 디렉터리 1건(`.claude/worktrees/agent-a992b5e50a78fd047`) — `.git/worktrees/` 에 없다. 워크트리 계수 밖.

## 1. B 가 실어 나를 변경분 — `git diff --stat d9e89a3..56eac76`

**총계 = 182 파일 · +11,217 / −501.**

| 영역 | 파일 수 | 내용 |
|---|---|---|
| `gates` | 57 | 검사기 43→50 (서비스 pytest 4 ＋ 셀프테스트 ＋ 도구·설정·픽스처) |
| `frontend` | 27 | 17차(자기 연구실 묘비 `Gone`) · 정직한 빈 상태 · 화면 시험 |
| `dev-package` | 25 | 회차 기록·노트·대장 (코드 아님) |
| `services/core-api` | 24 | **`colab_api`→`colab_core` 패키지 개명** · 경계·검증 · 라우트 ＋ 시험 |
| `services/viz-render` | 22 | 캐시 키에 timestamp ＋ 격자 digest 추가 · 값 조회 테넌트 경계 · 트리거 격리 |
| `services/pipeline-worker` | 15 | 커널·d5 ＋ 시험 |
| `services/ai-service` | 10 | 경계·시험 |
| `contracts` | 1 | seam |
| `.github` | 1 | 워크플로 (서비스 pytest CI) |

**마이그레이션 — 0건. 실측이다(추정 아님).**

```
git diff --name-status d9e89a3..56eac76 -- '*/versions/*'   → 0 줄
ls db/platform/versions/   → 0001 … 0011.  **0012 없음**
ls db/ai/versions/         → 0001 … 0005.  그대로
```

⟹ **리팩터링이 마이그레이션을 더하지 않았다.** platform head 는 `0011`, ai head 는 `0005` 에서 움직이지 않는다.
**이것이 롤백 한계를 정한다** — §5.

⚠ **이 배포의 실질 파괴 축은 스키마가 아니라 캐시 키다.** viz-render 캐시 키에 timestamp ＋ 격자 digest 가 들어가면서
**지금 자리에 선 산출물·타일이 전부 무효가 된다.** 되돌릴 수 없는 것이 아니라 **다시 구워야 하는 것**이다(§6).

### 1.1 `a1ff6af..56eac76` = **문서만** (전수 실행을 대신하는 근거)

```
dev-package/03-HANDOFF.md                         | 12 +++-
dev-package/PLAN-SoT.md                           |  1 +
dev-package/WORK-UNITS.md                         |  2 +-
dev-package/sessions/CODE-REVIEW-20260903-PLAN.md |  6 +-
dev-package/sessions/CODE-REVIEW-20260903.md      |  1 +
dev-package/work-items.yaml                       | 74 ++++++++++++++++++++++-
6 files changed, 89 insertions(+), 7 deletions(-)
```

⟹ **코드 변경 0건.** 오케스트레이터가 `a1ff6af` 에서 실측한 **50/0/0** 을 `56eac76` 의 값으로 읽는다.
⛔ **이 창에서 `gates/run.sh all` 은 돌리지 않았다**(창 점유 규약 · pg 슬롯 풀 호스트 전역 4).
**이번에 세지 않은 판정기준 = `all` 전수** — B 뒤 전수 실행이 다음 회차의 진입조건이다.

## 2. 패키지 개명 `colab_api`→`colab_core` — 잔여 0건 (grep 실측)

1차 창의 red 는 판정 결함이 아니라 **프런트 타입 오류로 이미지 빌드가 죽은 것**이었다. 개명은 같은 자리를 다시 깨뜨릴 수 있는 축이라 먼저 잰다.

```
$ grep -rn "colab_api" infra/                      → 0 줄
$ grep -rn "colab_api" services/*/Dockerfile       → 0 줄 (exit 1)
$ grep -rn "colab_api" . (.git·dev-package 제외)   → 0 줄
```

- `services/core-api/src/` 아래 실물 패키지 = **`colab_core` 하나**.
- `services/core-api/Dockerfile` 의 기동 줄 = `CMD ["python","-m","uvicorn","--factory","colab_core.app.main:create_app", …]` — **새 이름**.
- `infra/staging/compose.i2.yml` 은 core-api 에 `command:` 를 얹지 않는다(이미지 태그·env 만) ⟹ **개명 표면은 Dockerfile 한 곳**이고 그 한 곳이 새 이름을 쓴다.
- `infra/staging/deploy.sh` 에는 패키지 이름이 아예 없다(grep exit 1).

⟹ **개명 잔여로 이미지 빌드가 깨질 자리 = 실측 0건.**

## 3. 배포 전 검사기 — 전부 **단독 실행**(`COLAB_GATE_JOBS=1`) · 축자

```
contract-lint green — seam 3건, 룰 위반 0.
generated-up-to-date green — 등기부 4건 전부 재생성 일치, 등기부 밖 자칭 생성물 0건.
contract-breaking green — 기준 HEAD (3건) 대비 파괴적 변경 없음.
migration-single-head green — 두 체인 모두 head 1개.
    # db/platform: 리비전 11건 · head 1개 (0011_lv1_drop_level_user_set)
    # db/ai: 리비전 5건 · head 1개 (0005_k2b_concept_graph_seed)
frontend-typecheck green — tsc --noEmit(frontend/tsconfig.json · include=src·test) 오류 0건.
frontend-test green — vitest run(frontend/vite.config.ts · jsdom) 통과 570건 · 실패 0건.
    Test Files  36 passed (36)
         Tests  570 passed (570)
service-tests-core-api green — 실행 624건 전부 통과 (skipped 0 · deselected 6 은 요약줄에 드러나 있다).
    — 선택자 «not e2e» · 수집 624 · 실행 624 · skipped 0 · deselected 6 · failed 0 · errors 0 · 소요 90.9초
service-tests-viz-render green — 실행 269건 전부 통과 (skipped 0 · deselected 40 은 요약줄에 드러나 있다).
    — 선택자 «not e2e and not perf» · 수집 269 · 실행 269 · skipped 0 · failed 0 · errors 0 · 소요 11.5초
service-tests-ai-service green — 실행 130건 전부 통과 (skipped 0 · deselected 26 은 요약줄에 드러나 있다).
    — 선택자 «not dictdb» · 수집 130 · 실행 130 · skipped 0 · failed 0 · errors 0 · 소요 2.4초
service-tests-pipeline-worker green — 실행 231건 전부 통과 (skipped 0 · deselected 46 은 요약줄에 드러나 있다).
    — 선택자 «not e2e and not dbint» · 수집 231 · 실행 231 · skipped 0 · failed 0 · errors 0 · 소요 6.3초
```

- **9건 전부 green · red 0 · 못 돈 것 0.**
- ⚠ `service-tests` 는 **한 게이트가 아니라 넷**이다(`gates/README §33`) — 넷을 각각 돌렸고 계수를 그대로 옮겼다.
- ⚠ `service-tests-selftest` 는 **이 회차에 돌리지 않았다** — `gates/run.sh all` 의 명시 면제 2건 중 하나이고 CI 의 `service-tests` 잡이 돈다. **이번에 세지 않은 판정기준으로 남긴다.**
- 게이트 총수 = **50** (`gates/run.sh` `ALL_GATES` 원소 계수 실측). 종전 43 에서 7 증가.

## 4. 단계 A — 배포 전 짝 백업 (집행 완료 · GREEN)

- 명령 = `infra/staging/backup/backup-full.sh` (플래그 없음) · `main` 체크아웃에서.
- 최종 줄 축자 — **`2026-09-03T21:17:28 전범위 백업 GREEN — 원장 2 프로파일 ＋ 볼륨 2 개 · 보관처 규약 밖 파일 0`**
- 0단 보관처 위생 통과(규약 밖 파일 0) · `═══ 1단` · `═══ 2단` 표지 양쪽 실재.

| 산출물 | 스탬프 | 크기(B) | 판정 |
|---|---|---|---|
| `platform-….sql.gz` | `20260903T211644` | 33,212 | GREEN (통과 7 · SKIP 0) — 표 6 · 데이터 행 91 · 신선도 0분 |
| `ai-….sql.gz` | `20260903T211644` | 5,159 | GREEN (통과 7 · SKIP 0) |
| `vol-uploads-….tar.gz` | `20260903T211645` | 393,734,961 | GREEN (통과 8 · SKIP 0) — V4 157=157 · V5-a 133=133 · V5-b 전건 |
| `vol-previews-….tar.gz` | `20260903T211725` | 29,466,364 | GREEN (통과 6 · **승인된 SKIP 1** = V5 오라클 명시 면제 `none`) — V4 199=199 |

**짝(회차 한 벌) — `.pair` 실측**

```
vol-uploads-20260903T211645.pair   -> platform-20260903T211644.sql.gz
vol-previews-20260903T211725.pair  -> platform-20260903T211644.sql.gz
```

두 원장 덤프의 스탬프가 **동일**(`20260903T211644`) — P5(같은 회차) 조건이 시각차 0초로 선다.

**최신본 재검사** — `infra/staging/backup/latest-check.sh` → **`최신본 재검사 GREEN — 프로파일 2 개 · 볼륨 2 개`**.

**덤프가 담은 스키마 상태 (읽기 전용 대조)**

```
platform-20260903T211644.sql.gz : alembic_version_platform = 0011_lv1_drop_level_user_set · '0012' 문자열 0건
ai-20260903T211644.sql.gz       : alembic_version_ai       = 0005_k2b_concept_graph_seed
```

⟹ 이 덤프는 **배포 후에도 그대로일 스키마**를 담는다 — 스키마 되돌림 재료가 아니라 **데이터 되돌림 재료**다.

경로 = `$COLAB_BACKUP_DIR`(홈 백업 보관처). **값·접속 문자열은 이 문서에 적지 않는다.**

## 5. B 가 무엇을 하는가 · 되돌림의 한계

- 명령 = `infra/staging/deploy.sh --target staging` (플래그 없음) · `main` 체크아웃에서 · 홈 0600 env 를 export 한 셸에서.
- **마이그레이션 = 없음**(§1 실측). `deploy.sh ⑧` 이 돌아도 head 는 `0011`/`0005` 에서 움직이지 않는다.
- **이미지 6종 재빌드** — `core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`·`migrator`. 별칭 `:i2` 6/6 재부착 ＋ digest 이력 6종.
- 실질 내용 = 클린 아키텍처 리팩터(7 레인) ＋ 17차 묘비 ＋ 게이트 50 ＋ 서비스 pytest CI, 그리고 **캐시 키 변경**.

| 축 | 값 |
|---|---|
| 스키마 변경 | **없음** (실측) |
| `rollback.sh --to-last-green` 의 복원 범위 | **이미지 = 배포 내용 전부** ⟹ **완전 복구** |
| 파괴적 복원(`--yes-drop-schema`) 필요 여부 | **불필요** |
| 롤백 대상 | **`d9e89a3a074b`** — 현재 서빙이자 직전 green |
| 그 태그 이미지 6종 실재 | ✅ **6/6** — `docker images` 실측: `core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`·`migrator` 전부 `:d9e89a3a074b`. 보존 세대 = `d9e89a3a074b`·`72408173be50`·`77813d391f94` 각 6종 ＋ `:i2` 6 ＋ `:prev` 6 |

⚠ **롤백이 되돌리지 못하는 것 하나 — 캐시.** 배포로 무효화된 산출물은 롤백해도 **옛 키로 자동 복구되지 않는다**. 옛 이미지는 옛 키를 다시 계산하므로 **옛 산출물이 그대로 살아 있는 한 화면은 돌아온다**(무효화가 파일을 지우는 것은 재굽기 경로에서만 일어난다). ⟹ **B 뒤 재굽기를 돌리기 전에 롤백하면 손실 0**, **재굽기 뒤 롤백하면 옛 키 산출물이 이미 치워졌을 수 있다.** 순서가 되돌림 비용을 정한다.

## 6. 배포 후 이 창이 돌릴 순서 (착수 전에 고정한다)

**⑴ 배포 판정 6항**(`RUNBOOK §2`) — 이미지 태그 앱 5종 ＝ 배포 커밋 · 별칭 `:i2` 6/6 동일 ID · 체인 `0011`/`0005` · 헬스 **본문** 대조 · 8 healthy ＋ `0.0.0.0` 0건 · ⑩-b 엣지 설정 바이트 대조 · 원장 마지막 줄.

**⑵ 재굽기(무효화된 산출물 다시 굽기) — 인가된 경로 실측 결과**

- ⛔ **레포에 운영자용 재굽기 스크립트·CLI 는 없다**(`infra/`·`scripts/`·`services/*/ops/` 전수 조사 · 0건). **지어내지 않는다.**
- ⛔ **스풀 봉투 경로(`COLAB_WORKER_EVENT_SPOOL=/srv/viz-events`)는 이 창에서 쓸 수 없다** — `JobStore._latest_for` 가 **프로세스 메모리**(`services/viz-render/.../jobs.py:826-832`)라 재배포로 비고, 봉투를 넣어도 `regenerate` 가 `LookupError`(「그린 적 없는 대상」)로 ack 하고 끝난다.
- ⛔ `POST /viz/v1/renders` 는 **내부 전용** — `infra/staging/nginx.i2.conf:102` 가 `/viz/` 를 404 로 막는다(타일 GET 정규식 하나만 예외).
- ✅ **인가된 유일한 경로 = 공개 엣지의 미리보기 생성**. 데이터셋 1건당 1회 —

```
curl -X POST https://www.colab-hydro.com/api/v1/previews \
  -H "Authorization: Bearer <해당 연구실 사용자 토큰 · 값은 적지 않는다>" \
  -H "Content-Type: application/json" \
  -d '{"target":{"datasetId":"<DATASET_ULID>"},"style":{"palette":"<GET /api/v1/preview-palettes 결과값>"}}'
```

  → 202 ＋ `renderId` · `GET /api/v1/previews/{renderId}` 로 완료 확인.
- **판정 = staging WRITE 다** (새 렌더 작업 생성 · `/srv/viz-previews` 볼륨에 바이트 기록). **Ted 의 ⓑ 승인이 이 자리를 덮는다.**
- 대상 = 지금 자리에 산출물이 선 **3건**(`01M0YWDXN7CA13KB7MQC9PGEW5` · `01M1JM3JTQSSTKR9A2QQ67QN81` · `01M19RENXAANZ2HDWM4Z46C37Y` · 근거 `work-items.yaml RC-1.note` 〈305〉 관측).
- ⚠ **값 조회는 굽지 않는다** — `routes/values.py` 에 `jobs.submit` 호출 0건(`V-2` 완료 정의 ⑵ 와 일치). **값 조회로 재굽기를 대신할 수 없다.**
- ⚠ 테넌트 경계 헤더 `X-CoLAB-Lab` 은 **core-api 중계가 채운다**(`app/deps.py:74` — 없으면 400 `TENANT_SCOPE_MISSING`). 사람이 붙이지 않는다.

**⑶ 소유 재실측** — `COLAB_GATE_JOBS=1 ./gates/run.sh artifact-ownership`
- ⚠ **이 회차의 배포 전 실행은 `red(준비)` 다** — 축자 「`입력미선언|missing=COLAB_ARTIFACT_OWNER_DB_URL`」. **fail-closed 가 옳은 동작이고 우회하지 않았다.** 홈 0600 env 를 주입한 셸에서 다시 돌린다.
- 직전 값(창 2 · 13:10:42) = 대상 90 · 살아 있다 57 · 접수분에만 14 · **고아 0** · 판정 불가 19. **재굽기 뒤 이 다섯 값을 다시 잰다** — 캐시 키가 바뀌었으므로 계수가 크게 움직이는 것이 정상이고, **움직이지 않으면 재굽기가 안 일어난 것**이다.
- ⛔ `RC-1` 판정(판정 불가 49→19 등)을 **재굽기 전에 내리지 않는다**(`〈307〉` 하드 진입조건).

**⑷ 현물 확인**

| # | 대상 | 기대 |
|---|---|---|
| ㉠ | 값 조회 — 산출물이 선 3건 | 200 ＋ **값이 실린다**. ⚠ **재굽기 전에는 `200 ＋ available:false`(자리에 산출물이 없다)가 정상**이다 — 캐시 키가 갈렸다. 재굽기 뒤 다시 잰다 |
| ㉡ | 값 조회 — 나머지 11건 | 재굽기 대상이 아니다. `200 ＋ available:false` 유지 |
| ㉢ | **자기 연구실 묘비 410 `Gone`** | ⛔ **도달 불가 — 실측이다.** 짝 덤프 `platform-20260903T211644` 의 `d3_dataset` **14행 전수**에서 `deleted_at IS NOT NULL` = **0건**(전 14행이 같은 연구실 `…HYMETS`). ⟹ **staging 에 묘비가 하나도 없어 410 을 현물로 확인할 수 없다.** 만들려면 데이터셋 삭제 = staging 파괴적 쓰기이고, **이 창의 승인 범위 밖이다.** 시험으로만 서 있음을 적는다(`service-tests-core-api` 624건에 포함) |
| ㉣ | 연구실 정보 탭 | 200 · 정직한 빈 상태 문구 확인 |
| ㉤ | 검색 `summary`·`period` | 200 · 질의 반영 |
| ㉥ | 타 연구실 자원 접근 | **404**(403 이 아니다 — `DEPLOY-269 §3` 판정 규약) |

## 7. go/no-go ① — 증거 대조표 (`RUNBOOK §10-①`)

| # | 조건 | 실측 | 판정 |
|---|---|---|---|
| ㉠ | A 두 프로파일 GREEN | §4 · platform·ai 각 통과 7 · SKIP 0 | ✅ |
| ㉡ | 볼륨 산출물 실재 ＋ 짝 성립 | `vol-uploads` 393,734,961B · `vol-previews` 29,466,364B · `.pair` 둘 다 `platform-20260903T211644.sql.gz` | ✅ |
| ㉢ | 워킹트리 변경 0건 | `git status --porcelain` 0줄 | ✅ |
| ㉣ | 워크트리 = `main` 하나 | ⚠ **21:11:56 에 `review-pr1` 등록** — §0.1. A 집행 시점에는 둘이었다 | ⚠ **확인 필요** |
| ㉤ | 스택 가동 중 | 8개 healthy | ✅ |
| ㉥ | 크론 시각 비충돌 | 21:05 착수 · 크론 03:30/04:10/04:40 | ✅ |
| ㉦ | 원장 green `deploy` ≥2 | `d9e89a3a074b`·`72408173be50`·`77813d391f94` | ✅ |
| ㉧ | 이미지 빌드 파괴 요인 | 게이트 9건 green(§3) ＋ 개명 잔여 grep 0건(§2) | ✅ |
| ㉨ | 마이그레이션 델타 | **0줄** · `0012` 부재 | ✅ |
| ㉩ | 롤백 대상 이미지 6종 실재 | `d9e89a3a074b` **6/6** | ✅ |
| ㉪ | **승인 행이 배포 행보다 먼저** | ❌ **미기재** — Ted 의 GO 로만 채워진다 | ⏸ |
| ㉫ | `COLAB_STAGING_ENV` | 셸 기본 미설정. B 실행 셸에서 export 필요 | ⚠ 조치 필요 |

## 8. 판정 — **GO 권고 (조건 하나 붙임)**

**결정적 사실 = 마이그레이션 0건이 실물로 확인됐고, 그 덕에 `rollback.sh --to-last-green` 하나로 배포 내용 전부가 되돌아간다.**
`git diff … -- '*/versions/*'` 0줄 ＋ `db/platform/versions/` 에 `0012` 부재 ＋ 롤백 대상 `d9e89a3a074b` 이미지 **6/6 실재**.
1차 창을 red 로 만든 이미지 빌드 자리는 게이트 9건과 개명 잔여 grep 0건으로 배포 전에 실측했다.

**붙이는 조건 = §0.1.** `review-pr1` 워크트리가 무엇을 하는지 확인하고, **staging 을 만지는 레인이면 창을 닫는다.** 레포만 읽는 레인이면 그대로 간다.

**no-go 로 뒤집는 조건** — ㉪(승인 행) 미기재 상태로 `deploy.sh` 를 부르는 것. **승인 행이 먼저다.**

## 9. Ted 의 GO 시 원장에 덧붙일 승인 행 — **축자 (이 회차는 적지 않았다)**

`~/colab-v2-releases/release-ledger.tsv` 에 탭 구분 5열로 **한 줄** append (시각은 append 시점으로 갱신) —

```
2026-09-03T<HH:MM:SS>+0900	approve	56eac76	-	-	승인자=Ted 본것=go/no-go ① GO · 짝 백업 platform-20260903T211644/vol-uploads-20260903T211645/vol-previews-20260903T211725 GREEN · latest-check GREEN · 마이그레이션 델타 0줄(0012 부재) · 개명 colab_api→colab_core 잔여 grep 0건 · 배포전 게이트 9건 green(contract-lint·generated-up-to-date·contract-breaking·migration-single-head·frontend-typecheck·frontend-test·service-tests 4단위) · 롤백 대상 d9e89a3a074b 이미지 6/6 실재 · 사후 재굽기(POST /api/v1/previews · staging WRITE) 승인 포함 · WINDOW-20260903-A3
```

⛔ **이 회차는 append 하지 않았다.** Ted 의 GO 문면을 받은 뒤 B 착수 셸에서 배포 행보다 **먼저** 넣는다.
