# staging 단독 창 — 단계 B·C 회차 기록 (배포 red · 창 중단)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md`. 이 파일은 **이 회차에 실제로 잰 값**만 적는다.
> 집행 범위 = **B-0(원장 승인 행) ＋ B(배포 시도)**. **단계 C 미착수** · 단계 D 이후 미착수.
> 회차 시각 = 2026-09-02 22:31~22:35 KST · 체크아웃 = `main`(`5a71779510f8`).

## 0. 결론 한 줄

**`deploy.sh --target staging` 이 ④ 빌드 단계에서 red 로 중단했다(exit 70).**
`frontend` 이미지의 `npm run build`(= `tsc --noEmit && vite build`)가 타입 오류 4건으로 실패했다.
⟹ **마이그레이션 `0011` 은 돌지 않았다. staging 스키마·서빙 릴리스는 창 진입 전 그대로다.**
런북 §2 「배포가 red 면 창을 닫는다」에 따라 **단계 C 를 착수하지 않았다.**

## 1. 단계 B-0 — 릴리스 원장 승인 행 등재 (집행 완료)

`~/colab-v2-releases/release-ledger.tsv` 에 append 한 행(탭 구분 6칸 · 비밀 없음):

```
2026-09-02T22:31:13+0900	approve	b3c4085ccdf0	-	-	승인자=Ted 본것=사후 등재 2026-09-02 · 대화 승인으로 나간 배포 · 결정 〈59〉 수정분 · 원장 행 없이 배포된 것을 사후에 채운다
2026-09-02T22:31:13+0900	approve	14d3136aa55a	-	-	승인자=Ted 본것=사후 등재 2026-09-02 · 대화 승인으로 나간 배포 · 결정 〈60〉 주기 트리거 드레인 루프(Y-1) · 원장 행 없이 배포된 것을 사후에 채운다
2026-09-02T22:31:13+0900	approve	e935db203b07	-	-	승인자=Ted 본것=사후 등재 2026-09-02 · 대화 승인으로 나간 배포 · 결정 〈273〉 P7·A-1·CT-1 · 원장 행 없이 배포된 것을 사후에 채운다
2026-09-02T22:31:13+0900	approve	de15d4379525	-	-	승인자=Ted 본것=go/no-go ① GO · 짝 백업 20260902T222341 GREEN · WINDOW-20260902-A
2026-09-02T22:31:31+0900	approve	5a71779510f8	-	-	승인자=Ted 본것=go/no-go ① GO 의 실제 배포 커밋 5a71779510f8 = de15d4379525 + 회차 증거표 문서 1건(dev-package/sessions/WINDOW-20260902-A.md · 132줄 추가 · 코드 변경 0건 · git diff --stat 실측) · 짝 백업 20260902T222341 GREEN · WINDOW-20260902-A
```

- 앞 넷 = 지시받은 행. 다섯째 = **지시에 없던 추가분**(§5-㉮ 이탈 항목 참조).
- 등재 전 원장 20줄 → 등재 후 25줄. `LEDGER_KEEP=30` 이라 **잘려나간 행 0건**(실측).
- 사후 3행은 **시각을 소급하지 않았다** — 등재 시각은 실제 등재 시각이고, 사후임은 본문에 적었다.

## 2. 단계 B — 배포 (red)

| 축 | 값 |
|---|---|
| 명령 | `COLAB_STAGING_ENV=$HOME/.colab-v2-staging.env infra/staging/deploy.sh --target staging` (플래그 없음) |
| 릴리스 태그 | `5a71779510f8` (워킹트리 변경 0건 — `-dirty` 접미 없음) |
| 종료 코드 | **70** (`abort()` 경로) |
| 멈춘 단계 | **④ 이미지 빌드** |
| 통과한 단계 | ⓪ 타깃 확인 · ⓪-b 필수 설정 프리플라이트 · ② 호스트 게이트 `migration-single-head` · ③ `:prev` 태그 보존 |
| 도달하지 못한 단계 | ⑤ 배포 전 백업 · ⑦ 부트스트랩 · **⑧ 마이그레이션** · ⑨ 기동 · ⑩ 검증 · `:i2` 재부착 |

**`deploy.sh` 축자 종료 3줄**

```
2026-09-02T22:32:14 !!! 표식 파일 생성 — /home/<홈>/colab-v2-releases/DEPLOY-FAILED.txt
2026-09-02T22:32:14 !!! 배포 중단 — 단계 [빌드] · docker compose build 실패
2026-09-02T22:32:14 자동 롤백은 기본 off 다. 되돌리려면 사람이 rollback.sh 를 부른다(〈168〉-㉳).
```

**빌드 실패 축자 (`frontend` 스테이지 6/6 · `Dockerfile:11`)**

```
> colab-v2-frontend@0.1.0 build
> tsc --noEmit && vite build

test/e01-apply-points.test.ts(11,53): error TS2307: Cannot find module 'node:fs' or its corresponding type declarations.
test/e01-apply-points.test.ts(12,32): error TS2307: Cannot find module 'node:path' or its corresponding type declarations.
test/e01-apply-points.test.ts(15,19): error TS2304: Cannot find name '__dirname'.
test/e01-apply-points.test.ts(45,44): error TS7006: Parameter 'l' implicitly has an 'any' type.
target frontend: failed to solve: ... exit code: 2
```

**원인 (실측 · 추정 아님)**

| 축 | 실측 |
|---|---|
| 파일 | `frontend/test/e01-apply-points.test.ts` — 커밋 `74deb54`(2026-09-02 12:01:57 +0900) 가 추가 |
| 왜 빌드가 본다 | `frontend/tsconfig.json:20` `"include": ["src", "test", "vite.config.ts"]` — **`test` 가 포함**되어 `tsc --noEmit` 이 시험 파일까지 검사한다 |
| 왜 실패한다 | 같은 파일 `:8` `"types": ["vite/client", "vitest/globals", "@testing-library/jest-dom"]` — **`node` 가 없다.** `node:fs`·`node:path`·`__dirname` 은 Node 타입이 있어야 선다 |
| 언제부터 red 였나 | 서빙 릴리스 `e935db203b07` 배포 시각 = 2026-09-02 03:51. 원인 커밋 = 12:01. ⟹ **`main` 은 12:01 이후 계속 배포 불가 상태였고, 이 창이 그것을 처음 밟았다** |
| 게이트가 왜 안 잡았나 | `grep -rl 'tsc --noEmit' gates/` = **히트 0건.** 프런트엔드 타입 검사를 도는 게이트가 없다. 배포 파이프라인의 빌드가 유일한 오라클이다 |

⚠ **우회하지 않았다.** `--allow-dirty`·`--skip-backup`·`--skip-alias-reattach` 미사용 · 코드 무수정 ·
컨테이너 무접촉 · DB 무접촉. 스크립트의 거부가 올바른 동작이다.

## 3. 배포 전/후 대조 — 상태가 바뀌지 않았음의 실측

| 축 | 배포 전(22:31) | 배포 후(22:35) | 판정 |
|---|---|---|---|
| 서빙 릴리스 | `e935db203b07` (앱 5종 태그 동일) | **`e935db203b07`** (동일) | 불변 |
| 컨테이너 | 8개 healthy | **8개 healthy** | 불변 |
| 체인 head (platform) | `0010_p6_access_request` | **`0010_p6_access_request`** | 불변 — `0011` **미적용** |
| 체인 head (ai) | `0005_k2b_concept_graph_seed` | 동일 | 불변 |
| 체인 판정 | `GREEN (통과 2건 · SKIP 0)` | 동일 | 불변 |
| `d3_dataset.processing_level_user_set` | 실재 | **실재** (`information_schema` 읽기 전용 조회) | **떨어지지 않았다** |
| `main` 워킹트리 | 변경 0건 | **변경 0건** | 불변 |

- `deploy.sh` 가 레포에 쓴 것 = **0건**(워킹트리 변경 0건 실측).
- `deploy.sh` 가 레포 **밖**에 쓴 것 = ⑴ 원장 red 행 1줄 ⑵ `~/colab-v2-releases/DEPLOY-FAILED.txt` 표식 파일
  ⑶ `colab-v2/<앱>:prev` 태그 5종(③ 이 빌드 전에 붙인 것 — 현행 `:i2` 와 같은 이미지를 가리킨다).

**원장 마지막 줄 (배포 결과)**

```
2026-09-02T22:32:14+0900	deploy	5a71779510f8	5a71779510f8	red	빌드 — docker compose build 실패
```

## 4. 단계 C — **미착수**

- 근거 = 런북 §2 「오염 범위: 이후 **전 단계**. 배포가 red 면 **창을 닫는다**」.
- `crontab -l` 실행 줄은 **2건 그대로**(창 진입 시점과 동일 · `WINDOW-20260902-A §1`).
- `install-schedule.sh install` **미실행** · `check-cron-streak.sh` **미실행**.
- ⟹ 크론 셋째 줄은 다음 창의 산출로 남는다. 단계 C 자체는 배포와 독립이지만,
  런북이 red 배포에서 창을 닫으라고 정했으므로 **절차를 앞질러 집행하지 않았다.**

## 5. 런북·지시와 어긋난 것

| # | 항목 | 내용 |
|---|---|---|
| ㉮ | **승인 행 1건 추가** | 지시는 `de15d43` 승인 행이었으나 **실제 배포 커밋은 `5a71779510f8`**(A 회차 증거표 커밋이 그 위에 있다). `deploy.sh:69` 가 `HEAD` 로 태그를 짓고 `approve.sh` 도 `HEAD` 로만 적으므로, 「승인 행이 배포 행보다 먼저」 규약을 실제 배포 커밋에 대해 세우려면 그 sha 의 행이 필요했다. 두 커밋의 차이는 `git diff --stat` 실측으로 **문서 1파일 132줄 추가 · 코드 변경 0건**이며, 그 사실을 행 본문에 적었다. **새 승인이 아니라 같은 GO 의 대상 특정이다.** |
| ㉯ | 배포 후 게이트 `./gates/run.sh all -j 1` | **미실행.** 지시의 전제(배포 성공)가 서지 않았다. 배포가 상태를 바꾸지 않았으므로 창 진입 전 값이 그대로 유효하다 |
| ㉰ | `0.1-ⓒ` env 미설정 | A 회차가 지목한 대로 `COLAB_STAGING_ENV` 를 export 하고 호출 — `deploy.sh:45` die 없이 통과 |
| ㉱ | `0.1-ⓓ` 다른 레인 0개 | `docker ps` 에 staging 스택 밖 컨테이너 `a2_pg`·`ai_pg` 2개가 **4일째 가동 중**. staging 스택이 아니고 이 회차가 건드리지 않았다(정지 금지). 기록만 — 정체·소유 레인 **`[미확인]`** |
| ㉲ | — | `viz-render` 만 가동 29분(A 회차의 19분 관측과 연속). 태그는 `e935db203b07` 동일 |

## 6. Go/No-go ③ (단계 D 복원 착수 직전) — 현재 상태

⛔ **선행 조건 첫째가 무너졌으므로 ③ 을 열 수 없다.**

| # | 런북이 요구하는 것 | 현재 상태 |
|---|---|---|
| 0 | **단계 B GREEN** (§4 선행 조건) | ❌ **red** — 배포 미완. D 는 「배포된 릴리스 기준」의 복원이어야 한다(§9-2-ⓒ) |
| 1 | 단계 A 의 **짝 회차** 존재 | ✅ `platform-20260902T222341` ＋ `vol-uploads`·`vol-previews` 의 `.pair` 가 그 덤프를 가리킨다(`WINDOW-20260902-A §2`) |
| 2 | `preflight.sh` P1~P9 통과 | **미실행** — B 가 red 라 돌리지 않았다 |
| 3 | `COLAB_RESTORE_CAUSE` 실값 | **미기재** (비면 P9 RED 가 올바른 동작) |
| 4 | `COLAB_RESTORE_PRE_BACKUP` 지목 | **미지목** |
| 5 | `/tmp/pre-digests.tsv` 를 다른 창에 보관 | **미생성** |
| 6 | 무해 리허설 `restore/rehearsal.sh` 선행 | **미실행** — 살아 있는 staging 을 읽기만 하므로 배포 red 와 무관하게 돌릴 수 있다. **다음 창의 착수 후보** |

## 7. 다음 창의 진입조건 (판정 대기 — 이 회차가 결정하지 않는다)

1. **`frontend` 타입 오류 4건의 처리 방침** — ⑴ `tsconfig` 의 `types` 에 `node` 를 넣고 `@types/node` 를 의존에 세우거나
   ⑵ 빌드용 `tsconfig` 에서 `test` 를 제외하거나 ⑶ 시험 파일을 고치거나. **어느 것도 이 회차가 고르지 않았다** —
   ⑵ 는 검사 범위를 줄이는 쪽이라 게이트 규율(§3 「범위 축소 금지」)과 부딪친다.
2. **프런트엔드 타입 검사 게이트 신설 여부** — 지금은 배포 빌드가 유일한 오라클이라, 깨진 채로 반나절이 지났다.
3. `~/colab-v2-releases/DEPLOY-FAILED.txt` 표식의 해소 — 다음 배포 착수 전에 `deploy.sh` 가 이 표식을 어떻게 다루는지 확인이 필요하다. **`[미확인]`**
4. 위 셋이 정리되면 **B 를 처음부터 다시 돈다** — `0011` 은 아직 안 돌았고, 단계 A 의 짝 백업은 그대로 유효하다.
