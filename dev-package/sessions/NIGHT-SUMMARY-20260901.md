# 야간 회차 종합 — 병합 뒤 `main` 실측 (2026-09-01 · 오케스트레이터)

> **무엇인가** — 이 야간에 돌아간 레인들의 산출을 `main` 에 병합한 뒤, **병합 커밋 기준으로 다시 잰 값**만 모은 회차 기록이다.
> **값과 근거의 정본은 `PLAN-SoT §9` 와 각 레인 회차 파일이다** — 이 문서는 그 위에서 「무엇이 닫혔고 무엇이 남았는가」만 세운다.
> **이 회차의 접촉 범위** — 배포 **0건** · DB 쓰기 **0건** · push **0건** · 게이트 우회·비활성화·검사 축소 **0건** · 산출물 삭제 **0건**.
> 병합 = `lane-p6` → `main`. 충돌은 `dev-package/PLAN-SoT.md` 한 곳(`§9` 표 꼬리)뿐이고 3-way 대조 후 **양쪽 전부 보존 · 삭제 0건**.

---

## 1. 대장 실측 — 닫힌 것 · 진전된 것 · 못 닫은 것

### 1-1. 이번 야간 회차가 대장에서 **닫은 항목 = 0건**

- `done` 으로 넘어간 항목이 이 회차에 없다. **부분 완료로 닫지 않는다**는 규칙대로다.
- 이미 `done` 인 것(직전 회차들의 성과) = `F-2` · `F-1` · `P3` · `PV-1` · `P5` · `D5`.

### 1-2. 진전 — 「기능적으로 무엇이 달라졌는가」를 함께 적는다

| 항목 | 이 회차의 진전 | 기능으로 말하면 |
|---|---|---|
| `P6` (승인 처리) | 요청 저장처 신설(마이그레이션 `0010_p6_access_request` · 표 2) ＋ 여덟 op 개통 ＋ 화면 3자리(`〈265〉`·`〈266〉`) | **데이터셋 접근을 신청하고, 관리자가 그것을 승인·거절하는 길이 제품 안에 생겼다.** 종전에는 신청을 담아 둘 자리가 없었다 |
| `S3` (실데이터 E2E) | 5종 실파일 계보 확정 시험 6 ＋ 배치 정본 회귀 시험 5(`〈267〉`) | **실제 위성 파일 다섯 종류가 업로드부터 계보 확정까지 한 번에 통과하는 것을 기계가 매번 확인한다.** 종전 시험은 합성 바이트였다 |
| `A-1` (산출물 소유) | 대장에 항목으로 등재(`〈263〉`) ＋ 배선 절단 지점을 `#56` 으로 상신 | **미리보기 산출물의 주인이 누구인지가 처음으로 추적 대상이 됐다** |
| 문서 정합 | 정본↔대장 어긋남 9묶음(`D-1`~`D-9`) 전건 해소(`〈268〉`) ＋ `CLAUDE.md` 를 게이트 대조 대상에 편입 | **읽는 사람이 낡은 문서를 옳다고 믿는 자리 아홉이 사라졌다** |
| 게이트 | 원장 대조 게이트에 롤 판정 추가(`〈262〉`) | **권한이 모자란 접속이 낸 「0행」을 「데이터 없음」으로 읽고 통과하는 자리가 막혔다** |
| `PLAN-SoT §9` | 수록 결정 **218개** · `〈51〉`–`〈268〉` · 중복 0 · 빠진 번호 0 · 오름차순 | 결정 이력이 병합 뒤에도 끊기지 않는다 |

### 1-3. 못 닫은 것 — **남은 조건 한 줄**을 반드시 붙인다

| 항목 | 상태 | 남은 조건 |
|---|---|---|
| `P6` | open | **staging 배포 green** 한 건 |
| `S3` | open | **5종 각각이 공개 엣지 화면에 도달하는 것**의 포맷별 실측(현행 엣지 실측 13건의 포맷 구성이 어디에도 없다 — `[미확인]`) |
| `Y-1` | partial | **staging 배포 green**(완료 정의 ⓕ · ⓐ~ⓔ 는 `〈253〉` 으로 섰다) |
| `A-1` | open | **`#56` 판정** — 등록 전환 때 미리보기 산출물의 자리를 누가 옮기는가 |
| `CT-1` | open | 정본 다섯 조항 중 **다운로드 한 칸**의 판정(`〈244〉`) |
| `R-1` | partial | 복원된 스택에서 잰 회차 0건 · 회차마다 다시 재는 기구가 게이트 밖 · 손검사 2건(`〈255〉`) |
| `I3` | partial | 자동 트리거 완주(크론 미설치) · 롤백 왕복 미검증 — ＋ `#43` |
| `V-1`·`V-2`·`J-1` | open | **`#54`** — 완료 조건이 「초안」이라 착수 자격이 없다 |

⭑ **⟨정리 2026-09-01 · 이 회차⟩ `NumPy` 헤더 파싱 결함**(`§2` ⑧)**을 블로커 `#58` 로 열었다.** 소유가 갈려 있던 것 — `sessions/S3-E2E-20260901.md §4-b` 는 `D5`(`partial`)로 적었으나 **대장 `work-items.yaml` 의 `D5` 는 `done`**(2026-08-30 재실측 · `sessions/D5-CLOSE-MEASURE-20260830.md`)이다. **대장 값을 쓴다** — 이 결함은 「닫힌 `D5` 를 되돌리는 것」이 아니라 **닫힌 항목 위의 신규 결함**으로 읽고, `03-HANDOFF §4 #58`·`PR-CHECKLIST §2` 에 행으로 세웠다. `〈267〉` 에도 그 취지의 오케스트레이터 주를 덧붙였다(초안 문면은 지우지 않았다). **`S3` 를 막지 않는다** — 화면 경로는 `D7` 이다.

---

## 2. 이번 회차의 실질 소득 — **「조용히 틀리던 것」 아홉**

> 공통 무늬 = **에러 없이 그럴듯한 값**. 아홉 중 여덟은 예외를 던지지 않았고 사람 눈에도 통과처럼 보였다(`DATA-REFERENCE §0` 머리말).

| # | 무엇이 틀리고 있었나 | 무엇이 기능적으로 달라졌나 | 근거 |
|---|---|---|---|
| ⑴ | **복원 사전점검의 무결성 검사가 한 번도 실행되지 않았다** — `"${!ART[@]:-}"` 표기 때문에 bash 가 변수명 오류를 내고 루프가 0회 돌았다. `fail` 이 안 불리므로 **손상된 백업 산출물이 그대로 통과**했다 | 백업이 깨져 있으면 복원 **전에** 걸린다 | `PLAN-SoT §9 〈249〉`-㉰ · `infra/staging/restore/preflight.sh` P4 · 셀프테스트 `selftest-restore.sh` **SR19**·**SR19-b**(fixture 23 → 25 GREEN) |
| ⑵ | **게이트가 빈 데이터베이스를 정답지로 보고 있었다** — `autometa-loss` 가 `schema-diff` 와 공유하는 **스키마 전용 일회용 DB** 를 대조했고, 그 DB 의 접수분은 **구조적으로 영원히 0건**이었다 | 「실제로 접수한 것 중 메타가 빠진 것이 있는가」를 **실물 staging DB** 에 묻는다. 읽기 전용은 매 회차 **쓰기 탐침**으로 증명한다 | `〈237〉` · `03-HANDOFF §4 #50` · `sessions/PV-1-AUTOMETA-ORACLE-20260831.md §1`~`§3` |
| ⑶ | **스위치(설정 플래그)가 배포에 실리지 않았다** — 코드는 `COLAB_VIZ_TILE_BRANCH` 를 읽는데 `compose.i2.yml` 의 `viz-render` 에 그 키가 없어 컨테이너 환경에 **0건**이었다. `--env-file` 은 compose 치환에만 쓰이고 컨테이너로 흐르지 않는다 | 문서에 적힌 「홈 env 한 줄」 절차로 **타일 갈래가 실제로 켜진다** | `〈241〉` · `sessions/P3-AB-DEPLOY-20260831.md §4` · 시험 `services/viz-render/tests/test_tile_branch_deployment_declaration.py` **3건**(착수 시 3 red) |
| ⑷ | **설정을 고쳐도 컨테이너가 교체되지 않았다** — `nginx.i2.conf` 는 바인드 마운트인데 배포가 `up -d` 뿐이라 **컨테이너 ID 가 그대로**였다(일회용 프로젝트로 재현) | 설정 해시를 환경에 실어 **바뀐 회차에만 재생성**하고, `⑩-b` 가 도는 컨테이너 안의 설정을 레포 판과 **바이트로 대조**해 다르면 배포가 스스로 멈춘다 | `〈240〉` · `sessions/P3-TILE-AB-20260831.md §4` · `sessions/P3-EDGE-TILE-20260831.md §3` · 실측 `sessions/P3-AB-DEPLOY-20260831.md §3 ⑩-b`(해시 `56cc63f04dad443a`) |
| ⑸ | **계보 제안의 실패 기록이 0줄이었다** — `relay.py` 의 `honest_empty_suggestions` 네 갈래(미연결·못 닿음·비200·범위 불일치) 중 **어느 것도 로그를 남기지 않아** 「지금 몇 건 거부당했나」를 셀 수 없었다 | 운영자가 **거부(우리 쪽 고장)** 와 **못 닿음(저쪽 사정)** 을 이름으로 갈라 센다. 정직한 빈 상태에는 기록하지 않는다 | `〈246〉`-㉯·㉰ · 로거 `colab_core.suggest` · `LINEAGE_SUGGEST_REJECTED`/`_UNAVAILABLE` · 시험 `services/core-api/tests/test_suggestion_failure_record.py` **6건**(3 red 선확인) |
| ⑹ | **없는 화면으로 사용자를 안내하고 있었다** — 카탈로그가 「홈의 AI 검색이 빨라요」로 링크를 걸었는데 그 화면(S-01·S-06)은 **stage 3 으로 빠져 이번 릴리스에 없다** | 문구가 「표 헤더의 열 이름을 눌러 조건을 걸어 보세요」로 바뀌고 **링크 0건**. 정본 `Policy_데이터_찾기` v1.8 → v1.9 | `〈245〉` · `sessions/CT1-F2-CANON-CHECK-20260831.md §1` · 시험 `frontend` `test/catalog.test.tsx` **2건 신설**(구현 전 red 2) |
| ⑺ | **경계 롤로 조회해 나온 0행을 「데이터 없음」으로 읽었다** — 산출물 소유 설계 회차가 staging 원장을 「전 표 0행」으로 적고 그 위에 「산출물 80건 회수」를 권고했다. 실물은 `d3_dataset` **13** · `d3_file` **130** | 그 권고를 **집행하지 않았다**. 원장 대조 게이트가 **자기 롤을 먼저 찍고**, 관리자 롤이 아니면 계수를 내지 않고 red 를 낸다 | `〈261〉`(정정) · `〈262〉`(게이트 · Ted RULING ㉟) · `DATA-REFERENCE §0 M-9` · `sessions/GATE-ROLE-CHECK-20260901.md` · `sessions/ARTIFACT-OWNER-DESIGN-20260831.md §2.1` |
| ⑻ | **`NumPy` 가 「지원 목록 밖」이라는 거짓 메시지를 냈다** — `detect_format` 은 `NumPy` 로 바르게 판정하는데 다음 단계가 `파싱 실패(NumPy): 지원 목록 밖: NumPy` 로 떨어진다. 실제로는 `SUPPORTED_FORMATS`·`RENDERABLE_FORMATS` **둘 다에 들어 있고** 처리 분기만 없다(**선언 여섯 · 처리 넷**) | 읽는 사람이 이것을 **정책으로 착각하지 않는다** — 구현 결함으로 상신됐다. 화면 경로는 `viz-render` 라 `S3` 를 막지 않는다 | `〈267〉` · `sessions/S3-E2E-20260901.md §4-b` · `d5/formats.py`·`d5/renderable.py`·`d5/parse.py`·`d5/pipeline.py` |
| ⑼ | **E2E 가 잘못된 파일 이름으로 돌고 있었다** — 배치 정본은 본체를 **확장자 없는 ULID**(`{uploadsPrefix}/{targetId}/{fileId}`)로 놓는데 시험은 `HLS.S30….tif`·`gk2a_….nc` 로 놓고 있었다 | 포맷 판정이 확장자에 조금이라도 기대면 **E2E 전건 초록인데 실업로드는 한 장도 안 그려지는** 사고가 난다. 측정 결과 **결함 0건**(전량 매직바이트 판정)이었고, 그 사실을 지키는 회귀 시험을 세웠다 | `〈267〉` · `sessions/S3-E2E-20260901.md §2` · `contracts/storage/layout.json` `keys.본체` · 시험 `services/viz-render/tests/test_e2e_storage_layout_real.py` **5건**(변이 시 5 red) |

**근거를 못 찾은 항목 = 0건.**

---

## 3. Ted 판정 대기 — **전량 14건 · 급한 순**

> 기준 = `dev-package/PR-CHECKLIST.md §2`·`§3` ＋ `dev-package/03-HANDOFF.md §4`.
> **행 수 = 14. 문면의 건수와 아래 표의 행 수가 같다.**
> ⭑ **⟨정정 2026-09-01 · 이 회차⟩ `PR-CHECKLIST §2` 가 낡아 있던 것을 고쳤다.** 그 표는 열린 블로커를 **11건**으로 세면서 이미 해소된 `#55`(`03-HANDOFF §4` ✅ · `〈253〉`)를 열린 채로 두고 있었다. 고친 뒤 = **열린 것 11건**(`#55` 를 닫고 `#58` 을 새로 열었다 · 표는 12행이고 그중 1행은 닫힌 이력) · 그중 **Ted 자리 7건**. 나머지 7건은 `PR-CHECKLIST §3`(`03-HANDOFF §4.5` 진입조건)에서 온다.

| 순 | 배경 한 줄 | 현재 상태 | 선택지의 뜻 | 고르면 달라지는 것 | 권고 ＋ 되돌리는 비용 |
|---|---|---|---|---|---|
| 1 | **`#57`** 미리보기 타일 자리 검사기가 접속 롤을 확인하지 않고 발행 건수를 센다 | 경계 롤이면 발행 0 → 핵심 판정이 통째로 건너뛰어지고 green 이 난다(green-by-skip) | ⓐ 롤 판정을 붙인다 ⓑ 배선을 먼저 실물 DB 로 바꾼다 ⓒ 둘을 묶는다 | ⓐ 는 즉시 거짓 green 을 없앤다 · ⓑ 는 검사 대상이 실제로 생긴다 · ⓒ 는 회차가 하나 더 든다 | **권고 = ⓒ** — `〈262〉` 가 원장 게이트에 이미 같은 판정을 붙였으므로 형제 자리를 함께 닫는다. 되돌리기 = 게이트 파일 1본 revert(제품 무영향) |
| 2 | **`#56`** 등록 전환 때 미리보기 산출물의 자리를 누가 옮기는가 | 접수분 바이트는 `uploadId`→`datasetId` 로 옮겨지는데 굽는 시점 대상은 `uploadId` 이고 그리는 쪽은 `datasetId` 를 본다 · 재생성 트리거 없음 | ⓐ core-api 에 볼륨을 붙인다(경계를 넘는다) ⓑ 등록 사건을 D5·D7 이 받는다(**동결 해제 한 번 더**) ⓒ 재생성 트리거를 신설한다 | 정하지 않고 배선하면 **에러 없이 빈 지도**가 난다 · `A-1` 의 배선 절단이 이 판정 뒤에 열린다 | **권고 = ⓑ** — 이벤트 계약은 `〈253〉` 에서 이미 한 번 확장돼 경로가 서 있다. 되돌리기 = 계약 개정이라 **비싸다**(동결 해제 절차 재수행) |
| 3 | **`#39`** 대장↔산문 갈라짐을 CI 가 잡지 않는다 | `work-item-consistency` 가 CI 에 배선돼 있지 않고 `work-item-selftest` 만 돈다 · 지금 그대로 배선하면 CI 가 항상 실패한다 | ⓐ 배선하고 설계상 red 를 실패로 둔다 ⓑ 배선하되 경고로 둔다 ⓒ 배선하지 않는다 | 이번 회차가 고친 어긋남 9묶음이 **다시 쌓이는가**가 여기서 갈린다 | **권고 = ⓐ** — 어긋남 9묶음이 CI 부재 상태에서 쌓였다. 되돌리기 = 워크플로 1본 revert(싸다) |
| 4 | **`#54`** `V-1`·`V-2`·`J-1` 의 완료 조건이 「초안」이다 | 대장 `completion_def` 는 셋 다 **「완료 정의 미작성」** 이고 초안은 다른 필드에 있다 | ⓐ 초안을 그대로 확정 ⓑ 수정해 확정 ⓒ 항목을 stage 3 으로 미룬다 | 정하지 않으면 **세 항목 모두 착수 자격이 없다** | **권고 = ⓑ** — `J-1` 은 값이 빈 5자리가 있어 통째 확정이 안 된다(`PR-CHECKLIST §6`). 되돌리기 = 대장 필드 편집(싸다) |
| 5 | Verified 우선 정렬을 언제 투입하는가 | 실행 경로에 `verified` 0건이라 `P6` 확정 전에는 판정 자체가 불가 | ⓐ `P6` staging 배포 뒤 투입 ⓑ 이번 릴리스에서 뺀다 | `P4`(검색·정렬 화면)가 열린다 | **권고 = ⓐ** — `P6` 잔여가 배포 green 하나라 곧 조건이 선다. 되돌리기 = 정렬 규칙 1자리(싸다) |
| 6 | 「전 종 green」 합격선을 어디서 재는가 ＋ 자동 트리거 점화 시점 | `I3` `partial` · 크론 미설치 | ⓐ 배포 경로 안에서 잰다 ⓑ 게이트로 뺀다 | `I3` 가 닫히는 자리가 정해진다 · `〈255〉` 가 적은 약점(배포를 안 돌리는 회차는 이 행을 안 잰다)이 여기서 풀린다 | **권고 = ⓑ** — 배포 경로 안에만 두면 침묵 구간이 남는다. 되돌리기 = 게이트 1본(싸다) |
| 7 | **`#58`** `NumPy` 헤더 파싱이 멈추는데 실패 메시지가 「지원 목록 밖」이라고 말한다 | `SUPPORTED_FORMATS`·`RENDERABLE_FORMATS` **둘 다에 `NumPy` 가 있다** — 선언 여섯 · 처리 넷. 메시지가 「구현 없음」을 「원래 대상 아님」으로 바꿔 읽게 만든다 | ⓐ 처리를 넷 → 여섯으로 채운다 ⓑ 정본 지원 목록을 넷으로 줄인다 ⓒ 메시지만 정직하게 고치고 판정을 뒤로 미룬다 | ⓐ 면 `S3` 의 포맷 정본(`gates/config/e2e-format-coverage.toml`)이 그대로 서고, ⓑ 면 `S3` 완료 정의의 「5종」이 흔들려 정본 개정이 따라온다 | **권고 = ⓒ 를 즉시 ＋ ⓐ 를 정식 판정으로.** 거짓말하는 메시지는 판정을 기다릴 이유가 없다. 되돌리는 비용 — ⓒ 는 메시지 문자열 1곳, ⓐ 는 파서 구현 1회차, ⓑ 는 정본·대장 각 1행 |
| 8 | **`#42`** 문서만 바꾼 커밋의 CI success 는 빈 성공이다 | 경로 필터가 `dev-package/**` 를 어느 잡에도 걸지 않아 잡 5개가 전부 skipped 인데 success 가 찍힌다 | ⓐ 문서 계열을 경로 필터에 건다 ⓑ 걸지 않기로 문면에 못 박는다 | CI green 을 근거로 인용할 수 있는가가 갈린다 | **권고 = ⓐ** — 이번 회차 산출 상당수가 문서다. 되돌리기 = 워크플로 1본(싸다) |
| 9 | 확대·타일을 `P3` 범위에 넣는가 | 정본에 규정 **0건** · `PR-CHECKLIST §1 D-8` 과 같은 뿌리 | ⓐ 범위에 넣는다 ⓑ 후속 항목으로 뺀다 | `P3` 는 이미 `done` 이라 **닫은 근거의 문면**이 바뀐다 | **권고 = ⓑ** — `〈240〉` 이 기본을 「한 장」으로 못 박았고 타일은 명시로 켤 때만이다. 되돌리기 = 정본 개정 이력 1행(싸다) |
| 10 | 기존 12개 데이터셋을 미리보기 뒷단에 소급 반영하는가 | 면제 선언이 **비어 있다** | ⓐ 소급한다 ⓑ 면제를 명시 선언한다 | 비워 두면 세 상태 규칙상 **아무 말도 없는 자리**로 남는다 | **권고 = ⓑ** — 건수를 드러낸 채 면제하는 것이 규칙이다. 되돌리기 = 선언 1행(싸다) |
| 11 | 자연어 검색 1세대 평가셋의 **기대값 개정 6건** 을 적용하는가 | `K4` 는 stage 3 · 적용하면 미충족 8 → 2 | ⓐ 적용 ⓑ 미적용 | `K4` 의 합격 계수가 갈린다 | **권고 = ⓐ** — 근거는 `〈220〉`-㉱ 에 실측으로 있다. 되돌리기 = 평가셋 파일 revert(싸다) |
| 12 | 검색 평가셋의 **결합 규칙**·유사도 임계·설명 본문 갱신 | `〈89〉` 미판정 | ⓐ AND ⓑ OR ⓒ 가중 | 평가셋의 정답 자체가 바뀐다 | **권고 = 10번과 한 회차에 묶어 판정** — 따로 정하면 평가셋을 두 번 다시 짠다. 되돌리기 = 평가셋 재계산 1회 |
| 13 | 제안을 한 번도 만들지 않은 채 「제안 서비스」를 닫는 것이 판정의 뜻인가 | `K3` 는 stage 3 · 수치 합격선 없음(`〈211〉`) | ⓐ 뜻이 맞다(문면대로 닫는다) ⓑ 아니다(합격선을 새로 세운다) | `K3` 완료 정의가 확정된다 | **권고 = ⓐ** — ⑸ 로 실패 기록 자리가 서서 운영 관측은 이미 가능하다. 되돌리기 = 대장 필드(싸다) |
| 14 | **`#44`** staging 실물의 `403` 을 잴 **시험 데이터**가 없다 | 결함이 아니라 데이터 부재 — 거부를 유발할 주체가 staging 에 없다 | ⓐ 스위치가 꺼진 `연구원` 토큰 1건을 주체 등기부에 등재한다 ⓑ 등재하지 않고 `[미확인]` 로 둔다 | `X-5` 의 마지막 `[미확인]` 1건이 닫힌다 | **권고 = ⓐ** — 다만 **자격증명 파일 쓰기라 사람 자리**다. 되돌리기 = 등기부에서 1행 제거(싸다) |

---

## 4. 다음 착수 순서 — 차단 유형별로 가른 뒤 번호로 준다

### ⓐ 지금 착수 가능 (판정도 선행도 필요 없다)

| 순 | 착수할 것 | 선행 | 왜 이 순서인가 |
|---|---|---|---|
| **1** | `P6` staging 배포 green | 배포 승인(Ted) | **한 번의 배포가 `P6` 와 `Y-1` 의 잔여를 동시에 닫는다** — 둘 다 남은 조건이 「staging 배포 green」 하나다 |
| **2** | `S3` 포맷별 공개 엣지 실측 | 1(같은 배포 위에서 잰다) | 배포가 서면 5종 왕복은 읽기만으로 끝난다. 따로 배포하지 않는다 |
| **3** | `#40` 실행 비트 52개 일괄 정정 | 없음 | 판정이 필요 없고 리눅스 실행 경로 전반을 막는다. 로컬(DrvFs)에서는 안 드러나므로 **미루면 CI 에서 터진다** |
| **4** | `#22` D2·D8 고아 행 삭제 실행 | 없음(판정 완료) | ⚠ **삭제 대상을 조건문이 아니라 id 목록으로 고정한 뒤** 실행한다 |
| **5** | `#41` CI 실패 3종의 **원인 실측** | 없음 | 추정으로 고치지 않는다 — 재는 것이 먼저다. `#39` 배선의 전제가 된다 |

### ⓑ 판정 대기 (Ted 가 정해야 열린다)

| 순 | 열리는 것 | 기다리는 판정 |
|---|---|---|
| **6** | `A-1` 배선 | `#56` |
| **7** | 게이트 신뢰도 회복 | `#57` |
| **8** | `V-1`·`V-2`·`J-1` 착수 | `#54` |
| **9** | CI 가 드리프트를 잡는 상태 | `#39` ＋ 5번 실측 |
| **10** | `CT-1` 다운로드 한 칸 | `〈244〉` 잔여 판정 |

### ⓒ 선행 대기

- `P4`(검색·정렬 화면) — 선행 = `P6` 확정(1번) ＋ 판정 5(Verified 정렬).
- `PA-G` — 선행 = **`stage1`·`stage2` 항목 중 미완 0건**(셈법 정본 `〈258〉`-㉮ · `partial` 도 미완 · `deferred` 제외 · `PA-G` 자기 제외 · `after_stage2`·`out_of_scope` 불산입). **현재 미완 = 17건**(2026-09-01 대장 실측) — `stage1` **3**(`SC-5`·`LV-1`·`LV-2`) ＋ `stage2` **14**(`I3`·`I4`·`IS4`·`R-1`·`V-1`·`V-2`·`J-1`·`Y-1`·`CT-1`·`P6`·`P7`·`P8`·`S3`·`A-1`). ⚠ **2026-08-31 실측 16 에서 1 늘었다 — 정의 차가 아니라 항목 신설이다**(`A-1` · `〈263〉`). 셈법은 그대로다.
- `NumPy` 헤더 파싱 결함(`#58`) — 소유는 **닫힌 `D5` 위의 신규 결함**으로 정리했다(`§1-3`). 선행 = **범위 판정**(처리를 넷 → 여섯으로 채울지, 정본 지원 목록을 넷으로 줄일지 · `§3` 7번). 메시지 정정은 판정 없이 지금 할 수 있다.

### ⓓ 자격 필요 (사람이 해야 한다)

- `#44` 시험 토큰 등재 — **자격증명 파일 쓰기**.
- `#43` 스크립트 롤백 경로 — green `deploy` 행이 하나뿐이라 롤백 대상 선택 규칙을 `I3` 레인이 세워야 한다.
- staging 배포 승인 자체(`approve.sh Ted`).

---

## 5. 게이트·시험 최종 계수 — 병합 뒤 `main` 실측

| | 값 |
|---|---|
| `bash gates/run.sh all` | **green 35 / red(판정) 0 / red(준비) 0** |
| `schema-diff` | **green** — `lane-p6` 병합으로 `0010_p6_access_request` 가 `main` 에 들어와 **선언↔적용 일치**(`S3` 회차가 적은 red 는 병합으로 해소됐다) |

| 단위 | 값 | 명령 |
|---|---|---|
| core-api | **516 passed** | `.venv/bin/python -m pytest` — **2회 실행 · 두 번 다 516 passed**(58.9초 · 57.6초). 공유 시험 DB 경합으로 산발 실패가 이 세션에 두 번 관측돼 단독 재실행으로 확인했고, **이번 회차에서는 재현되지 않았다** |
| frontend | **399 passed** (20 파일) | `npm test` |
| viz-render | **199 passed** | `.venv/bin/python -m pytest` |
| pipeline-worker | **222 passed** | `.venv/bin/python -m pytest` |
| ai-service | **145 passed** | `.venv/bin/python -m pytest` |
| 타입 | **exit 0** | `npx tsc --noEmit` |

**core-api 516 의 셈 근거** — 직전 `main` **511** ＋ `S3` 계보 E2E **6**(`services/core-api/tests/test_e2e_s3_real.py`) ＝ 517 → `P6` 이 501 표를 16 → 8 로 줄여 파라미터화 시험 32 → 16(**−16**) ＋ 새 승인 시험 **15**(`services/core-api/tests/test_approval.py`) ＝ **516**.

⚠ **착수 지시문의 예측치 510 은 틀렸다** — `S3` 의 ＋6 을 빼먹은 값이다. **실측 516 이 옳다.**

---

## 6. 이번에 세지 않은 판단기준 (다음 회차의 진입조건)

- **`S3` 5종의 공개 엣지 도달** `[미확인]` — staging 배포 뒤 왕복하면 풀린다.
- **`NumPy`·`GRIB` 의 D5 헤더 파싱** `[미확인]` — `GRIB` 은 `NOT_RENDERABLE_FORMATS` 라 정책일 가능성이 있어 `NumPy` 와 한 덩어리로 접지 않는다.
- **`NumPy` 를 정본 지원 목록에 남길지** `[미확인]` — 블로커 `#58` 의 범위 판정으로 풀린다(소유는 정리됐다 · `§1-3`).
- **`R-1` 잔여 셋** `[미확인]` — 복원된 스택에서 재야 풀린다.
