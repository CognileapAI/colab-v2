# staging 단독 창 5b — 단계 A(짝 백업) 재집행 ＋ go/no-go ① 증거 (2026-09-05 10:46~11:00 KST)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md §1`·`§10-①`. 이 파일은 **이 회차에 실제로 잰 값**만 적는다.
> 집행 범위 = **A 만**. B(배포) 미착수 — go/no-go ① 은 Ted 판정 자리이고 이 회차는 증거만 세운다.
> 선행 회차 = `WINDOW-20260905-A5.md`(판정 NO-GO). 이 파일은 그 시트를 **대체하지 않고 갱신**한다 — A5 의 §6(배포 후 순서)·§7(승인 행)은 **그대로 유효**하고 §2(검사기)·§0(진입 전 상태)만 이 회차 값으로 다시 잰다.
> ⛔ **결론을 앞에 적는다 — 이 회차의 판정은 `GO` 다**(§8). A5 를 NO-GO 로 만든 단 하나의 red 가 `〈317〉` 로 수정됐고, 이 회차에서 **단독 3회 연속 green** 으로 실측됐다.

---

## 0. 창 진입 전 상태 (실측 · 읽기 전용 · 2026-09-05 10:46~11:00 KST)

| 축 | 실측값 | 재는 방법 |
|---|---|---|
| 커밋 | **`e37808c`**(`e37808cace69446b6d7db9821a39669ae3cf880d`) · 브랜치 `main` · `origin/main` 과 동일 · 「단독 창 5 NO-GO 증거표 …」 | `git log -1` · `git rev-parse` |
| ⛔ 지시문과의 차이 | 지시문은 `main @ 1eb247f` 로 적혀 있었으나 **실측 HEAD 는 `e37808c`** 다. `1eb247f` 는 그 부모이고, `e37808c` 는 **`WINDOW-20260905-A5.md` 315줄 한 파일만 더한 문서 커밋**(`git show --stat e37808c`). 코드 델타 0. **배포 후보는 `e37808c` 다.** | `git show --stat e37808c` |
| 배포 후보 릴리스 태그 | **`e37808cace69`** (12자리 규약 — `f465d83`→`f465d83a2ef1` 과 같은 표) | `git rev-parse HEAD \| cut -c1-12` |
| 워킹트리 | **변경 0건** (백업·검사기 8회 실행 후 재확인도 0건) | `git status --porcelain` = 0줄 |
| 워크트리 | **2개** — `main` ＋ `.claude/worktrees/review-pr1`(detached `0e294e5` · **다른 세션 소유 · 이 회차 무접촉**). ＋ 이 회차가 만든 `lane-window-a5b`(문서 전용). ⚠ **회차 착수 시각(10:46)에는 `lane-window-a5`(`85d6863`) 가 3번째로 살아 있었고, 10:47 관측에서 사라졌다** — 오케스트레이터의 병합 후 정리로 읽는다(`85d6863` 의 문서가 `e37808c` 로 `main` 에 들어와 있다). 이 회차가 지운 것이 아니다. | `git worktree list` (2회 관측) |
| 서빙 릴리스 | **`f465d83a2ef1`** — 앱 5종 이미지 태그 동일 ＋ 원장 마지막 `deploy` 행 `2026-09-04T21:54:09+0900 deploy f465d83a2ef1 f465d83a2ef1 green` ＋ `LAST-SUCCESS.txt` 마지막 줄 `2026-09-04T21:54:09+0900 deploy OK f465d83a2ef1` | `docker ps` ＋ `release-ledger.tsv` ＋ `LAST-SUCCESS.txt` |
| 컨테이너 | staging **8개 전부 healthy** (`nginx`·`frontend`·`core_api`·`pipeline_worker`·`viz_render`·`ai_service`·`cloudflared`·`pg`) | `docker ps` |
| 체인 head (platform) | **`0011_lv1_drop_level_user_set`** · 리비전 11건 · head 1개 | `gates/run.sh migration-single-head` |
| 체인 head (ai) | **`0005_k2b_concept_graph_seed`** · 리비전 5건 · head 1개 | 〃 |
| 원장 green `deploy` 행 | **≥2** (`f465d83a2ef1` · `64791f910f49`) | `release-ledger.tsv` |
| 창 시각 충돌 | **없음** — 10:46 착수, 백업 크론 03:30 · 04:10(월) · 04:40 | `crontab -l` |
| 자리 파일 (`~/.colab-v2-staging/previews`) | ⛔ **285** (`json 113`·`png 85`·`pgw 43`·`webp 42`·`tif 2`) — A5 끝값 **236 에서 +49**. §0.1 참조 | `find -type f` |
| `COLAB_STAGING_ENV` | 셸 기본 환경에 **미설정** — B 착수 시 홈 0600 env 를 export 해야 한다 | 종전 회차와 같음 |

⚠ **staging 스택 밖 컨테이너 2건**(`a2_pg`·`ai_pg` · `Up 14 hours` · 게이트 DB)은 8개 계수에 넣지 않았다. **게이트 전용이고 이 회차 무접촉이다.**

### 0.1 ⛔ A5 이후 staging 에 일어난 일 — 이 회차가 하지 않은 것이다

A5 시트 §6(iii) 가 「자리 파일 236 이 나오는 것이 정상, 갈리면 그 사이 누가 썼다는 뜻」이라고 못 박아 두었다. **갈렸다.**

| 실측 | 값 |
|---|---|
| 자리 파일 총수 | **285** (A5 끝값 236) |
| `mtime > 2026-09-05 09:47` 인 파일 | **56** ⟹ 신규 49 ＋ 덮어쓰기 7 |
| 그 56건의 시각대 | **10:07 ~ 10:09 KST** 한 덩어리 |
| `colab_v2_staging_viz_render` | `RestartCount=1` · `FinishedAt=2026-09-05T01:08:39.726Z` · `StartedAt=2026-09-05T01:08:39.935Z` (= **10:08:39 KST**) · `ExitCode=0` · `OOMKilled=false` · `RestartPolicy=unless-stopped` |
| 재기동 후 이미지 | **`colab-v2/viz-render:f465d83a2ef1`** — 서빙 릴리스 불변 |
| 재기동 후 로그 | `GET /healthz 200` 뿐. **렌더 기록은 이전 런의 로그에 있었고 회수 불가** |

**⟹ 원인 `[미확인]`.** 렌더 버스트(10:07~10:09)와 컨테이너 정상종료·자동재기동(10:08:39)이 같은 3분에 있었다는 사실까지가 실측이다. **이 회차는 staging 에 아무것도 쓰지 않았다**(§9) — 다른 세션 또는 사람의 화면 조작으로 읽는다.

**이 사실이 바꾸는 것 둘 (A5 §6 을 무효화하지는 않는다).**
1. **§6(iii) 의 before 기대치가 236 이 아니라 285 다.** 창은 285 를 기준선으로 잡고 시작한다.
2. ⛔ **§6(iv) 의 「구판 19벌」은 이제 실측값이 아니다.** `〈312〉` 의 `대상 106 · 판정 불가 19` 는 자리 파일 236 시점 계수다. **창은 (iii) 의 `artifact-ownership` 을 돌려 판정 불가 건수를 먼저 다시 세고, 그 값으로 (iv) 를 연다.** A5 §6(iv) 가 미리 잰 「사이드카 없는 `.json` 5건」도 `.json` 92→113 으로 늘었으므로 **다시 센다.**

**이 사실이 바꾸지 않는 것.** 배포 안전성 축은 전건 그대로다 — 짝 백업은 **이 쓰기가 끝난 뒤**에 떠서 285 를 담았고(§4), 롤백 대상 이미지 6/6 실재하며(§5), 마이그레이션 델타는 0줄이다(§1.1).

---

## 1. B 가 실어 나를 변경분 — `git diff --stat f465d83..e37808c`

**총계 = 31 파일 · +3,517 / −85.** (A5 의 `f465d83..1ae5dc0` = 29 파일 / +2,987 / −82 대비 **＋2 파일 · ＋530 / −3** — 그 증분이 `〈317〉` 수정과 A5 문서다.)

| 영역 | 파일 | 내용 |
|---|---:|---|
| `services/viz-render/src/.../d7_visualization` | **8** | **V-1** 팔레트 재렌더(`invalidation.supersede_plan` ＋ `jobs.py`) · **TL-1** 타일 회수(`tile_reclaim.py`·`tile_liveness.py`·`tile_liveness_cli.py` ＋ `trigger_loop.py` 결선) |
| `services/viz-render/src/.../kernel` | 1 | `config.py` +46 — 타일 회수 설정 3종 ＋ 기본값 |
| `services/viz-render/src/.../app` | 2 | 결선 |
| `services/viz-render/tests` | **3** | `test_palette_rerender.py` **321줄**(A5 시점 229줄 ＋ `〈317〉` 회귀 가드 2) · `test_tile_liveness.py` 283줄 · `test_tile_reclaim.py` 377줄 |
| `frontend` | **2** | **BF-4** 미리보기 캡션 — `DatasetPreviewSection.tsx` 가 사이드카를 **타일 갈래 밖에서도** 조회 ＋ 시험 1파일 |
| `infra/staging` | **1** | `compose.i2.yml` — 타일 회수 설정 3종 `${…:-}` 통과(344~346행) |
| `dev-package/notes` | 6 | 등재문 초안 ＋ `STAGE2-COMPLETION-PATH-20260905.md` ＋ `V1-FLAKE-LEDGER-PENDING.md`(코드 아님) |
| `dev-package/sessions` | 4 | `TL-1-20260905.md`·`WINDOW-20260904-B4.md`·`FIRSTTOUCH.tsv`·**`WINDOW-20260905-A5.md`**(코드 아님) |
| `dev-package` 산문·대장 | 4 | `work-items.yaml`·`PLAN-SoT.md`(〈313〉~〈317〉)·`WORK-UNITS.md`·`03-HANDOFF.md` |

**코드 표면은 셋 그대로다** — `services/viz-render` · `frontend`(1파일) · `infra/staging/compose.i2.yml`(환경변수 통과 3줄).

### 1.1 마이그레이션 판정 — **0건. 실측이다(추정 아님).**

```
git diff --stat f465d83..e37808c -- db/   → 0 줄
ls db/platform/versions/  → 0001 … 0011.  0012 없음
ls db/ai/versions/        → 0001 … 0005.  그대로
```

⟹ **`db/*/versions/` 무변경 · `0012` 부재.** platform head `0011` · ai head `0005` 에서 움직이지 않는다. **이것이 롤백 한계를 정한다** — §5.

### 1.2 `〈317〉` 이 실제로 고친 자리 — 코드 실측

A5 §2.1 의 근원인 진단(「`STATUS_DONE` 이 회수 집행보다 먼저 선다」)에 대응한다.

```
jobs.py:705  job.invalidation_removed = invalidation.apply(...)      # 회수 집행
jobs.py:725  job.invalidation_removed = invalidation.apply(          # (다른 갈래)
jobs.py:732  finally:
jobs.py:739      job.status = STATUS_DONE                            # ← 회수 뒤 · 단일 대입
jobs.py:576~577  주석 축자 — 「종전에는 이 자리가 `job.status = STATUS_DONE` 이었고,
                  색인 등재 · 회수 범위 계산 · `invalidation.apply()` 는 **그 뒤**에서 돌았다」
```

⟹ **「완료」 발행이 회수 집행 뒤 `finally` 안의 단일 대입으로 옮겨졌다.** A5 가 잰 ~15 ms 의 틈이 구조적으로 닫힌다.

**회귀 가드 2건 (시험 8 → 10 · 게이트 수집 324 → 326 으로 실측 확인)**

```
test_palette_rerender.py:145  test_화면이_완료를_본_그_순간_옛_벌은_이미_지워져_있다
test_palette_rerender.py:192  test_회수가_실패해도_렌더는_완료로_선다
```

⟹ 앞엣것이 red-first 로 그 틈을 잠그고, 뒤엣것이 **`finally` 로 옮기면서 「회수가 죽으면 렌더가 영원히 「그리는 중」」이 되는 반대 방향 결함**을 잠근다.

---

## 2. 배포 전 검사기 8건 — 전부 **단독 실행**(`COLAB_GATE_JOBS=1`) · 축자

```
frontend-typecheck green — tsc --noEmit(frontend/tsconfig.json · include=src·test) 오류 0건.
frontend-test green — vitest run(frontend/vite.config.ts · jsdom) 통과 595건 · 실패 0건.
    Test Files  38 passed (38) · Tests  595 passed (595)
contract-lint green — seam 3건, 룰 위반 0.
    # 대상 3건 — contracts/seams/core-ai.yaml contracts/seams/core-viz.yaml contracts/seams/fe-core.yaml
generated-up-to-date green — 등기부 4건 전부 재생성 일치, 등기부 밖 자칭 생성물 0건.
contract-breaking green — 기준 HEAD (3건) 대비 파괴적 변경 없음.
migration-single-head green — 두 체인 모두 head 1개.
    # db/platform: 리비전 11건 · head 1개 (0011_lv1_drop_level_user_set)
    # db/ai: 리비전 5건 · head 1개 (0005_k2b_concept_graph_seed)
service-tests-core-api — 선택자 «not e2e» · 수집 626 · 실행 626 · skipped 0 · deselected 6 · failed 0 · errors 0 · 소요 87.7초
service-tests-core-api green — 실행 626건 전부 통과 (skipped 0 · deselected 6 은 요약줄에 드러나 있다).
```

- **green 8 · red 0 · 못 돈 것 0.**
- ⚠ `service-tests-core-api` 는 **1회차 실행에서 바로 green** 이라 재실행 조건이 서지 않았다. A5 와 같다. 종전 회차들이 지목한 간헐 red(연구실 A 데이터셋 계수 오염)는 **이번 표본에서도 재현되지 않았다** — ⛔ **원인은 `[미확인]` 그대로**이고 「고쳐서 green」이 아니다.

### 2.1 `service-tests-viz-render` × 3 연속 — **3/3 green** · 축자 전문

A5 를 NO-GO 로 만든 자리다. 단독(`COLAB_GATE_JOBS=1`)으로 **연속 3회** 돌렸고 **한 회도 red 가 없다.**

```
[1회] 326 passed, 40 deselected in 26.48s
      service-tests-viz-render — 선택자 «not e2e and not perf» · 수집 326 · 실행 326 · skipped 0 · deselected 40 · failed 0 · errors 0 · 소요 26.5초
      service-tests-viz-render green — 실행 326건 전부 통과 (skipped 0 · deselected 40 은 요약줄에 드러나 있다).

[2회] 326 passed, 40 deselected in 26.41s
      service-tests-viz-render — 선택자 «not e2e and not perf» · 수집 326 · 실행 326 · skipped 0 · deselected 40 · failed 0 · errors 0 · 소요 26.4초
      service-tests-viz-render green — 실행 326건 전부 통과 (skipped 0 · deselected 40 은 요약줄에 드러나 있다).

[3회] 326 passed, 40 deselected in 26.30s
      service-tests-viz-render — 선택자 «not e2e and not perf» · 수집 326 · 실행 326 · skipped 0 · deselected 40 · failed 0 · errors 0 · 소요 26.2초
      service-tests-viz-render green — 실행 326건 전부 통과 (skipped 0 · deselected 40 은 요약줄에 드러나 있다).
```

- **수집 324 → 326** — `〈317〉` 회귀 가드 2건이 실제로 실행되고 있다는 실측이다(선언만 늘고 안 돌면 326 이 안 나온다).
- A5 의 실패 케이스 `test_팔레트_재렌더는_새_키를_세우고_옛_벌을_지운다` 는 **3회 모두 통과**했다.

**⚠ 이번에 세지 않은 판정기준 (다음 회차 진입조건).**
- `service-tests-ai-service`·`service-tests-pipeline-worker` 단독 실행 — **이 회차는 지시대로 8건만 돌렸다.**
- `gates/run.sh all` 전수 50건 — **배포 후 §6(ii) 자리**다. 이 회차는 돌리지 않았다.
- `service-tests-core-api` 간헐 red 의 원인.
- ⛔ **`viz-render` 의 재현율** — A5 가 4회 중 3회 red, 이 회차가 3회 중 0회 red. **둘 다 표본이지 확률이 아니다.** `〈317〉` 은 5회 연속 green 을 기록했고 이 회차가 3회를 더했다(합 8회 연속). 그래도 「간헐이 사라졌다」가 아니라 **「8회 표본에서 안 났다」**로 적는다.

---

## 3. TL-1 안전 사전점검 — **이 배포는 자동 삭제기를 싣는다**(관측 전용 기본) · 3항 전건 통과

### (a) `COLAB_VIZ_TILE_RECLAIM_APPLY` 가 staging 에 닿는 자리에 **켜져 있지 않다**

```
$ grep -rn "TILE_RECLAIM" infra/
infra/staging/compose.i2.yml:344:      COLAB_VIZ_TILE_RECLAIM_APPLY: ${COLAB_VIZ_TILE_RECLAIM_APPLY:-}
infra/staging/compose.i2.yml:345:      COLAB_VIZ_TILE_RECLAIM_MAX_KEYS: ${COLAB_VIZ_TILE_RECLAIM_MAX_KEYS:-}
infra/staging/compose.i2.yml:346:      COLAB_VIZ_TILE_RECLAIM_INTERVAL_SECONDS: ${COLAB_VIZ_TILE_RECLAIM_INTERVAL_SECONDS:-}
```

⟹ **`infra/` 안에 켜진 값 0건** — 셋 다 `${…:-}` 통과뿐이고 **enabled 값이 박힌 자리가 없다.**

**홈 0600 env(`~/.colab-v2-staging.env`) — 키 이름만 읽었다. 값은 읽지도 적지도 않았다.** (권한 `600` 실측)
실재 키 **23개** = `CF_ACCOUNT_ID`·`CF_API_TOKEN`·`CF_TUNNEL_ID`·`CF_TUNNEL_TOKEN`·`COLAB_AI_APP_PASSWORD`·`COLAB_APP_PASSWORD`·`COLAB_CORE_SESSION_SECRET`·`COLAB_OWNER_PASSWORD`·`COLAB_PG_SUPER_PASSWORD`·`COLAB_STAGING_AI_DB_URL_FILE`·`COLAB_STAGING_AI_OWNER_DB_URL_FILE`·`COLAB_STAGING_CORE_DB_URL_FILE`·`COLAB_STAGING_CREDENTIALS_FILE`·`COLAB_STAGING_PGDATA_DIR`·`COLAB_STAGING_PIPELINE_DB_URL_FILE`·`COLAB_STAGING_PLATFORM_OWNER_DB_URL_FILE`·`COLAB_STAGING_PREVIEWS_DIR`·`COLAB_STAGING_SUBJECTS_FILE`·`COLAB_VIZ_SERVICE_TOKEN`·`COLAB_VIZ_TILE_SIGNING_SECRET`·`COLAB_WORKER_ACCOUNT_ID`·`COLAB_WORKER_LAB_ID`·`OPENAI_API_KEY`.

> 정정 — A5 는 같은 목록을 **「24개」**로 적었으나 나열한 키는 23개였다. **집합은 동일하고 계수만 A5 가 틀렸다.**

⟹ `grep -c RECLAIM` = **0**. **`COLAB_VIZ_TILE_RECLAIM_*` 는 홈 env 에 한 줄도 없다** ⟹ compose 의 `${…:-}` 가 **빈 값**으로 떨어진다.

⭑ 보조 실측 — 현재 도는 `colab_v2_staging_viz_render`(구판 `f465d83a2ef1`)의 `Config.Env` 에도 `RECLAIM` 키 **0건**이다.

### (b) 빈 값 = 꺼짐임을 코드로 확인

```
config.py:84   TILE_BRANCH_ON_VALUES = frozenset({"1", "true", "on", "yes"})
config.py:87   def _tile_branch_from_env(raw): return (raw or "").strip().lower() in TILE_BRANCH_ON_VALUES
config.py:103  DEFAULT_TILE_RECLAIM_APPLY = False
config.py:224  tile_reclaim_apply=_tile_branch_from_env(os.environ.get("COLAB_VIZ_TILE_RECLAIM_APPLY"))
config.py:223  # 주석 축자 — 「모르는 값(오타 포함)도 꺼짐이다」
```

⟹ **빈 값·미선언·오타 전부 꺼짐.**

### (c) 상한·주기 기본값이 `〈316〉` 이 말한 값과 같다

```
config.py:106  DEFAULT_TILE_RECLAIM_MAX_KEYS = 20
config.py:110  DEFAULT_TILE_RECLAIM_INTERVAL_SECONDS = 3600.0
config.py:165  _positive_int_from_env  — 못 읽는 값·0·음수는 기본값
```

⟹ **상한 20 · 주기 3600초 · 오타는 기본값** — `〈316〉` 축자와 **전건 일치**.

**판정 = 사전점검 3항 전부 통과. TL-1 은 이 배포의 NO-GO 사유가 아니다.**
⚠ 다만 배포되면 루프가 **실제로 돈다**(관측 전용). 그 계수를 창에서 반드시 읽는다 — §6 (vi).

---

## 4. 단계 A — 배포 전 짝 백업 (집행 완료 · GREEN)

- 명령 = `infra/staging/backup/backup-full.sh` (플래그 없음) · `main` 체크아웃에서. 종료코드 **0**.
- 최종 줄 축자 — **`2026-09-05T10:59:05 전범위 백업 GREEN — 원장 2 프로파일 ＋ 볼륨 2 개 · 보관처 규약 밖 파일 0`**

| 산출물 | 스탬프 | 크기(B) | 판정 |
|---|---|---:|---|
| `platform-….sql.gz` | `20260905T105822` | 30,799 | GREEN (통과 7 · SKIP 0) |
| `ai-….sql.gz` | `20260905T105822` | 5,159 | GREEN (통과 7 · SKIP 0) |
| `vol-uploads-….tar.gz` | `20260905T105823` | 393,734,961 | GREEN (통과 8 · SKIP 0) · 아카이브 157건 = 매니페스트 157건 · 원장 133건 전건 포함 |
| `vol-previews-….tar.gz` | `20260905T105900` | 41,926,182 | GREEN (통과 6 · **승인된 SKIP 1** = V5 오라클 명시 면제 `none`) · 아카이브 **285건** = 매니페스트 285건 |

**짝(회차 한 벌) — `.pair` 실측**

```
vol-uploads-20260905T105823.pair   -> platform-20260905T105822.sql.gz
vol-previews-20260905T105900.pair  -> platform-20260905T105822.sql.gz
```

두 원장 덤프의 스탬프가 **동일**(`20260905T105822`) — P5(같은 회차) 조건이 시각차 0초로 선다.

**최신본 재검사** — `infra/staging/backup/latest-check.sh` → **`최신본 재검사 GREEN — 프로파일 2 개 · 볼륨 2 개`** (종료코드 0).

**덤프가 담은 체인 (읽기 전용 대조)**

```
platform-20260905T105822.sql.gz : COPY public.alembic_version_platform → 0011_lv1_drop_level_user_set · '0012' 문자열 0건
ai-20260905T105822.sql.gz       : COPY public.alembic_version_ai       → 0005_k2b_concept_graph_seed
```

⟹ 이 덤프는 **배포 후에도 그대로일 스키마**를 담는다 — 스키마 되돌림 재료가 아니라 **데이터 되돌림 재료**다.
⭑ `vol-previews` 는 자리 파일 **285** 상태를 담는다 — §0.1 의 10:07~10:09 쓰기 **뒤**의 상태다. **그 쓰기는 백업에 들어 있다.**

경로 = `$COLAB_BACKUP_DIR`(홈 백업 보관처) 아래 `staging/`. **값·접속 문자열은 이 문서에 적지 않는다.**

---

## 5. 되돌림 준비 상태 (rollback readiness)

| 축 | 값 |
|---|---|
| 스키마 변경 | **없음** (§1.1 실측) |
| `rollback.sh --to-last-green` 의 복원 범위 | **이미지 = 배포 내용 전부** ⟹ **완전 복구** |
| 파괴적 복원(`--yes-drop-schema`) 필요 여부 | **불필요** |
| 롤백 대상 | **`f465d83a2ef1`** — 현재 서빙이자 직전 green |
| 그 태그 이미지 6종 실재 | ✅ **6/6** — `docker images` 실측 |

```
colab-v2/ai-service:f465d83a2ef1        27d21caee12e
colab-v2/core-api:f465d83a2ef1          77d675a7752d
colab-v2/frontend:f465d83a2ef1          b5604978809f
colab-v2/migrator:f465d83a2ef1          e4986ae2bb2a
colab-v2/pipeline-worker:f465d83a2ef1   7ffcb30c6f14
colab-v2/viz-render:f465d83a2ef1        6c628cd7ee0c
```

⚠ **롤백이 되돌리지 못하는 것 둘.**
⑴ **캐시** — `〈308〉`·`〈312〉` 와 같은 자리. **재굽기 뒤 롤백하면 옛 키 산출물이 회수로 치워졌을 수 있다.** 순서가 되돌림 비용을 정한다.
⑵ **자동 삭제기가 지운 타일** — 이 배포는 관측 전용이라 **0건 지운다**(§3). 켜지 않는 한 이 항은 비어 있다.

---

## 6. 배포 후 이 창이 돌릴 순서 — **A5 §6 과 동일. 아래는 그 전문이고 (iii)(iv) 의 기준선만 §0.1 로 갱신했다.**

### (i) 값 조회 4건 — **8 healthy 직후, 다른 무엇보다 먼저**

⛔ **배포당 한 번뿐인 창이다.** `〈304〉` 의 1,164 / 2,111 ms 스파이크 갈래는 `〈312〉` 채집에서도 재현되지 않아 **`[미확인]` 그대로**다. **다시 잡을 자리는 다음 재기동뿐이다.**

- **시점** = 배포 판정 6항 중 **8 healthy 를 확인한 그 순간**. ⛔ 게이트·재굽기·현물 확인보다 먼저. **사람이 부르는 첫 요청이 이것**이어야 한다.
- **요청** = `POST /api/v1/datasets/{id}/value-lookup` · 점 `36.75,127.40` · **데이터셋당 1회씩** · UA 는 `〈312〉` 와 같은 축.
- 대상 4건 = `01M0YWDXN7CA13KB7MQC9PGEW5`(GK-2A) · `01M1JM3JTQSSTKR9A2QQ67QN81`(U-Net) · `01M19RENXAANZ2HDWM4Z46C37Y`(PV-1) · `01M0Y1VQ09TMHAC78ESDRTMAZD`(DEM · 대조군).
- **남길 것** = `Server-Timing` 원문 축자 ＋ `time_connect`·`time_appconnect`·`time_starttransfer`·`time_total` ＋ `http_code`. 원값은 `sessions/WINDOW-20260905-FIRSTTOUCH.tsv` 로.
- ⚠ **1회씩만 부른다.** 두 번째 요청은 이미 따뜻하다.

### (ii) 게이트 전수 — `gates/run.sh all -j 1` (선언 50)

- `artifact-ownership` 은 재굽기 전이므로 **판정 불가 건수를 그대로 낼 것이 정상**이다.
- ⚠ `service-tests-viz-render` 가 §2.1 에서 3/3 green 이었으나 **뜨면 감추지 말고 그 회차 값으로 적는다.**

### (iii) `artifact-ownership` 계수 — **재굽기 전(before)**

- ⛔ **기준선이 바뀌었다** — 자리 파일 실측 **285**(A5 의 236 아님 · §0.1). `〈312〉` 의 `대상 106 · 살아 있다 87 · 접수분 0 · 고아 0 · 판정 불가 19` 는 **236 시점 계수라 그대로 나오지 않는 것이 정상**이다.
- **이 자리에서 나오는 값이 (iv) 의 대상 계수다.** 「19」를 전제로 두지 않는다.

### (iv) **구판 재굽기** — 대상 목록은 **창에서 실측해 먼저 적고** 나서 손댄다 · **갈래를 먼저 가른다**

`〈315〉`-㉯ 판정 = 「`tolerate` 기한 연장이 아니라 **재굽고 재계수**한다」. **대상 목록은 이 세션이 갖고 있지 않다 — 추측하지 않는다.**

**⛔ 갈래를 먼저 가른다 (등급 정의 축자 = 「사이드카가 **없거나** **구판**이다」 · `ownership.py:12`).**
- **갈래 ㉮ = 사이드카 판 없음** — `.json` 은 있는데 `sidecarVersion`·`baked_for` 가 없고 `source`(ULID)만 있는 벌.
  A5 가 236 시점에 잰 값 = `.json` 92 중 **5**건(`source` = `01M0V0MAPTYPEFILE00000000B`·`01M0Y1WNWK8Y7DT4RD2TSZ6H12`·`01M1BF69AJEA3BWK5NGXWTKCWX`·`01M0Y1WM3DKAWPBKVV0883285M`·`01M0Y1WMG1BBBQ8E7PMKP37NX9`).
  ⛔ **이 5건은 확정 목록이 아니다.** `.json` 이 92 → **113** 으로 늘었으므로(§0.1) **창에서 다시 센다.**
- **갈래 ㉯ = 사이드카 부재** — `.json` 자체가 없는 벌. **사이드카로는 원천이 안 나오므로 `artifact-ownership` 스냅숏 쪽에서 읽어야 한다.**

**창에서의 도출 절차(순서 고정)**
1. (iii) 의 `artifact-ownership` 전수 스냅숏에서 **등급 `판정 불가` 인 벌만** 뽑는다.
2. 그것을 **갈래 ㉮ / ㉯ 로 가르고 각 건수를 회차 파일에 적는다.**
3. ㉮ 는 사이드카의 `source`(ULID)로, ㉯ 는 스냅숏으로 원천 식별자를 얻는다.
4. 그 ULID 를 **읽기 전용**(`BEGIN READ ONLY`)으로 platform DB `d3_file` 에 대조해 데이터셋을 얻는다.
5. **얻은 데이터셋 목록을 회차 파일에 먼저 적고**, 그 다음에 `POST /api/v1/previews` 를 부른다(조건문이 아니라 **목록으로 고정한 뒤 집행** · `CLAUDE.md §6`).
- **판정 = staging WRITE.** Ted 의 승인이 이 자리를 덮어야 한다.
- 인가된 경로는 공개 엣지 `POST /api/v1/previews` 하나뿐이다(`WINDOW-20260903-A3 §6`⑵).
- ⚠ **상세 화면을 재굽기보다 먼저 열지 않는다** — `〈312〉` §3.1 이 그 순서 오류로 온디맨드 굽기 1벌을 냈다.

### (v) `artifact-ownership` 계수 — **재굽기 후(after)** · **판정 불가가 (iii) 값에서 내려가는가**

- 이것이 `〈315〉`-㉯ 의 오라클이다. **내려가면 판정이 맞았고, 안 내려가면 「재굽기로는 안 덮인다」가 실측된다** — 어느 쪽이든 값으로 적는다.
- ⚠ **「내려갈 것」을 미리 적지 않는다.** `〈312〉` 에서 기대치 `+9` 가 실측 `+3` 이었던 것과 같은 자리다.

### (vi) **TL-1 관측 전용 계수** — viz-render 로그에서 회수 바퀴를 읽는다

- `docker logs colab_v2_staging_viz_render` 에서 회수 한 바퀴의 기록을 찾아 **무엇을 셌는지** 적는다(자리의 타일 벌 수 · 살아 있다 · 접수분에만 닿는다 · 고아 · 판정 불가 · 계산 불가).
- ⛔ **삭제는 반드시 0건이어야 한다**(관측 전용 · §3). **0 이 아니면 그 자체가 사고**이고 즉시 창을 닫는다.
- ⚠ 주기 3600초이므로 **한 바퀴가 돌 때까지 로그에 안 나올 수 있다.** 안 나오면 **「못 봤다」로 적는다 — 0 으로 적지 않는다.**
- `〈313〉` 실측 = 자리의 `tile-` 벌 2 · 못 닿는 벌 0 ⟹ **켜도 지금 지울 것이 없다**가 예측값이다.

### (vii) 현물 — 사용자 눈에 보이는 것

| # | 대상 | 기대 |
|---|---|---|
| ㉠ | **BF-4** 캡션 3 데이터셋 (**GK-2A 포함**) | 머리에 데이터셋 이름·파일명 ＋ **격자 `<grid>` · W × H** 표기. `〈315〉`-㉮ 로 **산출 격자(821 × 1024)를 그대로 수용**한다. ⛔ **`〈312〉` 에서 자리째 빠졌던 GK-2A 가 이번엔 서야 한다** — 그것이 `〈314〉` 가 고친 자리(사이드카를 타일 갈래 밖에서도 조회) |
| ㉡ | **V-1** 팔레트 재렌더 1회 | 같은 데이터셋에 다른 팔레트로 1회 → **색만 바뀌고 구간 경계(`classes[].min/max`)·`bounds` 불변** ＋ **옛 벌이 자리에서 사라진다**. **자리 파일 수를 전후로 센다.** ⛔ **이 관측이 `〈317〉` 을 staging 에서 증명하는 자리이기도 하다 — 「완료」를 본 그 순간 옛 벌이 이미 없어야 한다**(시험 `test_화면이_완료를_본_그_순간_옛_벌은_이미_지워져_있다` 의 실물 판) |
| ㉢ | 값 조회 | 재굽기 뒤 3건 `200 ＋ available:true ＋ 값` · DEM 대조군 `available:false` |
| ㉣ | 검색 | `POST /api/v1/dataset-searches` — 질의 반영 · `verified` 걸름 동작 |
| ㉤ | `/lab` | 200 · ⚠ `BF-8` 미수정이라 여백 결함이 남아 있는 것이 정상 |
| ㉥ | 404 | 타 연구실·없는 자원 접근 = **404**(403 이 아니다 · `DEPLOY-269 §3`) |

⛔ **자기 연구실 묘비 410 은 이 창도 재지 못한다** — `deleted_at` 행 0건이고 만들려면 파괴적 쓰기가 필요하다.

### (viii) `BF-4`·`V-1` 완료 판정 — 조건별로

| 항목 | 마지막 열린 조건 | 이 창이 닫는가 |
|---|---|---|
| **BF-4** | ⑶ **화면 실측** (`〈314〉`-㉳⑴) | (vii)㉠ 이 3 데이터셋에서 서면 **닫힌다** ⟹ `in_progress` → `done` |
| **V-1** | ⑹ **staging 배포 green** | (vii)㉡ ＋ 배포 판정 green 이면 **닫힌다**. ⛔ A5 가 막아 둔 선행 조건(§2.1 의 red)은 **`〈317〉` 로 해소됐고 이 회차에서 3/3 green 으로 실측됐다** |

**⟹ 이 회차(A5b) 판정 = 둘 다 「아직 닫지 않는다」.** 배포가 서지 않았으므로 화면 실측도 배포 green 도 없다.

---

## 7. Ted 의 GO 시 원장에 덧붙일 승인 행 — **축자 (이 회차는 적지 않았다)**

`~/colab-v2-releases/release-ledger.tsv` 에 탭 구분 5열로 **한 줄** append (시각은 append 시점으로 갱신) —

```
2026-09-05T<HH:MM:SS>+0900	approve	e37808c	-	-	승인자=Ted 본것=go/no-go ① GO · 짝 백업 platform-20260905T105822/vol-uploads-20260905T105823/vol-previews-20260905T105900 GREEN · latest-check GREEN · 마이그레이션 델타 0줄(0012 부재 · 덤프 alembic_version_platform=0011) · 배포전 게이트 8건 전부 green(frontend-typecheck·frontend-test·contract-lint·generated-up-to-date·contract-breaking·migration-single-head·service-tests-core-api·service-tests-viz-render) · service-tests-viz-render 는 단독 3회 연속 green(수집 326 · A5 의 간헐 red 는 〈317〉로 수정 · 회귀 가드 2건 포함) · TL-1 자동삭제 관측전용 확인(APPLY 미선언·상한20·주기3600) · 롤백 대상 f465d83a2ef1 이미지 6/6 실재 · 배포 직후 값 조회 4건 Server-Timing 채취 승인 포함 · 구판 재굽기(POST /api/v1/previews · staging WRITE · 대상 계수는 창에서 재실측) 승인 포함 · 자리 파일 236→285(A5 이후 외부 쓰기 · 원인 미확인) · sha=e37808c(=1eb247f+A5문서) · WINDOW-20260905-A5b
```

⛔ **이 회차는 append 하지 않았다.** A5 의 승인 행에 있던 「red 를 안 자리에서 승인함」 조항은 **red 가 해소됐으므로 지웠다.**

---

## 8. 판정 — **GO**

**결정적 사실 = A5 를 NO-GO 로 만든 단 하나의 축(`service-tests-viz-render` 의 간헐 red)이 `〈317〉` 로 수정됐고, 배포 후보 `e37808c` 에서 단독 3회 연속 green(수집 326 · failed 0 · errors 0)으로 실측됐다. 나머지 7개 게이트도 전부 green 이고, A5 가 GO 쪽으로 세워 둔 다른 축은 전건 그대로다.**

- 그 수정은 **A5 가 진단한 근원인 바로 그 자리**를 고쳤다 — 「완료」 발행이 회수 집행 뒤 `finally` 안의 단일 대입으로 옮겨졌고(§1.2), red-first 회귀 가드 2건이 그 틈과 반대 방향 결함을 함께 잠갔다. **수집 324 → 326 이 그 두 가드가 실제로 도는 증거다.**
- ⛔ **「3회 green」을 「간헐이 사라졌다」로 적지 않는다.** `〈317〉` 5회 ＋ 이 회차 3회 = **8회 표본에서 안 났다**가 실측이다(§2.1).

**GO 를 받치는 축 전건.**
게이트 8/8 green(단독) · 마이그레이션 델타 0줄(실측 · `0012` 부재 · 덤프 head `0011`/`0005`) · 짝 백업 GREEN(스탬프 동일 `20260905T105822`) ＋ `latest-check` GREEN · TL-1 자동 삭제기 관측 전용 3항 전건 통과 · 롤백 대상 `f465d83a2ef1` 이미지 6/6 실재 · staging 8 healthy · 워킹트리 0건 · 크론 비충돌.

**⚠ GO 에 붙는 조건 하나 — 계수 기준선이 바뀌었다.**
§0.1 의 미확인 쓰기(자리 파일 236 → 285 · 10:07~10:09)로 **`〈312〉` 의 「구판 19벌」이 더는 실측값이 아니다.** 이것은 **배포 안전성 축이 아니라 배포 후 회계 축**이다 — 백업이 285 를 담았고 롤백 재료는 온전하다. 창은 §6(iii) 에서 `artifact-ownership` 을 먼저 돌려 계수를 다시 세운 뒤 (iv) 를 연다. **「19」를 전제로 재굽기를 시작하면 안 된다.**

**추가 no-go 조건 (종전 회차에서 이어지는 것 · 전건 유효)**
- 승인 행 미기재 상태로 `deploy.sh` 를 부르는 것. **승인 행이 먼저다.**
- (i)(값 조회 4건)을 8 healthy 직후에 부를 준비가 안 된 상태로 배포하는 것.
- (iv) 재굽기 대상 목록을 **적지 않은 채** `POST /api/v1/previews` 를 부르는 것.
- (vi) 에서 **삭제가 0건이 아닌 것.** 그 자체가 사고이고 즉시 창을 닫는다.

---

## 9. 이 회차가 만진 것 · 만지지 않은 것

- **staging 쓰기 0** — 부른 것은 `docker ps`·`docker images`·`docker inspect`·`docker logs`·`find`(읽기)와 `pg_dump`(읽기 전용 백업)뿐이다.
- **컨테이너 수명 조작 0 · 파괴 플래그 0 · DDL 0 · SQL 쓰기 0 · 삭제 0 · 배포 0 · push 0 · 병합 0 · 코드 0줄 수정.**
- ⛔ **§0.1 의 자리 파일 +49 와 `viz_render` 재기동은 이 회차 착수(10:46) 이전(10:07~10:09)에 일어났다.** 이 회차의 행위가 아니다.
- `a2_pg`·`ai_pg` **무접촉**. `review-pr1` 워크트리 **무접촉**.
- `lane-window-a5` 워크트리는 10:46 관측에 있었고 10:47 관측에 없었다 — **이 회차가 지우지 않았다.**
- 자격증명·접속 문자열 **미출력** — 홈 env 는 **키 이름만** 읽었다.
- **B 미착수.** `deploy.sh` 미실행 · 원장 승인 행 미기재.
