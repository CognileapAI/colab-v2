# S2-EXEC-PLAN — 초기 데이터 적재 실행 절차서

> **WU-S2 집행 문서.** 작성 2026-08-26. **조사는 읽기 전용으로 수행했고 staging 에 접촉하지 않았다.**
> 코드·계약·마이그레이션·기존 문서 무수정. 커밋 없음.
>
> **채택안 = ㈎ 화면 밖 생성 — 실연구실 C 신설** (2026-08-26 Ted 판정). 3안 비교는 `§2` 에 보존.
> **멱등 채택안 = ㈑ 적재 도구 귀속** (2026-08-26 Ted 판정 · `〈106〉`). 확정 설계는 `§5.4`, 도구 명세는 `§14`. 종전 `§5.3`(절차적 멱등)은 **대체 사유와 함께 무수정 보존**한다.
> **㈐(픽스처 전용) 은 대비책으로 `§10` 에 보존** — 전환 시 3단만 교체된다.
> 근거는 `file:line`. 확인하지 못한 것은 `[미확인]`. 권고는 ★.
> 절대경로를 적지 않는다 — `<원천>` = `03 Reference-Data`.

---

## 0. 요약 — 이 문서가 확정한 것

| # | 항목 | 확정값 |
|---|---|---|
| 1 | 연구실 생성 수단 | **화면·계약·코드 전부 부재.** 레포 내 신규 스크립트 1건으로 화면 밖 생성 (`§3`) |
| 2 | `㊾-③` 저촉 | **미저촉** — 조항 대상은 데이터셋·파일·계보 (`§2.1`) |
| 3 | 주체 추가 | **core-api 재기동 필수.** 표는 프로세스 기동 시 1회만 읽힌다 (`§4`) |
| 4 | 멱등 판정 | **DB 에 재실행 방지 키가 없다** (`§5.2`). **귀속처 = 적재 도구** — 도구가 `listDatasets` 전수 순회로 동명 존재를 확인하고 건너뛴다. 계약 개정 0 · 마이그레이션 0 · 제품 코드 변경 0 (`§5.4` · `〈106〉`) |
| 5 | `d8_activity` 철거 | **행 DELETE 불가**(트리거). `TRUNCATE` 또는 트리거 일시 해제 (`§6`) |
| 6 | 적재 대상 | **데이터셋 11건 · 파일 139건 · 계보 간선 5** (`§7.1`). *(개정 2026-08-26 `〈104〉` — 종전 「15건 · 292건」. 적재 제외 4건 153 파일이 `①-b` 로 이동했다)* |
| 7 | 절차 | **5단 · 롤백 지점 4** (`§8`) |
| 8 | S2b 미승인분 | ✅ **해소 (2026-08-26 Ted)** — 설명 15건 일괄 승인 · 적재 제외 4건 판정(`〈104〉`). 권고 ㈏ 유지 (`§9`) |
| 9 | 멱등 귀속처 | **적재 도구** (`§5.4`). 제품 계층 멱등·완료 조건 개정은 기각 (`〈106〉`) |
| 10 | 적재 도구 실행 형태 | **스크립트 1건 · 공개 API 호출.** DB 직접 INSERT 0. `㊾-③` 정합 (`§14`) |
| 11 | 연구실 C ID | **결정론적 고정값** `00000000000000000000HYMETS` (`§3.4`) |
| 12 | 철거 결과 표기 | **「철거 완료」 기재 금지.** 잔존 3종을 사유와 함께 적는다 (`§5단`) |

**Ted 판정 대기 0건.** 종전 5건은 2026-08-26 Ted 판정으로 전건 확정 — `§11`. **이 절차서는 판정 대기 없이 집행 가능하다.**

---

## 1. 조사 1-A — 연구실 생성 수단 부재 실증

### 1.1 계약

- `contracts/seams/fe-core.yaml` 의 `operationId` **50건 전수 확인**. 연구실 관련 4건뿐
  - `getLab` — `contracts/seams/fe-core.yaml:105`
  - `updateLab` — `contracts/seams/fe-core.yaml:120`
  - `listLabMembers` — `contracts/seams/fe-core.yaml:144`
  - `saveLabMemberPermissions` — `contracts/seams/fe-core.yaml:171`
- **`createLab` 계열 0건.** 경로도 `/lab` 단수형이며 `/labs` 컬렉션 자원이 없다
- `updateLab` 은 `LabUpdate` 를 받고 `name` 을 포함 — `contracts/seams/fe-core.yaml:2009-2019`

### 1.2 코드 — 서버

- `updateLab` 은 **501 표에 있다** — `services/core-api/src/colab_core/app/routes/not_implemented.py:58`
  (`Op("updateLab", "PATCH", "/lab", "NOT_IMPLEMENTED_P1")`)
- 501 표의 총수 = **23**, 실동작 목록에 `updateLab` 없음 — `services/core-api/tests/test_not_implemented.py:21-55`, `:100`
- **귀결 — 화면으로 연구실 표시명을 고치는 경로가 지금 존재하지 않는다.** `getLab` 만 실동작

### 1.3 코드 — 화면

- 라우트 9개 전수 — `frontend/src/app/routes.tsx:21-32`. 연구실 생성 라우트 0건
- `createLab`·`연구실 생성`·`newLab` 문자열 `frontend/src` 전수 **0건**
- **S-07 「연구실 설정」의 범위 = 기존 연구실 편집 + 구성원 2탭** — `frontend/src/routes/LabSettingsPage.tsx:1`
  (「`연구실 정보` / `구성원 · 권한` 두 탭」). 생성 기능 없음
- ⚠ **`연구실 정보` 탭은 아직 빈 패널이다** — `frontend/src/routes/LabSettingsPage.tsx:40`
  (`data-fills-in="WU-G4"`). 표시명 편집 화면은 서버(501)와 화면(미구현) 양쪽에서 부재

### 1.4 판정

**연구실 생성 수단은 화면·계약·코드 어디에도 없다.** 부재는 실측으로 확정.

---

## 2. 조사 1-B — 대안 3안

### 2.1 ㈎ 화면 밖 생성 ★**채택** (2026-08-26 Ted)

| 항목 | 값 |
|---|---|
| 성립 | **성립.** 스키마상 `d1_lab` 은 외부 의존이 없는 뿌리 테이블 — `db/platform/schema.sql:98` 이하 참조 방향이 전부 `→ d1_lab` |
| 정본 저촉 | **미저촉** (아래 인용) |
| 롤백 | 백업 복원 1회 + 스크립트 역순 DELETE (D3~D8 무데이터 상태에서는 FK 저촉 없음) |
| 잔존 문제 | 연구실 C 의 표시명·프로필을 나중에 화면으로 고칠 수 없다(`updateLab` 501). 스크립트 재실행이 유일한 수정 수단 |

**`㊾-③` 원문 인용** — `dev-package/PLAN-SoT.md:297`

> ③ 적재는 DB 직접 INSERT 가 아니라 업로드 모달(S-04) → 파싱 → COG → 계보 확정, 화면이 쓰는 경로 그대로 간다

**저촉 판정 = 미저촉.** 근거 3건

1. **조항의 대상이 명시돼 있다** — 열거된 경로가 「업로드 모달 → 파싱 → COG → 계보 확정」이며 전부 **데이터셋·파일·계보**의 처리 단계다. 연구실·계정은 그 열거에 없다
2. **조항의 근거가 데이터에 한정된다** — 같은 줄의 근거 ②가 「직접 INSERT 는 파싱·좌표계·COG·계보가 한 번도 안 돌아본 채 green 이 된다」로, **파이프라인을 통과해야 할 대상**을 겨눈다. 연구실 행은 통과할 파이프라인이 없다
3. **선례가 이미 있다** — 계정·역할·권한·연구실이 전부 화면 밖에서 구성돼 왔다
   - `services/core-api/tests/fixtures/seed.sql:12-36` — `d1_lab`·`d1_lab_profile`·`d1_account`·`d2_member_role`·`d2_permission_switch` 를 SQL 로 직접 심는다
   - `services/core-api/src/colab_core/kernel/auth.py:1-9` — 「자격증명 발급 방식은 정본에 없고, v2 1차에서는 **개발자가 계정을 심어 제공한다**(P-17)」

> **정본 인용이 위 판정을 반증하지 않았다.** `㊾` 전문·`〈70〉`·`〈52〉` 를 확인했고 연구실 생성을 화면 경로로 제한하는 문장은 없다.
> 반증이 나오면 `§10` 의 ㈐ 로 전환한다.

### 2.2 ㈏ 연구실 생성 화면 신설 — 미채택

| 항목 | 값 |
|---|---|
| 성립 | 성립하나 **범위 확대**. `CLAUDE.md §5`(범위 늘리기 금지)로 **정본 개정 선행** |
| 개정 자리 | `dev-package/PLAN-SoT.md §9` 신규 판정 + `dev-package/WORK-UNITS.md` WU 신설 + `contracts/seams/fe-core.yaml` 5차 동결 해제 (동결 해제는 이미 5회 — `PLAN-SoT.md:381`) |
| 비용 | 계약 op 신설 1 + 화면 1 + 권한 설계(누가 연구실을 만드는가 — 정본에 주체 규정 없음) + 501 표 증감 |
| 잔존 문제 | 「연구실을 만드는 사람」이 어느 연구실에도 속하지 않은 상태 — 스코프 커널이 `lab_id = NULL` 로 **기본 거부**(`db/platform/schema.sql:38-40`). 인증 모델 자체를 고쳐야 한다 |

### 2.3 ㈐ 픽스처 연구실 전용 — 대비책 보존 (`§10`)

| 항목 | 값 |
|---|---|
| 성립 | 성립. 주체·권한·`subjects.json` 무수정 |
| 정본 저촉 | 미저촉 |
| 롤백 | 백업 복원 1회 |
| 잔존 문제 | **표시명이 시험용 문자열로 잔존한다** — `d1_lab.name = 'A 연구실'`(`seed.sql:13`), `d1_lab_profile.university = 'A 대학교'`(`seed.sql:18`). 화면 수정 수단 부재(`updateLab` 501 — `not_implemented.py:58`)이므로 **SQL 직접 UPDATE 외 방법이 없고, 그것을 하는 순간 ㈎ 와 같은 성격의 화면 밖 조작이 된다**. 그리고 `〈52〉` 의 귀속(고려대학교 수문학연구실·전창현 교수)이 화면에 표시되지 않는다 |
| 대조군 | 연구실 B 만 남아 대조군이 1개로 줄어든다 |

---

## 3. 실연구실 C — 생성 수단 확정

### 3.1 소유 디렉터리 — ★ `infra/staging/`

**확정 = `infra/staging/provision-lab.sh` (+ 동반 SQL).** 근거 3건

1. **`db/platform/versions/` 는 마이그레이션 체인이다** — `0001`~`0006` 전부 스키마 변경이며(`db/platform/versions/`), 데이터 행을 넣으면 `schema-diff` 게이트가 보는 대상이 흐려진다
2. **`db/platform/` 자체가 「스키마 정본」으로 선언돼 있다** — `infra/staging/db-bootstrap.sh:4`
   (「스키마 정본은 db/{platform,ai}/schema.sql 이고 그것을 적용하는 절차는 alembic 체인이다. 여기서 CREATE TABLE 을 하면 정본이 둘이 된다」)
3. **선례가 `infra/staging/` 에 있다** — `db-bootstrap.sh` 가 이미 「롤과 데이터베이스만 만든다」는 **운영 기반 구성**을 담당한다(`infra/staging/db-bootstrap.sh:2`). 연구실 신설은 같은 층의 작업이다

### 3.2 `seed.sql` 과 분리하는 근거

- 시험 픽스처와 운영 기반 구성을 한 파일에 두면 **공개 staging 에 픽스처 값이 반입되는 사고**가 반복된다 — `dev-package/PLAN-SoT.md:371` (`〈93〉`)
- `03-HANDOFF.md:20` 진입조건 ④ — 「주체 표에 레포 픽스처 토큰이 없다. 있으면 그 자체가 사고다」
- **분리 형태** — `services/core-api/tests/fixtures/seed.sql` 무수정. 신규 파일만 추가

### 3.3 실연구실 표시명의 정본 근거

- `dev-package/SEED-DATA.md:5` — 「**귀속 연구실** = **고려대학교 수문학연구실 (전창현 교수)** — `〈52〉`. **v2 의 첫 실제 연구실**이고, 모든 시드 데이터셋의 `lab_id` 가 여기를 가리킨다」
- `dev-package/03-HANDOFF.md:158` — 「〈52〉 초기 데이터 귀속 = 고려대학교 수문학연구실(전창현 교수) — v2 의 첫 실제 연구실」

| 열 | 값 | 근거 |
|---|---|---|
| `d1_lab.name` | 고려대학교 수문학연구실 | `SEED-DATA.md:5` · `〈52〉` |
| `d1_lab_profile.university` | 고려대학교 | 같음 |
| `d1_lab_profile.principal_investigator` | 전창현 | 같음 |
| `d1_lab_profile.department` | `[원천 무근거]` → **NULL** | 정본·원천에 학과 기재 없음 |
| `d1_lab_profile.research_field` | 수문학 | `〈52〉` 의 「수문학연구실」에서 직접 도출 |
| `d1_lab_profile.introduction` | **NULL** | 근거 없음. 지어내지 않는다 (`㊴-②`) |
| `d1_lab_profile.default_visibility` | `열림` | 스키마 기본값 — `db/platform/schema.sql:104-105` |

### 3.4 최소 필수 행 구성

**ID 규격** — `ulid` 도메인 = `char(26)`, Crockford Base32(`^[0-9A-HJKMNP-TV-Z]{26}$`) — `db/platform/schema.sql:35-36`.

**ID 확정값 (2026-08-26 Ted 판정 ① — 결정론적 ULID)**

| 대상 | 값 | 길이 |
|---|---|---:|
| `d1_lab.id` (연구실 C) | `00000000000000000000HYMETS` | 26 |
| `d1_account.id` (교수 1인) | `000000000000000000HYMETSP1` | 26 |

- **형식 유효성 실측** — 두 값 모두 `contracts/schemas/common.json:11` 의 `^[0-9A-HJKMNP-TV-Z]{26}$` 와 `services/core-api/src/colab_core/kernel/ids.py:17` 의 같은 정규식에 **일치**(`re.match` 실행 확인). 길이 26
- ⚠ **`L`·`I`·`O`·`U` 를 쓰지 않는다** — Crockford Base32 제외 문자다. 초안에서 검토한 `…LABC` 형태는 `L` 때문에 **패턴 위반**이고 실행 확인으로 거짓임이 드러났다. 그 안은 기각한다
- **`HYMETS` 의 근거** — 지어낸 문자열이 아니다. `〈53〉` 이 PoC 시드에서 확정한 `organization = 'Korea University HyMetS'` 이고, `〈52〉` 의 고려대학교 수문학연구실과 같은 실체다
- **픽스처와의 구별** — 픽스처는 `0`×25＋`A`/`B`(`services/core-api/tests/fixtures/seed.sql:13-14`) 및 `…AP1`·`…A1`·`…BP1`(`:22-24`). 접미사 `HYMETS`·`HYMETSP1` 은 육안·`grep` 양쪽에서 구별된다
- **결정론의 목적** — `provision-lab.sh` 재실행 시 같은 값이 나온다. **연구실 행도 멱등 판정 대상에 든다**(`§5.4-㈁`)

연구실 C 의 ID 는 **픽스처 ID 와 육안으로 구분되는 값**을 쓴다. 값은 스크립트에만 두고 문서에 적지 않는다.

| 테이블 | 행 수 | 필수 열 | 근거 |
|---|---:|---|---|
| `d1_lab` | **1** | `id`, `name`, `opened_at` | `seed.sql:12-14` |
| `d1_lab_profile` | **1** | `lab_id` (나머지 NULL 허용, `default_visibility` 기본값) | `db/platform/schema.sql:97-108` |
| `d1_account` | **1 이상** | `id`, `lab_id`, `name`, `email` — `UNIQUE (lab_id, email)` | `db/platform/schema.sql:111-118` |
| `d2_member_role` | **계정 수와 같다** | `role IN ('교수','연구원')` — 계정 1:1 (PK 가 `account_id`) | `db/platform/schema.sql:128-133` |
| `d2_permission_switch` | **아래 참조** | `switch` 4값 CHECK | `db/platform/schema.sql:139-147` |

### 3.5 `d2_permission_switch` 최소 필수 조합 — 확정

**교수 계정 1인만 두면 `d2_permission_switch` 행은 0 이다.**

- 근거 — `db/platform/schema.sql:137-138`: 「**교수는 네 스위치가 항상 켜진 것으로 취급한다**(P-5) — 저장이 아니라 판정이라 컬럼을 두지 않는다」
- 픽스처도 같은 구조 — `seed.sql:31` (「교수는 저장하지 않는다 — 판정이다 (P-5)」)

**연구원 계정을 함께 두면 4행이 필요하다** — 기본값이 스위치 성격을 따른다(P-4, `db/platform/schema.sql:136`)

| `switch` | `enabled` |
|---|---|
| `업로드·편집` | `true` |
| `프로젝트 생성` | `true` |
| `승인 위임` | `false` |
| `연구실 설정` | `false` |

★ **권고 = 교수 계정 1인만 생성.** 근거 — S2 적재는 업로드·등록·계보 확정만 하며, 교수는 네 스위치가 켜진 것으로 판정되므로 권한 부족이 발생하지 않는다. 계정 수를 줄이면 `subjects.json` 회전 대상도 1개다.
**Ted 판정 필요** — 연구원 계정을 함께 둘지 (`§11-③`).

---

## 4. 조사 2 — 주체 추가 절차

### 4.1 재기동 필요 여부 — **필수**

- `SubjectRegistry.from_file` 은 **`create_app` 안에서 1회만 호출된다** — `services/core-api/src/colab_core/app/main.py:43`
  (`app.state.subjects = SubjectRegistry.from_file(settings.subjects_file)`)
- 파일을 읽는 지점은 그 한 곳 — `services/core-api/src/colab_core/kernel/auth.py:36`
- 요청 처리 경로는 **메모리 표만 조회**한다 — `auth.py:45-46`, `app/deps.py:22`
- 재읽기 엔드포인트·감시 루프 **0건**
- **판정 = 파일을 고쳐도 프로세스를 다시 띄우기 전에는 새 토큰이 401 이다.** core-api 컨테이너 재기동이 절차에 포함된다

### 4.2 파일 취급 규약 (`〈93〉` — `dev-package/PLAN-SoT.md:371`)

- 권한 **`0600`**, 소유자 **컨테이너 uid `10001`** — `dev-package/RESTART.md:73-76`
- **`0644` 로 완화하지 않는다** — 「고치는 것이 아니라 노출을 넓히는 것」(`RESTART.md:74`)
- **회전은 제자리 덮어쓰기** — 바인드 마운트는 inode 에 붙는다. `mv` 하면 컨테이너가 옛 파일을 계속 읽는다 (`PLAN-SoT.md:371` `㉰`)
- **레포 픽스처 토큰을 반입하지 않는다.** 값은 어디에도 적지 않는다
- 마운트 지점 — `infra/staging/compose.i2.yml:119`(`COLAB_CORE_SUBJECTS_FILE: /etc/colab/subjects.json`) · `:129`(`${COLAB_STAGING_SUBJECTS_FILE}:/etc/colab/subjects.json:ro`)

### 4.3 대응 구조

```
subjects.json  {  "<토큰>": { "accountId": <ULID>, "labId": <ULID> }  }
                        │              │                 │
                        │              ▼                 ▼
                        │        d1_account.id     d1_lab.id
                        ▼
              Subject(account_id, lab_id)   ← auth.py:39-42
                        │
                        ▼
              apply_scope(session, subject) ← deps.py:34  → RLS 경계
```

- **`labId` 를 요청에서 받지 않는다** — `auth.py:7-9`. 연구실 경계는 오직 주체에서 나온다
- `d2_member_role` 은 `subjects.json` 에 나타나지 않는다 — 역할은 DB 조회로만 판정된다
- ⚠ **표에 있는 토큰의 `accountId`·`labId` 가 DB 에 없어도 인증은 통과한다** — `SubjectRegistry` 가 DB 를 보지 않는다. 스코프만 적용되어 **결과가 빈다**. 순서를 지킨다: **DB 행 생성 → `subjects.json` 갱신 → 재기동**

### 4.4 ㈐ 채택 시 (대비책)

- 성격이 「추가」에서 **「기존 주체 승계 확인」**으로 바뀐다
- 확인 항목 — ⓐ 현 표의 주체가 연구실 A 를 가리키는가 ⓑ 그 `accountId` 가 `d1_account` 에 실재하는가 ⓒ 역할이 `교수` 인가
- **파일 무수정 · 재기동 불요.** 확인 수단은 `/api/v1/me` 200 + 본문 대조 (값은 문서에 적지 않는다)

---

## 5. 조사 3 — S2 멱등 판정 기준

### 5.1 완료 조건 원문

`dev-package/WORK-UNITS.md:250` — 「② **재실행 멱등** — 두 번 돌려도 데이터셋 수가 늘지 않는다」

### 5.2 실물 확인 — **재실행 방지 키가 없다**

| 후보 | 실물 | 판정 |
|---|---|---|
| 원천 파일 해시 | `checksum`·`sha256`·`content_hash` 열 **0건** (`db/platform/schema.sql` 전수) | 없다 |
| 데이터셋 이름 유일성 | `d3_dataset_description.name` 에 UNIQUE 없음. 인덱스는 trigram 검색용 — `db/platform/schema.sql:285-286` | 없다 |
| 파일 이름·`storage_key` 유일성 | `d3_file` 의 UNIQUE 는 **격자 축 2건뿐** — `db/platform/schema.sql:346-349` | 없다 |
| 애플리케이션 중복 검사 | `d3_catalog.py` 의 존재 확인은 `id` 조회 1건뿐 — `services/core-api/src/colab_core/domains/d3_catalog.py:44`, `:361-363` | 없다 |
| 멱등 키 | `idempotency_key` 는 **`d5_pipeline_event` 전용** — `db/platform/schema.sql:559-560`, `:572`. 데이터셋 등록 경로가 아니다 | 대상 아님 |

**판정 — 같은 파일을 두 번 올려 두 번 등록하면 데이터셋이 2건 생긴다. DB 도 코드도 막지 않는다.**

### 5.3 판정 방법 — 제안

**멱등은 시스템의 성질이 아니라 절차의 성질로 세운다.** 근거 — 재실행 방지 키를 새로 만드는 것은 스키마 변경이며 **범위 확대**(`CLAUDE.md §5`).

★ **권고 = ㉮ 계수 대조 + ㉯ 절차 재개 지점**

**㉮ 계수 대조 (판정 수단)**

적재 완료 직후와 재실행 후에 같은 값을 재고 대조한다. 관측 수단은 **화면·API 읽기 전용**.

| 항목 | 취득 경로 | 기대 |
|---|---|---|
| 데이터셋 수 | `listDatasets` 의 `total` (연구실 C 스코프) | **11 불변** *(개정 2026-08-26 `〈104〉` — 종전 15)* |
| 파일 수 | 데이터셋별 `fileCount` 합 | **139 불변** *(개정 2026-08-26 `〈104〉` — 종전 292)* |
| 계보 간선 수 | `getDatasetLineage` — ⚠ **501** (`not_implemented.py:63`) → 화면 계보 표시로 육안 대조 | **5 불변** *(무변동 — 차감 0)* |

**㉯ 절차 재개 지점** — 적재는 데이터셋 단위로 진행하고, **완료한 데이터셋 번호(`D-01`~`D-09`·`D-13`·`D-15` 열한 개)를 진행표에 기록**한다. *(개정 2026-08-26 `〈104〉` — 종전 `D-01~D-15`)* 재실행은 **기록되지 않은 번호부터** 시작한다. 이것이 재실행 방지 키의 자리를 사람이 채우는 형태다.

**㉰ 판정 문구** — 「두 번 돌려도 늘지 않는다」를 **「진행표대로 재개하면 늘지 않는다」**로 읽는다. **전량 재실행은 데이터셋을 2배로 만든다 — 그것이 실측된 성질이다.** 이 사실을 완료 보고에 그대로 적는다.

> ⚠ **Ted 판정 필요** (`§11-④`) — ㉰ 의 읽기가 완료 조건 ②의 충족으로 성립하는가. 성립하지 않으면 **재실행 방지 키 신설이 WU 로 서야 하고, S2 는 그 WU 뒤로 밀린다.**

> **⟨개정 2026-08-26 — `〈106〉`⟩ 위 `§5.3` 은 대체됐다. 원문은 무수정 보존한다.**
> **대체 사유** — `㉮ 계수 대조`·`㉯ 진행표`·`㉰ 판정 문구` 는 **사람이 진행표를 정확히 적었을 때만** 성립하고, 진행표가 틀리거나 없으면 전량 재실행이 데이터셋을 2배로 만든다. `㉰` 가 그 성질을 정직하게 적었으나 **완료 조건 ② 를 충족시키지는 않는다.**
> **보존하는 이유** — `㉮` 의 계수 대조 3항(11 / 139 / 5)은 **개정판에서도 4단 검증 수단으로 그대로 쓴다.** 대체된 것은 `㉯`·`㉰`(사람의 진행표에 멱등을 맡기는 부분)다.
> **확정 설계는 `§5.4`.**

### 5.4 멱등 설계 — 확정 (2026-08-26 Ted 판정 ㈑ · `〈106〉`)

#### 5.4.1 귀속처 = 적재 도구. 제품 계층 무변경

**확정** — 멱등은 **적재 도구가 자기 재실행에 대해 보장**한다. 제품(계약·스키마·core-api)은 손대지 않는다.

**판단 근거 3건**

1. **사용자가 같은 파일을 두 번 올려 두 데이터셋을 만드는 것은 정상 사용이다.** 이름·해시 전역 유일 제약은 그 정상 동작을 차단한다 → 제품 계층 멱등은 부적합
2. 완료 조건 ②(`dev-package/WORK-UNITS.md:250`)의 **「재실행」은 적재 도구의 재실행**이다. 제품의 중복 방지 요구가 아니다
3. **계약 개정 0 · 마이그레이션 0 · 제품 코드 변경 0** — 동결 해제 회차를 늘리지 않는다(`〈98〉` 이 회차 수를 정직하게 세라고 못 박았다)

**기각안 2건 — 완곡하지 않게 적는다**

| 안 | 내용 | 기각 사유 |
|---|---|---|
| ㈏ | 제품 계층 멱등 — `d3_dataset_description.name` 또는 원천 해시에 전역 UNIQUE 신설 | **기각.** 사용자의 정상 동작(같은 파일로 두 데이터셋)을 차단한다. 시드 적재 편의를 위해 제품의 허용 동작을 좁히는 것이라 방향이 거꾸로다. 마이그레이션·계약 개정도 함께 따라온다 |
| ㈐ | 완료 조건 ② 를 「진행표대로 재개하면 늘지 않는다」로 개정 | **기각.** 위험이 해소되지 않는다 — 진행표를 잘못 적거나 잃으면 전량 재실행이 그대로 2배를 만든다. 완료 조건을 실측 가능한 성질에서 사람의 기록 정확도로 옮기는 개정이다. **완료 조건 ② 원문을 유지한다** |

#### 5.4.2 조회 기준 — 확정

**기준 = `listDatasets` 전수 순회 + `name` 완전 일치.**

| 판정기준 | 확정 | 근거 |
|---|---|---|
| 사용 op | **`listDatasets`** (`contracts/seams/fe-core.yaml:438`) | 계약 기존 op. 신설 0 |
| 대조 필드 | `DatasetRow.name` — **완전 일치(문자열 동등)** | `contracts/seams/fe-core.yaml:2390-2398` — `name` 은 required |
| 연구실 조건 | **요청에 싣지 않는다.** 스코프 커널이 자동 주입 | `CLAUDE.md §3-5` · `services/core-api/src/colab_core/app/deps.py:34` (`apply_scope`) · `kernel/auth.py:7-9` (「`labId` 를 요청에서 받지 않는다」) |
| 파일 구성 대조 | **불요** — 이름 11건이 전수 상이하다(`§5.4.3`) | 이름만으로 구별이 성립하므로 조건을 더하지 않는다 |
| 페이지네이션 | **있다.** `nextCursor` 가 `null` 이 될 때까지 순회 | `contracts/schemas/common.json:211-223` (`ListEnvelope` — `items`·`totalCount`·`nextCursor` required) · 페이지 크기 **20** (`services/core-api/src/colab_core/app/routes/catalog.py:37` `PAGE_SIZE = 20`) |
| 소프트 삭제분 | 조회에서 빠진다 | `services/core-api/src/colab_core/domains/d3_catalog.py:235` (`WHERE d.deleted_at IS NULL`) |

⚠ **`listDatasets` 에 이름 질의 파라미터가 없다.** 조건 파라미터는 `cursor`·`sortColumn`·`sortOrder`·주제·Level·업로더·계보·Verified 뿐이다(`contracts/seams/fe-core.yaml:445-453`). **따라서 도구가 전 페이지를 받아 이름 대조를 도구 안에서 한다.** 서버에 이름 조건을 새로 여는 것은 계약 개정이므로 하지 않는다.

**`searchDatasets` 기각 — 조회 수단으로 쓰지 않는다** (종전 판정문이 「기존 `listDatasets`·`searchDatasets`」로 둘 다 지목했으나, 실물 확인 결과 후자는 판정 수단으로 성립하지 않는다)

1. **부분 일치다.** `tsvector` 는 접두 매칭(`…::text || ':*'`, `d3_catalog.py:233`), 보조 팔은 trigram 유사도 **0.3 문턱**(`d3_catalog.py:253` `TRGM_THRESHOLD = 0.3`). `D-03`~`D-06` 은 접두 `GK-2A NDVI 1 km 충청권 — ` 를 공유하므로 **서로를 「이미 있다」로 오판할 수 있다**
2. **ai-service 에 의존한다.** 도달 실패 시 **`503` + `SEARCH_UNAVAILABLE`**(`contracts/seams/fe-core.yaml:594-602`). 멱등 판정이 AI 가용성에 걸린다
3. `CLAUDE.md §3` — AI 없이도 v2 는 완결된 제품이다. **적재 도구의 안전장치를 AI 경로에 매다는 것은 그 성질에 어긋난다**

#### 5.4.3 이름 구별 가능성 — 11건 전수 대조 (실측)

대상 = `dev-package/sessions/S2b-DATASET-DESCRIPTIONS.md:113-127` 「이름」 열(`SEED-DATA §3.1` 그대로).

| # | 이름 | 완전 일치 중복 |
|---|---|---|
| D-01 | GK-2A NDVI 2 km 한반도 (2024-06-15~24) | 없음 |
| D-02 | DEM 100 m 충청권 | 없음 |
| D-03 | GK-2A NDVI 1 km 충청권 — Nearest | 없음 |
| D-04 | GK-2A NDVI 1 km 충청권 — Bilinear | 없음 |
| D-05 | GK-2A NDVI 1 km 충청권 — IDW | 없음 |
| D-06 | GK-2A NDVI 1 km 충청권 — Co-Kriging | 없음 |
| D-07 | HSR 레이더 합성 반사도 (Lv.0) | 없음 |
| D-08 | RN15 지상 강수 (Lv.0) | 없음 |
| D-09 | GK2A/AMI NDVI 원자료 (Lv.0) | 없음 |
| D-13 | MODIS MOD15A2H FPAR/LAI 견본 | 없음 |
| D-15 | HSR 레이더 견본 | 없음 |

**판정 = 완전 일치 중복 0건. 11건이 서로 다른 문자열이다.** 조회 기준 보강 불요.

⚠ **유사명은 있다. 그것이 `searchDatasets` 를 기각한 실물 근거다.**

- `D-03`~`D-06` — 접두 `GK-2A NDVI 1 km 충청권 — ` **24자 공유**, 뒤 낱말만 다르다
- `D-01` 대 `D-03`~`D-06` — `GK-2A NDVI ` 공유, 해상도·영역이 다르다
- `D-07` 대 `D-15` — `HSR 레이더 ` 공유
- `D-09` 대 `D-01` — 표기가 `GK2A/AMI` 와 `GK-2A` 로 갈린다. **완전 일치 기준에서는 서로 다른 문자열이다**

**픽스처 이름과의 충돌은 판정 대상이 아니다** — RLS 가 연구실 경계로 잘라 연구실 C 주체의 `listDatasets` 응답에 A·B 행이 들지 않는다(`db/platform/schema.sql:791-799` · `§6.2-㉠`).

#### 5.4.4 부분 적재 재개 — 확정

**실물 확인 — 「데이터셋 생성 성공 · 파일 첨부 실패」·「데이터셋 생성 성공 · 계보 확정 실패」는 `createDataset` 경로에서 발생하지 않는다.**

- `services/core-api/src/colab_core/app/routes/ingestion.py:390-391` — 「**한 요청 = 한 트랜잭션이라 전환 + 파일 + 계보 + 프로젝트가 통째로 서거나 통째로 없다**(음성 시험 ㉰ 등록 원자성). 중간에 하나라도 실패하면 D3 에 반쪽 행이 남지 않는다」
- 계보 부모는 `DatasetCreate.lineageParents` 로 **같은 요청에 실린다** — `contracts/seams/fe-core.yaml:2353-2358`
- 업로드 재사용도 제품이 막는다 — `record.registered_at` 확인 + `ledger.mark_registered` 행 잠금으로 **409**(`ingestion.py:423-424`, `:437-440`). 같은 `uploadId` 로 데이터셋이 둘 생기지 않는다

**따라서 남는 부분 상태는 2종뿐이다.**

| 상태 | 실물 | 판정 수단 | 재개 처리 |
|---|---|---|---|
| **㈀ 업로드 접수 성공 · 등록 전환 미실행** | D3 에 행 0. `createUpload` 는 저장하지 않는다(`contracts/seams/fe-core.yaml:215-216`) | `listDatasets` 에 그 이름이 **없다** | **그 데이터셋을 처음부터 재실행.** 앞 업로드는 수명 만료로 소멸한다(`〈67〉-ⓐ`) — 도구가 정리하지 않는다. **데이터셋 수는 늘지 않는다** |
| **㈁ 등록 전환 성공 · 격자 후주입(회차 ⑦) 미완** | 데이터셋 존재 · `fileCount` 미달 | `DatasetRow.fileCount` 를 계획 파일 수와 대조 (`contracts/seams/fe-core.yaml:2399-2402`) | **건너뛰지 않고 이어서 완성한다** — `attachUploadGridFiles`(`services/core-api/src/colab_core/app/routes/ingestion.py:554`)로 격자만 붙인다 |

**판정 로직 (데이터셋 1건당, 적재 직전 1회)**

```
listDatasets 전 페이지 순회 → {name: (datasetId, fileCount, lineageState)}
  이름 없음                                   → 적재한다 (㈀ 포함)
  이름 있음 · fileCount = 계획치 · 계보 일치   → 건너뛴다
  이름 있음 · fileCount < 계획치              → ㈁ — 격자만 이어 붙인다
  이름 있음 · fileCount > 계획치              → 중단하고 보고한다 (원인 미상)
  이름 2건 이상                                → 중단하고 보고한다 (멱등 위반이 이미 일어났다)
```

- **`fileCount` 를 손으로 세지 않는다** — 트리거가 유지하는 값이다(`㊼` · `db/platform/schema.sql:351-352`)
- **`listDatasetFiles` 는 판정에 쓰지 않는다.** `listDatasets` 한 번의 순회로 `name`·`fileCount`·`lineageState` 셋이 함께 온다. 파일 단위 대조가 필요한 때만 부른다(`contracts/seams/fe-core.yaml:695`)

**계보 판정 수단 — `getDatasetLineage` 를 쓰지 않는다**

- `getDatasetLineage` 는 **501** — `services/core-api/src/colab_core/app/routes/not_implemented.py:63`
- 대체 = **`DatasetRow.lineageState`** — `DatasetRow` 의 required 필드이고(`contracts/seams/fe-core.yaml:2390-2391`, `:2424`) 값은 4값 enum `확정 | 확인 필요 | 기록 없음 | 원천`(`contracts/schemas/common.json:154-159`)
- 기대값 — `D-03`~`D-06` = `확정`(부모가 실린 상태로 등록됐다) · `D-01`·`D-02`·`D-07`~`D-09`·`D-13`·`D-15` = `원천` 또는 `기록 없음`
- ⚠ **간선 수 5 자체를 API 로 셀 수 없다.** 셀 수 있는 것은 데이터셋별 상태값이다. 간선 수 대조는 **화면 계보 표시 육안**(`§5.3-㉮` 3행)으로 남는다 — 이 한계는 종전판과 같고 해소되지 않았다

**삭제 경로 — 존재하지 않는다**

| op | 상태 | 근거 |
|---|---|---|
| `deleteDataset` | **501** `NOT_IMPLEMENTED_P1` | `services/core-api/src/colab_core/app/routes/not_implemented.py:59` |
| `getDatasetDeletionImpact` | **501** `NOT_IMPLEMENTED_P1` | 같은 파일 `:60-61` |

- **귀결 = 「삭제 후 재생성」은 재개 처리의 선택지가 아니다.** 도구가 API 로 지울 수 없다
- 지우는 경로는 **소유자 롤 DB 조작뿐**이고, 그것은 회차 롤백 `R3`(`§8` 3-B)의 절차이지 도구의 동작이 아니다. **도구에 DB 삭제를 넣지 않는다** — 넣으면 `㊾-③` 을 정면으로 깬다

#### 5.4.5 완료 조건 ② 판정 방법 — 확정값

`dev-package/WORK-UNITS.md:250` 원문 「② **재실행 멱등** — 두 번 돌려도 데이터셋 수가 늘지 않는다」 **무개정.**

**판정 절차**

| # | 행위 | 기대 |
|---|---|---|
| 1 | 1회차 적재 완료 후 `listDatasets` 전수 순회 | `totalCount` = **11** · 이름 11건 · `fileCount` 합 **139** |
| 2 | **적재 도구를 인자 없이 전량 재실행** (진행표·재개 지점을 주지 않는다) | 도구가 11건 전부를 「이미 있다」로 건너뛴다. **API 쓰기 호출 0건** |
| 3 | 재실행 후 `listDatasets` 전수 순회 | `totalCount` = **11 불변** · `fileCount` 합 **139 불변** · `lineageState` 11건 불변 |
| 4 | 화면 계보 표시 육안 | 간선 **5 불변** (`getDatasetLineage` 501 이라 API 대조 불가) |

- **판정 = 2 와 3 이 모두 성립.** 특히 **2 의 「쓰기 호출 0건」이 이 개정의 핵심이다** — 종전 `§5.3-㉰` 는 전량 재실행이 2배를 만든다는 사실을 인정하고 시작했고, 개정판은 **전량 재실행이 안전해야 성립한다**
- **사람의 진행표는 멱등의 근거가 아니다.** 진행 기록은 작업 추적용으로 남기되, 완료 조건 ② 는 **진행표 없이** 판정한다

---

## 6. 조사 4 — 픽스처 철거 시 `d8_activity` 제약

### 6.1 트리거 범위

```
db/platform/schema.sql:62-68   deny_update_delete()  → RAISE EXCEPTION 무조건
db/platform/schema.sql:633-635 d8_activity_append_only  BEFORE UPDATE OR DELETE ... FOR EACH ROW
db/platform/schema.sql:646-648 d8_download_append_only  BEFORE UPDATE OR DELETE ... FOR EACH ROW
```

- **조건 없는 FOR EACH ROW 트리거.** 어떤 롤이 어떤 WHERE 로 DELETE 해도 예외가 난다
- 같은 함수를 쓰는 세 번째 테이블 — `d2_permission_change` (`db/platform/schema.sql:163-165`). **픽스처는 이 테이블에 행을 넣지 않는다**(`seed.sql` 전수 확인) → 철거 대상 아님
- 픽스처 행 — `d8_activity` **2행**(`seed.sql:99-103`) · `d8_download` **2행**(`seed.sql:105-107`)

### 6.2 처리 방법 3안

| 안 | 수단 | 성립 | 대가 |
|---|---|---|---|
| **㉠ 존치** | 철거 대상에서 뺀다 | 성립 | 픽스처 활동 4행이 남는다. **연구실 A·B 스코프이므로 연구실 C 화면에 나타나지 않는다**(RLS — `db/platform/schema.sql:791-799`). `listActivities` 는 501(`not_implemented.py:87`)이라 화면 노출 경로 자체가 없다 |
| **㉡ `TRUNCATE`** | `TRUNCATE d8_activity, d8_download` | 성립 — **행 트리거는 TRUNCATE 를 잡지 않는다**(BEFORE UPDATE OR DELETE 로만 선언) | **테이블 전체가 비므로 연구실 C 행도 같이 지워진다.** 철거를 적재 **전에** 하면 무해, **후에** 하면 실데이터 활동 기록 소실 |
| **㉢ 트리거 일시 해제** | `ALTER TABLE ... DISABLE TRIGGER` → DELETE → `ENABLE` | 성립(소유자 권한) | **감사 기록의 불변성을 손으로 끈다.** `㉘`(「고칠 수 있는 감사 기록은 감사 기록이 아니다」 — `db/platform/schema.sql:61`)의 취지에 정면으로 걸린다. 해제 상태로 복구를 잊으면 조용히 남는다 |

★ **권고 = ㉠ 존치.** 근거 3건

1. **RLS 가 이미 격리한다** — `d8_activity`·`d8_download` 는 `FORCE ROW LEVEL SECURITY` + `lab_boundary` 정책(`db/platform/schema.sql:791-799`). 연구실 C 주체에게 A·B 행은 존재하지 않는 것과 같다
2. **화면 노출 경로가 없다** — `listActivities` 501, 열람 기록 화면 없음(`db/platform/schema.sql:620`)
3. **㉢ 은 감사 불변성을 스스로 깨는 선례가 된다.** ㉡ 은 적재 후에 쓰면 실데이터 기록을 지운다

### 6.3 연구실 단위 삭제가 성립하는 순서

**㉠ 을 채택하면 연구실 A·B 행 자체를 지울 수 없다** — `d8_activity.lab_id`·`d8_download.lab_id` 가 `d1_lab(id)` 를 FK 로 잡는다(`db/platform/schema.sql:625`, `:640`).
**연구실 행 존치가 확정 결과다.** 지우는 것은 **D3~D8 시험 데이터**뿐이며, 운영 기반(D1·D2)과 `d8_*` 는 남는다.

**철거 순서 (FK 역방향 · 자식 → 부모)** — `seed.sql` 삽입 순서의 역순이 성립 순서다

```
1. d6_project_dataset        (seed.sql:93-97)
2. d6_project                (seed.sql:89-91)
3. d4_lineage_edge           (seed.sql:83-87)
4. d2_verified               (seed.sql:79-81)
5. d2_dataset_access         (seed.sql:75-77)
6. d3_file                   (seed.sql:68-72)   ⚠ file_count 트리거 발화 — ㊼
7. d3_dataset_autometa       (seed.sql:55-58)
8. d3_dataset_description    (seed.sql:50-53)
9. d3_dataset                (seed.sql:38-45)
── 여기까지가 「시험 데이터」. 아래는 지우지 않는다 ──
   d8_activity · d8_download  → ㉠ 존치 (append-only)
   d2_permission_switch · d2_member_role · d1_account · d1_lab_profile · d1_lab → 운영 기반, 존치
```

- **`d3_dataset.deleted_at` 묘비 열이 있다**(`d3_catalog.py:44` 의 `deleted_at IS NULL`). 소프트 삭제로 끝내면 행은 남고 조회에서만 빠진다 — **철거 방식(하드 DELETE 대 묘비)도 Ted 판정 대상** (`§11-⑤`)
- **RLS 아래에서는 두 연구실 행을 한 번에 지울 수 없다** — 소유자·superuser 로 실행한다(`seed.sql:1-2` 와 같은 이유)
- ⚠ **`d3_file` DELETE 는 `file_count` 트리거를 발화시킨다**(`㊼` — `db/platform/schema.sql:351-352`). 순서를 지켜 `d3_file` 을 `d3_dataset` 보다 먼저 지운다

---

## 7. 적재 대상 — 실측 건수

### 7.1 총계

| 항목 | 수 | 근거 |
|---|---:|---|
| **데이터셋** | **11** | `SEED-DATA.md` §3.1 개정표 — `D-01`~`D-09`·`D-13`·`D-15`. *(개정 2026-08-26 `〈104〉` — 종전 15)* |
| **파일** | **139** | `SEED-DATA.md` §2 개정표 「① 넣는다」. *(개정 2026-08-26 `〈104〉` — 종전 292)* |
| 계보 간선 (최소) | **5** | `SEED-DATA.md` §4.1 — D-01→D-03·04·05·06 (주입력 4) + D-02→D-06 (보조입력 1). **차감 0** — 여섯 데이터셋 전부 적재 대상 |

**적재 제외 4건** (`〈104〉`) — `D-10` 4 · `D-11` 2 · `D-12` 141 · `D-14` 6 = **153**. 분류 `①-b stage 2 재판정 대기`. 원천 보존.

**전체 배정 대조 (재작성)** — 139(넣는다) + **153(`①-b` 재판정 대기)** + 148(시험용) + 1,682(넣지 않는다) = **2,122** (`SEED-DATA.md` §2 개정표)

> **종전 식은 292 + 148 + 1,682 = 2,122 였다.** 제외분 153 은 ② 시험용으로도 ③ 넣지 않는다로도 가지 않고 **신규 분류 `①-b` 로 이동**한다 — ② 는 카탈로그에 영영 안 세우는 것, ③ 은 영영 안 넣는 것이고, `①-b` 는 판정이 stage 2 에 남아 있는 것이다.

### 7.2 묶음별 (「① 넣는다」 세부 — `SEED-DATA.md` §2)

> **⟨개정 2026-08-26 — `〈104〉`⟩ 아래 표는 종전 292 기준이며 무수정 보존이다.** ⚠ 표시 네 행(4＋2＋141＋6 = 153)은 `①-b` 로 이동한다. 잔여 139 의 내역은 `SEED-DATA §2` 개정표.

| 묶음 | 파일 수 | 포맷 |
|---|---:|---|
| 강우 Lv.0 HSR | 10 | `.bin.gz` |
| 강우 Lv.0 RN15 | 10 | `.nc` |
| 식생 Lv.0 GK2A NDVI | 31 | `.nc` |
| 식생 Lv.1 보조입력 4종 ⚠ **`①-b`** | 4 | `.tif` 등 |
| 가뭄 Lv.1 SPI/SPEI ⚠ **`①-b`** | 2 | `.gpkg` |
| 포맷견본 nc (GK2A L2 LST) ⚠ **`①-b`** | 141 | `.nc` |
| 포맷견본 hdf (MODIS) | 8 | `.hdf` (HDF4) |
| 포맷견본 tif (HLS S30) ⚠ **`①-b`** | 6 | `.tif` (이미 COG) |
| 포맷견본 bin (HSR) | 12 | `.bin.gz` |
| KWRA 다운스케일 Input | 11 | `.tif` (NDVI 10 + DEM 1) |
| KWRA 다운스케일 Output 4종 | 40 | `.tif` |
| 기준 격자 파일 `04.Lat_Lon_info` | 16 | `.npy` 14 + `.nc` 2 |
| 기타 tif | 1 | `.tif` |
| **합계** | **292** | |

### 7.3 stage 1 판 적재 경로

`dev-package/WORK-UNITS.md:250` — 「**stage 1 판 경로 = 「S-04 → 감지 → 등록 → 미리보기 → 계보 확정」.** 파싱 → COG 는 stage 2」

- **헤더 파싱이 없다** → `d3_dataset_autometa` 의 `crs`·`grid`·`period_*` 는 채워지지 않는다.
  `SEED-DATA.md` §3.1 의 `[원천 무근거]` 칸은 **S2 에서 채워지지 않는다** — 그 문서의 「S2 가 실제로 파싱할 때 채워진다」는 stage 2 로 이월된다. **이 사실을 완료 보고에 적는다**
- 격자 파일 16건은 **기준 격자 파일 종류로 등록**한다 — `〈75〉-㉮` 로 stage 1 에 복귀. `d3_file.carries_lat`·`carries_lon` 필수(`db/platform/schema.sql:352-357`)
- 격자 후주입 경로 = `attachUploadGridFiles` (`〈94〉` — `PLAN-SoT.md:373`). **원천은 격자와 본체가 함께 있으므로 S2 는 이 경로를 안 타도 된다**(`SEED-DATA.md` §3.5)

### 7.4 선행 ⛔ 2건의 영향

| 블로커 | S2 영향 | 판정 |
|---|---|---|
| **#20 지도형 미리보기 · 격자 전달 규약** (`03-HANDOFF.md:17`) | stage 1 경로에 「미리보기」가 있다. **지도형이 staging 에서 안 뜬다** | **S2 완료 조건 ①(카탈로그에서 보이고 상세가 열린다)에는 걸리지 않는다.** 지도형 미리보기는 완료 조건에 없다. **다만 적재 중 지도형이 실패해도 중단하지 않고 목록으로 남긴다** |
| **#21 `addDatasetFile` 202** (`〈98〉` — `PLAN-SoT.md:381`) | **집행 경로는 확보돼 있다**(`〈94〉` — `PLAN-SoT.md:373`). 계약이 「낼 수 없는 응답」을 선언한 상태일 뿐 | **S2 를 막지 않는다** |

---

## 8. 실행 절차 — 5단

> **전 단계 공통 중단 규칙** — 판정 항목이 하나라도 어긋나면 **다음 단으로 넘어가지 않고 보고한다**(`CLAUDE.md §4`).
> **staging 상태는 각 단 시작 전에 `RESTART.md` 로 확인한다** — `-f compose.i2.yml` 을 빼면 조용히 I2 이전으로 롤백된다(`03-HANDOFF.md:20` ①).

---

### 1단 — 백업 + 복원 리허설

**명령**

| # | 명령 | 비고 |
|---|---|---|
| 1-1 | `infra/staging/backup/backup.sh` | 두 체인(platform · ai). 산출물은 레포 밖(`IS3.md:42`) |
| 1-2 | `infra/staging/backup/verify-artifact.sh` | 산출물 자체 검사 |
| 1-3 | `infra/staging/backup/restore-rehearsal.sh` | **실 staging 백업을 일회용 인스턴스에 복원**(`restore-rehearsal.sh:3-5`). 살아 있는 DB 는 읽기만 한다 |

**확인 항목**

- 두 체인 산출물이 각각 생성됐다 (platform · ai)
- 신선도 검사 통과 — 오래된 잔존 파일이 성공으로 기록되지 않는다 (`IS3.md:90` F6c)
- 복원 후 **테이블 수 · 테이블별 행 수 · 총 행 수 · 내용 다이제스트**가 전부 일치 (`verify-restore.sh:5`)
- 일회용 컨테이너(`is3_pg_*`)가 정리됐다 (`restore-rehearsal.sh:7`)

**판정 기준** — 위 4항 전부 통과. **중단 조건 = 하나라도 실패. 이후 단계를 시작하지 않는다.**

**롤백 지점 R1** — 이 시점의 백업이 **전 절차의 복귀선**이다. 이후 어느 단에서 실패해도 여기로 돌아온다.

---

### 2단 — 픽스처 관측치 고정 (A·B 양쪽)

**수단** — 읽기 전용 조회만. 화면 또는 API GET. **변경 0.**

**고정표 양식** (`§8.6` 에 전문)

| 판정기준 | 연구실 A | 연구실 B |
|---|---|---|
| 데이터셋 수 | | |
| 검색 `강우` 결과 건수 | | |
| 검색 `degraded` 값 | | |
| 잠김 표시 데이터셋 수 | | |
| 패싯 주제별 건수 | | |
| 파일 수 합 | | |
| 계보 간선 수 | | |
| 미리보기 ①썸네일 산출 성공 수 | | |
| 미리보기 ②비지도형 산출 성공 수 | | |
| cross-tenant 음성 — A 주체가 B 데이터셋 조회 | | |

**판정 기준** — 표의 모든 칸이 채워졌다. 빈 칸은 `[미확인]` 로 명시하고 **그 칸은 4단 대조에서 제외**한다.

**중단 조건** — 관측 중 쓰기가 발생한 정황(활동 기록 증가 등). 발생 시 R1 복원 후 재개.

**롤백 지점 없음** — 읽기 전용.

---

### 3단 — 실연구실 C 신설 + S2 적재

#### 3-A 연구실 C 신설 (화면 밖 · `㊾-③` 미저촉 — `§2.1`)

| # | 명령·행위 | 확인 |
|---|---|---|
| 3-A-1 | `infra/staging/provision-lab.sh` 작성 (`§3.1` 디렉터리 · `§3.3` 값 · `§3.4` 행 구성) | `seed.sql` 무수정 |
| 3-A-2 | **소유자 롤**로 실행 — RLS 아래 다중 연구실 조작은 경계 밖에서만 성립(`seed.sql:1-2`) | 삽입 행 수 = `d1_lab` 1 · `d1_lab_profile` 1 · `d1_account` 1 · `d2_member_role` 1 · `d2_permission_switch` 0 (교수 1인 안) |
| 3-A-3 | `subjects.json` 갱신 — **제자리 덮어쓰기**(`〈93〉-㉰`). `0600` · uid `10001` | `mv` 금지. 값은 어디에도 적지 않는다 |
| 3-A-4 | **core-api 재기동** (`§4.1` — 표는 기동 시 1회만 읽힌다) | |
| 3-A-5 | 검증 — 신 토큰이 **200 + 옳은 주체 본문**, 구 토큰이 **401** (`〈93〉-㉰`: 상태코드가 아니라 본문으로) | 음성 확인까지가 증거 |

**판정 기준** — 3-A-5 의 양성·음성 두 확인이 모두 성립.
**중단 조건** — 신 토큰이 401 이거나 구 토큰이 200. `subjects.json` 을 원본으로 되돌리고 재기동(**롤백 지점 R2**).

**롤백 지점 R2** — `subjects.json` 원본 사본 + 연구실 C 행 역순 DELETE. **이 시점의 연구실 C 는 D3~D8 데이터가 0 이므로 FK 저촉 없이 지워진다.**

#### 3-B 적재 — 화면 경로 (`㊾-③`)

**경로** = S-04 업로드 모달 → 감지 → 등록 → 미리보기 → 계보 확정 (`WORK-UNITS.md:250` stage 1 판)

**순서** — `SEED-DATA.md` §4.1 이 지목한 대로 **KWRA 묶음을 가장 먼저** 적재한다(「S2 는 이것을 가장 먼저 적재한다」). 계보·다중 부모·부모 역할 2값이 이 묶음 하나로 채워진다.

| 회차 | 대상 | 파일 수 | 계보 |
|---|---|---:|---|
| ① | D-01 · D-02 (KWRA Input) | 11 | 부모 |
| ② | D-03 · D-04 · D-05 · D-06 (KWRA Output 4종) | 40 | **간선 5** (주입력 4 + 보조입력 1) |
| ③ | D-07 · D-08 (강우 Lv.0) | 20 | 원천 |
| ④ | D-09 (식생) | 31 | 원천. *(개정 2026-08-26 `〈104〉` — `D-10` 4건 제외, 종전 35)* |
| ⑤ | ~~D-11 (가뭄 SPI/SPEI)~~ | — | **회차 소멸** — `D-11` 이 `①-b` 로 이동(`〈104〉`) |
| ⑥ | D-13 · D-15 (포맷견본) | 20 | 계보 없음(`SEED-DATA.md` §4.3). *(개정 — `D-12` 141 · `D-14` 6 제외, 종전 167)* |
| ⑦ | 기준 격자 파일 16 + 기타 tif 1 | 17 | 해당 데이터셋에 `기준 격자 파일` 종류로 부착 |
| | **합계** | **139** | **간선 5** |

> **⟨개정 2026-08-26 — `〈104〉`⟩ 회차 ⑤ 가 비었다. 번호를 당기지 않는다** — 진행표의 재개 지점 기록(`§5.3-㉯`)이 번호로 되어 있어 번호를 바꾸면 종전 기록과 대조할 수 없다.
> **회차 ①②③⑦ 은 무변동이다.** 제외분 153 은 ④⑤⑥ 에서만 빠진다.

**진행표 기록 (멱등의 재개 지점 — `§5.3-㉯`)** — 회차마다 완료한 데이터셋 번호를 기록한다. 재실행은 기록되지 않은 번호부터.

> **⟨개정 2026-08-26 — `〈106〉`⟩ 진행표는 남기되 멱등의 근거에서 내린다.** 재개 지점은 이제 **도구가 `listDatasets` 로 실측**한다(`§5.4.4`). 진행표는 작업 추적용이다. **종전 기재를 지우지 않는 이유** — `§5.3-㉯` 가 이 문장을 지목하고 있고, 대조 이력이 끊기면 개정 전후를 맞춰볼 수 없다.

**3-B-0 멱등 판정 지점 (데이터셋 1건마다 · 적재 직전)**

| # | 행위 | 분기 |
|---|---|---|
| 3-B-0-1 | `listDatasets` 를 `nextCursor` 가 `null` 이 될 때까지 순회해 `{name → (datasetId, fileCount, lineageState)}` 를 만든다 | 연구실 조건은 싣지 않는다 — 스코프 커널이 주입한다 |
| 3-B-0-2 | 이 데이터셋의 **확정 이름**(`S2b-DATASET-DESCRIPTIONS §2.1`)으로 **완전 일치** 조회 | 없음 → 3-B-1 로 적재 · 있음 → 3-B-0-3 |
| 3-B-0-3 | `fileCount` 를 회차표 계획치와 대조 | 일치 → **건너뛴다**(`createUpload` 도 부르지 않는다) · 미달 → **격자만 이어 붙인다**(`attachUploadGridFiles`) · 초과 → **중단·보고** |
| 3-B-0-4 | 이름이 2건 이상이면 | **즉시 중단.** 멱등이 이미 깨진 상태다 |

- **판정 순서를 지킨다 — 조회가 `createUpload` 보다 먼저다.** 뒤집으면 건너뛰는 경우에도 업로드가 접수되고, 그 업로드는 수명이 다할 때까지 남는다
- `searchDatasets` 를 이 자리에 쓰지 않는다 — 부분 일치·AI 의존 (`§5.4.2`)

**확인 항목 (회차마다)**

- 등록된 데이터셋 수가 계획치와 일치
- `fileCount` 가 계획 파일 수와 일치 (**손으로 넣지 않는다 — 트리거가 유지한다**, `㊼`)
- 기준 격자 파일이 `carries_lat`·`carries_lon` 중 최소 하나 `true` (`db/platform/schema.sql:352-354`)
- 계보 간선이 **확정 상태**로 남는다 (`d4_lineage_edge.confirmed_at`)
- **AI 제안은 사람이 확인한 것만 커밋한다** (`CLAUDE.md §3-2`). [모두 승인] 없음

**중단 조건 4건**

1. **감지가 포맷을 못 여는 파일이 나온다** → 그 파일을 **목록으로 남기고 회차를 끝까지 간다.** 조용히 건너뛰지 않는다
2. **`fileCount` 가 계획과 어긋난다** → 즉시 중단. 트리거가 유지하는 값이 어긋나면 원장이 어긋난 것이다
3. **cross-tenant 노출** — 연구실 C 주체로 A·B 데이터가 보인다 → **즉시 중단, R1 복원.** 경계 사고다
4. **데이터셋 수가 계획치를 초과** → 멱등 위반. 중단하고 `§5.3` 진행표로 원인 확인

**롤백 지점 R3** — 회차 단위. 실패 회차의 데이터셋을 `§6.3` 순서로 지우고 그 회차만 다시 돈다.
⚠ **`d8_activity` 는 되돌아가지 않는다** — 적재 활동 기록은 append-only 로 남는다(`§6.1`). **이것이 R3 의 한계다.**

---

### 4단 — 검증

#### 4-1 완료 조건 3항 (`WORK-UNITS.md:250`)

| # | 조건 | 판정 수단 | 기대 |
|---|---|---|---|
| ① | 카탈로그(S-03)에서 적재분이 **보이고** 상세(S-05)가 열린다 | 화면 | 데이터셋 **11** 전부 *(개정 2026-08-26 `〈104〉` — 종전 15)* |
| ② | **재실행 멱등** | **`§5.4.5` 4단 절차** — 도구 전량 재실행 후 `listDatasets` 전수 순회 대조 *(개정 2026-08-26 `〈106〉` — 종전 「`§5.3-㉮` 계수 대조」. 계수 대조 자체는 3·4행으로 살아 있고, 바뀐 것은 **재실행을 진행표 없이 돌린다**는 조건이다)* | **11 / 139 / 5 불변** ＋ **재실행 중 API 쓰기 호출 0건** *(개정 — 종전 15 / 292 / 5)* |
| ③ | **IS3 백업·복원 리허설을 적재 후 상태로 1회 재통과** | 1단과 같은 3명령 | 전 항 통과 |

> ③ 은 **복원 소요 시간이 처음 실측되는 자리**다(`IS3.md:63`, `:151`). 값을 기록한다.

#### 4-2 검색 평가셋 20건

- 대상 — `dev-package/sessions/S2b-SEARCH-EVALSET.md` (초안 20건)
- ⚠ **Ted 승인 대기 상태**(`WORK-UNITS.md:251`). **미승인분으로 채점하지 않는다**
- **기대와 실제가 갈리면 기대를 고치지 않고 불일치를 보고한다** (`S2b-SEARCH-EVALSET.md:9`)

#### 4-3 A·B 관측치 불변 확인 — 경계 격리 실증

- 2단 고정표를 **같은 수단으로 다시 재고 칸별 대조**
- **판정 기준 = 전 칸 일치.** 한 칸이라도 달라지면 경계 격리 실패
- ⚠ **`[미확인]` 로 둔 칸은 대조에서 제외한다** — 재지 않은 것을 「불변」으로 적지 않는다(`DATA-REFERENCE §0`)

**중단 조건** — 4-1 ①②③ 중 하나라도 미달, 또는 4-3 불일치. **5단으로 넘어가지 않는다.**

**롤백 지점 없음** — 검증은 읽기 전용. 실패 시 R1 또는 R3 으로 복귀.

---

### 5단 — 픽스처 A·B 철거

> **철거 단위를 둘로 가른다** — **시험 데이터(D3~D8)** 와 **운영 기반(D1·D2)**.
> `seed.sql` 이 `d1_account`·`d1_lab`·`d2_*` 를 함께 담고 있어(`seed.sql:12-36`), 일괄 철거하면 **로그인 주체가 소멸한다.**

**철거 대상 = 시험 데이터만.** `§6.3` 의 9단계 순서.

**존치 대상**

- `d8_activity` · `d8_download` — append-only(`§6.2-㉠`). RLS 로 격리, 화면 노출 경로 없음
- `d1_lab` · `d1_lab_profile` · `d1_account` · `d2_member_role` · `d2_permission_switch` — 운영 기반. **`d8_*` 가 FK 로 잡고 있어 지울 수도 없다**(`§6.3`)

**명령**

| # | 행위 | 확인 |
|---|---|---|
| 5-1 | **철거 직전 백업 1회 더** (1단과 같은 명령) | **롤백 지점 R4** |
| 5-2 | 소유자 롤로 `§6.3` 9단계 순서 실행 | 단계마다 영향 행 수를 기록 |
| 5-3 | 연구실 C 관측치 재측정 | **11 / 139 / 5 불변** *(개정 2026-08-26 `〈104〉` — 종전 15 / 292 / 5)* |
| 5-4 | 연구실 A·B 주체로 조회 | 데이터셋 0 · 검색 0건 · **500 이 아니라 정상 빈 상태** |
| 5-5 | 게이트 전수 | `./gates/run.sh` — **red 0** |

**판정 기준** — 5-3 불변 · 5-4 빈 상태 정상 · 5-5 red 0.

**중단 조건 3건**

1. **FK 저촉으로 DELETE 가 거부된다** → 순서 오류. 트랜잭션 롤백 후 순서 재확인. **제약을 풀지 않는다**
2. **연구실 C 관측치가 변한다** → 스코프 오류. **즉시 R4 복원.** 경계 사고다
3. **게이트 red** → R4 복원

**롤백 지점 R4** — 5-1 백업. 철거는 되돌릴 수 없는 유일한 단이므로 **직전 백업이 필수다.**

---

### 8.6 관측치 고정표 — 전문 양식

> 2단에서 채우고 4-3 에서 대조한다. **같은 수단으로 두 번 잰다** — 수단이 바뀌면 대조가 성립하지 않는다.

| # | 판정기준 | 취득 수단 | A (2단) | B (2단) | A (4단) | B (4단) | 일치 |
|---|---|---|---|---|---|---|---|
| 1 | 데이터셋 수 | `listDatasets` `total` | | | | | |
| 2 | 파일 수 합 | 데이터셋별 `fileCount` 합 | | | | | |
| 3 | 검색 `강우` 결과 건수 | `searchDatasets` | | | | | |
| 4 | 검색 `degraded` | 같음 | | | | | |
| 5 | 패싯 — 주제별 건수 | `listDatasetFacets` | | | | | |
| 6 | 잠김 표시 데이터셋 수 | 카탈로그 화면 | | | | | |
| 7 | Verified 표시 수 | 카탈로그 화면 | | | | | |
| 8 | 계보 간선 수 | 상세 화면 육안 (`getDatasetLineage` 501) | | | | | |
| 9 | 미리보기 ①썸네일 성공 수 | 상세 화면 | | | | | |
| 10 | 미리보기 ②비지도형 성공 수 | 상세 화면 | | | | | |
| 11 | 미리보기 ③지도형 성공 수 | 상세 화면 (⚠ ⛔ #20 — 0 예상) | | | | | |
| 12 | cross-tenant 음성 — A 주체 → B 데이터셋 | API | | | | | |
| 13 | cross-tenant 음성 — B 주체 → A 데이터셋 | API | | | | | |
| 14 | 프로젝트 수 | `listProjects` | | | | | |
| 15 | 프로젝트-데이터셋 연결 수 | `getProject` | | | | | |

**기재 규칙** — 재지 못한 칸은 **`[미확인]`**. 추정치를 적지 않는다(`DATA-REFERENCE §0`).

---

## 9. S2b 미승인분 처리 — 2안

**현황** — ✅ **승인 완료 (2026-08-26 Ted).** 설명 15건 일괄 승인 · 그중 4건은 적재 제외(`〈104〉`). 종전 기록은 아래에 보존한다.

> **⟨개정 2026-08-26 — `〈104〉`⟩ 이 절의 전제(미승인)가 해소됐다.** ★권고 ㈏(승인 후 적재)가 **그대로 성립한 상태로 진입조건이 충족**됐다 — 승인이 3단보다 앞섰다.
> **적재 대상 설명은 11건**(원천 근거 9 — `D-01`~`D-09` · 실측 도출 2 — `D-13`·`D-15`). **제외 4건의 설명은 문서에 보존**하고 stage 2 재판정 시 재사용한다.
> ⚠ **아래 「`[원천 무근거]` 4건은 빈 설명으로 적재된다」는 더 이상 성립하지 않는다** — 그 4건(`D-12`~`D-15`)의 설명은 2026-08-26 에 폴더·파일명·헤더 실측으로 채워졌고, 그중 `D-12`·`D-14` 는 적재 자체를 하지 않는다.

**종전 기록 (2026-08-25 시점 · 삭제하지 않는다)**

| 산출물 | 건수 | 내부 셈 |
|---|---:|---|
| `sessions/S2b-DATASET-DESCRIPTIONS.md` | 15 | 원천 근거 있는 설명 **11/15** · `[원천 무근거]` **4/15**(D-12·D-13·D-14·D-15) · 주제 확정 **12/15** · 주제 NULL **3/15**(D-10·D-11·D-12) |
| `sessions/S2b-SEARCH-EVALSET.md` | 20 | 기대값은 정본·판정에서만 도출 |

**현행 셈 (2026-08-26 · `〈104〉` 반영)**

| 산출물 | 건수 | 내부 셈 |
|---|---:|---|
| `sessions/S2b-DATASET-DESCRIPTIONS.md` | 15 작성 / **11 적재** | 승인 15/15 · 원천 근거 11 · 실측 도출 4 · **적재 제외 4**(`D-10`·`D-11`·`D-12`·`D-14`) |
| `sessions/S2b-SEARCH-EVALSET.md` | **18** | 종전 20 — 대상 소멸 3건 삭제(`Q-15`·`Q-16`·`Q-16b`) ＋ `Q-17` C축 이동 |

### ㈎ 빈 설명으로 적재 후 승인 시 갱신

| 항목 | 값 |
|---|---|
| 절차 | 3-B 에서 `d3_dataset_description.summary` 를 **비운 채** 등록 → 승인 후 갱신 |
| 성립 | ⚠ **갱신 수단이 없다.** `updateDataset` 은 **501** — `services/core-api/src/colab_core/app/routes/not_implemented.py:89`(`NOT_IMPLEMENTED_NO_STORE`) |
| 귀결 | 승인 후 갱신하려면 **재적재**(데이터셋 삭제 후 재등록) 또는 **DB 직접 UPDATE**(`㊾-③` 저촉) |
| 대가 | 재적재는 계보를 끊는다. 검색 색인이 빈 설명으로 한 번 돌아 **4-2 평가셋 판정이 무의미해진다** |

### ㈏ 승인 후 적재 ★권고

| 항목 | 값 |
|---|---|
| 절차 | S2b 승인을 **3단 진입조건**으로 세운다. 1단·2단은 승인 없이 진행 가능 |
| 성립 | 성립. 갱신 수단 부재 문제가 발생하지 않는다 |
| 대가 | **S2 착수가 Ted 승인 시점까지 대기.** 1·2단은 선행 가능하므로 대기는 3단부터 |
| 근거 | `WORK-UNITS.md:251` — 「승인 전까지 `K4`·`P4` 는 닫히지 않는다」. 미승인 문구로 색인하면 K4·P4 판정이 오염된 색인 위에서 이뤄진다 |

★ **권고 = ㈏.** 근거 = `updateDataset` 501 로 인해 ㈎ 의 「승인 시 갱신」이 **집행 불가**다. 갱신할 수 없는 것을 「나중에 갱신」으로 두는 것은 `CLAUDE.md §5`(「나중에」로 남기기 금지)에 걸린다.

⚠ **`[원천 무근거]` 4건은 승인 여부와 무관하게 빈 설명으로 적재된다** — 그것이 정답이다(`㊴-②`). 빈 설명을 「미승인 때문」으로 읽지 않는다.

---

## 10. 대비책 — ㈐ 픽스처 전용 (정본 인용이 `§2.1` 판정을 반증할 경우)

**교체 대상 = 3단만.** 1·2·4·5단은 그대로다.

**3단 대체 (㈐-3)**

| # | 행위 | 비고 |
|---|---|---|
| ㈐-3-1 | 연구실 A 의 **시험 데이터(D3~D8)만** `§6.3` 순서로 철거 | 운영 기반 D1·D2 존치 |
| ㈐-3-2 | 기존 주체 승계 확인 (`§4.4`) | `subjects.json` 무수정 · **재기동 불요** |
| ㈐-3-3 | 연구실 A 스코프로 S2 적재 (`§7.3` 경로 · `§8` 3-B 회차표 그대로) | |

**5단 변경** — 철거 대상이 **연구실 B 만**으로 줄어든다.
**2·4단 변경** — 대조군이 **B 하나**. 고정표의 A 열은 「적재 전/후」 비교가 아니라 「철거 대상」이 된다.

**잔존 문제** — `§2.3`. **표시명이 `A 연구실`·`A 대학교` 로 남고 고칠 수단이 없다**(`updateLab` 501).

---

## 11. Ted 판정 — 5건 (전건 확정)

| # | 항목 | 선택지 | 이 문서의 권고 |
|---|---|---|---|
| ① | 연구실 C 의 `d1_lab.id` 값 규칙 | 픽스처와 육안 구분되는 값 / 실 ULID 생성 | ✅ **닫힘 (2026-08-26)** — 결정론적 고정값 `00000000000000000000HYMETS`. 값·형식 유효성 실측은 `§3.4` |
| ② | 스크립트 소유 디렉터리 | `infra/staging/` / `db/platform/` | ★ `infra/staging/` (`§3.1` 근거 3건) |
| ③ | 계정 구성 | 교수 1인 / 교수 + 연구원 | ★ 교수 1인 (`§3.5`) |
| ④ | **멱등 판정 기준 성립 여부** | `§5.3` 절차적 멱등 수용 / 재실행 방지 키 신설 WU | ✅ **닫힘 (2026-08-26 Ted · `〈106〉`)** — **둘 다 채택하지 않았다.** 제3안 ㈑ = **멱등의 귀속처를 적재 도구로** 확정. 제품 계층 무변경 · 완료 조건 ② 무개정 (`§5.4`) |
| ⑤ | 픽스처 철거 방식 | 하드 DELETE / 묘비(`deleted_at`) | ★ 하드 DELETE — 묘비는 「철거」가 아니다 |
| ⑥ | **S2b 미승인분** | ㈎ 빈 설명 후 갱신 / ㈏ 승인 후 적재 | ✅ **닫힘 (2026-08-26 Ted)** — ㈏ 성립. 승인 완료 (`〈104〉`) |

---

## 12. 남은 `[미확인]` — 6건

| # | 항목 | 왜 미확인인가 |
|---|---|---|
| U-1 | 연구실 A·B 의 현재 실관측치 15항 | **staging 무접촉** 지시. 2단에서 실측한다 |
| U-2 | `subjects.json` 의 현 주체 구성 | `〈93〉` 이 비밀로 규정. 값을 읽지도 적지도 않는다 |
| U-3 | 적재 **139** 파일의 실제 소요 시간·용량 | 잰 적이 없다. 3단에서 처음 실측된다. *(개정 2026-08-26 `〈104〉` — 종전 292)* |
| U-4 | 복원 소요 시간 (실데이터 규모) | `IS3.md:151` — 「19행짜리 리허설로는 의미가 없다」. 4-1 ③ 에서 처음 실측된다 |

| U-5 | **계보 간선 5 의 API 대조 수단** | `getDatasetLineage` 501 (`not_implemented.py:63`). `lineageState` 로 데이터셋별 상태는 재지만 **간선 수 자체는 못 센다** — 화면 육안에 남는다 (`§5.4.4`) |
| U-6 | **적재 도구의 실동작** | 명세만 작성했다. **구현·실행이 없으므로 `§5.4.4` 판정 로직은 실측된 적이 없다** (`§14`) |

**측정하지 않은 수를 측정한 것처럼 적지 않았다** (`DATA-REFERENCE §0`).

---

## 13. 이 절차서가 만지지 않는 것

- 코드 · 계약 · 마이그레이션 · 기존 문서 — **무수정**
- `services/core-api/tests/fixtures/seed.sql` — **무수정** (`§3.2`)
- `db/platform/schema.sql` · `versions/` — **무수정**. 멱등 키를 만들지 않는다(`§5.3` · 확정 근거는 `§5.4.1` ㈏ 기각)
- **계약(`contracts/seams/fe-core.yaml`) — 무수정.** 멱등 설계가 쓰는 op 4건(`listDatasets`·`createUpload`·`createDataset`·`attachUploadGridFiles`)은 전부 기존 계약이다 (`§5.4.2`)
- append-only 트리거 — **해제하지 않는다** (`§6.2` ㉢ 미채택)
- 게이트 — 우회·비활성화 없음 (`CLAUDE.md §4`)

---

## 14. 적재 도구 명세 (2026-08-26 · `〈106〉`)

> **명세만 적는다. 구현은 이번 범위 밖이다** — 아래 요구사항은 도구를 만드는 회차의 입력이다.

### 14.1 실행 형태 — **스크립트 1건**

**판정 = 스크립트.** 수동 절차가 아니다.

**근거 4건**

1. **재실행이 완료 조건이다.** 완료 조건 ②는 **전량 재실행**을 요구한다(`§5.4.5` 2행). 수동 절차의 재실행은 사람이 11회 조회·판정을 되풀이하는 것이고, 그 판정 결과가 기록이 아니라 기억에 남는다
2. **판정이 기계적이다.** `§5.4.4` 의 분기는 이름 완전 일치와 정수 비교 둘뿐이라 사람이 개입할 자리가 없다
3. **파일 139건 · multipart 다중 파트.** `createUpload` 는 `files` 배열과 `fileKinds` 배열의 **순서 짝**을 요구한다(`contracts/seams/fe-core.yaml:230-243`). 순서 어긋남은 조용히 틀린다 — 격자 파일이 본체로 접수되고 오류가 나지 않는다
4. **재현성.** 같은 스크립트가 staging 재구축 시 같은 결과를 낸다

**소유 디렉터리** — `infra/staging/` (`§3.1` 근거 3건과 같다. `provision-lab.sh` 와 같은 자리)

**⚠ 스크립트가 대체하지 않는 것** — **S-04 업로드 모달의 실사용 시험.** 도구는 FE 코드를 지나지 않는다. `§14.3` 이 이 결손을 다룬다.

### 14.2 `㊾-③` 정합 판정 — **정합**

`㊾-③` 원문(`dev-package/PLAN-SoT.md:297`) — 「적재는 DB 직접 INSERT 가 아니라 업로드 모달(S-04) → 파싱 → COG → 계보 확정, **화면이 쓰는 경로 그대로** 간다」

**판정 = 도구가 공개 API 만 호출하는 형태로 성립한다.**

| 판정기준 | 도구 | `㊾-③` |
|---|---|---|
| DB 직접 INSERT | **0건** | 금지 — 지킨다 |
| 호출 대상 | `contracts/seams/fe-core.yaml` 의 op 4건 | 「화면이 쓰는 경로」와 같은 표면 |
| 서버 실행 경로 | `core-api` 의 같은 핸들러·같은 트랜잭션·같은 이벤트 발행 | 동일 |
| 인증·권한 | `subjects.json` 주체 토큰 → 스코프 커널 → RLS | 동일 |

**`㊾-③` 이 든 근거 ②와의 대조** — 「직접 INSERT 는 거짓 완료를 만든다 — 화면은 채워지지만 파싱·좌표계·COG·계보가 한 번도 안 돌아본 채 green 이 된다」. **도구는 이 근거에 걸리지 않는다** — `createUpload` 가 `upload.accepted` 를 발행하고(`contracts/seams/fe-core.yaml:210-212`) 감지·등록 전환·계보 확정이 전부 실제로 돈다.

⚠ **다만 결손 1건을 완곡하게 적지 않는다 — 도구는 FE 코드를 지나지 않는다.** `㊾-③` 이 이름으로 지목한 것은 「업로드 모달(S-04)」이고, 도구가 재현하는 것은 **그 모달이 부르는 서버 표면**이지 모달 자신이 아니다. 화면의 파일 놓기·종류 지정·등록 폼·계보 확정 UI 는 도구 경로에서 **한 번도 실행되지 않는다.**

### 14.3 결손 보전 요구 — 회차 ① 은 화면으로 한다

- **`D-01`·`D-02`(회차 ①, 파일 11건)를 S-04 화면으로 손수 적재한다.** 도구는 회차 ② 부터 돈다
- **근거** — 회차 ① 은 파일 수가 가장 적고(11), `SEED-DATA §4.1` 이 「가장 먼저 적재한다」고 지목한 묶음이라 **계보 부모가 여기서 선다.** 화면의 계보 확정 UI 가 실제로 도는 것을 이 회차가 증명한다
- **이후 회차 ②~⑦ 은 도구.** 회차 ② 의 계보 간선 5건은 도구가 `lineageParents` 로 싣는다 — **[모두 승인] 이 아니다.** 부모·역할은 `S2b`·`SEED-DATA §4.1` 이 이미 사람이 확정한 값이고, 도구는 그 확정값을 옮기기만 한다(`CLAUDE.md §3-2`)
- ⚠ **화면 적재분도 도구의 멱등 판정 대상이다** — 도구는 자기가 넣은 것과 사람이 넣은 것을 구별하지 않는다. `listDatasets` 에 이름이 있으면 건너뛴다

### 14.4 요구사항 — 구현 회차의 입력

| # | 요구 | 판정 |
|---|---|---|
| R-1 | 호출 op 을 **4건으로 한정** — `listDatasets` · `createUpload` · `createDataset` · `attachUploadGridFiles` | 그 밖의 op 을 부르면 근거를 명시한다 |
| R-2 | **DB 접속 문자열을 갖지 않는다** | 이 요구는 **적재 도구에만** 걸린다 — `provision-lab.sh`(`§3`)는 연구실 신설용이고 `㊾-③` 미저촉이다(`§2.1`). 적재 도구에 DB 드라이버 의존을 넣지 않으면 `㊾-③` 위반이 코드로 불가능해진다 |
| R-3 | **모든 적재 전에 `§5.4.4` 판정을 실행** | 판정을 건너뛰는 인자(`--force` 류)를 두지 않는다 |
| R-4 | **`listDatasets` 순회는 `nextCursor` 가 `null` 일 때까지** | 첫 페이지만 읽고 「없다」 하지 않는다 (`DATA-REFERENCE §0` — 한 곳만 보고 「없다」 하기) |
| R-5 | 이름 대조는 **완전 일치**. 정규화·공백 제거·대소문자 접기 **금지** | 이름은 `S2b §2.1` 확정값을 그대로 쓴다 |
| R-6 | **삭제 호출 0건** | `deleteDataset` 501 (`not_implemented.py:59`). 삭제는 도구의 동작이 아니다 |
| R-7 | 중단 조건 발생 시 **즉시 종료 + 상태 출력** | 다음 데이터셋으로 넘어가지 않는다 (`CLAUDE.md §4`) |
| R-8 | 토큰을 **인자·파일로 받고 로그·산출물에 적지 않는다** | `〈93〉` |
| R-9 | `files`·`fileKinds` **순서 짝을 단언**한다 | `contracts/seams/fe-core.yaml:231` — 어긋나도 오류가 나지 않는 자리다 |
| R-10 | 실행 기록에 **회차·데이터셋 이름·판정 결과(적재/건너뜀/이어붙임)** 를 남긴다 | 진행표를 대체하는 것이 아니라 사후 대조용이다 |
| R-11 | **재실행 시 쓰기 호출 0건**을 도구 자신이 세어 출력한다 | 완료 조건 ② 판정 2행의 증거다 (`§5.4.5`) |

