# staging 단독 창 2 — 단계 E (`S3` 실데이터 E2E ＋ 데이터 정비) 회차 기록

> 절차서 = `notes/STAGING-WINDOW-RUNBOOK.md §5`. 값·근거의 정본은 `PLAN-SoT §9 〈300〉`.
> 선행 = `sessions/WINDOW-20260903-D4.md`(복원 완주·원상복구) · `sessions/WINDOW-20260903-B2.md`(배포 `72408173be50`).
> 이 회차 범위 = **E-1 NumPy 본체 적재 · E-2 포맷 5종 공개 엣지 왕복 · E-3 기존 13건 데이터 정비 · 검사기 2종 · 단계 G 진입조건 읽기 전용 실측.**
> ⛔ 단계 G(`terraform apply`)·단계 F(롤백)는 **착수하지 않았다.**

## 0. 전제 실측 (착수 시점)

| # | 조건 | 실측 | 판정 |
|---|---|---|---|
| ㉠ | 서빙 릴리스 | `72408173be50` (`B2 §2`) · 컨테이너 8 healthy | ✅ |
| ㉡ | 베어러 | `~/.colab-v2-staging-subjects.tokens.json` (0600 · 경로만 사용 · 값 미출력) · `GET /api/v1/me` **200** | ✅ |
| ㉢ | 단계 D 선행 | **직전 회차 `〈286〉` 에서 완료**(D4 §4.4 종점 증명 8축). 이 창에는 D 가 없다 ⟹ `B2 §5-㉤` 의 no-go 사유는 **낡았다** | ✅ |
| ㉣ | NumPy 본체 원천 | `01.level-data/02.vegetation/02.vegetation/Lv.2/Prediction_20230501.npy` — 헤더 실측 `(1280, 1280)` `<f4` · 6,553,728 B | ✅ |
| ㉤ | 동봉 격자 짝 | `02.vegetation/#metadata/LAT_crop.npy`·`LON_crop.npy` — 둘 다 `(1280, 1280)` `<f8` · 각 13,107,328 B. **본체와 형상 일치** | ✅ |
| ㉥ | 적재 전 계수 | 데이터셋 **13** · `d3_file` **130** · `d5_upload_file` **143** (`B2 §3.1`) | — |

⚠ **`LAT.npy`·`LON.npy` 는 `(852, 1200)` 이라 짝이 아니다** — 헤더로 재고 쓰지 않았다(`RUNBOOK §5` 경고 그대로).
⭑ **원천은 읽기만 했다** — `.npy` 헤더 바이트를 직접 파싱했다(호스트에 numpy 미설치 · 원천 파일 무변경).

---

## 1. E-1 — NumPy 본체 적재 (업로드 모달과 같은 요청 순서)

경로의 정본 = `frontend/src/components/upload/uploadSource.ts` — `POST /uploads`(multipart · `files` ＋ 같은 순서의 `fileKinds`) → `GET /uploads/{id}` → `POST /datasets`.
**격자는 같은 접수에 `기준 격자 파일` 로 실었다** — 후주입(`POST /datasets/{id}/grid-files`)은 부르지 않았다(모달의 두 갈래 중 「한 번에 올리는」 갈래).

### 1.1 접수 — `POST /uploads` **201**

| 파일 | `fileId` | `kind` | `byteSize`(영수증) |
|---|---|---|---|
| `Prediction_20230501.npy` | `01M1JM2P8PX4XWWAKTQMN936AW` | 본체 | 6,553,728 |
| `LAT_crop.npy` | `01M1JM2P8Z8MQ6BSEQTNA2GSVY` | 기준 격자 파일 | 13,107,328 |
| `LON_crop.npy` | `01M1JM2P9A2GZ1TTSXC5N3HGRG` | 기준 격자 파일 | 13,107,328 |

**`uploadId` = `01M1JM2P8K6RRJHGY2B973VYYF`.** 영수증의 세 `byteSize` 가 **호스트 원파일 크기와 일치**한다.

### 1.2 접수 판정 — `GET /uploads/{id}` 축자

```
ready: true · renderable: true · failure: null · gridRejections: []
LAT_crop.npy  gridAxis {carriesLat: true,  carriesLon: false}
LON_crop.npy  gridAxis {carriesLat: false, carriesLon: true}
metadataComplete: false
```

**축이 파일에서 판별됐다** — 요청은 축을 보내지 않았다(`〈63〉-㉰`). 격자 거절 **0건**.
⚠ **관측 2건을 감추지 않고 적는다** — ⑴ 상태 응답의 기준 격자 파일 `byteSize` 가 **0** 이다(영수증은 실크기를 냈다). ⑵ `metadataComplete` 가 **false** 다. 둘 다 이 회차가 원인을 재지 않았다 — **`[미확인]`**. 뒤의 등록·렌더는 둘과 무관하게 섰다.

### 1.3 등록 — `POST /datasets` **201** · `datasetId` = **`01M1JM3JTQSSTKR9A2QQ67QN81`**

사람이 적는 정보(`〈280〉` 확대 3필드)를 등록 요청에 실어 보냈다 — **400 이 아니라 201 이다.**

| 칸 | 보낸 값 | 근거 |
|---|---|---|
| `name` | `GK-2A NDVI 100 m 경기남부·충청권 U-Net 예측 (Lv.2 · 2023-05-01)` | 원천 경로 ＋ `SEED-DATA §식생 사슬` |
| `topic` | `식생·NDVI` | 4값 중 하나 |
| `variables` | `["NDVI"]` | `SEED-DATA:305` 「Lv.0 (GK2A NDVI …) → Lv.1 → Lv.2」 |
| `crs` | **보내지 않았다** | 이 `.npy` 는 좌표계를 싣지 않는다 — HSR 과 같은 자리(`〈277〉`) |
| `period` | `start 2023-05-01T00:00:00Z` · `end null` | 파일명 `Prediction_20230501` |
| `summary` | 한 줄(원천 경로 ＋ `SEED-DATA:310` 축자 인용 ＋ 형상 실측) | 지어낸 문장 0건 |

### 1.4 등록 후 상세 실측 — 서버가 채운 것

```
format = NumPy · grid = 1280x1280 · crs = "WGS84 (기준 격자 파일)"
files.count = 1 · totalSizeBytes = 6,553,728 · hasReferenceGridFile = true
variables = ["NDVI"] · period = 2023-05-01T00:00:00+00:00 ~ null
```

⭑ **비워 보낸 `crs` 를 서버가 기준 격자 파일에서 세웠다** — 사람 값을 덮은 것이 아니라 **빈 칸을 채운 것**이다(`〈280〉-㉲` 의 `COALESCE` 규율 그대로). **`.npy` 본체가 `format = NumPy` 로 판정됐다.**

### 1.5 미리보기 산출물 — 공개 엣지 실측

렌더 = `POST /previews` `{target.datasetId, style.palette="다색-무지개"}` → `GET /previews/{renderId}` **`완료`**.
`precisionBadge = 동봉 격자 적용` · `colorRangeStage = 확정` · `bounds` = 126.677029~127.955045°E / 36.078551~37.356608°N.

| 층 | 주소 | 상태 | Content-Type | 매직 | 바이트 |
|---|---|---|---|---|---|
| ①썸네일 | `/previews/321830c3….webp` | **200** | `image/webp` | `52494646`(RIFF) | 8,168 |
| ②비지도형 | `/previews/eb6b1cc9….png` | **200** | `image/png` | `89504e47` | 1,030,082 |
| ③지도형(주 화면) | `/previews/384c43ef….png` | **200** | `image/png` | `89504e47` | 1,386,488 |

⚠ **①썸네일은 `image/png` 가 아니라 `image/webp` 다 — 그것이 정본이다**(`〈74〉-㉮` 「①썸네일 128 px WEBP」). 지시문의 「3층 전부 `200 + image/png`」는 **정본과 어긋나므로 실측을 적는다.** `text/html` **0건.**

---

## 2. E-2 — 포맷 5종 공개 엣지 왕복 (`S3` 잔여 조항)

주 화면이 받는 한 장(`RenderResult.imageUrl`)을 **공개 엣지 `https://www.colab-hydro.com`** 로 받아 쟀다.

| 포맷 | `datasetId` | 상태 | Content-Type | 매직 바이트 | 바이트 |
|---|---|---|---|---|---|
| **tif** | `01M0Y1VQ09TMHAC78ESDRTMAZD` (DEM 100 m 충청권) | **200** | `image/png` | `89504e47` | 1,383,032 |
| **nc** | `01M0Y1WMH6M87QFM790QPEYJ2R` (RN15 지상 강수 Lv.0) | **200** | `image/png` | `89504e47` | 16,070 |
| **gz** | `01M0Y1WM5JBDJRZ8FT3AX8HXE1` (HSR 레이더 합성 반사도 Lv.0) | **200** | `image/png` | `89504e47` | 475,020 |
| **hdf** | `01M0Y1WNXRRFBZ7XW9K9TE4352` (MODIS MOD15A2H 견본) | **200** | `image/png` | `89504e47` | 260,678 |
| **npy** | `01M1JM3JTQSSTKR9A2QQ67QN81` (**이 회차 적재분**) | **200** | `image/png` | `89504e47` | 1,386,488 |

**5/5 = `200 ＋ image/png ＋ 매직 89504e47` · `text/html` 0건.** 다섯 건 전부 렌더 `status` 는 `완료` 이고 `failure` 는 없다.
⭑ **포맷 구성이 적힌 계수다** — `〈241〉` 의 「13/13」이 못 닫던 자리가 이것이다.

---

## 3. E-3 — 기존 13건 데이터 정비 (`PATCH /datasets/{id}` · 공개 API 만)

**규칙** — 값의 출처는 ⑴ 그 데이터셋의 **설명문(`summary`)** ⑵ **파일명** 둘뿐이다. 원천이 말하지 않으면 **비운다.**
`summary` 는 **한 건도 고치지 않았다** — 12건은 이미 채워져 있었고, 비어 있는 1건은 근거가 0건이다.

| # | `datasetId` | 이름 | `variables` | `crs` | `period` |
|:--:|---|---|---|---|---|
| 1 | `01M19RENXAANZ2HDWM4Z46C37Y` | PV-1 실측 업로드 | **건너뜀** (설명문 0건 · 파일명 `pv1b_upload.tif`) | 기존 `EPSG:4326` 유지 | **비움** |
| 2 | `01M0Y1SYZ6J68XBMM91N3KX4VX` | GK-2A NDVI 2 km 한반도 | `NDVI` | `EPSG:4326` | 2024-06-15 ~ 2024-06-24 |
| 3 | `01M0Y1VQ09TMHAC78ESDRTMAZD` | DEM 100 m 충청권 | `고도(elevation)` | `EPSG:4326` | **비움**(원천 침묵) |
| 4 | `01M0YWDXN7CA13KB7MQC9PGEW5` | GK-2A NDVI 100 m 월평균 | `NDVI` | `EPSG:4326` | 2023-05-01 ~ 2023-05-31 |
| 5 | `01M0Y1WPE3G340DSYDW4Y7QNTF` | HSR 레이더 견본 | **비움** | **비움** | 2025-08-13 10:00 ~ 10:55 |
| 6 | `01M0Y1WNXRRFBZ7XW9K9TE4352` | MODIS MOD15A2H 견본 | SDS 6종 | `Sinusoidal (GCTP_SNSOID)` | 2019-09-30 ~ 2019-10-24 |
| 7 | `01M0Y1WNCHGHBGK7Y31SSY4FQV` | GK2A/AMI NDVI 원자료 (Lv.0) | `NDVI`·`EVI`·`FVC`·`DQF`·`gk2a_imager_projection` | `Lambert Conformal Conic (LCC)` | 2023-05-01 ~ 2023-05-31 |
| 8 | `01M0Y1WMH6M87QFM790QPEYJ2R` | RN15 지상 강수 (Lv.0) | `15분 누적 강수량` | **비움**(원천 침묵) | 2019-07-28 ~ 2024-07-09 |
| 9 | `01M0Y1WM5JBDJRZ8FT3AX8HXE1` | HSR 합성 반사도 (Lv.0) | `반사도` | **비움**(원천 침묵) | 2019-07-28 ~ 2024-07-09 |
| 10 | `01M0Y1WKGKVYZVCRMSPDQZ4WB5` | 1 km 충청권 — Co-Kriging | `NDVI` | `EPSG:4326` | 2024-06-15 ~ 2024-06-24 |
| 11 | `01M0Y1WKC799VBKSSA91GPWTWD` | 1 km 충청권 — IDW | `NDVI` | `EPSG:4326` | 2024-06-15 ~ 2024-06-24 |
| 12 | `01M0Y1WK7MMC24DMF9ATFD8NQT` | 1 km 충청권 — Bilinear | `NDVI` | `EPSG:4326` | 2024-06-15 ~ 2024-06-24 |
| 13 | `01M0Y1WK2YCZNQHME5JBKV5JMQ` | 1 km 충청권 — Nearest | `NDVI` | `EPSG:4326` | 2024-06-15 ~ 2024-06-24 |

**`PATCH` 호출 12회 · 전건 `200` · 4xx/5xx 0건.** 1번은 근거가 0건이라 **호출하지 않았다.**

### 3.1 근거 축자 (지어낸 값 0건 — 어디서 읽었는지)

- **`crs`** — 2·3·10~13 = 설명문 「GeoTIFF, EPSG:4326」 / 4 = 설명문 「[실측 2026-08-26] GeoTIFF · EPSG:4326」 / 6 = 설명문 「좌표계 = Sinusoidal(GCTP_SNSOID)」 / 7 = 설명문 「좌표계 Lambert Conformal Conic (LCC)」.
- **`crs` 를 비운 셋** — 5 = 설명문이 **「좌표계·변수명·단위 = [미확인]」이라고 스스로 적는다** / 8·9 = 설명문에 좌표계 문장이 없다.
- **`variables`** — 2·4·10~13 = 설명문의 NDVI 정의문 / 3 = 「고도(elevation) 격자」 / 6 = 「SDS 6 종 = Fpar_500m · Lai_500m · FparLai_QC · FparExtra_QC · FparStdDev_500m · LaiStdDev_500m」 / 7 = 「자료 구성 — DQF · EVI · FVC · gk2a_imager_projection · NDVI 총 5개」 / 8 = 「변량 15분 누적 강수량」 / 9 = 「변량 반사도」.
- **`period`** — 2·10~13 = 파일명 `…20240615…`~`…20240624…`(12·13 은 설명문도 「입력 일자 2024-06-15 ~ 2024-06-24」로 적는다) / 4 = 설명문 「자료 전달 기간 2023. 05. 01. ~ 2023. 05. 31.」 / 5 = 설명문 「기간 2025-08-13 10:00 ~ 10:55」 / 7 = 파일명 31건 `20230501`~`20230531` / 8·9 = 파일명 최소 `20190728` · 최대 `20240709`.

### 3.2 이 절이 **덧붙인** 것 두 가지 — 감추지 않는다

1. **6번의 기간은 파일명 DOY 표기를 환산한 값이다** — `A2019273`~`A2019297` → 2019-09-30 ~ 2019-10-24. 산술이지 새 사실이 아니다.
2. **시각대 표기.** 원천 문면에 시간대가 없다. 날짜만 적힌 것은 화면(`UploadModal.humanMetadata()`)이 쓰는 규약대로 `T00:00:00Z` 로, 5번처럼 시:분이 적힌 것은 그 시:분을 그대로 `Z` 로 실었다. **`Z` 는 원천이 말한 것이 아니다.**

---

## 4. 검증 — 재조회 ＋ 검사기 2종

### 4.1 상세 재조회 (14건 전수 · `PATCH` 뒤)

| 칸 | 정비 전(13건) | 정비 후(13건) | ＋신규 1건 포함(14건) |
|---|---:|---:|---:|
| `variables` 채워짐 | **1** | **12** | **13** |
| `crs` 채워짐 | **1** | **10** | **11** |
| `period` 채워짐 | **0** | **11** | **12** |

정비 전 값은 `〈280〉-㉯`(2026-09-02 읽기 전용 조회)와 이 회차 착수 시점 재조회가 **같다**.
⟹ **QA 가 `—` 로 지적하던 칸이 값으로 섰다.** 남은 공란은 **원천이 말하지 않는 자리 5칸**(1번 period · 3번 period · 5번 variables·crs · 8번 crs · 9번 crs)이고 **그 사유를 위 §3 에 이름으로 적었다.**

### 4.2 게이트 — 단독 실행 `-j 1` (`CLAUDE.md §1-b ⑸`)

| 게이트 | 결과 (축자) |
|---|---|
| `e2e-format-coverage` | **green** — `실데이터 포맷 완주 — 필수 5종 · 표식 붙은 케이스 5건 · 면제 0종` · GeoTIFF/NetCDF/Binary/HDF4/NumPy 각 `통과 1 / 관측 1` · pytest `15 passed` |
| `artifact-ownership` | **green** — `산출물 소유 — 대상 한 벌 90 · 살아 있다 57 · 접수분에만 닿는다 14 · 고아 0 · 판정 불가 19` |

`artifact-ownership` 계수 이동 (`B2 §3.1` ↔ 이 회차) —

| 축 | 배포 후(B2) | 이 회차 | 차 |
|---|---:|---:|---:|
| 대상 한 벌 | 75 | **90** | ＋15 |
| 살아 있다 | 42 | **57** | ＋15 |
| 접수분에만 닿는다 | 14 | **14** | 0 |
| **고아** | **0** | **0** | **0** |
| 판정 불가(구판) | 19 | 19 | 0 |
| `d3_file` / `d5_upload_file` | 130 / 143 | **133 / 146** | ＋3 / ＋3 |
| 지도 타일(대상 밖) | 1 | 2 | ＋1 |
| 스냅숏 | 156 줄 | 192 줄 | ＋36 |

**고아 0 유지.** `d3_file` ＋3 = 이 회차가 올린 본체 1 ＋ 기준 격자 2. 삭제 **0건**.
⚠ `판정 불가 19` 는 구판 사이드카 보류이고 **건수를 드러낸 채** 통과했다 — 이 회차가 만든 것이 아니다.
⛔ **`gates/run.sh all` 은 이 단계에서 돌리지 않았다**(지시 경계).

---

## 5. `S3` 조건별 판정 — **닫는다**

| 조건 (`completion_def` 4항) | 판정 | 근거 |
|---|---|---|
| ⑴ 5종 각각 최소 1건이 시각화 화면에 그려진다 | ✅ | 로컬 = `e2e-format-coverage` green(이 회차 재측정) · **공개 엣지 = 위 §2 표 5/5** |
| ⑵ 계보가 확정 상태로 남는다 | ✅ | `〈267〉` — `test_e2e_s3_real.py` 6건(이 회차가 다시 재지 않았다 · 종전 실측을 인용한다) |
| ⑶ 실패 파일은 목록으로 남긴다 | ✅ | 게이트가 포맷 이름으로 찍고 `[면제].formats` **0종**(이 회차 축자) |
| ⑷ 공개 엣지 포맷별 왕복 (`〈277〉` 잔여) | ✅ | 위 §2 — **포맷 이름과 함께** `200 ＋ image/png ＋ 매직` · `text/html` 0건 |

⟹ **`work-items.yaml` `S3` `status: open` → `done`.** 선행 차단 둘(`NumPy` 본체 0건 · 자격증명)이 이 회차에 **둘 다** 풀렸다.

---

## 6. 되돌림 목록 (만든 것 전수)

| 종류 | id | 되돌리는 법 |
|---|---|---|
| 업로드 | `01M1JM2P8K6RRJHGY2B973VYYF` | 수명 만료 `2026-09-04T03:12:56Z` — 등록에 소비됨 |
| 데이터셋 | **`01M1JM3JTQSSTKR9A2QQ67QN81`** | `DELETE /api/v1/datasets/{id}`(묘비) · 원장 행까지 지우려면 `RUNBOOK` 방식으로 `d3_file` 3행 ＋ `d3_dataset` 1행 |
| 파일 | `01M1JM2P8PX4XWWAKTQMN936AW`(본체) · `01M1JM2P8Z8MQ6BSEQTNA2GSVY`(LAT) · `01M1JM2P9A2GZ1TTSXC5N3HGRG`(LON) | 위와 한 벌 |
| 미리보기 산출물 | 렌더 `01M1JM4CP7XY78P56YP6B60HRF` 외 4건이 구운 캐시 벌 | 볼륨 바이트는 고아로 남을 수 있다 — **정상**(`RUNBOOK:120`) |
| `PATCH` 12건 | §3 표의 2~13번 | 되돌리려면 각 칸에 `null` 을 **명시**해 다시 `PATCH`(생략 = 그대로 두라는 뜻). **정비 전 원본은 `sessions` 밖 산출 `/tmp/e2/detail_before.json` 이 아니라 이 문서 §3·§4.1 이 정본이다** — 정비 전 값은 `variables` 1건·`crs` 1건·`period` 0건뿐이고 그 1건들은 이 회차가 **건드리지 않았다**(1번 건너뜀) |

**`DELETE` · SQL 쓰기 · 컨테이너 조작 · 파괴 플래그 · 자격증명 출력 = 전부 0건.**

---

## 7. 단계 G 진입조건 — 읽기 전용 실측 (⛔ apply 하지 않았다)

| # | 조건 | 실측 | 판정 |
|---|---|---|---|
| ⓐ | `terraform` 설치 | `Terraform v1.9.8` · `on linux_amd64` · `~/.local/bin/terraform` | ✅ |
| ⓑ | 자리 | `infra/staging/tunnel`(⚠ 런북이 적은 `infra/tunnel` 이 아니다) · `terraform.tfstate` **실재** | ✅ |
| ⓒ | 자격증명 | `~/.colab-v2-staging.env` 의 `CF_API_TOKEN`·`CF_ACCOUNT_ID`·`CF_TUNNEL_ID` 를 `TF_VAR_*` 로 주입 · **값 미출력** | ✅ |
| ⓓ | 단계 E 왕복 선행 | 위 §2 완료 | ✅ |

**`terraform plan -no-color -lock=false` 출력 축자 (전문) —**

```
cloudflare_zero_trust_tunnel_cloudflared_config.staging: Refreshing state... [id=39e9977b-875b-4cb4-8f42-a01c9e6f3cfc]

No changes. Your infrastructure matches the configuration.

Terraform has compared your real infrastructure against your configuration
and found no differences, so no changes are needed.
```

⛔ **지시가 기대한 `0 add · 1 change · 0 destroy` 가 아니다 — `No changes.` 다. 멈추고 보고한다.**
가장 그럴듯한 사유(**판정하지 않는다**) = `infra/staging/tunnel/README.md §0` 이 「**적용 완료 (2026-08-23)** · `terraform plan` = `No changes.`」라고 적는다 ⟹ `IS4 ①` 의 apply 는 **이미 한 번 집행됐고**, 지금의 `No changes.` 는 런북 `§7` 증거 ⑵(「apply 후 plan 이 문자 그대로 `No changes.`」)에 해당한다.
⟹ **단계 G 는 「apply 할 것이 없다」일 가능성이 있고, 그 판정은 이 회차의 몫이 아니다.** `destroy`·`replace`·`ingress_rule` 값 변경 **0건**(변경 자체가 0건이다).
**`apply` 는 하지 않았다. 상태 파일도 고치지 않았다**(`plan` 은 읽기 전용 · `-lock=false`).

---

## 8. 이 회차가 건드리지 않은 것

- SQL 쓰기 0건 · 컨테이너 조작 0건 · 파괴 플래그 0건 · `terraform apply` 0건 · 자격증명 값 출력 0건(경로만).
- staging 쓰기는 **전부 공개 API** — `POST /uploads` 1 · `POST /datasets` 1 · `POST /previews` 5 · `PATCH /datasets/{id}` 12. **계 19 회.**
- 단계 F(롤백 왕복)·단계 G(apply)·단계 H 미착수. `gates/run.sh all` 미실행.
- 원천 데이터(`03 Reference-Data`) 무변경 — 읽기만.
- `main` 무접촉. 이 기록은 워크트리 `lane-window-e2`(브랜치 동명)에서 커밋한다. push 없음.
