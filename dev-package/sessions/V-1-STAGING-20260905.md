# `V-1` staging 실물 증명 — `§6(vii)㉡` 팔레트 재렌더 1회 (2026-09-05 14:54~14:58 KST)

> 이 시트는 `sessions/WINDOW-20260905-A5b.md §10.11` 이 **경계 때문에 집행하지 않고 넘긴 한 자리**를 집행한 기록이다.
> **배포 없다.** 인가된 staging 쓰기는 **「팔레트 변경 재렌더」 1건**뿐이고 그 밖의 `POST`·업로드 0건이다.
> ⛔ **대장은 만지지 않았다** — `work-items.yaml`·`PLAN-SoT.md`·`03-HANDOFF.md` 무접촉. §9 등재 초안은 `notes/V1-STAGING-LEDGER-PENDING.md` 에 따로 둔다
> (지시가 부른 이름 `V1-LEDGER-PENDING.md` 는 **미등재 초안이 이미 쓰고 있어** 덮지 않았다 — 그 시트 머리에 사유를 적었다).

---

## 0. 창 진입 전 상태 (실측 · 읽기 전용)

| 축 | 실측 | 잰 법 |
|---|---:|---|
| 서빙 릴리스 | **`e37808cace69`** (A5b 단계 B 가 세운 그것) | `docker ps` |
| 컨테이너 | **8/8 healthy** (`colab_v2_staging_*`) | `docker ps` |
| `colab_v2_staging_viz_render` | `StartedAt` **2026-09-05T04:22:00Z** (= **13:22:00 KST**) · `RestartCount` **0** | `docker inspect` |
| 자리 파일 총수 | **306** — A5b 끝값과 **일치**(그 사이 아무도 쓰지 않았다) | `find ~/.colab-v2-staging/previews -type f \| wc -l` |
| 사이드카(`.json`) | **122** | 같은 자리 |
| 사이드카 없는 산출물 | **16** (A5b `§10.7` 계열 · 이 창이 만든 것 아니다) | 키 대조 |
| `tile-` 키 | **2** | 자리 목록 |
| 대상 데이터셋 | **GK-2A `01M0YWDXN7CA13KB7MQC9PGEW5`** | ↓ §1 |

### 0.1 대상 선정 — **정본이 이름을 대지 않아 지시대로 GK-2A 를 썼다**

- `A5b §6(vii)㉡` 은 「**같은 데이터셋**에 다른 팔레트로 1회」라고만 적고 **데이터셋 이름을 대지 않는다.**
- ⟹ 이 창의 지시가 정한 **GK-2A `01M0YWDXN7CA13KB7MQC9PGEW5`** 를 썼다. **정본과 어긋나지 않는다** — ㉠ 이 GK-2A 를 포함하고 있고, A5b 재굽기 3건에도 들어 있어 **살아 있는 벌이 이 프로세스의 색인 안에 있다**(회수 후보가 되는 조건).

### 0.2 재렌더 **전** GK-2A 자리 (사이드카 10벌 · 실측)

| 캐시 키(앞 16) | 층 | `created` | 딸린 확장자 |
|---|---|---|---|
| `14cdbadfbaf4b4aa` | 지도형 | 2026-09-02T13:23:28Z | json·pgw·png |
| `1b3375781cf140e2` | 비지도형 | 2026-09-03T12:40:05Z | json·png |
| `2e7d50d8c4a784d3` | 썸네일 | 2026-09-02T13:23:26Z | json·webp |
| `48a4a3f686729628` | 썸네일 | 2026-09-03T12:40:05Z | json·webp |
| `4e0ac3e3819227c0` | 지도형 | 2026-09-03T16:05:54Z | json·pgw·png |
| `82327483c4240de4` | 지도형 | 2026-09-03T12:40:05Z | json·pgw·png |
| `f0f9b12ab799e2c1` | 비지도형 | 2026-09-02T13:23:26Z | json·png |
| ⭑ `ad863d60de2ff8e7` | **썸네일** | 2026-09-05T04:49:34Z | json·webp |
| ⭑ `f48dc45c5c11a81c` | **비지도형** | 2026-09-05T04:49:34Z | json·png |
| ⭑ `50dc2d4da52c0f6f` | **지도형** | 2026-09-05T04:49:35Z | json·pgw·png |

- ⭑ 3벌(**7파일**)이 **A5b 재굽기가 세운 살아 있는 벌**이고 팔레트는 `단색-파랑` 이다. **이 창이 대체할 대상은 이 셋뿐이다.**
- ⚠ 나머지 7벌은 **컨테이너 기동(13:22) 이전의 구판**이라 `_produced_for` 색인 밖이다 — **`V-1` 이 지지 않는다**(완료 정의 ⓑ ⟹ `RC-1`).

---

## 1. 이 창이 집행한 절차

1. **화면 열기(쓰기 0)** — 실제 브라우저(Chromium 헤드리스 1440×1100)로 공개 엣지 `/datasets/{GK-2A}` 를 열어 **재렌더 전** 범례·머리·콘솔을 잤다. 세션 토큰은 **파일 경로로만** 읽어 `localStorage`(`colab.session.token`)에 주입했고 **값은 이 문서·로그·대화 어디에도 남기지 않았다.** 확인 뒤 `localStorage` 를 비웠다(**잔여 0**).
2. **인가된 쓰기 1건** — 공개 엣지 `POST /api/v1/previews` **1회**, 몸통 `{"target":{"datasetId":"01M0YWDXN7CA13KB7MQC9PGEW5"},"style":{"palette":"다색-무지개","classCount":6}}`.
   - **화면이 만드는 요청과 글자 그대로 같은 모양이다** — `DatasetPreviewSection.tsx` 가 `{datasetId, palette, classCount}` 로 `create` 를 부르고 `classCount` 기본이 **6** 이다. **바꾼 것은 `palette` 한 칸뿐이다.**
3. **관측** — `GET /api/v1/previews/{renderId}` 를 20 ms 주기로 폴링하면서, **응답을 받은 그 순간마다** 옛 7파일의 실재 여부를 셌다. 동시에 별도 스레드가 자리 목록을 **5 ms** 주기로 훑어 생성·소멸에 시각을 박았다.
4. **뒷정리 없음** — 되돌리는 두 번째 팔레트 변경은 **하지 않았다**(§3 ⑸ 판정 참조).

### 1.1 ⛔ 「화면에서 팔레트를 바꾼다」가 화면 조작이 될 수 없는 이유 — **감추지 않는다**

- `PreviewControls.tsx` 의 팔레트 칸은 실물에서 **「고를 수 있는 팔레트 목록을 아직 불러올 수 없어요」** 한 줄이다(이 창의 DOM 실측 = 팔레트 컨트롤 노드 **없음**).
- 근거는 결함이 아니라 **범위**다 — `V-1` 완료 정의 ⑺ 축자 「**고르는 UI 는 `J-6` 소유**」. 화면은 언제나 `listPalettes` 의 **첫 항목**(`단색-파랑`)으로 렌더를 건다.
- ⟹ **사람이 화면에서 팔레트를 고르는 경로는 아직 없다.** 이 창은 **화면이 부르는 바로 그 중계 엔드포인트에 팔레트 한 칸만 바꿔** 넣었다. **서버측 재렌더 경로 = `V-1` 의 범위**이고 그 범위를 그대로 쟀다.

---

## 2. 원값 (raw · 시각은 KST · 마이크로초)

### 2.1 재렌더 전 화면 — 축자

```
머리    미리보기 — GK-2A NDVI 100 m 경기남부·충청권 월평균 (2023-05)
파일명  GK2A_NDVI_mean_202305.tif
격자    821 × 1024
범례    변수 / band1
        0.188 ~ 0.2776 / 0.2776 ~ 0.3673 / 0.3673 ~ 0.4569
        0.4569 ~ 0.5466 / 0.5466 ~ 0.6362 / 0.6362 ~ 0.7259
콘솔 오류 2건 — 둘 다 https://static.cloudflareinsights.com/beacon.min.js/... ERR_CONNECTION_REFUSED
자리 파일 306 → 306 (화면을 열어도 굽지 않았다 = 캐시 적중)
```

- **앱 귀책 콘솔 오류 0건.** 2건은 `QA-SCREENS-20260903 §1` 이 환경 귀책으로 분류한 그 beacon 이고 **요청 URL 로 확인했다**(추정 아님).

### 2.2 재렌더 전 렌더 응답 — 화면이 실제로 받은 값 (`renderId 01M1R27VD6MK2P89NGYK40AJRB`)

```
status   완료
palette  단색-파랑          variable  band1        classes  6
bounds   {"west":126.676219,"south":36.077845,"east":127.956574,"north":37.3582}
classes  #f0f6ff 0.18798384070396423 ~ 0.27763089040915173
         #b7d2f0 0.27763089040915173 ~ 0.36727794011433923
         #7aadde 0.36727794011433923 ~ 0.4569249898195267
         #4789c6 0.4569249898195267  ~ 0.5465720395247142
         #2463a4 0.5465720395247142  ~ 0.6362190892299016
         #0b3d75 0.6362190892299016  ~ 0.7258661389350891
layers   null   (타일 갈래 비활성 — `〈74〉-㉳`)
```

### 2.3 인가된 쓰기 1건 — 팔레트 재렌더 (`renderId 01M1R298204P32QTE2DM00ZS5R`)

```
14:57:23.452570  PRE_COUNT 306
14:57:23.452585  옛 벌 7파일 전건 실재 (50dc2d4d ×3 · ad863d60 ×2 · f48dc45c ×2)
14:57:23.685086  POST /api/v1/previews → 202 · status 그리는 중
14:57:23.755510  NEW  6258e5fe….webp        ← 썸네일 새 키
14:57:23.776490  NEW  6258e5fe….json
14:57:23.776517  NEW  fa20cea7….json        ← 비지도형 새 키
14:57:23.776522  NEW  fa20cea7….png
14:57:24.090728  POLL status 그리는 중 · stage 범례 만드는 중 · result 없음 · 옛 벌 생존 7
14:57:24.168381  NEW  fdf7586b….json        ← 지도형 새 키
14:57:24.168402  NEW  fdf7586b….pgw
14:57:24.168408  NEW  fdf7586b….png
14:57:24.173629  GONE 50dc2d4d….json
14:57:24.173640  GONE 50dc2d4d….pgw
14:57:24.173644  GONE 50dc2d4d….png
14:57:24.173648  GONE ad863d60….json
14:57:24.173652  GONE ad863d60….webp
14:57:24.173656  GONE f48dc45c….json
14:57:24.173659  GONE f48dc45c….png
14:57:24.306060  POLL status 완료 · stage 없음 · result 있음 · 옛 벌 생존 0
14:57:27.308643  POST_COUNT 306
```

**이 시간축이 말하는 것 셋.**

- ⑴ **새 것이 먼저 섰다** — 마지막 신규 파일 `24.168408` **＜** 첫 소멸 `24.173629`. **볼 그림이 하나도 없는 순간이 없다**(완료 정의 ⑷-b).
- ⑵ ⭑ **「완료」는 회수가 끝난 뒤에 섰다** — 소멸 완료 `24.173659` **＜** 화면이 「완료」를 본 `24.306060`. 그 직전 폴링(`24.090728`)은 **`그리는 중` ＋ 옛 벌 7 생존**이었다. ⟹ **`〈317〉` 의 주장이 staging 실물에서 그대로 선다** — 시험 `test_화면이_완료를_본_그_순간_옛_벌은_이미_지워져_있다` 의 실물 판이다.
- ⑶ **계약 유효성** — 회수 구간의 응답은 `status 그리는 중` ＋ `stage 범례 만드는 중` ＋ `result` 없음. `core-viz.yaml RenderJob` 안이다(`〈317〉`-㉴ 와 같은 판정).

### 2.4 재렌더 뒤 렌더 응답 — 축자

```
status   완료
palette  다색-무지개        variable  band1        classes  6
bounds   {"west":126.676219,"south":36.077845,"east":127.956574,"north":37.3582}   ← 전과 글자 하나까지 동일
classes  #3b4cc0 0.18798384070396423 ~ 0.27763089040915173
         #2fa4a0 0.27763089040915173 ~ 0.36727794011433923
         #7cc03a 0.36727794011433923 ~ 0.4569249898195267
         #f0c11a 0.4569249898195267  ~ 0.5465720395247142
         #e8622a 0.5465720395247142  ~ 0.6362190892299016
         #b31b1b 0.6362190892299016  ~ 0.7258661389350891
layers   null
```

⟹ **구간 경계 6쌍이 전후 완전 동일** · **`bounds` 4값 완전 동일** · **`variable` `band1` 동일** · **바뀐 것은 `color` 6칸뿐**. 완료 정의 ⑴ 의 「같은 데이터·같은 색범위·같은 구간 수에서 **색만 바뀐 그림**」이 **문자 대조로** 선다.

### 2.5 새 벌 · 전후 계수

| 축 | 전 | 후 | 델타 |
|---|---:|---:|---:|
| 자리 파일 총수 | **306** | **306** | **0** |
| 사이드카(`.json`) | 122 | 122 | 0 |
| GK-2A 사이드카 벌 | 10 | 10 | 0 |
| 사이드카 없는 산출물 | 16 | 16 | 0 |
| `tile-` 키 | 2 | 2 | **0 — 무접촉** |

- **델타 = (새 벌 7파일) − (지운 벌 7파일) = 0.** 지시의 계산식과 실측이 맞는다.
- 새 벌 3키 — `6258e5fe…` 썸네일 128×128(webp 5,806 · json 389) · `fa20cea7…` 비지도형 1024×1024(png 41,827 · json 393) · `fdf7586b…` 지도형 **821×1024**(png 38,082 · pgw 96 · json 633). **격자가 전 벌과 같다**(821 × 1024).
- **고아 0 · 중복 벌 0** — GK-2A 사이드카는 10벌 그대로이고 **새 3벌이 옛 3벌 자리를 정확히 대체**했다. 새로 생긴 사이드카 없는 산출물 0건.
- ⚠ 남은 구판 7벌(2026-09-02·09-03 생성)은 **이 창이 만든 것도 이 창이 지울 것도 아니다** — `RC-1` 소유(완료 정의 ⓑ).

### 2.6 `colab_v2_staging_viz_render` 로그

```
POST /viz/v1/renders                                    202 Accepted
GET  /viz/v1/renders/01M1R298204P32QTE2DM00ZS5R          200 OK   (그리는 중)
GET  /viz/v1/renders/01M1R298204P32QTE2DM00ZS5R          200 OK   (완료)
```

- **접수·조회 3줄이 전부다.** 회수 성공은 **로그를 남기지 않는 설계**다 — `jobs.py` 는 회수가 **실패했을 때만** `log.warning("팔레트 재렌더의 옛 벌 회수가 실패했다 …")` 을 찍는다.
- ⟹ **경고 0건 = 회수가 예외 없이 끝났다**는 뜻이고, 실제 집행 증거는 §2.3 의 자리 대조다. **「로그에 회수 줄이 있다」로 적지 않는다 — 없는 것이 정상이다.**
- 컨테이너 **재기동 0** · `RestartCount` **0** 유지 · 8/8 healthy 유지.

---

## 3. 완료 정의 조항별 판정 — `work-items.yaml` `V-1` `completion_def` ⑴~⑺

| 조항 | 요구 축자 | 이 창의 증거 | 판정 |
|---|---|---|---|
| **⑴** | 같은 데이터·같은 색범위·같은 구간 수에서 **색만 바뀐 그림** — `classes[].min/max` 와 `bounds` 불변 | §2.2 ↔ §2.4 문자 대조 — 경계 6쌍·`bounds` 4값·`variable` 전건 동일, `color` 6칸만 갈렸다 | ✅ **충족** |
| **⑵** | 고를 수 있는 것은 `listPalettes` 가 준 것뿐 — 목록 밖 이름은 거절(**음성 시험 1건**) | ⭑ **조항이 요구하는 합격선이 「음성 시험 1건」이고 그 시험이 실재한다** — `services/viz-render/tests/test_palette_rerender.py:84 test_목록_밖의_팔레트는_거절한다` (`"없는-팔레트"` → **400** ＋ 목록이 준 값 전건은 `완료`). A5b `§2.1` 의 326건 안에 있고 **9회 연속 green** 이다. 이 창의 staging 실측이 그 위에 얹는 것 = `GET /preview-palettes` **3항목**(`단색-파랑`·`다색-무지개`·`발산-한난`)이고 **그중 하나로 재렌더가 섰다**(§2.4) | ✅ **충족** |
| **⑶** | 옛 색이 섞이지 않는다 — 화면에 도달한 산출물의 키가 **새 키**임을 대조 | `완료` 응답이 가리키는 벌 = 새 3키 · 옛 3키는 자리에 **없다**(§2.3) ⟹ 옛 색 산출물이 도달할 자리 자체가 소멸 | ✅ **충족** |
| **⑷** | 새 `.png`(＋`.json`·`.pgw`)가 **새 키**로 서고 화면이 그 키를 가리키며 **옛 산출물은 지운다** | 새 키 3벌 7파일 실재(`.png`·`.json`·`.pgw` 전건) · 옛 벌 7파일 **전건 소멸** | ✅ **충족** |
| **⑷-b** | **새 것이 선 뒤에 옛 것을 지운다** — 볼 그림이 없는 순간이 없다 | 마지막 신규 `24.168408` ＜ 첫 소멸 `24.173629` (**≈5 ms 앞선다**) | ✅ **충족** |
| **⑷-c** | 지우는 것은 렌더 산출물뿐 — 원본·기준 격자·데이터셋·**`tile-`** 무접촉 | 소멸 7파일 전건이 GK-2A 미리보기 산출물 · `tile-` 키 **2 → 2** · 자리 밖 삭제 0 · DB 쓰기 0 | ✅ **충족** |
| **⑸** | **켠 갈래(타일)** — 통과 조건은 「무효화할 것이 없음이 **유지된다**」 | 렌더 응답 `layers null`(타일 갈래 비활성 · `〈74〉-㉳`) · 자리의 `tile-` 키 2개는 D5 소유이고 이 재렌더가 세우지도 지우지도 않았다 ⟹ **서버에 구워 두는 조각이 늘지 않았다** | ✅ **충족**(음성 조건 유지) |
| **⑹** | 두 갈래 어느 쪽이든 **staging 배포 green** | A5b `§10.2` — 릴리스 `e37808cace69` 배포 **GREEN**(통과 15 · SKIP 0 · red 0 · 롤백 미발동 · 8/8 healthy). 이 창이 그 릴리스 위에서 잤고 배포는 **하지 않았다** | ✅ **충족**(A5b 실측 승계) |
| **⑺** | 범위 밖 = 고르는 UI 는 `J-6` 소유 | 이 창은 UI 를 만지지 않았고 코드 0줄 수정 | ✅ **해당 없음(경계 준수)** |

### 3.1 계 — **8/8 충족 · 막는 조항 0건**

- 조항 계수 = ⑴·⑵·⑶·⑷·⑷-b·⑷-c·⑸·⑹ **여덟**(⑺ 은 범위 선언이라 판정 대상이 아니다 · 위반 0).
- ⚠ **⑵ 를 staging 에서 쏘지 않은 것이 미달이 아니다** — 조항이 못 박은 합격선이 **「음성 시험 1건」**이고 그 시험이 실재·green 이다. 목록 밖 이름을 실서버에 쏘는 것은 조항이 요구한 적이 없고, 이 창의 쓰기 경계(한 건)도 그것을 금한다.
- ⛔ **되돌리는 두 번째 팔레트 변경은 하지 않았다** — 완료 정의 ⑴~⑺ 어디에도 **왕복(round-trip) 요구가 없다.** 되돌리면 이 시트가 잰 상태가 지워진다.
- ⚠ **자리는 지금 `다색-무지개` 로 서 있다.** 다음에 사람이 상세 화면을 열면 화면이 `listPalettes` 첫 항목(`단색-파랑`)으로 다시 걸어 **자동으로 되돌아간다**(그리고 같은 회수가 다시 돈다). **남는 쓰레기가 아니라 자기 복원이다.**

---

## 4. `[미확인]` — 이 창이 재지 못한 것

| # | 무엇 | 왜 못 쟀나 | 무엇이 풀어 주나 |
|---|---|---|---|
| ① | **완료 정의 ⑵ 의 staging 판**(목록 밖 이름을 실서버가 400 으로 내치는가) | 인가된 쓰기가 **1건**이라 두 번째 `POST` 를 쏠 수 없다. ⚠ **완료 판정을 막는 자리가 아니다** — 조항의 합격선은 음성 시험이고 그것은 섰다(§3 ⑵) | 굳이 재려면 음성 요청 1건에 대한 명시 승인. **`V-1` 완료의 선행 조건은 아니다** |
| ② | **화면에 팔레트 갱신이 보이는가** | 팔레트 선택 UI 가 **없다**(`J-6` 미착수 · 완료 정의 ⑺). 화면은 언제나 `listPalettes` 첫 항목으로 걸어 `다색-무지개` 를 **띄울 방법이 없다** | `J-6`(팔레트 선택 UI). ⭑ 다만 **간접 근거는 섰다** — 재렌더 전 화면 DOM 범례 6줄이 §2.2 응답의 `classes` 6쌍과 **글자 대조로 일치**했다 ⟹ **화면은 받은 값을 그대로 그린다.** 변수명 `band1` 은 전후 동일 |
| ③ | **`TL-1` 관측 전용 루프의 첫 한 바퀴** | ↓ §5 | ↓ §5 |
| ④ | 회수 실패 갈래(`log.warning`)의 실물 발동 | 회수가 성공했다. 실패를 만들려면 자리를 망가뜨려야 한다(파괴적) | 시험 `test_회수가_실패해도_렌더는_완료로_선다` 가 지는 자리다 — staging 에서 잴 것이 아니다 |

---

## 5. `TL-1` 관측 전용 루프 첫 한 바퀴 — ⛔ **「못 봤다」**

| 축 | 실측 |
|---|---|
| 컨테이너 기동 | **2026-09-05T04:22:00Z** = **13:22:00 KST** · `RestartCount 0` |
| 최종 확인 | **2026-09-05 15:00 KST** ⟹ 경과 **≈98분** · 주기 기본 **3600초(60분)** |
| `docker logs` 안의 `reclaim`·`회수`·`liveness`·`타일` | **0건** |
| 등급별 계수 | ⛔ **「못 봤다」** — 0 으로 적지 않는다 |
| **삭제 건수** | ⭑ **자리 기준 0건** — 이 창의 재렌더가 지운 **인가된 7파일 말고 사라진 파일 0개**. `tile-` 키 **2 → 2 무변** |

**왜 98분인데 안 보이는가 — 후보 둘. 어느 쪽인지 이 창은 가르지 못했다 `[미확인]`.**

- ㉮ **로그 설정 귀책이 유력하다.** `services/viz-render` 코드 전수에 `logging.basicConfig`·`dictConfig`·`setLevel` 이 **0건**이다. 회수 한 바퀴의 요약은 `trigger_loop.py:118` 의 **`log.info`** 로 나가는데, 루트 로거에 처리기가 붙지 않으면 `INFO` 는 **어디에도 찍히지 않는다**(`lastResort` 는 `WARNING` 이상만 낸다). ⟹ **루프가 돌아도 `docker logs` 로는 안 보인다**가 성립한다.
- ㉯ 루프 미기동. `main.py` 는 `app.state.triggers` 가 있을 때만 루프를 세우는데, `compose.i2.yml:335` 가 `COLAB_VIZ_TRIGGER_SPOOL: /srv/viz-events` 를 **주고 있다** ⟹ **㉯ 는 약한 후보**다.
- **푸는 법** = 로그 준위·처리기를 세우는 자리 하나(코드 변경 · 이 창의 범위 밖), 또는 회수 요약을 `warning` 준위로 올리는 판정. **주기를 줄이는 것은 설정 변경이라 여기서도 범위 밖이다.**
- ⛔ **`§6(vi)` 의 no-go 조건(「삭제가 0건이 아니면 즉시 창을 닫는다」)은 걸리지 않았다** — 인가된 7파일 밖의 삭제가 0개다.

---

## 6. 게이트 — `./gates/run.sh work-item-consistency`

```
work-item-consistency: green — 대장과 산문의 불일치 0
```

- **기대대로다** — 이 창은 대장도 산문 대장 반영본도 만지지 않았으므로 영향이 없어야 한다. 실측이 그것을 확인했다.
- 부수 출력(관측 1건 `㈔ 건너뛴 결정 번호 〈290〉` · 검사 대상 밖 8건 · 항목표 아닌 표 2건)은 **이 창 이전부터 있던 값**이고 위반이 아니다.

---

## 7. 이 창이 만진 것 · 만지지 않은 것

- **staging 쓰기 = 하나.** `POST /api/v1/previews` **1건**(GK-2A · `다색-무지개` · `classCount 6`). ⛔ **그 밖의 `POST`·업로드·재렌더 0건.**
- **배포 0** · **원장 append 0** · **컨테이너 정지·재기동·재생성·down 0** · **파괴 플래그 0** · **DDL 0** · **SQL 0**(DB 를 아예 열지 않았다) · **롤백 0** · **병합 0** · **코드 0줄 수정**.
- **자리에서 지워진 파일 = 인가된 재렌더가 회수한 7개뿐.** `tile-`·원본·기준 격자 **무접촉**.
- `work-items.yaml`·`PLAN-SoT.md`·`03-HANDOFF.md` **무접촉**. `main` **무접촉**.
- 접속 문자열·비밀번호·토큰 **미출력** — 토큰은 파일 경로로만 읽어 브라우저·요청 헤더로만 건넸고 문서·로그·대화에 **한 글자도 남기지 않았다.** 확인 뒤 `localStorage` 잔여 **0**.
- `a2_pg`·`ai_pg` **무접촉**.
