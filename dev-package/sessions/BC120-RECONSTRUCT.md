# `2단-BC120` 분해 재료 — 셋이 각각 무엇이었나

**성격** 읽기 전용 조사 (2026-08-29). 편집·커밋 없음.
**대상** `work-items.yaml` 의 `2단-BC120`(`status: conflict`) — `WORK-UNITS §10.3` 2단 한 줄이 done 과 deferred 를 뭉쳐 적어 분해 불가로 판정된 항목.
**결론** **셋 전부 문서에서 복원됐다. Ted 판정이 필요한 잔여 0.**

---

## 0. 문제의 한 줄 (축자)

`dev-package/WORK-UNITS.md` `§10.3` 순서표 2단 (`WORK-UNITS.md:459`):

```
2단  A 동결해제 프로토콜 ✅(〈163〉 3등급제)  →  E main 병합 ✅(2026-08-28 · 5866b49 → 9b312da fast-forward · 〈173〉-㉳)
     ※ 종전의 B·C·〈120〉 셋은 착수 후보가 아니다 — 전부 이미 끝났거나 안 하기로 한 것이다
       (B = 〈159〉 집행됨 · C = 〈160〉/〈114〉 집행됨 · 〈120〉 3건 = 〈145〉 로 닫힘 — 「하지 않기로 한 것」)
```

라벨 `A`~`E` 는 이 순서표 안에서만 쓰이는 자리표다. **원문 정의는 이 파일의 종전 판에 있었고, 현행 판에서 지워졌다** — 그래서 현행 문서만으로는 분해가 안 됐다.

---

## 1. `B`·`C` 의 원문 정의 복원 — 근거는 git 이력

라벨을 세운 커밋 = **`058b69b`** 「stage 2 계획 — 범위 물음 넷을 닫고 재기동을 맨 앞으로 옮긴다 (`〈157〉`·`〈158〉`)」.
그 판의 `§10.3` 2단 축자 (`git show 058b69b:dev-package/WORK-UNITS.md`):

```
2단  A 동결해제 프로토콜 ∥ B 계보 간선 D-09→D-16 ∥ C 격자 전용 워커   → E main 병합(게이트 green 확인)
     ⚠ C 는 워커 재배포를 수반한다 — 1단 재기동과 묶지 않는다
```

- **`B` = 계보 간선 `D-09` → `D-16`**
- **`C` = 격자 전용 워커**
- (`A` = 동결 해제 프로토콜 · `E` = `main` 병합 — 둘은 현행 줄에 이미 ✅ 로 살아 있어 이 항목의 대상이 아니다. `D` 라벨은 그 판에 없다.)

「종전의 B·C」 문구를 넣은 커밋 = **`4cebc98`** 「판정 넷을 더 닫고, 문서가 실물보다 낡아 있던 자리 열넷을 걷는다 (`〈164〉`·`〈165〉`)」. 이 회차가 정의줄을 요약줄로 갈아 끼우면서 이름이 사라졌다.

---

## 2. 대장 항목으로 세울 수 있는 형태 — 셋

### ⑴ 계보 간선 「원자료 → 월평균 산출물」 확정 (종전 `B`)

| | |
|---|---|
| **무엇이었나** | GK2A/AMI NDVI 원자료(`D-09`)에서 NDVI 100 m 경기남부·충청권 월평균(`D-16`)으로 가는 계보 간선을 사람이 확정하는 일 |
| **지금 상태** | **끝남 (done)** — 착수 후보가 아니다 |
| **근거** | `PLAN-SoT §9 〈159〉` — 「`B`(계보 간선 `D-09` → `D-16`)는 이미 그어져 있었다 — 집행됐는데 등재가 없었다」. `d4_lineage_edge` 실측 = `01M0YWDXN7CA13KB7MQC9PGEW5`(`D-16`) ← `01M0Y1WNCHGHBGK7Y31SSY4FQV`(`D-09`), `confirmed_at` 2026-08-26 12:14:18Z · `confirmed_by` 전창현(교수) |
| **덧** | 같은 항목 `-㉲` 가 재기동 검증 기준선의 **계보 간선 5 → 6** 정정을 함께 확정했다(`〈158〉-㉱` 의 「간선 5」가 오기). 현행 `§10.3` 1단 검증줄은 「간선 6」으로 이미 갱신돼 있다 |
| **규칙 저촉** | 없음 — 사람이 확정했으므로 「AI 는 계보에 쓰지 않는다」에 저촉하지 않는다(`〈159〉-㉯`) |

### ⑵ 격자 전용 업로드를 워커 처리에서 제외 (종전 `C`)

| | |
|---|---|
| **무엇이었나** | 본체 파일 없이 격자 파일만 올라온 업로드를 파이프라인 워커가 「형식 인식 실패」로 처리하지 않게 하는 일 — 실패 대신 곧장 준비 완료로 내보낸다 |
| **지금 상태** | **끝남 (done)** — 착수 후보가 아니다 |
| **근거** | 확정·구현 = `PLAN-SoT §9 〈114〉`(2026-08-26 Ted 판정 「워커에서 격자전용은 제외하라」 · 해소안 ⓐ 채택 · `services/pipeline-worker/src/colab_pipeline/domains/d5_ingestion.py` `process_upload`). 사후 실측 = `§9 〈160〉` — 「`C`(격자 전용 업로드)도 이미 집행돼 있었다」, 커밋 `4a8e41b`(2026-08-26 · `origin/main` 포함) · 실물 분기 `d5_ingestion.py:167` `if not bodies and grids:` early return. 산출 `sessions/C-GRID-ONLY-REVIEW.md` |
| **기각된 것** | `〈114〉-㉰` 접수 단계 별도 상태 신설 · `-㉱` 화면만 무해 취급. `〈160〉-㉯` 이 착수 지시서가 적으려던 한 줄 수정(`if bodies and not readable:`)을 **틀린 수정**으로 판정 |
| **남은 위험(별건)** | `〈160〉-㉳` — 미리보기 뒷단을 켤 때 `:167` 분기를 뒤로 밀면 「내부 오류」로 부활한다. 이것은 `PV-1` 이 지고 갈 주의사항이지 이 항목의 잔여가 아니다 |

### ⑶ 실패로 굳은 격자 전용 업로드 3건의 처분 (`〈120〉` 3건)

| | |
|---|---|
| **무엇이었나** | ⑵ 이전에 이미 실패로 굳은 격자 전용 업로드 **3건**의 업로드 레코드·파일 행을 만료 회수에 맡겨 없애는 일 |
| **대상 3건 (실물 id)** | `01M0Y1XND62XV5EKYZHFGNVJGS` · `01M0YTT4VJKMGV2FXZZE12SEA4` · `01M0YTT941XPYHH10RT3S2V9YY` (`〈145〉-㉰` staging 실측) |
| **지금 상태** | **하지 않기로 함 (deferred)** — 착수 후보가 아니다. 집행할 것이 없다 |
| **근거** | `PLAN-SoT §9 〈145〉` — 「`〈120〉` 을 ⓒ 로 닫는다 — 그대로 둔다」. Ted 2026-08-27 판정 문언 = **「그대로 둬」**. 축자: 「`〈120〉` 은 여기서 닫힌다 — **미실행이 아니라 「하지 않기로 한 것」이다**」 |
| **왜 그렇게 닫혔나** | `〈145〉-㉯㉰` — 회수 질의가 `registered_at IS NULL` 을 요구하는데 3건 모두 `registered_at` 이 있다. **만료가 와도 회수 대상이 아니다.** `〈120〉` 이 채택한 「시간에 맡긴다」가 실물에서 작동하지 않는다는 것이 ⓒ 의 근거 |
| **기각된 것** | ⓐ 회수 조건을 `registered_at IS NULL OR failed_at IS NOT NULL` 로 확대(등록된 데이터셋의 업로드 원장을 지우는 길이 새로 열린다) · ⓑ `d5_upload` 3행 직접 삭제(원장을 손으로 지우는 선례) |
| **남는 대가** | `d5_upload` 3행에 「형식 인식 실패」 기록이 영구히 남는다. 사용자 화면에 나타나지 않고, ⑵ 로 재발이 막혀 늘지 않는다 |
| **⚠ 실제 사고 확인됨** | `03-HANDOFF.md:263` 축자 — 「⚠ **「미이행」으로 적으면 다음 세션이 다시 착수 후보로 올린다** — **실제로 2026-08-27 에 그렇게 됐다**」. `〈160〉-㉲` 가 그 순간의 기록이다(「`〈120〉` 3건이 `03-HANDOFF.md:195` 에 ⬜ 로 미집행이다 — 진입조건에 다시 적지 않으면 또 넘어간다»). **같은 자리를 이번에 다시 열지 않는다** |

---

## 3. 인용 번호 검증 — 충돌 없음

`PLAN-SoT §9` 행 머리(`| 〈n〉`) 전수 스캔:

- `〈159〉`·`〈160〉`·`〈114〉`·`〈120〉`·`〈145〉` — **각 1행. 중복 0.**
- `§9` 전체 행 머리 중복 스캔 결과 **중복 번호 0건.**
- 각 번호의 내용이 인용된 취지와 **일치**함을 본문 축자로 확인(위 §2 의 근거 칸).

즉 **오늘 있었던 번호 충돌 2건(원문자 소진 · 동시 회차)은 이 다섯 번호에는 미치지 않았다.**

---

## 4. `[미확인]`

**없다.** 셋 모두 이름·범위·현재 상태·근거가 실물 문서와 git 이력에서 복원됐다.

참고로 아래 둘은 이 항목의 잔여가 **아니다**(별건이며 각자 자리가 있다):
- ⑵ 의 `:167` 분기 위치 주의 → `PV-1`(미리보기 뒷단)이 진다.
- ⑶ 의 원인인 「D3 원장 성공 ↔ D5 상태 실패」 불일치(`〈145〉-㉱`) → `〈114〉-㉲` 의 `file_count` 좁히기와 함께 미제기 사안. 이번 분해와 무관.

---

## 5. Ted 판정 필요 잔여

**없다 — 전부 복원됐다.**

셋 다 이미 Ted 판정 또는 실측으로 닫힌 것이고, 이번 조사는 **새 판정을 만들지 않고 이름만 되찾았다.** 대장 `2단-BC120` 은 사람의 판정 없이 위 §2 의 세 항목으로 갈라 세우면 된다 — done 둘 · deferred 하나.

**후속(집행 항목 · Ted 판정 불요)**
1. `work-items.yaml` 의 `2단-BC120` 한 행을 §2 의 세 행으로 대체 — 상태 `done`·`done`·`deferred`.
2. `WORK-UNITS §10.3` 2단 요약줄에 이름을 되살린다(라벨 `B`·`C` 만 남기면 다음 회차가 또 못 읽는다). 원문은 지우지 말고 개정 표시로 잇는다.
