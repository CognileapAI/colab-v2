# staging 단독 창 5 — 단계 A(짝 백업) 집행 ＋ go/no-go ① 증거 (2026-09-05 09:29~09:47 KST)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md §1`·`§10-①`. 이 파일은 **이 회차에 실제로 잰 값**만 적는다.
> 집행 범위 = **A 만**. B(배포) 미착수 — go/no-go ① 은 Ted 판정 자리이고 이 회차는 증거만 세운다.
> 양식 = `WINDOW-20260904-A4.md`(A 시트) · 배포 후속 양식 = `WINDOW-20260904-B4.md`.
> 이번 배포 후보 = **`main` @ `1ae5dc0`** (`1ae5dc063db3da71f94bb2c9a1fa54000c81bcce`).
> ⛔ **결론을 앞에 적는다 — 이 회차의 판정은 `NO-GO` 다**(§8). 배포 전 검사기 하나가 red 이고, 그 red 가 **B 가 실어 나를 기능(`V-1`) 자신의 완료 정의 ⑷ 시험**이다.

---

## 0. 창 진입 전 상태 (실측 · 읽기 전용 · 2026-09-05 09:29~09:47 KST)

| 축 | 실측값 | 재는 방법 |
|---|---|---|
| 커밋 | **`1ae5dc0`** · 브랜치 `main` · 「TL-1 종결 — 타일 회수를 주기 배경 루프에 …〈316〉」 | `git log -1` |
| 워킹트리 | **변경 0건** | `git status --porcelain` = 0줄 |
| 워크트리 | **2개** — `main` ＋ `.claude/worktrees/review-pr1`(detached `0e294e5` · **다른 세션 소유 · 이 회차 무접촉**). ＋ 이 회차가 만든 `lane-window-a5`(문서 전용) | `git worktree list` |
| 서빙 릴리스 | **`f465d83a2ef1`** — 앱 5종 이미지 태그 동일 ＋ 원장 마지막 `deploy` 행 `2026-09-04T21:54:09+0900 deploy f465d83a2ef1 green` ＋ `LAST-SUCCESS.txt` 마지막 줄 동일 | `docker ps` ＋ `release-ledger.tsv` ＋ `LAST-SUCCESS.txt` |
| 컨테이너 | staging **8개 전부 healthy** (`nginx`·`frontend`·`core_api`·`pipeline_worker`·`viz_render`·`ai_service`·`cloudflared`·`pg`) · `Up 12~14 hours` | `docker ps` |
| 체인 head (platform) | **`0011_lv1_drop_level_user_set`** · 리비전 11건 · head 1개 | `gates/run.sh migration-single-head` |
| 체인 head (ai) | **`0005_k2b_concept_graph_seed`** · 리비전 5건 · head 1개 | 〃 |
| 원장 green `deploy` 행 | **≥2** (`f465d83a2ef1` · `64791f910f49`) | `release-ledger.tsv` |
| 창 시각 충돌 | **없음** — 09:29 착수, 백업 크론 03:30 · 04:10(월) · 04:40 | `crontab -l` |
| 자리 파일 (`~/.colab-v2-staging/previews`) | **236** (`json 92`·`png 71`·`pgw 36`·`webp 35`·`tif 2`) — `〈312〉` 끝값 236 과 **동일** ⟹ B4 이후 자리 쓰기 0건 | `find -type f` |
| `COLAB_STAGING_ENV` | 셸 기본 환경에 **미설정** — B 착수 시 홈 0600 env 를 export 해야 한다 | 종전 회차와 같음 |

⚠ **staging 스택 밖 컨테이너 2건**(`a2_pg`·`ai_pg` · `Up 13 hours` · 게이트 DB)은 8개 계수에 넣지 않았다. **게이트 전용이고 이 회차 무접촉이다.**

---

## 1. B 가 실어 나를 변경분 — `git diff --stat f465d83..1ae5dc0`

**총계 = 29 파일 · +2,987 / −82.**

| 영역 | 파일 | 내용 |
|---|---:|---|
| `services/viz-render` | **14** | **V-1** 팔레트 재렌더(`invalidation.supersede_plan` ＋ `jobs.py` 호출 한 자리 ＋ `cache.py`·`preview.py`) · **TL-1** 타일 회수(`tile_reclaim.py`·`tile_liveness.py`·`tile_liveness_cli.py` ＋ `trigger_loop.py` 결선 ＋ `config.py` 설정 3종) · 시험 3파일 신설(`test_palette_rerender` 229줄 · `test_tile_liveness` 283줄 · `test_tile_reclaim` 377줄) |
| `dev-package/notes` | 5 | 등재문 초안 ＋ `STAGE2-COMPLETION-PATH-20260905.md`(370줄 · 코드 아님) |
| `dev-package/sessions` | 3 | `TL-1-20260905.md` · `WINDOW-20260904-B4.md` · `FIRSTTOUCH.tsv` (코드 아님) |
| `frontend` | **2** | **BF-4** 미리보기 캡션 — `DatasetPreviewSection.tsx` 가 사이드카를 **타일 갈래 밖에서도** 조회(+19/−?) ＋ 시험 1파일(60줄) |
| `infra` | **1** | `compose.i2.yml` +11 — 타일 회수 설정 3종 `${…:-}` 통과 |
| `dev-package` 산문·대장 | 4 | `work-items.yaml`·`PLAN-SoT.md`(〈313〉~〈316〉)·`WORK-UNITS.md`·`03-HANDOFF.md` |

**코드 표면은 셋이다** — `services/viz-render` · `frontend`(1파일) · `infra/staging/compose.i2.yml`(환경변수 통과 3줄).

### 1.1 마이그레이션 판정 — **0건. 실측이다(추정 아님).**

```
git diff --stat f465d83..1ae5dc0 -- db/   → 0 줄
ls db/platform/versions/  → 0001 … 0011.  0012 없음
ls db/ai/versions/        → 0001 … 0005.  그대로
```

⟹ **`db/*/versions/` 무변경 · `0012` 부재**(`0012` 는 `J-1` 예약 · `〈316〉`). platform head `0011` · ai head `0005` 에서 움직이지 않는다. **이것이 롤백 한계를 정한다** — §5.

---

## 2. 배포 전 검사기 8건 — 전부 **단독 실행**(`COLAB_GATE_JOBS=1`) · 축자

```
frontend-typecheck green — tsc --noEmit(frontend/tsconfig.json · include=src·test) 오류 0건.
frontend-test green — vitest run(frontend/vite.config.ts · jsdom) 통과 595건 · 실패 0건.
contract-lint green — seam 3건, 룰 위반 0.
generated-up-to-date green — 등기부 4건 전부 재생성 일치, 등기부 밖 자칭 생성물 0건.
contract-breaking green — 기준 HEAD (3건) 대비 파괴적 변경 없음.
migration-single-head green — 두 체인 모두 head 1개.
    # db/platform: 리비전 11건 · head 1개 (0011_lv1_drop_level_user_set)
    # db/ai: 리비전 5건 · head 1개 (0005_k2b_concept_graph_seed)
service-tests-viz-render  red  — failed 1 · errors 0.   ⛔ ①회차
    — 선택자 «not e2e and not perf» · 수집 324 · 실행 324 · skipped 0 · deselected 40 · failed 1 · errors 0 · 소요 26.3초
    FAILED tests/test_palette_rerender.py::test_팔레트_재렌더는_새_키를_세우고_옛_벌을_지운다
service-tests-core-api green — 실행 626건 전부 통과 (skipped 0 · deselected 6 은 요약줄에 드러나 있다).
```

- **green 7 · red 1 · 못 돈 것 0.**
- ⚠ **`service-tests-core-api` 는 이 회차 1회차 실행에서 바로 green** 이라 재실행 조건이 서지 않았다. 지시문이 지목한 간헐 red(연구실 A 데이터셋 계수 오염)는 **이번 표본에서 재현되지 않았다** — ⛔ **「고쳐서 green」이 아니고 원인은 `[미확인]` 그대로**다.

### 2.1 red 는 `service-tests-viz-render` 다 — **4회 중 3회 red** (감추지 않는다)

지시문의 재실행 규약은 `core-api` 에 붙어 있었으나, **실제로 red 를 낸 것은 `viz-render`** 다. 단발 red 를 환경 탓으로 넘기지 않기 위해 **단독(`-j 1`)으로 3회 더** 돌렸다.

| 회 | 결과 | 실패 케이스 |
|---|---|---|
| ① | **red** | `test_팔레트_재렌더는_새_키를_세우고_옛_벌을_지운다` |
| ② | green (324/324) | — |
| ③ | **red** | 같은 케이스 |
| ④ | **red** | 같은 케이스 |

**축자 실패문** — `AssertionError: 팔레트 재렌더가 옛 벌을 하나도 안 지웠다` / `assert () = RenderJob(...).invalidation_removed`.

**시험 파일 단독 실행은 6/6 green** (`.venv/bin/python -m pytest tests/test_palette_rerender.py` × 6 · 매회 8 passed). ⟹ **단독에서는 안 뜨고 전체 묶음에서만 뜬다.**

**근원인 방향 — 코드 실측(고치지 않았다).**
- `jobs.py:567` 이 `_run(job)` 안에서 **`job.status = STATUS_DONE` 을 먼저 세운다.**
- `_produced` 색인 등록과 `_supersede_for(job)` → `invalidation.apply()` 는 **그 뒤**에 돈다(`jobs.py:683~701`).
- 시험은 `status != "그리는 중"` 을 보고 다음 렌더를 던진다(`_render`/`_wait`) ⟹ **첫 벌이 색인에 들기 전에 둘째 렌더가 회수 범위를 계산할 수 있다.** 그러면 `supersede_plan` 의 후보가 비고 `invalidation_removed = ()` 가 된다.
- ⟹ **「완료 표시」와 「회수 집행」 사이가 벌어져 있다.** 부하가 높은 전체 묶음에서 그 틈이 벌어지고, 단독 실행에서는 안 벌어진다.
- ⛔ **이것은 시험만의 문제라고 말할 수 없다** — 사용자가 팔레트를 연달아 바꾸면 같은 순서가 실물에서 선다. **옛 벌이 남는다**는 뜻이고, 그것이 `V-1` 완료 정의 ⑷(「옛 산출물은 지운다」)가 요구하는 바로 그 동작이다.
- ⚠ **`〈316〉` 은 같은 커밋 계열에서 `green 50 / red(판정) 0 / red(준비) 0`(2026-09-05T02:27 · 워크트리 `lane-tl1b`)을 기록했다.** 그 값이 틀렸다고 적지 않는다 — **간헐이라 그 표본에서 green 이 나왔다**로 읽는다. 이 회차의 4회 표본이 그 간헐성을 처음 드러냈다.

**이번에 세지 않은 판정기준** — `service-tests-ai-service`·`service-tests-pipeline-worker` 단독 재실행 · `gates/run.sh all` 전수(이 회차는 지시대로 단독 8건만 돌렸다) · `service-tests-core-api` 간헐 red 의 원인 · **`viz-render` red 의 재현율(4회 중 3회는 표본이지 확률이 아니다)**.

---

## 3. TL-1 안전 사전점검 — **이 배포는 자동 삭제기를 싣는다**(관측 전용 기본)

### (a) `COLAB_VIZ_TILE_RECLAIM_APPLY` 가 staging 에 닿는 자리에 **켜져 있지 않다**

```
$ grep -rn "TILE_RECLAIM" infra/
infra/staging/compose.i2.yml:344:      COLAB_VIZ_TILE_RECLAIM_APPLY: ${COLAB_VIZ_TILE_RECLAIM_APPLY:-}
infra/staging/compose.i2.yml:345:      COLAB_VIZ_TILE_RECLAIM_MAX_KEYS: ${COLAB_VIZ_TILE_RECLAIM_MAX_KEYS:-}
infra/staging/compose.i2.yml:346:      COLAB_VIZ_TILE_RECLAIM_INTERVAL_SECONDS: ${COLAB_VIZ_TILE_RECLAIM_INTERVAL_SECONDS:-}
```

⟹ **`infra/` 안에 켜진 값 0건** — 셋 다 `${…:-}` 통과뿐이다.

**홈 0600 env(`~/.colab-v2-staging.env`) — 키 이름만 읽었다. 값은 읽지도 적지도 않았다.**
실재 키 24개 = `CF_ACCOUNT_ID`·`CF_API_TOKEN`·`CF_TUNNEL_ID`·`CF_TUNNEL_TOKEN`·`COLAB_AI_APP_PASSWORD`·`COLAB_APP_PASSWORD`·`COLAB_CORE_SESSION_SECRET`·`COLAB_OWNER_PASSWORD`·`COLAB_PG_SUPER_PASSWORD`·`COLAB_STAGING_AI_DB_URL_FILE`·`COLAB_STAGING_AI_OWNER_DB_URL_FILE`·`COLAB_STAGING_CORE_DB_URL_FILE`·`COLAB_STAGING_CREDENTIALS_FILE`·`COLAB_STAGING_PGDATA_DIR`·`COLAB_STAGING_PIPELINE_DB_URL_FILE`·`COLAB_STAGING_PLATFORM_OWNER_DB_URL_FILE`·`COLAB_STAGING_PREVIEWS_DIR`·`COLAB_STAGING_SUBJECTS_FILE`·`COLAB_VIZ_SERVICE_TOKEN`·`COLAB_VIZ_TILE_SIGNING_SECRET`·`COLAB_WORKER_ACCOUNT_ID`·`COLAB_WORKER_LAB_ID`·`OPENAI_API_KEY`.

⟹ **`COLAB_VIZ_TILE_RECLAIM_*` 는 홈 env 에 한 줄도 없다.** ⟹ compose 의 `${…:-}` 가 **빈 값**으로 떨어진다.

### (b) 빈 값 = 꺼짐임을 코드로 확인

```
config.py:103  DEFAULT_TILE_RECLAIM_APPLY = False
config.py:224  tile_reclaim_apply=_tile_branch_from_env(os.environ.get("COLAB_VIZ_TILE_RECLAIM_APPLY"))
config.py:87   def _tile_branch_from_env(raw): return (raw or "").strip().lower() in TILE_BRANCH_ON_VALUES
```

⟹ **빈 값·미선언·오타 전부 꺼짐**(`〈316〉`-㉰⑴ 축자와 일치).

### (c) 상한·주기 기본값이 `〈316〉` 이 말한 값과 같다

```
config.py:106  DEFAULT_TILE_RECLAIM_MAX_KEYS = 20
config.py:110  DEFAULT_TILE_RECLAIM_INTERVAL_SECONDS = 3600.0
config.py:165  _positive_int_from_env  — 못 읽는 값·0·음수는 기본값
```

⟹ **상한 20 · 주기 3600초 · 오타는 기본값** — `〈316〉` 축자와 **전건 일치**.

**판정 = 사전점검 3항 전부 통과. TL-1 은 이 배포의 NO-GO 사유가 아니다.**
⚠ 다만 배포되면 루프가 **실제로 돈다**(관측 전용). 그 계수를 창에서 반드시 읽는다 — §6 (vi).

---

## 4. 단계 A — 배포 전 짝 백업 (집행 완료 · GREEN)

- 명령 = `infra/staging/backup/backup-full.sh` (플래그 없음) · `main` 체크아웃에서.
- 최종 줄 축자 — **`2026-09-05T09:44:37 전범위 백업 GREEN — 원장 2 프로파일 ＋ 볼륨 2 개 · 보관처 규약 밖 파일 0`**

| 산출물 | 스탬프 | 크기(B) | 판정 |
|---|---|---:|---|
| `platform-….sql.gz` | `20260905T094356` | 30,807 | GREEN (통과 7 · SKIP 0) |
| `ai-….sql.gz` | `20260905T094356` | 5,158 | GREEN (통과 7 · SKIP 0) |
| `vol-uploads-….tar.gz` | `20260905T094356` | 393,734,961 | GREEN (통과 8 · SKIP 0) |
| `vol-previews-….tar.gz` | `20260905T094434` | 35,990,551 | GREEN (통과 6 · **승인된 SKIP 1** = V5 오라클 명시 면제 `none`) |

**짝(회차 한 벌) — `.pair` 실측**

```
vol-uploads-20260905T094356.pair   -> platform-20260905T094356.sql.gz
vol-previews-20260905T094434.pair  -> platform-20260905T094356.sql.gz
```

두 원장 덤프의 스탬프가 **동일**(`20260905T094356`) — P5(같은 회차) 조건이 시각차 0초로 선다.

**최신본 재검사** — `infra/staging/backup/latest-check.sh` → **`최신본 재검사 GREEN — 프로파일 2 개 · 볼륨 2 개`**.

**덤프가 담은 체인 (읽기 전용 대조)**

```
platform-20260905T094356.sql.gz : COPY public.alembic_version_platform → 0011_lv1_drop_level_user_set · '0012' 문자열 0건
ai-20260905T094356.sql.gz       : COPY public.alembic_version_ai       → 0005_k2b_concept_graph_seed
```

⟹ 이 덤프는 **배포 후에도 그대로일 스키마**를 담는다 — 스키마 되돌림 재료가 아니라 **데이터 되돌림 재료**다.
⭑ `vol-previews` 는 자리 파일 **236** 상태를 담는다(`〈312〉` 끝값과 동일).

경로 = `$COLAB_BACKUP_DIR`(홈 백업 보관처). **값·접속 문자열은 이 문서에 적지 않는다.**

---

## 5. 되돌림 준비 상태 (rollback readiness)

| 축 | 값 |
|---|---|
| 스키마 변경 | **없음** (§1.1 실측) |
| `rollback.sh --to-last-green` 의 복원 범위 | **이미지 = 배포 내용 전부** ⟹ **완전 복구** |
| 파괴적 복원(`--yes-drop-schema`) 필요 여부 | **불필요** |
| 롤백 대상 | **`f465d83a2ef1`** — 현재 서빙이자 직전 green |
| 그 태그 이미지 6종 실재 | ✅ **6/6** — `docker images` 실측: `core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`·`migrator` 전부 `:f465d83a2ef1` |

⚠ **롤백이 되돌리지 못하는 것 둘.**
⑴ **캐시** — `〈308〉`·`〈312〉` 와 같은 자리. **재굽기 뒤 롤백하면 옛 키 산출물이 회수로 치워졌을 수 있다.** 순서가 되돌림 비용을 정한다.
⑵ **자동 삭제기가 지운 타일** — 이 배포는 관측 전용이라 **0건 지운다**(§3). 켜지 않는 한 이 항은 비어 있다.

---

## 6. 배포 후 이 창이 돌릴 순서 (착수 전에 고정한다 · GO 가 나올 때만 집행)

### (i) 값 조회 4건 — **8 healthy 직후, 다른 무엇보다 먼저**

⛔ **배포당 한 번뿐인 창이다.** `〈304〉` 의 1,164 / 2,111 ms 스파이크 갈래는 `〈312〉` 채집에서도 재현되지 않아 **`[미확인]` 그대로**다(후보 ⓓ 가 하나 줄었을 뿐). **다시 잡을 자리는 다음 재기동뿐이다.**

- **시점** = 배포 판정 6항 중 **8 healthy 를 확인한 그 순간**. ⛔ 게이트·재굽기·현물 확인보다 먼저. **사람이 부르는 첫 요청이 이것**이어야 한다.
- **요청** = `POST /api/v1/datasets/{id}/value-lookup` · 점 `36.75,127.40` · **데이터셋당 1회씩** · UA 는 `〈312〉` 와 같은 축(`colab-v2-window-a5/1.0 (VL-1 branch d probe)`).
- 대상 4건 = `01M0YWDXN7CA13KB7MQC9PGEW5`(GK-2A) · `01M1JM3JTQSSTKR9A2QQ67QN81`(U-Net) · `01M19RENXAANZ2HDWM4Z46C37Y`(PV-1) · `01M0Y1VQ09TMHAC78ESDRTMAZD`(DEM · 대조군).
- **남길 것** = `Server-Timing` 원문 축자 ＋ `time_connect`·`time_appconnect`·`time_starttransfer`·`time_total` ＋ `http_code`. 원값은 `sessions/WINDOW-20260905-FIRSTTOUCH.tsv` 로.
- ⚠ **1회씩만 부른다.** 두 번째 요청은 이미 따뜻하다.

### (ii) 게이트 전수 — `gates/run.sh all -j 1` (선언 50)

- ⛔ **`service-tests-viz-render` 가 §2.1 의 간헐 red 를 다시 낼 수 있다.** 뜨면 **감추지 말고 그 회차 값으로 적는다.**
- `artifact-ownership` 은 재굽기 전이므로 판정 불가 19 를 그대로 낼 것이 정상이다.

### (iii) `artifact-ownership` 계수 — **재굽기 전(before)**

- 직전 값(`〈312〉` §5) = `대상 106 · 살아 있다 87 · 접수분 0 · 고아 0 · 판정 불가 19` · 자리 파일 **236**.
- 이 회차 진입 전 자리 파일 실측 **236** ⟹ 같은 값이 나오는 것이 정상이다. 갈리면 **그 사이 누가 썼다**는 뜻이니 먼저 그것을 밝힌다.

### (iv) **구판 19벌 재굽기** — 대상 목록은 **창에서 실측해 먼저 적고** 나서 손댄다

`〈315〉`-㉯ 판정 = 「`tolerate` 기한 연장이 아니라 **재굽고 재계수**한다」. **대상 목록은 이 세션이 갖고 있지 않다 — 추측하지 않는다.**

**창에서의 도출 절차(순서 고정)**
1. (iii) 의 `artifact-ownership` 전수 스냅숏(236줄)에서 **등급 `판정 불가` 인 벌만** 뽑는다.
2. 각 벌의 사이드카를 읽어 원천 식별자를 얻는다 — **구판 사이드카는 `sidecarVersion`·`baked_for` 가 없고 `source`(ULID)만 있다.**
3. 그 ULID 를 **읽기 전용**(`BEGIN READ ONLY`)으로 platform DB `d3_file` 에 대조해 데이터셋을 얻는다.
4. **얻은 데이터셋 목록을 회차 파일에 먼저 적고**, 그 다음에 `POST /api/v1/previews` 를 부른다(조건문이 아니라 **목록으로 고정한 뒤 집행** · `CLAUDE.md §6`).

⚠ **이 회차가 미리 잰 사실 하나 — 19 는 전부 「구판 사이드카」가 아니다.** 자리의 `.json` **92** 개 중 `sidecarVersion` 이 없는 것은 **5** 개뿐이다(`source` = `01M0V0MAPTYPEFILE00000000B`·`01M0Y1WNWK8Y7DT4RD2TSZ6H12`·`01M1BF69AJEA3BWK5NGXWTKCWX`·`01M0Y1WM3DKAWPBKVV0883285M`·`01M0Y1WMG1BBBQ8E7PMKP37NX9`). ⟹ **나머지 14벌은 「사이드카가 아예 없는」 갈래**로 보인다(등급 정의 축자 = 「사이드카가 없거나 **구판**이다」 · `ownership.py:12`). **그 14벌의 원천은 사이드카로는 안 나오므로 스냅숏 쪽에서 읽어야 한다** — 창이 이 갈래를 먼저 가른 뒤에 굽는다. 이 5건도 **확정 목록이 아니다**(등급은 게이트가 매기고 이 회차는 게이트를 그 목적으로 돌리지 않았다).
- **판정 = staging WRITE.** Ted 의 승인이 이 자리를 덮어야 한다.
- 인가된 경로는 공개 엣지 `POST /api/v1/previews` 하나뿐이다(`WINDOW-20260903-A3 §6`⑵).
- ⚠ **상세 화면을 재굽기보다 먼저 열지 않는다** — `〈312〉` §3.1 이 그 순서 오류로 온디맨드 굽기 1벌을 냈다.

### (v) `artifact-ownership` 계수 — **재굽기 후(after)** · **판정 불가가 19 에서 내려가는가**

- 이것이 `〈315〉`-㉯ 의 오라클이다. **내려가면 판정이 맞았고, 안 내려가면 「재굽기로는 안 덮인다」가 실측된다** — 어느 쪽이든 값으로 적는다.
- ⚠ **「내려갈 것」을 미리 적지 않는다.** `〈312〉` 에서 기대치 `+9` 가 실측 `+3` 이었던 것과 같은 자리다.

### (vi) **TL-1 관측 전용 계수** — viz-render 로그에서 회수 바퀴를 읽는다

- `docker logs colab_v2_staging_viz_render` 에서 회수 한 바퀴의 기록을 찾아 **무엇을 셌는지** 적는다(자리의 타일 벌 수 · 살아 있다 · 접수분에만 닿는다 · 고아 · 판정 불가 · 계산 불가).
- ⛔ **삭제는 반드시 0건이어야 한다**(관측 전용 · §3). **0 이 아니면 그 자체가 사고**이고 즉시 창을 닫는다.
- ⚠ 주기 3600초이므로 **한 바퀴가 돌 때까지 로그에 안 나올 수 있다.** 안 나오면 **「못 봤다」로 적는다 — 0 으로 적지 않는다.**
- `〈313〉` 실측 = 자리의 `tile-` 벌 2 · 못 닿는 벌 0 ⟹ **켜도 지금 지울 것이 없다**가 예측값이다.

### (vii) 현물 — 사용자 눈에 보이는 것

| # | 대상 | 기대 |
|---|---|---|
| ㉠ | **BF-4** 캡션 3 데이터셋 (**GK-2A 포함**) | 머리에 데이터셋 이름·파일명 ＋ **격자 `<grid>` · W × H** 표기. `〈315〉`-㉮ 로 **산출 격자(821 × 1024)를 그대로 수용**한다. ⛔ **`〈312〉` 에서 자리째 빠졌던 GK-2A 가 이번엔 서야 한다** — 그것이 `〈314〉` 가 고친 바로 그 자리(사이드카를 타일 갈래 밖에서도 조회) |
| ㉡ | **V-1** 팔레트 재렌더 1회 | 같은 데이터셋에 다른 팔레트로 1회 → **색만 바뀌고 구간 경계(`classes[].min/max`)·`bounds` 불변** ＋ **옛 벌이 자리에서 사라진다**. ⛔ §2.1 의 red 가 지목한 자리가 바로 여기다 — **자리 파일 수를 전후로 세서 옛 벌 삭제를 실물로 확인한다** |
| ㉢ | 값 조회 | 재굽기 뒤 3건 `200 ＋ available:true ＋ 값` · DEM 대조군 `available:false` |
| ㉣ | 검색 | `POST /api/v1/dataset-searches` — 질의 반영 · `verified` 걸름 동작 |
| ㉤ | `/lab` | 200 · ⚠ `BF-8` 미수정이라 여백 결함이 남아 있는 것이 정상 |
| ㉥ | 404 | 타 연구실·없는 자원 접근 = **404**(403 이 아니다 · `DEPLOY-269 §3`) |

⛔ **자기 연구실 묘비 410 은 이 창도 재지 못한다** — `deleted_at` 행 0건이고 만들려면 파괴적 쓰기가 필요하다.

### (viii) `BF-4`·`V-1` 완료 판정 — 조건별로

| 항목 | 마지막 열린 조건 | 이 창이 닫는가 |
|---|---|---|
| **BF-4** | ⑶ **화면 실측** (`〈314〉`-㉳⑴ · 프론트 변경이라 배포 전에는 사용자 눈에 안 보인다) | (vii)㉠ 이 3 데이터셋에서 서면 **닫힌다** ⟹ `in_progress` → `done` |
| **V-1** | ⑹ **staging 배포 green** | (vii)㉡ ＋ 배포 판정 green 이면 닫힌다. ⛔ **단, §2.1 의 red 가 먼저 해소돼야 한다** — 그 시험이 잠근 것이 ⑷ 자신이다 |

**⟹ 이 회차 판정 = 둘 다 「아직 닫지 않는다」.** 배포가 서지 않았으므로 화면 실측도 배포 green 도 없다.

---

## 7. Ted 의 GO 시 원장에 덧붙일 승인 행 — **축자 (이 회차는 적지 않았다)**

`~/colab-v2-releases/release-ledger.tsv` 에 탭 구분 5열로 **한 줄** append (시각은 append 시점으로 갱신) —

```
2026-09-05T<HH:MM:SS>+0900	approve	1ae5dc0	-	-	승인자=Ted 본것=go/no-go ① GO · 짝 백업 platform-20260905T094356/vol-uploads-20260905T094356/vol-previews-20260905T094434 GREEN · latest-check GREEN · 마이그레이션 델타 0줄(0012 부재 · 덤프 alembic_version_platform=0011) · 배포전 게이트 8건 중 green 7(frontend-typecheck·frontend-test·contract-lint·generated-up-to-date·contract-breaking·migration-single-head·service-tests-core-api) · service-tests-viz-render 는 4회중 3회 red(test_팔레트_재렌더는_새_키를_세우고_옛_벌을_지운다)이며 그 red 를 안 자리에서 승인함 · TL-1 자동삭제 관측전용 확인(APPLY 미선언·상한20·주기3600) · 롤백 대상 f465d83a2ef1 이미지 6/6 실재 · 배포 직후 값 조회 4건 Server-Timing 채취 승인 포함 · 구판 19벌 재굽기(POST /api/v1/previews · staging WRITE) 승인 포함 · WINDOW-20260905-A5
```

⛔ **이 회차는 append 하지 않았다.** ⛔ **그리고 §8 이 NO-GO 이므로 이 문면은 「red 를 안 자리에서 승인한다」를 명시로 담는다** — Ted 가 그 사실을 모르고 GO 를 내는 일이 없게 한다. red 를 고치고 다시 재면 그 조항을 지운다.

---

## 8. 판정 — **NO-GO**

**결정적 사실 = 배포 전 검사기 `service-tests-viz-render` 가 이 커밋에서 4회 중 3회 red 이고, 실패한 단 하나의 케이스가 `test_팔레트_재렌더는_새_키를_세우고_옛_벌을_지운다` — 즉 B 가 실어 나를 `V-1` 의 완료 정의 ⑷(「옛 산출물은 지운다」) 그 자체다.**

- 그 red 는 **환경 결손이 아니다** — 단독 실행(`-j 1`)에서 났고, 시험 파일 단독 6/6 green 과 전체 묶음 3/4 red 가 갈린다 ⟹ **완료 표시와 회수 집행 사이의 간극**(§2.1)이 재현 조건이다.
- ⛔ **`〈316〉` 의 `green 50/0/0` 을 근거로 넘어가지 않는다.** 같은 커밋 계열의 다른 표본이고, **간헐인 것이 이 회차에 실측됐다.** 「지난번 green 이었다」는 이번 값이 아니다(`CLAUDE.md §0`).
- ⛔ **「main 과 같다」도 근거가 아니다** — 이 red 는 **이번 변경분이 새로 들여온 시험과 코드**에서 났다(`test_palette_rerender.py` 는 이 diff 의 신설 파일이다).

**다른 축은 전부 GO 쪽이다 — 그래서 남은 문 하나가 이것뿐이라는 것도 함께 적는다.**
마이그레이션 0건(실측) · 롤백 대상 이미지 6/6 · 짝 백업 GREEN ＋ latest-check GREEN · TL-1 자동 삭제기 관측 전용 3항 전건 통과 · 나머지 게이트 7건 green · 8 healthy · 크론 비충돌.

**GO 로 뒤집는 조건 (하나)**
- `service-tests-viz-render` 가 **단독 연속 3회 이상 green** 이 되는 것. 그 방법은 둘 중 하나다 —
  ⑴ **완료 순서를 고친다** — 색인 등록·회수 집행이 끝난 뒤에 `STATUS_DONE` 을 세우거나, 시험이 `job.done` 을 기다리게 한다. **어느 쪽인지는 코드 소유 레인이 판정할 자리**이고 이 창이 정하지 않는다.
  ⑵ **`V-1` 을 이번 배포에서 뺀다** — 되돌림 비용이 크다(`supersede_plan` 이 `jobs.py`·`cache.py`·`preview.py` 와 물려 있다). **권고하지 않는다.**

**추가 no-go 조건 (종전 회차에서 이어지는 것)**
- 승인 행 미기재 상태로 `deploy.sh` 를 부르는 것. **승인 행이 먼저다.**
- (i)(값 조회 4건)을 8 healthy 직후에 부를 준비가 안 된 상태로 배포하는 것.
- (iv) 재굽기 대상 목록을 **적지 않은 채** `POST /api/v1/previews` 를 부르는 것.

---

## 9. 이 회차가 만진 것 · 만지지 않은 것

- **staging 쓰기 0** — 부른 것은 `docker ps`·`docker images`·`find`(읽기)와 `pg_dump`(읽기 전용 백업)뿐이다.
- **컨테이너 수명 조작 0 · 파괴 플래그 0 · DDL 0 · SQL 쓰기 0 · 삭제 0 · 배포 0 · push 0 · 병합 0.**
- **코드 0줄 수정** — red 를 보고 고치지 않았다. 원인 방향만 적었다(§2.1).
- `a2_pg`·`ai_pg` **무접촉**. `review-pr1` 워크트리 **무접촉**.
- 자격증명·접속 문자열 **미출력** — 홈 env 는 **키 이름만** 읽었다.
