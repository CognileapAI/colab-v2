# Stage 1·2 dev d564 인증 회귀 smoke

- 대상: dev 공개 엣지, core 이미지 `dev-d56428d6945d`
- 직접 메타: digest `sha256:d14a7121b96fe2f95d7d0da284a77a7830d213a187592474875fa9a8db630003`, health `healthy`
- 도구: 별도 agent-browser 세션 `stage12-auth-d564`
- 범위: 실제 UI 입력과 버튼 동작을 사용했고, 상태 판정은 세션 API와 `/me`를 함께 확인했다.

## 결과

1. 보호된 기존 TEST A 자격으로 로그인했다. `POST /api/v1/sessions` 201, 토큰 존재, `/me` 200이었다.
2. 새로고침 뒤 토큰이 유지됐고 `/me` 200이었다.
3. 화면의 로그아웃 버튼을 눌렀다. `DELETE /api/v1/sessions/current` 204, 토큰 제거, `/me` 401, 로그인 화면 표시를 확인했다.
4. 같은 보호 자격으로 다시 로그인했다. 세션 생성 201, `/me` 200이었다.
5. 다시 새로고침한 뒤 토큰이 복원됐고 `/me` 200이었다.

인증된 셸이 표시된 동안 주소 경로는 `/login`에 남았다. 이번 완료 조건은 인증 상태와 세션 지속성이라 별도 판정하지 않았다. 계정 생성·비밀번호 변경·TEST B 변경·업로드·서비스 재적재는 모두 0회다. 자격 값, Authorization, raw HAR는 기록하지 않았다.
