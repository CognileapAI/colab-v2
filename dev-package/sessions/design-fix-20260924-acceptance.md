# design-fix 20260924 · 수용 검토(워크플로 · advisor ② 자리) — 확정 결함 목록

- 출처: 워크플로 `design-fix-lane-acceptance-L1-L2`(에이전트 165 · 확정 22 · 기각 30) · `design-fix-lane-acceptance-L3`(에이전트 93 · 확정 16 · 기각 13). 확정 = 반박자 3명 중 2명 이상이 유지.
- 병합 검사: L1→L2→L3 텍스트 충돌 0 · 파일 겹침 0 · 합친 트리 vitest 전건 통과(단 dataset-preview 계열 unhandled error 1 → exit 1, 부하 의존 의심).
- L1·L2 워크플로 critic 의 「L3 없음」은 L3 완료 전 판단이라 기각.

| id | 수정 레인 | 출처 | 심각도 | 파일:줄 | 결함 | 수정 힌트 | 유지 |
|---|---|---|---|---|---|---|---|
| A1 | F-css | L1/spec | minor | `frontend/src/components/lineage/lineage.css`:165 | The #19 comment ("주석을 렌더에 맞춤") now documents the wrong background and a dead selector, so the comment still does not match what renders. | Rewrite line 165 to name `.lin-scope-lv` (`--color-text-muted` #565c63 on `--color-surface-alt` #f5f7fa = 6.30:1), or drop it, because `.lin-over-why` is already covered by line 163. Mention the `.lin-unknown-why` dead rule as a residual. Ask Ted/orchestrator to confirm the spec row 19 premise. | 2/3 |
| A2 | F-css | L1/spec | minor | `frontend/src/shell/primitives.css`:29 | The #9 hover changes have no disabled guard, so disabled primary and secondary buttons now darken on hover (primary→primary-700, secondary→gray-50) and look clickable. There is no generic `:disabled` button style anywhere. | Scope the hover rules with `:not(:disabled)` (no new value or token), or record this for advisor ③. | 3/3 |
| A3 | F-css | L1/correctness | major | `frontend/src/shell/tokens.css`:145,155 (lane head eb0efcb7); consumers  | In the dark theme, the new press state for catalog rows (WU-A3, value 16) and main nav links (WU-A2, value 15) is the same color as their hover state, so pressing gives no visible feedback. The fault is in the confirmed value, not in how the lane applied it. | This needs a spec or value decision from Ted, not a lane-local change. Options: give these two selectors a stronger press token in dark (e.g. gray-200 #45566a), or move them to a hover→active pair that differs in both themes. Add a per-theme hover≠active assertion to the test. | 3/3 |
| A4 | F-css | L1/correctness | minor | `frontend/src/shell/primitives.css`:37 (with components/upload/GridUploadBlo | The new `.btn-sm` sets `padding: 0 11px` but no `display` or `align-items`. So the `<label class="btn btn-strong btn-sm">` file picker in the grid-upload block now puts its text at the top of the box instead of centering it. At ≤640px, where `min-height` is 44px, the text sits about 10px above center. | Add `display:inline-flex; align-items:center` to `.btn-sm`, or to `label.btn` in primitives. Either needs a spec note because it goes beyond value 11's four declarations. | 3/3 |
| A5 | F-css | L1/correctness | minor | `frontend/src/shell/primitives.css`:29,33 | #9 extends hover backgrounds to `.btn-primary` and `.btn-secondary`, but no `.btn:disabled` rule exists anywhere. Disabled primary and secondary buttons now change color on hover, which suggests they can be clicked. | Scope the rules as `.btn:where(:not(.btn-primary)):hover:not(:disabled)` and `.btn-primary:hover:not(:disabled)`. This is a small divergence from the spec's literal selector text, so record it. | 3/3 |
| A6 | F-css | L1/correctness | minor | `frontend/src/shell/primitives.css`:63,72 | This confirms the lane's own finding: on `.chip--off`, value 18 is overridden, so its outline is still invisible in light. |  | 3/3 |
| A7 | F-css | L1/tests | minor | `frontend/test/design-fix-20260924-L1.test.ts`:217 | The value-18 assertion only checks that the `.chip` block contains a `border-color` declaration. It does not check the border that `.chip--off` actually ends up with. `.chip--off` is the judged row (design-system ⑦ row 4), so the suite stays green even though its outline is invisible in light mode. This confirms the defect the lane already reported and adds why the tests miss it: no test would fail on it. | After Ted rules on `.chip--off`, add an assertion that no later chip-modifier block in primitives.css sets a `border` shorthand or a `border-color` other than border-strong. At minimum, assert that `.chip--off` has no `border:` shorthand. | 2/3 |
| A8 | F-css | L1/tests | note | `frontend/src/shell/shell.css`:186-191 | The new `.mainnav a:active` rule has no effect on the current-page nav item. `.mainnav a.is-active` has equal specificity (0,2,1), comes after it, and sets `background: var(--color-surface)`. Pressing the active tab therefore shows no press feedback. The WU-A2 test (test:352) passes regardless. This does not break spec §4 (the block exists with value 15), and it matches how hover already behaves on the active item. Recorded as evidence for TD-2. | No action required by the spec. If press feedback on the current tab is wanted, give `.mainnav a.is-active:active` its own rule. | 3/3 |
| A9 | F-css | L1/tests | note | `dev-package/sessions/design-fix-20260924-L1.md`:§1 RED 확인 paragraph | The count of 17 green tests before implementation is correct, but the list that explains it names only 15. It leaves out `.btn-primary:active` 그대로 primary-700 and `primitives.css 에 !important 0`, both of which also passed before implementation. | Add the two missing tests to the list in the report's enumeration. | 3/3 |
| A10 | F-css | L1/visual | minor | `frontend/src/components/catalog/catalog.css`:37 | In dark theme the new pressed state for catalog rows and main-nav links looks exactly like hover, so pressing gives no feedback. The confirmed value gray-100 has the same hex as --color-surface-hover in dark. | Needs a value decision from Ted: for surfaces whose hover is surface-hover, use gray-200 (L #dfe3e8 / D #45566a) as the press value, or accept the no-op in dark and correct the stated rule. | 3/3 |
| A11 | F-css | L1/visual | minor | `frontend/src/components/catalog/catalog.css`:40 | While a catalog row is pressed in light theme, the new gray-100 background drops gray-500 text below 4.5:1. `.lvl-mismatch` passes only barely. | Either use a lighter press value in light, or accept it as a transient-state exception and record it explicitly. Not covered by the lane's contrast tests, which check muted and text colours on gray-100 only. | 3/3 |
| A12 | F-css | L1/visual | minor | `frontend/src/components/search/search.css`:104 | The value-18 chip border (border-strong) never reaches search-page chips. A screens-layer `border` shorthand resets the colour to --color-border, so neutral chips on search still have an invisible outline in light. This is the same failure as the known `.chip--off` issue, but in a different selector and file. | Drop `border:` from `.search-page .chip`, or change it to border-width/border-style only. Or record it as a known remainder beside `.chip--off`. | 3/3 |
| A13 | F-css | L1/visual | minor | `frontend/src/shell/primitives.css`:33 | The new hover rules also fire on disabled buttons. `.btn-primary:hover` is new, and `.btn-secondary` is newly included in the gray-50 hover. No `.btn:disabled` style exists anywhere, so disabled primary and secondary buttons now change colour on hover. That falsely signals they can be clicked. | Add `:not(:disabled)` to the two hover selectors, e.g. `.btn-primary:not(:disabled):hover`, or add a `.btn:disabled` rule. Needs a test change, since the L1 tests pin the exact selector text. | 3/3 |
| A14 | F-css | L1/visual | note | `frontend/src/shell/primitives.css`:67 | Confirms and extends the known `.chip--off` issue: the outline is also nearly invisible in dark, not only in light. | Same fix options as the lane listed: delete the `border` declaration from `.chip--off`. | 2/3 |
| A15 | F-css | L1/visual | note | `frontend/src/auth/login.css`:189 | Changing the account modal's z-index from 120 to 200 does not change stacking against any existing layer. No conflict with the GNB, toasts or auth overlay. | None needed. The comment 「GNB(100) 위」 was already true at 120. | 2/3 |
| A16 | F-css | L2/spec | note | `dev-package/sessions/design-fix-20260924-L2.md`:§5 「하지 않은 것」 bullet 2; §4-2 | The lane report does not meet the spec §5 lane-completion contract: frontend-test and frontend-visual are not green (red 78 on the host mutex), there is no lane capture (fix0924-L2) or 「바뀐 장면 → 원인 항목 #」 table, and there is no evidence path. All three are disclosed as not done, so the report is honest about them. They are listed here only so the orchestrator does not count L2 as complete. | Re-run `gates/run.sh task` on 1b263dae when the host is free (this is the known stale-run item). Capture and diff fix0924-L2 against fix0924-base during integration, or record that it was waived. | 3/3 |
| A17 | F-preview | L2/correctness | minor | `frontend/src/components/preview/preview.css`:255-260 | Value 2 says the backdrop passes presses through during closing, but the preview tool panels inside the modal explicitly set `pointer-events: auto`. Those panels still catch clicks while the modal fades out. | Add `[data-state="closing"] * { pointer-events: none !important }` scoped to the backdrop, or use `inert` (see L2-BEH-1). Either covers explicit `auto` descendants. | 2/3 |
| A18 | F-upload | L2/correctness | minor | `frontend/src/components/upload/upload.css`:586-587 | During dragover in the empty/pick scene, the circular drop icon disappears into the new dropzone fill. `.up-drop-icon` uses the same `--color-primary-50` background as `.dropzone.is-dragover`, in both themes. | Give the icon a contrasting fill while `.is-dragover` is set (e.g. surface or primary-100), or accept this and note it for advisor ③ / visual capture. | 2/3 |
| A19 | F-css | L2/correctness | note | `frontend/test/design-fix-20260924-L2.test.tsx`:491-549 | The value 2 reopen-while-closing path is tested only through UploadEntry. GridAttachEntry's parallel `open`/`rendered`/`onCloseStart` wiring (GridAttachEntry.tsx:39-75) has no behavior test. | Add one RTL case: open grid attach, click × with a stubbed 0.3 s transition, click '기준 격자 추가' again, and assert that closing is cleared and onClose is not called. | 2/3 |
| A20 | F-css | L2/tests | note | `frontend/test/design-fix-20260924-L2.test.tsx`:531-547 | At the RED commit the '완전히 닫힌 뒤 다시 열면 처음 (PRD-13)' test most likely failed because the harness threw, not because an assertion failed. The behaviour it pins already held at base. | None needed for correctness. If the report's RED reasons are meant as evidence, name this test as failing because the modal is not kept, via a harness throw. | 3/3 |
| A21 | F-upload | L2/visual | major | `frontend/src/components/upload/upload.css`:508-509, 538-540 | In the dark theme, the new :active rules on `.dr-nav button` and `.dr-useg button` resolve to the same color as their :hover rules. A press therefore looks exactly like a hover (1.00:1), which breaks the spec's own rule 「누름 = hover 의 한 단 진한 값」. | This needs a Ted/advisor decision because it changes a confirmed value. One option is active = `--color-gray-200`. In dark that is #45566a: text-body 5.86:1 and 1.61:1 against the hover color. Caveat: `.dr-nav button` has a `--color-border-strong` border, which is also #45566a in dark, so the border | 3/3 |
| A22 | F-upload | L2/visual | note | `frontend/src/components/upload/upload.css`:584-586 | The comment says the `.up-empty .dropzone.is-dragover` line has 「같은 특이도」 as `.up-empty .dropzone`, but it is (0,3,0) versus (0,2,0). The cascade outcome is still correct. | Change the comment wording only. | 3/3 |
| A23 | F-preview | L3:spec | minor | `frontend/src/components/preview/useZoomPan.ts`:465 (with 425-458, 434) | pointercancel is routed to the same `up` handler as pointerup. A cancelled gesture therefore starts #5 inertia, and the cancel event's own clientX/clientY becomes the last sample in the 100ms velocity window. Value 6 ties inertia to release (놓을 때), and a cancel is not a release. | Use a separate cancel handler that clears drag/samples without inertia, or at least leave the cancel event's coordinates out of the velocity samples. | 3/3 |
| A24 | F-preview | L3:spec | note | `frontend/src/components/preview/useZoomPan.ts`:202-203, 446 | The inertia start point is read from `viewNow.current`, i.e. the last committed render, and not from the latest setView. Under React 19 a setView from a native window `pointermove` gets continuous (non-sync) priority. If `pointerup` is dispatched before that render commits, the spring starts one move behind and the projected goal is short by that delta. | Track the latest view in a ref that the setView updater writes, or compute start inside the first frame's updater from `cur`. | 3/3 |
| A25 | F-preview | L3:correctness | minor | `frontend/src/components/preview/useZoomPan.ts`:465 (with 425-461, 434) | pointercancel runs the same handler as pointerup, so a cancelled gesture launches inertia, and the cancel event's coordinates become the last velocity sample. | Use a separate `cancel(e)` handler that clears drag.current, samples and dragged without starting inertia. At minimum, never use the cancel event's coordinates as a sample. Add an RTL case: pointerCancel with clientX/Y 0 leaves the transform unchanged. | 3/3 |
| A26 | F-preview | L3:correctness | minor | `frontend/src/components/preview/useZoomPan.ts`:447-459 (spring.ts 29-38) | When the projected goal is cut by clampView, the critically damped spring overshoots the goal whenever the remaining distance is below v0/ω (≈0.064 s × v0). clampView hides the overshoot as an abrupt stop at the bound while still moving, and the rAF loop keeps calling setView with the same clamped value until the unclamped spring settles. | When the goal was clamped, cap the handed-over velocity so that \|v0\| ≤ ω·\|x0\| (no zero crossing). Alternatively, end the loop once the clamped value equals the goal and the spring is past it. Add a unit or behavior case with a goal inside v0/ω. | 3/3 |
| A27 | F-preview | L3:correctness | note | `frontend/src/components/preview/useZoomPan.ts`:395-401 | A second pointer's pointerdown replaces the drag already in progress. There is no isPrimary check and no guard for an existing drag.current, so a two-finger touch hands the pan to finger 2 and silently drops finger 1's gesture: its moves and its pointerup are ignored because pointerId differs. | Ignore pointerdown when drag.current is set with a different id, or when `isPrimary === false`, until the first pointer ends. | 3/3 |
| A28 | F-preview | L3:correctness | note | `frontend/src/components/preview/useZoomPan.ts`:202-203, 446 | The inertia start position comes from `viewNow.current`, which is set during render. If pointerup arrives before React commits the last pointermove's setView, the spring starts from a stale position. Frame 1 then overwrites `cur` (which includes the last move delta) with a value computed from that stale start. | Keep the latest committed pan in a ref inside the setView updater (or read `cur` in the first frame to set the start), instead of using the render-time ref. | 2/3 |
| A29 | F-preview | L3:correctness | note | `frontend/src/components/preview/useZoomPan.ts`:501-511 | The resize-remeasure path and the baseScale 'start position' effect do not call stopInertia. If bounds change during inertia, the effect's reset to {scale: baseScale, x: 0, y: 0} is overwritten by the next spring frame, which uses x/y from the old scale (clamped at the new scale). | Call stopInertia() in the baseScale effect (and optionally in remeasure). | 2/3 |
| A30 | F-preview | L3:tests | minor | `frontend/test/design-fix-20260924-L3b.test.tsx`:196-208 | No behavior test checks that the release velocity is actually handed to the spring (the '속도 인계' part of #5). A hook that called spring(start, goal, 0, t), or passed px/ms instead of px/s, would pass every L3b behavior test. | After slowFling and advance(16), assert yOf(layers) is close to Y0 + spring(released, target, v*1000, 0.016).value. Alternatively, assert the first-frame displacement is larger than the v0 = 0 value. | 2/3 |
| A31 | F-preview | L3:tests | note | `frontend/test/design-fix-20260924-L3b.test.tsx`:222-224 | The comment says every frame stays in range ('매 프레임 범위 안이다'), but the test samples only two points (after 1216ms and after 1716ms). The clamped-goal case, where the raw spring overshoots the goal (v0 > omega*\|x0\|) and only the per-frame clampView hides it, is not covered by the unit tests or by any per-frame assertion. | Either step advance(16) in a loop and assert yOf <= Y0+HALF on each frame with a fixture whose edge is close (e.g. about 50px of room at v=2000), or reword the comment. | 2/3 |
| A32 | F-preview | L3:tests | note | `dev-package/sessions/design-fix-20260924-L3.md`:RED 기록 (L3b bullet) | At the committed RED point (4f137bbb), L3b fails only because the module is missing (import resolve error). The behavioral RED evidence (9 failed / 6 passed) came from an uncommitted, deleted temporary spring.ts stub, so it cannot be reproduced from history. | Acceptable as disclosed. For stricter discipline, commit a minimal stub spring.ts in the RED commit so the RED reason is assertion-level and reproducible. | 2/3 |
| A33 | F-preview | L3:a11y-ux | minor | `frontend/src/components/preview/useZoomPan.ts`:425-465 | pointercancel goes through the same `up()` path as pointerup, so it starts inertia, and it adds the cancel event's own clientX/clientY as the last velocity sample. | On pointercancel, end the drag without inertia: set drag=null and dragged=false and return. Alternatively, never use the cancel event's coordinates in the samples. | 3/3 |
| A34 | F-preview | L3:a11y-ux | minor | `frontend/src/components/preview/useZoomPan.ts`:202-203, 446, 458 | Inertia starts from the last rendered view (`viewNow.current`), not the live value. Each frame then writes absolute x/y and ignores `cur`. If the last pointermove's setView has not rendered yet when pointerup runs, that move is thrown away and the map snaps back by one move delta at release. This goes against canon §3 (animate from the presentation value). | Take the start value from the live state. Either keep a ref that the move updater at line 423 writes to, or read `cur` in the first frame and store it as the spring origin. | 2/3 |
| A35 | F-preview | L3/critic | minor | `frontend/src/components/preview/useZoomPan.ts`:onPointerDown (~L395-405) / up() (~L425- | Tapping the map to stop an inertia glide also runs a value lookup on the detail screen. The tap that interrupts inertia is not treated as a drag end, so the click that follows goes to onPickPoint. | In onPointerDown, record `const caught = inertia.current !== null` before stopInertia(). In up(), set `dragged.current = from.active \|\| caught`. Add an L3b test: fling, then pointerDown/Up inside the threshold during inertia, then click, and expect lookupValue to be called 0 times. | critic |
| A36 | F-preview | L3/critic | minor | `dev-package/sessions/design-fix-20260924-L3.md`:하지 않은 것 § | No lane ran the spec §5 capture comparison ('바뀐 장면 → 원인 #' table). The only capture on disk is the baseline, so the whole visual-diff evidence for advisor ③ still has to be produced at integration. | Before advisor ③, run visual:capture on the merged tree with the fix0924-base scene set and visual:diff it, then fill the changed-scene → cause # table for all 3 lanes. Stop on any unexplained diff. For L3, expect no static diff. | critic |
| A37 | F-css | L3/critic | minor | `gates/tools/frontend-visual.sh / .agents/skills/design-review/scripts/live_audit.sh`:L1 report :121 | When the integration frontend-visual gate runs with the union URL set plus &theme=dark, evidence will be silently overwritten and the page count understated. live_audit.sh truncates file slugs to 60 chars, so some dark URLs collide with their light twins. L3's lane gate also declared no dark URLs, even though spec §5 says to declare &theme=dark when live_audit's media switch does not apply (L1 established that it does not). | For the integration run, declare dark URLs for all 18 scenes including preview/detail. Check page count = number of URLs in gate-summary, or record the slug collision as a known undercount. Do not report the scenes as all measured. | critic |
| A38 | F-css | L3/critic | minor | `(integration gates on merged tree)`:spec §5 gate table '통합' column | The spec's integration gates have not run on the merged L1→L2→L3 tree. The merge review ran vitest, tsc and the design-lint judges directly, but not gates/run.sh. frontend-fixture-reach, frontend-design-lint-selftest (required because L1 changes same-in-dark.txt), frontend-visual and the production build (marked ○ for 통합) are unverified on the combined tree. | After merging onto the integration branch, run `bash gates/run.sh` serially (-j 1) for typecheck, test, fixture-reach, design-lint, design-lint-selftest and visual (URLs declared), then `npm run build` in frontend/. Report the 3 counts. | critic |
| A39 | F-css | L3/critic | note | `gates/tools/frontend-visual.sh`:L3 report 추기 § | The host-mutex fd inheritance defect found by L3 has not been fixed or filed, and it will hit the integration gate run. agent-browser daemons started by frontend-visual inherit the gate lock fd and outlive the gate as orphans (ppid 1), so a later frontend-test waits 900s and returns 78. | Before the integration gate, check `fuser -v /tmp/colab-v2-gate-host-mutex/host`, and record a follow-up to close fd 11 in frontend-visual.sh/live_audit.sh or stop the daemon at the end. Do not count a 78 from this cause as a verdict. | critic |
| A40 | F-preview | L3/critic | note | `frontend/test/design-fix-20260924-L3.test.tsx`:(absent) | No test covers pointercancel. Three reviewers found that pointercancel launches inertia, but the tests pin neither the current behavior nor the intended one, so a fix or regression would go unnoticed. | When fixing the confirmed pointercancel finding, add a test: drag beyond 10px, fire pointerCancel, and expect no rAF scheduled and no position change. | critic |
| A41 | F-preview | L3/critic | note | `frontend/src/components/preview/useZoomPan.ts`:useEffect([clampView]) window listeners; | The inertia `frame` closure keeps the clampView and goal from release time. If clampView changes during a glide (box resize, bounds/baseScale change, or maxScale learned from onNativeWidth), later frames clamp with the old bounds and head for a goal computed at the old scale. This extends the confirmed resize/baseScale finding: the effect re-subscribes its listeners, but the running rAF chain is not restarted or stopped. |  | critic |
