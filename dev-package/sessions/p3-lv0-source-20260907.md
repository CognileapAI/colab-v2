# WU-B6 · Lv0 출처 칸 (PRD-19) — 레인 `p3-lv0-source` · 2026-09-07

기점 `b982100`(= `integration/r-b`) · 계층 DB·계약·서버·FE · 크기 M · 선행 WU-B1 · WU-B3 · B5 병합분 위.

## 완료 조건 ↔ 판정 ↔ 근거

| 완료 조건 (라운드 §5 WU-B6) | 판정 | 근거 |
|---|---|---|
| 두 칸이 서고 **선택 입력**이다 | 충족 | `db/platform/schema.sql` `d3_dataset.source_url`·`source_downloaded_on` — CHECK 0 · NOT NULL 0. 오라클 `db/platform/tests/0018-assertions.sql` C-⑶⑷ 가 「막을 장치 자체가 없다」를 센다 |
| Lv0 에서 비운 채 등록 **성공** | 충족 | `services/core-api/tests/test_lv0_source.py::test_lv0_with_empty_source_url_registers` |
| Lv1 이상에서 값을 실어도 **저장** | 충족 | 같은 파일 `test_lv1_with_source_url_succeeds_and_stores` ＋ `test_every_level_accepts_the_two_fields`(4단 전부) |
| `sourceLabel` 은 **늘 보인다** | 충족 | 같은 파일 `test_source_label_stays_visible_at_every_level` · 오라클 D-⑴⑵⑶(정규화 열·자동완성 색인·검색 색인 무변) |
| Lv 변경 시 두 칸이 열리고 닫히며 숨은 값이 전송되지 않는다 | 충족 | `frontend/test/lv0-source-20260907.test.tsx` ㈎-b·㈒ |
| ⛔ 폐기된 판정을 되살리지 않았다 | 충족 | 서버에 Lv 조건 분기 0(`_UPDATABLE`·`validate_human_metadata` 주석) · 화면에 `필수` 배지 0(FE ㈎ 가 `.reqtag` 0건을 센다) |

## 수용 기준 5 (라운드 파일 축자) ↔ 시험

| # | 축자 | 시험 |
|---|---|---|
| ㈎ | ① Lv0 → ③ 진입 → 두 칸이 보이고 `선택` 표기 | FE ㈎ (placeholder·안내 문면·`.reqtag` 0건까지) |
| ㈏ | ① Lv1 → ③ 진입 → 두 칸 안 보이고 **원천 표기는 그대로** | FE ㈏ (슬롯은 남고 children 0 · `reg-source` 존재) |
| ㈐ | Lv0 ∧ 출처 주소 빔 → `createDataset` **성공** | 서버 `test_lv0_with_empty_source_url_registers` · FE ㈐(두 열쇠 미전송) |
| ㈑ | Lv1 인데 `sourceUrl` 전송 → **성공하고 저장** | 서버 `test_lv1_with_source_url_succeeds_and_stores` (화면 쪽은 FE ㈑ 가 「Lv1 에서 안 싣는다」를 잰다) |
| ㈒ | 채운 뒤 Lv2 로 바꿔 ③ 재진입 → 숨고 두 값 미전송 | FE ㈒ |

## RED → GREEN

- 서버 `services/core-api/tests/test_lv0_source.py` 10건 — RED **7 failed / 3 passed**.
  축자 `AssertionError: {"code":"BAD_REQUEST","message":"계약에 없는 필드다: ['sour…`.
  ⚠ RED 시점에 통과한 3건 중 날짜 400 두 건은 **「계약에 없는 필드다」 400** 으로 통과한 것이라
  그 시점에는 오라클이 아니었다. GREEN 뒤에는 `_is_date` 가 그 400 을 낸다.
- 프런트 `frontend/test/lv0-source-20260907.test.tsx` 13건 — RED **11 failed**(`frontend/src` 를
  기점 상태로 되돌린 채 실측). 축자
  `TestingLibraryElementError: Unable to find an element by: [data-testid="reg-source-lv0"]`.
  통과한 2건은 「Lv1 이면 안 보인다」 계열이라 블록이 없을 때 참이다 — 그 둘은 회귀 시험이다.
- 드리프트 `db/platform/tests/0018-drift.sh` — ㈏(0018 없음)·㈐(downgrade 후)·㈑-b(백필 흉내)
  **세 자리가 red 를 내는 것**이 오라클임의 증명이다.
- GREEN — 서버 10/10 · 프런트 13/13 · `service-tests-core-api` 수집 901 실행 901 failed 0.

## 마이그레이션

- head `0018_rb6_lv0_source`(19자) · `down_revision = 0017_rb4_access_state_3` · `migration-single-head` green.
- 되돌림 = `DROP COLUMN` 둘. 배포 직후 **잃는 값 0**(전 행 NULL · backfill 0). 값이 쌓인 뒤에는
  사람이 적은 값이라 다시 올려도 안 돌아온다 — 그때의 정규 경로는 **소비를 멈추는 쪽**이다.
- ⭑ **NO FORCE ROW LEVEL SECURITY 구간이 없다** — `0017`(`M-4`)은 매핑 `UPDATE` 가 있어 FORCE 인
  두 표를 NOBYPASSRLS 롤이 0행으로 읽는 결함을 피하려고 그 구간이 필요했다. 이 WU 는 **데이터
  마이그레이션이 0** 이라 내릴 정책이 없다(`0015` 와 같은 자리). 그래서 `0017-drift.sh` 의 ㈑-c
  대조군(수정 전 델타 → 매핑 0행)에 해당하는 자리가 이 파일에는 없고, 대신 ㈑ 를 **소유자 롤로**
  적용해 「NOBYPASSRLS 마이그레이터가 이 델타를 끝까지 돌린다」를 실행으로 보였다.
- 적용 DB(`COLAB_APPLIED_DB_URL_PLATFORM`)에 `alembic upgrade head` 1회 — 그 전에는 `schema-diff`
  가 판정 red 였다(선언에 2열, 적용에 0열).

## 계약

- 추가 2키 `sourceUrl: [string,"null"]` · `sourceDownloadedOn: [string,"null"], format: date`
  → `DatasetCreate`·`DatasetUpdate`·`DatasetBasicInfo`. `basicInfo` 에서 **required 아님**.
- `contract-breaking` 축자 — `contract-breaking green — 기준 b982100 (3건) 대비 파괴적 변경 없음.`
- 소비자 수 `grep -rn 'sourceUrl\|sourceDownloadedOn' contracts/ services/ frontend/src | wc -l` = **91**
  (기점 실측 0 — 신설이다).

## 게이트 (`COLAB_GATE_REPORT_DIR=dev-package/reports/R-B/p3-lv0-source`)

```
schema-diff              green — 두 체인 각각 선언 = 적용.
migration-single-head    green — 두 체인 모두 head 1개.
contract-lint            green — seam 3건, 룰 위반 0.
contract-breaking        green — 기준 b982100 (3건) 대비 파괴적 변경 없음.
generated-up-to-date     green — 등기부 10건 전부 재생성 일치, 등기부 밖 자칭 생성물 0건.
db-boundary              green — 단위 7개 · 스캔 대상 342건 · 위반 0
service-tests-core-api   green — 선택자 «not e2e» · 수집 901 · 실행 901 · failed 0
frontend-typecheck       green
frontend-test            green
```

## 자기 표시

- `frontend/test/lv0-source-20260907.test.tsx` 의 harness 는 `register-steps-20260907.test.tsx` 의
  것을 **복사**했다(같은 화면·같은 대역). 두 벌이 갈릴 위험이 있으나 시험 파일 간 공유 모듈이
  이 레포에 없어 선례를 따랐다.
- 상세 안내의 판정 기준을 **파생 Lv**(`processingLevelDerived === 0`)로 뒀다. PRD-19 축자가
  「파생 Lv 가 Lv0 인 기존 행」이고, 그 행들은 사람 값이 `null` 이다(backfill 0).
- `sourceUrl` 에 형식 검사를 두지 않았다(문자열 여부만) — `VAL-006` 계열 자유 입력 규율.
- 등록·수정 두 자리 모두 서버 400 이 화면에 닿지 않던 경로가 있었다 — 등록은 `UploadModal.submit`
  의 `catch` 가 `UploadGone` 외 전부를 일반 실패 문구로 덮었고, 수정 폼은 형상 검사 자체가
  없었다. 인라인 선검사(형상이 `^\d{4}-\d{2}-\d{2}$` ∧ 유효 날짜가 아니면 전송하지 않는다)로
  해소했다(advisor ②).

## 하지 않은 것

- 목록(`DatasetRow`)에 두 열쇠를 넣지 않았다 — 목록 계약에 `basicInfo` 가 없고 지시문의
  「list basicInfo」에 해당하는 자리가 실재하지 않는다. `DatasetRow` 확장은 `WU-B7` 몫이다.
- 검색 색인을 건드리지 않았다 — 색인 재정의는 `M-10`(R-B-2 · WU-B7 뒤). 오라클 D-⑷ 가
  `search_vector` 가 두 칸을 물지 않았음을 단언한다.
- 대장 `status` 를 `done` 으로 넘기지 않았다(지시문). `PLAN-SoT §9` 〈N〉 등재도 하지 않았다.
- `03-HANDOFF.md`·`work-items.yaml`(1단계 외)·`40 COLAB-기획/` 무수정.

## §후속

1. **적용 DB 갱신이 게이트 밖에 있다** — `schema-diff` 는 「선언 ≠ 적용」을 red 로 내지만, 적용
   DB 에 `alembic upgrade head` 를 돌리는 것은 **사람의 절차**이고 어느 게이트도 그것을 잡지
   않는다. 레인마다 이 red 를 한 번씩 밟는다. 승격 후보 = 게이트가 적용 DB 를 스스로 올리거나,
   올리지 않은 상태를 「준비 red」로 갈라 표시하는 것.
2. `M-10` 「색인 재정의 `topic`→`category`」 문면과 실측의 불일치는 미해결로 남아 있다
   (라운드 §2 「마이그레이션 — 한 head」 ⚠). 이 WU 의 범위 밖이다.

## advisor ② 반영

오라클 = `/home/ttlhi10/.claude/jobs/18e71f5e/tmp/advisor2-b6.md` F1(필수)·F3(후속).

- **F1 ㈀** — 등록 `UploadModal.submit()` 직전과 수정 폼 저장(`useDatasetEdit.submit()`) 직전에
  `sourceDownloadedOn` 형상을 인라인으로 선검사한다. 형상이 `^\d{4}-\d{2}-\d{2}$` ∧ 유효 달력
  날짜가 아니면 **전송하지 않고** 서버 400 과 같은 문구 `내려받은 날은 날짜(YYYY-MM-DD)다.`
  (`SOURCE_DOWNLOADED_ON_INVALID` · `frontend/src/components/common/toastCopy.ts`)를 그 칸
  아래에 세운다(`reg-source-downloaded-on-error` · `edit-sourceDownloadedOn-error`).
- **F3** — 서버 `_is_date`(`services/core-api/src/colab_core/app/routes/catalog.py`)에
  `^\d{4}-\d{2}-\d{2}$` 선검사를 더했다. `date.fromisoformat` 은 3.11+ 에서 `20260820`(기본
  형식)·`2026-W34-1`(주 표기)도 받아 계약 `format: date`(RFC3339 full-date)보다 넓었다 —
  실측 결과 `20260820` 은 선검사 전에는 500(내부 오류)이었다.
- **RED → GREEN**:
  - `frontend/test/advisor2-b6-20260907.test.tsx`:2건 (등록 `2026.08.20` → `register` 미호출
    ∧ 문구 표시 · 수정 `8/20` → `update` 미호출 ∧ 문구 표시). RED 확인 — 구현 커밋을 스택 밖으로
    잠시 뺀 상태에서 두 건 모두 실측 실패(등록: `calls.registered` 길이 0 기대에 1 수신,
    수정: `calls` 길이 0 기대에 1 수신), 되돌린 뒤 두 건 모두 green.
  - `services/core-api/tests/test_lv0_source.py::test_basic_format_date_is_400`:1건. RED 확인 —
    선검사 전에는 500(`Internal Server Error`), 선검사 뒤 400.
- 회귀 확인 — `frontend/test/lv0-source-20260907.test.tsx`·`detail-edit.test.tsx`·
  `prd22-detail-edit2-20260907.test.tsx`·`upload.test.tsx`(141건)·`toast-copy-20260906.test.tsx`
  (15건)·`services/core-api/tests/test_lv0_source.py`(11건 전체) 전부 green.
- **게이트 밖 실측** — 워크트리의 시험용 일회용 DB(`a2_pg`)가 세션 시작 시점에 시드가
  비어 있어(host 재기동 뒤 재구성 필요 · `dev-package/RESTART.md §2-④`) `test_dataset_registration.py`
  등 core-api 전 시험이 이 WU 와 무관하게 이미 500/JSONDecodeError 로 실패하고 있었다
  (`git stash` 로 이 WU 변경분을 뺀 상태에서도 같은 22건 실패로 재현·확인). `CONTAINER=a2_pg
  APP_PASSWORD=<시험 env 값> services/core-api/tests/fixtures/setup-db.sh` 로 재시드한 뒤
  `test_lv0_source.py` 전체가 green 으로 돌아왔다 — 「main 과 동일」로 넘기지 않고 원인(DB
  재시드 불요 상태)을 확인한 결과다(colab-rules §3-3).
