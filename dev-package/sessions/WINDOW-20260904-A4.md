# staging 단독 창 4 — 단계 A(짝 백업) 집행 ＋ go/no-go ① 증거 (2026-09-04 21:05~21:25 KST)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md §1`·`§10-①`. 이 파일은 **이 회차에 실제로 잰 값**만 적는다.
> 집행 범위 = **A 만**. B(배포) 미착수 — go/no-go ① 은 Ted 판정 자리이고 이 회차는 증거만 세운다.
> 선행 회차 = `WINDOW-20260903-A3.md`(A 양식·창 3) · `WINDOW-20260903-B3.md`(배포 후속) · `VL-1-20260904.md`(계측).
> 이번 배포 후보 = **`main` @ `513013c`**.

## 0. 창 진입 전 상태 (실측 · 읽기 전용 · 2026-09-04 21:09~21:18 KST)

| 축 | 실측값 | 재는 방법 |
|---|---|---|
| 커밋 | **`513013c`** · 브랜치 `main` (`c502a49` 를 조상으로 포함 — `git merge-base --is-ancestor` 참 · 그 위 2 커밋은 `RESTART.md` 하나뿐) | `git log -1` |
| 워킹트리 | **변경 0건** | `git status --porcelain` = 0줄 |
| 워크트리 | **2개** — `main` ＋ `.claude/worktrees/review-pr1`(detached `0e294e5` · **다른 세션 소유 · 이 회차 무접촉**) | `git worktree list` |
| 서빙 릴리스 | **`64791f910f49`** — 앱 5종 이미지 태그 동일(`core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`) ＋ 원장 마지막 `deploy` 행 `2026-09-03T23:27:43+0900 deploy 64791f910f49 green` ＋ `LAST-SUCCESS.txt` 마지막 줄 동일 | `docker ps` ＋ `release-ledger.tsv` ＋ `LAST-SUCCESS.txt` |
| 컨테이너 | staging **8개 전부 healthy** (`nginx`·`frontend`·`core_api`·`pipeline_worker`·`viz_render`·`ai_service`·`cloudflared`·`pg`) · `Up 2 hours` | `docker ps` |
| 체인 head (platform) | **`0011_lv1_drop_level_user_set`** · 리비전 11건 · head 1개 | `gates/run.sh migration-single-head` |
| 체인 head (ai) | **`0005_k2b_concept_graph_seed`** · 리비전 5건 · head 1개 | 〃 |
| 원장 green `deploy` 행 | **≥2** (`64791f910f49` · `d9e89a3a074b` · `72408173be50`) | `release-ledger.tsv` |
| 창 시각 충돌 | **없음** — 21:09 착수, 크론은 03:30 · 04:10(월) · 04:40 | `crontab -l` |
| `COLAB_STAGING_ENV` | 셸 기본 환경에 **미설정** — B 착수 시 홈 0600 env 를 export 해야 한다 | 종전 회차와 같음 |

⚠ **staging 스택 밖 컨테이너 2건**(`a2_pg`·`ai_pg` · `Up 50 minutes` · 재부팅 뒤 게이트 DB 복구분 · `RESTART.md ㉲`)은 8개 계수에 넣지 않았다. **게이트 전용이고 이 회차 무접촉이다.**

⚠ **호스트 재부팅** — 약 1시간 전 재부팅. 스택은 자력 복구됐고 게이트 DB 는 `RESTART.md ㉲` 절차로 복구됐다. 재부팅 뒤 전수 게이트 = **50/0/0**(`gates-postreboot2.log` · 병렬도 `-j 1` · **잰 커밋은 `c502a49`** — §1.2).

## 1. B 가 실어 나를 변경분 — `git diff --stat 64791f9..513013c`

**총계 = 66 파일 · +3,793 / −98.**

| 영역 | 파일 | 증감 | 내용 |
|---|---:|---|---|
| `dev-package/reports` | 24 | +811 / −0 | 회차 보고 (코드 아님) |
| `frontend` | 18 | +769 / −45 | **BF-1·3·4·5·6** 수정 ＋ 화면 시험 |
| `dev-package/sessions` | 7 | +1,289 / −0 | 회차 기록 (코드 아님) |
| `services/viz-render` | 6 | +390 / −32 | **BF-2**(3857 리샘플 교체) ＋ **캐시 키 라벨 정정** ＋ **VL-1 `Server-Timing` 계측** ＋ 시험 |
| `services/core-api` | 4 | +121 / −6 | **VL-1 계측 중계**(`relay.py` `_request_timed` · `routes/preview.py`) ＋ 시험 |
| `dev-package/work-items.yaml` | 1 | +259 / −9 | 대장 |
| `dev-package/notes` | 1 | +66 / −0 | `BUGFIX-20260903-STATUS.md` |
| `dev-package` 산문 5종 | 5 | +88 / −6 | `WORK-UNITS`·`RESTART`·`PREVIEW-IMPLEMENTATION`·`PLAN-SoT`·`03-HANDOFF` |

**코드 표면은 둘뿐이다** — `frontend` 와 `services/{viz-render,core-api}`. 나머지 39 파일은 문서·대장이다.

**실릴 것 (사용자 언어)**
- **BF-1~BF-6** — 계보 그래프 빈 칸·화살표 / 지도 미리보기 흰 줄·성긴 채움 / 확대 한계 무동작 / 미리보기 캡션 부재 / 프로젝트 화면 여백·버튼 간격 / 설명문 `／` 줄 나눔.
- **VL-1 계측** — 값 조회 응답에 `Server-Timing` 을 싣는다. **동작 변경 0** · 계약 몸통 0 (표준 응답 헤더).
- **대장·문서**.

### 1.1 마이그레이션 — **0건. 실측이다(추정 아님).**

```
git diff --stat 64791f9..513013c -- db/          → 0 줄
ls db/platform/versions/   → 0001 … 0011.  **0012 없음**
ls db/ai/versions/         → 0001 … 0005.  그대로
```

⟹ **버그 회차가 마이그레이션을 더하지 않았다.** platform head `0011` · ai head `0005` 에서 움직이지 않는다. **이것이 롤백 한계를 정한다** — §4.

### 1.2 `gates/run.sh all` 을 다시 돌리지 않은 근거

재부팅 뒤 전수 **50/0/0**(`gates-postreboot2.log` · 병렬도 `-j 1` · 단독 8 · 병렬 42 · 미선언 0)은 **`513013c` 가 아니라 `c502a49` 에서 쟀다** — 로그 파일 mtime `2026-09-04T21:05:59` 이고 그 뒤 커밋 둘이 `21:06:15`(`92b3fb7`) · `21:07:21`(`513013c`) 이다. **감추지 않고 적는다.**

**그 델타가 문서뿐이라는 실측 —**

```
$ git diff --stat c502a49..513013c
 dev-package/RESTART.md | 37 +++++++++++++++++++++++++++++++++++++
 1 file changed, 37 insertions(+)
```

⟹ **코드 변경 0건 · 게이트 대상 파일 0건.** 그래서 `c502a49` 에서 잰 **50/0/0** 을 `513013c` 의 값으로 읽는다.
전수 재실행 대신 **배포 전 8건 단독 재실행**(§2)을 이 커밋에서 돌렸다. 창 점유 규약(pg 슬롯 풀 호스트 전역 4) 그대로.
⚠ **`gates/run.sh all` 을 `513013c` 에서 돌리지 않은 것을 「이번에 세지 않은 판정기준」으로 남긴다** — B 뒤 전수(§6⑵)가 그것을 닫는다.

## 2. 배포 전 검사기 — 전부 **단독 실행**(`COLAB_GATE_JOBS=1`) · 축자

```
contract-lint green — seam 3건, 룰 위반 0.
generated-up-to-date green — 등기부 4건 전부 재생성 일치, 등기부 밖 자칭 생성물 0건.
contract-breaking green — 기준 HEAD (3건) 대비 파괴적 변경 없음.
migration-single-head green — 두 체인 모두 head 1개.
    # db/platform: 리비전 11건 · head 1개 (0011_lv1_drop_level_user_set)
    # db/ai: 리비전 5건 · head 1개 (0005_k2b_concept_graph_seed)
frontend-typecheck green — tsc --noEmit(frontend/tsconfig.json · include=src·test) 오류 0건.
frontend-test green — vitest run(frontend/vite.config.ts · jsdom) 통과 593건 · 실패 0건.
    Test Files  38 passed (38)
         Tests  593 passed (593)
service-tests-core-api green — 실행 626건 전부 통과 (skipped 0 · deselected 6 은 요약줄에 드러나 있다).
    — 선택자 «not e2e» · 수집 626 · 실행 626 · skipped 0 · deselected 6 · failed 0 · errors 0 · 소요 88.9초
service-tests-viz-render green — 실행 279건 전부 통과 (skipped 0 · deselected 40 은 요약줄에 드러나 있다).
    — 선택자 «not e2e and not perf» · 수집 279 · 실행 279 · skipped 0 · deselected 40 · failed 0 · errors 0 · 소요 23.4초
```

- **8건 전부 green · red 0 · 못 돈 것 0.**
- ⚠ **`service-tests-core-api` 는 오늘 간헐 red 였다**(연구실 A 데이터셋 계수 오염 · 4회 중 3회). **이 회차는 1회차 실행에서 바로 green 이라 재실행 조건이 서지 않았다** — 감추는 것이 아니라 **이번 표본에서는 재현되지 않았다**. ⛔ **「고쳐서 green」이 아니다 — 그 간헐성의 원인은 `[미확인]` 그대로**이고, B 뒤 전수에서 다시 뜰 수 있다.
- ⚠ `service-tests-ai-service`·`service-tests-pipeline-worker` 는 이 회차 **단독 재실행하지 않았다** — 변경분에 그 두 단위가 **0 파일**이고 `gates-postreboot2.log` 가 같은 커밋에서 green 으로 세었다. **이번에 세지 않은 판정기준으로 남긴다.**
- 게이트 총수 = **50**.

## 3. 캐시 키 라벨 — 다시 갈린다 (코드 실측)

```
$ grep -rn "warp+nearest\|warp+blockavg" services/viz-render/src -r
services/.../d7_visualization/preview.py:433:  key = cache.render_cache_key(long_side=DETAIL_SIDE, downsample="warp+nearest",

$ git show 64791f9:services/.../d7_visualization/preview.py | grep 'downsample="warp'
331:  key = cache.render_cache_key(long_side=DETAIL_SIDE, downsample="warp+blockavg",
```

⟹ **서빙 중인 `64791f910f49` 는 `warp+blockavg`, 배포 후보 `513013c` 는 `warp+nearest`.** `src` 안에 `warp+blockavg` **잔여 0건**.
`downsample` 라벨은 `render_cache_key` 의 입력이므로 **③지도형 층의 키가 배포 즉시 전건 갈린다.**

**⟹ `〈308〉` 에서 다시 구운 3 데이터셋의 타일이 또 stale 이 된다. 배포 뒤 재굽기 1회가 더 필요하다.**

| # | 데이터셋 id | 이름 | `〈308〉` 재굽기 renderId |
|---|---|---|---|
| 1 | `01M0YWDXN7CA13KB7MQC9PGEW5` | GK-2A NDVI 월평균 | `01M1M09Z6Y4F08210QZEKW978K` |
| 2 | `01M1JM3JTQSSTKR9A2QQ67QN81` | U-Net 예측 (동봉 격자) | `01M1M09ZH744J9ASB1A999E6PN` |
| 3 | `01M19RENXAANZ2HDWM4Z46C37Y` | PV-1 업로드 | `01M1M09ZT1XKFNE6QXKWN2CA2T` |

⚠ **갈리는 것은 ③지도형 한 층뿐인가 — `[미확인]`.** 라벨은 ③층 호출부에만 있으나 `〈308〉` 실측에서는 **세 층 키가 전부 갈렸다**(팔레트 입력이 섞여 원인이 갈리지 않았다 · `WINDOW-20260903-B3 §3.1`). **재굽기는 데이터셋 단위(3층 동시)로 도는 경로 하나뿐이라 실무상 차이가 없다.**

## 4. 단계 A — 배포 전 짝 백업 (집행 완료 · GREEN)

- 명령 = `infra/staging/backup/backup-full.sh` (플래그 없음) · `main` 체크아웃에서.
- 최종 줄 축자 — **`2026-09-04T21:19:22 전범위 백업 GREEN — 원장 2 프로파일 ＋ 볼륨 2 개 · 보관처 규약 밖 파일 0`**
- 0단 보관처 위생 통과(규약 밖 파일 0) · `═══ 1단` · `═══ 2단` 표지 양쪽 실재.

| 산출물 | 스탬프 | 크기(B) | 판정 |
|---|---|---|---|
| `platform-….sql.gz` | `20260904T211840` | 30,800 | GREEN (통과 7 · SKIP 0) — 표 25 · 데이터 행 420 · 신선도 0분 |
| `ai-….sql.gz` | `20260904T211840` | 5,159 | GREEN (통과 7 · SKIP 0) — 표 6 · 데이터 행 91 · 신선도 0분 |
| `vol-uploads-….tar.gz` | `20260904T211841` | 393,734,961 | GREEN (통과 8 · SKIP 0) — V4 157=157 · V5-a 133=133 · V5-b 전건 |
| `vol-previews-….tar.gz` | `20260904T211919` | 34,824,840 | GREEN (통과 6 · **승인된 SKIP 1** = V5 오라클 명시 면제 `none`) — V4 **227=227** |

**짝(회차 한 벌) — `.pair` 실측**

```
vol-uploads-20260904T211841.pair   -> platform-20260904T211840.sql.gz
vol-previews-20260904T211919.pair  -> platform-20260904T211840.sql.gz
```

두 원장 덤프의 스탬프가 **동일**(`20260904T211840`) — P5(같은 회차) 조건이 시각차 0초로 선다.

**최신본 재검사** — `infra/staging/backup/latest-check.sh` → **`최신본 재검사 GREEN — 프로파일 2 개 · 볼륨 2 개`**.

**덤프가 담은 스키마 상태 (읽기 전용 대조)**

```
platform-20260904T211840.sql.gz : alembic_version_platform = 0011_lv1_drop_level_user_set · '0012' 문자열 0건
ai-20260904T211840.sql.gz       : alembic_version_ai       = 0005_k2b_concept_graph_seed
```

⟹ 이 덤프는 **배포 후에도 그대로일 스키마**를 담는다 — 스키마 되돌림 재료가 아니라 **데이터 되돌림 재료**다.
⭑ **`vol-previews` 227 파일**은 `RC-1` 회수 후 계수(`자리 파일 227`)와 **일치**한다 — 회수 뒤 상태가 그대로 백업됐다.

경로 = `$COLAB_BACKUP_DIR`(홈 백업 보관처). **값·접속 문자열은 이 문서에 적지 않는다.**

## 5. B 가 무엇을 하는가 · 되돌림의 한계

- 명령 = `infra/staging/deploy.sh --target staging` (플래그 없음) · `main` 체크아웃에서 · 홈 0600 env 를 export 한 셸에서.
- **마이그레이션 = 없음**(§1.1 실측). `deploy.sh ⑧` 이 돌아도 head 는 `0011`/`0005` 에서 움직이지 않는다.
- **이미지 6종 재빌드** — `core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`·`migrator`.

| 축 | 값 |
|---|---|
| 스키마 변경 | **없음** (실측) |
| `rollback.sh --to-last-green` 의 복원 범위 | **이미지 = 배포 내용 전부** ⟹ **완전 복구** |
| 파괴적 복원(`--yes-drop-schema`) 필요 여부 | **불필요** |
| 롤백 대상 | **`64791f910f49`** — 현재 서빙이자 직전 green |
| 그 태그 이미지 6종 실재 | ✅ **6/6** — `docker images` 실측: `core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`·`migrator` 전부 `:64791f910f49`. ＋ `:i2` 6 ＋ `:prev` 6 |

⚠ **롤백이 되돌리지 못하는 것 하나 — 캐시.** `〈308〉` 과 같은 자리다. **B 뒤 재굽기 전에 롤백하면 손실 0**(옛 이미지가 옛 키 `warp+blockavg` 를 다시 계산하고 옛 산출물이 자리에 그대로 있다), **재굽기 뒤 롤백하면 옛 키 산출물이 회수로 치워졌을 수 있다.** **순서가 되돌림 비용을 정한다.**

## 6. 배포 후 이 창이 돌릴 순서 (착수 전에 고정한다)

### ⑴ 값 조회 4건 — **8 healthy 직후, 다른 무엇보다 먼저**

⛔ **이것이 이 창에서 가장 시간에 민감한 한 자리다.** `VL-1 §0`·`§5`⑶ 축자 — `〈304〉` 의 1,164 / 2,111 ms 스파이크는 **배포 직후 첫 요청**에서 났고, 그것이 후보 **ⓓ(프로세스 재기동 후 첫 접촉 · 모듈 적재 · 연결 풀)** 그 자체다. `VL-1` 회차는 **재시작 0회**라 그 갈래를 한 점도 재지 못했다. **다음 배포가 그것을 잡는 유일한 창이고, 놓치면 다음 재기동까지 다시 `[미확인]`** 이다.

- **시점** = 배포 판정 6항 중 **8 healthy 를 확인한 그 순간**. ⛔ **게이트·재굽기·현물 확인보다 먼저.** 어떤 요청도 그 앞에 넣지 않는다(헬스 체크는 스택이 스스로 부르는 것이라 어쩔 수 없다 — **사람이 부르는 첫 요청이 이것**이어야 한다).
- **요청** = 데이터셋 4건 × **각 1회** · 공개 엣지 · 같은 점 하나.

```
POST https://www.colab-hydro.com/api/v1/datasets/<DATASET_ULID>/value-lookup
  Authorization: Bearer <해당 연구실 사용자 토큰 · 경로로만 · 값은 적지 않는다>
  User-Agent: colab-v2-window-a4/1.0 (VL-1 branch d probe)
  Content-Type: application/json
  {"point":{"lat":36.75,"lon":127.40}}
```

| 순서 | 데이터셋 id | 이름 | 자리 산출물 |
|---|---|---|---|
| 1 | `01M0YWDXN7CA13KB7MQC9PGEW5` | GK-2A NDVI 월평균 | 있음(타일) |
| 2 | `01M1JM3JTQSSTKR9A2QQ67QN81` | U-Net 예측 | 있음(타일) |
| 3 | `01M19RENXAANZ2HDWM4Z46C37Y` | PV-1 업로드 | 있음(타일) |
| 4 | `01M0Y1VQ09TMHAC78ESDRTMAZD` | DEM | **없음(대조군)** |

- **남길 것** = 응답 **헤더 `Server-Timing` 원문 축자** ＋ `curl` 의 `time_connect`·`time_appconnect`·`time_starttransfer`·`time_total` ＋ `http_code`. 점군·순서·헤더는 `VL-1-PROBE-20260904.sh` 와 같게 두어 **같은 축에서 비교되게** 한다.
- **읽을 구간** — core-api 가 `coreAccess`·`coreRelay`·`coreTotal`, viz-render 가 `vizResolve`·`vizFindTile`·`vizReadPoint`·`vizTotal`. **`coreTotal − vizTotal` = 중계 비용**, **`time_total − coreTotal` = 엣지**.
- ⚠ **재굽기 전이라 1~3 은 `200 ＋ available:false` 가 정상이다**(③층 키가 갈렸다). **지연 측정에는 영향이 없다** — 자리 탐색까지는 같은 경로를 탄다. **값의 유무는 ⑸ 에서 재굽기 뒤에 다시 잰다.**
- ⚠ **1회씩만 부른다.** 두 번째 요청은 이미 따뜻하다 — **첫 접촉은 데이터셋당 한 번뿐**이고 그것이 재는 대상이다.

### ⑵ 게이트 전수 — `gates/run.sh all -j 1`

배포 뒤 **50** 을 다시 센다. `artifact-ownership` 은 **재굽기 전**이므로 red(판정) 이 뜰 수 있다 — 그것이 정상이고 ⑷ 가 닫는다.

### ⑶ 재굽기 — 공개 엣지 `POST /api/v1/previews` 3건

```
curl -X POST https://www.colab-hydro.com/api/v1/previews \
  -H "Authorization: Bearer <토큰 · 값은 적지 않는다>" \
  -H "Content-Type: application/json" \
  -d '{"target":{"datasetId":"<DATASET_ULID>"},"style":{"palette":"단색-파랑"}}'
```

- 대상 = §3 표의 **3건**. 팔레트 = `단색-파랑`(`GET /api/v1/preview-palettes` 첫 값 · `〈308〉` 과 **같은 값**으로 두어 팔레트 차이를 변수에서 뺀다).
- → 202 ＋ `renderId` · `GET /api/v1/previews/{renderId}` 로 완료 확인 · **3층 × 3건 = 9층 전건 200** 을 확인한다.
- **판정 = staging WRITE** (`/srv/viz-previews` 볼륨에 바이트 기록). **Ted 의 승인이 이 자리를 덮어야 한다.**
- ⛔ 운영자용 재굽기 CLI 는 레포에 없고, `POST /viz/v1/renders` 는 nginx 가 404 로 막는다. **인가된 경로는 이 하나뿐이다**(`WINDOW-20260903-A3 §6`⑵).
- ⭑ **캐시 키 전후를 한 데이터셋(`01M0YWDXN7CA13KB7MQC9PGEW5`)에서 3층 다 남긴다** — `〈308〉` 의 `4e0ac3e3…`(③지도형)이 갈리는지가 **라벨 변경이 실제로 물린 증거**다.

### ⑷ `artifact-ownership` 전후

`COLAB_GATE_JOBS=1 ./gates/run.sh artifact-ownership` 을 **재굽기 전·후 각 1회**. 홈 0600 env 를 주입한 셸에서(없으면 `red(준비)` — **fail-closed 가 옳은 동작이고 우회하지 않는다**).

- **직전 값**(`〈309〉` RC-1 회수 후) = `대상 103 · 살아 있다 84 · 접수분 0 · **고아 0** · 판정 불가 19` · 자리 파일 **227**.
- **기대** — 재굽기가 **더하기만** 하므로 대상·살아 있다가 각 **+9**(3 데이터셋 × 3층). 고아·판정 불가 19 는 움직이지 않는 것이 정상이다.
- ⛔ **움직이지 않으면 재굽기가 안 일어난 것이다.** 계수 변화가 재굽기의 증거다.
- ⚠ **옛 키 벌 9 개가 새로 고아가 될 수 있다** — 그러면 `RC-1` 계열 회수가 다시 필요하다. **이 창에서는 지우지 않는다**(회수는 별개 승인 자리 · `CR-2`⑶).

### ⑸ 현물 확인

| # | 대상 | 기대 |
|---|---|---|
| ㉠ | **BF-1** 계보 그래프 | 빈 칸 없음 · 고아 화살표 0 |
| ㉡ | **BF-2** ③지도형 미리보기 (재굽기 **뒤**) | 흰 줄 0 · 성긴 채움 해소 · 결측 행 0 |
| ㉢ | **BF-3** 확대 한계 | 한계 상태가 **처음부터** 표시 · 버튼이 무동작으로 보이지 않음 |
| ㉣ | **BF-4** 미리보기 머리·범례 | 데이터셋 이름 · 파일명 · 원본 격자 · 범례 변수명 표기 |
| ㉤ | **BF-5** 프로젝트 목록·상세 | 뿌리 여백·카드 면 적용 · 연구실 설정 버튼 gap 6px |
| ㉥ | **BF-6** 설명문 `／` | 표시 단계에서 줄 나눔. ⚠ 정본 §8 「한 줄 요약」 문구와의 충돌은 **Ted 확인 대기** — 위반이면 `git revert 02738e4` |
| ㉦ | 값 조회 3건 (재굽기 **뒤**) | 200 ＋ `available:true` ＋ **값이 실린다** |
| ㉧ | 값 조회 나머지 11건 | 200 ＋ `available:false` 유지 (재굽기 대상 아님) |
| ㉨ | 검색 | `POST /api/v1/dataset-searches` — 질의 반영 · `verified` 걸름 동작 |
| ㉩ | `/lab` 연구실 화면 | 200 · 정직한 빈 상태 문구 · ⚠ **BF-8**(`LabPage` 뿌리 스타일 규칙 부재)은 **미수정**이라 여백 결함이 남아 있는 것이 정상 |
| ㉪ | 타 연구실 자원 접근 | **404** (403 이 아니다 · `DEPLOY-269 §3`) |

⛔ **자기 연구실 묘비 410 은 이 창도 재지 못한다** — staging 에 `deleted_at` 행 0건이고 만들려면 삭제(파괴적 쓰기)가 필요하다. `〈308〉` 과 같다.

## 7. go/no-go ① — 증거 대조표 (`RUNBOOK §10-①`)

| # | 조건 | 실측 | 판정 |
|---|---|---|---|
| ㉠ | A 두 프로파일 GREEN | §4 · platform·ai 각 통과 7 · SKIP 0 | ✅ |
| ㉡ | 볼륨 산출물 실재 ＋ 짝 성립 | `vol-uploads` 393,734,961B · `vol-previews` 34,824,840B · `.pair` 둘 다 `platform-20260904T211840.sql.gz` | ✅ |
| ㉢ | 워킹트리 변경 0건 | `git status --porcelain` 0줄 | ✅ |
| ㉣ | 워크트리 | `main` ＋ `review-pr1`(다른 세션 · 레포 읽기 레인 · staging 무접촉) ＋ 이 회차의 `lane-window-a4`(문서 전용) | ⚠ **판단이지 실측이 아니다** — B 착수 전 `review-pr1` 이 staging 을 만지지 않음을 확인한다 |
| ㉤ | 스택 가동 중 | 8개 healthy | ✅ |
| ㉥ | 크론 시각 비충돌 | 21:09 착수 · 크론 03:30/04:10/04:40 | ✅ |
| ㉦ | 원장 green `deploy` ≥2 | `64791f910f49`·`d9e89a3a074b`·`72408173be50` | ✅ |
| ㉧ | 이미지 빌드 파괴 요인 | 배포 전 게이트 **8건 green**(§2) · 타입 오류 0 · 서비스 시험 실패 0 | ✅ |
| ㉨ | 마이그레이션 델타 | **0줄** · `0012` 부재 · 덤프 안 `'0012'` 0건 | ✅ |
| ㉩ | 롤백 대상 이미지 6종 실재 | `64791f910f49` **6/6** | ✅ |
| ㉪ | **승인 행이 배포 행보다 먼저** | ❌ **미기재** — Ted 의 GO 로만 채워진다 | ⏸ |
| ㉫ | `COLAB_STAGING_ENV` | 셸 기본 미설정. B 실행 셸에서 export 필요 | ⚠ 조치 필요 |
| ㉬ | 재굽기 승인(staging WRITE) | 필요 — §6⑶ | ⏸ **Ted 승인 필요** |

## 8. 판정 — **GO 권고 (조건 둘 붙임)**

**결정적 사실 = 마이그레이션 0건이 실물로 확인됐고(`git diff … -- db/` 0줄 · `0012` 부재 · 짝 덤프의 `alembic_version_platform` = `0011`), 그 덕에 `rollback.sh --to-last-green` 하나로 배포 내용 전부가 되돌아간다. 롤백 대상 `64791f910f49` 이미지 6/6 이 실재한다.**

배포 전 게이트 **8건 green** 이 이미지 빌드가 깨질 자리를 미리 쟀다. 코드 표면은 프런트와 서비스 둘뿐이고, 서비스 변경의 절반은 **동작이 바뀌지 않는 계측**(`Server-Timing`)이다.

**붙이는 조건 둘**
1. **`review-pr1` 확인** — staging 을 만지는 레인이면 창을 닫는다. 레포만 읽는 레인이면 그대로 간다.
2. **재굽기 승인** — §6⑶ 은 staging WRITE 다. 승인 없이 배포만 하면 **3 데이터셋의 미리보기가 stale 인 채로 남는다**(BF-2 의 사용자 효과가 화면에 나타나지 않는다).

**no-go 로 뒤집는 조건**
- ㉪(승인 행) 미기재 상태로 `deploy.sh` 를 부르는 것. **승인 행이 먼저다.**
- **⑴(값 조회 4건)을 8 healthy 직후에 부를 준비가 안 된 상태로 배포하는 것.** 그 창은 배포당 한 번뿐이고, 놓치면 `〈304〉` 스파이크 원인은 다음 재기동까지 `[미확인]` 이다.

**이번에 세지 않은 판정기준** — `service-tests-ai-service`·`service-tests-pipeline-worker` 단독 재실행 · `gates/run.sh all` 전수(재부팅 직후 값을 그대로 읽었다) · `service-tests-core-api` 간헐 red 의 원인.

## 9. Ted 의 GO 시 원장에 덧붙일 승인 행 — **축자 (이 회차는 적지 않았다)**

`~/colab-v2-releases/release-ledger.tsv` 에 탭 구분 5열로 **한 줄** append (시각은 append 시점으로 갱신) —

```
2026-09-04T<HH:MM:SS>+0900	approve	513013c	-	-	승인자=Ted 본것=go/no-go ① GO · 짝 백업 platform-20260904T211840/vol-uploads-20260904T211841/vol-previews-20260904T211919 GREEN · latest-check GREEN · 마이그레이션 델타 0줄(0012 부재 · 덤프 alembic_version_platform=0011) · 배포전 게이트 8건 green(contract-lint·generated-up-to-date·contract-breaking·migration-single-head·frontend-typecheck·frontend-test·service-tests-core-api·service-tests-viz-render) · 롤백 대상 64791f910f49 이미지 6/6 실재 · 배포 직후 값 조회 4건 Server-Timing 채취 승인 포함 · 사후 재굽기(POST /api/v1/previews 3건 · staging WRITE) 승인 포함 · WINDOW-20260904-A4
```

⛔ **이 회차는 append 하지 않았다.** Ted 의 GO 문면을 받은 뒤 B 착수 셸에서 배포 행보다 **먼저** 넣는다.

## 10. 이 회차가 만진 것 · 만지지 않은 것

- **staging 쓰기 0** — 부른 것은 `docker ps`·`docker images`(읽기)와 `pg_dump`(읽기 전용 백업)뿐이다.
- **컨테이너 수명 조작 0 · 파괴 플래그 0 · DDL 0 · SQL 쓰기 0 · 배포 0 · push 0 · 병합 0.**
- `a2_pg`·`ai_pg` **무접촉**. `review-pr1` 워크트리 **무접촉**.
- 자격증명·접속 문자열 **미출력**.
