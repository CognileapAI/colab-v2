# R6 — 계보 제안 서비스를 세운다 ＋ 소비자·생산자의 계약 표류를 닫는다

레인 = `wt/lane2-search` · 회차 2026-08-30 · 운영 스택 무접촉 · 배포 0건 · push 0건 · `main` 병합 0건.
`main`(`cfc98e3`) 병합 = **충돌 0** (52 파일 · 3372 삽입).

---

## 1. 완료 정의 (대장 `work-items.yaml` `K3` · 축자)

> ⭑ ⟨기재 2026-08-29 · Ted 판정 `PLAN-SoT §9 〈211〉`⟩ **⑴ D4 쓰기 경로 부재 음성 테스트 green** —
> AI 가 계보를 직접 쓰지 못한다(`CLAUDE.md §3-2` 불변 규칙) **⑵ 정직한 빈 상태** — 제안할 것이
> 없으면 지어내지 않고 「없음」을 돌려주며 화면이 그대로 적는다 **⑶ 품질의 수치 합격선은 이
> 항목이 정하지 않는다 — 별건으로 뺀다.** 사유 = 모수가 계보 간선·데이터셋 각각 한 자릿수여서
> 퍼센트가 오라클 행세만 한다(계수와 근거 = `〈211〉`). 별건의 재개 조건도 `〈211〉` 에 적었다

**미작성 아님.** 수치 합격선을 이번에 만들지 않았다(`〈211〉`-㉰ 이 재개 조건을 조건으로 적었다).

## 2. 정본이 말한 것과 침묵한 것

정본 = `E-04_업로드와_계보_확정/documents/Policy·PRD` (레포 밖 · 읽기 전용).

**말한 것 (따랐다)**

| 항목 | 축자 |
|---|---|
| 두 종류 | `AI 계보 제안: 가공 전 데이터·가공 방식, 근거 표시, 확신도 3단(확실/애매/모름)` (`PRD §5.1`) |
| 확신도 | `퍼센트는 쓰지 않는다` (`Policy §1.3-6`) · `퍼센트 금지` (`§4 용어`) |
| 근거 | `근거 없는 제안은 내놓지 않는 것이 전제다` (`Policy §10`) · `근거가 없으면 '모름'으로 표시한다` (`PRD §6`) |
| 빈 상태 | `제안 카드 대신 정직한 빈 상태를 보여준다` (`Policy §8`) |
| 일괄 승인 | `일괄 승인 버튼은 없다` (`Policy §1.3-7`) · `계보가 AI 창작물이 되는 것을 막기 위해 의도적으로 만들지 않는다` (`PRD §5.2`) |
| 후보 범위 | `헤더 속성·연구실 기존 데이터를 근거로 부모 후보를 찾는다` (`Policy §10`) |
| 사람 확인 | `사람이 확인한 것만 계보에 저장된다` (`PRD §6`) |

**침묵한 것 (정하지 않았다 — `§7` 로 올린다)**

1. **부모 후보를 누가 뽑는가** — AI 자신인지 서버인지. 기능 단위로만 적고 주체 구분이 없다
2. **부모 없는 가공 방식 제안이 성립하는가** — 모든 서술이 두 제안을 짝으로만 다룬다
3. **확신도 판정 기준** — 어떤 근거 강도가 `확실` 이고 어떤 것이 `애매` 인지의 매핑
4. **원자료 판정 방법** — `가공 흔적이 없어 원자료로 판정되면` 의 판정 대상·규칙
5. **범위를 먼저 밝히는 규칙이 계보 제안에도 붙는가** — 검색 쪽에만 있다
6. **자동 호출인가 버튼인가** — 어느 쪽도 명문이 없다 (버튼 신설은 별건 `LV-2`)

⚠ **겸하여 발견** — `40 COLAB-기획/_delta_업로드계보_260826.md` 가 E-04 를 업로드·계보 한정으로
대체한다고 적고, **AI 일체(메타 자동 추출·계보 제안·자연어 검색)를 Phase 2+ 로 뺐다**고 적는다.
`CLAUDE.md §0` 의 현재 위상(제안 서비스 = stage 2)과 방향이 같다. 대체본 원문은 이번에 읽지 못했다 — **`[미확인]`**.

## 3. 착수 전 실측 — 무엇이 있었나

| 있던 것 | 상태 |
|---|---|
| 중계의 정직한 빈 상태 (`relay.py honest_empty_suggestions`) | 있음 · green (`〈225〉`-㉮) |
| 0건 세 갈래 구분 (㈎㈏㈐) · 화면 `data-kind` | 있음 · green |
| `ai-no-lineage-write` 음성 게이트 | green |
| **제안을 실제로 만드는 서비스** | **없음** — `ai-service/app/main.py` 가 「`K3` 의 자리이고 여기서 흉내 내지 않는다」로 비워 둠 |
| **나가는 요청이 계약과 같은 모양인지 보는 시험** | **없음** — `_FakeAi` 가 본문을 읽어 **버렸다** |

## 4. 무엇을 지었나

### 4.1 ai-service — 계보 제안 표면이 실제로 선다

- **신설** `src/colab_ai/domains/d10_suggestion.py`
  - `Suggestion` — 제안 한 장. **규격을 어기면 객체가 서지 않는다**
  - `SuggestionEnvelope.build` — 응답 조립. **세 상태**(아래 `§6`)
- **개정** `src/colab_ai/app/main.py` — `POST /lineage-suggestions` 를 연다.
  계약 열쇠 집합을 닫아 검증(`additionalProperties: false` 를 표면이 실제로 요구) ·
  본문 연구실 ↔ 헤더 연구실 이중 확인 · 주체 없으면 401 · `file` 없으면 400.

### 4.2 core-api — 나가는 요청을 계약에 맞춘다 (표류 정정)

**동결된 계약(2026-08-22)이 먼저이고 중계(2026-08-23)가 뒤에 다른 모양으로 섰다.**
계약은 `file`(`UploadedFileMeta`)이 required 이고 `uploadId` 라는 낱말이 **0회**인데,
중계는 `file` 없이 `uploadId` 를 보내고 있었다. **정본은 계약이므로 고친 쪽은 중계다.**
계약 개정 0건 · 마이그레이션 0건.

- `ports/relay.py` — `upload_id` → `file_meta`
- `app/relay.py` — 본문에 `file` 을 싣는다
- `app/routes/ingestion.py` — **신설** `_uploaded_file_meta()`. 업로드 원장(`files` ＋
  보류된 자동 메타 사건)에서 읽은 값으로만 조립한다. **헤더를 못 읽은 항목은 열쇠 자체를
  만들지 않는다** — 빈 문자열로 채우면 「못 읽음」과 「값 없음」이 갈리지 않는다.

**왜 아무도 못 잡았나** — 생산자가 없어 거절할 쪽이 없었고, 계약 게이트는 정적 스펙만 보고
실제 요청 바이트를 보지 않는다. green-by-skip 이 아니라 **검사 대상이 애초에 없던 자리**다.

## 5. 쓰기 경로 부재를 어떻게 지켰나

- ai-service 코드에 D4 접근이 없다. **없는 것으로 지킨다** — 카탈로그·플랫폼 커넥션 0
- 계약에 확정 오퍼레이션이 없다 (시험이 `paths` 를 훑어 쓰기 메서드 0건을 단언)
- 제안은 프로세스 안에서 태어나 응답과 함께 죽는다. 저장·원장 0
- **게이트가 실측 대상을 더 갖게 됐다** — L2 코드층 `1739건 → 1743건`

## 6. 세 상태 — 새 검사 전건에 대해

| 상태 | 동작 | 증명 |
|---|---|---|
| **대상 있음** | 제안 전건을 타입으로 검증. `dict` 는 받지 않는다 | `test_제안이_있으면_전건을_타입으로_검증한다` · `test_타입을_지나지_않은_제안은_실릴_수_없다` |
| **명시 면제(0건 선언)** | 통과하되 **건수와 뒤진 범위를 응답이 스스로 말한다** | `test_선언된_0건은_건수와_사유를_응답에_드러낸다` (`"0건"`·`"12"` 를 사유에서 확인) |
| **아무 선언 없음** | **실패** | `test_선언_없는_0건은_통과하지_못한다` · `test_목록_자체가_없으면_통과하지_못한다` |

⚠ **왜 이 구조인가.** 모수가 작다(계보 간선 6 · 데이터셋 12 · `〈211〉`-㉯). 제안 기능은
재료가 없으면 **무엇이든 0건**이라 「응답을 검사한다」가 대상 0건인 채 통과한다.
그래서 규격을 **응답 검사가 아니라 제안을 만드는 자리**에 두었다 — 대상이 0건이어도 규칙이 시험된다.

### 변이 시험 (되돌리면 red · 되돌린 코드는 전부 복원)

| 되돌린 것 | 결과 |
|---|---|
| 확신도 enum 검사 제거 | **8 failed** (숫자 `1`·`True` 가 통과) |
| 근거 필수 검사 제거 | **2 failed** |
| 선언 없는 0건 통과시킴 · `None` 허용 | **2 failed** |
| 중계가 다시 계약 밖 열쇠를 보냄 | **4 failed** |
| ai-service 에 D4 쓰기 한 줄 삽입 | `ai-no-lineage-write` **red** (`[⑦] 쓰기 SQL + D4 테이블`) |

## 7. ⛔ 멈춘 자리 — 이 서비스는 아직 제안을 만들지 못한다

**표면은 섰고, 참인 답은 여전히 0건이다.** 지어낼 수 없기 때문이지 게을러서가 아니다.

- **「가공 전 데이터」** — 계약 `ParentCandidateSuggestion` 이 `parentDatasetId` 를 required 로
  둔다. 그 값의 출처는 카탈로그 하나뿐인데 **이 배포 단위는 카탈로그를 읽지 못하고**(`〈72〉-㉮`
  가 그은 분담), **요청 본문에도 후보를 실을 자리가 없다.** 계약을 고쳐야 풀린다.
- **「가공 방식」** — 부모 없이 단독으로 낼 수 있는지를 **정본이 말하지 않는다**(침묵 2).

**그래서 0건을 `degraded: true` ＋ 사유로 낸다.** `false` 로 내면 「살펴봤는데 없더라」라는
**하지 않은 판정**을 주장하게 된다 — `〈225〉`-㉮ 가 가른 ㈏ 와 ㈐ 를 다시 섞는 것이다.

**⛔ `K3` 를 닫지 않았다.** `status: open` 무변경. 정본 침묵 6건(`§2`) 중 1·2 가 풀려야
「제안 서비스」가 제안을 만든다. **완료 정의 ⑴⑵ 는 green 이나, 그 글자대로 닫을지는 제품 판정이고
정본이 말하지 않는다** — 지난 회차(`〈225〉`)가 올린 같은 질문이 그대로 남는다.

## 8. 실측

| 대상 | 값 | 시점·조건 |
|---|---|---|
| ai-service | **145 passed** / 0 failed (기준선 98 ＋ 신규 47) | 2026-08-30 · 일회용 사전 DB |
| core-api | **495 passed** / 0 failed | 2026-08-30 · 일회용 postgres (`--rm` ＋ tmpfs ＋ 포트 미공개) |
| frontend | **314 passed** / 14 파일 · 0 failed | 2026-08-30 · FE 무수정 |
| 구현 전 red | ai-service **14 failed / 3 passed** ＋ 모듈 2건 수집 실패 · core-api **4 failed** | red 확인 후 구현 |

### 게이트 (`./gates/run.sh all -j 2` · 29종)

**green 26 / red 3.**

- **`ai-no-lineage-write` green** — L1 seam 3 · **L2 ai-service 텍스트 1743건** · L3 체인 12/14
- `contract-lint`·`contract-breaking`·`seam-consistency`·`import-boundary`·`banned-import`·
  `generated-up-to-date`·`work-item-consistency` 전건 green
- `schema-diff` **red(준비)** — 적용 DB 미지정. 운영 DB 는 읽기 전용 경계라 대지 않았다. 기준선과 같다
- `autometa-loss` **red(준비)** — 같은 사유. 기준선과 같다
- `stage2-markers` **red(판정)** — ⚠ **기준선(27/2)과 갈린 한 건이고, 이 회차와 무관하다.**
  `pipeline-worker` 무수정(파일 0건 변경). 원인 = **핀 자체가 서로 안 맞는다** —
  `services/pipeline-worker/requirements.txt:19-20` 이 `pydantic==2.13.4` ＋
  `pydantic_core==2.46.4` 를 박았는데 설치된 `pydantic 2.13.4` 는 실행 시 `2.46.5` 를 요구한다
  (설치본은 핀과 정확히 일치 — 실측). **통과로 세지 않았고 우회·재핀·범위 축소도 하지 않았다.**
  푸는 법 = 파이프라인 레인이 두 핀을 맞춘다

## 9. 경계

- 운영 스택 **무접촉** — `colab_v2_staging_*` 질의 0 · 컨테이너 조작 0 · DDL 0
- `a2_pg`·`ai_pg` 는 **그대로 두었다**(정지·재생성 0)
- 접속 문자열·비밀번호를 출력·문서 어디에도 남기지 않았다
- 계약(`contracts/`) 무수정 · 마이그레이션 0 · `PLAN-SoT §9` 무접촉 · `03-HANDOFF` 무접촉
- 화면의 호출 방식 무변경 (`LineageStep.tsx` 미접촉 — 버튼 전환은 별건 `LV-2`)
- 배포 0 · cron 0 · push 0 · `main` 병합 0

## 10. `PLAN-SoT §9` 등재용 (번호 공란 — 값을 옮겨 적는 회차가 채운다)

| 〈 〉 | **계보 제안 서비스의 표면을 세우고 소비자·생산자의 계약 표류를 닫는다 — 다만 제안을 만들 재료가 정본 침묵으로 아직 없다** | **집행 ＋ 실측 ＋ 정지 (2026-08-30 · Claude · 레인 `wt/lane2-search` · `main`(`cfc98e3`) 병합 충돌 0 · push 0).** 정본 = `sessions/R6-SUGGEST-SERVICE.md`. **㉮ 표면이 섰다** — `POST /lineage-suggestions` 가 ai-service 에 실재하고, 확신도 3값 enum 과 근거 필수·한 줄을 **값 검사가 아니라 생성자**가 진다(`domains/d10_suggestion.py`). 모수가 작아 「응답을 검사한다」가 대상 0건인 채 통과하는 자리라 규격을 **만드는 자리**로 옮겼다. **세 상태** — 제안 있음(전건 타입 검증) · 0건 명시 선언(**건수와 뒤진 범위를 사유가 말한다**) · **선언 없음은 실패**. 변이 5종 전건 red 로 증명(되돌린 코드 전부 복원). ／ **㉯ 소비자가 동결된 계약과 다른 모양을 보내고 있었다** — 계약(2026-08-22 동결)은 `file`(`UploadedFileMeta`) required ＋ `additionalProperties: false` 인데 중계(2026-08-23)는 `file` 없이 계약에 낱말조차 없는 `uploadId` 를 보냈다. **동결이 먼저이므로 정본은 계약이고 고친 쪽은 중계다** — 계약 개정 0 · 마이그레이션 0. 못 잡은 이유는 green-by-skip 이 아니라 **검사 대상 부재**다(시험의 가짜 서버가 본문을 읽어 버렸고, 계약 게이트는 요청 바이트를 보지 않는다). 요청 본문을 계약 yaml 로 대조하는 시험 4건을 신설했다. ／ **㉰ ⛔ 제안을 만들지 못한다 — 정본 침묵 둘이 막는다.** ⓐ **부모 후보를 누가 뽑는가**(정본은 기능 단위로만 적고 주체를 구분하지 않는다 — 계약에 후보를 실을 자리가 없고 이 단위는 카탈로그를 못 읽는다) ⓑ **부모 없는 가공 방식 제안이 성립하는가**(모든 서술이 두 제안을 짝으로만 다룬다). 그래서 0건을 **`degraded: true` ＋ 사유**로 낸다 — `false` 는 「살펴봤는데 없더라」라는 **하지 않은 판정**의 주장이고 `〈225〉`-㉮ 가 가른 ㈏·㉐ 를 다시 섞는다. **원자료 판정도 주장하지 않는다**(`rawDataLikely: false`) — 정본이 판정 방법을 적지 않았다. **`K3` 무변경(`open`).** ／ **㉱ 정본 침묵 6건** = 후보 선정 주체 · 부모 없는 가공 방식 · 확신도 판정 기준 · 원자료 판정 방법 · 계보 제안의 범위 선고지 · 자동 호출 대 버튼. ⚠ 겸하여 `40 COLAB-기획/_delta_업로드계보_260826.md` 가 **AI 일체를 Phase 2+ 로 뺐다**고 적는 것을 확인했다(대체본 원문 미독 — **`[미확인]`**). ／ **㉲ 실측** — ai-service **145 passed**(기준선 98 ＋ 신규 47) · core-api **495 passed** · frontend **314 passed** · 전건 실패 0. 구현 전 red 확인 = ai-service 14 failed ＋ 모듈 2건 수집 실패 · core-api 4 failed. **게이트 `-j 2` = green 26 · red 3 / 29종**, `ai-no-lineage-write` **green**(L2 실측 대상 **1739 → 1743건**으로 늘었다). `schema-diff`·`autometa-loss` 는 기준선과 같은 **적용 DB 미지정**. ⚠ **기준선(27/2)과 갈린 한 건 = `stage2-markers`** 이고 **이 회차와 무관하다** — `pipeline-worker` 파일 0건 변경이며 원인은 **핀 자체의 불일치**(`requirements.txt:19-20` 의 `pydantic==2.13.4` ＋ `pydantic_core==2.46.4` 인데 설치된 pydantic 이 `2.46.5` 를 요구 · 설치본은 핀과 정확히 일치 — 실측). **통과로 세지 않았고 우회하지 않았다.** ／ **㉳ 경계** — 운영 스택 **무접촉**(질의 0 · 컨테이너 조작 0) · 계약·마이그레이션·화면 호출 방식 무수정 · 배포 0 · push 0. *2026-08-30* |
