# NAME-SPLIT — 한 이름 `S1` 이 가리키던 셋을 가른다 (화면 레인 접두사 개명)

> **이 파일은 `PLAN-SoT §9` 등재문 초안이다.** 등재 시점에 `PLAN-SoT.md` 가 다른 에이전트 소유라
> 여기에 완성해 둔다. **오케스트레이터가 §9 표에 한 행으로 옮긴다.**
> 회차 = 2026-08-29 · 브랜치 `name-split` · 문서 전용(운영 스택·`services/**`·`contracts/**` 무접촉).

---

## §9 등재문 초안 (표 한 행 형태 — 번호는 등재 시 부여)

| 〈미부여〉 | **한 이름 `S1` 이 셋을 가리키던 것을 화면 레인 접두사만 `SC-` 로 갈라 닫는다** | **판정 (2026-08-29 Ted)** — 근거 조사 = `sessions/DOC-DRIFT-SWEEP.md §4` 질의 ②(읽기 전용 회차). ⭑ **㉮ 갈림의 실체 — 식별자 `S1` 이 셋을 동시에 가리켰다.** ⑴ **WU `S1`** = 초기 데이터 적재 범위 확정(대장 `work-items.yaml` 의 항목 식별자 · `03-HANDOFF §1 T-S 초기 데이터` 행 · `WORK-UNITS §8.5`·`§11`) ⑵ **화면 레인 접두사 `S1-*`** = `03-HANDOFF §1 T-S stage 1 물결` 표의 다섯 레인(미리보기·검색·온톨로지 그래프·프로젝트·미리보기 화면) ⑶ **집행 계획 문서 `S1-PLAN`** = `sessions/S1-PLAN.md`·`S1-PLAN-REFOUND.md` 와 그 체크포인트 계열(`S1-api`·`S1-fe`·`S1-detach`·`S1-search-a` 등). `03-HANDOFF §1` 에서 `S1` 로 읽히는 행이 다섯이고 넷은 ✅·하나는 🟧 라 **기계가 어느 것의 상태인지 못 갈랐다** — `work-item-consistency` 의 `㈓ S1` 이 그 자리다. ⭑ **㉯ 확정 = ⑵ 화면 레인 접두사만 바꾼다. 새 접두사 = `SC-`.** **기각 둘** — **ⓐ WU `S1` 개명 = 기각.** 장부의 식별자라 대장·인계·계획서·순서표가 전부 딸려 오고 **이력 추적이 끊긴다**(`S1` 을 진입조건·`depends_on` 으로 참조하는 항목이 `S2`·`S3` 계열에 있다). **ⓑ 계획 문서만 개명 = 기각. 문제가 안 풀린다** — WU 와 화면 레인 접두사가 여전히 겹친다. **채택 근거** = 셋 중 화면 레인 접두사가 **가장 최근에 붙었고 가장 얕게 박혀 있다**(레인 다섯은 전부 `sources: ["03-HANDOFF §1 T-S"]` 단일 출처 · `WORK-UNITS` 읽기에는 이 레인이 아예 없다 — 대장 각 레인 `note` 축자 「WORK-UNITS 읽기에는 이 레인이 없다」). ⭑ **㉰ 새 접두사 = `SC-`(screen · 화면 레인). 다섯 레인은 `SC-1`~`SC-5` 다.** 대응 = `SC-1` 미리보기 렌더(종전 `S1-preview`) · `SC-2` 검색(`S1-search`) · `SC-3` 온톨로지 그래프(`S1-ontology-graph`) · `SC-4` 프로젝트 화면 셋(`S1-project`) · `SC-5` 미리보기 화면(`S1-preview-ui`). **고른 이유 셋 — 조건마다 실측으로 답한다.** **ⓐ 이 레포에서 이미 다른 것을 가리키지 않는다** — 착수 전 실측(2026-08-29 · `node_modules` 제외) = `grep -rnI "SC-" .` **0건** · `grep -rnIow "SC" --include=*.md --include=*.yaml --include=*.py --include=*.ts --include=*.tsx .` **0건** · `grep -rnI "SC1" .` **0건**. ⚠ **이 확인을 건너뛰면 작업이 무의미하다** — 이 레포는 방금 「가공」을 갈랐고(`〈189〉`) 「미리보기」가 넷을 가리키는 것도 확인했다. **새 충돌을 만들지 않았다.** **ⓑ 성격이 드러난다** — `SC` = screen(화면). 이 표의 머리도 「레인」이고, `SC-n` 은 **화면 레인 묶음**을 가리키는 것 외의 뜻을 갖지 않는다. **ⓒ 기존 명명 관례와 어긋나지 않고, 검사기가 통째로 읽는다** — 대문자 두 자 ＋ `-` ＋ 숫자는 이 대장의 다수 관례다(`R-1`·`X-2`·`T-1`·`J-1`·`V-1`·`F-1`·`PV-1`·`DP-1`). 그리고 검사기 식별자 정규식 `[A-Z]{1,3}(?:-[A-Z0-9]+|\d+[a-z]?)`(`gates/tools/work_item_consistency.py:92`)가 **`SC-1` 을 통째로 읽는다** — 대장 `id` 와 진실원 행이 **1:1 로 대조된다.** ⚠ **여기서 한 번 틀렸다가 게이트가 잡았다 — 숨기지 않고 적는다.** 1차 안은 `SC1-preview` 처럼 **소문자 서술어를 접미로 붙인 형태**였는데, 같은 정규식이 그것을 **`SC1` 까지만** 읽어 다섯 행이 전부 하나의 식별자로 접혔고 대장에 `SC1` 이 없어 **㈐ red 5건**이 났다(실측 — 불일치 5 → **9**). **그리고 그것이 종전 `S1-*` 가 숨어 있던 방식과 같은 방식이다** — 다섯 레인 행이 전부 `S1` 로 접혀 WU `S1` 에 대조되고 있었고, WU `S1` 이 `conflict` 라 검사기가 **조용히 건너뛰고 있었다**(`work_item_consistency.py` ㈐ 의 `if it.get("status") == "conflict": continue`). **즉 이름을 가르지 않았다면 이 다섯 행은 계속 안 세어졌다.** 소문자 접미형을 고르면 다섯 행이 「검사 대상 밖」으로 떨어지는데, **그것은 검사 범위를 줄이는 것이라 고르지 않았다**(`CLAUDE.md §3`). 서술어는 버리지 않고 **대장 `name` 과 진실원 행 본문**으로 옮겼다(예 `name: "화면 레인 — 미리보기 렌더 (viz-render ①②③ …)"`). ⭑ **㉱ 그래서 WU `S1` 을 `done` 으로 닫는다.** 이름이 갈리자 `03-HANDOFF §1` 에서 `S1` 로 읽히는 행은 **이 항목 하나**뿐이고, 세 읽기가 모두 ✅ 다 — `03-HANDOFF §1 T-S 초기 데이터` `S1` 행 ✅ · `WORK-UNITS §8.5` `↳ S1 실적` = 「✅ 닫힘 (2026-08-23)」 · `§11` 초기데이터 줄 `S1 ✅`. 실물 = `SEED-DATA.md`(2,122 파일 전부 배정 · 미분류 0 · 데이터셋 15건 · 계보 3갈래 · `〈57〉`). **상태값을 고른 것이 아니라 이름을 갈라 갈림 자체를 없앤 것이다.** ⭑ **㉲ 고친 자리와 남긴 자리를 가른다.** **고친 참조 = 51곳**(세는 기준 = 다섯 레인 토큰의 출현 수 · 시점 2026-08-29 · 파일 7 — `03-HANDOFF.md` **15** · `work-items.yaml` **6**(항목 `id` 5 ＋ 본문 1) · `sessions/S1-PLAN.md` **14** · `S1-PLAN-REFOUND.md` **7** · `S2-PLAN-DRAFT.md` **6** · `P2-MEASURE.md` **2** · `STAGE2-READY.md` **1**). **＋ 새로 적은 개정 표시 7곳**(대장 레인 `note` 5 · 대장 `S1` `note` 1 · `03-HANDOFF` 레인표 배너 1) — 이것은 치환이 아니라 신규 기재다. **의도적으로 남긴 옛 표기 = 21곳** — ⑴ `03-HANDOFF.md` **10곳**: 「／ 이전 다음 WU」(74행 2) · 「／ 이전 진입조건(`W4`)」(84행 3) · 「／ 이전 진입조건(`W2`)」(85행 1) · 블로커 #12 의 「／ 이전」 원문(3) · 블로커 #19 의 「*(이하 원문)*」(1) ⑵ 대장 `work-items.yaml` 의 개정 `note` **5곳**(「종전 식별자는 `S1-preview` 였다」 형태 — 개명 이력 자체라 남는 것이 맞다) ⑶ `PLAN-SoT §9` **6곳**(`〈82〉`·`〈83〉`·`〈84〉`·`〈179〉`·`〈189〉` — **결정 이력이므로 소급 수정하지 않는다**. `〈189〉-㉵-ⓔ` 가 같은 규약을 이미 적었다). **원문을 지우지 않는다**(`CLAUDE.md §7`). ⭑ **㉳ 건드리지 않은 것 셋.** **ⓐ 계획 문서 `S1-PLAN`·`S1-PLAN-REFOUND` 의 파일명과 그 체크포인트 계열**(`S1-api`·`S1-fe`·`S1-detach`·`S1-fe-preview`·`S1-fe-search`·`S1-search-a`·`S1-search-b`·`S1-search-infra`·`S1-canon`·`S1-ontology-canon`·`S1-seed`·`S1-K4b`)과 조사 산출 파일명(`S1-W0-baseline`·`S1-CONTRACT-GAP-SWEEP`·`S1-upload-path-audit`·`S1-scope-cut-inventory`·`S1-preview-rescope-review`·`S1-v1-info-harvest`·`S1-ai-search-readiness`) — **⑶ 계열이고 이번 판정 대상이 아니다.** **ⓑ WU `S1` 의 식별자.** **ⓒ 코드** — 실측으로 **레인 접두사가 코드에 0건**이다(아래 ㉴). ⭑ **㉴ 코드 접촉 여부 = 없다(실측).** `S1-preview`·`S1-search`·`S1-ontology-graph`·`S1-project`·`S1-preview-ui` 다섯 토큰을 `services/**`·`frontend/**`·`db/**`·`infra/**`·`contracts/**` 에서 찾은 결과 **0건**. 코드에 남은 `S1` 문자열은 **다른 것**이다 — ⑴ 마이그레이션 파일명 `0005_s1_search_index.py`·`0006_s1_trgm_matching.py`(⑶ 계획 계열의 흔적 · 파일명 변경은 마이그레이션 이력이라 별도 판단) ⑵ `db/ai/versions/0004_k1b_concept_graph.py:25` 주석의 `S1-search-infra`(⑶ 계열) ⑶ 위성·파이프라인 도메인의 `stage1`·`S1` 밴드 등 무관 토큰. **`services/**` 는 다른 에이전트 소유이므로 고치지 않고 보고한다.** ⭑ **㉵ 게이트 실측 (2026-08-29 · 본 회차 전후 · 축자).** 아래 「게이트 축자」 절 참조. ⭑ **㉶ `[미확인]`** — 아래 「`[미확인]`」 절 참조. *2026-08-29* |

---

## 게이트 축자

**`./gates/run.sh work-item-consistency`**

- **전 (기준선 · 이 브랜치 착수 시점)**
  `work-item-consistency: 대장 88건 · ㈐ 진실원 대조 56행 · ㈏ 체크리스트 대조 46건 · ㈑ 착수 후보 33행 · ㈒ 기한 5건 · ㈓ conflict 5건`
  `::error::work-item-consistency — 불일치 5건` (`S2b`·`R-1`·`S1`·`S2`·`2단-BC120`)
  검사 대상 밖 8건 · 항목표 아닌 표 1건.
- **후 (이 회차 종료 시점)**
  `work-item-consistency: 대장 88건 · ㈐ 진실원 대조 62행 · ㈏ 체크리스트 대조 47건 · ㈑ 착수 후보 33행 · ㈒ 기한 5건 · ㈓ conflict 4건`
  `::error::work-item-consistency — 불일치 4건` (`S2b`·`R-1`·`S2`·`2단-BC120` — **`S1` 이 빠졌다**)
  검사 대상 밖 8건 · 항목표 아닌 표 1건 — **전후 동일. 「안 본 자리」를 늘리지 않았다.**
- **차이** — **불일치 5 → 4**(닫힌 것 = `S1` 하나 · 새로 난 것 0). **㈐ 진실원 대조 56행 → 62행(＋6)** — 다섯 화면 레인 행이 이제 각자 대장 항목과 1:1 로 대조되고(＋5), WU `S1` 이 `conflict` 를 벗어나 대조 대상이 됐다(＋1). **즉 검사 범위가 늘었다.** **㈏ 46 → 47**(WU `S1` 이 `conflict` 를 벗어나 체크리스트 대조에 들어왔다). 대장 88건 · ㈑ 33행 · ㈒ 5건 불변.

**`./gates/run.sh planning-freshness`** — 전 **green** · 후 **green**, 축자 `planning-freshness green — 15개 임베드 블록 전부 원본과 일치.` **기획 정본 임베드에 이 이름은 닿지 않는다** — 15개 임베드 블록은 전부 `PRD_*`·`Policy_*`·`DataModel_*` 이고 다섯 레인 토큰이 **0건**이다(실측).

---

## `[미확인]`

| 무엇 | 왜 못 쟀나 | 푸는 법 |
|---|---|---|
| `PLAN-SoT §9` 등재 번호 | `PLAN-SoT.md` 를 다른 에이전트가 쓰고 있어 이 회차가 쓰지 않았다 | 오케스트레이터가 §9 표에 위 초안을 한 행으로 옮기고 번호를 부여한다 |
| 마이그레이션 파일명 `0005_s1_search_index.py`·`0006_s1_trgm_matching.py` 의 `s1` | ⑶ 계획 계열의 흔적이고, 마이그레이션 파일명 변경은 이력·체인에 닿는 별도 판단이다 | 이름 정리 회차에서 마이그레이션 체인 영향(단일 head·`schema-diff`)과 함께 판정 |
| `db/ai/versions/0004_k1b_concept_graph.py:25` 주석의 `S1-search-infra` | `services/**`·`db/**` 는 이 회차 소유가 아니고, ⑶ 계열이라 이번 판정 대상도 아니다 | ⑶ 계열 이름을 다룰 때 함께 |
| 기획 정본 폴더의 `_작업계획서_P2라운드_260826.md` 48행 `S1-project` | **레포 밖 파일이고 이 회차 소유가 아니다**(`40 COLAB-기획/`). `planning-freshness` 의 15개 임베드 블록에는 들어 있지 않아 게이트에는 닿지 않는다 | 기획 정본을 손대는 회차에서 `SC-4` 로 고치거나, 그 문서의 시점 표기로 남긴다 — **사람 판정** |

**이번에 세지 않은 판단기준** — ⓐ ⑶ 계획 계열(`S1-PLAN` 과 그 체크포인트 20여 종)의 이름이 WU 식별자와 또 겹치는 자리가 있는지 전수(예: 대장 `C1` 항목 `note` 축자 「⚠ `S1-PLAN` 체크포인트 `C1` 과 이름이 충돌한다」 — **같은 유형의 미해결 겹침이 하나 더 있다**) ⓑ `SC-1`~`SC-5` 다섯 레인의 `completion_def` 가 전부 「완료 정의 미작성」인 것(이번 회차 범위 밖) ⓒ `03-HANDOFF §1` 레인표가 `WORK-UNITS` 어느 절과도 대조되지 않는 구조 자체.
