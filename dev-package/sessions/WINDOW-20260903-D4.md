# staging 단독 창 — 단계 D4 검사기 교정 · 복원 완주 · 원상복구 (2026-09-03 · 워크트리 `lane-window-d4`)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md §4` ＋ `infra/staging/restore/RUNBOOK.md`.
> 선행 회차 = `sessions/WINDOW-20260903-D3.md`(복원이 둘째 원장에서 정지 · **부분 복원 상태로 인계**).
> **Ted 판정 ⓐ(2026-09-03) = 「리허설을 끝내고 원상복구한다」.** 등재 = `PLAN-SoT §9 〈286〉`.
> **이 회차는 ①검사기 교정 → ②복원 완주 → ③R-1 실측 → ④원상복구 를 전부 집행했다.**

## 0. 창 진입 상태 (D3 인계분 · 실측 재확인)

| 축 | 값 |
|---|---|
| 커밋 | `3ae90c8` · `main` 에서 갈린 워크트리 `lane-window-d4`(브랜치 동명) |
| `colab_platform` | 체인 `0010_p6_access_request` · `processing_level_user_set` **존재** · 13/130/6 · **`colab_app` 권한 0건** |
| `colab_ai` | 체인 `0005_k2b_concept_graph_seed` — **손대지 않음** |
| 볼륨 | `uploads`·`previews` **손대지 않음** |
| 컨테이너 | `pg`·`cloudflared` **둘만** · 앱 6종 정지 · 사이트 무응답 |

---

## 1. 검사기 교정 — `restore-db.sh` 가 프로파일 합격선을 쓰게 했다

**결함(`D3 §4.2` 실측)** — `restore-db.sh` 만 전역 `COLAB_BACKUP_MIN_TABLES`(**20** · platform 형상)를
`verify-artifact.sh` 에 넘겼다. 형제 호출 둘(`backup/latest-check.sh` · `backup/restore-rehearsal.sh`)은
이미 `profile_min_tables` 를 쓴다. ⟹ 표 6개가 **정상**인 `colab_ai` 덤프가 합격선 20 에 걸려
**구조적 거짓 RED** 로 거부됐고, ai 원장이 어느 방향으로도 움직이지 않았다.

**고친 자리 셋.**

| 파일 | 무엇 |
|---|---|
| `infra/staging/backup/lib.sh` | `profile_for_db()` 신설 — DB 이름 → 프로파일. **못 찾으면 기본값으로 안 떨어지고 `미해결` 을 돌려 호출처가 선다**(`〈171〉-㉯` 규약) |
| `infra/staging/restore/restore-db.sh` | 합격선을 `profile_min_tables`/`profile_min_rows` 로 교체. ⭑ **행 합격선도 종전에는 전역 `1` 이었다** — 함께 프로파일 값(platform 190 · ai 45)으로 올렸다. **검사가 세졌다** |
| `infra/staging/restore/selftest-restore.sh` | 픽스처 **4건 증설 (34 → 38)** |

### 1.1 픽스처가 못 박은 두 방향 — **범위를 줄인 것이 아님의 증거**

| # | 픽스처 | 결과 (축자) |
|---|---|---|
| SR24 | ⓐ 온전한 ai 덤프(표 6 · 행 96) | `합격선 프로파일 = ai (테이블 4 · 행 45)` · `PASS  C4 CREATE TABLE 6개 (>= 4)` — **통과** |
| SR25 | ⓑ **진짜 잘린 ai 덤프(표 2 · 행 32)** | `FAIL  C4 CREATE TABLE 2개 < 4` ＋ `FAIL  C5 데이터 행 32건 < 45` → **여전히 RED** |
| SR26 | ⓑ′ 중간에서 끊긴 platform 덤프(표 5) | `FAIL  C4 CREATE TABLE 5개 < 20` → **여전히 RED**(platform 합격선 20 유지) |
| SR27 | 프로파일에 없는 DB 이름 | `ERROR: DB 'colab_unknown' 에 대응하는 백업 프로파일이 없다` → **RED** |

**셀프테스트 요약 축자 — `복원 셀프테스트 GREEN — fixture 38 건 전부 기대대로`** (종전 34).
형제 셀프테스트 회귀 0건 — `backup/selftest.sh` 30건 GREEN · `backup/selftest-volume.sh` 24건 GREEN.

⭑ **픽스처는 홈 env 파일에 기대지 않는다** — `COLAB_BACKUP_CONFIG` 로 세계를 고정한다. 호스트 설정이 바뀌어도 판정이 안 흔들린다.

---

## 2. D-2 복원 완주 — **옛 짝 `20260902T234147/48` 로 스택을 세웠다**

| # | 단계 | 결과 |
|---|---|---|
| 1 | `restore-db.sh --db colab_ai --dump ai-20260902T234148.sql.gz --yes-drop-schema` | **GREEN** — `합격선 프로파일 = ai (테이블 4 · 행 45)` · C4 `6개 (>= 4)` · C5 `91건` · `결과: GREEN (통과 6건 · 승인된 SKIP 1건)` · 적재 완료 |
| 2 | `db-bootstrap.sh app-grants` | `app role grants: ok (colab_app@platform · colab_ai_app@ai · SELECT only)` |
| 3 | `restore-volume.sh --volume uploads --yes-overwrite-volume` | **GREEN** — V4 154건 전건 일치 · V5-a 130=130 · V5-b 전건 · 매니페스트 전건 sha256 일치 |
| 4 | `restore-volume.sh --volume previews --yes-overwrite-volume` | **GREEN** — V4 128건 전건 일치 · 매니페스트 전건 sha256 일치 |
| 5 | `docker compose -f infra/staging/compose.i2.yml --env-file <env> up -d` | **8개 healthy** (`--build` 미사용) |
| 6 | `verify-restored.sh` | **GREEN — `--manual-ok` 없이** |

⚠ **`COLAB_RELEASE_TAG` 를 앞에 붙여야 뜬다**(`RESTART.md §2-②`). 값은 손으로 적지 않고
`~/colab-v2-releases/release-ledger.tsv` 의 마지막 green `deploy` 행에서 읽었다 — 실측 `77813d391f94`.

### 2.1 옛 짝 상태 실측 (계수 롤 postgres)

| 축 | 값 |
|---|---|
| 체인 head | platform **`0010_p6_access_request`** · ai **`0005_k2b_concept_graph_seed`** |
| `processing_level_user_set` 열 | **존재(1건)** |
| 행수 | `d3_dataset` **13** · `d3_file` **130** · `d4_lineage_edge` **6** |
| 앱 롤 권한 | `colab_app` **100건** · `colab_ai_app` **6건** — `§2`-2 가 되돌렸다(D3 는 0건이었다) |

### 2.2 `verify-restored.sh` GREEN 축자 (요지)

- ① ② ⑥ `d3_dataset 13` · `d3_file 130` · `d4_lineage_edge 6` — **기대치를 짝 덤프에서 읽었다**
- ③ `d9_method_term 13` · `d9_place_alias 4` · `d9_topic_synonym 5` · `d9_concept 49` · `d9_concept_edge 19`
- ③-보 `POST /searches 200 · degraded=false · 확장 낱말 1건` (질의 「강우데이터」 → 「강우·강수」)
- ④ **`digest 대조 GREEN — 10 건 전건 일치 · 면제 0`** · **④-b `복원 전(P8)과 동일`** — D3 의 P8 기록과 붙는다
- ⑤ `⑤-0` 앱 롤 비-superuser·비-BYPASSRLS · `⑤-a` 양성 13행 · `⑤-b` 음성 0행 · `⑤-c` ai 앱 롤 49
- ⑦ 헬스 6종 200
- 최종 축자 — **`복원 검증 GREEN — 자동분 전건 통과 ＋ 손검사 2건(③-보 읽는 경로 · ⑤ 앱 롤 양성·음성)도 기계가 돌았다`**

⭑ **D3 이 `[미확인]` 으로 남긴 「되돌아온 열이 앱을 깨뜨리는가」가 여기서 풀렸다** —
`0010` 스키마 ＋ `77813d3` 앱 이미지로 8개가 healthy 로 섰고 `verify-restored` 가 GREEN 이다.
**그 열은 무해했다** — 이제 추론이 아니라 실측이다.

---

## 3. `R-1` ⑵ 실측 — **복원된 스택 위에서**

| 항 | 값 | 시점 |
|---|---|---|
| ai-service 직행 `POST /searches` 상태 | **200** | 옛 짝 스택 · 원상복구 스택 **양쪽에서 각 1회** |
| `degraded` | **false** | 〃 |
| 원 질의에 없던 **확장 낱말** | **1건** (「강우데이터」 → 「강수」) | 〃 |

**`〈255〉`-㉯ 의 판정 기준 축자 = 「200 ＋ `degraded:false` ＋ 원 질의에 없던 확장 낱말 ≥1」. 셋 다 섰다.**

### 3.1 정문 경로 — **`[미확인]` 유지 (판정 기준 아님)**

- 살아 있는 주체 표는 `0600` · uid `10001` 이고 이 회차의 실행 주체는 uid `1000` 이다.
  **한 번 시도했고 읽기가 거부됐다. 우회하지 않았다** — 토큰을 손으로 짓지 않았고 표를 읽지도 않았다.
- **Ted 가 칠 한 줄** (root) —
  `sudo -E env COLAB_STAGING_ENV_FILE=~/.colab-v2-staging.env infra/staging/restore/throwaway-stack.sh reissue-subjects`
  뒤이어 `docker restart colab_v2_staging_core_api` · `/api/v1/me` 200 ＋ 옛 토큰 401 로 회전 확인.
- ⚠ **`〈255〉`-㉯ 축자 — 「그 행의 잔여 조각인 core-api 중계 다리(정문 `POST /api/v1/dataset-searches`
  세션 왕복)**만** stage 3 으로 뺐다」.** ⟹ **정문은 `R-1` ⑵ 의 판정 기준이 아니다.** ai-service 직행이 기준이다.

### 3.2 사용자 시야 읽기 전용 호출 2건 — **`401` (베어러 부재)**

| 호출 | 상태 |
|---|---|
| `GET /api/v1/datasets` (카탈로그 목록) | **401** |
| `GET /api/v1/datasets/{id}` (데이터셋 상세 · 실 ULID 1건) | **401** |

- **경로는 살아 있고 fail-closed 다** — 무자격 요청을 통과시키지 않았다.
- ⚠ **인증된 사용자 시야는 여전히 `[미확인]`** 이고 **막힌 자리는 `§3.1` 과 같다**(주체 토큰 root 권한).
  **401 을 「통과」로 세지 않는다.** 푸는 법도 `§3.1` 의 한 줄과 같다.

---

## 4. ④ 원상복구 — **집행 완료 (Ted 승인 ⓐ에 포함)**

### 4.1 되돌림의 되돌림 재료 — **새로 떴다**

`infra/staging/backup/backup-full.sh` **GREEN** — `전범위 백업 GREEN — 원장 2 프로파일 ＋ 볼륨 2 개 · 보관처 규약 밖 파일 0`.

| 산출물 | 스탬프 |
|---|---|
| 원장 | `platform-20260903T011623.sql.gz` · `ai-20260903T011623.sql.gz` |
| 볼륨 | `vol-uploads-20260903T011624.tar.gz` · `vol-previews-20260903T011701.tar.gz` (둘 다 짝 = `platform-20260903T011623.sql.gz`) |

⭑ **이 회차가 담은 것은 「옛 짝으로 되돌아간 상태」다** — 원상복구를 다시 되돌려야 할 때의 재료다.

### 4.2 preflight — **GREEN (실패 0건)**

입력 넷 = `COLAB_STAGING_ENV_FILE` · `COLAB_RESTORE_PRE_BACKUP=…/platform-20260903T011623.sql.gz` ·
`COLAB_RESTORE_CAUSE=「원상복구 · 단독 창 2026-09-03 · 〈286〉」` · `--record-digests /tmp/d4-post-digests.tsv`.
명령 = `preflight.sh --stamp 20260903T000540 --record-digests /tmp/d4-post-digests.tsv`.

P1~P5 · P5-b(볼륨 짝 2종) · P6(비밀 7종 ＋ env = 8건) · P7 · P8(digest **10건 전건 일치 · 면제 0**) · P9 · P10 **전건 PASS**.
**최종 줄 축자 — `사전조건 GREEN — 복원을 시작해도 된다`.**

### 4.3 단계별 결과

| # | 단계 | 결과 |
|---|---|---|
| 1 | `compose … stop frontend nginx core-api pipeline-worker viz-render ai-service` | 완료 · 잔존 `pg`·`cloudflared` **둘만** |
| 2 | `restore-db.sh --db colab_platform --stamp 20260903T000540 --yes-drop-schema` | **완료** |
| 3 | `restore-db.sh --db colab_ai --dump ai-20260903T000540.sql.gz --yes-drop-schema` | **완료** — C4 `6개 (>= 4)` · C5 `91건` · GREEN. ⭑ **`§1` 의 교정이 없으면 여기서 D3 와 같은 RED 였다** |
| 4 | `db-bootstrap.sh app-grants` | `ok (colab_app@platform · colab_ai_app@ai · SELECT only)` |
| 5 | `restore-volume.sh --volume uploads --archive vol-uploads-20260903T000541.tar.gz` | **GREEN** — 매니페스트 전건 sha256 일치 |
| 6 | `restore-volume.sh --volume previews --archive vol-previews-20260903T000618.tar.gz` | **GREEN** — 매니페스트 전건 sha256 일치 |
| 7 | `compose … up -d` (`--build` 없음) | **8개 healthy** |
| 8 | `verify-restored.sh` | **GREEN — `--manual-ok` 없이** |

⚠ **ai 스탬프 실측** — 이 회차 보관처의 ai 산출물은 `ai-20260903T000540.sql.gz` 다(`…41` 이 아니다).
볼륨 쪽은 `vol-uploads-…000541` · `vol-previews-…000618` 이고 **둘 다 `.pair` 가 `platform-20260903T000540.sql.gz`** 를 가리킨다 — 짝이 맞다.

### 4.4 **종점 증명** — 창 진입 전과 같은가

| 축 | 실측 | 기대(D3 §0 창 진입 상태) | 판정 |
|---|---|---|---|
| platform 체인 head | **`0011_lv1_drop_level_user_set`** | 같음 | ✅ |
| ai 체인 head | **`0005_k2b_concept_graph_seed`** | 같음 | ✅ |
| `processing_level_user_set` 열 | **부재(0건)** | 부재 | ✅ |
| 행수 | **13 / 130 / 6** | 같음 | ✅ |
| digest | **10건 전건 일치 · 면제 0** ＋ **④-b 복원 전(P8)과 동일** | post-deploy 한 벌 | ✅ |
| `verify-deploy.sh` | **`배포 판정: GREEN (통과 15건 · SKIP 0 — 모든 항목이 실제로 돌았다)`** | 15 PASS | ✅ |
| 서빙 이미지 | core-api·frontend·pipeline-worker·viz-render·ai-service **전부 `:77813d391f94`** | `77813d391f94` | ✅ |
| 컨테이너 | **8개 healthy** | 8 healthy | ✅ |

**⟹ staging 은 창 진입 전 상태로 돌아왔다. 추론이 아니라 여덟 축 실측이다.**

### 4.5 다운타임 창

| 구간 | 시각 (KST) |
|---|---|
| 시작 — D3 회차의 앱 6종 정지 | **≈2026-09-03 00:57** (⚠ **분 단위 추정** — D3 이 정지 시각을 초 단위로 남기지 않았다. 근거 = D3 의 ai 거부 로그 `00:58:26` 직전 ＋ 이 회차 착수 시점의 `docker ps` 경과 표시) |
| (중간) 옛 짝 스택 8 healthy | 01:13:45 — **리허설 완주 구간** |
| (중간) 원상복구용 재정지 | 01:17:51 |
| 끝 — 원상복구 스택 8 healthy | **01:19:21** |
| **총 창 길이** | **≈22분** (그중 서비스가 실제로 서 있던 것 ≈4분 · **정지 구간 합 ≈18분**) |

---

## 5. 게이트 재측정 — **39 green / red 0**

`./gates/run.sh all -j 1` (단독 · 병렬도 1 — `CLAUDE.md §1-b ⑸`).
**축자 — `── 계 : green 39 / red(판정) 0 / red(준비) 0`.**

⚠ **첫 실행은 `green 29 / red(판정) 1 / red(준비) 9` 였다. 이것은 판정이 아니라 체크아웃 준비였다** —
새 워크트리에 `node_modules`·`.venv` 가 없고 게이트 입력 4종이 선언되지 않았다.
`generated-up-to-date` 의 `red(판정)` 도 실체는 `exit 127: openapi-typescript: No such file` 로 **준비 결손**이었다.
**조치 = 검사를 줄인 것이 아니라 입력을 선언한 것이다** — 게이트가 스스로 지시한 그대로
(`main` 체크아웃의 의존 트리 심볼릭 링크 ＋ `~/.colab-v2-test.env` 의 DB URL·원천 마운트 선언).
**건너뛰기·기본값·상한 연장은 쓰지 않았다.**

### 5.1 `artifact-ownership` 계수 (요구 항목)

**축자 — `산출물 소유 — 대상 한 벌 63 · 살아 있다 30 · 접수분에만 닿는다 14 · 고아 0 · 판정 불가 19`**

| 축 | 값 |
|---|---|
| 대상 | **63 벌** (세는 단위 = 한 캐시 키 아래 선 산출물 한 벌) |
| 살아 있다 | 30 · 접수분에만 닿는다 14 · **고아 0** · **판정 불가(구판) 19** |
| 지도 타일 | 1 벌 — **대상이 아니다**(D5 가 구운 것이라 kept · 완료 정의 ⑷) |
| 스냅숏 | 128 줄 · **삭제 0건** |
| 원장 | `d3_file` 130 · `d5_upload_file` 143 · 시점 `2026-09-03T01:34:22+09:00` |

⚠ **「판정 불가 19」는 고아가 아니라 보류다**(`sidecarVersion`·`baked_for` 없는 구판 사이드카 · `A-1` 완료 정의 ⑴).
**건수를 드러낸 채 통과했다** — 숨기지 않았다.

---

## 6. `R-1` 조건별 판정 — **닫는다**

| 조건 | 판정 | 이 회차의 근거 (**전부 이 회차 실측**) |
|---|---|---|
| ⑴ 크론 무인 연속 3회 GREEN | **충족** | `check-cron-streak.sh` **GREEN (통과 11건 · SKIP 0)** — C1 무인 연속 3회(08-31~09-02) · C1-b 최신 회차 신선 · C2 `1단` 표지 3건 · C3 볼륨 산출물 6건 실재. **D3 의 「미재측정」을 이 회차가 실제로 다시 쟀다** |
| ⑵ 복원된 스택에서 `POST /searches` | **충족** | `§3` — 200 ＋ `degraded:false` ＋ 확장 낱말 1건. **복원된 스택 위에서 2회**(옛 짝 · 원상복구) |
| ⑶ `preflight` = 「입력을 다 주면 GREEN」 | **충족** | `§4.2` — exit 0 · **실패 0건** · 입력 넷 전부 선언 |
| ⑷ `:i2` 별칭 6종 · `[미측정]` 0 | **충족** | `§2.2`·`§4.4` — `digest 대조 GREEN — 10 건 전건 일치 · 면제 0` **2회** ＋ ④-b P8 동일 |
| ⓒ `RUNBOOK §5` ⑤·③-보 자동화 | **충족** | `verify-restored.sh` 가 **손검사 2건을 기계로 돌린 채 GREEN** — 축자 「손검사 2건(③-보 읽는 경로 · ⑤ 앱 롤 양성·음성)도 기계가 돌았다」. **복원된 스택 위 실측** |
| D3 이 세운 신설 차단 | **해소** | `§1` — `restore-db.sh` 프로파일 합격선 교정 ＋ 픽스처 4건(34→38). **ai 원장이 이 회차에 양방향으로 실제 움직였다** |

### 6.1 `〈255〉` 가 남긴 세 조건(ⓐⓑⓒ)의 처리

`〈255〉` 축자 — 「실측으로 확인한 남은 조건 셋 = ⓐ ⑵ 를 **복원된 스택에서** 잰 회차가 0건
ⓑ ⑴ 을 회차마다 다시 재는 기구가 게이트 밖 ⓒ `RUNBOOK §5` ⑤·③-보가 여전히 손검사」.

| | 처리 |
|---|---|
| ⓐ | **닫힌다** — `§3`. 이 회차가 그 「0건」을 2건으로 만들었다 |
| ⓑ | **기구가 섰다** — `infra/staging/backup/check-cron-streak.sh` 가 ⑴ 을 회차마다 다시 잰다(`§6` ⑴). ⚠ **그러나 `gates/run.sh` 39종에 이 행을 재는 게이트는 여전히 0건이다** — 사람이 스크립트를 부른다. `〈255〉` 가 적은 약점(자동 감시 월요 04:10 주 1회 · 최대 1주 침묵)은 **그대로 남는다.** 아래 `§6.2` |
| ⓒ | **닫힌다** — `§2.2`·`§4.3`-8 |

### 6.2 닫으면서 남기는 것 — **이번에 세지 않은 판단기준**

⛔ **아래 셋은 `R-1` 완료 정의 밖이지만, 다음 회차가 모르고 지나가지 않게 이름으로 적는다.**

1. **⑴ 을 재는 게이트가 0건이다.** `check-cron-streak.sh` 는 있으나 `gates/run.sh` 에 편입되지 않았다 —
   **사람이 안 부르면 안 돈다.** 백업이 조용히 멈춘 창(최악 약 8일 · `RUNBOOK §0.1`)은 좁아지지 않았다.
2. **이미지 digest 대장이 배포마다 손으로 낡는다**(`〈284〉`-㉳ pending). 이 회차의 P8 GREEN 은
   `〈284〉` 가 대장을 손으로 갱신해 둔 덕이다 — **다음 배포 뒤에는 다시 RED 로 선다.**
3. **인증된 사용자 시야가 `[미확인]`** — `§3.2`. root 권한 한 줄(`§3.1`)로 풀린다.

---

## 7. 이 회차의 커밋

- `903dff3` — 복원 원장 검사기가 프로파일 합격선을 쓰게 고친다 (`§1`)
- (이 문서) — 회차 증거표 ＋ 대장 `R-1` `done` ＋ `〈286〉` 등재 ＋ `03-HANDOFF §4`

**`main` 병합·push 는 하지 않았다**(`CLAUDE.md §9`).
