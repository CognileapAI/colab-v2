# K4 · 검색 평가셋 A층 실행과 건별 판정 — 실측 회차

> 대상 항목 = **자연어 검색 서비스**(`K4`) · **검색 히어로·검색 결과 화면**(`P4`).
> 합격선 = `PLAN-SoT §9 〈217〉` — **「미판정 0건 ＋ 판정 후 전건 통과」. 퍼센트가 아니다.**
> **판정마다 사유를 기록한다** — 사유 없는 재분류는 통과로 세지 않는다(같은 항목).
> 작업 사본 = 워크트리 `lane2-search` · 브랜치 `wt/lane2-search` (병합 안 함 · push 0건).

---

## 1. 실행 — **2026-08-30T00:52:15+09:00** (KST)

```
python3 eval/s2b-alayer/run.py --json /tmp/alayer-out.json     # rc=0 (전건 실행)
```

- **A층 16건 전건 실행. 충족 7 · 미충족 8 · 판정보류 1.**
- 결정성 = 2단 포함 전량 반복 md5 전회 동일.
- **기준선(`baseline.json` 2026-08-29T17:37:03+09:00) 대비 변화 0건.**
- **미실행 2건**(`Q-19`·`Q-20`) ＋ **B층 16건** = `[미확인]`. **통과로 세지 않는다.**
  - `Q-19` 푸는 법 = 일회용 스택에서 `ai-service` 를 내려 재현(운영 스택 정지 금지).
  - `Q-20` 푸는 법 = 다른 연구실 앱 롤 자격 1회.
  - B층 푸는 법 = 자연어 해석을 켜는 회차(`〈136〉`).

### ⚠ 종전 기록 「통과 8 · 실패 8」은 이번 실측과 다르다 — **값을 지우지 않고 시점을 붙인다**

| 시점 | 충족 | 미충족 | 판정보류 |
|---|---:|---:|---:|
| 2026-08-26 (`S2b-SEARCH-EVALSET §2.10`) | 8 | 8 | — |
| **2026-08-30T00:52 (이 회차)** | **7** | **8** | **1** |

갈린 자리 넷 — `Q-07` 실패→충족 · `Q-08`·`Q-09` 충족→**미충족(회귀)** · `Q-12` 실패→판정보류(실행기가 `undecided` 로 분류).
**회귀 둘의 원인은 2026-08-27 설명 적재분이 새 토큰을 넣어 결과 집합을 넓힌 것**이고, 기준선(2026-08-29)과는 변화 0 이다 — **이 회차가 만든 회귀가 아니다.**

---

## 2. 건별 판정 — **9건 전건 판정 · 미판정 0**

판정 갈래는 셋이다 — **고친다** / **한계로 인정해 기대값을 고친다** / **범위 밖**.

| # | 질의 | 기대 | 실측 | 판정 | 사유 (실측 근거) |
|---|---|---|---|---|---|
| **Q-01** | `25년도 낙동강 유역 강우 데이터` | 0건 | 3건 `D-07`·`D-08`·`D-15` | **범위 밖** | 검색어를 `' \| '`(OR)로 잇는 결합 규칙이 원인이다(`d3_catalog._PREFIX_TSQUERY`). `낙동강`·`유역`·`낙동강 유역` 은 0건이고 `강우` 하나가 주제 `강우·강수` 에 맞아 질의 전체를 낸다. **결합 규칙 개정은 `〈89〉` 매칭 규칙 개정 사안으로 아직 미판정이다**(`S2b-SEARCH-EVALSET §2.10.3` — 「개정하지 않는다」). 임의 개정은 정본이 금한 자리 → 이 회차가 정하지 않는다 |
| **Q-04** | `Bilinear 로 만든 자료` | `D-04` | `D-04`·`D-03` | **기대값을 고친다** | `D-03`(Nearest) **설명 본문에 `Bilinear`·`이중선형보간` 이 실제로 있다**(실측 `summary ILIKE` = 참). 기법 비교를 적은 설명에서 전문 검색은 언급만으로도 맞는다 — **문서에 있는 낱말을 안 맞히게 만드는 것이 오히려 결함**이다. 기대 「1건」은 설명 적재 이전에 이름만 보고 세운 값이다 |
| **Q-05** | `D-01`·`D-03`~`D-06`·`D-09` | ＋`D-02`·`D-13`·`D-16` | **기대값을 고친다** | 초과 3건이 전부 실물 근거가 있다 — `D-02`·`D-13` 설명에 `NDVI` 존재(실측), `D-16` 은 이름에 `NDVI` 를 갖는 **12번째 데이터셋**으로 `〈111〉` 이후 대상에 들어왔는데 기대값은 11건 기준이다. 검색어 셋(`GK2A`·`위성`·`NDVI`)의 합집합이 정의대로 나온 값이다 |
| **Q-08** | `D-03`·`D-04`·`D-05` | ＋`D-02` | **기대값을 고친다** | `D-02`(DEM 100 m 충청권) **설명에 `다운스케` 토큰이 실제로 있다**(실측). 확장 검색어 `다운스케일` 이 그 토큰을 접두로 맞힌다. 기대값은 **설명 적재(2026-08-27) 이전에 세운 값**이고, 그 뒤 실물이 넓어졌다 |
| **Q-09** | `충청` 6건 | ＋`D-01` | **기대값을 고친다** | `D-01` 설명 원문에 *「코드 내부에서 **충청권**으로 잘라 사용」* 이 있다(실측). **자료 자체가 충청권과 관계있다고 적혀 있으므로 걸리는 것이 옳다.** 기대 6건은 이름에 `충청권` 이 든 것만 센 값이다 |
| **Q-11** | `강수량` 0건 | `D-08` | **기대값을 고친다** | `D-08`(RN15 지상 강수) **설명에 `강수량` 토큰이 있다**(실측). 기대 「0건」의 전제는 *「이름·주제에 `강수량` 이 없다」* 였고, **설명 색인이 켜지면서 그 전제 자체가 무효**가 됐다. 정본이 이미 같은 판정을 적었다 — *「매칭 규칙 개정으로 안 고쳐진다 · 개정 대상에서 뺀다」*(`§2.10.3`) |
| **Q-12** | `위성` — `D-01`·`D-09` 「등」 | `D-01` 1건 | **기대값을 고친다** | `D-09` 는 설명에 `위성` 이 있으나 **`국가기상위성센터` 가 `simple` 파서에서 한 덩어리 어휘소**라 접두 질의가 토큰 **중간**을 잡지 못한다(실측 = `위성:*` 이 `D-09` 를 못 맞힘). 이것을 잡을 수단인 **형태소 분석기 도입은 `〈182〉` 로 폐기**됐다 — 되찾을 손이 없는 기대다. `D-01` 만 기대로 남기는 것이 실물과 맞는다 |
| **Q-13** | `다운스케` — `D-01`·`D-03`~`D-06` | `D-02`~`D-05` | **범위 밖** | 검색 쪽 결함이 아니라 **설명 본문의 문제**다(실측 = `D-01`·`D-06` 설명에 `다운스케` 토큰 **없음** · `D-02` 설명에는 **있음**). 접두 매칭은 있는 토큰을 다 맞히고 없는 토큰을 못 맞힌 것뿐이다. 고칠 자리는 **데이터셋 설명 본문**(`S2b-DATASET-DESCRIPTIONS`)이고, 그 갱신은 **운영 DB 쓰기**라 이 회차의 경계 밖이다 |
| **Q-14** | `HSR레이더합성반사도` — `D-07` | 0건 | **범위 밖** | `pg_trgm` 보조 팔 도달 후 **임계 0.3 미달**(실측 `similarity` = **0.2308** · 공백 제거 시 0.7059). 즉 **공백·`(Lv.0)` 접미가 분모를 키우는 것**이 원인이다. 그런데 **Ted 가 이미 「임계 0.3 현행 유지 · 임계·적용 범위 변경 금지 — `〈89〉` 매칭 규칙 개정 사안」으로 확정**했다(`§4-② ⓐ`). 게다가 공백 정규화를 넣어도 `D-15` 가 0.4000 으로 함께 걸려 **기대 「1건」은 여전히 안 맞는다**(실측) |

**갈래별 셈 — 고친다 0 · 기대값을 고친다 6 · 범위 밖 3. 미판정 0.**

### 2-a. 「고친다」가 0건인 이유를 적는다

**고칠 손이 없어서가 아니라, 실측이 「검색이 틀렸다」를 한 건도 내지 않았기 때문이다.**
9건 전부 **검색이 문서에 실제로 있는 낱말을 맞히고 없는 낱말을 못 맞힌** 결과이거나(`Q-04`·`Q-05`·`Q-08`·`Q-09`·`Q-11`·`Q-13`),
**정본이 판정을 유보·금지한 규칙**(`Q-01`·`Q-14`)이거나, **폐기된 수단**이 있어야 닫히는 것(`Q-12`)이다.
검색 규칙(`_PREFIX_TSQUERY`·`_SEARCH`·`_SEARCH_TRGM`)을 **한 글자도 고치지 않았다** — 고치면 정본이 금한 개정을 레인이 대신 하는 것이 된다.

---

## 3. ⛔ 「기대값을 고친다」 6건은 **이 회차가 적용하지 않았다** — Ted 확정 자리다

정본이 기대값 개정 권한을 못 박아 뒀다.

- `〈208〉` 계열 판정 ⑵ 는 **개정 대상을 `Q-06`·`Q-09` 두 행으로 한정**했고, 그 밖의 행은 열지 않았다.
- `S2b-SEARCH-EVALSET §2.10.5` 는 `Q-05` 에 대해 **「⛔ Ted 판단 필요 · 이 문서가 정하지 않는다」** 로 적었다.
- `§3-2` = **「기대와 실제가 다르면 기대를 고치지 않는다」** — 실물에 맞춰 기대를 낮추는 것은 이 레포가 금지한 검사 대상 축소다. **예외는 Ted 가 낼 때뿐이다**(`§4` 가 그 형식이다).

따라서 **`evalset.json` 을 수정하지 않았다.** 아래 6건을 **판정 요청으로 올린다.**

| # | 원 기대 | 제안 기대 | 무엇이 원 기대를 틀리게 만드는가 (실물 한계) |
|---|---|---|---|
| `Q-04` | `D-04` 1건 | ＋`D-03` | 기법 비교 설명이 다른 기법 이름을 본문에 싣는다. 전문 검색은 언급을 구분하지 못한다 |
| `Q-05` | 6건 | ＋`D-02`·`D-13`·`D-16` | 검색어 합집합 ＋ 대상이 11건 → **12건**(`D-16`)으로 늘었는데 기대가 안 따라왔다 |
| `Q-08` | 3건 | ＋`D-02` | 설명 적재(2026-08-27) 후 `D-02` 본문에 `다운스케` 가 들어왔다 |
| `Q-09` | 6건 | ＋`D-01` | `D-01` 설명이 스스로 「충청권으로 잘라 사용」이라 적는다 |
| `Q-11` | 0건 | `D-08` 1건 | 「이름·주제에 없다」는 전제가 **설명 색인 도입으로 무효**가 됐다 |
| `Q-12` | `D-01`·`D-09` | `D-01` 만 | 복합명사 내부 접두 매칭은 형태소 분석 없이는 불가. 그 수단은 `〈182〉` 로 폐기 |

⚠ **이 표를 적용하면 A층 미충족은 8 → 2 가 된다**(`Q-01`·`Q-13`·`Q-14` 중 `Q-13` 은 설명 본문 갱신으로 닫히므로).
**그것이 「검사를 좁힌 것」이 아니라는 근거는 위 사유 칸 전부가 실측이라는 점이다** — 사유를 못 적는 행은 이 표에 넣지 않았다.

---

## 4. 합격선(`〈217〉`) 대비 — **미판정 0 달성 · 전건 통과 미달**

| 조건 | 상태 |
|---|---|
| ⑴ A층 1회 실행·기록 | **충족** (2026-08-30T00:52 · `rc=0` · 전건 실행) |
| ⑵ 미판정 0건 | **충족** (9건 전건 판정 · 사유 기록) |
| ⑶ 판정 후 전건 통과 | **미충족** — `§3` 6건이 **Ted 확정 대기**이고, `Q-01`·`Q-14` 는 규칙 개정 판정 대기, `Q-13` 은 설명 본문 갱신 대기 |

**⟹ `K4` 를 닫지 않는다.** 닫히는 조건 = ⓐ `§3` 6건 확정 ⓑ 검색어 결합 규칙(`Q-01`)·`pg_trgm` 적용 범위(`Q-14`) 판정 ⓒ `D-01`·`D-06` 설명 본문에 다운스케일 서술 반영(`Q-13`).

---

## 5. 화면 층(`P4`) 정렬 — **완료 정의는 있다. 대조는 못 쟀다**

- **완료 정의 미작성이 아니다.** `work-items.yaml` `P4.completion_def` 에 *「검색 화면이 내는 결과를 평가셋 A층 실행 결과와 대조해 건별 판정 · 미판정 0」* 이 축자로 있다.
- **정적으로 확인한 것** — 화면 문안 두 갈래가 코드에 실재한다(`frontend/src/routes/SearchResultsPage.tsx`): 0건은 **정직한 빈 상태**로 억지 제안 없음 · 검색 불가는 **「없다는 뜻이 아니에요」**＋카탈로그 링크. `Q-01`·`Q-19` 가 요구하는 두 문안이 서로 다른 말로 서 있다.
- **[미확인] = 화면 층 실결과 대조.** 로그인 뒤 검색 화면을 띄워 A층 값과 맞춰 보는 일을 **하지 않았다** — staging 로그인 자격이 필요하고, **자격 취급이 이 회차 경계 밖**이다(`〈208〉` 의 `[미확인]` ㉠ 과 같은 자리).
  **푸는 법 = 비밀번호를 작업 기록에 남기지 않는 확인 경로로 staging 세션 1회 · 그 세션에서 A층 16 질의를 화면으로 던져 값 대조.**
- **문안을 이 회차에 고치지 않았다** — 고칠 근거가 되는 `Q-01` 판정(결합 규칙)이 미판정이라, 지금 문안을 손대면 **미판정 위에 문안을 얹는 것**이 된다.

---

## 6. 이번에 세지 않은 판단기준 (다음 회차 진입조건)

ⓐ 화면 층 결과 대조(`P4`) — staging 세션 자격 ／ ⓑ `Q-19` 검색 불가 시 503 — 일회용 스택 ／ ⓒ `Q-20` 연구실 경계 — 앱 롤 자격 ／ ⓓ B층 16건 — 자연어 해석 재개(`〈136〉`) ／ ⓔ `POST /searches` 정문 경로로 잰 값(이 하네스는 내부 함수·SQL 경로다)

## 7. 경계 준수

운영 스택 **읽기 전용** — `SELECT`·카탈로그 조회만. 정지·재기동·재생성·`down` **0건** · DDL·`DELETE`/`UPDATE` **0건** · 파괴 플래그 **0건** · 접속 문자열·비밀번호 출력 **0건** · 컨테이너 내 파일 생성 **0건**(SQL 은 stdin).
`services/**`·`frontend/**`·`db/**`·`infra/**`·`gates/**`·`eval/**` **무수정** — 산출은 이 문서뿐이다. push · force-push · `main` 병합 **0건**.

---

## 7-a. 게이트 — `./gates/run.sh all -j 2` (2026-08-30T00:56~01:0x KST · 이 워크트리)

**green 24 · red 3.** 기준선으로 받은 값은 green 26 · red 1(`schema-diff`)이다. **늘어난 red 둘은 이 회차의 변경 때문이 아니라 이 작업 사본에 도구가 안 깔린 것이다** — 산출이 `dev-package/sessions/` 의 문서 한 장뿐이고 두 검사기는 그 경로를 읽지 않는다.

| red | 실측 원인 | 푸는 법 |
|---|---|---|
| `schema-diff` | `COLAB_APPLIED_DB_URL_PLATFORM`·`_AI` 미지정 — 기준선과 같은 red | 체인별 DB 를 `upgrade head` 한 뒤 URL 을 넘긴다 |
| `generated-up-to-date` | `frontend/node_modules/.bin/openapi-typescript` 부재 (exit 127) | 이 사본에서 `frontend` 의존성 설치 |
| `stage2-markers` | `services/pipeline-worker/.venv/bin/python` 부재 | 이 사본에서 `pipeline-worker` venv 생성·설치 |

**뒤의 둘은 「못 돌았음」이고, 그것을 통과로 세지 않았다.** 검사기·설정을 고치지 않았고 검사 범위를 줄이지 않았다.

---

## 8. `PLAN-SoT §9` 등재문 초안 — **번호는 오케스트레이터가 발급한다**

> | 〈번호〉 | **검색 평가셋 A층을 다시 재고 실패 전건을 사유와 함께 판정했다 — 「고친다」가 0건인 것이 실측 결과다** | **실측 ＋ 판정 (2026-08-30 · Claude · 워크트리 `lane2-search`).** 정본 = `sessions/K4-ALAYER-ADJUDICATION.md`. **㉮ 실측 2026-08-30T00:52:15+09:00** — `eval/s2b-alayer/run.py` `rc=0` · A층 16건 전건 실행 · **충족 7 · 미충족 8 · 판정보류 1** · 기준선(2026-08-29T17:37) 대비 **변화 0**. **종전 기록 「통과 8 · 실패 8」(2026-08-26)과 다르다 — 값을 지우지 않고 시점을 붙였다.** 갈린 넷 = `Q-07` 실패→충족 · `Q-08`·`Q-09` 충족→**미충족** · `Q-12` 실패→판정보류. 회귀 둘의 원인 = 2026-08-27 설명 적재분이 새 토큰을 넣은 것이고 **이 회차가 만든 것이 아니다**(기준선 대비 0). ／ **㉯ 미판정 0 — 9건 전건 판정 · 사유 기록**(`〈217〉` 요건). **고친다 0 · 기대값을 고친다 6**(`Q-04`·`Q-05`·`Q-08`·`Q-09`·`Q-11`·`Q-12`) **· 범위 밖 3**(`Q-01`·`Q-13`·`Q-14`). ／ **㉰ ⭑ 「고친다」가 0건인 것은 손이 없어서가 아니라 검색이 틀린 자리가 없어서다.** 6건은 **문서 본문에 그 낱말이 실제로 있다**(실측 — `D-03` 설명의 `Bilinear` · `D-02` 설명의 `다운스케` · `D-01` 설명의 「충청권으로 잘라 사용」 · `D-08` 설명의 `강수량`), 2건은 **정본이 개정을 금하거나 유보한 규칙**(`Q-01` 검색어 OR 결합 = `〈89〉` 미판정 · `Q-14` `pg_trgm` 임계 = `§4-② ⓐ` 로 변경 금지), 1건은 **폐기된 수단**이 있어야 닫힌다(`Q-12` 형태소 분석기 = `〈182〉` 폐기). **검색 규칙 코드 무수정.** ／ **㉱ ⛔ 기대값 개정 6건을 적용하지 않았다 — Ted 확정 자리다.** 종전 판정이 개정 대상을 `Q-06`·`Q-09` 로 한정했고 `§2.10.5` 는 `Q-05` 를 「이 문서가 정하지 않는다」로 남겼다. **실물에 맞춰 기대를 낮추는 것은 검사 대상 축소이고, 예외는 Ted 가 낸다.** 6건 표 = 위 문서 `§3`. **적용 시 미충족 8 → 2.** ／ **㉲ 합격선(`〈217〉`) 대비** — ⑴ 실행·기록 **충족** ⑵ 미판정 0 **충족** ⑶ 판정 후 전건 통과 **미충족** ⟹ **`K4` 를 닫지 않는다.** ／ **㉳ 화면 층(`P4`)** — **완료 정의 있음**(미작성 아님). 문안 두 갈래는 코드에 실재(`SearchResultsPage.tsx` — 0건 정직한 빈 상태 · 검색 불가 「없다는 뜻이 아니에요」). **화면 실결과 대조는 `[미확인]`** — staging 로그인 자격이 회차 밖. **문안을 고치지 않았다 — 근거가 될 `Q-01` 판정이 미판정이라 미판정 위에 문안을 얹지 않는다.** ／ **㉴ 이번에 세지 않은 판단기준** = ⓐ 화면 층 대조 ⓑ `Q-19` 503 ⓒ `Q-20` 연구실 경계 ⓓ B층 16건 ⓔ 정문 경로 값. ／ **㉵ 경계** — 운영 스택 읽기 전용 · 정지·재기동·DDL·쓰기 **0건** · 접속 문자열 출력 0 · 코드 무수정 · push·병합 0. *2026-08-30* |
