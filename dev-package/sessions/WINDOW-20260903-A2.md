# staging 단독 창 — 단계 A(짝 백업) 집행 ＋ go/no-go ① 증거 (2026-09-03 10:34~ KST)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md §1`·`§10-①`. 이 파일은 **이 회차에 실제로 잰 값**만 적는다.
> 집행 범위 = **A 만**. B(배포) 미착수 — go/no-go ① 은 Ted 판정 자리이고 이 회차는 증거만 세운다.
> 선행 회차 = `WINDOW-20260902-ABC2.md`(A·B·C 집행 · `77813d391f94` 배포) · `WINDOW-20260903-D4.md`(복원 완주·원상복구).
> 이번 배포 후보 = **`main` @ `8e3a0daee013`**.

## 0. 창 진입 전 상태 (실측 · 읽기 전용 · 2026-09-03 10:33~10:34 KST)

| 축 | 실측값 | 재는 방법 |
|---|---|---|
| 커밋 | `8e3a0daee0131524fd84f06f68e87bc754494e46` · 브랜치 `main` | `git log -1` |
| 워킹트리 | **변경 0건** | `git status --porcelain \| grep -c .` = 0 |
| 워크트리 | **`main` 하나뿐** (기록용 `lane-window-a2` 는 A 집행 **뒤** 생성) | `git worktree list` |
| 서빙 릴리스 | **`77813d391f94`** — 앱 5종 이미지 태그 동일(`core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`) | `docker ps` ＋ `LAST-SUCCESS.txt` 마지막 줄 `2026-09-02T23:44:32+0900 deploy OK 77813d391f94` |
| 컨테이너 | staging **8개 전부 healthy** (`nginx`·`frontend`·`core_api`·`pipeline_worker`·`viz_render`·`ai_service`·`cloudflared`·`pg`) | `docker ps` |
| 체인 head (platform) | **`0011_lv1_drop_level_user_set`** | `verify/verify-chains.sh` → `체인 판정: GREEN (통과 2건 · SKIP 0)` |
| 체인 head (ai) | **`0005_k2b_concept_graph_seed`** | 〃 |
| 크론 | 블록 안 실행 줄 **3건** (03:30 · 04:10 월 · 04:40) · 경로 전부 `main` 체크아웃 | `crontab -l` |
| 창 시각 충돌 | **없음** — 10:34 착수, 크론은 03:30·04:10·04:40 | `0.1-ⓔ` |
| 원장 green `deploy` 행 | **≥2** (`77813d391f94` · `e935db203b07` · `14d3136aa55a` …) | `release-ledger.tsv` |
| `COLAB_STAGING_ENV` | **셸 기본 환경에는 미설정(UNSET)** — 이 회차는 명령마다 홈 0600 env 파일을 지목해 export 했다. **B 착수 시에도 같은 방식으로 export 필요** | `echo "${COLAB_STAGING_ENV:-UNSET}"` |

⚠ **관측되었으나 staging 스택 밖인 것 2건** — `a2_pg`·`ai_pg`(4일 가동). `colab_v2_staging_*` 접두 밖이므로 위 8개 계수에 넣지 않았다. 이 회차는 건드리지 않았다.

## 1. B 가 실어 나를 변경분 — `git diff --stat 77813d391f94..8e3a0da`

**총계 = 126 파일 · +8,043 / −383 · 커밋 42건.**

| 영역 | 파일 수 | 내용 |
|---|---|---|
| `frontend` | 64 | 검색 15·16차(요약·기간·verified 질의 · Verified 우선 정렬 · 서버 걸름) · lab/detail/lineage/preview/catalog 화면 · 테스트 18 |
| `dev-package` | 22 | 회차 기록·노트 (코드 아님) |
| `services/core-api` | 11 | 라우트·포트·커널 ＋ 테스트 |
| `gates` | 8 | 검사기 도구·설정 |
| `infra` | 6 | `staging/restore` 5(D4 검사기 교정) · `staging/backup` 1 |
| `services/viz-render` | 5 | `values.py` 라우트 · `d7_visualization/value_lookup.py` · `storage_layout.py` ＋ 테스트 |
| `services/pipeline-worker` | 5 | d5·커널·도메인 ＋ 테스트 |
| `contracts` | 4 | `seams/fe-core.yaml` · `seams/core-viz.yaml` · `storage/layout.json` · `codegen/gen_storage_layout.py` |
| `.github` | 1 | 워크플로 |

**계약(contracts) — 변경 있음.** 15차·16차분으로 `layout.json`·`fe-core.yaml`·`core-viz.yaml` 셋이 바뀌었고,
그에 따른 생성물 `frontend/src/generated/fe-core.ts` 1건이 함께 바뀌었다. ⟹ **B 는 계약 기반 생성물이 든 이미지를 새로 굽지 않고는 정합하지 않는다.**

**마이그레이션 — 0건. 실측으로 확인했다(추정 아님).**

```
git diff --name-status 77813d391f94..8e3a0da -- '*/versions/*'   → 0 줄
ls db/platform/versions/   → 0001 … 0011 까지. **0012 없음**
ls db/ai/versions/         → 0001 … 0005 까지
```

⟹ **B 는 스키마를 건드리지 않는다. platform head 는 `0011` 그대로, ai head 는 `0005` 그대로 남는다.**
이것이 직전 회차(`0011` 이 열·CHECK 를 떨어뜨린 배포)와 이번 배포의 **결정적 차이**다.

## 2. 이미지 빌드를 깨뜨릴 자리 — 배포 전 검사기 (전부 단독 실행 · `-j 1` 상당)

1차 창의 red 는 판정 결함이 아니라 **프런트 타입 오류로 이미지 빌드가 죽은 것**이었다(`WINDOW-20260902-BC.md`). 그 자리를 먼저 잰다.

```
frontend-typecheck green — tsc --noEmit(frontend/tsconfig.json · include=src·test) 오류 0건.
frontend-test green — vitest run(frontend/vite.config.ts · jsdom) 통과 520건 · 실패 0건.
    Test Files  32 passed (32)
         Tests  520 passed (520)
contract-lint green — seam 3건, 룰 위반 0.
generated-up-to-date green — 등기부 4건 전부 재생성 일치, 등기부 밖 자칭 생성물 0건.
```

⟹ 계약이 바뀐 회차에서 **생성물이 계약과 일치**함을 `generated-up-to-date` 가 실측했다.
⚠ `gates/run.sh all` 은 이 창에서 돌리지 않았다(창 점유 규약). **이번에 세지 않은 판정기준 = 나머지 게이트 전건** — B 뒤 전수 실행이 다음 회차의 진입조건이다.

## 3. 단계 A — 배포 전 짝 백업 (집행 완료 · GREEN)

- 명령 = `infra/staging/backup/backup-full.sh` (플래그 없음) · exit 0.
- 로그에 `═══ 1단` · `═══ 2단` 표지 양쪽 실재.
- 최종 줄 — **`전범위 백업 GREEN — 원장 2 프로파일 ＋ 볼륨 2 개 · 보관처 규약 밖 파일 0`**

| 산출물 | 스탬프 | 크기(B) | 판정 |
|---|---|---|---|
| `platform-….sql.gz` | `20260903T103411` | 31,741 | GREEN (통과 7 · SKIP 0) — 표 25 (≥20) · 행 437 (≥190) |
| `ai-….sql.gz` | `20260903T103411` | 5,158 | GREEN (통과 7 · SKIP 0) — 표 6 (≥4) · 행 91 (≥45) |
| `vol-uploads-….tar.gz` | `20260903T103411` | 387,882,103 | GREEN (통과 8 · SKIP 0) — V4 154=154 · V5-a 130=130 · V5-b 전건 |
| `vol-previews-….tar.gz` | `20260903T103448` | 14,738,180 | GREEN (통과 6 · **승인된 SKIP 1** = V5 오라클 명시 면제 `none`) — V4 156=156 |

**짝(회차 한 벌) — `.pair` 파일이 짝을 정한다.**

```
vol-uploads-20260903T103411.pair   -> platform-20260903T103411.sql.gz
vol-previews-20260903T103448.pair  -> platform-20260903T103411.sql.gz
```

두 원장 덤프의 스탬프가 **동일**(`20260903T103411`) — P5(같은 회차) 조건이 시각차 0초로 선다.

**최신본 재검사** — `infra/staging/backup/latest-check.sh` → **`최신본 재검사 GREEN — 프로파일 2 개 · 볼륨 2 개`**.

**덤프가 담은 스키마 상태 (읽기 전용 대조)**

```
platform-20260903T103411.sql.gz : alembic_version_platform = 0011_lv1_drop_level_user_set  (단일 행) · '0012' 문자열 0건
ai-20260903T103411.sql.gz       : alembic_version_ai       = 0005_k2b_concept_graph_seed
```

⟹ 이 덤프는 **배포 후에도 그대로일 스키마**(`0011`/`0005`)를 담는다. 직전 회차와 달리 이번 백업은
「스키마를 되돌릴 유일한 재료」가 아니라 **데이터 되돌림 재료**다 — 아래 §5 롤백 한계와 함께 읽는다.

경로 = `$COLAB_BACKUP_DIR`(홈 백업 보관처). **값·접속 문자열은 이 문서에 적지 않는다.**

## 4. B 가 무엇을 하는가 (착수 시)

- 명령 = `infra/staging/deploy.sh --target staging` (플래그 없음) · `main` 체크아웃에서.
- **마이그레이션 = 없음.** `deploy.sh ⑧` 이 돌아도 head 는 `0011`/`0005` 에서 움직이지 않는다(§1 실측 근거).
- **이미지 6종 재빌드** — `core-api`·`pipeline-worker`·`viz-render`·`ai-service`·`frontend`·`migrator`.
  별칭 `:i2` 6/6 재부착 ＋ digest 이력 6종 append.
- **계약 기반 생성물이 이미지 안으로 들어간다** — `layout.json`·`fe-core.yaml`·`core-viz.yaml` 변경분과
  `frontend/src/generated/fe-core.ts` 가 빌드로 이미지에 굳는다. 이것이 이번 배포의 실질 내용이다.
- `deploy.sh ⑤` 의 원장 덤프 2프로파일은 **끄지 않는다**. §3 은 그 위에 얹은 짝 회차다.

**B 의 증거는 `RUNBOOK §2` 의 여섯 항 그대로 적는다** — 200 은 증거가 아니다.

## 5. 되돌림의 한계 — 이번 회차는 **완전 복구가 가능하다**

| 축 | 직전 회차(`0011` 배포) | 이번 회차(`8e3a0da`) |
|---|---|---|
| 스키마 변경 | 열·CHECK **삭제** (forward-only) | **없음** (마이그레이션 0건 · 실측) |
| `rollback.sh --to-last-green` 의 복원 범위 | 이미지만. **스키마는 안 돌아온다** | **이미지 = 배포 내용 전부** |
| 파괴적 복원 필요 여부 | 필요(덤프 ＋ `--yes-drop-schema`) | **불필요** |

⟹ **이번 배포의 되돌림은 `infra/staging/rollback.sh --to-last-green` 하나로 완결된다.**
스키마가 안 바뀌므로 「옛 이미지가 없어진 열을 읽어 red」인 forward-only 위험이 이 회차에는 없다.
롤백 대상 = `77813d391f94`(직전 green · 이미지 실물 보존 대상). ⚠ **원장 한 줄이 곧 롤백 가능을 뜻하지 않는다**
(`rollback.sh:68-69`) — F 착수 시 그 태그의 이미지 6종 실재를 따로 실측한다.

## 6. go/no-go ① — 증거 대조표

| # | 조건 (`RUNBOOK §10-①`) | 실측 | 판정 |
|---|---|---|---|
| ㉠ | A 두 프로파일 GREEN | §3 · platform·ai 각 통과 7 · SKIP 0 | ✅ |
| ㉡ | 볼륨 산출물 실재 ＋ 짝 성립 | `vol-uploads` 387,882,103B · `vol-previews` 14,738,180B · `.pair` 둘 다 `platform-20260903T103411.sql.gz` | ✅ |
| ㉢ | 워킹트리 변경 0건 | `git status --porcelain` 0줄 (`deploy.sh:62`) | ✅ |
| ㉣ | 워크트리 = `main` 하나 | A 집행 시점 1줄 | ✅ |
| ㉤ | 스택 가동 중 | 8개 healthy | ✅ |
| ㉥ | 크론 시각 비충돌 | 10:34 착수 | ✅ |
| ㉦ | 원장 green `deploy` ≥2 | `77813d391f94`·`e935db203b07`·`14d3136aa55a` | ✅ |
| ㉧ | 배포 전 빌드 파괴 요인 없음 | `frontend-typecheck`·`frontend-test`·`contract-lint`·`generated-up-to-date` 4건 green | ✅ |
| ㉨ | **승인 행이 배포 행보다 먼저** | ❌ **미기재** — Ted 의 GO 로만 채워진다 |
| ㉩ | `COLAB_STAGING_ENV` | 셸 기본 미설정. B 실행 셸에서 홈 0600 env 파일로 export 필요 | ⚠ 조치 필요 |

## 7. 판정 — **GO 권고**

**결정적 사실 = 마이그레이션 0건이 실물로 확인됐다.**
`git diff … -- '*/versions/*'` 0줄 ＋ `db/platform/versions/` 에 `0012` 부재 ⟹ B 는 스키마를 건드리지 않고,
따라서 이 창에서 유일하게 되돌릴 수 없던 축(forward-only 스키마)이 **이번 회차에는 존재하지 않는다.**
남는 위험은 이미지 빌드 실패 하나였고, 1차 창을 red 로 만든 그 자리를 배포 전에 4개 게이트로 green 실측했다.
되돌림은 `rollback.sh --to-last-green` 하나로 완결된다.

**no-go 로 뒤집는 조건** — ㉨(승인 행) 미기재 상태로 `deploy.sh` 를 부르는 것. 승인 행이 먼저다.

## 8. Ted 의 GO 시 원장에 덧붙일 승인 행 — **축자 (이 회차는 적지 않았다)**

`~/colab-v2-releases/release-ledger.tsv` 에 탭 구분 5열로 **한 줄** append (시각은 append 시점으로 갱신):

```
2026-09-03T<HH:MM:SS>+0900	approve	8e3a0daee013	-	-	승인자=Ted 본것=go/no-go ① GO · 검색 15·16차 + 값 조회 · 마이그레이션 0건(head 0011 유지 실측) · 짝 백업 20260903T103411 GREEN · 사전게이트 typecheck·test·contract-lint·generated 4건 green · WINDOW-20260903-A2
```

⚠ 이 행은 **Ted 의 GO 가 나온 뒤** 오케스트레이터가 적는다. 이 회차가 적지 않았다.

## 9. 이 회차가 건드리지 않은 것

- `main` 워킹트리 = **변경 0건**(A 집행 후 재확인). 산출물은 전부 레포 밖(`~/colab-v2-backups/staging/`).
- staging 컨테이너 조작 0건 · 운영 DB 쓰기 0건 · 파괴 플래그 0건 · 자격증명 값 출력 0건(경로만).
- 단계 B~H 미착수. 단계 E(`S3`)는 베어러 자격증명 파일 **경로** 미입수로 여전히 차단(`RUNBOOK §11-⛔1`).
- 이 기록 파일은 워크트리 `lane-window-a2`(브랜치 동명)에서 커밋한다. push 없음.
