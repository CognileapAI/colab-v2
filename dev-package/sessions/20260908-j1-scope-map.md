# J-1 편의 묶음 범위와 구현 대응 — 2026-09-08

기준: 격리 `.codex/worktrees/product-finish`. 코드/대장 수정0, 시험 실행0. 이름 기반 좁은 조사이며 미발견을 전수 구현 부재로 단정하지 않았다.

## 범위 판정

- 현재 실행 상태 정본인 대장 `J-1.completion_def`는 2026-09-03 〈289〉-㉱ 확정으로 draft의9건을 승격했다. 따라서 현재 대장이 요구하는 것은9건이다.
- 참조 `S1-PLAN-REFOUND.md:1018`은 〈339〉의 파일 관리 결정에 따라 J-10 폴더 드롭을 F-3로 승격하고9→8이라고 적는다. 같은 문서 앞1001은 아직9건이다.
- 둘은 단순 합산 오류로 조용히 고칠 수 없다. 〈289〉 확정은 날짜9/3, 〈339〉 승격 기록은8/29라 번호 순서만으로 우선순위를 정하면 안 된다. 대장에는9/3에도 J-10 개별 완료 정의가 존재한다.
- [판단] 제품 구현 책임을 중복시키지 않으려면 J-10 코드는 F-3 기존 구현을 재사용하고, J 묶음에서 남길 것은 수용 증거 참조인지 범위 제외인지를 확정해야 한다. 현재범위를8로 임의 확정하지 않는다.
- 특히 대장 draft 말미의 묶음 전체 조건은9건이 함께 서며 하나씩 꺼내지 않는다는 것이다. 참조§J 규칙③도 일치한다. 따라서 아래 최소 후보는 순차 구현 착수 단위일 수 있으나 독립 출시/완료로 따로 닫을 승인 근거는 없다.

## 항목 대응표

| 항목 | 기존 완료 요구 요약 | 현재 구현·시험 근거 | 현재 분류 |
|---|---|---|---|
| J-1 다른 자료 격자 가져오기 | 같은 연구실 후보·형상 일치·타 연구실404 | `GridUploadBlock.tsx:6`은 매번 직접 올리는 경로. `gridFlow.ts`, `grid-attach.test.tsx`는 직접 업로드/기존 상세 격자 교체 | 재사용 후보/복제 UI 미발견, 직접 업로드와 구분 |
| J-2 연구실 기본 격자 | 교수만 지정·제시만/자동 적용 금지·스키마 up/down | 같은 GridUploadBlock 경로에 추천/기본값 없음. 대장의0012 예약은 최신 체인과 다시 대조해야 하며 예약 번호 그대로 사용 금지 | 구현 미발견, 계약/스키마 범위 큼 |
| J-3 격자 불일치 경고 | 형식·해시 다름 경고·판정 비차단·쌍별 거리/미상 | `grid-rejection.test.ts`, `gridFlow.ts`는 거부/업로드 상태. 비교 후 경고에 대한 J-3 전용 근거 미발견 | 미판정/구현 후보. 기존 rejection을 완료로 세지 않음 |
| J-4 적용 전 예상 영역 | 적용 전 경계4값·적용후와 일치·모르면미상 | `grid-preview.test.tsx`는 렌더 결과 bounds·지도형/비지도형, 적용 전 예상 bbox와 대조 아님 | 기존 결과표시 존재, 선행 예상 영역은 미검증 |
| J-5 지도없음 배지/필터 | 상세와 같은 근거·연구실 경계·아직모름과 구분 | `grid-preview.test.tsx` 상세/미리보기 상태 근거. 카탈로그 전용 해당배지/필터 대응 시험 미발견 | 카탈로그 구현/수용 미확인 |
| J-6 팔레트 선택 | listPalettes 목록만·선택→RenderStyle.palette·3×7=21상한 | `DatasetPreviewSection.tsx:141-156` API목록 첫값을 선택. `PreviewControls.tsx`는 팔레트 선택 대신 아직 못 불러온다는 문구, 구간수3~9만 select. `upload.test.tsx:657,673` 출처·실패 시험, dataset-preview 시험은 선택을 범위밖으로 명시 | API/재렌더 기반 있음, 사용자 선택 UI 미구현 |
| J-8 남은시간 예측 | 표본 없으면숨김·단계별중앙값·표본 선행 | `GridUploadBlock.tsx`는 sentBytes/totalBytes 진행. 관련 코드를 좁은 median/remaining/예측 검색했으나 단계별 예측 근거 미발견 | 표본/예측 구현 미확인, 상수 추정 금지 |
| J-9 첫장 조기 렌더 | 전량전 첫장 도달·수렴·변경알림1회 | `pipeline.py` 첫2D 배열 선택 함수는 데이터 파싱이며 조기 전송/렌더 오라클 아님 | 조기완료·수렴 시험 미발견, 범위 큼 |
| J-10 폴더 드롭 | 2파일과 같은결과·서버 축판별·3개후보 거부 | `dropTree.ts`, `upload-droptree.test.tsx`에 실제 폴더 수집 및 상대경로 시험. F-3에 폴더 보존 구현 귀속 | 기존 구현 있음; 격자 특화0~2/축미질문까지 동일 증거로 덮는지 미검증 |

## 가장 작은 다음 단위

- 미구현은 J-6 팔레트 선택 UI가 가장 경계가 작다. API목록/RenderStyle 전달 기반이 이미 있고 DB/마이그레이션을 추가할 이유가 없다. 다만 묶음 전체 승인/현재 화면 노출 규칙을 따르는 실행 계획 안에서 착수해야 한다.
- 먼저 검증만 할 최소 단위는 J-10의 기존 폴더 드롭이 격자2개/3개 거부 및 서버 축판별을 보존하는지 대응 시험을 대조하는 것이다. 이 작업은 범위8/9를 임의로 결정하지 않고 F-3 기존 코드의 증거를 재사용할 수 있다.
- J-6만 완성했다고 J-1 done으로 변경하거나 개별 출시하면 묶음 규칙에 어긋난다. 사용자에게 물을 것은 코드찾기가 아니라 필요시8/9 책임 범위 및 단독 완료 허용 여부다. 그 전에는 전체 묶음 계획 내 순차 구현/검증으로 표현한다.

## 한계

이름이 같은 PRD43의 J-08/J-12 toast는 이 편의묶음 J-8/J-10이 아니다. 혼동해 완료로 세지 않았다. 실제서비스/E2E/전체시험은 재실행하지 않았고 메인 기존 성과도 이9개 완료로 재사용하지 않았다.
