# 단독 창 3 — 배포 후속 (재굽기 → 소유 재실측 → 현물 확인)

> 워크트리 `.claude/worktrees/lane-window-b3` · 브랜치 `lane-window-b3` · 기준 `main` @ `64791f9`
> 등재 = `PLAN-SoT.md §9 〈308〉` · Ted GO = 「배포 → 재굽기 → 재실측 연속」
> ⚠ **이 창은 배포를 집행하지 않았다** — 배포는 앞선 레인이 이미 끝냈고(커밋 0건 남김 · 이후 게이트 대기 루프에서 정지), 이 창은 **재검증부터** 이었다.

## 1. 배포 — 원장 2행 (축자)

```
2026-09-03T23:26:19+0900	approve	64791f9	-	-	승인자=Ted 본것=go/no-go ① GO · 짝 백업 platform-20260903T211644/vol-uploads-20260903T211645/vol-previews-20260903T211725 GREEN · latest-check GREEN · 마이그레이션 델타 0줄(0012 부재) · 개명 colab_api→colab_core 잔여 grep 0건 · 배포전 게이트 9건 green(contract-lint·generated-up-to-date·contract-breaking·migration-single-head·frontend-typecheck·frontend-test·service-tests 4단위) · 롤백 대상 d9e89a3a074b 이미지 6/6 실재 · 사후 재굽기(POST /api/v1/previews · staging WRITE) 승인 포함 · sha=64791f9(=56eac76+A3문서) · WINDOW-20260903-A3
2026-09-03T23:27:43+0900	deploy	64791f910f49	64791f910f49	green	별칭재부착GREEN digest이력=6종 배포전백업GREEN 워킹트리변경=0
```

**이 창이 다시 돌린 배포 판정** (`infra/staging/verify/verify-deploy.sh` · 2026-09-04 01:0x) —

```
배포 판정: GREEN (통과 15건 · SKIP 0 — 모든 항목이 실제로 돌았다)
```

- PASS 15건 = `/healthz` 6종 본문 대조 · 컨테이너 8종 healthy · 호스트 노출 `0.0.0.0` 0건.
- **서빙 태그 실측** (`docker ps` · 읽기 전용) = 앱 5종 전부 `64791f910f49` — `core-api`·`frontend`·`pipeline-worker`·`viz-render`·`ai-service`. 나머지 3종은 외부 이미지(`nginx:1.27-alpine`·`cloudflared:latest`·`postgres:16-alpine`).

**다운 시간** — `approve 23:26:19` → `deploy green 23:27:43` = **1분 24초**.
⚠ **이 값은 착수~판정 구간이고 5xx 지속 시간이 아니다** — 5xx 를 재는 계측이 이 경로에 없다(`[미확인]`).

## 2. 게이트 — 재굽기 **전**

`set -a; . ~/.colab-v2-test.env; set +a; ./gates/run.sh all -j 1` (단독 8 · 병렬 42 · 미선언 0 = **선언 50**)

```
  ── 계 : green 49 / red(판정) 1 / red(준비) 0
```

- **red(준비) 0** — 환경이 없어 못 돈 게이트가 0건이다.
- **red(판정) 1 = `artifact-ownership`** 하나이고, 그것이 이 창이 재려던 바로 그 대상이다(고아 14 벌 미선언). **통과시키지 않았다.**
- ⚠ **실행 자리는 레포 루트 체크아웃이다** — 워크트리 `lane-window-b3` 에는 `services/*/.venv`·`frontend/node_modules` 가 없어 같은 명령이 **red(준비) 13건**을 냈다(축자 「의존 트리가 이 체크아웃에 없다」). 루트 체크아웃은 같은 커밋 `64791f9` · 워킹트리 변경 0 이고, **쓰기는 전부 레인 브랜치에만 했다.**
- ⚠ **같은 호스트에서 다른 세션이 `./gates/run.sh all -j 2` 를 동시에 돌리고 있었다** — 이 창은 `-j 1` 로 돌렸다.

### 2.1 `artifact-ownership` 계수 — **재굽기 전** (축자)

```
산출물 소유 — 대상 한 벌 108 · 살아 있다 75 · 접수분에만 닿는다 0 · 고아 14 · 판정 불가 19
  지도 타일 2 벌은 **대상이 아니다** — D5 가 구운 것이라 kept 로 산다 (완료 정의 ⑷)
  세는 단위 = 한 캐시 키 아래 선 산출물 한 벌 · 자리 = /home/ttlhi10/.colab-v2-staging/previews · 원장 = d3_file 133 · d5_upload_file 133
  시점 = 2026-09-04T01:01:06+09:00
```

⚠ **`〈305〉` 의 관측(대상 90 · 살아 있다 57 · 접수분 14 · 고아 0 · 판정 불가 19)과 갈린다.** 어느 쪽이 틀린 것이 아니라 **시점이 다르다** — 그 사이 회차들이 자리에 더 구웠다. 값을 지우지 않고 시점을 붙인다.

## 3. 재굽기 — 공개 엣지 `POST /api/v1/previews` 3건

- 경로 = **공개 엣지 하나뿐**(`〈300〉`·A3 §6 ⑵ 가 고정한 인가 경로). 베어러는 **경로로만** 썼고(`~/.colab-v2-staging-subjects.tokens.json`) `User-Agent` 를 매 요청에 붙였다. SQL 쓰기 0 · 컨테이너 조작 0 · 파괴 플래그 0.
- **팔레트 = `단색-파랑`**(`GET /api/v1/preview-palettes` 의 첫 값).
  ⚠ **「현재 팔레트」를 데이터셋 상세에서 읽을 수 없다** — `GET /api/v1/datasets/{id}` 최상위 키 17개에 팔레트가 **0건**이고 자리의 사이드카에도 없다. 그래서 **직전에 무슨 팔레트로 구웠는지는 `[미확인]`** 이다.

| 데이터셋 | renderId | 상태 | 3층 (status · content-type · bytes) |
|---|---|---|---|
| `01M0YWDXN7CA13KB7MQC9PGEW5` | `01M1M09Z6Y4F08210QZEKW978K` | 202 → **완료** | ①썸네일 `200 image/webp 3,934` · ②비지도형 `200 image/png 40,142` · ③지도형 `200 image/png 39,426` |
| `01M1JM3JTQSSTKR9A2QQ67QN81` | `01M1M09ZH744J9ASB1A999E6PN` | 202 → **완료** | ①썸네일 `200 image/webp 6,878` · ②비지도형 `200 image/png 999,069` · ③지도형 `200 image/png 1,415,107` |
| `01M19RENXAANZ2HDWM4Z46C37Y` | `01M1M09ZT1XKFNE6QXKWN2CA2T` | 202 → **완료** | ①썸네일 `200 image/webp 3,934` · ②비지도형 `200 image/png 40,142` · ③지도형 `200 image/png 39,426` |

- 9층 전건 `200` · `text/html` 0건 · 실패 0건. ①이 `image/webp` 인 것은 정본이다(`〈74〉`-㉮ · `〈300〉` 과 같은 선).
- `01M1JM3JTQSSTKR9A2QQ67QN81` 의 `precisionBadge` = **`동봉 격자 적용`**, 나머지 둘 = `투영 계산 격자`. 색범위 단계는 3건 모두 `확정`.

### 3.1 캐시 키 — 한 데이터셋의 전후 (`01M0YWDXN7CA13KB7MQC9PGEW5`)

| 층 | 전 (직전 벌 · `2026-09-03T12:40:05Z`) | 후 (`2026-09-03T16:05:54Z`) |
|---|---|---|
| ①썸네일 | `48a4a3f6867296286410d2bece3c790a7d95eb83547520d2daf830fa76bf97b7` | `ad863d60de2ff8e77e7cf87cb0fb63f69b18cfe49e45000eabde88325b680c34` |
| ②비지도형 | `1b3375781cf140e23e6b3c4ee0258fe546f398dd4a27b3faf105d5cd83b79541` | `f48dc45c5c11a81c01396ee629f3bf96772b5c5042703bbb4a83018aa2dc8647` |
| ③지도형 | `82327483c4240de40d8f3e9e3408ac19a6b7896af88964a8576f3f140b593262` | `4e0ac3e3819227c04f718d2a6fb31a6107f6c5deb24047c7275db4be7f4e50f8` |

- **세 층 모두 키가 갈렸다.** 새 사이드카는 `sidecarVersion: 2` · `baked_for.target_id = 01M0YWDXN7CA13KB7MQC9PGEW5` · `is_upload: false` · `sources` 1건이다.
- ⛔ **「키가 갈린 것이 리팩터 때문」이라고 판정하지 않는다 — `[미확인]`.** 팔레트가 이미 `render_cache_key` 의 입력이고(`〈250〉` 실측) **직전 팔레트를 읽을 경로가 없어**(위 §3) 팔레트 차이와 리팩터 차이가 이 실측에서 **갈리지 않는다.** 가르는 법 = 같은 팔레트로 두 번 굽는 대조군 또는 키 생성부의 입력 목록 대조.
- ⭑ **곁가지 실측 하나** — `01M0YWDXN7CA13KB7MQC9PGEW5` 와 `01M19RENXAANZ2HDWM4Z46C37Y` 는 **범례·경계·3층 바이트 수가 전부 같은데 키는 다르다**(`4e0ac3e3…` ↔ `f7b007c5…`). 즉 키는 픽셀 내용만으로 서지 않는다.

## 4. 소유 재실측 — 재굽기 **후**

`./gates/run.sh artifact-ownership -j 1` (축자)

```
산출물 소유 — 대상 한 벌 117 · 살아 있다 84 · 접수분에만 닿는다 0 · 고아 14 · 판정 불가 19
  시점 = 2026-09-04T01:06:35+09:00
```

**게이트가 델타에 대해 말하는 것** —

| 등급 | 전 | 후 | 차 |
|---|---|---|---|
| 대상 한 벌 | 108 | **117** | **+9** |
| 살아 있다 | 75 | **84** | **+9** |
| 접수분에만 닿는다 | 0 | 0 | 0 |
| 고아 | 14 | **14** | **0** |
| 판정 불가 | 19 | **19** | **0** |

- **+9 = 3 데이터셋 × 3층** 이고 전건 `살아 있다` 로 섰다 — 재굽기가 **실제로 일어났고** 새 벌이 원장 대조를 통과한다.
- **판정 불가 19 는 움직이지 않았다** — 구판 사이드카는 재굽기로 사라지지 않는다. **새로 구운 것은 더해지고 옛 것은 그 자리에 남는다.** 그것을 치우는 것은 이 게이트가 아니라 `invalidation.reclaim_plan()` → `apply()` **한 자리**다(완료 정의 ⑶).
- **고아 14 도 그대로**이고 게이트는 같은 사유로 여전히 **red(판정)** 이다 — 축자 「**고아 등급 14 벌 중 선언되지 않은 것**」. 조각 `fileId` 가 `d3_file` 에도 `d5_upload_file` 에도 없다.
- **아무것도 지우지 않았다.** 게이트도 지우지 않는다(축자 「⚠ 이 게이트는 아무것도 지우지 않았다」).

## 5. 현물 확인 (읽기 전용 · 베어러 경로 · UA 부착)

| 점검 | 결과 |
|---|---|
| `GET /api/v1/me` | **200** · `labId 00000000000000000000HYMETS` · `role 교수` |
| `GET /api/v1/datasets/01M0YWDXN7CA13KB7MQC9PGEW5` | **200** · 최상위 키에 **`projects` 있음** · **`actions.canDownload` = true** |
| `POST …/01M0YWDXN7CA13KB7MQC9PGEW5/value-lookup` (36.75, 127.40) | **200** · `available true` · **`value 0.4836253468321484`** |
| `POST …/01M1JM3JTQSSTKR9A2QQ67QN81/value-lookup` (동일 점) | **200** · `available true` · **`value 0.31521356105804443`** |
| `POST …/01M19RENXAANZ2HDWM4Z46C37Y/value-lookup` (동일 점) | **200** · `available true` · **`value 0.4836253468321484`** |
| `POST /api/v1/dataset-searches` `{"query":"강수","verified":true}` | **200** · **0행** · `degraded false` — 대조군 `verified` 없이는 **3행**이라 걸름이 실제로 걸린다. staging 전건 `verified:false` 실측과 일치 |
| `GET /api/v1/lab` | **200** · `고려대학교 수문학연구실` |
| `GET /api/v1/datasets/01M0YWDXN7CA13KB7MQC9PGEW6` (없는 것) | **404** · `{"code":"NOT_FOUND","message":"없거나 연구실 경계 밖이다."}` — **중립 문면**(있음/없음을 가르지 않는다) |

- **값 조회 3건이 테넌트 경계 헤더 경로를 끝에서 끝까지 세운다** — `X-CoLAB-Lab` 은 core-api 중계가 채우고(`app/deps.py:74` · 없으면 400 `TENANT_SCOPE_MISSING`) 사람이 붙이지 않는다. 3건 전부 200 · `available true` 이므로 **엣지 → core-api → viz-render → 자리** 가 한 줄로 이어진다.
- ⚠ **`verified:true` 만 보낸 요청은 400 이다** — `{"code":"BAD_REQUEST","message":"검색 질문은 1~200자다."}`. `query` 가 필수다.
- ⚠ **자리에 없는 ID 는 형식부터 갈린다** — 25자를 보내면 400(`datasetId 가 정규 ID 가 아니다`)이고 404 가 아니다. 위 표는 **26자 정규 ULID** 로 잰 값이다.
- ⛔ **410 자기 연구실 묘비는 이 창이 재지 않았다 — 도달 불가.** 삭제된 데이터셋이 staging 에 0건이라 조건을 만들 수 없고, 만들려면 삭제가 필요하다(이 창의 경계 밖).

## 6. 끝 상태 8축

| # | 축 | 값 |
|---|---|---|
| ① | 서빙 회차 | `64791f910f49` — 앱 5종 이미지 태그 실측 일치 |
| ② | 배포 판정 | **GREEN (통과 15건 · SKIP 0)** — 이 창이 재실행 |
| ③ | 컨테이너 | **8 healthy** · 호스트 노출 `0.0.0.0` **0건** |
| ④ | 게이트 (재굽기 전) | 선언 50 · **green 49 / red(판정) 1 / red(준비) 0** |
| ⑤ | 산출물 소유 | 대상 **117** · 살아 있다 **84** · 접수분 **0** · 고아 **14** · 판정 불가 **19** (2026-09-04T01:06:35+09:00) |
| ⑥ | 재굽기 | 3 데이터셋 · renderId 3 · 9층 전건 200 · 실패 0 |
| ⑦ | 현물 | `/me`·상세·값 조회 3건·검색·`/lab` 전건 200 · 없는 것 404 중립 |
| ⑧ | 삭제 | **0건** — 자리에서 한 파일도 지우지 않았다 |

## 7. `RC-1` 이 다음에 필요한 것

1. **하드 진입조건은 충족됐다** — `〈307〉` 이 「재굽기 전 판정 금지」를 걸었고, **재굽기가 집행됐고 전후 계수가 둘 다 축자로 남았다**(§2.1 · §4).
2. **남은 집행 = 회수 한 자리** — `invalidation.reclaim_plan()` → `invalidation.apply()`. 재굽기는 **더하기만** 했고 판정 불가 19 · 고아 14 는 그대로다.
3. **회수 전에 갈라야 할 것 둘** — ㈎ **고아 14** 는 원본이 사라진 것이라 지우는 대상이고, ㈏ **판정 불가 19** 는 「구판 · 판정 보류」라 **없는 필드를 근거로 지우면 그것이 오삭제다**(게이트 덫 ②). 보류는 `gates/config/artifact-ownership.toml` `[legacy] tolerate` 에 **이름으로** 선언하고 건수를 드러낸다.
4. **비가역이라 go/no-go 게이트가 선다** — 삭제 대상은 조건문이 아니라 **키 목록으로 고정**한 뒤 집행한다.
5. **이 창이 재지 않은 판단기준** — 캐시 키 변화의 원인 귀속(§3.1 `[미확인]`) · 410 묘비 경로(도달 불가) · 5xx 지속 시간.

## 8. 이 창이 건드리지 않은 것

- `services/**`·`frontend/**`·계약·생성물 **0줄** · 마이그레이션 0 · 배포 0회 · 롤백 0회.
- 운영 DB 쓰기 0 · 컨테이너 정지·재기동·재생성 0 · 파괴 플래그 0.
- 자리(`~/.colab-v2-staging/previews`) 삭제 0 — **더해진 9벌 21파일**만 늘었다(234 → 255 파일).
- 접속 문자열·토큰 값을 어디에도 남기지 않았다.
