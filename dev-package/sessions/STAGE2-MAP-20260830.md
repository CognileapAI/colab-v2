# STAGE2-MAP — Stage 2 전수 실측 지도 (2026-08-30)

> 측정 시점 = 2026-08-30 (+09:00) · `main` = `cfc98e3` · 읽기 전용 회차
> **이 회차는 파일 편집 0 · 커밋 0 · 운영 스택 무접촉**(원장·크론·백업 로그 **읽기**만).
> 값의 정본은 `PLAN-SoT §9` 다 — 이 문서는 대장(`work-items.yaml`)과 실물 증거를 대조한 **지도**이지 정본이 아니다.
> ⚠ **번호 미발급** — `§9` 등재는 오케스트레이터 자리다.

---

## 0. 세는 기준 (Counting basis)

문서마다 34 · 35 로 갈렸던 것은 **기준이 달랐기 때문**이지 어느 하나가 오기인 것이 아니다.

| 기준 | 값 | 시점 · 근거 |
|---|---|---|
| `work-items.yaml` 의 `stage: stage2` 행 수 | **36** | 2026-08-30 · `main`=`cfc98e3` · 파싱 실측 |
| 〈212〉 배정 **전** 의 `stage2` 행 수 | **34** | `STAGE2-ROADMAP` 머리말(2026-08-29 · `main`=`d05171b`) |
| 위 34 ＋ `〈212〉` 로 편입된 둘(`IS4`·`P4`) | **36** | 〈212〉-㉯㉰ |
| 「실질 항목」 = 36 − 중복 계상 1(`S3-e2e-4종`) − 하지 않기로 한 것 1(`2단-격자전용-실패3건-처분`) | **34** | 아래 §1 주석 |

**본 문서의 기본 계수는 대장 행 수 36 이다.** 다른 값을 쓰는 자리는 그때마다 기준을 붙였다.

### 0.1 계수 요약

| 구분 | 건수 | 비고 |
|---|---|---|
| 총계 (`stage: stage2` 행) | **36** | |
| `done` | **10** | `X-1`·`X-2`·`X-3`·`X-4`·`X-5`·`#22`·`2단-A`·`2단-E`·`2단-계보간선-원자료→월평균`·`2단-격자전용-워커제외` |
| `deferred` (하지 않기로 한 것) | **1** | `2단-격자전용-실패3건-처분` |
| 잔여 (`open`19 ＋ `partial`4 ＋ `in_progress`2) | **25** | |
| 완료 정의 미작성 (잔여분) | **3** | `V-1`·`V-2`·`J-1`(J-1 은 범위만 있고 닫는 기준 없음) |

### 0.2 잔여 25건의 차단 분류 계수

| 분류 | 건수 | 항목 |
|---|---|---|
| (a) Ted 상품 판정 대기 | **5** | `F-1`·`T-1`·`V-1`·`V-2`·`P4` |
| (b) 자격·운영 연락처 대기 | **0** | — (해당 없음. 운영 접촉은 전부 자기 호스트에서 가능) |
| (c) 다른 항목 선행 | **13** | `I4`·`P3`·`P6`·`P7`·`P8`·`V-1`*·`V-2`*·`Y-1`·`K5`·`S3`·`S3-e2e-4종`·`F-2`·`PA-G`·`J-1` |
| (d) 막는 것 없음 — 지금 지을 수 있다 | **6** | `PV-1`·`K3`·`X-6`·`R-1`·`전범위백업`·`W-1`·`I3` |
| (e) `IS4` | **1** | (c) 와 (d) 사이 — 아래 §2 참조 |

⚠ **합이 25 를 넘는다 — 한 항목이 두 분류에 동시에 걸리기 때문이다.**
`V-1`·`V-2` 는 **(a)＋(c)** 이고, `I3`·`R-1`·`전범위백업` 은 (d) 지만 **잔여 행이 운영 회차 1회를 요구**한다.
분류를 하나로 눌러 담지 않았다 — 눌러 담으면 「무엇을 하면 풀리는가」가 사라진다.
**중복 없는 1차 분류**(항목마다 가장 앞선 차단 하나) = (a) 3 · (c) 15 · (d) 7.

---

## 1. 전 항목 표 (36건)

기능명이 1차 라벨이고 내부 코드는 상호참조용 별도 칸이다(스킬 §8).

| # | 기능명 | 코드 | 대장 상태 | **실측 상태** | 완료 정의 | 막는 것 |
|---|---|---|---|---|---|---|
| 1 | 미리보기 뒷단 — 헤더 판독·좌표계 통일·지도용 영상 변환 | `PV-1` | open | open (사건 소비자·유실 감지·타일 키 규칙은 `〈223〉` 로 **섬**) | 있음 | (d) 없음 — 배포 green 이 방금 섰다 |
| 2 | 계보 그래프 · 격자 렌더 · 화면 갈무리 | `P3` | open | open (계보 그래프 조회 라우트는 `〈227〉` 로 섬) | 있음 | (c) `PV-1`(COG) — 엄격 직렬 |
| 3 | 팔레트 선택 서버측 재렌더 | `V-1` | open | open | **미작성** | (a) 완료 정의 기재 판정 ＋ (c) `P3` |
| 4 | 지도 값 조회(점 클릭) | `V-2` | open | open | **미작성** | (a) 완료 정의 기재 판정(정본 근거 0건) ＋ (c) `P3` |
| 5 | 렌더 산출물 자동 무효화 | `Y-1` | open | open | 있음 | (c) `P3`·`PV-1` |
| 6 | 실데이터 4종 E2E 완주 | `S3` | open | open | 있음 | (c) `P3` · `S2`(done) |
| 7 | 〃 의 무식별자 중복 계상 줄 | `S3-e2e-4종` | open | **중복 계상** — 별건 아님 | 있음(= S3) | (c) `S3` 과 동일 대상 |
| 8 | 검색 히어로 · 검색 결과 화면 판정 | `P4` | open | open — **화면은 서 있고 남은 것은 판정** | 있음(＋`〈208〉` 가산) | (a) 「Verified 우선 정렬」의 소유가 승인 처리라 순위 판정 불가(`〈224〉`) |
| 9 | 승인 처리(Verified 배지·우선 정렬·접근 요청) | `P6` | open | open | 있음 | (c) `P4`·`P3`·`P5`(partial) |
| 10 | 연구실 대시보드 | `P7` | open | open | 있음 | (c) `P6` |
| 11 | E-01 적용 지점 표 | `P8` | open | open | 있음 | (c) `P7` |
| 12 | 계보 제안 서비스 | `K3` | open | open — 빈 상태 3갈래는 `〈225〉` 로 섬 | 있음(`〈211〉`) | (d) 없음 |
| 13 | 계보 제안 원장 | `K5` | open | open | 있음 | (c) `K3` |
| 14 | 미등록 미리보기 화면 | `F-1` | in_progress | in_progress | 있음(`〈183〉`＋`〈210〉`) | (a) 이 화면의 권한·노출 규칙 `[미확인]` |
| 15 | 카탈로그 키워드 입력 제거 — 검색 진입점 일원화 | `F-2` | open | **실물 이미 충족 · 잔여 = 문구 1건** | 있음 | (c) `T-1` 판정 뒤 |
| 16 | 한국어 형태소 검색 설정 재작성 | `T-1` | open | open — ⓐⓒ 폐기 · ⓑ 다음 회차 · **ⓓ 는 `〈220〉` 로 판정 완료** | 있음(개정됨) | (a) 「잔여가 0 인가」의 상태 판정 |
| 17 | 편의 기능 묶음 9건 | `J-1` | open | open | **범위만 · 닫는 기준 미작성** | (c) Stage 2 마지막으로 못 박음 |
| 18 | 구글 계정 로그인 전환 | `PA-G` | open | open (`deadline.fired=false`) | 있음 | (c) 「v2 기존 기능 전부 완성」 조건 |
| 19 | 배포 자동화 | `I3` | partial | partial — **15행 중 열림/부분 6 → 이번 배포로 2 진전** | 있음 | (d) 없음. 잔여는 크론 설치 ＋ 롤백 왕복 회차 |
| 20 | 운영 준비(추적·알람·복구 리허설) | `I4` | open | open | 있음(`〈215〉`) | (c) `I3` |
| 21 | terraform state 보관 | `IS4` | partial | partial | 있음 | (c) 맨몸 리허설은 `R-1` 뒤가 싸다 ＋ 원격 백엔드는 `I0` 대기 |
| 22 | 복원 절차 | `R-1` | partial | **partial — 꼬리 4 중 3 이 닫혔다**(§3) | 있음(12행) | (d) 없음. 잔여 = `:i2` 이력 축적 · `preflight` 재측정 |
| 23 | 전범위 백업(원장 2 ＋ 볼륨 2) | `전범위백업` | partial | **완료 정의 4행이 실측으로 전건 충족**(§3) | 있음(`〈213〉`) | (d) 없음 — **상태 갱신만 남았다** |
| 24 | 배포 별칭 재부착 실패 삼킴 | `X-6` | open | **완료 정의 2항 코드로 전건 충족**(§3) | 있음 | (d) 없음 — **상태 갱신만 남았다** |
| 25 | 항목 상태 대장·검사기 | `W-1` | in_progress | in_progress | 있음(ⓐ~ⓔ) | (d) 없음 — ⓓ(대장 conflict 0)는 이미 0건 |
| 26 | 슈퍼유저 DB 비밀번호 회전 | `X-1` | done | done | 있음 | — |
| 27 | 계약 동결 해제 프로토콜 명문화 | `X-2` | done | done | 있음 | — |
| 28 | 프로젝트↔데이터셋 실연결 | `X-3` | done | done | 미작성(닫힘) | — |
| 29 | `_FILE` 배선 재기동 실전 검증 | `X-4` | done | done | 있음 | — |
| 30 | 계약 위반 소급 전수확인 | `X-5` | done | done | 있음 | — |
| 31 | 고아 행 삭제 판정 | `#22` | done | done — `〈216〉` 로 **철회**(측정·문서화로 닫음) | 있음 | — |
| 32 | 동결 해제 프로토콜 2단 | `2단-A` | done | done | 미작성(닫힘) | — |
| 33 | 2단 main 병합 | `2단-E` | done | done | 미작성(닫힘) | — |
| 34 | 원자료→월평균 계보 간선 확정 | `2단-계보간선…` | done | done | 있음 | — |
| 35 | 격자 전용 업로드 워커 제외 | `2단-격자전용-워커제외` | done | done | 있음 | — |
| 36 | 실패로 굳은 격자 전용 업로드 3건 처분 | `2단-격자전용-실패3건-처분` | deferred | ⏸ 하지 않기로 한 것 | 닫힘 사유가 정본 | — |

---

## 2. 대장 ↔ 증거가 어긋나는 자리 (어느 한쪽을 고르지 않는다)

| 항목 | 대장 값 · 시점 | 증거 값 · 시점 | 무엇이 갈리나 |
|---|---|---|---|
| 배포 별칭 재부착(`X-6`) | `status: open`(대장 기재 2026-08-29) | 완료 정의 2항 **전건 충족** — 삼킴 구문 부재(`9c6fc92`·`31f36a5`) ＋ 실패 픽스처 P15·P16·P20 실재. 실측 2026-08-29 `STAGE2-READINESS-AUDIT §1` | 상태값만 안 따라왔다. 다만 **staging 배포 green** 조항이 완료 정의에 없어 코드 충족＝닫힘인지 여부는 상태 판정 자리 |
| 전범위 백업 | `status: partial`(2026-08-29) | ⑴⑵⑷ **크론 무인 회차 GREEN 실측 2026-08-30T03:30:38**(원장 2 프로파일 ＋ 볼륨 2 · 「보관처 규약 밖 파일 0」) · ⑶ 은 코드 성질(`verify-volume-artifact.sh:174`) | 대장이 「크론 회차 대기」로 서 있는데 **그 회차가 돌았다** |
| 카탈로그 키워드 입력 제거(`F-2`) | `status: open` | `frontend/src/components/catalog/**` `<input>` **0건** · `useCatalog.ts:29` 에 `keyword` 없음 — ⓑⓒ 실물 충족(2026-08-29) | 남은 것은 문구 1건뿐인데 상태가 `open` |
| 복원 절차(`R-1`) | `status: partial`, **`note` 는 「`status: conflict` 를 그대로 둔다」로 적혀 있다** | 대장 필드는 `partial`. `note` 본문이 낡았다 | 같은 행 안에서 필드와 주석이 갈린다 |
| 실데이터 E2E(`S3`) 지원 포맷 | 이름·`§11` = **4종** | `§8.5` S3 행 · `㊿`·`〈77〉` = **5종**(＋NumPy) | 같은 문서 안에서 목록이 어긋난다 |

**[미확인] 로 남기는 것**

| 미확인 | 푸는 법 |
|---|---|
| 이번 배포 회차의 게이트 전 종 결과(`I3` ⑽) | 배포 판정 시점에 적용 DB URL 두 체인을 선언하고 `./gates/run.sh all` 1회 |
| 이번 배포 회차의 `healthz` **전** 값(`I3` ⑾) | 다음 배포 **직전**에 값과 시각을 먼저 기록 |
| `IS4` 맨몸 호스트 조건 · 마지막 `apply` | `R-1` 리허설과 같은 회차에 재료를 공유해 1회 실측 |
| `F-1` 미등록 미리보기 화면의 권한·노출 규칙 | Ted 가 정본에 이 화면의 컨트롤 행을 세운다 |

---

## 3. 배포 green 이 방금 푼 것 (satisfiable ≠ satisfied)

**실측 증거** — `~/colab-v2-releases/release-ledger.tsv` 마지막 줄
`2026-08-30T10:41:50+0900 deploy cfc98e302ae4 cfc98e302ae4 **green** 별칭재부착GREEN digest이력=6종 배포전백업GREEN 워킹트리변경=0`
＋ `LAST-SUCCESS.txt` 마지막 줄 동일 시각.
`〈226〉`-㉮ 축자 — **모든 stage 2 완료 정의의 ⑶ 이 「staging 배포 green」이므로 배포 자동화가 서지 않으면 어떤 항목도 닫히지 않는다.**

### 3.1 「staging 배포 green」 **하나에만** 걸려 있던 것

| 항목 | 이제 만족 가능해진 조항 | **여전히 남은 것** |
|---|---|---|
| 배포 자동화(`I3`) ⑷⑸ 롤백 왕복 | 원장의 green `deploy` 행이 **1 → 2**(`30b3e0a7b3f3`·`cfc98e302ae4`) → `ledger_rollback_target()` 의 대상이 0 을 벗어났다 | **롤백 → 재배포 회차를 실제로 1회 태워야 한다.** 열림이지 닫힘 아님 |
| 배포 자동화(`I3`) ⑵-b | 배포 판정 15건 · 별칭 재부착 GREEN · digest 이력 6종 기록 | **실배포 red 로그**가 여전히 없다(부분) |
| 복원 절차(`R-1`) ⑷ `:i2` 태그 이력 | `〈219〉`-㉯ digest 이력 기구가 **실회차에 처음 기록됐다**(「digest이력=6종」) | **이력 1회분뿐** — 이동 이력의 축적은 다음 회차부터. 과거 이동은 복원 불가 |
| 스키마 드리프트(`schema-diff`) | `0008_lineage_origin_labels.py` 가 **적용됐다** — `〈228〉`-㉮ 가 유일 드리프트로 지목한 `d4_lineage_edge_origin_check` 허용값 | **아직 재측정 0회.** ＋ `〈R5〉` 가 드러낸 **적용 DB URL 미선언 red** 는 배포로 풀리지 않는다 — 두 체인 URL 을 선언하고 다시 돌려야 한다 |
| 미리보기 뒷단(`PV-1`) | `〈223〉` 산출물이 배포에 실렸다 | 완료 정의의 사건 3종(`file.header-parsed`·`file.crs-normalized`·`preview.cog-built`) **발행 실적 0건 실측** — 워커 스위치(`COLAB_WORKER_STAGE2`)를 켜고 실데이터로 돌려야 한다 |
| 계보 그래프(`P3`) | `〈227〉` 의 `getDatasetLineage` 라우트가 배포에 실렸다 | 「각 P 의 완료 판정」 4항 중 ①②④(수용 기준 · 도메인 게이트 · 목업 대비) 미판정 ＋ 타일 서빙·확대는 **정본 규정 0건** |
| 계보 제안(`K3`) | `〈225〉` 의 빈 상태 3갈래가 배포에 실렸다 | 완료 정의 ⑴⑵ 의 **staging 실측 판정 미실행** |

### 3.2 배포로 **풀리지 않는** 것 — 착각하지 않는다

- **자동 트리거(`I3` ⑴)** — `crontab -l` 실측(2026-08-30) = `run-pipeline.sh`·`watch.sh` 행 **0건**. 걸려 있는 것은 백업 둘(`backup-full.sh` 03:30 · `latest-check.sh` 월 04:10)뿐. **스케줄 미설치 그대로.**
- **(a) 판정 대기 5건** — 배포는 Ted 의 상품 판정을 대신하지 않는다.
- **`P4` 의 정지 사유** — 「Verified 우선 정렬」의 소유가 승인 처리라는 구조 문제(`〈224〉`)이고, 배포와 무관하다.

---

## 4. 의존 간선 (그래프로 그릴 수 있는 형태)

### 4.1 간선 목록 — `A -> B` = **A 가 B 의 선행**

대장 `depends_on` 실측 ＋ 완료 정의가 명시한 선행. stage 2 밖 노드는 `[stage1]`·`[after]` 로 표시.

```
# 사슬 A — 시각화
D5[stage1,partial]   -> PV-1
PV-1                 -> P3
P2[stage1,done]      -> P3
D5[stage1,partial]   -> P3
P3                   -> V-1
P3                   -> V-2
P3                   -> Y-1
D5[stage1,partial]   -> Y-1
P3                   -> S3
S2[stage1,done]      -> S3
S3                   -> S3-e2e-4종
P3                   -> LV-3[after]

# 사슬 B — 화면·승인
P1[stage1,done]      -> P4
G2[stage1,done]      -> P4
K4[stage1,open]      -> P4
T-1                  -> P4
T-1                  -> K4[stage1,open]
P1[stage1,done]      -> P6
P2[stage1,done]      -> P6
P3                   -> P6
P4                   -> P6
P5[stage1,partial]   -> P6
P6                   -> P7
P7                   -> P8
P4                   -> G1b[after]

# 사슬 C — 배포·운영
I2[stage1,done]      -> I3
I3                   -> I4
IS2[stage1,done]     -> IS4
R-1                  -> 전범위백업
R-1                  -> #22   (done)
R-1                  -> X-1   (done)
R-1                  -> X-4   (done)
X-1                  -> X-4   (done · 상호)
X-4                  -> X-1   (done · 상호)
#22                  -> X-1   (done)
I4                   -> I5[out_of_scope]
P7                   -> I5[out_of_scope]

# 사슬 D — 검색
T-1                  -> F-2

# 사슬 E — 계보 제안
K2[stage1,done]      -> K3
D2[stage1,done]      -> K3
P2[stage1,done]      -> K3
K3                   -> K5

# 전건 후행 / 횡단
(v2 기존 기능 전부 완성) -> PA-G
(Stage 2 마지막)         -> J-1
W-1  = 횡단(전 항목의 상태를 적는다 · 회차 끝에 한 번)
I3   = 관문(모든 항목의 완료 정의 ⑶ · 〈226〉-㉮)

# 무의존 (선행 0)
PV-1(D5 stage1 파트 완료 후) · X-6 · R-1 · T-1 · F-1 · W-1
```

### 4.2 간선이 **아닌** 것 (오독 방지 · 판정 근거 있음)

| 오독되던 간선 | 판정 |
|---|---|
| `I4 ── P6` | **WU 의존 아님**(`〈168〉`-㉵ · `〈215〉`-㉱). `§9` 그래프의 인프라 진행 순서를 의존으로 읽은 것 |
| `R-1` ⊃ `I4` (흡수) | **분리 유지**(`〈215〉`-㉮). `I4` 는 `R-1` 의 결과를 받아 쓰고 리허설을 다시 돌리지 않는다 |
| `F-1` ↔ `F-2` 파일 면 충돌 | 겹치는 파일 **0건** 실측. `F-2` 를 막는 것은 파일 면이 아니라 `T-1` |

---

## 5. 임계 경로 (Stage 2 완주까지의 최장 사슬)

회차 크기의 기준 = **어제(2026-08-29~30) 밤의 실적**. 「1회차」 ＝ 에이전트 작업 약 15분.
어제 실적 근거 — `〈219〉`~`〈228〉` 10항목이 야간 3레인 병렬로 났고, 회차당 산출이 문서 1 ＋ 코드 변경 1묶음이었다.

```
PV-1 → P3 → P6 → P7 → P8 → (J-1) → PA-G
        ↑
       P4
```

| 홉 | 기능 | 크기(회차) | 근거 |
|---|---|---|---|
| 1 | 미리보기 뒷단 — 스위치 해제 ＋ 실데이터 완주 ＋ 사건 3종 발행 실적 | **4~6** | `〈223〉` 이 소비자·감지·키 규칙까지 3회차. 남은 것은 실행·실측이고 실데이터 회차는 재시도가 붙는다 |
| 2 | 계보 그래프·격자 렌더·화면 갈무리(`P3`) | **6~8** | 「`P3` 은 stage 2 전체다」(`〈74〉`). `〈227〉` 이 라우트 하나에 1회차 — 타일 서빙·화면·목업 대비가 남았다 |
| 2' | (병렬) 검색 화면 판정(`P4`) | **2~3** | `〈224〉` 가 이미 정지 지점을 확정. 판정 자체는 승인 처리에 종속 |
| 3 | 승인 처리(`P6`) — 배지·우선 정렬·접근 요청·잠금을 **7 화면**에 | **6~8** | 화면 7곳 × (구현 ＋ 목업 대비). 열린 `⑭` 판정 1건 포함 |
| 4 | 연구실 대시보드(`P7`) — D8 실동작 op 0 이 여기서 닫힌다 | **4~5** | 데이터 맵·할 일 함·최근 활동·정보 모달 4묶음 |
| 5 | E-01 적용 지점 표(`P8`) — 정책·패키지 재생성 | **2~3** | 표 작성 ＋ 재생성. 코드 면 좁다 |
| 6 | 편의 기능 묶음(`J-1`) 9건 ＋ **닫는 기준 기재** | **5~7** | 9건 · 완료 정의부터 써야 한다 |
| 7 | 구글 계정 로그인 전환(`PA-G`) — 어댑터 교체 ＋ 비밀번호 경로 제거 | **3~4** | ⓐ~ⓕ 6항 · `PA` 5항 재통과 포함 |
| — | 각 홉마다 **staging 배포 green 회차** | **＋1 씩 (총 ＋6~7)** | 완료 정의 ⑶. 배포는 사람 승인 게이트를 탄다 |

**임계 경로 합 = 약 36~48 회차 ＋ 배포 6~7 회차 ≒ 42~55 회차.**
15분 환산 시 **약 11~14 시간의 에이전트 작업**이나, ⓐ 배포 승인 대기 ⓑ Ted 판정 5건 ⓒ 실데이터 회차의 재시도가 이 값에 들어 있지 않다 — **경과 시간은 이 값보다 크다.**

⚠ **이번에 세지 않은 판단기준(다음 회차 진입조건)**
① 각 홉의 화면 수·시험 수 실계수 ② 병렬 레인 3 을 유지했을 때의 벽시계 단축률 ③ 배포 승인 대기 시간의 실적 분포. 셋 다 `[미확인]`.

---

## 6. 지금 바로 지을 수 있는 것 (막는 것 없음 · 우선순)

| 순위 | 기능 | 코드 | 실작업 | 크기 |
|---|---|---|---|---|
| 1 | 미리보기 뒷단 실행 — 워커 스위치 해제 ＋ 사건 3종 발행 실적 | `PV-1` | 임계 경로의 머리 | 4~6 |
| 2 | 배포 자동화 잔여 — 롤백 왕복 ＋ 크론 설치 | `I3` | **관문**. 대상 0건이 방금 풀렸다 | 2~3 ＋ 배포 |
| 3 | 계보 제안 서비스 staging 판정 | `K3` | 코드는 `〈225〉` 로 섰다 · 판정만 | 1~2 |
| 4 | 상태 갱신 3건 | `X-6`·`전범위백업`·`R-1` | **코드 0** — 대장 `status`·`evidence` 를 실측에 맞춘다 | 1 |
| 5 | 항목 상태 대장·검사기 마무리 | `W-1` | ⓐ~ⓔ 중 잔여 배선 | 1~2 |
