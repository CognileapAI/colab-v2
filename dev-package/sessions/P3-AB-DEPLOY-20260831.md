# P3 — A/B 를 실배포에 올려 두 갈래를 실물로 재고, `#51` 을 닫았다 (2026-08-31 · 워크트리 `lane-abdeploy`)

> **한 줄** — 배포는 **GREEN**, 타일은 **공개 엣지에서 실제로 나온다**(`#51` 해소).
> 그리고 **문서가 적어 둔 켜는 법이 실제로는 통하지 않았다** — 스위치가 배포에 배선되지 않아
> 홈 env 에 무엇을 적어도 컨테이너는 영영 꺼짐이었다. 고치고 나서야 A/B 를 잴 수 있었다.
> **staging 은 기본값(꺼짐 = 한 장)으로 두고 나왔다.**

값·근거의 정본은 `PLAN-SoT §9 〈241〉`(배포·A/B)과 `〈242〉`(자격 회전) 둘이다.
여기는 순서와 계수, 그리고 **양갈래 대조표**만 적는다.

---

## 1. 순서 — 되돌릴 수 있는 것부터 했다

지난 회차가 순서를 뒤집어 red 를 낸 자리(`〈236〉`-㉯ — 스택을 내린 채 배포 전 백업)를 반복하지 않았다.

| # | 한 일 | 왜 이 순서인가 |
|---|---|---|
| ① | **자격 회전**(`〈242〉`) → 새 토큰 200 · 옛 토큰 401 실측 | 파일 한 장 + 한 단위 재기동이라 **되돌리기가 가장 싸다.** 여기서 막히면 배포를 시작하지 않는다 |
| ② | **배포**(스택 8/8 healthy 인 채로 착수) | 배포 전 백업은 **스택이 살아 있어야** 돈다 |
| ③ | 꺼짐 실측 → **켜짐**으로 전환 → 켜짐 실측 → **꺼짐으로 복귀** | 기본값이 마지막에 남게 한다. 중간에 끊겨도 정본 문면 쪽이다 |
| ④ | 게이트 `all` · 상태 갱신 | 상태 변경은 대장이 먼저다 |

## 2. 자격 회전 — **이름만 적는다**

| 축 | 실측 |
|---|---|
| 회전 대상 | `COLAB_STAGING_SUBJECTS_FILE` 이 가리키는 **심어 둔 주체 표의 베어러 토큰 1건** |
| 회전하지 않은 것 | `COLAB_VIZ_SERVICE_TOKEN` · `COLAB_VIZ_TILE_SIGNING_SECRET` · DB 3롤 · `CF_*` — **유출 사실이 없다.** 범위를 늘리지 않는다 |
| 방법 | `RESTART.md §2-①` 규약 그대로 — **제자리 덮어쓰기**(바인드 마운트는 inode 에 붙는다) ＋ `docker restart` 로 core-api 재기동. 권한 `0600` · 소유자 uid `10001` 유지 |
| 불변 | 주체의 `accountId`·`labId` 는 **그대로다** — 바뀐 것은 열쇠뿐이다 |
| **양성 검증** | 새 토큰 → 공개 엣지 `GET /api/v1/me` **200**, 본문의 주체가 옳다(연구실 `…HYMETS` · 이름 일치) |
| **음성 검증** | 옛 토큰 → **401 `UNAUTHORIZED`** · 무토큰 → **401**. 「새 것이 된다」만으로 회전을 인정하지 않는다 |
| 옛 값의 사본 | 회전 검증 뒤 **전부 삭제**(백업본 포함). 레포·문서·원장·세션 기록 어디에도 값이 없다 |

⚠ **값은 이 문서에도, 커밋 메시지에도, 원장에도 없다.** 이름과 판정만 적는다.

## 3. 배포 — 두 번 했고 두 번 다 GREEN

**두 번인 이유가 이 회차의 발견이다**(§4).

| 축 | 1차 | 2차(최종) |
|---|---|---|
| 릴리스 태그 | `273bbd64eef6` | **`de2241ad83ef`** |
| 배포 판정 | GREEN · 통과 15건 · SKIP 0 | **GREEN · 통과 15건 · SKIP 0** |
| 컨테이너 | 8개 healthy | **8개 healthy** |
| 호스트 노출 `0.0.0.0` | 0건 | **0건** |
| 헬스 | 6종 200 ＋ 본문 대조 | **6종 200 ＋ 본문 대조** — 루트 200 으로 판정하지 않았다 |
| 체인 head | platform `0008_lineage_origin_labels` · ai `0005_k2b_concept_graph_seed` | **같다 · 마이그레이션 0건** |
| 별칭 `:i2` | 6/6 이미지 ID 대조 GREEN | **6/6 GREEN** |
| 배포 전 백업 | GREEN(스택 UP 상태) | **GREEN** · 착수 red 0건 |
| 워킹트리 변경 | 0건 | **0건** |

### ⑩-b — **새로 고친 함정이 실제로 돌았다**

`deploy.sh` ⑩ 이 `sha256sum nginx.i2.conf` 를 `COLAB_EDGE_CONF_SHA` 로 실어 nginx 를 재생성시키고,
**⑩-b 가 도는 컨테이너 안의 설정을 읽어 레포 판과 바이트로 대조**한다.

```
⑩-b 엣지 설정 반영 판정 — 도는 컨테이너 안의 파일과 바이트 대조
   확인 엣지 설정 — 레포 판과 도는 판이 같다(56cc63f04dad443a)
```

**독립 확인** — 배포 밖에서 다시 쟀다: 도는 nginx 안의 설정 해시 = `56cc63f04dad443a`,
컨테이너 환경의 `COLAB_EDGE_CONF_SHA` = 같은 값. **「고쳤다」를 「돌고 있다」로 읽지 않았다.**

## 4. ⚠ 켜는 법이 통하지 않았다 — 배선 결함 1건, 고치고 배포했다

`〈240〉`-㉲ 가 스위치를 `COLAB_VIZ_TILE_BRANCH` 로 세우고 `P3-TILE-AB-20260831.md §1` 이
「홈 env 에 한 줄 넣고 viz-render 를 다시 띄운다」라고 적었다. **그 절차로는 켜지지 않는다.**

**실측** — 1차 배포 뒤 `docker inspect colab_v2_staging_viz_render` 의 환경에
`COLAB_VIZ_TILE_BRANCH` **0건**. `compose.i2.yml` 의 `viz-render` 블록에 그 키가 없었고,
`--env-file` 은 compose 파일의 `${...}` 치환에만 쓰여 **컨테이너 환경으로 흐르지 않는다.**

> **코드는 스위치를 읽는데 배포가 그것을 실어 나르지 않았다.**
> `#20`(접수분 루트) · `#49`(미리보기 자리)와 같은 무늬다 — **선언은 코드에, 배선은 배포에.**

**고친 자리 하나** — `infra/staging/compose.i2.yml` 의 `viz-render` 에
`COLAB_VIZ_TILE_BRANCH: ${COLAB_VIZ_TILE_BRANCH:-}`. **레포에 켜진 값을 박지 않는다** —
비면 정본 문면대로 「한 장」이다.

**회귀는 시험이 진다** — `services/viz-render/tests/test_tile_branch_deployment_declaration.py` **3건**
(착수 시 **3 red** 실측 → 배선 뒤 green). ⑴ 선언이 있는가 ⑵ 홈 env 를 통과시키는 `${...}` 꼴인가
⑶ **레포가 켜진 기본값을 싣고 있지 않은가**. viz-render **161 → 164**.

## 5. 양갈래 대조표 — **같은 릴리스(`de2241ad83ef`) · 같은 데이터셋 13건 · 각 2회**

| 볼 것 | **꺼짐(기본값 = 한 장)** | **켜짐(타일)** |
|---|---|---|
| `/healthz/viz-render` 의 `tileBranch` | **`꺼짐`** | **`켜짐`** — 실제 상태와 일치 |
| 컨테이너 환경 | `COLAB_VIZ_TILE_BRANCH` **빈값** | `1` |
| 결과 본문의 갈래 | **`imageUrl` 26/26** (`tileUrlTemplate` 0건) | **`tileUrlTemplate` 26/26** (`imageUrl` 0건) |
| 렌더 완주 | **26/26 완료** | **26/26 완료** |
| 공개 엣지 — 주 화면 | `/previews/<해시>.png` **13/13 = 200 ＋ `image/png` ＋ 매직 `89504e47`** (15,852 ~ 1,214,406 B) | 같은 자리(미등록 업로드 건) **200 ＋ `image/png`** — **꺼진 갈래 회귀 0건** |
| 공개 엣지 — 타일 | (표면 없음) | **104/104 = 200 ＋ `image/png` ＋ 매직 `89504e47`** · 데이터셋 26 × **배율 4종**(`z=6·8·10·12`) · 334 ~ 16,254 B |
| **미리보기 최초 표시**(공개 API 왕복 · 표본 26) | 중앙값 **1.286 s** · p95 **14.223 s** · 최대 **14.256 s** | 중앙값 **1.234 s** · p95 **14.077 s** · 최대 **14.089 s** |
| **확대 1회** | **서버 왕복 0건** — 한 장을 화면이 변환한다 | 타일 1조각 왕복 **중앙값 270.2 ms · p95 342.8 ms**(찬 요청 104건) / 재요청 **중앙값 158.2 ms · p95 175.0 ms** |
| 미등록 업로드 | 한 장 | **한 장**(실표본 1건 — §6) |
| 비지도형 | — | **실표본 0건**(§6) |

### 눈금과 대면 — **두 자리가 갈리고, 갈리는 이유를 적는다**

| 눈금 | 집행부(게이트) | 공개 엣지 왕복(이 회차 추가 계측) |
|---|---|---|
| 미리보기 최초 표시 **p95 10초 · 상한 60초** | **green** — 표본 25 · 포맷 5종 · 중앙값 0.710 s · **p95 3.061 s** · 최대 3.811 s | **p95 14.2 s** — 눈금 초과 · 상한(60 s)은 이내 |
| 확대 1회 **100 ms** | 꺼짐 갈래는 **왕복이 없다** | 켜짐 갈래 **p95 175 ~ 343 ms** — 눈금 초과 |

⚠ **두 수는 다른 것을 잰다 — 하나로 뭉치지 않는다.**
게이트 `render-latency` 가 재는 것은 **렌더 시간**이고, 위 오른쪽 칸은 **공개 엣지 왕복 전체**다
(`gates/config/render-latency.toml` 이 「재지 않은 넷」으로 이름을 적어 둔 그 몫 — 중계 왕복 ·
조회 주기 · 내려받기 · 브라우저). **엣지 왕복의 바닥값을 따로 쟀다** — `/healthz`(3 바이트)가
**중앙값 77.6 ms · p95 92.5 ms** 다. 즉 **타일 한 조각의 서버 몫은 약 80 ms 이고, 100 ms 눈금의
대부분을 터널 왕복이 먼저 먹는다.** 이 호스트에서 공개 엣지를 거치는 한 켜진 갈래는 그 눈금 안에 들지 않는다.

⚠ **p95 14 s 의 정체도 적는다** — 26 표본 중 **둘**(`MODIS MOD15A2H 견본` 14.2 s ·
`GK2A/AMI NDVI 원자료(Lv.0)` 12.2 s)이 나머지(0.34 ~ 2.5 s)와 자릿수가 다르다. **두 갈래 모두 같은 두 건**이고
차이는 갈래가 아니라 **그 원천**이다. **꺼짐과 켜짐 사이에 유의미한 성능 차가 없다** — 이것이 A/B 의 첫 결론이다.

## 6. 경계 셋 — 켜도 넘지 않는가

| 경계 | 실측 |
|---|---|
| **미등록 업로드는 언제나 한 장** | ⭑ **실표본 1건 — 두 회차 만에 0건을 벗어났다.** 켜진 갈래에서 실파일(GeoTIFF 24.6 MB)을 등록하지 않고 접수해 `uploadId` 로 그렸다 → 결과 키에 **`imageUrl` 있음 · `tileUrlTemplate` 없음**. 그 PNG 는 공개 엣지에서 **200 ＋ `image/png`**(1,074,597 B · 매직 `89504e47`) |
| **좌표 없는 자료는 언제나 한 장** | ⛔ **실표본 0건 — 지어내지 않는다.** 등록 13건이 전부 좌표를 가진 격자인 것은 지난 회차와 같다. **그리고 이번에 이유를 하나 더 쟀다** — 좌표가 없는 파일(작은 CSV)을 접수해 그리면 **갈래에 닿기 전에 표면이 거절한다**(`503 RENDER_UNAVAILABLE` · 「viz-render 가 415 로 답했다」). 즉 **이 경계는 실물 표본으로는 재기 어렵고, 잠그는 자리는 시험**이다(`test_render_result_tiles.py` 음성) |
| **서명 비밀이 없으면 아예 그리지 않는다** | 이 회차는 비밀이 배선돼 있어 해당 없음. 잠그는 자리는 시험(503 `TILE_SIGNING_UNCONFIGURED`) |

## 7. 서명 음성 3종 — **켜진 갈래 · 공개 엣지에서 실측**

| 요청 | 결과 |
|---|---|
| (대조군) 정상 서명 | **200 ＋ `image/png`** |
| 서명 **없음** | **401** ＋ `application/json` — 「타일 주소의 서명이 없거나 맞지 않는다」 |
| 서명 **변조**(한 글자) | **401** — 같은 문구 |
| **만료**(지난 `exp` ＋ **그 `exp` 를 덮는 유효 서명**) | **401** — 「타일 주소의 **서명이 만료됐다**. 미리보기를 다시 그려 주세요」 |

**만료와 불일치를 갈라 답한다** — `kernel/signing.verify` 의 3값(`True`/`False`/`None`)이
공개 면까지 그대로 살아 있다. 만료 표본은 **컨테이너 안에서** 서명을 만들어 던졌다(값은 밖으로 나가지 않았다).

## 8. `#51` — 닫는다

**물음** = 「타일 주소가 엣지에서 화면에 닿지 않는다 — 브라우저가 PNG 대신 SPA 의 `index.html` 을 200 으로 받는다」.

**실측(배포된 스택 · 릴리스 `de2241ad83ef` · 켜진 갈래)** = 공개 엣지에서 타일 조각이
**104/104 · 200 ＋ `image/png` ＋ PNG 매직**. `text/html` **0건**. 배율 4종에서 같다.
**꺼진 갈래의 회귀 0건**(`/previews/…` PNG 200 ＋ `image/png`).
**동반 `[미확인]`(nginx 재생성)도 닫혔다** — ⑩-b 가 배포마다 도는 설정을 바이트로 대조한다(§3).

## 9. 게이트

| 축 | before | after |
|---|---|---|
| `gates/run.sh all` | **green 35 / red 0**(`〈240〉` 회차 · 같은 트리) | **green 35 / red 0** |
| `render-latency` | green | **green** — 표본 25 · 포맷 5종 · p95 3.061 s |
| `preview-tile-slot` · `autometa-loss` | green | **green** |

⚠ **첫 실행에서 red 4건이 났고 전부 「준비」였다 — 우회하지 않고 환경을 세웠다.**
새 워크트리에 `services/viz-render`·`services/pipeline-worker` 의 venv 와 `frontend/node_modules` 가
없어 `generated-up-to-date`·`stage2-markers`·`e2e-format-coverage`·`render-latency` 가 검사기를 못 띄웠다.
**셋을 설치해 다시 돌렸고 전부 green 이 됐다.** `rls-effect-selftest` 는 일회용 postgres 대기로
한 번 red(준비)를 냈고 **재실행에서 green**(케이스 18건). **눈금을 낮추거나 검사를 줄인 건 0건이다.**

## 10. `P3` — 조건별로 대조했고, **닫는다**

「각 P 의 완료 판정」 4항(`WORK-UNITS §7` 말미) ＋ `〈240〉` 의 개정.

| # | 조건 | 판정 |
|---|---|---|
| 1 | 해당 단계 스토리의 **수용 기준 통과** | ✔ **충족.** 직전 회차의 유일한 미충족 사유는 `#51` 이었고 이번에 닫혔다(§8). **기본 갈래(한 장)가 공개 엣지에서 13/13 도달** · **선택 갈래(타일)도 104/104 도달** · `createScreenshot` 이 배포된 스택에서 **200 ＋ `image/png`**(중계 `POST /api/v1/preview-screenshots` 실측) · 확대는 `〈232〉` 회차에 정본 조건 축자로 착지 |
| 2 | **도메인 게이트 green** | ✔ `all` **green 35 / red 0** |
| 3 | **staging 배포 green** | ✔ 릴리스 **`de2241ad83ef`** · 판정 GREEN(§3) |
| 4 | **목업 대비 화면 검증** | — **`P3` 의 차단이 아니다.** 판정 주체가 Ted 의 직접 확인이고 WU 밖이다(`〈234〉`) |
| ＋ | `〈240〉` — **A/B 결과가 나오기 전까지 어느 갈래도 완료 조건이 아니다** | ✔ **A/B 결과가 이 회차에 났다**(§5). 그리고 **어느 갈래도 완료 조건으로 세우지 않았다** — 기본값(한 장)이 도달하는 것으로 1항을 판정했고, 타일은 선택 갈래로서 **함께** 도달함을 보였다 |

**부분 완료가 아니다 — 네 항이 다 선다.** `status: open → done`.

⚠ **닫는 것과 함께 남기는 것 하나** — **어느 갈래를 기본으로 둘 것인가는 Ted 의 판정이고 열려 있다.**
`〈240〉` 이 그렇게 정했고 이 회차는 **자료만 만들었다.** staging 은 **기본값(꺼짐 = 한 장)** 으로 두고 나왔다.

## 11. `P3` 가 연 것 — **해제 4건**

| 항목 | 이번 회차 뒤 남은 선행 |
|---|---|
| `P6` 승인 처리 | **선행 0건 — 착수 가능.** `P1`·`P2`·`P5`·**`P3` done**. 미판정 ⑭(접근 승인 조기 회수)는 대장 판정이 「안 막힘」 |
| `Y-1` 자동 무효화 | **선행 0건 — 착수 가능.** `D5` done ＋ `P3` done |
| `S3` 실데이터 E2E | **선행 0건 — 착수 가능.** `S2` done ＋ `P3` done |
| `V-1` 팔레트 재렌더 | `P3` 는 풀렸다. **남은 선행 = 완료 정의 미작성**(`〈213〉`) — Ted 판정 |
| `V-2` 값 조회 | `P3` 는 풀렸다. **남은 선행 = 완료 정의 미작성**(`〈213〉` · `〈232〉` 는 확대만 판정했다) |

## 12. 계수

| 축 | 값 |
|---|---|
| 배포 | **2건 green**(`273bbd64eef6` · **`de2241ad83ef`**) · 착수 red 0건 |
| 자격 회전 | **1건**(주체 표 베어러) · 양성·음성 검증 각 1건 · **값 출력 0건** |
| 코드·설정 변경 | **1파일**(`infra/staging/compose.i2.yml` — 환경 1줄) |
| 시험 | 새 파일 **3건**(착수 시 **3 red** 실측) · viz-render **161 → 164** |
| 계약 개정 · 동결 해제 · 마이그레이션 · 생성물 손수정 | **0건** |
| 게이트 `all` | before **green 35 / red 0** → after **green 35 / red 0** |
| 이번 변경이 만든 red | **0건** · 우회·비활성화·면제 선언·검사 축소 **0건** |
| 블로커 | **1건 해소(`#51`)** · 신설 **0건** |
| 상태값 변경 | **1건** — `P3` `open → done` |
| push | **0건** |
| staging 최종 상태 | **기본값 = 꺼짐(한 장)** · 8/8 healthy · 릴리스 `de2241ad83ef` |
