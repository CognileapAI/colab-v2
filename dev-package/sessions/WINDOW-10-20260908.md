# 창 10 배포 창 집행 — staging 리허설 → dev 판정 (2026-09-08 · 등재 `PLAN-SoT §9 〈381〉`)

- 대상 `main` = `ccf76ecf0bb5` · 계획 = `dev-package/sessions/WINDOW-10-plan-20260908.md` · 선례 = 창 9(`dev-package/sessions/R-C-ROUND-20260908.md §9` · 원장 `〈378〉`).
- 파일명 = 지시받은 이름 그대로(계획서 §5 초안은 `WINDOW-20260908-10.md`) · 로그 = 집행 세션 산출 17건(호스트 임시 폴더 · 레포 미반입).

## 0. 대상 · 전제

| 값 | 실측 |
|---|---|
| 배포 대상 sha | `ccf76ecf0bb5` — `main` = `origin/main` 동일 · 워킹트리 변경 0 |
| 마이그레이션 변화 | **0건** — 두 체인 head 는 창 9 적용값 그대로(platform `0022_rc7_variable_mirror_stmt` · ai `0007_merge_vocab_and_category`) · 프론트 변화도 0건이라 `ops/deploy_web.py` 미실행 |
| staging 사전 | 컨테이너 8개 전부 `healthy`(6개 `Up 11 hours` · `cloudflared`·`pg` `Up 2 days`) |
| 판정처 | **dev 하나.** staging 은 리허설이라 배포 창 행을 만들지 않는다(`CLAUDE.md §0` · `〈335〉`-㉯) |

## 1. staging 리허설 (판정 아님)

| # | 단계 | 결과 |
|---|---|---|
| S3 | `approve.sh` | `승인자=Ted 본것=stage-dev-둘다-배포하자·계획서§1§2·마이그레이션0·staging8/8healthy` (19:57:16) |
| S4 | `infra/staging/deploy.sh --target staging` | **GREEN 41초**(19:57:25 → 19:58:06) · `배포 GREEN — 커밋 ccf76ecf0bb5 · 태그 ccf76ecf0bb5` · SKIP 플래그 0 |
| — | S4 내부 | ② 호스트 게이트 `migration-single-head` 19:57:26 통과 · ⑤ 배포 전 백업 두 프로파일 GREEN(합격선 = 테이블 20 · 행 190) · ⑧ 마이그레이션 2체인 19:57:32 platform · 19:57:34 ai **무동작**(전제대로) |
| S5 | 원장 마지막 행 | `2026-09-08T19:58:06+0900 deploy ccf76ecf0bb5 ccf76ecf0bb5 green 별칭재부착GREEN digest이력=6종 배포전백업GREEN 워킹트리변경=0 브랜치=main` |
| S6 | `verify-deploy.sh` · `verify-chains.sh` | `배포 판정: GREEN (통과 15건 · SKIP 0)` · `체인 판정: GREEN (통과 2건 · SKIP 0)` — head platform `0022…` / ai `0007…` |
| S7 | 온톨로지 계수(읽기 전용) | `concept=49 edge=19 method_term=13 topic_synonym=18 place_alias=4` |

## 2. dev 판정 배포

| 순서 | 단계 | 시각 | 결과 |
|---|---|---|---|
| 1 | `infra/dev/build.sh` | 19:59:07→19:59:16 (**9초**) | 캐시 적중 · `viz-render` 만 재빌드 · `dist/colab-v2-dev-ccf76ecf0bb5.tar` 274M · exit 0 |
| 2 | `infra/dev/ship.sh` | 19:59:55→20:00:32 (**37초**) | **반입 게이트 첫 실전** exit 0 · `Loaded image:` 5줄 · `loaded: dev-ccf76ecf0bb5` |
| 3 | 반입 결과 확인 | 20:00 | `MAIN_SHA=main=ccf76ecf0bb5 candidate=ccf76ecf0bb5 ancestor=yes` · `CURRENT_SHA=ccf76ecf0bb5` · EC2 tar 3세대 보존 |
| 4 | `/opt/colab-repo` 동기 | 20:01 | `deploy_doctor.py` md5 `a2d5651512ff786f0a71eeb36e51ed9c` — 개발 기계와 일치 |
| 5 | `backup.sh`(up 전) | 20:01:23~26 | GREEN · `_ops/backups/dev/2026-09-08T110123Z-colab_platform.sql.gz` 71,669 B · `…-colab_ai.sql.gz` 5,518 B |
| 6 | `/opt/colab-v2/up.sh` | 20:01 | ① 마이그레이션 **무동작** · ② `viz_render` 만 Recreate · ③ 4단위 healthy · ④ `storageMode=s3` · `sourceMode=s3` · `up: ok (sha ccf76ecf0bb5)` exit 0 |
| 7 | `deploy_doctor --env dev` **1회** | 20:02 | `항목 15 — ✓ 15 · ✗ 0 · ─ 0` · exit 0 · `--allow-skip` 없음 · `op.env removed: yes` |
| 8 | `infra/dev/tag-release.sh dev` | 20:03 | `태그 생성(로컬): dev-20260908-1 → ccf76ecf0bb5` |

- 전체 소요 ≈ **12분**(19:57 → 20:09) · 반입 거절 0 · SKIP 0 · 비밀 출력 0.

## 3. `deploy_doctor` 15 항목 (요약 축자)

```
  ① 운영자 자격증명                                 ✓
  ② 데이터 버킷 설정  colab-platform-data-dev       ✓
  ③ 웹 버킷  colab-platform-web-dev             ✓
  ④ DB 연결 (platform)                         ✓
  ⑤ DB 연결 (ai)                               ✓
  ⑥ 스키마 head (platform)                      ✓
  ⑦ 스키마 head (ai)                            ✓
  ⑧ RLS 전수 (살아 있는 DB)                        ✓
  ⑨ 앱 롤 속성                                   ✓
  ⑩ 5 단위 헬스                                  ✓
  ⑪ 앱 자격증명 출처 (core /healthz/storage)        ✓
  ⑫ 환경 짝  dev                                ✓
  ⑬ 진입·라우팅 (endpoint)                        ✓
  ⑭ 백업 24h                                   ✓
  ⑮ 실행 sha ∈ main                            ✓

  항목 15 — ✓ 15 · ✗ 0 · ─ 0
  전 항목 통과 (─ 0 — 15 항목이 실제로 돌았다)
```

- 값 = ⑥ `0022_rc7_variable_mirror_stmt`(DB = 레포) · ⑦ `0007_merge_vocab_and_category`(DB = 레포) · ⑧ 테이블 34건 · ⑨ `colab_app`·`colab_owner` NOSUPERUSER·NOBYPASSRLS · ⑪ 출처 `imds` · 만료 360분 · ⑬ `/` 200 html · `/api/v1/me` 401 JSON · `/previews/*` 403 · ⑭ 최신 **0.0시간 전 · 객체 27건** · ⑮ `ccf76ecf0bb5 ∈ main (origin/main=ccf76ecf0bb5 · 반입 시 조상 확인됨)`.

## 4. 태그 · push 완료(2026-09-08 · Ted 「push」 판정 뒤 오케스트레이터 집행 · 원격 `dev-20260908-1` = ccf76ec 대조 일치)

- 태그 `dev-20260908-1` → `ccf76ecf0bb5` · **로컬만.** `tag-release.sh` 는 EC2 를 읽지 않고 push 도 하지 않는다.
- `tag-release.sh` 첫 실전 · 동명 태그 충돌 0. 원격 반영 = `git push origin dev-20260908-1` 한 줄 — **Ted 가 15/15 를 본 뒤 오케스트레이터가 집행**(비가역 원격 행위).

## 5. 첫 실전 관측 (창 9 에는 없던 것)

- **반입 조상 게이트(`ship.sh`)** — 첫 실사용 · exit 0 · 거절 0회 · `COLAB_SHIP_ALLOW_NONMAIN` 미사용.
- **`MAIN_SHA` 파일** — EC2 `/opt/colab-v2/MAIN_SHA` 첫 생성 · 형식 `main=… candidate=… ancestor=yes` 그대로.
- **원장 `브랜치=` 필드** — `2026-09-08T19:58:06` 행이 첫 기록(`브랜치=main`). 직전 행 `09:19:43`(창 9)에는 그 필드가 없다.
- **`deploy_doctor` ⑮** — 첫 실사용이자 첫 ✓. 완료 조건 계수가 14 → **15** 로 바뀐 뒤 첫 판정.

## 6. 세지 못한 것

- **마이그레이션 NOTICE 축자 미수집** — 이번 회차는 변화 0 이라 ⑧ 무동작이 정상이고, 창 9 후속(`db/ai/alembic.ini` 로거 설정)은 그대로 열려 있다.
- **`up.sh` 가 컨테이너 3개를 재생성하지 않았다** — `core_api`·`pipeline_worker`·`ai_service` 는 `Running` 유지 · `viz_render` 만 `Recreated`. 원인 = 이미지 동일(코드 변화가 배포 스크립트·doctor·문서뿐). ⚠ 「재생성 0」이 「반영 불필요」임을 **이번 로그가 직접 증명하지 않는다** — 실행 컨테이너 이미지 ID 대조를 이번에 하지 않았다(창 9 는 했다).
- **온톨로지 옛 기준 대응 불가** — 계획 §1 S7 은 「사전 22」를 기대했으나 실측 계수는 다섯 표(49·19·13·18·4)로 갈리고 22 에 대응하는 단일 계수가 없다. 22 의 출처(`RESTART.md`)를 이번에 재확인하지 않았다. staging 41초의 내역 중 이미지 빌드 몫도 미분리다.

## 7. 후속

1. **태그 push** — `git push origin dev-20260908-1` · Ted 확인 뒤.
2. **CI `schema-gates` 배선** — 대장 `X-8` 개설. `schema-gates` 잡이 일회용 postgres ＋ `alembic upgrade head` ＋ 적용 DB URL export 스텝을 갖고 있지 않아 `schema-diff` 가 red(준비 · exit 78)로 난다. **2026-09-01 이래 전건 failure** 이고 R-D 기인 아니다(`git diff --stat 18c0228..ccf76ec -- db contracts` 빈 결과). 근거 = `dev-package/reports/R-D/ci-schema-gates-20260908.md`.
3. **`ccf76ec` CI 상태 3건이 걸리는 자리** — ⑴ `schema-gates` = CI 잡에만 있고 로컬 게이트·Dockerfile·배포 경로에는 걸리지 않는다(항목 2) ⑵ `planning-gates` = CI 러너에 기획 폴더 부재(환경 red · 로컬 게이트에서는 green) ⑶ `harness-eval` = CI 시크릿 미발급(exit 78 · 승격 전 면제 모드). 셋 다 브랜치 보호 필수 검사 아님(`required_status_checks` 키 없음)이고 `deploy_doctor` 경로 밖.
4. **`X-7`** — 이번 `ccf76ec` 실행에서 `service-tests (core-api)` **통과**. 원인 판정(시험 결함 vs 제품 결함)이 미기록이라 open 유지.
5. **프론트 업로드 2·3단계 결함 진단 완료** — `dev-package/reports/R-D/upload-step3-overlap-20260908.md`(77행 · 결함 6건 · 차단 2) · Ted 판정 「핫픽스 4 ＋ R-E 2」 → 레인 `hotfix-upload`(`integration/hotfix-upload` · X-9) 진행 · 글자 크기 29건·기간 표시 위치는 R-E(X-10 예정).
