# DR-4 — dev 재생성 1회차 실행 기록 (2026-09-14 09:51 ~ 10:05 KST · `WU-C4`)

- 성격 = **실행 기록**. 결과 = **배포 green · 재생성은 `preflight` 에서 정지(바꾸는 단계 0건 실행)**. 판정 입력은 계수·축자이고 해석은 §7 에만 둔다.
- 대상 = dev · `origin/main` `b955bece7384`(full `b955bece738496416cb8a4aa3a4e9054128ae79f`) · 작업 사본 브랜치 `lane/wu-c4-run`.
- 승인 근거(오케스트레이터 전달) = Ted 「main 병합 · 무인 실행 · 계정 재생성」 · advisor 게이트 ③ GO(순서·선행 단계 조건부).
- 경계 준수 = dev 한정 · `_ops/` 무접촉 · staging·prod 무접촉 · `purge_datasets.py` 0회 · `COLAB_DEPLOY_MANAGED` 수동 설정 0회 · 파괴 단계 재시도 0회 · **`reset` 이후 단계 실행 0회**.
- 비밀값·접속 문자열·비밀번호·DB 호스트 = 이 문서에 없음. 절대경로 없음.

---

## 1. 실행 창 (KST)

| 구간 | 시각 | 소요 | 결과 |
|---|---|---|---|
| 0 · 자격·접속 확인 | 09:51 | — | `aws sts` 신원 = `colab-platform-s3-uploader-dev` · ssh `ok` |
| 1-a · EC2 디스크 정리 | 09:56 ~ 09:57 | 약 1분 | §2 |
| 1-b · dev 배포(release executor) | 09:58:03 ~ 09:59:45 | **1분 42초** | §3 · `deployment: verified` · exit 0 |
| 2 · 재생성 dry-run | 09:59 | 1초 | exit 0 · DRY 줄 38 |
| 2 · 재생성 `--preflight-only`(실모드) | 10:00:23 ~ 10:00:26 | 3초 | **exit 1 · 10항목 중 9 ✓ · `secrets` ✗** — §5 |
| 2 · 원인 분리 재측정(`--preflight-only` · 원격 기본 자리) | 10:01:41 | 3초 | exit 1 · `secrets` ✗ 8건(§5 ⑵) |
| 3~5 · `reset` 이후 · 계정 · UI 실사 | — | — | **미실행**(정지 규칙 · 지시 §2 「red 면 STOP」) |

- 사람 입력 = **0회**(대화형 프롬프트 발생 0 · BatchMode ssh · 실행기 무인 완주).

## 2. EC2 디스크 정리 (advisor must-do 6)

| 시점 | `df -h /` | `docker system df` Images |
|---|---|---|
| 정리 전 | `20G 16G 4.8G 77%` | 76 개 · 4.591GB · 회수 가능 4.581GB(99%) |
| 정리 후 | `20G 13G 7.6G 62%` | 10 개 · 1.483GB · 회수 가능 1.055GB(71%) |
| 배포 후(`up.sh` 뒤) | `20G 14G 6.6G 68%` | — |

- 삭제 방식 = **고정 목록 파일**(122 태그 · `dev-package/reports/dev-reseed-runs/pre/ec2-images-to-remove.txt` · 무시 대상) → `docker rmi`(`-f` 없음 · prune 0회). `Untagged/Deleted` 줄 353.
- 삭제한 태그 = `colab-v2/{core-api,pipeline-worker,viz-render,ai-service,migrator}` 의 `dev-<sha>` 24 sha(`0045f2233a04` `09e2b9b2db1a` `0a4aecb58e68` `20b3715d1db0` `2264bc54edc2` `30f5adf67747` `321bdaef1201` `42cffb328b1e` `42e44c8990a3` `47cce303f0ef` `6ff0eecd2cba` `71ee15757737` `7fe92b6bf7fb` `81784897ec74` `83d3eb52cb1f` `89a28d9c0fd5` `9e3ff19f6d27` `a3389bd89cbf` `bbb9e0fa6259` `ccf76ecf0bb5` `d56428d6945d` `ea036c50c921` `f8ac7ee07ff6` `fc45a9aa7c64`) ＋ `stage3-dual-20260912-arm64`(core-api·migrator 2건). 보존 = `:dev` 5 ＋ `:dev-e9ca26b15bd1` 5.
- 삭제한 파일 = `/tmp/stage12-doctor-*.tar.gz` 3 ＋ `/tmp/colab-operator-source-*.tar.gz` 1 = 4 파일 33,460,039 B. 삭제 뒤 잔존 0.
- 손대지 않은 것 = `/opt/colab-v2/images/*.tar`(약 1.5GB · 되돌리기 원본) · `/tmp/colab-repo-sync.tgz`(09-14 1회차 잔존물).
- 정리 전후 컨테이너 4종 `:dev-e9ca26b15bd1` healthy 유지.

## 3. dev 배포 — release executor (`scripts/deploy_release.py run --plan …`)

- 계획 = `dist/release-c4-run-dv/release.json`(gitignore · 비밀값 0) · id **`c4-run-dev-b955bece7384`** · `--check` exit 0 「배포 계획 확인 완료 — 실행·전송 없음」.
- 구조 = 09-13 계획 `dr2-dev-81784897ec74-3` ＋ 09-14 1회차(`dev-package/reports/lth-review-260913/deploy-dev-20260914-1.md` §3) 차이 3건 그대로. 고정 입력 8건 sha256 = 09-13 계획과 전건 동일(실측). 검증 단계 1건 추가 = `reset_dev_environment.py` md5 대조(advisor must-do 7).
- 자격 = 실행 셸 한정 `aws configure export-credentials --profile colab-dev --format env`(파일 기록 0).
- 되돌리기 사본 = 웹 버킷 배포 전 **150 객체 · 65,262,068 B** → `dist/web-backup-e9ca26b1/`(150 파일 · gitignore).
- `deploy_doctor` **1회** · 요약줄 축자 **`항목 15 — ✓ 15 · ✗ 0 · ─ 0`**.

| # | 단계 | exit | 요지 |
|---|---|---|---|
| d0 | `infra/dev/build.sh` | 0 | `arm64 ✓` 5건 · tar 285M · 태그 `dev-b955bece7384` |
| d1 | df 정지선(ship 전 · 2.0GiB) | 0 | 8,140,341,248 B |
| d2 | `infra/dev/ship.sh` | 0 | 「ops source green — CURRENT_SHA b955bece7384 · manifest 전건 일치」 · 「실었다: dev-b955bece7384」 |
| d3 | df 정지선(load 후 · 1.5GiB) | 0 | 6,947,090,432 B |
| d4 | `dev.env` `COLAB_IMAGE_TAG=dev-b955bece7384` | 0 | 사전 사본 `dev.env.bak-before-b955bece7384` |
| d5 | `backup.sh && tail` | 0 | 「2026-09-14T00:58:56Z backup[dev] GREEN — 두 데이터베이스 모두 올렸다」 |
| d6 | `up.sh` | 0 | `Running upgrade` 0건 · 「up: ok (sha b955bece7384)」 |
| d7 | df 기록(up 후) | 0 | `6.6G 68%` |
| d8 | `/opt/colab-repo` 동기화(`git archive b955bece…`) | 0 | — |
| d9 · d10 | `npm ci` · `npm run build` | 0 | — |
| d11 | `ops/deploy_web.py` | 0 | 「올렸다: 96 파일 · 6638475 B → s3://colab-platform-web-dev (index.html 마지막)」 |
| v0 | `deploy_doctor.py` md5 | 0 | local = ec2 = `a2d5651512ff786f0a71eeb36e51ed9c` |
| v1 | `deploy_doctor`(원격 프로브 1회) | 0 | **`항목 15 — ✓ 15 · ✗ 0 · ─ 0`** |
| v2 | `MAIN_SHA` | 0 | `main=b955bece7384 candidate=b955bece7384 ancestor=yes` |
| v3 | `CURRENT_SHA` | 0 | `b955bece7384` |
| v4 · v5 | `/` · `/api/v1/me` | 0 | `root=200` · `api/v1/me=401` |
| v6 | `index.html` md5 | 0 | local = public = `7371a284fcc2be10cd3dd20fb88ba525` |
| v7 | 컨테이너 태그 | 0 | 4종 `:dev-b955bece7384` healthy · `tagged_units=4` |
| v8 | `reset_dev_environment.py` md5 | 0 | local(`git show b955bece:…`) = ec2 = `b6f28f1714b07bf731851a6408236618` |

- 배포 기록 = git common dir `deploy-releases/c4-run-dev-b955bece7384/`(`state.json` `deployment: verified` · `notification: queued` · `verified_at` 2026-09-14T00:59:45Z). 로그에 계정 id·DB 호스트가 찍혀 있어 레포로 옮기지 않는다.
- 태그 = **`dev-20260914-2`** 로컬 생성(`b955bece` 가리킴) · **미push**(원격 push 는 오케스트레이터 몫 · `CLAUDE.md §10`).
- 되돌리기 = `dev.env` 의 `COLAB_IMAGE_TAG=dev-e9ca26b15bd1`(또는 사본 복원) → `up.sh`. 마이그레이션 신규 0건. 프런트 = `dist/web-backup-e9ca26b1/`(실행기 계획 안에서 재업로드).

## 4. 재생성 선행 단계 (비파괴)

- 러너 자격 파일 = `install -m 600` 으로 작업 자리에 복사 → 정지 확정 뒤 `shred -u` 로 삭제(잔존 0).
- 계정 변수 = `RESEED_ACCOUNT_EMAIL=pi@hymets.invalid` · `RESEED_ACCOUNT_NAME=전창현` · **`RESEED_ACCOUNT_ID=000000000000000000HYMETSP1`** · `RESEED_ACCOUNT_ROLE=교수`.
  - id 근거 = `infra/staging/provision-lab.sql`(prelude ①)이 **같은 이메일로 이 id 의 `d1_account`＋`d2_member_role`(교수)을 이미 심는다**. `DR-2-run-2026-09-13.md` 「런북 5절 ③·④ 가 요구하는 `account_id` 는 ① 이 심은 값을 그대로 썼다」. `d1_account` 는 `UNIQUE (lab_id, email)`(`db/platform/schema.sql`) 라 **새 ULID 로 ② 를 내면 유일성 위반으로 prelude 가 죽는다**. 새로 만든 ULID `01M2EPQWBPDQZTE24DGY719KQ2` 는 **미사용**.
  - 부수 효과(다음 회차 확인) = ② `provision-account.sql` 이 같은 id 로 돌면 `d1_account`·`d2_member_role` 은 `ON CONFLICT DO NOTHING` 이고 **`d2_permission_switch` 4행이 새로 생긴다**(09-13 은 ② 를 건너뛰어 0행). 교수는 스위치가 항상 켜진 것으로 판정되므로 동작 차이는 `[미확인]`.
- `--dry-run --from reset --target-ref b955bece` = exit 0.
- `--accounts-file` 미전달(지시 §2 · `stage_seed` 가 `--accounts-password-file` 을 넘기지 않아 러너가 표준입력을 기다린다).

## 5. 정지 지점 — `preflight` `secrets` ✗ (원인 2건 · 둘 다 실물 결함 아님)

- 1차(`RUN_ID` `20260914T010023Z` · 지시대로 `dev-operator.env` source) = 9 ✓ / 1 ✗ — `secrets — 부재·모드 어긋남 9 건`.
- 실물 = EC2 `/etc/colab/` 9 파일 **전건 존재 · 전건 0600**(`sudo stat -c '%n %a'` 직접 실측).

### ⑴ 환경변수 이름 충돌 — `COLAB_DEV_SECRETS_DIR`

- `~/.config/colab-platform/dev-operator.env` 의 `COLAB_DEV_SECRETS_DIR` = **개발 기계의 로컬 비밀 폴더**. `reseed.sh` 의 `SECRETS_DIR="${COLAB_DEV_SECRETS_DIR:-/etc/colab}"` 는 **EC2 위 경로**(`preflight.sh` `pf_secrets` 의 원격 `stat` · `stages.sh` `reset_docker_cmd`·`stage_prelude` 의 `docker run -v $SECRETS_DIR/…:ro`). 같은 이름이 두 뜻으로 쓰인다.
- 영향 = 이 값이 실린 채 `reset`·`prelude` 가 돌면 EC2 에 없는 호스트 경로를 마운트한다 → **preflight 가 실제 결함을 잡은 것이 맞다**(정지 정당).
- 확인 = `COLAB_DEV_SECRETS_DIR` 를 비우고 재측정(`RUN_ID` `20260914T010141Z`) → `master.url` 통과 · 나머지 8건 부재로 **여전히 ✗**(⑵).

### ⑵ `pf_secrets` 의 `printf` 짝짓기 결함 (`dev-package/tools/dev-reseed/preflight.sh` `pf_secrets`)

- 문장 = `sudo stat -c '%n %a' $(printf "'%s/%s' " "$dir" "${SECRET_FILE_NAMES[@]}")`. 서식 `%s/%s` 에 인자 10개(dir ＋ 이름 9)가 **둘씩** 묶여 5개 경로가 된다 — 로컬 재현 축자 `'/etc/colab/master.url' 'platform-owner-db.url/ai-owner-db.url' 'core-database.url/pipeline-db.url' 'ai-db.url/account-admin-database.url' 'subjects.json/credentials.json'`.
- 결과 = 이름 9건 중 `master.url` 만 `$dir/<이름> ` 로 잡히고 8건은 구조적으로 「부재」. **이 항목은 지금까지 green 이 된 적이 없다** — `tests/preflight-red.sh` 는 ssh 부재 시 red 만 검사(green 픽스처 없음).
- 고침 후보(코드 변경 · 이 레인 밖) = `printf "'%s' " "${SECRET_FILE_NAMES[@]/#/$dir/}"` ＋ green 픽스처(9건 0600 목록 → ✓) ＋ red 픽스처(1건 644 → ✗) 를 `tests/` 에 추가.
- 지시 §2 「red 면 STOP · 약화 금지」에 따라 **`reset` 이후를 실행하지 않았다.** `--from` 재개 0회.

## 6. 현재 dev 상태 (정지 시점)

- 실행 sha `b955bece7384` · 컨테이너 4종 healthy · `deploy_doctor` 15/15 · 마이그레이션 변화 0.
- 데이터 = **09-13 투입분 그대로**(초기화 0 · 삭제 0 · S3 변경 0 · 계정 변화 0). 웹 번들만 `b955bece` 판으로 갱신.
- 잔존물 = EC2 `/opt/colab-v2/images/colab-v2-dev-b955bece7384.tar`(285M) · `dev.env.bak-before-b955bece7384` · 작업 사본 `dist/`(계획·이미지 tar·웹 백업 · gitignore) · `dev-package/reports/dev-reseed-runs/pre/`(정리 목록·dry-run·preflight 로그 2벌 · gitignore).

## 7. 다음 회차 진입조건

1. `preflight.sh` `pf_secrets` 짝짓기 고침 ＋ 픽스처 2건(§5 ⑵) — 별도 레인.
2. `COLAB_DEV_SECRETS_DIR` 뜻 하나로 통일 — `reseed.sh` 쪽 이름을 바꾸거나(예 `COLAB_RESEED_REMOTE_SECRETS_DIR`) 지시문에서 `dev-operator.env` 의 값을 비우고 실행(`env -u COLAB_DEV_SECRETS_DIR`). 결정은 오케스트레이터.
3. 위 둘 뒤 `--preflight-only` **10/10 ✓** 를 한 번의 실행으로 확인 → `--from reset --target-ref b955bece`(dev 는 이미 그 sha).
4. `RESEED_ACCOUNT_ID=000000000000000000HYMETSP1` 로 고정(§4) · `d2_permission_switch` 4행 부수 효과 확인.
5. 계정 단계는 별도 명령(`runner.py --phase accounts --accounts-file … --accounts-password-file …`) — 지시 §4 그대로.
