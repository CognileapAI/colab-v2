# `V-2` 값 조회 — 마감 실측 (2026-09-03 · 워크트리 `lane-v2close`)

> 이 회차 범위 = **`〈294〉` 가 남긴 완료 정의 둘의 실측**뿐이다 — ⑻ 응답 눈금 · staging 배포 green.
> **코드·계약·생성물·DB 무접촉.** staging 접촉은 **값 조회 op 의 `POST` 하나**뿐이고 그것은 읽기다.
> 등재 = `PLAN-SoT §9 〈304〉`.

## 1. 배포 증명 — `〈294〉` 코드가 서빙 릴리스에 실려 있다

원장 `~/colab-v2-releases/release-ledger.tsv` 꼬리 행 (축자) —

```
2026-09-03T12:43:56+0900	deploy	d9e89a3a074b	d9e89a3a074b	green	별칭재부착GREEN digest이력=6종 배포전백업GREEN 워킹트리변경=0
```

서빙 릴리스 전체 SHA = `d9e89a3a074ba859a27ffc4270579bc180317c81`.

```
$ git merge-base --is-ancestor d570f2d d9e89a3a074ba859a27ffc4270579bc180317c81 ; echo $?
0
```

- `d570f2d` = `V-2` 15차 병합 커밋(「값 조회 15차 병합 — … 〈294〉」). **종료 코드 0 = 조상이다** ⟹ `〈294〉` 의 계약·서버·화면이 서빙 중인 릴리스에 들어 있다.
- 창 종료 실측(`WINDOW-20260903-F2 §7.1`)과 일치 — 서빙 릴리스 `d9e89a3a074b` · 컨테이너 8개 healthy · `verify-deploy` GREEN 15건 · digest 10건 전건 일치.

## 2. 응답 눈금 실측 (완료 정의 ⑻)

**계측 지점 = 클라이언트 벽시계이고 Cloudflare ＋ nginx 가 포함된 값이다.** 서버 단독 시간은 재지 않았다 — 응답 헤더에 `Server-Timing: cfCacheStatus;desc="DYNAMIC"` 뿐이라 서버 처리 구간을 가르는 재료가 없다 ⟹ **서버 측 p95 = `[미확인]`**. 푸는 법 = 서버가 자기 처리 시간을 `Server-Timing` 에 실어 보내면 그때 갈린다.

- 엣지 = `https://www.colab-hydro.com` · 베어러 = `~/.colab-v2-staging-subjects.tokens.json`(경로만 · 값 미출력) · `User-Agent` 부착.
- 부른 op = `POST /api/v1/datasets/{datasetId}/value-lookup` (본문 `point{lat,lon}`) **하나뿐**.

### 2.1 회차 정본 값 (n = 95)

| 데이터셋 | n | p50 | p95 | max | 오류 |
|---|---:|---:|---:|---:|---:|
| `01M0YWDXN7CA13KB7MQC9PGEW5` GK-2A NDVI 100 m 월평균 | 30 | 192 ms | 263 ms | 270 ms | 0 |
| `01M1JM3JTQSSTKR9A2QQ67QN81` U-Net 예측 (NumPy 적재분) | 30 | 216 ms | 255 ms | 280 ms | 0 |
| `01M19RENXAANZ2HDWM4Z46C37Y` PV-1 실측 업로드 | 30 | 184 ms | 249 ms | 259 ms | 0 |
| **값 있는 3종 합산** | **90** | **193 ms** | **261 ms** | **280 ms** | **0** |
| `01M0Y1VQ09TMHAC78ESDRTMAZD` DEM 100 m (자리 산출물 없음) | 5 | 188 ms | 226 ms | 230 ms | 0 |
| **전체** | **95** | — | **260 ms** | **280 ms** | **0** |

- 표본 구성 = 데이터셋마다 **경계 안 10 점 × 3 반복**. 점은 세 데이터셋 모두에서 `available: true` 인 것만 골랐다(사전 확인 실측).
- 무타일 데이터셋 5회는 전건 **200 ＋ `available:false` ＋ `unavailableReason: "자리에 산출물이 없다"`** — 값을 지어내지 않는다(완료 정의 ⑸⑹ 의 모양이 실물에서 유지된다).
- **합격선 대조 — p95 261 ms ≤ 1,000 ms ✅ · max 280 ms ≤ 5,000 ms ✅.** 데이터셋 단위로 갈라 봐도 셋 다 p95 ≤ 263 ms 이다.

### 2.2 ⚠ 유휴 후 첫 접촉의 꼬리 — 눈금을 한 번 넘었다. 감추지 않는다

이 날 **첫 회 실행**(같은 모양 n = 95 · 이후 실행 넷은 전부 이 뒤에 붙여 돌렸고 **유휴 간격은 재지 않았다 `[미확인]`**)의 값 —

| 데이터셋 | p50 | p95 | max |
|---|---:|---:|---:|
| GK-2A NDVI 100 m 월평균 | 195 ms | **1,164 ms** | 2,052 ms |
| U-Net 예측 | 209 ms | 279 ms | 285 ms |
| PV-1 실측 업로드 | 209 ms | 275 ms | 2,236 ms |
| DEM (자리 없음) | 257 ms | 2,111 ms | 2,448 ms |

- **데이터셋 둘의 p95 가 1 초 선을 넘었다** — 값 있는 쪽 1,164 ms · 「없다」 쪽 2,111 ms. 뒤이은 **네 차례 전량 실행에서는 재현되지 않았다**(같은 데이터셋 p95 251 · 263 · 253 ms).
- **상한 5 초는 어느 실행에서도 넘지 않았다** — 다섯 실행 통틀어 관측된 최대가 **2,448 ms** 다.
- ⛔ **이 실행의 합산 p95 는 되살릴 수 없다 — 표본 원값을 남기지 않고 요약값만 적었다.** 데이터셋별 p95·max 로부터 1 초 초과 표본이 **95 중 약 4~5 건**으로 추정되고, 합산 p95 의 경계(4.75 건)와 **겹친다** ⟹ **추정으로 판정하지 않는다.** 푸는 법 = 표본 원값을 남긴 채 유휴 뒤 첫 실행을 다시 잰다.
- 원인도 **귀속하지 못했다** — 자리 파일 최초 열기인지 · OS 페이지 캐시인지 · 엣지 경로 확립인지가 `[미확인]`. 「냉시동」은 **관측에 붙인 이름이지 밝혀진 원인이 아니다.** 서버 구간 시간이 없어 가를 재료가 없다.

## 3. 값 대조 — 답하는 수가 그 칸의 실측값인가 (완료 정의 ⑶⑷)

대상 = NumPy 적재분 `01M1JM3JTQSSTKR9A2QQ67QN81`. 원천 = `03 Reference-Data/01.level-data/02.vegetation/02.vegetation/Lv.2/Prediction_20230501.npy` ＋ 동봉 격자 `#metadata/LAT_crop.npy`·`LON_crop.npy`.

⭑ **호스트에 numpy 가 없어 `.npy` 를 순수 파이썬으로 직접 읽었다**(헤더 파싱 ＋ `array` 모듈). **원천 파일은 읽기만 했다.** `LAT_crop`·`LON_crop` 는 `fortran_order: True`, 본체는 `False` — 메모리 순서가 달라 색인을 갈라 계산했다.

| 요청 좌표 | staging 응답 | 응답이 밝힌 칸 | 원천 같은 칸의 값 | 차 |
|---|---:|---|---:|---:|
| (36.45, 126.95) | 0.4624658823 | `row 908 · col 274` | `src[908,274]` = 0.4624658823 | 1.3e-12 |
| (36.75, 127.40) | 0.3152135611 | `row 608 · col 724` | `src[608,724]` = 0.3152135611 | 4.2e-11 |
| (37.05, 127.85) | 0.4859274030 | `row 307 · col 1174` | `src[307,1174]` = 0.4859274030 | **0** |

- **3/3 이 원천 격자의 실제 한 칸 값과 같다** — 남은 차 1e-11 대는 `float32` 원천을 `float64` 로 견준 표현 오차이고, 셋째 점은 **완전히 같다**. **블록 평균이 아니다**(완료 정의 ⑶ · `〈294〉` 가 red 로 못 박아 둔 그 성질이 실물에서도 산다).
- ⚠ **셋째 점은 「가장 가까운 칸」의 선택이 내 계산과 한 행 갈린다** — 내가 원천 격자에서 고른 최근접은 `row 308`(0.4969887733)이고 서버는 `row 307` 을 골랐다. 요청 위도가 두 행 사이(37.049594 / 37.050595)에 앉아 있고, **서버가 보는 격자는 원천이 아니라 자리에 구운 산출물의 격자**라 반 칸까지 자리가 옮겨질 수 있다 — `〈254〉`·`〈294〉` 가 좌표를 따로 입히는 격자(nc·hdf·npy)에 대해 **미리 적어 둔 한계 그대로**다.
- **응답이 그 한계를 스스로 말한다** — 세 건 전부 `exactness: "가장 가까운 칸"` 이다(완료 정의 ⑷). `sizeDegrees` 0.000999231 로 답하는 단위가 **한 칸**임도 같은 응답에 실려 온다.

## 4. ⚠ 지시받은 전제가 실물과 어긋났다 — 자리에 산출물이 있는 데이터셋은 3건이다

이 회차는 「`E-2` 의 5종(tif·nc·gz·hdf·npy)에 타일이 있다」는 전제로 착수했으나 **실물이 다르다.** staging 카탈로그 **14건 전수**를 값 조회 1회씩으로 훑은 결과 —

| 구분 | 건수 | 대상 |
|---|---:|---|
| 자리에 산출물이 있다 (`available:true`) | **3** | `01M0YWDXN7CA13KB7MQC9PGEW5` · `01M1JM3JTQSSTKR9A2QQ67QN81`(NumPy) · `01M19RENXAANZ2HDWM4Z46C37Y` |
| 없다 (`200 ＋ available:false ＋ 「자리에 산출물이 없다」`) | **11** | `E-2` 의 **tif·nc·gz·hdf 넷을 포함** |

⭑ **축자로 적는다 — 「포맷 5종을 staging 에서 왕복했다」는 이 회차에 대해 참이 아니다.** 눈금 표본은 **데이터셋 3건**이고, `E-2` 가 엣지 왕복시킨 tif·nc·gz·hdf 넷은 값 조회에서 전부 「자리에 산출물이 없다」로 답한다. ⟹ **`WINDOW-20260903-E2.md` 를 근거로 「그 5종에 타일이 있다」고 읽으면 안 된다** — `E-2` 가 잰 것은 **렌더 한 장의 엣지 왕복**이지 **자리에 구운 COG 의 유무**가 아니다. 둘은 다른 자리다.

⛔ **원인은 이 회차가 귀속하지 못했다 — `[미확인]`.** 처음에는 `〈254〉` 실측 ⑥(「`NumPy`·`GRIB` 는 지원 목록 밖 · 이미 COG 인 GeoTIFF 는 변환을 건너뛴다」)으로 설명하려 했으나 **그 문면은 낡았다** — `d5/pipeline.py` `COG_BUILDERS` 실측이 **다섯**(`GeoTIFF`·`Binary`·`NetCDF`·`HDF4`·**`NumPy`**)이고, `NumPy` 는 `#58`(`44bef51`)이 파서를 넷에서 여섯으로 채우면서 들어왔다. 즉 **`〈254〉`-⑥ 은 `#58` 이후 시점에서 부분적으로 뒤집혔다.**
- 남는 설명 후보(어느 것도 이 회차가 증명하지 않았다) — ⓐ 자리 산출물이 있는 3건이 **최근 적재분**이고(`npy` 는 `E-2` 가 이 날 적재), 나머지 11건은 **자리에 놓는 경로(`previews_root`)가 서기 전에 적재**돼 자리에 파일이 없다 · ⓑ 이미 COG 인 GeoTIFF 의 변환 건너뜀(tif 에만 해당).
- 푸는 법 = staging 의 자리(`previews` 볼륨)를 읽기 전용으로 세어 대상별 `tile-` 벌의 유무를 직접 대조하고, 각 데이터셋의 적재 시각과 견준다. **이 회차는 그 자리를 열지 않았다.**

## 5. 이 회차가 재지 않은 것

| # | 항목 | 무엇을 하면 풀리나 |
|---|---|---|
| 1 | **서버 측 단독 p95** `[미확인]` | 응답에 서버 처리 구간을 실은 `Server-Timing` 을 세우고 다시 잰다 |
| 2 | **냉시동 p95 의 원인** `[미확인]` | 유휴 뒤 첫 호출만 모아 따로 재고, 서버 구간 시간과 대조한다 |
| 3 | **포맷을 가로지르는 눈금** — 표본 3건이 5종을 덮지 않는다 | 굽는 경로가 없는 형식(이미 COG 인 GeoTIFF · NumPy · GRIB)의 자리 산출물 문제가 풀린 뒤 다시 잰다 |
| 4 | 동시 부하 아래의 눈금 | 이 회차는 **직렬 1 요청**만 보냈다. 동시 호출 폭을 세워 다시 잰다 |
| 5 | 완료 정의 ⑴⑵⑸⑹⑺ 의 **staging 실물 재확인** | `〈294〉` 가 시험으로 닫은 다섯이다. 이 회차는 ⑸⑹ 의 모양만 응답에서 확인했다(§2.1) |
| 6 | **화면(`ValueLookupPanel`)의 staging 실동작** | 이 회차는 **API 만 불렀다.** `〈294〉` ㉸ⓓ 가 남긴 축이 그대로 열려 있다 — 브라우저로 상세 화면을 열어 한 점을 눌러 본다 |
| 7 | **tif 계열의 「완전히 같다」(⑶ 전단)를 staging 에서** | 오늘 원천과 대조한 것은 **NumPy 1종**(좌표를 따로 입힌 격자)뿐이다. tif 계열의 비트 동일 주장은 `〈294〉` 의 단위 시험에 서 있지 staging 실물에 서 있지 않다 |
| 8 | **유휴 후 첫 실행의 합산 p95** | §2.2 — 표본 원값을 남긴 채 다시 잰다 |
| 9 | **자리 산출물이 11건에서 없는 이유** | §4 — staging `previews` 자리를 읽기 전용으로 세어 적재 시각과 대조한다 |

## 6. 이 회차가 건드리지 않은 것

- **staging 쓰기 0건** — 부른 op 은 값 조회 `POST` 하나이고 **읽기다.** 원장 행 0건 · 렌더 작업 생성 0건(경로 실측 = `core-api routes/preview.py` 는 접근 판정 뒤 중계만 하고, `viz-render value_lookup.py` 는 창 1×1 을 읽는다 · `Port 0 · 재굽기 0`).
- 컨테이너 조작 0건 · 파괴 플래그 0건 · 운영 DB 쓰기 0건 · 자격증명 값 출력 0건(경로만).
- **코드·계약·생성물·DB·인프라 무접촉** · 배포 0 · push 0 · 병합 0.
- `gates/run.sh all` **미실행** — 다른 레인(`lane-r17`)이 그 자리를 쥐고 있다. 이 회차가 돌린 것은 `work-item-consistency -j 1` 하나다.

## 7. 판정 — `V-2` 를 닫지 않는다 (`in_progress` 유지)

**충족된 것 하나 = staging 배포 green (§1).** 원장 행 · 전체 SHA · `merge-base` 종료 코드 0 으로 증명됐고 다툴 자리가 없다.

**⑻ 는 닫지 않는다 — 값이 나빠서가 아니라 「무엇을 재는 눈금인가」가 정해져 있지 않아서다.**

| 읽는 법 | 이 회차 실측 | 판정 |
|---|---|---|
| 따뜻한 상태 · 합산 | p95 251~261 ms · max 280 ms | ✅ 통과 (실행 4회 전건) |
| 따뜻한 상태 · 데이터셋별 | 셋 다 p95 ≤ 263 ms | ✅ 통과 |
| 유휴 후 첫 접촉 · 데이터셋별 | 1,164 ms · 2,111 ms | ⛔ **1 초 초과** |
| 유휴 후 첫 접촉 · 합산 | **되살릴 수 없다** (§2.2) | `[미확인]` |
| 상한 5 초 (「넘으면 실패」) | 다섯 실행 통틀어 max 2,448 ms | ✅ 통과 |

- **완료 정의 ⑻ 의 문면은 「응답 p95 1초 · 상한 5초」뿐이다** — **데이터셋별인지 합산인지 · 따뜻한 상태인지 유휴 후 첫 접촉인지를 적지 않았다.** 정본 `Policy_데이터셋_상세 v2.8 §8` 도 같다.
- **그 읽는 법을 이 레인이 고르지 않는다.** 고르는 쪽에 따라 판정이 뒤집히고(위 표), 뒤집히는 판정을 집행 레인이 자기 편한 쪽으로 정하면 그것이 이 레포가 막으려는 모양이다.

⟹ **Ted 판정 대기 1건 — ⑻ 의 눈금을 어느 표본으로 읽는가.**
- ⓐ **따뜻한 상태 합산으로 읽는다** ⟹ ⑻ 충족 · `V-2` 즉시 닫힘. 근거 = 값 조회는 화면에서 **연속으로 여러 점을 누르는** 조작이라 두 번째 이후가 실사용에 가깝다.
- ⓑ **유휴 후 첫 접촉도 포함해 읽는다** ⟹ **한 번 더 재야 한다**(표본 원값 보존 · §2.2). 근거 = 이용이 뜸한 연구 플랫폼에서 **하루 첫 접촉이 사용자가 실제로 만나는 값**이다.
- **권고 = ⓑ 를 눈금으로 삼되, 지금 값으로 닫지 않고 한 회차만 더 잰다.** 되돌리는 비용 = 측정 1회(약 100 호출 · staging 쓰기 0). ⓐ 를 골라 지금 닫아도 **상한 5 초는 이미 어느 실행에서도 안 넘었다**는 사실은 그대로다.
