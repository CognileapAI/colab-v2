# DR-2 — dev 배포 실행 기록 (2026-09-13 · 중단)

- 대상 = `main` sha `81784897ec747709906717a96e27ede75a0c832b`(short `81784897ec74`) · 계획 id `dr2-dev-81784897ec74`
- 성격 = **배포 미완료 · 1단계에서 중단.** 재시도 0 · 수정 0 · 우회 0.
- 원격 상태 변경 = **0건.** EC2 접촉 0 · `scp` 0 · `ssh` 0 · S3 쓰기 0 · DB 접촉 0 · 태그 0.
- 레포 추적 파일 변경 = 0건. 초기화 도구 미실행. staging 컨테이너 무접촉.
- 비밀 취급 = 접속 문자열·키·URL 미기재. 운영자 env 는 이름만 참조(`COLAB_DEV_SSH`·`COLAB_DEV_KEY_FILE`·`AWS_PROFILE`).

---

## 1. 전제 확인 — 통과

| # | 항목 | 실측 | 판정 |
|---|---|---|---|
| ㄱ | 작업 사본 HEAD | `81784897ec747709906717a96e27ede75a0c832b` | 일치 |
| ㄴ | `git fetch origin && git rev-parse origin/main` | 같은 값 | 일치 |
| ㄷ | 작업 트리 | `git status --porcelain` 출력 0줄 | 청결 |
| ㄹ | 운영자 env 파일 | 레포 밖 1건 존재 · 키 6개(`export` 형식) | 적재 성공 |

## 2. 단계별 실행 결과

| # | 명령(값 은닉) | 종료코드 | 요지 |
|---|---|---|---|
| 1 | `python3 scripts/deploy_release.py run --plan <계획> --check` | **0** | 축자 「배포 계획 확인 완료 — 실행·전송 없음」 |
| 2 | `python3 scripts/deploy_release.py run --plan <계획>` | **1** | `deploy` 0번 단계(`bash infra/dev/build.sh`) 실패 |
| 3 | `python3 scripts/deploy_release.py status dr2-dev-81784897ec74` | 0 | `dv.deploy=failed` · `dv.verify=pending` · 실패 단계 `index 0` |
| 4 | 트리 동기화 3줄(`git archive origin/main …`) | **미실행** | 2번 비영 종료로 중단 |
| 5 | `deploy_doctor`(원격 프로브 1회) | **미실행** | 같은 사유 |
| 6 | `infra/dev/tag-release.sh dev` | **미실행** | 같은 사유 |

## 3. 실패 축자

- 이미지 = `core-api` · 플랫폼 `linux/arm64` · 레이어 `[6/6]`
- 축자 3줄

  > `#10 0.177 exec /bin/sh: exec format error`
  > `#10 ERROR: process "/bin/sh -c useradd --system --create-home --uid 10001 colab" did not complete successfully: exit code: 255`
  > `ERROR: failed to build: failed to solve: process "/bin/sh -c useradd --system --create-home --uid 10001 colab" did not complete successfully: exit code: 255`

- 실행기 마지막 2줄 = 「배포 기록: `<git-common-dir>/deploy-releases/dr2-dev-81784897ec74/state.json`」 · 「배포 또는 검증 실패 — 완료 알림 없음」
- 단계 로그 자리 = `<git-common-dir>/deploy-releases/dr2-dev-81784897ec74/dv.deploy.0.log`
- 알림 아웃박스 = `release:dr2-dev-81784897ec74:dev:deploy:1` · `severity=critical` · `kind=deploy.failed` · `exit_code=1`

## 4. 원인 — 호스트 QEMU 미등록 (기정 사례)

| 항목 | 실측 |
|---|---|
| 호스트 아키텍처 | `uname -m` = `x86_64` |
| `docker buildx ls` | 기본 노드 PLATFORMS = `linux/amd64 (+3)` — `linux/arm64` 부재 |
| `/proc/sys/fs/binfmt_misc/` | 항목 = `WSLInterop`·`python3.12`·`register`·`status` — `qemu-aarch64` 부재 |
| 캐시 레이어 | `[2/6]`~`[4/6]` CACHED(이전 회차 산물) · 새로 실행되는 `[6/6]` 에서 처음 에뮬레이션 요구 |

- 대조 정본 = `infra/dev/README.md:128` — 같은 증상(`exec format error`)의 원인을 「WSL2 buildx 에 `linux/arm64` 없음 · QEMU 미등록」으로 이미 등재.
- 성격 = 배포 대상 커밋의 결함 아님. **호스트 준비 상태 결함**이고 재기동으로 `binfmt_misc` 등록이 소멸한 것과 정합.

## 5. 다음 회차 진입조건

1. 개발 기계에서 QEMU arm64 등록 1회(정본 `infra/dev/README.md:128` 의 축자 명령) — `--privileged` 컨테이너 1회 실행이므로 **Ted GO 대상**.
2. 등록 후 `docker buildx ls` 의 PLATFORMS 에 `linux/arm64` 포함 확인.
3. 같은 계획 id `dr2-dev-81784897ec74` 로 재실행할지 새 id 로 낼지 판정 필요 — 런북 §1-2 는 「같은 id 로 무조건 재배포하지 않는다」.
4. 재배포 성공 뒤에만 트리 동기화 3줄 → `deploy_doctor` 1회 → 15/15 판정 → 태그.

## 6. 근거 자리

- 실행 로그 = 작업 임시 폴더 `deploy-dr2.log`(레포 밖 · 커밋 대상 아님)
- 계획 JSON = 작업 임시 폴더 `release-dev-81784897.json`(레포 밖 · 비밀값 0)
- 런북 = `dev-package/sessions/DR-2-runbook.md` §0~§1
- 정찰 = `dev-package/reports/r-dev-reset/dev-access-recon.md`

---

# 2차 시도 (2026-09-13 · 호스트 QEMU 등록 후 · 중단)

- 대상 = 같은 `main` sha `81784897ec747709906717a96e27ede75a0c832b`(short `81784897ec74`) · 계획 id **`dr2-dev-81784897ec74-2`**(런북 §1-2 「같은 id 로 무조건 재배포하지 않는다」 적용 · 1차 id 재사용 0)
- 계획 JSON = 1차 사본에서 `id` 한 필드만 교체 · 나머지 전 필드 동일(`diff` 출력 1행) · 실행기 모듈의 `validate(plan, root)` 직접 호출 통과
- 성격 = **배포 미완료 · `deploy` 9단계 중 마지막 단계에서 중단.** 재시도 0 · 수정 0 · 우회 0 · 태그 0 · 초기화 도구 미실행 · staging 무접촉
- 레포 추적 파일 변경 = 0건
- 비밀 취급 = 접속 문자열·키·URL 미기재. 운영자 env 는 이름만 참조

## 7. 전제 재확인

| # | 항목 | 실측 |
|---|---|---|
| ㄱ | 작업 사본 HEAD ＝ `origin/main` | `81784897ec747709906717a96e27ede75a0c832b` 일치 |
| ㄴ | 작업 트리 | 추적 파일 변경 0(미추적 본 문서 1건) |
| ㄷ | QEMU arm64 등록 | `/proc/sys/fs/binfmt_misc/qemu-aarch64` 존재 |

## 8. 단계별 실행 결과

| # | 단계 | 종료코드 | 요지 |
|---|---|---|---|
| 0 | `--check` | **0** | 축자 「배포 계획 확인 완료 — 실행·전송 없음」 |
| 1 | `deploy[0]` `infra/dev/build.sh` | **0** | 1차 실패 지점 통과 — 이미지 5종 `arm64 ✓` · tar 285M · 「ops source bundle green — commit 81784897ec74」 |
| 2 | `deploy[1]` `infra/dev/ship.sh` | **0** | 축자 「ops source green — CURRENT_SHA 81784897ec74 · manifest 전건 일치」 · 「실었다: dev-81784897ec74」 |
| 3 | `deploy[2]` 이미지 태그 | **0** | `COLAB_IMAGE_TAG=dev-81784897ec74` 확인 |
| 4 | `deploy[3]` 백업 | **0** | — |
| 5 | `deploy[4]` `up.sh` | **0** | 4 단위 healthy · 축자 「up: ok (sha 81784897ec74)」 · 헬스 본문 `storageMode`·`sourceMode`·`previewSink` 전부 `s3` |
| 6 | `deploy[5]` 트리 동기화 | **0** | 계획 내장 단계(tar ＋ scp ＋ 원격 untar) |
| 7 | `deploy[6]` `npm ci` | **0** | — |
| 8 | `deploy[7]` `npm run build` | **0** | `tsc --noEmit` 통과 · 모듈 214 · 축자 「built in 1.76s」 |
| 9 | `deploy[8]` `ops/deploy_web.py` | **1** | **중단 지점** — AWS 자격증명 부재 |
| 10 | `status dr2-dev-81784897ec74-2` | 0 | `dv.deploy=failed` · `dv.verify=pending` · 실패 `index 8` |
| 11 | 손 트리 동기화 3줄 | **미실행** | 9번 비영 종료로 중단 |
| 12 | `deploy_doctor`(원격 프로브 1회) | **미실행** | 같은 사유 — `항목 15` 재측정 없음 |
| 13 | `infra/dev/tag-release.sh dev` | **미실행** | 같은 사유 |

## 9. 실패 축자

> `RuntimeError: AWS 자격증명을 찾지 못했다 — AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY 환경변수, ECS 컨테이너 자격증명, EC2 IMDSv2 를 순서대로 시도했다. 설정 방법: dev-package/S3.md §1.`

- 발생 자리 = `services/core-api/ops/deploy_web.py` → `colab_core/kernel/s3.py` `put_object` → `aws_credentials.py` `load_credentials`
- 시점 = 업로드 대상 목록 출력 뒤 **첫 `put_object`** — S3 웹 버킷 쓰기 **0건**
- 실행기 마지막 2줄 = 「배포 기록: …/deploy-releases/dr2-dev-81784897ec74-2/state.json」 · 「배포 또는 검증 실패 — 완료 알림 없음」
- 알림 아웃박스 = `release:dr2-dev-81784897ec74-2:dev:deploy:1` · `severity=critical` · `kind=deploy.failed` · `exit_code=1`

## 10. 원인 — 개발 기계 자격증명 공급자 불일치

| 항목 | 실측 |
|---|---|
| 운영자 env 의 변수 이름 | `AWS_PROFILE`·`COLAB_DEV_ENV`·`COLAB_DEV_KEY_FILE`·`COLAB_DEV_SECRETS_DIR`·`COLAB_DEV_SSH`·`COLAB_DEV_WEB_URL` |
| `load_credentials()` 공급자 순서 | `_from_env`(`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`) → `_from_ecs` → `_from_imds` |
| 대조 | `AWS_PROFILE` 을 읽는 분기가 **없다** — 자작 SigV4 클라이언트라 AWS CLI 프로파일·`~/.aws/credentials` 를 해석하지 않는다 |

- 성격 = 배포 대상 커밋의 결함 아님. **개발 기계(프런트 업로드 주체)의 자격증명 전달 경로 결함**
- 1차와 다른 지점 = 1차는 `deploy[0]`(호스트 QEMU 미등록), 2차는 `deploy[8]`(호스트 자격증명). 둘 다 호스트 준비 상태
- 범위 = EC2 쪽은 IAM 역할(IMDSv2)로 동작하므로 이 결함은 **개발 기계에서 S3 로 직접 쓰는 단계에만** 나타난다

## 11. 다음 회차 진입조건

1. 개발 기계에서 `deploy_web.py` 가 쓸 자격증명 전달 방식 판정 — `AWS_PROFILE` 해석 분기를 코드에 추가할지, 임시 자격증명을 env 두 변수로 내릴지(정본 `dev-package/S3.md §1`).
2. 새 계획 id(`…-3`)로 재실행. 1·2차 id 재사용 배제.
3. 앞 8단계는 원격에 반영 완료 — 이미지·`COLAB_IMAGE_TAG`·컨테이너·`/opt/colab-repo` 트리가 `81784897ec74` 기준. 프런트 정적 자산만 미반영.
4. 재배포 성공 뒤에만 손 트리 동기화 3줄 → `deploy_doctor` 1회 → 15/15 판정 → 태그.

## 12. 근거 자리 (2차)

- 실행 로그 = 작업 임시 폴더 `deploy-dr2-2.log`(레포 밖)
- 계획 JSON = 작업 임시 폴더 `release-dev-81784897-2.json`(레포 밖 · 비밀값 0)
- 단계 로그 = 배포 기록 폴더 `deploy-releases/dr2-dev-81784897ec74-2/dv.deploy.<0-8>.log`

---

# 3차 시도 (2026-09-13 · 자격증명 주입 후 · **완료**)

- 대상 = 같은 `main` sha `81784897ec747709906717a96e27ede75a0c832b`(short `81784897ec74`) · 계획 id **`dr2-dev-81784897ec74-3`**
- 성격 = **배포·검증 완료.** 재시도 0 · 게이트 우회 0 · 코드 수정 0 · 추적 파일 변경 0 · staging 무접촉 · 초기화 도구 미실행 · 태그 0
- 비밀 취급 = 자격증명 값·접속 문자열·키 미출력. 프로파일 이름과 호출자 ARN 접미만 기재

## 13. 이어받기(resume) 가능 여부 — 불가 판정

| 근거 | 실측 |
|---|---|
| CLI 하위명령 | `run`(`--plan`·`--check`·`--retry-notification`) · `staging` · `status` — `--from`·`resume`·`retry` 부재(`scripts/deploy_release.py` `cli()` 의 `add_parser`·`add_argument` 전건) |
| 단계 색인 재개 | `run_plan()` 축자 `phases = () if previous else ('deploy', 'verify')` — 기존 기록이 있으면 **어느 단계도 실행하지 않는다** |
| 같은 id 재실행 결과 | `deployment='failed'` · `notification='queued'` 분기에서 **1 반환**(재배포 없음) |
| `--retry-notification` 범위 | `deployment=='verified'` 이고 `notification=='failed'` 일 때만 — 알림 재전송 전용 |

- 판정 = **실패 지점(deploy[8])부터의 이어받기 없음.** 새 계획 id `…-3` 으로 전체 재실행.
- 계획 JSON = 2차 사본에서 `id` 한 필드만 교체 · 나머지 전 필드 동일(`id` 제거 후 정렬 비교 `diff` 출력 0행)

## 14. 자격증명 공급 — 실행 셸 한정 주입

- 방식 = 운영자 env 적재와 같은 하위 셸에서 `aws configure export-credentials --profile "$AWS_PROFILE" --format env` 를 `eval`
- 프로파일 = `colab-dev`(운영자 env `AWS_PROFILE` 값) · 호출자 확인 `aws sts get-caller-identity` 성공 · ARN 접미 `colab-platform-s3-uploader-dev`
- 주입 결과 = `AWS_ACCESS_KEY_ID` 존재 · `AWS_SECRET_ACCESS_KEY` 존재 · `AWS_SESSION_TOKEN` 부재(장기 IAM 사용자 키)
- 성격 = **레포 파일·코드 수정 0.** 실행 셸 수명 동안만 존재 · 파일 기록 0 · 로그 기록 0

## 15. 단계별 실행 결과 — 전건 종료코드 0

| # | 단계 | 종료코드 | 요지 |
|---|---|---|---|
| 0 | `--check` | **0** | 축자 「배포 계획 확인 완료 — 실행·전송 없음」 |
| 1 | `deploy[0]` `infra/dev/build.sh` | **0** | 이미지 5종 `arm64 ✓` · tar 285M · 「ops source bundle green — commit 81784897ec74」 |
| 2 | `deploy[1]` `infra/dev/ship.sh` | **0** | 축자 「실었다: dev-81784897ec74」 |
| 3 | `deploy[2]` 이미지 태그 | **0** | `COLAB_IMAGE_TAG=dev-81784897ec74` |
| 4 | `deploy[3]` 백업 | **0** | `colab_platform` 덤프 업로드 |
| 5 | `deploy[4]` `up.sh` | **0** | 4 단위 healthy · 축자 「up: ok (sha 81784897ec74)」 · `storageMode`·`sourceMode`·`previewSink` 전부 `s3` |
| 6 | `deploy[5]` 트리 동기화 | **0** | 계획 내장 단계 |
| 7 | `deploy[6]` `npm ci` | **0** | — |
| 8 | `deploy[7]` `npm run build` | **0** | `tsc --noEmit` 통과 |
| 9 | `deploy[8]` `ops/deploy_web.py` | **0** | **2차 중단 지점 통과** — 웹 버킷 업로드 성립 |
| 10 | `verify[0]` `deploy_doctor.py` md5 대조 | **0** | 개발 기계 ↔ EC2 일치 `a2d5651512ff786f0a71eeb36e51ed9c` |
| 11 | `verify[1]` 원격 프로브 | **0** | — |
| 12 | `verify[2]` `MAIN_SHA` | **0** | `main=81784897ec74 candidate=81784897ec74 ancestor=yes` |
| 13 | `verify[3]` `CURRENT_SHA` | **0** | `81784897ec74` |
| 14 | `verify[4]` 진입 `/` | **0** | `root=200` |
| 15 | `verify[5]` `/api/v1/me` | **0** | `api/v1/me=401` |
| 16 | `verify[6]` `index.html` md5 | **0** | 개발 기계 ↔ 공개 주소 일치 `c484e6ea64e9f7af04c06cbfb6a0febb` |
| 17 | 실행기 종료 | **0** | 축자 「배포·검증 완료 (알림 상태는 배포 기록에서 확인)」 |

- `status dr2-dev-81784897ec74-3` = `deployment: verified` · `dv.deploy: passed` · `dv.verify: passed` · 단계 16건 전건 `exit 0` · `notification: queued`
- 알림 아웃박스 = `release:dr2-dev-81784897ec74-3:dev:verify:0` · `severity=info` · `kind=deploy.succeeded` · `exit_code=0`

## 16. 손 트리 동기화 3줄 — 실행 (계획 내장 단계와 별도 1회)

- 원본 = `origin/main` 아카이브 4경로(`db`·`gates`·`services/core-api/ops`·`infra`) — 작업 사본 미추적 파일 유입 0
- 결과 = 전송 0 · 원격 `sudo tar … --overwrite` 0 · 확인 0
- 확인 실측 = `/opt/colab-repo/db/platform/versions` 최신 **`0031_search_evidence.py`** · `deploy_doctor.py` md5 `a2d5651512ff786f0a71eeb36e51ed9c`

## 17. `deploy_doctor` — 한 번의 실행으로 15/15

- 실행 = 원격 `sudo bash /opt/colab-repo/infra/ops/probes/deploy-verification.sh` **1회** · `--allow-skip` 미사용 · 재시도 0 · 부분 실행 합산 0
- 결과 축자 = **`항목 15 — ✓ 15 · ✗ 0 · ─ 0`** · 「전 항목 통과 (─ 0 — 15 항목이 실제로 돌았다)」 · 종료코드 **0**
- ✗ 항목 = **없음.** 정찰 시점의 유일한 ✗ ⑥(스키마 head · platform)은 `§16` 동기화로 해소
- ⑮ 실행 sha ∈ main = `81784897ec74 ∈ main (origin/main=81784897ec74 · 반입 시 조상 확인됨)`
- ⑪ 앱 자격증명 출처 = `imds (EC2 인스턴스 역할)` — 개발 기계 자격증명 주입은 EC2 런타임과 무관

## 18. 완료 판정 대조 (`CLAUDE.md §0` 「dev 배포 green ＋ `deploy_doctor` 15/15 를 한 번의 실행으로」)

| 조건 | 실측 | 판정 |
|---|---|---|
| dev 배포 green | 실행기 종료코드 0 · `deployment: verified` | 충족 |
| `deploy_doctor` 15/15 | `✓ 15 · ✗ 0 · ─ 0` | 충족 |
| 한 번의 실행 | 프로브 1회 · 재시도 0 · 부분 합산 0 | 충족 |
| `CURRENT_SHA` | `81784897ec74` | 일치 |
| `MAIN_SHA` | `main=81784897ec74 candidate=81784897ec74 ancestor=yes` | 일치 |
| 공개 주소 응답 | CloudFront 배포 주소 `HEAD /` 첫 줄 `HTTP/2 200` | 정상 |

## 19. 미실행 — 후속 판정 대상

- `infra/dev/tag-release.sh dev`(릴리스 태그) = **미실행.** 태그 발급은 이 회차 지시 범위 밖
- 초기화 도구(`reset_dev_environment.py`) = **미착수**(WU-R1a 미구현) · Ted GO 미기록
- 레포 추적 파일 변경 0 · 커밋 0 · 원장 행 기입 0

## 20. 근거 자리 (3차)

- 실행 로그 = 작업 임시 폴더 `deploy-dr2-3.log`(레포 밖 · 커밋 대상 아님)
- 계획 JSON = 작업 임시 폴더 `release-dev-81784897-3.json`(레포 밖 · 비밀값 0)
- 단계 로그 = 배포 기록 폴더 `deploy-releases/dr2-dev-81784897ec74-3/dv.deploy.<0-8>.log` · `dv.verify.<9-15>.log`
- 배포 기록 = 같은 폴더 `state.json`
