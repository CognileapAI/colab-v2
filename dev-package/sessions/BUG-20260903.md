# BUG-20260903 — 버그 회차 · Ted 접수 UI 12건 (계보 · 미리보기 · 레이아웃 · 문안)

> 등재 — `PLAN-SoT §9 〈309〉`. 대장 신설 = `BF-1`~`BF-6`(닫힘) · `BF-7`~`BF-11`(등록만). 반영 = `03-HANDOFF §1 T-B` · `WORK-UNITS §10.4`·`§11`.
> 보고 사본 — `dev-package/reports/bug-20260903/`(접수표 · 계획 · recon 4 · 레인 보고 5 · 스크린샷 PNG 12). 원본은 작업 폴더에 있었고 세션이 끝나면 사라지므로 여기로 옮겼다.
> 진행 — 접수 → recon 4(A 계보 · B 미리보기 · C 레이아웃 · D 범위) → advisor 게이트 ①(계획) → Ted 판정 5건 → 코드 레인 5(워크트리 격리 · 기반 `origin/main` `56eac76`) → advisor 게이트 ②(수용) → 등재(이 레인). 코드 병합은 통합 브랜치 `bugfix/20260903-ui-12`(다른 레인).
> ⛔ 이 등재 레인은 `dev-package/` 와 `CLAUDE.md` 만 읽고 고쳤다 — 코드·계약·DB·인프라·staging 무접촉 · 배포 0 · push 0 · 병합 0.

## 0. 처리 원칙 (전 레인 공통)

- 근본 원인 확인 전 수정 금지(recon 이 먼저) · 실패 시험 **red 선실측** → 최소 수정 → green · 「while I'm here」 금지.
- 범위 판정이 필요한 것(B-2 정책 충돌 · B-3 stage 2 여부 · B-4 배율 규칙 · B-5 오독 여부 · C-4 정본 문구)은 수정 전 advisor 게이트 → Ted 묶음 질문.
- staging 미접촉 — 재현·검증은 로컬 시험으로만.

## 1. 대응표 — Ted 번호 ↔ 접수 id ↔ `BF-` (정본 = `reports/bug-20260903/BUG-INTAKE-20260903.md`)

⚠ 레인 보고와 `PLAN-fanout.md` 는 접수 B-4·B-5 를 「버그 13·14」로 적었다 — 접수표의 Ted 번호는 **10·11** 이다. 아래 표가 정본이고, 레인 보고를 읽을 때 13 → 10 · 14 → 11 로 읽는다.

| Ted | 접수 | 증상 (요지) | 스크린샷 | 판정 | `BF-` | 상태 |
|---|---|---|---|---|---|---|
| 3 | A-1 | Lv0 앞·Lv1 뒤 노드가 없는데 화살표가 양끝에 | `bug03-*-a/b.png` | 결함 (고정 4칸 · 무조건부 화살표) | `BF-1` | ✅ |
| 5 | A-2 | 루트 Lv0 왼쪽에 화살표 2 + 빈 칸 2 | `bug05-*.png` | 결함 (같은 원인) | `BF-1` | ✅ |
| 7 | A-3 | 원천 ↔ Lv0 사이 「→ · 빈 칸 · →」 | `bug07-*.png` | 결함 (같은 원인) · 원천 edge 부재는 뒷단 | `BF-1` (＋ `BF-9`) | ✅ (⬜) |
| 4 | B-1 | Lv1 가공본(126×128 · 4326) 미리보기가 회색 바탕에 점 격자 | `bug04-*.png` | 결함 (viz-render 전방 산란) | `BF-2` | ✅ |
| 6 | B-2 | 위경도 자료에 바탕 지도(육지 윤곽) 없음 | `bug06-*.png` | 정책대로 동작(POL-021) · **Ted Q4 = 이번 회차 미착수** | `BF-10` | ⬜ |
| 8 | B-3 | 「확대·축소·기본 배율로」 무동작 | `bug08-*.png` | 결함 (`maxScale` 1.0 · 시험 픽스처가 못 잡음) · **Ted Q1 = ⓐ** | `BF-3` | ✅ |
| 10 | B-4 | 업로드 미리보기 ≠ 상세 미리보기(상세가 줌인) · 기본 배율 규칙 불명 · 가로 흰 줄 | `bug10-*.png` | **배율 차이 = 버그 아님**(컨테이너 폭) · 흰 줄 = `BF-2` 와 같은 원인 · **Ted Q2 = 원본 격자 캡션** | `BF-2` · `BF-4` | ✅ |
| 11 | B-5 | Co-Kriging(Lv1) 미리보기 정체불명 · 범례 2~521 · 흰 줄 | `bug11-*.png` | **DEM 페이지(D-02) 오독** — 범례가 파이프라인 실행값과 소수 4자리 일치 · **Ted Q3 = 이름표 ＋ 변수명** | `BF-4` (＋ `BF-2`) | ✅ |
| 1 | C-1 | 프로젝트 탭 카드 목록 좌측 여백 없음 | `bug01-*.png` | 결함 (`project.css` 뿌리 규칙 0건) | `BF-5` | ✅ |
| 2 | C-2 | 「연구실 설정」 톱니 아이콘·글자 간격 | `bug02-*.png` | 결함 (`.gnb-settings` `gap` 누락) | `BF-5` | ✅ |
| 9 | C-3 | 프로젝트 상세가 raw HTML 수준 | `bug09-*.png` | 결함 (뿌리 규칙 0건 · backlink 스코프) | `BF-5` | ✅ |
| 15 | C-4 | 데이터셋 설명문 `／` 구분자 과다 | `bug12-*.png` | 문안·표시 — **Ted Q5 = 표시만 분할(A안)** · 정본 §8 문구 확인 대기 | `BF-6` | ✅ (확인 대기) |
| — | (recon) | 스크린샷 버튼 — 객체 URL 즉시 revoke | — | Ted 미보고 · 등록만 | `BF-7` | ⬜ |
| — | (recon) | `LabPage` 뿌리 규칙 부재 | — | 곁가지 · 등록만 | `BF-8` | ⬜ |
| — | (recon) | `project.css` 미정의 토큰 5종 | — | 곁가지 · 등록만 | `BF-11` | ⬜ |

Ted 원문 번호 12·13·14 는 접수표에 없다(결번). 접수 12건 = `BF-` 6건 닫힘 ＋ 등록만 1건(`BF-10`) ＋ 버그 아님 2건(Ted 10 배율 · Ted 11 정체불명 — 둘 다 표시 보강 `BF-4` 로 해소).

## 2. 레인별 — 근본 원인 · 수정 · 증거

전 레인 기반 `origin/main` `56eac76` · 워크트리 격리 · `dev-package/` 무접촉 · `main` 무접촉. 시험 수는 각 레인 워크트리의 실측이라 레인마다 기준 파일 수가 다르다(36~37 파일).

### L-A · 계보 그래프 (`BF-1` · 커밋 `4d01d28` · `reports/bug-20260903/lane-A-report.md`)

- 근본 원인 — 가로축이 「노드 종류 = 고정 4칸」을 전제(`LineageSection.tsx:46-64`·`:214`)해 종류가 없는 칸도 DOM·폭 178px 을 들고 있었고, 레일 화살표가 인접 칸의 노드 유무와 무관하게 항상 출력(`:286-305`). CSS 가 빈 칸에도 폭을 준다(`lineageGraph.css:21-24`).
- 수정 — 노드 있는 칸만 렌더(`data-col` 칸 번호는 유지 → `§8` 축 순서 보존) · 화살표는 선 칸과 선 칸 사이에만 · 건너뛴 레일의 가공 방식 라벨은 그 한 칸에 모아 값 보존 · `.lin-axis` `min-width: 820px` → `width: max-content; min-width: 100%`.
- 증거 — 기존 4칸 고정 단언 교체 ＋ 픽스처 3종(루트 `[2,3]` 화살표 1 · 잎 `[1,2]` 화살표 1 · 원천 `[0,2,3]` 화살표 2 · 첫 레일 라벨 없음) ＋ 불변식(칸 노드 ≥ 1 · 화살표 = 칸 − 1 · 오름차순). vitest **36파일 / 575건** · `tsc` 0. 3파일 +179/−36.
- 보류 — 원천 → 루트 edge 를 core-api 가 안 내려준다(`routes/lineage.py:87-91`). 프론트는 「라벨 없는 화살표」로 명시 수용 → `BF-9`.

### L-B1 · viz-render 리샘플 (`BF-2` · 커밋 `40844ea` ＋ `5f6b75f` · `reports/bug-20260903/lane-B1-report.md`)

- 근본 원인 — `warp_to_3857` 이 `np.add.at` **전방 산란**이라 원본이 출력(1024)보다 성기면 대부분 픽셀이 결측(126×128 채움 **1.95 %** — bug04 의 실체) · 같은 해상도에서도 lat→y 비선형으로 **전 결측 행 2줄**(가로 흰 줄 · bug10·bug11). 로컬 원천으로 100 % 재현 — core-api·storage 무죄(`recon-B-preview.md §1·§4·§6`).
- 수정 — ⑴ 원본 긴 변 > `max_side` 면 `block_average` 로 먼저(「촘촘 → 평균」 보존) ⑵ 원본 셀을 3857 출력 격자에 씨앗으로(한 픽셀에 여럿이면 픽셀 중심에 가장 가까운 것 · NaN 셀도 씨앗) ⑶ 씨앗 없는 픽셀은 2패스(행·열) 근사 최근접 · 추정 원본 간격을 넘으면 채우지 않음(발자국 밖 보존).
- advisor 게이트 ② 수정 2건(`5f6b75f`) — 캐시 키 라벨 `warp+blockavg` → **`warp+nearest`**(알고리즘이 바뀌었는데 라벨이 그대로면 구·신 산출물이 같은 키 · `storage_layout.py` 원칙 위반) · docstring 「2패스 근사 최근접 · 정확 최근접 일치 86~95 %」 ＋ 「촘촘→평균은 ① 축소 단계에서만」.
- 증거 — RED = origin/main 원본으로 신규 6건 **전부 실패**(채움 0.0195 · 전 결측 행 잔존 · 흰 줄 2 · 결측 메움 0.0558→0.9816 · 촘촘 평균 안 됨 · 발자국 안쪽 채움 0.094) → GREEN 유닛 **269 → 275 passed · 실패 0**(deselected 40 = e2e/perf). 자체 지연 = 1024² 5회 **평균 0.378 s · 최대 0.415 s**(`render-latency` 게이트는 원천 미마운트로 readiness red — 통과로 세지 않음).
- 등재된 한계(항목 없음 · `〈309〉`-㉰) — ㉠ 근사 최근접(셀 경계 1 px) ㉡ 곡선 격자에서 「촘촘 → 평균」 미보존(현재 실원천에 해당 부류 0).
- 캐비어트(축자) — 「staging 은 〈307〉 캐시 키 변경 후 재굽기(RC-1) 미실행 상태라, 이 수정 배포 뒤에도 구판 산출물이 보일 수 있다 — 배포 순서에 재굽기를 넣는다」 → advisor 정련 **`BF-2` 배포 → 그 뒤 재굽기 1회**(`RC-1` 진입조건 증보).

### L-B35 · 확대 한계 · 이름표 · 원본 격자 캡션 (`BF-3`·`BF-4` · 커밋 `a1ea2d5` ＋ `78cad68` · `reports/bug-20260903/lane-B35-report.md`)

- 버그 8 근본 원인 — `useZoomPan.ts:192` `maxScale = max(1, PNG 폭 / 뷰포트 폭)` 이 실측 808~821 / ≈820 → **1.0**. 기존 시험 픽스처 `measure(4096, 512)` 는 파이프라인이 만들지 않는 해상도라 green 이었다. ⚠ stage 2 미구현이 아니다 — 확대는 `P3` 가 `〈241〉` 로 닫힐 때 완료 정의 그대로 충족된 기능이다(`recon-D-scope.md §2`).
- 버그 8 수정(Ted Q1 ⓐ) — `maxScale` 계산 **무변경** · `atLimit` 에 `maxScale <= 1` 추가 → 첫 클릭 전부터 「원본 해상도까지 봤어요」 안내 ＋ 확대 버튼 `disabled`. RED 2 → GREEN.
- 버그 11(레인 표기 14) — 렌더 결함이 아니라 DEM 페이지 오독. 수정 = `DatasetPreviewSection` 머리에 `detail.name` ＋ `fileName` · `PreviewPanels` 범례 첫 줄에 `legend.variable`(`preview/types.ts:17` · 비면 자리째 뺌). 새 API·계약 0.
- 버그 10 캡션(레인 표기 13 · Ted Q2) — `basicInfo.grid` ＋ ③지도형 사이드카 `mapGeometry` `width×height` → 「격자 0.05° (~5km) · 126 × 128」. ②비지도형은 격자 간격만 · 둘 다 없으면 캡션 없음.
- 증거 — RED(Part 2·3 소스 되돌림) **5 failed / 16 passed** → GREEN **37파일 / 580건** · `tsc` 0. 금지 파일(`LineageSection.tsx` · `.pv-tile` 크기 CSS · `pointFromViewport` · `ScreenshotButton.tsx`) diff 0. Part 2·3 은 같은 파일에 얽혀 커밋 하나로 묶음.

### L-C · 프로젝트 뿌리 스타일 · GNB gap (`BF-5` · 커밋 `61ef95b` ＋ `d3bf37e` · `reports/bug-20260903/lane-C-report.md`)

- 근본 원인 — 공용 PageShell 이 없고 화면 뿌리 클래스가 자기 여백을 적는 관례(`catalog.css:24`·`detail.css:27`)인데 `project.css` 에 `.project-page`·`.project-detail` 뿌리 규칙 **0건** · 되돌아가기 줄은 `detail.css` 가 `.detail-page` 로만 스코프. `shell.css:148` `.gnb-settings` 만 `gap` 누락(형제 6~7px).
- 수정 — `project.css` +70(뿌리 여백·최대폭 · h1 · backrow/backlink · 상세 카드 `background`·`box-shadow`) · `shell.css` `gap: 6px` · `vite.config.ts` `test.css.include` 에 `project.css`·`shell.css`(`(\?raw)?` 허용).
- Sonnet 초안에서 고친 것 — jsdom 이 `var()` 배경·`gap` 을 계산값으로 못 내 규칙 원문(`?raw`) 시험으로 교체 · `node:fs` 우회 배제(`e01-apply-points.test.ts:14` 선례 — 2026-09-02 main 배포 불가의 원인).
- 증거 — RED(CSS 되돌림) **3 failed / 52 passed** → GREEN **36파일 / 573건** · `tsc` 0.
- 등록만 — `LabPage` 뿌리 규칙 부재 → `BF-8` · `project.css` 가 쓰는 `--line`·`--fg-muted`·`--surface`(·`--surface-2`·`--warn`)가 `tokens.css` 에 0건 → `BF-11`.

### L-C4 · 설명문 `／` 표시 분할 (`BF-6` · 커밋 `02738e4` · `reports/bug-20260903/lane-C4-report.md`)

- 출처 — 시드 설명문(`S2b`)이 `／` 로 항목을 잇고 상세 `.dh-sum` 이 한 줄로 그린다. 3안 중 **A안(표시만 분할)** 을 Ted 가 승인(Q5).
- 수정 — `DetailHeader.tsx` `SUMMARY_SEPARATOR = '／'` ＋ `summarySegments()` · 조각 2개 이상일 때만 `<p class="dh-sum-lead">` ＋ `<ul class="dh-sum-list">` · 1개면 종전 그대로 · `.dh-sum` 껍데기·`data-testid` 유지. 저장·검색·시드·DB·계약 무접촉.
- 증거 — RED **2 failed / 3 passed**(통과 3 = 「종전 그대로」 가드) → GREEN **37파일 / 575건** · `tsc` 0. `git revert 02738e4` 1회로 복귀.
- 판정 대기 — 정본 `Policy_데이터셋_상세 §8` 「③ **한 줄** 요약」(`DetailHeader.tsx:2` 축자)과의 관계를 Ted 가 확인한다. 암묵 규약 「`／` = 구분자」는 `〈309〉`-㉳ 에 등재.

## 3. advisor 게이트

- **게이트 ① (계획 · 입력 `reports/bug-20260903/PLAN-fanout.md`)** — 요지: 원인이 확정된 5건(계보 · 리샘플 · 확대 · 프로젝트 CSS · gap)은 즉시 수정 레인으로, 판정이 필요한 5건(B-2 정책 · B-4 배율 · B-5 오독 · C-4 문구 · 스크린샷 버튼)은 코드 착수 전 Ted 묶음 질문으로. 파일 겹침 0 확인(L-B4·L-B5 만 `PreviewPanels.tsx` 공유 → 직렬). 스크린샷 버튼은 등록만. ⚠ 게이트 ① 의 축자 판정문은 작업 폴더에 남아 있지 않다 — 여기 적은 것은 계획 문서에서 읽은 요지다.
- **게이트 ② (수용)** — **전 레인 5 ACCEPT.** L-B1 에 수정 2건 부과 → `5f6b75f` 로 반영(캐시 키 라벨 `warp+nearest` · docstring 2패스 근사 최근접 86~95 % · 「촘촘→평균」은 ① 축소 단계에서만). RC-1 순서 정련 = **`BF-2` 배포 → 그 뒤 재굽기 1회**(배포 전 재굽기는 구 알고리즘을 굽는 낭비). Q5 의 정본 §8 문구 확인은 Ted 에게 남았다.
- **Ted 판정 (2026-09-03)** — Q1 = ⓐ `maxScale` 규칙 유지 · 한계 안내를 처음부터 / Q2 = 「상세에서 원본 픽셀 크기를 알 수 있게」 / Q3 = 권고대로(데이터셋 이름 ＋ 범례 변수명) / Q4 = 바탕지도 미착수 · 격자선 옵션(POL-021 준수)을 다음 회차 후보로 / Q5 = 상세 화면 `／` 표시 분할 승인 · §8 문구 확인은 대기.

## 4. 등재 (이 레인)

| 파일 | 무엇 |
|---|---|
| `dev-package/work-items.yaml` | `BF-1`~`BF-11` 신설(106 → 117) · `RC-1.entry_conditions` 순서 조항 증보 |
| `dev-package/PLAN-SoT.md §9` | `〈309〉` 1행 |
| `dev-package/PREVIEW-IMPLEMENTATION.md` | `§2` 표 ③행 · `§5.3` ⑤ ＋ 정의 주석 · `§7.2` 다운샘플 라벨 `warp+nearest` |
| `dev-package/03-HANDOFF.md` | 최종 갱신 · `§1 T-B` 표 신설 · `§4.5` ⑷⑸ |
| `dev-package/WORK-UNITS.md` | `§10.4` 등록만 다섯 · `§11` 「버그수정」 행 |
| `dev-package/reports/bug-20260903/` | 접수표 · 계획 · recon 4 · 레인 보고 5 · PNG 12 (절대경로는 레포 상대로 치환) |

계수 영향(`〈258〉` 규칙 · 실측) — `stage1` 미완 **0 → 5**(`BF-7`~`BF-11`) · `PA-G` 개시 조건 값 **10 → 15** · 총 미완(`PA-G` 포함 · `deferred` 제외) **11 → 16**.

## 5. 남은 것

- 통합 브랜치 `bugfix/20260903-ui-12` — 8 커밋 병합 → `./gates/run.sh all` ＋ 서비스 pytest ＋ vitest 전수 → advisor 게이트 ③ → PR. **로컬 green 은 CI green 이 아니다.**
- 배포 → **그 뒤 재굽기 1회**(`RC-1`) → 소유 네 등급 재실측(`CR-2` ⑶).
- Ted — `BF-6` 정본 §8 「한 줄 요약」 문구 확인 · `BF-10` 착수 회차 · `BF-10` 의 stage 배정 재검토.
- `BF-7`·`BF-8`·`BF-9`·`BF-11` 착수(진입조건 열림 · `BF-5` 병합 뒤).
