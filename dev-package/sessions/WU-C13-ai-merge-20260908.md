# WU-C13 — ai 체인 합류 (w9 `0006_topic_vocab_six` 흡수 ＋ `0007` 머지)
**Ted 판정 ⓐ (2026-09-08 · 배포 창 중 발견)** — 「w9 ai 3커밋 흡수 ＋ 0007 merge 리비전」. dev RDS 의
`alembic_version_ai` 가 `0006_topic_vocab_six` 로 스탬프돼 있는데 그 파일이 `main` 에 없었다(`w9-rebase` 의
`80aeb00` 에만 산다). **리비전 id 는 한 번 배포되면 사실이다** — 번호를 다시 붙이지 않는다.
## ⑴ `80aeb00` 흡수 (커밋 통째 cherry-pick 0 — w9-rebase 는 main 보다 382 파일 뒤다)
| 파일 | blob | 역할 |
|---|---|---|
| `db/ai/versions/0006_topic_vocab_six.py` | `a55e018` | CHECK 4→6값 재부착 ＋ 시드 실행. `down_revision = "0005_k2b_concept_graph_seed"` **무변** |
| `db/ai/seed/topic_synonym_six.sql` | `7435dab` | 동의어 13행 · `ON CONFLICT DO UPDATE`(멱등) |

수술한 공유 파일 — `db/ai/schema.sql`(주제 6값 ＋ R-C `category` 열의 **맨 뒤 순서 보존**) ·
`db/ai/tools/k2b_graph_check.py`(`CANON_TOPICS` 그래프 노드 4 ↔ 신설 `SYNONYM_TOPICS` 6 분리) ·
`db/ai/tests/0006-drift.sh`(선언 대조 상대를 체인 head 로) · `gates/config/migration-drift.toml`(db/ai `min 2→3`).
⛔ `services/ai-service` 무접촉 — `ports.TOPICS` 는 `〈359〉` 때 이미 6값(실측 diff 0). ⛔ `contracts/`·`frontend/` 무접촉.
## ⑵ 두 순서 수렴 증명 — `db/ai/tests/0007-drift.sh` (신설 · 일회용 postgres)
`0007_merge_vocab_and_category`(id **29자** · `varchar(32)` 제약 · 파일명은
`0007_merge_topic_vocab_and_rc7_category.py`) 의 `down_revision = ("0006_topic_vocab_six", "0006_rc7_synonym_category")`.
읽어 낸 논거 — rc7 의 `UPDATE … WHERE topic IN (3값)` 이 새 13행에 안 걸리고, 창 9 의 `DO UPDATE SET topic,
source_note` 가 `category` 를 안 덮으며, rc7 은 주제 CHECK 를 다시 걸지 않는다 ⟹ 이름 교집합 0 · 행 교차 0.
기계 증명 — ㈎ dev 순서(어휘 6값 → 분류 → 머지) ㈏ staging 순서(분류 → 어휘 6값 → 머지) 를 실제로 올리고
㈐ 두 `pg_dump` 동일 ㈑ `schema.sql` = 양쪽 ㈒ `d9_topic_synonym` **18행 동일** · 새 13행 `category = NULL`([미상]
그대로) · 이관 3쌍 보존 · 6값 실통과 / `유출·수문` 실거절. 0번 판정은 머지 `upgrade()` 본문 0문장을 `ast` 로 본다.
## ⑶ 게이트 (축자)
- `migration-single-head green — 두 체인 모두 head 1개.` / `# db/ai: 리비전 8건 · head 1개 (0007_merge_vocab_and_category)`
- `migration-drift green — 오라클 17 · 실행 17 · 실패 0.` / `[migration-drift] db/ai/tests — 오라클 3 · 실행 3 · 실패 0 (선언 최소 3)`
- `schema-diff green — 두 체인 각각 alembic upgrade head **뒤에** 선언 = 적용.`
- `service-tests-ai-service green — 실행 137건 전부 통과 (skipped 0 · deselected 26 은 요약줄에 드러나 있다).`
- `db-boundary: green — 단위 7개 · 스캔 대상 346건 · 위반 0` · `exec-bit green — .sh 138건 전부 인덱스 모드 100755 (100644 = 0건).`
- `work-item-consistency: green — 대장과 산문의 불일치 0` · `0007-drift green — 머지 차분 0 · 두 순서(dev·staging) 스키마·행 수렴 · 선언 = 적용 · 6값 실통과/실거절.`
⚠ venv 가 없어 `COLAB_ALEMBIC = worktrees/rc-c11/gates/.venv/bin/alembic` 을 선언하고 `services/ai-service/.venv` 를 새로 지어 돌렸다(부재는 red(준비)이지 skip 이 아니다).
## ⑷ dev 배포에 뜻하는 것
`0006_topic_vocab_six` 가 이제 **알려진 리비전**이다 ⟹ dev 에서 `alembic upgrade head` 가 올리는 것은
**`0006_rc7_synonym_category` ＋ `0007_merge_vocab_and_category` 둘뿐**이다(어휘 6값 재적용 0). staging·빈 DB 는
반대로 `0006_topic_vocab_six` ＋ `0007` 을 받고, ㈐ 가 두 끝이 같음을 증명했다.
`[미상]` — dev RDS 실접속 대조는 이 레인이 하지 않았다(배포 창의 몫). 창 9 의 나머지 두 커밋(dev 트리거 스풀 · `sourceLabel`)은 **이 판정 밖**이라 손대지 않았다.
