# R-LTH-REVIEW-1 — 원장·대장 마감 기재 초안

> **초안 · 미병합.** 오케스트레이터가 Task 8 에서 `dev-package/PLAN-SoT.md §9` 와 `dev-package/work-items.yaml` 에 옮긴다. 이 파일은 그 두 문서를 고치지 않는다.

- 작성 2026-09-14 · 대조 트리 `integration/r-lth-1` HEAD `0427ac91`.
- 입력 = 라운드 `dev-package/prd/rounds/R-LTH-REVIEW-1.md` `### Task 8` ＋ `## 계획 자체 점검` · 판정 등재문 `dev-package/reports/issues/2026-09-13-ted-decisions.md`(판정 10 ＋ `## 추가 확인 (2026-09-14 · 회차 실행 중)` 4) · 취합본 `dev-package/reports/issues/2026-09-13-lth-review-1-SUMMARY.md` §4·§5 · Task 7 수용 검토(오케스트레이터 전달).
- 형식 선례 = `PLAN-SoT §9` `〈396〉` 행 — 3열(`| 〈N〉 | **제목** | **확정 (…).** ㉮… |`) · 한 행 한 줄.
- `〈N〉` = 예약하지 않는다. 병합 직전 `bash dev-package/prd/tools/max-decision.sh` 재실측 값 ＋ 1(`.claude/rules/colab-rules.md §4-1`). 참고 실측 = 2026-09-14 통합 트리 최대 `396`(발급값 아님).
- 미측정 값 = `〈TBD: …〉`. 채우기 전에는 옮기지 않는다.

## 0. 옮기기 전 오케스트레이터 확인 1건

| # | 갈리는 두 문면 | 자리 | 영향 |
|---|---|---|---|
| ㈂ | 라운드 `## 실행 순서` 「Task 4 → Task 5 직렬 고정」 ↔ 통합 로그 실물 — Task 5 `0427ac91` 이 Task 3·4 보다 먼저 얹힘(2026-09-14 실측) | 라운드 파일 ↔ `git log origin/main..integration/r-lth-1` | `0427ac91` 이 `ProjectDatasetTable.tsx` 를 고쳤으므로 Task 4 rebase 충돌 가능 〈TBD: 해소 기록〉 |

## 1. `PLAN-SoT §9` 행 초안

- 기재 규칙 = 아래 블록의 줄바꿈을 공백 하나로 합쳐 **한 행**으로 넣는다. 기호 순서 유지.

```text
| 〈N〉 | **R-LTH-REVIEW-1 회차 — 기획자 1차 검증 16항목을 기존 화면·흐름 위에서 닫는다 · Ted 판정 10건 ＋ 회차 중 확인 4건 · 확정 판정 반전 2건(`〈282〉`-㉮ · PRD `미결-2 ⓐ`) (2026-09-13~〈TBD: 마감일〉)** |
**확정 (2026-09-13 · 판정자 **Ted** · 확인 문장 축자 「전부 권고대로 하자. 이걸로 스펙이랑 계획잡아보자」 · 등재문 `dev-package/reports/issues/2026-09-13-ted-decisions.md` · 라운드 `dev-package/prd/rounds/R-LTH-REVIEW-1.md` · spec `dev-package/prd/specs/2026-09-13-lth-review-1.md` · intent 6건 `dev-package/intent/2026-09-13-lth-*.md`(승인 2026-09-13) · 취합본 `dev-package/reports/issues/2026-09-13-lth-review-1-SUMMARY.md`).**
㉮**입력** — 기획자 이태헌 검증 16항목(결함 7 · 개선 9) · 구현 착수 14 · 작업 0 2건(「할 일 함」 표기 · 로그인 실패 뒤 이메일 칸). 계수 기준 = 피드백 원문 항목 1건 = 1항목.
㉯**Ted 판정 10건** — ⑴ 상단 「전체 연구실 (읽기 전용)」은 누를 수 없는 상태 표시로 바꾼다 · 한 연구실만 골라 보는 전환은 관리자 기능 묶음에서 따로 정한다 ⑵ 사용자 이름 옆 펼침 화살표를 지운다 · 계정 메뉴는 휴대전화 상단 정리와 한 묶음으로 정한다 ⑶ 휴대전화 상단은 「더보기」 하나를 남기고 누르면 기능 이름이 글자로 적힌 목록을 연다 ⑷ 비활성 계정은 구성원 권한 표에서도 「비활성」으로 보이고 그 줄 권한 편집이 잠긴다 ⑸ 본인 계정의 비활성화 버튼은 처음부터 누를 수 없고 이유가 적힌다 · 계정명 재입력·활성 관리자 셈은 별 항목 ⑹ 프로젝트 카드 한 자리만 키보드로 열리게 고친다 · 표 줄 3자리는 후속 ⑺ 화면 대표 제목 규약을 세우고 이번 회차는 연구실 설정 한 화면만 고친다 · 접근성 검사 게이트는 후보 등재 ⑻ 관리자 검색은 전 연구실을 뒤지고 범위 줄이 그 사실과 건수를 말한다 · 범위 고르개는 ⑴ 의 전환과 한 자리에서 정한다 ⑼ 승인 대기 칸 글자를 한국어 상태말로 바꾸고 취소선·회색·열 제목은 유지한다 · 승인 상태 3값은 별 항목 ⑽ 등록의 가공 단계 기본 선택값이 계보 계산값을 따른다 · 사유 입력 ＋ 한 번 더 확인 절차는 다음 회차 판정. 판정 성격 = 반전 2(⑼ ⑽) · 신규 3(⑴ ⑷ ⑺) · 범위 4(⑵ ⑶ ⑸ ⑹) · 갈래 1(⑻) · 계수 기준 = 판정 카드 1건 = 1건.
㉰**판정 정본의 불일치 2건 처리** — 카드 ⑥ 대 intent `…-lth-a11y-empty-states.md` Q1b = 카드 ⓐ(카드 한 자리 · 표 3자리 후속) · 카드 ③ 대 intent `…-lth-shell-header.md` 미해결 질문 = 카드 ⓒ(더보기 목록을 이번 회차에 신설 · `▾` 제거와 같은 레인). 근거 = 판정 등재문이 카드 표의 선택 열을 결정값으로 고정한다.
㉱**회차 중 Ted 확인 4건(2026-09-14 · 축자)** — ⑴ **폴더별 시험 DB 격리** 「B 해라 이 폴더는 다르기 때문에 이 폴더에서하는건 구분해도 됨 ( colab 폴더가 30, 31, 32 있는데 전부다르게할게)」 → 30·31·32 각각 `~/.colab-v2-test-<N>.env` · 컨테이너 `a2_pg_<N>`·`ai_pg_<N>` · `.claude/settings.local.json` 바인딩 · `schema-diff` green 3폴더 · 절차 `dev-package/RESTART.md §2-④-㉳` ⑵ **배포 보류** 「배포는 하지말고 이건 허락받고」 → dev 배포·`deploy_doctor` 는 전수 green ＋ `main` ff 뒤 Ted 허락으로만 실행 ⑶ **동시 실행 상한 해제** 「가능한형태로 진행해 상한묶지말고」 → 레인 5개 동시 · load 20~27(판정 등재문) / 14~37(레인 보고 · 측정 시점 다름) · vitest worker 타임아웃(판정 red 아님 · 재실행 처리) ⑷ **레포 ext4 이전** 「이번회차 끝나면 전부 옮길게 알았어」 → 근거 실측(2026-09-14 · 파일 2,400개) drvfs 읽기 22.9s · stat 7.6s vs ext4 0.05s · 0.02s(≈450배 · 캐시 무효) · 시점 = 이 회차 마감(전수 green · `main` ff · 배포 판정) 뒤 · 대상 30·31·32 전부 · 이전 뒤 env 파일·픽스처 경로·설정 바인딩 재점검.
㉲**반전 2건 개정 표시(원문 유지)** — ⒜ `〈282〉`-㉮ = 칸 글자 문안만(취소선·회색·꺼진 모양·열 제목 유지) ⒝ `dev-package/prd/PRD-260905-적용전기획.md` `미결-2 ⓐ` = 기본 선택값만(「막지 않고 경고만」 유지) ＋ 반전 기재 자리 `〈194〉`. 삽입 3자리 집행 〈TBD: 커밋 sha〉.
㉳**집행** — 통합 `integration/r-lth-1`(기점 `main` tip) · 커밋 = `ad2e40f8` 검색 범위 줄을 실제로 뒤진 스코프에 맞춘다(Task 2) · `b3ff16e9` 계정 관리 화면 본문 영역·표 안내·자동완성·자기 줄 가드 4건(Task 1) · `c5664a29` 폴더별 시험 DB 격리 절차를 `RESTART.md` 에 기재(㉱ ⑴) · `2b0156b5` 라운드 파일 통합 브랜치 이름을 실물에 맞춤 · `1e48a64b` 시험 간 되돌리기를 시각 기준에서 기본키 스냅숏 기준으로 변경 · `3a8d421c` Ted 확인 4건을 판정 기록에 추가 · `bdd89a90` main(DR-3 회차)을 통합 브랜치에 병합 · `0427ac91` 승인 대기 칸 글자를 한국어 한 표기로 모으고 목록에 가공 단계 불일치 표식(Task 5) · 〈TBD: Task 3 통합 sha — 2026-09-14 실측 `origin/lane/lth-processing-level` `8a87e19d` 통합 미반영〉 · 〈TBD: Task 4 sha〉 · 〈TBD: Task 6 sha〉 · 〈TBD: Task 7 sha〉 · Task 7 마이그레이션 = 0건(기존 계정 자격 저장소의 상태 조회 재사용 · 앱 롤 권한 무변 · 판정 등재문 「추가 확인 (2026-09-14)」 집행 편차 줄). 전수 = 〈TBD: 트리 해시 · `bash gates/run.sh all -j 4` green / red(판정) / red(준비) · 로그 경로〉 · `work-item-consistency` = 〈TBD: 요약줄〉 · `main` ff = 〈TBD: main ff sha · 통합 끝 트리와 트리 해시 동일 여부〉.
㉴**Ted 확인 대상 문면(전부 제안값으로 구현 · 코드 앵커 = 주석 `// Ted 문면 확정 대기 · R-LTH-REVIEW-1`)** — ⑴ 승인 대기 칸 「승인 전」 ⑵ 가공 단계 불일치 사유 한 줄 = spec §6 ㉲ 제안 축자 ⑶ 자기 줄 비활성화 잠금 이유 「자기 계정은 비활성화할 수 없어요」 ⑷ 구성원 권한 표 잠금 이유 「비활성 계정은 권한을 바꿀 수 없어요」(시험이 축자 고정) ⑸ 목록 불일치 표식 「계산값과 다름」(보조기기 이름 「가공 단계가 계보로 계산한 값과 다릅니다」). Ted 가 바꾸면 문면만 교체하고 구조·시험 구성은 유지. 확정 = 〈TBD: 확정 문면 · 일자〉.
㉵**배포** — **보류 — Ted 허락 대기**(㉱ ⑵). dev 배포 green ＋ `deploy_doctor` 15/15 한 번의 실행 ＋ 태그 `dev-YYYYMMDD-N` = 〈TBD: 허락 일자 · 태그 · 실행 결과〉. staging 은 리허설이고 완료 판정·배포 창 기록·사용자 확인을 하지 않는다.
㉶**기획자 회신 2건** — 「할 일 함」 현 표기 유지 · 로그인 실패 뒤 이메일 칸 관측 1건 요청 · 발송 = 〈TBD: 일자〉 · 관측 결과 = 〈TBD — 미회수면 `[미확인]` ＋ 후속 항목〉.
㉷**범위 밖·후속** — 대장 등재 = 〈TBD: 등재 id 목록〉(판정 유예분 ＋ 레인 발견분 ＋ 수용 검토 판정 대상 1건(상태를 모르는 구성원의 권한 편집) ＋ 〈TBD: Task 3·4·6·7 레인 보고분〉).
㉸**역링크** — 대장 `BF-18`·`BF-19`·`BF-20`·`BF-21`·`BF-22`·`BF-23` = `done` 〈TBD: 전 항목 증거 충족 후〉. |
```

## 2. 개정 표시 삽입 문면 (원문 유지)

- 형식 = `⭑ **⟨개정 2026-09-13 · `〈N〉`⟩ 새 값** ／ 종전 표기 = …`. 선례 = `PLAN-SoT` 의 `⭑ ⟨개정 2026-09-01 · `〈261〉`⟩` · PRD 의 `⭑ **⟨개정 2026-09-12 · `#34` · `〈388〉`⟩`(PRD 안 유일한 개정 표시 · 2026-09-14 실측 1건) · `CLAUDE.md §0` 의 「／ 종전」 병기.

### ⒜ `dev-package/PLAN-SoT.md` `〈282〉` 행

- 앵커 = `㉮ Ted 판정 = 승인 처리가 아직 오지 않은 행의 `Verified` 칸은 글자를 그대로 두되 취소선·회색·꺼진 조작 모양으로 그린다.**` — 그 뒤, `⭑ **종전 실물은 그 칸을 비워 두었다**` 앞에 삽입.

```text
 ⭑ **⟨개정 2026-09-13 · `〈N〉`⟩ 칸 글자는 한국어 상태말 「승인 전」〈TBD: Ted 문면 확정값〉이다 — 취소선·회색·꺼진 모양·`aria-disabled`·열 제목 `Verified` 는 그대로다** ／ 종전 표기 = 칸 글자 `Verified`(위 문장 원문 유지). 개정 범위 = 칸 글자 문안뿐 · 집행 `frontend/src/components/catalog/CatalogTable.tsx` 앵커 `VERIFIED_PENDING_LABEL` ＋ `frontend/src/components/project/ProjectDatasetTable.tsx` 같은 문면 상수.
```

- 같은 행 `**집행**` 문장의 「글자 `Verified` 를 그린다」·「시험 **3건**」 = 2026-09-02 시점 기록 · 무수정 권고(개정 표시 1자리로 이력 보존).

### ⒝ `dev-package/prd/PRD-260905-적용전기획.md`

- 실측(2026-09-14) = 문자열 `미결-2` 가 PRD 에 5행. 「기본 선택값 Lv2」는 표 행 `| 미결-2 |` 에 없고 앵커 `기본 선택값 = **Lv2**(rev1 `<option selected>`)` 문장에 있다(그 문장이 `(미결-2 ⓐ 확정 · PRD-10)` 인용). → 삽입 2자리.
- ⒝-1 앵커 `기본 선택값 = **Lv2**(rev1 `<option selected>`).` 바로 뒤:

```text
 ⭑ **⟨개정 2026-09-13 · `〈N〉`⟩ 기본 선택값은 계보 계산값을 따른다 — 확인된 부모가 1건 이상이고 부모 Lv 를 모두 알면 계산값(주입력 부모 최대 Lv ＋ 1) · 주입력 부모 0건이면 계산값 `Lv0` 이 기본 선택값(카드 ⑩ ⓐ 축자 「부모 0건이면 Lv0 · 그 경우 등록 화면에도 경고가 선다」) · 부모 Lv 를 하나라도 모르면 `Lv2` · 사용자가 계산값과 다른 단계를 고르면(부모 0건 포함) 등록 화면·상세에 불일치 안내와 사유 한 줄이 선다 · 막지 않는다 · 사람이 한 번 고른 값은 덮어쓰지 않는다** ／ 종전 표기 = 항상 `Lv2`(위 문장 원문 유지). 「어긋나도 경고만 내고 등록을 막지 않는다」는 무변.
```

- ⒝-2 표 행 앵커 `| 미결-2 |` 마지막 열 `**확정 (Ted 2026-09-05)**` 뒤:

```text
 ⭑ **⟨개정 2026-09-13 · `〈N〉`⟩ 기본 선택값만 계산값 추종으로 개정 · 「경고만」 유지**
```

### ⒞ `dev-package/PLAN-SoT.md` `〈194〉` 행 (반전 기재 자리)

- 선행 확인 = 〈TBD: `〈194〉` 행에 2026-09-05 `미결-2` 반전 표시가 이미 있는지 grep〉 — 있으면 그 뒤, 없으면 행 끝 `|` 앞에 삽입.

```text
 ⭑ **⟨개정 2026-09-13 · `〈N〉`⟩ 등록 화면의 가공 단계 기본 선택값이 계보 계산값을 따른다** — 사람이 Lv 를 고르는 것(PRD `미결-2 ⓐ` · 2026-09-05)과 계산값과 어긋나도 경고만 내는 것은 유지 · 이번 개정은 기본값만 계산값 쪽으로 옮긴다.
```

## 3. 대장 상태 계획 `BF-18`~`BF-23`

- 순서 = 대장 먼저 → `03-HANDOFF §1`(5줄 이내) → `bash gates/run.sh work-item-consistency` 1회.
- `done` 조건 = 닫는 Task 전부 통합 반영 ＋ 완료 정의 전 항목 증거 충족. 부분 완료로 `done` 금지(`CLAUDE.md §5`).
- `evidence:` 형식 제안 = `〈N〉` ＋ 통합 sha ＋ 게이트 배출처 `dev-package/reports/lth-review-260913/<레인>/gate-summary.json`.

| id | 내용 | 닫는 Task | 2026-09-14 실측 | 증거 슬롯 | 목표 |
|---|---|---|---|---|---|
| `BF-18` | 상단 셸 3건(연구실 표기 · `▾` 제거 · 더보기 목록) | Task 6 | 통합 미반영 · 로컬 `lane/lth-shell-header` | 통합 sha 〈TBD〉 · `shell-lth-20260913.test.tsx` 3건 red 선실측 한 줄 〈TBD〉 · `frontend-test` 연속 2회 3계수 〈TBD〉 · `frontend-typecheck` 〈TBD〉 · 수집 건수 감소 0 〈TBD〉 | `done` |
| `BF-19` | 계정 관리 4건 ＋ 구성원 비활성 칩·행 잠금 | Task 1(⑴~⑷) ＋ Task 7(⑸ 이후) | Task 1 = `b3ff16e9` · Task 7 = 통합 미반영(`lane/lth-lab-settings-members`) | Task 1 게이트 3계수 〈TBD〉 · Task 7 sha 〈TBD〉 · 롤 권한 대조 결과 〈TBD〉 · 계약 additive `accountStatus` ＋ 생성물 재생성 〈TBD〉 · 서버 저장 거절 시험 〈TBD〉 · 잠금 이유 문면 축자 고정 시험 〈TBD〉 · 완료 정의 잔여 항목 〈TBD〉 | `done` |
| `BF-20` | 카드 링크화 · 빈 표 문면 · 제목 규약 | Task 4(⑴~⑶) ＋ Task 7 제목 절(⑷⑸) | 둘 다 통합 미반영(`lane/lth-cards-empty` · `lane/lth-lab-settings-members`) | 신설 시험 3파일 red 선실측 〈TBD〉 · `frontend/README.md` `## 규칙` 한 줄 〈TBD〉 · 글자 크기 무변 계측 〈TBD〉 · `frontend-test` 2회 ＋ `frontend-typecheck` 〈TBD〉 | `done` |
| `BF-21` | 검색 범위 줄 정합 | Task 2 | `ad2e40f8` | `test_search_scope.py` red 선실측 〈TBD〉 · `service-tests-core-api` 〈TBD〉 · `frontend-test` 〈TBD〉 · 계약 무변(`contracts/` diff 0) 〈TBD〉 | `done` |
| `BF-22` | 승인 대기 칸 한국어 표기 | Task 5 | `0427ac91` | 문면 확정 〈TBD〉 · 시험 정정 3건(catalog · qa-20260903 · project) ＋ search-verified-20260903 무수정 통과(대장 `BF-22` ⑷ 개정 2026-09-14) · `frontend-test` 2회 ＋ `frontend-typecheck` 〈TBD〉 | `done` |
| `BF-23` | 가공 단계 4건 | Task 3(⑴⑵⑷⑸⑹) ＋ Task 5(⑶ 목록 표식) | Task 5 분 = `0427ac91`(`catalog-level-mismatch-20260913.test.tsx`) · Task 3 = `origin/lane/lth-processing-level` `8a87e19d` 통합 미반영 | Task 3 sha 〈TBD〉 · 부모 0건 규칙 = 카드 ⑩ ⓐ 축자로 정정·재구현(대장 `BF-23` ⑴ 개정 2026-09-14) · 신설 시험 2 ＋ `detail-edit.test.tsx` 확장 red 선실측 〈TBD〉 · 차단 0 회귀 〈TBD〉 · 게이트 〈TBD〉 | `done` |
