# staging 단독 창 — 단계 D 착수 회차 기록 (2026-09-03 00:05~00:12 KST · **D-0 에서 정지**)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md §4` ＋ `infra/staging/restore/RUNBOOK.md`.
> 이 파일은 **이 회차에 실제로 잰 값**만 적는다.
> 선행 회차 = `sessions/WINDOW-20260902-ABC2.md`(A·B·C · 배포 `77813d391f94` · 체인 `0011` · 게이트 39 green).
> **집행 범위 = D-0(입력 보충 ＋ preflight 재실행)까지. D-1 이후 미착수 — 복원을 실행하지 않았다.**

## 0. 창 진입 상태 (실측)

| 축 | 실측값 |
|---|---|
| 커밋 | `c404e3b` · 브랜치 `main` · 워킹트리 변경 0건 |
| 서빙 릴리스 | `77813d391f94` — 앱 5종 이미지 태그 동일 |
| 컨테이너 | 8개 전부 healthy |
| 시각 | 00:05 착수 — 크론(03:30·04:10 월·04:40)과 겹치지 않는다 |

## 1. D-0 — preflight 가 요구하던 입력 넷을 채웠다

| # | 입력 | 이 회차의 값 |
|---|---|---|
| 1 | `COLAB_STAGING_ENV_FILE` | 홈 0600 staging env 파일 지목(`COLAB_STAGING_ENV` 와 **다른 이름**). 값·경로 문자열은 적지 않는다 |
| 2 | `COLAB_RESTORE_PRE_BACKUP` | `platform-20260903T000540.sql.gz` — 아래 §1.1 의 회차 |
| 3 | `--record-digests` | `/tmp/pre-digests.tsv` (`RUNBOOK §1` 이 적은 경로 그대로) · **8줄 기록됨** |
| 4 | `COLAB_RESTORE_CAUSE` | `R-1 복원 리허설 · 단독 창 2026-09-03 · Ted go/no-go ③ GO` |

### 1.1 복원 직전 백업(P7 · 되돌림의 되돌림 재료) — GREEN

- 명령 = `infra/staging/backup/backup-full.sh` (플래그 없음) · 최종 줄
  **`전범위 백업 GREEN — 원장 2 프로파일 ＋ 볼륨 2 개 · 보관처 규약 밖 파일 0`**
- 이 회차는 **`0011` 배포 이후 상태**를 담는다 = 원상복구(④)의 재료다.

| 산출물 | 스탬프 | 크기(B) | 판정 |
|---|---|---|---|
| `platform-….sql.gz` | `20260903T000540` | 31,729 | GREEN (통과 7 · SKIP 0) · 표 25 · 행 437 |
| `ai-….sql.gz` | `20260903T000540` | 5,133 | GREEN (통과 7 · SKIP 0) · 표 6 · 행 91 |
| `vol-uploads-….tar.gz` | `20260903T000541` | 387,882,103 | GREEN (통과 8 · SKIP 0) · 매니페스트 154건 · 원장 130건 전건 |
| `vol-previews-….tar.gz` | `20260903T000618` | 11,565,701 | GREEN (통과 6 · **승인된 SKIP 1** = V5 오라클 명시 면제 `none`) |

### 1.2 preflight 재실행 — **RED 1건 · 복원을 시작하지 않았다**

명령 = `infra/staging/restore/preflight.sh --stamp 20260902T234147 --record-digests /tmp/pre-digests.tsv`
(`--stamp` 로 **복원 대상 회차**를 지목했다 — 인자 없이 돌리면 방금 뜬 `20260903T000540` 회차를 검사하게 되어
검사 대상과 복원 대상이 갈린다.)

| 항 | 판정 | 값 |
|---|---|---|
| P1 도커 데몬 | PASS | |
| P2 회차 한 벌 | PASS | `platform-20260902T234147` ＋ `ai-20260902T234148` ＋ `vol-uploads-20260902T234148` ＋ `vol-previews-20260902T234227` |
| P3 산출물 GREEN | PASS | 4종 전건(`--skip-age`) |
| P4 sha256 | PASS | 4종 일치 |
| P5 두 원장 같은 회차 | PASS | 시각차 1초 |
| P5-b 볼륨 짝 | PASS | 둘 다 `platform-20260902T234147.sql.gz` |
| P6 비밀 7종 ＋ env | **PASS 8건** | 종전 FAIL 1건(`COLAB_STAGING_ENV_FILE` 미선언)이 해소됐다 |
| P7 복원 직전 백업 | PASS | `platform-20260903T000540.sql.gz` |
| P8 digest | **FAIL** | 기록은 됐으나 **대장 대조 RED** — 아래 §2 |
| P9 원인 규명 | PASS | 위 사유 실값 |
| P10 보관처 규약 | PASS | 규약 밖 파일 0 |

**최종 줄 — `사전조건 RED (실패 1건) — **복원을 시작하지 않는다**`.**

⟹ 입력 넷 중 **셋이 해소되고(P6·P7·P9) 하나(P8)가 남았다.** 남은 것은 **입력 부재가 아니다.**

## 2. P8 이 RED 인 이유 — 입력 부재가 아니라 **대장이 실물보다 낡았다**

`check-image-digests.sh` 요약 축자 —
`불일치·미측정·미선언 7 건 (대조 8 건 · 승인된 면제 0건)`

| 유형 | 건수 | 대상 |
|---|---|---|
| 불일치 | 5 | `colab-v2/{core-api,pipeline-worker,ai-service,viz-render,frontend}:i2` — 실측 digest 가 `77813d391f94` 배포로 옮겨간 값(`c958298d…`·`db139f9a…`·`765d3fc6…`·`d726afc2…`·`f3d116b4…`)이고, 대장은 **2026-08-27 측정값** 그대로다 |
| 미선언 | 2 | `alpine:3.20` · `colab-v2/migrator:i2` — 대장에 줄이 없다 |
| 일치 | 3 | `postgres:16-alpine` · `nginx:1.27-alpine` · `cloudflare/cloudflared:latest` |

**이 RED 는 이 회차가 새로 만든 것이 아니다.** `dev-package/reference/IMAGE-DIGESTS.md §3` 이
⛔ 두 문단으로 **이 두 유형을 이미 적어 두고 있다**(불일치 5건 = 2026-08-29 Ted 판정 「값을 덮지 않는다」 ·
미선언 2건 = 2026-08-30 실측 `[미확인]`). 어제 배포(`77813d391f94`)가 자체 5종을 다시 옮겼으므로
불일치 폭만 커졌고, **대장을 갱신하는 회차가 그 사이에 없었다.**

### 2.1 왜 여기서 멈추는가 (판정)

- `PLAN-SoT §9 〈255〉`-㉰ 가 확정한 preflight 의 합격 성질 = **「입력을 다 주면 GREEN 이 된다」**이고,
  허용된 RED 사유는 **입력 부재(P6·P7·P8)와 의도된 fail-closed(P9)뿐**이다.
  **그 밖의 사유가 1건이라도 있으면 결함**이고, 「리허설이라 원래 RED」로 흡수하지 않는다.
- 지금 P8 은 `--record-digests` 를 **줬는데도** RED 다. 사유는 **대장 대조**이지 입력 부재가 아니다.
  ⟹ **허용 사유 밖 1건** ⟹ 규칙대로 **복원을 시작하지 않는다.**
- 우회 가능한 길이 둘 있으나 **둘 다 이 레인이 단독으로 할 일이 아니다.**
  ⓐ 대장의 자체 5줄을 오늘 값으로 갱신한다 — `IMAGE-DIGESTS.md §4-3` 이 정한 정규 절차이나
     **`PLAN-SoT §9` 등재가 따라붙고**(번호 발급 = 오케스트레이터 몫 · `CLAUDE.md §1-b-⑶`),
     2026-08-29 Ted 판정 「이 다섯 줄은 덮지 않는다」를 **뒤집는 판정**이라 Ted 확인이 필요하다.
  ⓑ 미선언 2건을 `면제: <사유>` 로 적는다 — 검사 대상을 줄이는 방향이라 `CLAUDE.md §3` 의 금지에 걸린다.
- ⛔ **`--record-digests` 를 빼거나 P8 을 우회해 preflight 를 GREEN 으로 만들지 않았다.**
  스크립트의 거부가 옳은 동작이다(`CLAUDE.md §6`).

### 2.2 이 RED 가 D-2 의 오라클에 미치는 영향 (미리 적는다)

- `verify-restored.sh ④`(대장 대조)는 복원 뒤에도 **같은 사유로 RED** 다 — 복원과 무관하다.
- `verify-restored.sh ④-b`(복원 전 P8 기록과 동일한가)는 **살아 있다.** `/tmp/pre-digests.tsv` 8줄이
  이미 찍혔고, 이것이 「복원이 아니라 재배포를 한 것」을 가르는 오라클이다.
  ⚠ 다만 그 기록은 **8줄**이라 `migrator:i2`·`alpine:3.20` 은 ④-b 의 대조 범위 밖이다.

## 3. 이 회차가 staging 에 한 일 (전부 · 감추지 않는다)

| 행위 | 등급 | 결과 |
|---|---|---|
| `backup-full.sh` 1회 | additive | 산출물 4종 생성(위 §1.1). 보관처에만 쓴다 |
| `preflight.sh` 1회 | **읽기 전용** | `/tmp/pre-digests.tsv` 8줄 기록 |

**하지 않은 것** — 컨테이너 정지·기동 0회 · `restore-db.sh` 0회 · `restore-volume.sh` 0회 ·
`DROP SCHEMA` 0회 · 볼륨 덮어쓰기 0회 · 손 DDL/DELETE/UPDATE 0건 · `gates/run.sh` 0회.

## 4. 현재 staging 상태 (기록 시점 2026-09-03 00:12 KST)

- 컨테이너 **8개 전부 healthy** · 앱 5종 이미지 `colab-v2/<서비스>:77813d391f94`.
- 원장·볼륨 **무접촉** — 체인 head 는 배포 직후 그대로 **`0011_lv1_drop_level_user_set`**(platform) ·
  `0005_k2b_concept_graph_seed`(ai). `processing_level_user_set` 열은 **부재** 상태 그대로다.
- ⟹ **앱↔스키마 불일치 없음.** staging 에 접속하는 사람이 보는 것은 어제 배포한 릴리스 그대로다.
  단독 창은 **열려 있고**, 다음 행동은 아래 §5 다.

## 5. 다음 수 — Ted 판정 1건이 필요하다

**질문** = 「이미지 digest 대장의 자체 5줄을 지금 서빙 중인 릴리스(`77813d391f94`)의 값으로 갱신할 것인가.」

| | |
|---|---|
| 배경 | 대장의 자체 5줄은 2026-08-27 측정값이고, 그 뒤 배포가 세 번 이 값을 옮겼다. 2026-08-29 에 「소급 불가라 덮지 않는다」로 얼렸다 |
| 지금 | 그 얼린 상태가 복원 사전조건(P8)을 RED 로 만들고, 복원이 시작되지 못한다 |
| 갱신하면 | preflight 가 GREEN 이 되어 복원을 시작할 수 있다. 잃는 것 = 「2026-08-27 이후 무엇이 옮겨졌는가」를 대장 한 장으로 보는 능력(다만 그 이력은 `~/colab-v2-releases/image-digest-ledger.tsv` 에 회차마다 남고 있다) |
| 갱신 안 하면 | 단계 D 는 이 창에서 집행되지 않는다. `R-1` ⑵(복원된 스택에서의 검색 1회)는 계속 미측정 |
| 권고 | **갱신한다.** 되돌리는 비용 = 대장 파일 한 줄씩 되돌리는 커밋 1건. 함께 할 일 = 미선언 2건(`alpine:3.20`·`colab-v2/migrator:i2`)의 값도 이 회차에 적어 대조 8건 → 10건으로 넓힌다 |

**갱신 판정이 나면 D 는 이 회차의 재료를 그대로 쓴다** — P7 백업(`20260903T000540`)과
`/tmp/pre-digests.tsv` 는 이미 서 있다. 단 **`/tmp/pre-digests.tsv` 는 대장 갱신 뒤 다시 찍는다**
(④-b 는 복원 전후 비교라 기록 시점이 복원 직전이어야 한다).

## 6. `R-1` 조건별 현재 상태 (이 회차 기준)

| 조건 | 상태 | 근거 |
|---|---|---|
| ⑴ 크론 무인 연속 3회 GREEN | 충족 | `〈278〉`-㉲ 재실측 5회. 이 회차 미재측정 |
| ⑵ 복원된 스택에서 `POST /searches` (200 ＋ `degraded:false` ＋ 확장 낱말 ≥1) | **열림 — 이 회차도 미측정** | 복원이 시작되지 못했다(§2.1). 모수(복원된 스택)가 아직 없다 |
| ⑶ `preflight` 성질 = 「입력을 다 주면 GREEN」 | **반증 1건 실측** | 입력 넷을 다 줬는데 P8 이 RED — 사유는 입력 부재가 아니라 대장 낡음. **기준을 못 지키는 것은 preflight 가 아니라 대장이다** |
| ⑷ `:i2` 별칭 6종 · `[미측정]` 0 | 충족(2026-09-02 배포 회차) | `WINDOW-20260902-ABC2 §3` 증거 1 |
| ⓒ `RUNBOOK §5` ⑤·③-보 손검사 2건 자동화 | **미착수** | D-4 는 D-2 의 GREEN 을 수용 시험으로 쓴다. 복원 없이 세우면 검증되지 않은 코드가 된다 |

⛔ **`R-1` 을 닫지 않는다.**
