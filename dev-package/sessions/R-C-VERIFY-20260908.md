# R-C · C12 검증 — 디자인 재실측 · intent 대조 · 존치 확인

레인 `r-c` · `integration/r-c` = `7e9d386`(코드 WU 11 병합) · 2026-09-08 · 코드 무접촉(이 파일 1개만 씀).
방법은 `dev-package/sessions/p3-design-audit-20260905.md` 를 그대로 따랐다 — 토큰 대조·`grep -n`·WCAG 2.x 상대휘도 계산.

## A. 디자인 검수 재실측

대상 7종 — `catalog.css`(182) `detail.css`(295) `upload.css`(457) `lineageGraph.css`(103) `preview.css`(383) `project.css`(531) `shell.css`(348).
토큰 정의는 `shell/tokens.css` 한 곳이 아니다 — 화면별 `:root`(`detail.css:5` `catalog.css:5` `upload.css:5,368` `project.css:9` `lineage.css:153` `preview.css:294` `toast.css:5`)의 합집합이 정의 집합이다(관례 축자 = `detail.css:3-4`).

| 항목 | R-B-4 판정표 값 | 지금 값 | 판정 |
|---|---|---|---|
| ⑪ 미정의 토큰 참조(폴백 없음) | 11건 (`lineageGraph.css:30·31·48·49·51·57·83·84`) | **0건** | 해소 |
| ⑪ 미정의 토큰 참조(폴백 있음) | (미계수) | 32건 (`detail.css:123·126·142` · `upload.css:291~341` 6 · `preview.css:20~305` 23) | 선언 유효 · 무해 |
| ⑪ 덮인 선언 `display` | 1건 (`lineageGraph.css:33`) | 0건 (`:29-31` 규칙 재작성) | 해소 |
| ⑩ 음수 margin | 2건 (`detail.css:93` · `shell.css:58`) | **1건 — `project.css:436` `margin: -6px 0 14px`** (`detail.css:86·146` · `shell.css:57` 은 제거 경위 주석) | 잔여 1 = 예상치 일치 |
| ⑩ 자식 `margin-top` (upload) | 9건 (`:95·112·127·130·148·150·171·189·191`) | **5건 — `:348 .partrow` `:357 .up-analyze` `:384 .fieldnote` `:388 .ad-more` `:424 .dr-foot`** (`:83·124·157` = 이관 경위 주석) | 잔여 5 = 예상치 일치 |
| ⑥ 카드 그림자 | 있음 (`catalog.css:31 .card{box-shadow}`) | 0건 — `catalog.css:58` 은 hover `inset`, `:86` 은 `.colmenu` 팝오버(허용) | 해소 |
| ⑦ 계보 그래프 13px 미만 | 13건 (최소 10px) | **0건** | 해소 |
| ⑧ 캡션 눌림 지목 7곳 | 7곳 | 0곳 — `detail.css:123·163` `var(--text-body-sm)` · `upload.css:174 .vizerr` 13px · `.filecard .fn`(`upload.css:136`) 크기 선언 없음 · lineageGraph 0 | 해소 |
| ⑨ 보더 2층 | 미분리·역전 | 분리 — `detail.css:79`(컨테이너 `--color-border-strong`) vs `:82`(칸 `--color-border`) · `catalog.css:36` 바깥선 `strong` 으로 역전 해소(`:35` 축자) | 해소 |

### AA 대비 (해결된 토큰 값에서 계산)

| 선택자 | 글자 / 바탕 | 비 | AA 4.5 |
|---|---|---|---|
| `.lvl-0` (`catalog.css:131`·`detail.css:67`) | `#565c63` / `#e8ecf2` | **5.70:1** | 통과 |
| `.lvl-1` (`:132`·`:68`) | `#0f62e0` / `#edf4ff` | **4.94:1** | 통과 |
| `.lvl-2` (`:133`·`:71`) | `#0f62e0` / `#e2eeff` | **4.66:1** | 통과 |
| `.lvl-3` (`:136`·`:72`) | `#0b4eb6` / `#bad7ff` | **5.13:1** | 통과 (R-B-4 = 규칙 부재) |
| `.lin--none` (`catalog.css:146`) | `#697077` / `#ffffff` | **5.02:1** | 통과 (R-B-4 = 3.41:1 미달) |
| `.aiflag` (`lineageGraph.css:89`) | `#4821db` / `#f1e8ff` | **7.05:1** | 통과 |
| `.aiflag .sp` (`:90`) | `#6742f5` / `#f1e8ff` | **4.82:1** | 통과 |

### 새 hex 0 확인

`git diff main..integration/r-c -- 'frontend/src/**/*.css' | grep '^+' | grep -E '#[0-9a-fA-F]{3,6}'` = **13줄**. 전부 토큰 선언 또는 폴백이다 —
신설 토큰 6(`detail.css:14·15·17·18·19·20` `--color-primary-200 #bad7ff` `-800 #0b4eb6` `--color-accent-50 #f1e8ff` `-200 #d2b6ff` `-500 #6742f5` `-700 #4821db`, 모두 목업 `:root` 축자 · `catalog.css` 중복 2줄 포함) ＋ `var(...,fallback)` 5(`upload.css:291·293·336` `preview.css` 계열 `#e2e2e2`·`#f5f5f4`·`#2f6feb`, 종전 관례 유지) ＋ `BasemapLayer` 획색 폴백 2(`--color-gray-500,#697077` · `-400,#848c94`, 토큰 값과 동일).
**규칙 밖 새 색 = 0건.**

⟹ **A 불일치 = 1건** — `project.css:436` 음수 상쇄 잔여(예상된 잔여). 그 밖 8항목 전부 R-B-4 대비 개선 또는 해소.

## B. intent 「원한 결과」 대조

`2026-09-08-preview-slot.md:92-106` 1~10 · `2026-09-08-r-c.md:29-45` 축 ②.

| # | 원한 결과 | 판정 | 도달 시험 · 근거 |
|---|---|---|---|
| 1 | 네 상태 무점프 자리 선점 | 충족 | `frontend/test/preview-slot-4x3.test.tsx:89,109,114,190` · gate `frontend-test` · `e235f45` |
| 2 | 진행 3단계 문구 | 충족 | `preview-slot-4x3.test.tsx:164` |
| 3 | 못 그림 문면 | 충족 | `preview-slot-4x3.test.tsx:154` · `PreviewPanel.tsx:22` · `DatasetPreviewSection.tsx:415` |
| 4 | 완성되면 같은 자리 | 충족 | `preview-slot-4x3.test.tsx:98,103` (`4/3` · 찌그러뜨리지 않음) |
| 5 | 상세 좌 이미지·우 정보 | 충족 | `detail.css:255-261`(`.dt-split` · 960px 분기 둘) · `:212` sticky · `1c06a35` |
| 6 | 500MB 초과도 그려진다 | 충족 | `preview-pick-and-fallback.test.tsx:157`(413 → files → 첫 renderable 재요청) · `PreviewPanel.tsx:188` · `DatasetPreviewSection.tsx:150` |
| 7 | 파일·변수·시각 선택 | **미달(부분)** | 고르개 셋은 섰다(`preview-pick-and-fallback.test.tsx:179,195,212,226` · `services/viz-render/tests/test_target_describe.py` · gate `service-tests-viz-render`). **시각 후보는 처음·마지막 둘뿐**이다 — `describe.py:75-76` 이 `{count, first, last}` 만 싣는다(8760시각 파일 방어 · `readers.py:328`). 중간 시각은 화면에서 고를 수 없다 |
| 8 | 하나의 축척 기준 | 충족 | `preview/scaleLadder.ts:32` `SCALE_LADDER_KM = [150,400,1200,3000,10000]` · `scale-ladder.test.tsx:95,102,113,125,135` · `91d738a` |
| 9 | 세 화면 확대/축소 | 충족 | `scale-ladder.test.tsx:240,248,256` · `bb4f62c` |
| 10 | 배경 지도 레이어 | 충족 ＋ **초과** | `preview/BasemapLayer.tsx` · `scale-ladder.test.tsx:161`(비지도형 자리 없음). 초과 = 번들 gzip 145.96 → 203.23 kB(**+57.3 kB**, `dev-package/sessions/WU-C5-basemap-20260908.md:13`) |
| ②(G) | 게이트 승격 3종 | 충족 | `gates/run.sh:273 migration-drift` · `:281` selftest · `:286 schema-diff`(준비 = `alembic upgrade head`) · `5cf9c9e`·`e1388a9`·`c416e59` |
| ②(S) | 서버·FE 소형 10건 | 충족 ＋ **초과** | `services/core-api/tests/test_project_name_duplicate.py`·`test_access_state_three.py`·`test_variable_rows.py`(마이그레이션 0020·0021·0022 · gate `service-tests-core-api`·`migration-drift`) · FE 5건 `c351b71`. 초과 = **`granularity` null 도달 불가** — 유일한 기입 경로가 `PeriodCalendarPopover.tsx:86,273`(기본 `'일'`)이라 `UploadModal.tsx:727` `granularity: granularity \|\| null` 의 null 가지가 죽었다 |
| ②(L) | Lv 표시·CSS 6건 | 충족 ＋ **미달** | `frontend/test/lv-display-unify-20260908.test.tsx:106,165` · `.lvl-3`/`.lin--none`/violet 액센트는 A 표 그대로. 미달 = 「여백 컨테이너 이관 잔여(49)」의 마지막 한 자리 `project.css:436` 음수 상쇄가 남았다 |
| ②(K) | 계약 필요 4건 | 충족 ＋ **미달 2** | 계약은 넷 다 섰다 — `fe-core.yaml:4964 byCategory` · `:1582` 사후 「기록 없음」 선언 · `core-ai.yaml:328` 제안 부모 Lv · `fe-core.yaml:4785 ProjectDatasetRow` · `core-viz.yaml:273 describeTarget`(21차) · `test_lineage_unknown.py`·`test_lineage_confirm.py`·`test_variable_rows.py`. 미달 ⑴ **AI 계보 제안에 부모 Lv — 생산자가 없다**: `services/ai-service/src/colab_ai/domains/d10_suggestion.py:19` 「저장이 없다」 · `dev-package/sessions/K3-BUILD.md:76` 「계약대로 선 생산자가 낼 수 있는 참인 답은 언제나 `suggestions: []`」 → 도달 시험은 FE 픽스처(`lv-display-unify-20260908.test.tsx:290,296`)뿐. 미달 ⑵ **`byCategory` 분류 1건 NULL**: `db/ai/versions/0006_rc7_synonym_category.py:35-39` 「`지형·DEM` → `[미상]` NULL 로 남긴다」 |

부수 근거 — 조각 `renderable` 판정은 계약에 조각별 플래그가 없어 **`kind == 본체`** 로 대신한다(`frontend/src/components/preview/pick.ts:60-62`, 생성물 실측 2026-09-08). 값 자체는 계약 근거 안이나 「그릴 수 있는 조각」의 근사다.

⟹ **미달 3건**(⑦ 시각 후보 둘 · ②(L) `project.css:436` · ②(K) 부모 Lv 생산자 부재 ＋ `byCategory` NULL 1) · **초과 2건**(⑩ gzip +57.3 kB · ②(S) `granularity` null 죽은 가지).

## C. 존치 규칙 확인

| 항목 | 파일:줄 | 도달 시험 |
|---|---|---|
| 2단 등록 게이트(접수 ↔ 등록 결정 분리) | `frontend/src/components/upload/UploadModal.tsx:10,763,1080` | `frontend/test/register-steps-20260907.test.tsx` · `upload.test.tsx` (gate `frontend-test`) |
| 기준 격자 파일 흐름 | `frontend/src/components/upload/GridUploadBlock.tsx` · `upload/gridFlow.ts` · 마운트 `PreviewPanel.tsx:577` | `frontend/test/rev1-keep-regression.test.tsx` |
| 이어올리기 배너 | `UploadModal.tsx:354,916` · `upload/UnfinishedUploads.tsx:88` | `frontend/test/upload.test.tsx` |
| `PreviewExpandOverlay` (`data-esc-layer`) | `upload/PreviewExpandOverlay.tsx:6` · 마운트 `PreviewPanel.tsx:597-639` · 판정 `UploadModal.tsx:643` · `upload/escLayer.ts` | `frontend/test/scale-ladder.test.tsx:256`(확장보기 층 규칙 그대로) |
| AI 계보 제안 | `lineage/LineageStep.tsx:126,136,147` · `lineage/lineageSource.ts:23` · 표기 `LineageSection.tsx:157` | `frontend/test/lv-display-unify-20260908.test.tsx:290`(부모 Lv 충돌 안내) |
| 값 조회 패널 | `datasetpreview/ValueLookupPanel.tsx:46` `lookupValue` · 마운트 `DatasetPreviewSection.tsx:398` · 계약 `types.ts:82` | `services/core-api/tests/test_preview_relay.py` (gate `service-tests-core-api`) |
| PRD-260905:385 장면1 무변 | `UploadModal.tsx:1016` `{picked.length > 0 && (` — 좌우 2단은 여전히 파일 선택 뒤에만 선다 | `preview-slot-4x3.test.tsx:89`(「그리기를 누르기 전에 틀이 있다」 = 장면2 기준) |

⟹ **C 7항목 전부 존치 · 도달 경로 있음. 걷힌 것 0건.**
