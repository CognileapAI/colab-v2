# P2 라운드 마감 — 다음 세션 인계서 (2026-08-27)

> **이 파일은 인계서다. 진실원은 `03-HANDOFF.md` 이고 값과 근거는 `PLAN-SoT.md §9` 에 있다.**
> 여기 있는 것은 **어디부터 손대야 하는가** 하나다. 두 곳에 같은 값을 적지 않는다.

---

## 0. 먼저 — 갈래가 하나다

**`main` = `worktree-p2-exec` = `c7878bc`.** 2026-08-27 에 fast-forward 로 합쳤다.

세션 초반의 「main 이 100커밋 뒤처져 있고 그쪽으로 배포하면 조용히 롤백된다」는
상태는 **없다**. 어느 갈래로 읽어도 같다.

⚠ **로컬 `main` 체크아웃은 아직 `ada8fa0` 일 수 있다** — 원격만 올렸다.
그쪽에서 `git pull --ff-only` 한 번이 필요하다.

---

## 1. 지금 어디까지 왔나 — 한 줄

**stage 1 완료 조건 8항 중 6항 ✅. 남은 둘은 미달이 아니라 범위 판정이다.**

| | 조건 | 판정 |
|:--:|---|---|
| ✅ | 1 에픽·스토리 | `〈150〉` 으로 결손 0 |
| ✅ | 2 화면 | 설명 결손 **0/12** |
| ✅ | 3 도메인 10개 | `〈149〉` 대조 완료 (D8 만 stage 2) |
| 🟧 | 4 AI 두 지점 | **`〈136〉` 으로 이번 릴리즈 범위 밖** — 평가셋·회귀 게이트는 살아 있다 |
| ✅ | 5 경계 장치 | 게이트 red **0** |
| ✅ | 6 staging 배포 | 헬스 6/6 · head `0007` |
| 🟧 | 7 실데이터 통과 | 계보 확정 완주 = **stage 2**(`〈70〉-㉮`) |
| ✅ | 8 초기 데이터 | 12건이 화면에서 보인다 |

**staging 실측** — 데이터셋 **12** · 파일 **129**(본체 123 ＋ 격자 6) · 계보 **6** ·
**설명 결손 0** · head `0007_p2_human_written_meta` · 헬스 6/6 · 컨테이너 8/8.

**시험** — core-api **445/0** · frontend **274/0** · ai-service **91/0** ·
pipeline-worker **143/1**(1 은 [미상] 아님 — 아래 §4 ⑤).

---

## 2. 착수 순서 — 위에서부터

### ⛔ ① 권한 차단 2건 — **판단이 아니라 허락이다. 스크립트를 다시 짜지 마라**

| 건 | 무엇 | 준비 상태 |
|---|---|---|
| `〈125〉` | `d6_project_dataset` **고아 1행 삭제** — 가리키는 데이터셋이 **존재하지 않는다**(실측) | 선백업 포함 |
| `〈124〉` | staging 주체 표에서 **픽스처 연구실 A·B 3건 회수** | 원본 백업 → 제거 → **남는 주체 0건이면 정지** |

둘 다 **하네스 분류기가 막았다**(staging DB `DELETE` · 자격 파일 수정). **우회하지 않았다.**
Ted 의 사전 승인은 받아 두었다 — 필요한 것은 Bash 권한 허용이거나 사람이 직접 실행이다.

⚠ **`〈124〉` 는 `〈93〉` 이 우려한 상태가 아니다** — 레포 픽스처 토큰 3건과 staging
토큰 4건이 **하나도 안 겹친다**(sha256 대조). 남은 것은 「테스트 연구실 3계정이
staging 에 로그인 가능」이고 **심각도가 한 단계 낮다.**

### ⛔ ② `〈121〉` DB 비밀번호 — **반쪽으로 시작하지 마라**

회전(㉮)과 파일 참조 전환(㉯)을 **함께** 해야 한다. 회전이 위와 같은 계열로 막히면
**파일 참조 전환만 해서는 누출된 값이 살아 있는 채로** 5개 단위의 DB 배선만 바뀐다.

### 🟧 ③ 판정 1건 — `#21` 계약의 `addDatasetFile 202`

**결함이 아니다. 걷을 것인가만 정하면 된다.**

- 계약이 **스스로** 적어 뒀다 — 「⛔ 아래 `202` 는 집행이 없는 응답으로 남아 있다 —
  **감추지 않고 적는다**」(`fe-core.yaml:857`)
- **소비자 0건** — FE 는 이 op 을 부르지 않는다. 격자 후주입은 `attachUploadGridFiles` 다
- 서버는 400 으로 **갈 곳까지 알려 준다**(`〈66〉` 「축을 지어내지 않는다」)
- 계약이 「철회는 동결 해제 판정 사안 · **다음 회차의 진입조건**」이라 적었고
  **그 회차가 지금**이다

**권고 = 걷는다.** 실현 안 되는 약속을 계약이 들고 있으면 **다음 사람이 그것을
구현하려 든다.**

### ④ 그다음이 stage 2

`〈70〉`·`〈78〉` 이 가른 범위 — 가공(파싱·좌표계·COG) · 타일·확대 · 스크린샷 ·
S-08 · 값 조회 · 편의 기능 묶음. **stage 1 은 위 셋이 닫히면 끝난다.**

---

## 3. 이 회차에 바뀐 것 중 **오해하기 쉬운 것 다섯**

### ⑴ `Q-04` 는 **의도적으로 red** 다 — 결함으로 알고 고치지 마라

`〈137〉`·`〈143〉`. 자연어 검색 UI 는 닫혔지만 **평가셋과 회귀 게이트는 계속 돈다** —
Phase 2 착수 시점에 **「지금 몇 점인가」를 아는 채로** 시작하기 위해서다. 그 값은
**A층 8/16** 이다. 지금 green 으로 만들면 **Phase 2 가 계기판이 고장 난 채 시작한다.**

⭑ 그리고 `Q-04` 의 **실패 성격이 바뀌었다**(`〈143〉`) — 과확장이 아니라
**`Nearest` 의 요약 366번째 글자에 「Bilinear」가 실제로 있다.** 다음에 볼 것은
「확장 방향」이 아니라 **「요약 일치를 어떻게 순위로 눌러 앉힐 것인가」**다.

### ⑵ 검색은 **낱말 그대로**가 정상 동작이다 — `degraded` 가 아니다

`〈136〉`·`〈148〉`. LLM 해석은 **설정으로** 꺼져 있다
(`COLAB_AI_QUERY_INTERPRETATION=literal`, `compose.i2.yml` 에 명시). **키를 빼서
끄지 않았다** — 「키를 못 넣은 것」과 「안 쓰기로 한 것」이 같은 상태로 보이면 안 된다.

⚠ **온톨로지 사전 확장은 계속 돈다** — `천리안위성2A호` 가 7건을 찾는다(그 말은 어느
데이터셋 이름에도 없다). **LLM 만 껐고 사전은 살아 있다.**

### ⑶ 지원 포맷은 **6종**이고 GRIB 은 **그리지 않는다**

`〈134〉`·`〈135〉`. `NetCDF · Binary · HDF4 · GeoTIFF · NumPy · GRIB`.
**숫자로 세지 말고 목록으로 읽어라** — 정본 5종과 `〈77〉` 5종이 **수는 같고 구성이 달랐다.**

⚠ **viz-render 의 같은 이름 목록에 GRIB 을 넣지 마라** — 그쪽은 「그려 낼 수 있는 것」이고
`renderableFormats` 로 나간다. 넣으면 **화면이 거짓을 말한다.** 맞출 상대는
`RENDERABLE_FORMATS` 다(그 파일 주석에 박아 뒀다).

### ⑷ Lv 은 **깊이가 아니라 종류**다 — 상한 2 는 정본이 준 값이다

`〈133〉`·`〈140〉`. `VAL-005`·`POL-020`. 계산 결과는 **저장하지 않고**
사람이 손댄 것만 `processing_level_user_set` 에 담는다 — `⑳` 을 깨지 않는다.

⚠ **기록 없음의 값 = `Lv0`(가공 안 함) / `Lv1`(가공함)** — Ted 판정(2026-08-27).
정본 `VAL-005` 의 「기본 Lv2」는 **목업의 데이터 타입별 예시에서 나온 값**이라 채택하지 않았다.

### ⑸ `〈120〉` 은 **하지 않기로 한 것**이다 — 미실행이 아니다

`〈145〉`(Ted: 「그대로 둬」). 만료 회수가 `registered_at IS NULL` 을 요구하는데
대상 3건은 **전부 등록돼 있어 기다려도 안 사라진다.** `d5_upload` 3행의
`형식 인식 실패` 기록은 **결함이 아니라 `〈114〉` 이전의 흔적**이다.

---

## 4. 이 회차에 **내가 틀렸던 것** — 같은 데서 미끄러지지 마라

**세 번 과하게 닫았고 셋 다 실물 확인에서 잡혔다. 셋 다 「값」이 아니라 「구조」를 덜 본 것이다.**

| | 무엇 | 어떻게 드러났나 |
|:--:|---|---|
| ⓐ | `#36` 을 **수단 배포만으로** 닫았다 | DB 를 열어 보니 `summary` 가 **여전히 `NULL` 2건**이었다. **수단을 만든 것과 채운 것은 다르다** |
| ⓑ | `#30` 을 **API 확인으로** 닫고 「화면」이라 적었다 | 조건은 「**브라우저에서** 카탈로그 12건」이다 |
| ⓒ | 블로커 표를 **헤더만 고치고** 본문을 안 고쳤다 | **표가 거짓을 말했다** — `#28`「데이터셋 0건」·`#20`「미리보기 안 뜬다」가 이미 해소됐는데 ⛔ 로 남아 있었다 |

그 밖에 **확인 방법이 틀린 적이 두 번** 있었고 둘 다 스스로 잡았다 —
백업 가드가 죽은 줄 알았으나 **검사기가 접미사 없는 변수를 읽는** 배선을 못 읽은 것이었고,
픽스처 토큰이 겹친 줄 알았으나 **주체 표는 토큰이 열쇠**고 겹친 것은 신원 값이었다.

**⑤ pipeline-worker 1 실패의 정체** — `COLAB_REFERENCE_DATA` 없이 돌리면 실데이터
E2E 가 **fail-closed** 된다(skip 이 아니다, 설계대로). 원천을 물리면 green 이다.

---

## 5. 시험을 돌리려면 — 변수 이름이 단위마다 다르다

```
core-api        COLAB_CORE_TEST_DATABASE_URL  ＋ COLAB_CORE_TEST_SUBJECTS_FILE
ai-service      COLAB_AI_TEST_DICT_DB_URL          ← `COLAB_AI_DB_URL` 이 아니다
pipeline-worker COLAB_PIPELINE_DB_URL         ＋ COLAB_REFERENCE_DATA
schema-diff     COLAB_APPLIED_DB_URL_PLATFORM ＋ COLAB_APPLIED_DB_URL_AI
```

**없으면 error 로 서고, 그 error 를 skip 으로 바꾸지 않는다.**

일회용 DB 절차는 `RESTART.md §2` 와 `sessions/D3-db.md §3` 에 있다 —
`postgres:16-alpine` ＋ `--tmpfs` ＋ **포트 미공개**(staging 과 충돌하지 않는다).
마이그레이션은 **staging 과 같은 `colab-v2/migrator:i2` 이미지**로 돌린다.

---

## 6. 되돌리는 길

- **롤백 태그** = `colab-v2/<단위>:p2round-prev` **6/6** (배포 전 `:i2` 판)
- **백업** = `~/colab-v2-backups/staging/` — 이 회차에 3회 GREEN.
  최소 행 수 합격선은 `〈144〉` 로 **platform 190 · ai 45**(실측의 절반)
- ⚠ **살아 있는 staging 을 제자리 복원하는 절차는 레포에 없다** — 복원 리허설은
  **일회용 인스턴스 전용**이다(`restore-rehearsal.sh:5`). 실사고 시 복원 명령은
  사람이 조립해야 한다. **열린 자리다**

---

*작성 2026-08-27. 이 파일은 인계 전용이고 판정을 담지 않는다 — 판정은 `PLAN-SoT §9`.*
