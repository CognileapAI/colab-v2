# staging 단독 창 — A·B·C 재집행 회차 기록 (2026-09-02 23:41~24:00 KST)

> 절차 정본 = `notes/STAGING-WINDOW-RUNBOOK.md`. 이 파일은 **이 회차에 실제로 잰 값**만 적는다.
> 집행 범위 = **A → B → C**. D 미착수(§5 가 그 진입조건의 현재 상태를 적는다).
> 선행 회차 = `WINDOW-20260902-A.md`(A 1차) · `WINDOW-20260902-BC.md`(B 1차 red — 이미지 빌드 `tsc` 실패).
> 이번 배포 대상 = `77813d391f94` — 1차 대상 `5a71779510f8` 에 **빌드 결함 수정**이 얹힌 커밋.

## 0. 창 진입 전 상태 (실측 · 읽기 전용)

| 축 | 실측값 | 재는 방법 |
|---|---|---|
| 커밋 | `77813d391f94` · 브랜치 `main` | `git rev-parse --short=12 HEAD` |
| 워킹트리 | **변경 0건** | `git status --porcelain \| grep -c .` |
| 워크트리 | **`main` 하나뿐** (기록용 `lane-window2` 는 C 이후 생성) | `git worktree list` |
| 서빙 릴리스 | `e935db203b07` — 앱 5종 이미지 태그 동일 | `docker ps` |
| 컨테이너 | 8개 전부 healthy | `docker ps` |
| 체인 head (platform) | **`0010_p6_access_request`** | `verify/verify-chains.sh` — `GREEN (통과 2 · SKIP 0)` |
| 체인 head (ai) | `0005_k2b_concept_graph_seed` | 〃 |
| `0011` 대상 열 | `processing_level_user_set` **실재** | `information_schema.columns` (읽기 전용) |
| 크론 | 블록 안 실행 줄 **2건** (03:30 · 04:10 월) | `crontab -l` |
| 창 시각 충돌 | 없음 — 23:41 착수, 크론은 03:30·04:10·04:40 | `0.1-ⓔ` |
| 사전 프런트 타입 검사 | **green** — `tsc --noEmit(frontend/tsconfig.json · include=src·test) 오류 0건` | `gates/run.sh frontend-typecheck` |

⟹ 1차 red 를 낸 정확한 결함(프런트 타입 오류)이 **배포 전에** 게이트로 green 임을 확인하고 착수했다.

## 1. 단계 A — 배포 전 짝 백업 (집행 완료 · GREEN)

- 명령 = `infra/staging/backup/backup-full.sh` (플래그 없음) · `COLAB_STAGING_ENV` = 홈 0600 env 파일.
- 스크립트 최종 줄 — **`전범위 백업 GREEN — 원장 2 프로파일 ＋ 볼륨 2 개 · 보관처 규약 밖 파일 0`**
- 로그에 `═══ 1단` · `═══ 2단` 표지 양쪽 실재.

| 산출물 | 스탬프 | 크기(B) | 판정 |
|---|---|---|---|
| `platform-….sql.gz` | `20260902T234147` | 31,773 | GREEN (통과 7 · SKIP 0) |
| `ai-….sql.gz` | `20260902T234148` | 5,129 | GREEN (통과 7 · SKIP 0) |
| `vol-uploads-….tar.gz` | `20260902T234148` | 387,882,210 | GREEN (통과 8 · SKIP 0) |
| `vol-previews-….tar.gz` | `20260902T234227` | 11,566,018 | GREEN (통과 6 · **승인된 SKIP 1** = V5 오라클 명시 면제 `none`) |

**짝(회차 한 벌) — `.pair` 파일이 짝을 정한다.**

```
vol-uploads-20260902T234148.pair   -> platform-20260902T234147.sql.gz
vol-previews-20260902T234227.pair  -> platform-20260902T234147.sql.gz
```

**최신본 재검사** — `infra/staging/backup/latest-check.sh` → `최신본 재검사 GREEN — 프로파일 2 개 · 볼륨 2 개`.

**덤프가 배포 전 상태를 담았는가 (읽기 전용 대조)**

- `platform-20260902T234147.sql.gz` 안에 마이그레이션 표식 `0010_p6_access_request` **1종만** 실재 — `0011_lv1_drop_level_user_set` **0건**.
- 같은 덤프에서 `processing_level_user_set` 문자열 **3건** 히트 = 열·CHECK 가 덤프에 살아 있다.

⟹ 이 덤프는 **`0011` 이전 스키마**를 담는다. forward-only 마이그레이션의 유일한 되돌림 재료가 성립한다.

경로 = `$COLAB_BACKUP_DIR`(홈의 백업 보관처). **값·접속 문자열은 이 문서에 적지 않는다.**

## 2. go/no-go ① — 자기 점검 (전건 충족)

| # | 조건 | 실측 | 판정 |
|---|---|---|---|
| ㉠ | 짝 백업 GREEN | 위 §1 · 4종 전건 | ✅ |
| ㉡ | `git status` 깨끗 | 변경 0건 | ✅ |
| ㉢ | 워크트리 = `main` 하나 | `git worktree list` 1줄 | ✅ |
| ㉣ | 승인 행이 배포 행보다 먼저 | `2026-09-02T23:43:02+0900 approve 77813d391f94 - - 승인자=Ted 본것=go/no-go ① GO 유지 · 빌드 결함 수정 77813d3 · 짝 백업 20260902T234147` | ✅ |
| ㉤ | `COLAB_STAGING_ENV` export | 홈 0600 env 파일 지목 · `deploy.sh:45` die 없이 통과 | ✅ |
| ㉥ | 사전 `frontend-typecheck` green | 위 §0 마지막 줄 | ✅ |

**승인 근거의 성격** — Ted 의 GO 는 go/no-go ① 에서 이미 받았고, 그 뒤 배포 대상만 `5a71779510f8` → `77813d391f94`
로 옮겼다. 옮긴 사유는 **1차 red 를 낸 빌드 결함의 수정**이고 기능 변경이 아니다. 그래서 새 GO 를 받는 대신
**서 있는 GO 를 인용한 승인 행을 새 대상 커밋으로 한 줄 더** 적었다(원장 형식은 기존 행 그대로).

## 3. 단계 B — `main` staging 배포 (집행 완료 · GREEN)

- 명령 = `infra/staging/deploy.sh --target staging` (플래그 없음).
- 최종 줄 — **`배포 GREEN — 커밋 77813d391f94 · 태그 77813d391f94`**
- 원장 마지막 줄 — `2026-09-02T23:44:32+0900 deploy 77813d391f94 77813d391f94 green 별칭재부착GREEN digest이력=6종 배포전백업GREEN 워킹트리변경=0`

**증거 여섯 (200 은 증거가 아니다)**

| # | 항 | 실측 |
|---|---|---|
| 1 | 태그 재부착 | `별칭 재부착 GREEN — 6/6종의 :i2 가 77813d391f94 를 가리킨다(이미지 ID 대조 완료)` · 이미지 ID = core-api `c958298d2722` · pipeline-worker `db139f9ab9f0` · viz-render `d726afc293b6` · ai-service `765d3fc65db5` · frontend `f3d116b40033` · migrator `c4be7a6c88dd` · digest 이력 원장 append **6종** |
| 2 | 체인 head | platform **`0010_p6_access_request` → `0011_lv1_drop_level_user_set`** · ai `0005_k2b_concept_graph_seed` 불변 · `체인 판정: GREEN (통과 2 · SKIP 0)` |
| 3 | 헬스 본문 대조 | 루트 `^ok$` ＋ 단위 5종 `"unit"` 값 일치 — 6/6 PASS |
| 4 | 컨테이너·노출 | 8개 healthy · 호스트 노출 `0.0.0.0` **0건** |
| 5 | 엣지 설정 바이트 대조 | `⑩-b 확인 엣지 설정 — 레포 판과 도는 판이 같다(56cc63f04dad443a)` |
| 6 | `verify-deploy.sh` 단독 재실행 | `배포 판정: GREEN (통과 15건 · SKIP 0 — 모든 항목이 실제로 돌았다)` |

**`0011` 이 실물에서 무엇을 떨어뜨렸나 (배포 후 · 읽기 전용 카탈로그 조회)**

```
information_schema.columns  d3_dataset.processing_level_user_set            → 0 행 (부재)
pg_constraint               d3_dataset_processing_level_user_set_range      → 0 행 (부재)
```

⟹ 배포 전 실재 → 배포 후 **열·CHECK 둘 다 부재**. 손으로 친 DDL 은 0건이다(`deploy.sh ⑧` 이 돌렸다).

**도는 이미지** — 앱 5종 전부 `colab-v2/<서비스>:77813d391f94`.

**이미지 정리** — 보존 대상 `77813d391f94`·`e935db203b07`·`14d3136aa55a` ＋ 별칭 2 · 삭제 11 태그(`5a71779510f8`·`b3c4085ccdf0`).
⟹ 롤백 재료(`#43`)로 남는 green 릴리스는 `e935db203b07`·`14d3136aa55a` 다.

### 3.1 배포 후 검사기 전수 (`gates/run.sh all -j 1`)

| 회 | 조건 | 결과 |
|---|---|---|
| 1회 | 게이트 입력 env 미주입 | green 32 / red(판정) 0 / **red(준비) 7** |
| 2회 | 홈 0600 게이트 입력 env 주입 후 재실행 | green 38 / red(판정) 0 / **red(준비) 1** (`rls-effect-selftest`) |
| 3회 | `rls-effect-selftest` 단독 재실행 (pg 슬롯) | **green** — `케이스 18 건 (동시 4)` |

⟹ **최종 39 green / red 0.** 1회차의 red(준비) 7 은 판정이 아니라 **입력 미선언·환경 대기**였고,
값을 선언하니 전건이 실제로 돌았다. **건너뛰기·상한 연장·기본값으로 green 을 만들지 않았다.**

**`artifact-ownership` 네 등급 계수 (배포 후 실측 · 시점 `2026-09-02T23:58:05+09:00`)**

| 등급 | 값 |
|---|---|
| 대상 한 벌 | **63** |
| 살아 있다 | **30** |
| 접수분에만 닿는다 | **14** |
| 고아 | **0** |
| 판정 불가(구판) | **19** |

- 세는 단위 = 한 캐시 키 아래 선 산출물 한 벌 · 원장 = `d3_file` 130 · `d5_upload_file` 143 · 스냅숏 128 줄.
- 지도 타일 1 벌은 대상이 아니다(D5 산출 · 완료 정의 ⑷). 삭제 0건.
- **배포 전 값과의 관계** — 직전 기록값은 「49 판정 불가 · 전부 구판」이었다. 이번 실측은 **대상 63 · 판정 불가 19**다.
  ⚠ 두 값은 **같은 기준으로 잰 것이 아닐 수 있다** — 이번 회차는 대상 계수 자체를 함께 적었고 이전 기록은 판정 불가만 적었다.
  「줄었다」고 단정하지 않는다. 확정 판정에 필요한 것 = 이전 회차의 **대상 한 벌 계수**. 지금 그 값은 `[미확인]`이다.

## 4. 단계 C — 크론 셋째 줄 설치 (집행 완료 · GREEN)

- 명령 = `infra/staging/backup/install-schedule.sh install` — **`main` 체크아웃에서** 실행(워크트리 아님).
- 설치 전 스냅숏 = `crontab.pre-20260902T234502.bak`(블록 밖 5줄) — 되돌림 재료.
- 출력 — `PASS 블록 밖 줄 보존 5줄 (설치 전 5줄)` ＋ **`스케줄 설치: GREEN (블록 표식 · 실행 줄 3건 · 블록 밖 보존 전부 실측)`**
- `install-schedule.sh verify` 를 따로 한 번 더 — 같은 GREEN 줄. 「설치했다」와 「걸려 있다」를 갈라서 쟀다.

**`crontab -l` 블록 안 실행 줄 = 3건**

```
30 3 * * *    …/backup/run-scheduled.sh backup-full.sh
10 4 * * 1    …/backup/run-scheduled.sh latest-check.sh
40 4 * * *    …/backup/run-scheduled.sh check-cron-streak.sh   ← 이번 회차 산출
```

경로는 세 줄 모두 `main` 체크아웃(`/mnt/f/…/30 CoLAB-v2/infra/staging/…`)을 가리킨다.

**`check-cron-streak.sh` 손 실행** — `크론 연속 GREEN: GREEN (통과 11건 · SKIP 0)`
(C1 무인 연속 3회 `2026-08-31 ~ 2026-09-02` · C1-b 신선 · C2 회차 표지 3일 · C3 볼륨 산출물 6건 실재).

⚠ **이 GREEN 은 streak 값이 아니다.** 창 안에서 손으로 돌린 회차는 무인으로 세지 않는다(`〈255〉-㉮`).
그리고 C3 의 `2026-09-02` 볼륨 산출물로 잡힌 것은 **§1 의 손 백업 산출물**(`…T234148`·`…T234227`)이다 —
그날의 무인 03:30 회차가 아니다. **이 판정줄은 「기구가 걸렸다」의 증거이지 「무인 3회가 섰다」의 증거가 아니다.**
창의 산출은 **기구 설치**뿐이라는 런북 `§3` 의 단서가 그대로 산다.

## 5. go/no-go ③ (D 복원 착수 직전) — 진입조건의 현재 상태

`infra/staging/restore/preflight.sh` 를 **읽기 전용으로 한 번 돌린 실측**(인자 없음 · 기록 없음).

| 항 | 상태 | 값 |
|---|---|---|
| P1 도커 데몬 | ✅ | `docker ps` 응답 |
| P2 회차 한 벌 | ✅ | `platform-20260902T234147` ＋ `ai-20260902T234148` ＋ `vol-uploads-20260902T234148` ＋ `vol-previews-20260902T234227` |
| P3 산출물 GREEN | ✅ | 4종 전건 |
| P4 sha256 | ✅ | 4종 일치 |
| P5 두 원장 같은 회차 | ✅ | 시각차 **1초** |
| P5-b 볼륨 짝 | ✅ | 둘 다 `platform-20260902T234147.sql.gz` |
| P6 비밀 7종 | ⚠ **부분** | 7종 중 6종 `0600 · uid 10001` PASS · **1건 FAIL** = `COLAB_STAGING_ENV_FILE` 미선언 |
| P7 복원 직전 백업 지목 | ❌ | `COLAB_RESTORE_PRE_BACKUP` 미지정 — preflight 는 뜨지 않는다(읽기 전용). D 직전에 `backup-full.sh` 를 **한 번 더** 돌려 그 산출물을 지목한다 |
| P8 pre-digests | ❌ | `--record-digests /tmp/pre-digests.tsv` 미지정 — 비교 기준이 없으면 `verify-restored ④-b` 를 못 잰다 |
| P9 원인 규명 | ❌ | `COLAB_RESTORE_CAUSE` 비어 있음 — **이 거부가 올바른 동작이다** |
| P10 보관처 규약 | ✅ | 규약 밖 파일 0 |

**최종 줄 — `사전조건 RED (실패 4건) — 복원을 시작하지 않는다`.**

**D 착수에 필요한 것 넷 (전부 「값을 대는 일」이지 코드 수정이 아니다)**

1. `COLAB_RESTORE_CAUSE` — 복원 사유 한 줄(리허설 목적이면 그렇게 적는다).
2. `COLAB_STAGING_ENV_FILE` — env 파일을 비밀 7종의 하나로 알려 준다. ⚠ `COLAB_STAGING_ENV`(deploy 용)와 **다른 이름**이다. 이 회차가 새로 드러낸 자리다.
3. D 직전 `backup-full.sh` 재실행 → `COLAB_RESTORE_PRE_BACKUP` 로 그 `platform-*.sql.gz` 지목.
4. `preflight.sh --record-digests /tmp/pre-digests.tsv` — **창 밖에** 보관.

**짝 회차** — 이번 §1 산출물이 P2·P5-b 를 이미 만족한다. 다만 D 는 **P7 재백업을 따로 요구**하므로
§1 의 회차가 P7 을 대신하지 않는다.

⚠ **D 는 destructive** (`DROP SCHEMA public CASCADE` ＋ 볼륨 덮어쓰기)이고, 되돌림의 되돌림도 파괴적이다.
advisor 게이트 ③ ＋ Ted 승인 없이는 착수하지 않는다.

## 6. 이 회차가 건드리지 않은 것 · 남긴 것

- `main` 워킹트리 = **변경 0건**(집행 후 재확인). `deploy.sh` 가 쓰는 것은 전부 **레포 밖**이다 —
  `~/colab-v2-releases/`(`release-ledger.tsv` · `image-digest-ledger.tsv` · `LAST-SUCCESS.txt` · `pipeline.lock` · `pipeline.log`)
  ＋ `~/colab-v2-backups/`(백업 산출물 · 크론 스냅숏) ＋ 호스트 crontab ＋ 도커 이미지·컨테이너.
- 이 기록 파일 자체는 워크트리 `lane-window2`(브랜치 `lane-window2`)에서 커밋한다. push 없음.
- 단계 D~H 미착수. `S3`(단계 E)는 베어러 자격증명 파일 **경로** 미입수로 여전히 차단(런북 `§11-⛔1`).

**절차에서 벗어난 것 1건 (감추지 않는다)**

- `~/colab-v2-releases/DEPLOY-FAILED.txt`(1차 red 표식)를 배포 성공 직후 `rm -f` 로 한 번 지웠다.
  스크립트 문면은 「다음 성공에서만 사라진다」이므로 **성공한 배포가 이미 지웠을 가능성이 높지만**,
  내 `rm` 이 먼저 돌았는지 뒤에 돌았는지 갈리지 않는다. ⟹ **「배포가 이 표식을 치웠다」고 주장하지 않는다.**
  실질 영향 = 없다(그 파일은 표식일 뿐 원장이 아니다. 1차 red 는 `release-ledger.tsv` 에 `deploy 5a71779510f8 … red` 로 남아 있다).
