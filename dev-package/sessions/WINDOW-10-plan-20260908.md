# 창 10 배포 계획 — staging 리허설 → dev 판정 (2026-09-08 · 대상 `main` = `ccf76ec`)

- 작성 = 조사만. **이 문서를 쓰는 세션은 배포·ssh·docker 를 실행하지 않았다.**
- 선례 = 창 9 (`dev-package/sessions/R-C-ROUND-20260908.md §9` · 원장 `PLAN-SoT §9 〈378〉` · 로그 `dev-package/reports/R-C/deploy/`).
- 파일명 규약 주 = 선례는 `WINDOW-<YYYYMMDD>-<라벨>.md` 형식이고 번호 파일은 없다. 이 파일만 지시받은 이름을 쓴다.

## 0. 전제 실측

| 값 | 실측 | 근거 |
|---|---|---|
| `main` HEAD | `ccf76ecf0bb53fa100088a36bcc112ae953ce3b9` (short12 `ccf76ecf0bb5`) | `git rev-parse HEAD` |
| `origin/main` | 동일 sha · 워킹트리 clean | `git rev-parse origin/main` · `git status --porcelain` 0행 |
| dev `CURRENT_SHA` | `83d3eb52cb1f` (창 9 적용분) | `〈378〉` ④ `up: ok (sha 83d3eb52cb1f)` |
| dev `MAIN_SHA` | **없다** — `ship.sh` 의 이 기능이 `ccf76ec` 에서 처음 실린다 ⟹ 지금 doctor 를 돌리면 ⑮ 는 「파일 없음 ✗」 | `git diff --stat 83d3eb5..HEAD -- infra/dev/ship.sh` |
| `83d3eb5..ccf76ec` 코드 변화 | 7파일 — `infra/dev/{README.md,ship.sh,tag-release.sh,tests/ship-gate.sh}` · `infra/staging/deploy.sh` · `services/core-api/ops/deploy_doctor.py` · 그 시험 | `git diff --stat` |
| 마이그레이션 변화 | **0건** — `db/` diff 공집합. 두 체인 head 는 창 9 적용값 그대로 = platform `0022_rc7_variable_mirror_stmt` · ai `0007_merge_vocab_and_category` | `git diff 83d3eb5..HEAD -- db/` · `db/{platform,ai}/versions/` |
| 프론트 변화 | 0건 ⟹ `ops/deploy_web.py` 재배포 **불필요** | 위 diff |
| staging 현재 상태 | `[미상]` — 컨테이너 8개 가동 여부는 이 세션이 재지 않았다(`docker` 미실행) | `03-HANDOFF.md:502` 판정 기준 |
| 시크릿·env (이름만) | staging `~/.colab-v2-staging.env`(0600) · dev EC2 `/opt/colab-v2/dev.env`(0600) ＋ `/etc/colab/` 파일 7 · 운영자 키는 `/tmp/op.env` 로 잠깐 넘기고 **직후 삭제** | `RESTART.md §2-①` · `infra/dev/README.md` 환경변수 표 · `docs/DEPLOY.md §6-1` |
| ssh 전제 (이름만) | `COLAB_DEV_SSH` · `COLAB_DEV_KEY_FILE` — 값은 `~/.config/colab-platform/dev-operator.env` · 키 `dev-key.pem` | `infra/dev/ship.sh:5-6` · `reports/R-D/d4-judgment-facts-20260908.md §1` |
| dev EC2 | `t4g.medium` (실측) — `docs/DEPLOY.md:160` 의 `t4g.small` 은 낡음 | 같은 파일 §1 |

## 1. staging 리허설 (판정 아님)

⛔ **staging 에서 완료 판정·배포 창 기록·사용자 확인을 하지 않는다** (`CLAUDE.md §0` · `03-HANDOFF.md:491` `〈335〉`-㉯).

| # | cwd | 명령 | 통과 증거 | 중단 조건 |
|---|---|---|---|---|
| S1 | 레포 루트 | `docker ps --filter name=colab_v2_staging --format '{{.Names}}\t{{.Status}}'` | 8개 전부 `healthy` | 8 미만이면 `RESTART.md §2-②` 한 줄로 기동 — ⚠ **`-f infra/staging/compose.i2.yml` 을 반드시 붙인다**(빼면 자리표시 오리진이 떠 I2 가 조용히 롤백되고 **루트 헬스는 200 을 낸다**) |
| S2 | 레포 루트 | `git status --porcelain \| wc -l` · `git rev-parse --short=12 HEAD` | `0` · `ccf76ecf0bb5` | 0행이 아니면 중단. `--allow-dirty` 를 쓰지 않는다 |
| S3 | 레포 루트 | `infra/staging/pipeline/approval/approve.sh Ted "<눈으로 본 것>"` | 원장 마지막 행 `approve ccf76ecf0bb5 - - 승인자=Ted …` | 「본 것」 칸이 비면 스크립트가 거부(exit 64) — 그것이 옳은 동작 |
| S4 | 레포 루트 | `infra/staging/deploy.sh --target staging` | `배포 GREEN — 커밋 ccf76ecf0bb5 · 태그 ccf76ecf0bb5` · exit 0 | 어느 단계든 `abort` ⟹ exit 70. `--allow-dirty`·`--skip-backup`·`--skip-alias-reattach`·`--auto-rollback` **전부 사용 금지** |
| S5 | 레포 루트 | `tail -1 ~/colab-v2-releases/release-ledger.tsv` | `deploy ccf76ecf0bb5 ccf76ecf0bb5 green … 워킹트리변경=0 **브랜치=main**` | `브랜치=` 필드 부재 = 낡은 `deploy.sh` 가 돈 것 ⟹ 중단 |
| S6 | 레포 루트 | `infra/staging/verify/verify-deploy.sh` · `infra/staging/verify/verify-chains.sh` | 헬스 6종 200 ＋ 본문 `unit` 일치 · `platform=0022_rc7_variable_mirror_stmt` · `ai=0007_merge_vocab_and_category` | 루트 헬스 200 하나로 넘어가지 않는다(`RESTART.md §2-③`) — 단위 5종 `/healthz/<unit>` 과 두 체인 head 가 증거다 |
| S7 | 레포 루트 | 온톨로지 계수 — `docker exec colab_v2_staging_pg psql -U postgres -d colab_ai -tAc "select count(*) from …"` (읽기 전용) | 사전 22 · 노드 49 · 엣지 19 (`RESTART.md:230` 시드 기준) | 0 이 나오면 **경계 롤을 먼저 의심한다**(`DATA-REFERENCE §0 M-9`) — 0 위에 판정을 세우지 않는다 |

- S4 안에서 자동으로 도는 것 = ② `migration-single-head` 게이트 · ③ `:prev` 보존 · ⑤ **배포 전 백업**(`backup/backup.sh` 두 프로파일 GREEN 아니면 중단) · ⑦ 롤 · ⑧ **체인별 `upgrade head` 두 번**(platform → ai, 합치지 않는다) · ⑨ GRANT · ⑩-b 엣지 설정 바이트 대조 · ⑪ 판정 · ⑫ 별칭·digest·원장.
- 이번 회차는 마이그레이션 변화 0 이라 ⑧ 은 **무동작이 정상**이다. 새 리비전이 적용되면 전제가 틀린 것 ⟹ 중단하고 보고.

## 2. dev 판정 배포

| # | 어디서 | 명령 | 통과 증거 | 중단 조건 |
|---|---|---|---|---|
| D1 | 개발 기계 · 레포 루트 | `infra/dev/build.sh` | `→ arm64 ✓` 5줄 · `── 저장: …/dist/colab-v2-dev-ccf76ecf0bb5.tar` · `dist/colab-v2-dev.sha` = `ccf76ecf0bb5` | `no match for platform` ⟹ `docker run --privileged --rm tonistiigi/binfmt --install arm64` 뒤 재실행(`infra/dev/README.md` 진단표) |
| D2 | 개발 기계 | `COLAB_DEV_SSH=… COLAB_DEV_KEY_FILE=… infra/dev/ship.sh` | **반입 게이트 첫 실사용** — exit 0 · `Loaded image:` 5줄 · `loaded: dev-ccf76ecf0bb5` · `── 실었다: dev-ccf76ecf0bb5`. EC2 `/opt/colab-v2/MAIN_SHA` = `main=ccf76ecf0bb5 candidate=ccf76ecf0bb5 ancestor=yes` | exit 65 = 비조상(반입 거부 · ssh 0회) · exit 78 = `origin` 조회 실패. ⛔ **`COLAB_SHIP_ALLOW_NONMAIN` 을 쓰지 않는다** |
| D3 | 개발 기계 → EC2 | `/opt/colab-repo` 동기 3줄(`tar czf … db gates services/core-api/ops infra` → `scp` → `sudo tar xzf … --overwrite`) | 개발 기계와 EC2 의 `services/core-api/ops/deploy_doctor.py` **md5 일치** | 생략 시 doctor 가 **옛 스크립트**를 돌려 ⑮ 자체가 없다(`infra/dev/README.md` 선행 단계) |
| D4 | EC2 | `/opt/colab-v2/up.sh` | `① 마이그레이션` 두 체인 무동작 → `③` 4단위 `healthy` → `④` 헬스 본문 4줄(`storageMode`/`sourceMode`=s3) → `up: ok (sha ccf76ecf0bb5)` · exit 0 | 하나라도 healthy 아니면 exit 1(fail-closed). ⚠ **`up.sh` 에는 조상 검사가 없다 — 설계상 게이트는 `ship.sh` 한 곳이다**(설계트리 Q2) |
| D5 | EC2 | `sudo /opt/colab-v2/backup.sh` (직전 백업이 24h 를 넘겼을 때만) | `_ops/backups/dev/` 에 새 객체 2건 | 넘긴 채로 doctor 를 돌리면 ⑭ 가 ✗ — 그것이 옳은 동작이다 |
| D6 | EC2 | `docs/DEPLOY.md §6-1` 의 `docker run … -v /opt/colab-repo:/repo:ro **-v /opt/colab-v2:/state:ro** … deploy_doctor.py --env dev …` **1회** | `항목 15 — ✓ 15 · ✗ 0 · ─ 0` ＋ `전 항목 통과 (─ 0 — 15 항목이 실제로 돌았다)` · exit 0 | ⛔ `--allow-skip` 금지. ⛔ **재시도해 모은 15 는 15 가 아니다**(`CLAUDE.md §0`). `/tmp/op.env` 는 실행 직후 삭제 |
| D7 | 개발 기계 | `infra/dev/tag-release.sh dev` | `태그 생성(로컬): dev-20260908-1 → ccf76ecf0bb5` ＋ push 명령 출력 | exit 65 = sha 파일 없음/비조상/동명 태그 존재. 스크립트는 EC2 를 읽지 않고 push 도 하지 않는다 |
| D8 | 개발 기계 | `git push origin dev-20260908-1` | 원격에 태그 1건 | **Ted 가 15/15 를 본 뒤 오케스트레이터가 집행**(비가역 원격 행위 · advisor ③ 뒤) |

### ⑮ 가 ✗ 일 때 진단표 (`docs/DEPLOY.md §6-1` · `deploy_doctor.py:686-717`)

| ✗ 문구 | 뜻 | 조치 |
|---|---|---|
| `마운트 없음` | `-v /opt/colab-v2:/state:ro` 를 빼고 돌렸다 | 명령을 그대로 다시 구성해 **처음부터 1회** 재실행 |
| `파일 없음` | `/state/CURRENT_SHA` 또는 `/state/MAIN_SHA` 부재 = **반입 게이트를 거치지 않은 배포** | D2 를 다시 밟는다. ─(준비 실패)로 낮추지 않는다 |
| `형식 불일치` | `MAIN_SHA` 한 줄이 `main=… candidate=… ancestor=…` 가 아니다 | 파일을 손으로 고치지 않고 D2 재집행 |
| `후보 불일치` | 반입 뒤 다른 이미지가 떴다(`CURRENT_SHA` ≠ `candidate`) | D2 → D4 순서 확인 후 재집행 |
| `ancestor=bypass` / `no` | 우회 선언 또는 비조상 | 이번 창에서는 발생 자체가 중단 사유 |

## 3. 되돌리기

- staging = `infra/staging/rollback.sh --to-last-green`(원장에서 직전 green 을 읽는다) 또는 `--to-tag <태그>`. ⛔ `--to-placeholder` 는 N-1 이 아니라 **0**(제품 이전)이다. **스크립트를 고치지 않는다.** 스키마·`pgdata` 는 되돌리지 않는다(forward-only `〈168〉-㉲`).
- dev = EC2 `/opt/colab-v2/dev.env` 의 `COLAB_IMAGE_TAG=dev-83d3eb52cb1f` 로 바꾸고 `/opt/colab-v2/up.sh`(`infra/dev/README.md` 되돌리기). 이미지는 불변 태그로 남아 있다.
- ⚠ **롤백 뒤 ⑮ 는 직전 `ship.sh` 반입 판정을 유지한다** — `dev.env` 롤백은 `CURRENT_SHA`·`MAIN_SHA` 를 갱신하지 않는다. 즉 롤백 상태에서의 15/15 는 **실행 이미지의 증거가 아니다.**

## 4. 경계 (레인·집행 지시문에 축자 삽입)

- 운영 데이터베이스는 **읽기 전용** — `pg_dump` · `:ro` 마운트 · 카탈로그 조회만. `DELETE`·`UPDATE`·DDL 금지. 파괴 플래그(`--yes-drop-schema`·`--yes-delete`) 금지.
- 컨테이너 정지·재기동·재생성·`down` 금지 — `deploy.sh`·`up.sh` 가 스스로 하는 것 밖으로 나가지 않는다.
- 일회용 postgres 는 `--rm` ＋ `--tmpfs` ＋ `PGDATA` 지정 ＋ 호스트 포트 미공개(없으면 이 호스트에서 `initdb` 가 죽는다).
- **접속 문자열·비밀번호·토큰을 출력·로그·문서 어디에도 남기지 않는다.** 로그 보존 전 비밀 스캔은 창 9 관행 그대로.
- **「스크립트가 파괴 플래그 없이 거부하면 그것이 옳은 동작이다. 우회하지 말고 보고하라.」**
- ⛔ `COLAB_SHIP_ALLOW_NONMAIN` 사용 금지 · ⛔ `rollback.sh` 편집 금지 · ⛔ `deploy.sh` 의 네 SKIP 플래그 사용 금지 · ⛔ `--allow-skip` 금지.

## 5. 원장 초안

- 번호 = **〈381〉**(실측 `dev-package/prd/tools/max-decision.sh` → `380`). ⚠ 병합 직전 재실측(`.claude/rules/colab-rules.md §4-1`).
- 초안(형식은 `〈378〉` 행과 같게 · 값은 집행 뒤 채운다):
  `| 〈381〉 | **창 10 — staging 리허설 1회 ＋ dev 실적용 `ccf76ecf0bb5` · 반입 게이트·`MAIN_SHA`·doctor ⑮ 첫 실사용 · 마이그레이션 변화 0** | **집행 (2026-09-08 · 대상 = dev EC2 ＋ RDS · `main` ff 완료 `ccf76ec`).** ①staging = `approve`＋`deploy` 2행(`브랜치=main` **첫 기록**) · 배포전백업GREEN · head platform `0022…` / ai `0007…` **무변** · 헬스 6종 ＋ 두 체인 판정 통과 · SKIP 0. ②dev 사전 = 이미지 `dev-ccf76ecf0bb5` 5/5 · 롤백 세대 `dev-83d3eb52cb1f` · `MAIN_SHA` = `main=ccf76ecf0bb5 candidate=ccf76ecf0bb5 ancestor=yes`. ③실적용 = `up.sh` exit 0 · 마이그레이션 무동작 · 4단위 healthy. ④`deploy_doctor`(EC2 · **1회** · `--allow-skip` 없음) = `항목 15 — ✓ 15 · ✗ 0 · ─ 0`. ⑤태그 `dev-20260908-1` → `ccf76ecf0bb5`(push = Ted 확인 뒤). ⑥이번에 세지 않은 축 = <집행 뒤 기재>. **역링크** — 계획: `dev-package/sessions/WINDOW-10-plan-20260908.md` |`
- HANDOFF 2줄 초안 — ⑴ 머리 「최종 갱신 / 현재 단계 / 다음 WU」에 `창 10 집행 · dev = ccf76ecf0bb5 · doctor 15/15` ⑵ `§1` 해당 행에 `〈381〉` 링크만(값은 원장 한 곳에).
- 집행 기록 파일 = `dev-package/sessions/WINDOW-20260908-10.md`(선례 이름 규약) · 로그 = `dev-package/reports/R-D/deploy/`.

## 6. 소요·순서

| 구간 | 추정 | 근거(창 9 실측) |
|---|---|---|
| S1~S3 확인·승인 | 3분 | 원장 `approve` 행 |
| S4 `deploy.sh` | **1~3분** | `staging-deploy-rc2.log` = 09:18:59 시작 → 09:19:46 종료 **47초**(이미지·마이그레이션 무변 회차). 이미지가 다시 구워지면 길어진다 |
| S5~S7 확인 | 5분 | — |
| D1 `build.sh` | **15~40분** `[미상]` | 창 9 로그에 벽시계 없음. tar 274 MB · buildx arm64 5 이미지 |
| D2 `ship.sh` | 5~10분 | 274 MB scp ＋ `docker load` |
| D3 `/opt/colab-repo` 동기 | 2분 | 창 9 md5 일치 확인 |
| D4 `up.sh` | 3~5분 | healthy 대기 최대 120초 ＋ 재생성 |
| D5~D6 백업·doctor | 5분 | 창 9 doctor 1회 |
| D7~D8 태그·push | 2분 | — |
| **합계** | **약 45~75분** (대기 제외) | 창 9 전체는 08:31~10:00 이었으나 그중 40분이 `WU-C13` 수습이다 |

- **병렬 가능 구간 = 없다.** S(리허설) → D(판정)가 직렬이고, D1~D6 은 서로의 산출을 입력으로 받는다. 전수 게이트를 동시에 돌리지 않는다(`.claude/rules/colab-rules.md §9` OOM 선례).

## 7. 세지 못한 것 · Ted 판정이 필요한 것

1. **staging 가동 여부 `[미상]`** — 이 세션은 `docker` 를 돌리지 않았다. S1 이 8개 미만이면 되살리는 한 줄(`-f compose.i2.yml`)을 먼저 밟는다. 판정 불필요, 절차로 닫힌다.
2. **`main` CI 기존 red 3건이 배포를 막는가 = 막지 않는다(권고).** 근거 — ⑴ `service-tests (core-api)` red 1(`a31111f` · `test_viz_service_credential.py:85` 403≠202)은 **호스트 게이트에서 재현되지 않는다**(로컬 core-api 게이트 **980/0/0 green** · `R-D-ROUND-20260908.md:69`) ⑵ `planning-gates` red 는 러너에 기획 폴더가 없어 나는 환경 red ⑶ `harness-eval` 78 은 시크릿 미발급. ⑷ 「main 과 동일」로 넘기지 않기 위해 되물은 **「`main` 이 배포 가능한가」의 답** = 같은 트리 `83d3eb5` 계열이 창 9 에서 `deploy_doctor` **14/14** 로 배포됐고, `83d3eb5..ccf76ec` 는 배포 경로 스크립트·doctor·문서뿐이다(런타임 서비스 코드 0). ⟹ **배포 가능.** 대장 `X-7` 은 열린 채로 둔다.
3. **⑮ 첫 실사용이라 「15/15」 선례가 0 이다** — 실패 시 위 진단표로 갈라 보고. `[미확인]` 을 「통과」로 세지 않는다.
4. **Ted 판정이 필요한 자리 셋** — ⑴ staging 리허설 착수 GO(승인 문구 = `approve.sh` 의 「무엇을 눈으로 봤는가」) ⑵ dev 실적용 GO ⑶ doctor 15/15 를 본 뒤 태그 push GO. 셋 다 창 9 와 같은 자리다.
