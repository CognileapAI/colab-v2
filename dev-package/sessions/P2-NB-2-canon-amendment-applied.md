# NB-2 정본 개정 · DR-8 재개정 — 적용 기록

> **성격 = 실행 기록이다.** `sessions/P2-NB-2-canon-amendment-proposal.md` 가 초안이고 이 문서가 그 이행이다.
> 실행 = WU-P2 정본개정 적용 레인 · 2026-08-23 · Ted 승인 (ⓐ NB-2 수명 · ⓒ DR-8 재개정, 둘 다)
>
> **증거(EVIDENCE)와 해석(INTERPRETATION)을 갈라 적는다**(`DATA-REFERENCE §0` M-5).
> **재지 않은 수는 적지 않는다**(M-4). 절대경로를 적지 않는다(`CLAUDE.md §3-8`).
> 정본 폴더 위치는 `planning/README.md §1`, 정본이 **아닌** 것은 같은 문서 §2 다.

---

## 0. 한 줄 결론

**260818 정본 4문서를 고쳤고**(NB-2 수명 규칙 + DR-8 포맷 표기 7자리), **패키지 임베드 3파일을 재동기했고**,
`planning-freshness` 는 **본 체크아웃에서 green(15블록 전부 MATCH)** 이다. 레포에서는 `DR-8`·`STOP-2` 의
✅ 를 되돌려 사실을 기록했고 `PLAN-SoT §9` 에 `〈67〉` 을 남겼다. **커밋하지 않았다** — 메인 세션 몫이다.

---

## 1. 백업 — 편집 전에 떴다 (유일한 되돌리기 수단)

정본은 git 저장소가 아니다. diff 로 확인해 줄 것이 없으므로 **사본이 유일한 롤백**이다.

> 위치 = `<작업공간>/40 COLAB-기획/_backup_260823_NB2-DR8/`
> (`<작업공간>` = 이 레포의 부모 폴더 — `planning/README.md §1` 의 표기 규칙 그대로)

| 백업 파일 | 원본 (정본 루트 기준 상대) |
|---|---|
| `E-04_Policy.md.bak` | `에픽/E-04_업로드와_계보_확정/documents/Policy_업로드와_계보_확정.md` |
| `E-03_Policy.md.bak` | `에픽/E-03_데이터셋_상세/documents/Policy_데이터셋_상세.md` |
| `E-04_PRD.md.bak` | `에픽/E-04_업로드와_계보_확정/documents/PRD_업로드와_계보_확정.md` |
| `E-00_DataModel.md.bak` | `에픽/E-00_공통_기반/documents/DataModel_공통_기반.md` |
| `E-04_package_업로드와_계보_확정_260817.html.bak` | `에픽/E-04_.../package/업로드와_계보_확정_260817.html` |
| `E-03_package_데이터셋_상세_260817.html.bak` | `에픽/E-03_.../package/데이터셋_상세_260817.html` |
| `E-00_package_공통_기반_260817.html.bak` | `에픽/E-00_.../package/공통_기반_260817.html` |

**7건이다 — 지시받은 4건보다 3건 많다.** *(해석)* DR-8 이 여는 문서가 E-04 Policy·E-03 Policy 둘만이
아니라 **E-04 PRD · E-00 DataModel 까지 넷**이고, 게이트가 해싱하는 임베드도 그래서 **E-00 을 포함해 셋**이다.
지시서의 「두 정본 md + 두 임베드 html」은 NB-2 만 셀 때의 수다.

---

## 2. 편집 전 실측 — 지시받은 목록을 그대로 믿지 않았다 (M-7)

정본 루트에서 실행한 명령과 **원문 그대로의 적중**:

```
grep -rni --include="*.md" -e "grib" -e "hdf5" .
```

| # | 파일 : 줄 | 적중 원문(발췌) |
|---|---|---|
| 1 | `E-00.../DataModel_공통_기반.md:66` | `포맷(grib·nc·bin·tif·HDF5 등)` |
| 2 | `E-03.../Policy_데이터셋_상세.md:162` | `**격자**(nc·grib·tif)는 칸을 색칠하고` |
| 3 | `E-03.../Policy_데이터셋_상세.md:199` | `함께 적는다**(GRIB · NetCDF · BIN · GeoTIFF · HDF5)` |
| 4 | `E-04.../PRD_업로드와_계보_확정.md:118` | `\| 파일 파서 \| grib·nc·bin·tif·HDF5 헤더에서` |
| 5 | `E-04.../Policy_업로드와_계보_확정.md:116` | `\| 파일 \| grib · nc · bin · tif · HDF5 등 [가정]` |
| 6 | `E-04.../Policy_업로드와_계보_확정.md:238` | `그릴 수 있는 형식은 GRIB · NetCDF · BIN · GeoTIFF · HDF5 예요.` |
| 7 | `E-04.../Policy_업로드와_계보_확정.md:247` | `\| 파일 파서 \| grib·nc·tif·HDF5·bin 헤더에서` |

**적중 7건 — 지시서의 7자리와 파일·줄이 전부 일치했다.** `HDF4` 는 같은 검색에서 **0건**이었다.

머리말 실측(정본 폴더 확인 — 제안 §6-0 의 절차):

| 문서 | 편집 전 `version` | 편집 전 `updated` |
|---|---|---|
| E-04 Policy | **2.2** | 2026-08-18 |
| E-03 Policy | **2.1** | 2026-08-18 |
| E-04 PRD | 1.2 | 2026-08-18 |
| E-00 DataModel | 1.8 | 2026-08-18 |

> ⚠ **제안과 어긋난 점 하나 — E-03 Policy 의 머리말 버전은 2.2 가 아니라 2.1 이었다.**
> 그런데 같은 문서 §12 변경 이력에는 **이미 `2.2` 행이 있다**(2026-08-18). 머리말이 뒤처져 있었다.
> 제안 §4-G 는 다음 판을 `2.3` 으로 적었는데, 그 수는 **변경 이력 기준으로는 맞고 머리말 기준으로는 건너뛴다.**
> *(판단)* **변경 이력을 기준으로 삼았다** — 이력에 2.2 가 실재하므로 다음은 2.3 이다. 머리말의 정체는 이번에 함께 맞췄고, 그 사실을 §12 에 적었다.

---

## 3. 정본 편집 — 무엇을 무엇으로 (EVIDENCE)

### 3-1. `E-04 .../documents/Policy_업로드와_계보_확정.md` — v2.2 → **v2.4**, updated 2026-08-23

**ⓐ NB-2 (= v2.3 분)**

| 자리 | 전 | 후 |
|---|---|---|
| §7.1 상태 정의 | (`미등록 미리보기` 행 다음이 바로 `등록됨`) | **행 추가** — `\| 만료됨 \| 미등록 미리보기 상태로 두고 돌아오지 않아 수명이 지난 상태 \| 저장 안 됨. 올린 파일과 미리보기 결과를 서버에서 지운다 \|` |
| §7.2 전이 규칙 | (표 마지막 행 = `미등록 미리보기 → (탭 화면)`) | **행 추가** — `\| 미등록 미리보기 \| 만료됨 \| 수명이 지남 \| 처리가 끝났거나 실패해 더 진행할 일이 없다 \| 파일과 미리보기 결과를 지운다. 그 주소로 다시 들어오면 없는 것으로 답한다 \|` |
| §8.1 `휘발 고지` | 「…한 줄에 둔다 \| 경고가 아니라 상태로 읽힌다. 등록으로 가는 길이 같은 자리에 있다」 | 「…한 줄에 둔다. **남은 시간을 숫자로 적지 않는다** \| 경고가 아니라 상태로 읽힌다. 등록으로 가는 길이 같은 자리에 있다. 「몇 시간 남았다」를 세면 등록을 서두르게 하는 화면이 되고, 등록하지 않아도 된다는 이 화면의 주장이 흐려진다」 (**요소·조건 칸은 손대지 않았다**) |
| §8.1 `나가기` 다음 | — | **행 추가** — `\| 수명 \| 나가지 않은 채 돌아오지 않음 \| 화면을 벗어나지 않았더라도 **일정 시간이 지나면** 파일과 미리보기 결과를 서버에서 지운다. 지운 뒤 그 주소로 들어오면 없는 것으로 답한다 \| 다시 들어오면 "이 파일은 더 이상 없어요. 다시 올려 주세요."로 만난다 \|` |
| §9 오류와 예외 | (마지막 행 = `이름 없이 데이터셋 만들기`) | **행 추가** — `\| 수명이 지난 파일로 등록하려 함 \| "이 파일은 더 이상 없어요. 다시 올려 주세요." \| 파일 놓기부터 다시 \| 등록 전 파일은 서버에 임시로만 두므로 수명이 있다 (8.1) \|` |
| §11 아직 정하지 않은 것 | (`대용량 업로드` 행 **그대로 둠** — 빼지 않았다) | **행 추가** — `\| 등록 전 파일의 수명 값 \| **수명이 있다는 것은 8.1 에서 정했다.** … **운영 설정으로 두고, 값과 그 근거는 개발이 기록한다.** 다만 **처리가 끝나기 전에 지우는 값이어서는 안 된다** \|` |
| §12 변경 이력 | — | **`2.3 / 2026-08-23` 행 추가** (제안 §4-F 문안 그대로) |

**ⓒ DR-8 (= v2.4 분)**

| 자리 | 전 | 후 |
|---|---|---|
| §5 입력값 `파일` | `grib · nc · bin · tif · HDF5 등 [가정] 형식 제한 없음` | `nc · bin · tif · HDF4 등 [가정] 형식 제한 없음` |
| §9 그릴 수 없는 형식 | `지금 그릴 수 있는 형식은 GRIB · NetCDF · BIN · GeoTIFF · HDF5 예요.` | `지금 그릴 수 있는 형식은 NetCDF · Binary · HDF4 · GeoTIFF 예요.` |
| §10 파일 파서 | `grib·nc·tif·HDF5·bin 헤더에서` | `nc·tif·HDF4·bin 헤더에서` |
| §12 변경 이력 | — | **`2.4 / 2026-08-23` 행 추가** |

### 3-2. `E-03 .../documents/Policy_데이터셋_상세.md` — v2.1 → **v2.4**, updated 2026-08-23

| 자리 | 전 | 후 |
|---|---|---|
| §8 「미리보기는 서버가 그린다」 | `… 서버에 임시로만 두므로 **수명이 있다**(아래 S-08 규칙).` | `… 서버에 임시로만 두므로 **수명이 있다**([업로드와 계보 확정](…E-04…Policy…) 8.1 S-08 규칙).` — **문단의 나머지 글자는 손대지 않았다** |
| §8 `그릴 수 없는 형식` | `함께 적는다**(GRIB · NetCDF · BIN · GeoTIFF · HDF5)` | `함께 적는다**(NetCDF · Binary · HDF4 · GeoTIFF)` |
| §8 `지도 표현` | `**격자**(nc·grib·tif)는 칸을 색칠하고` | `**격자**(tif·nc)는 칸을 색칠하고` |
| §12 변경 이력 | — | **`2.3`(참조 정정) · `2.4`(포맷 표기 + 머리말 정체 정정) 두 행 추가** |

> **§8 `지도 표현` 을 포함한 것은 Ted 판정이다.** *(EVIDENCE — 이 자리는 지원 포맷 목록이 아니라
> 시각화 서술 안의 예시다.)* *(판정 근거 — 정본에서 `grib` 을 본 독자는 그것을 지원 포맷으로 읽는다.)*

### 3-3. `E-04 .../documents/PRD_업로드와_계보_확정.md` — v1.2 → **v1.3**, updated 2026-08-23

| 자리 | 전 | 후 |
|---|---|---|
| §7 의존성 `파일 파서` | `grib·nc·bin·tif·HDF5 헤더에서 메타데이터를 읽는 기능` | `nc·bin·tif·HDF4 헤더에서 메타데이터를 읽는 기능` |

> 이 문서에는 변경 이력 절이 **없다**(실측). 그래서 이력 행을 넣지 않았고 머리말만 올렸다.
> **PRD 에 NB-2 수명은 넣지 않았다** — 수명은 판단 기준이라 Policy 소관이다(E-04 Policy §0). 제안 §6-1 그대로.

### 3-4. `E-00 .../documents/DataModel_공통_기반.md` — v1.8 → **v1.9**, updated 2026-08-23

| 자리 | 전 | 후 |
|---|---|---|
| §4.1 `자동으로 읽은 정보` | `포맷(grib·nc·bin·tif·HDF5 등)` | `포맷(nc·bin·tif·HDF4 등)` |
| §9 변경 이력 | — | **`1.9 / 2026-08-23` 행 추가** |

---

## 4. 개정 후 실측 — grep 원문 (EVIDENCE)

```
grep -rni --include="*.md" -e "grib" -e "hdf5" .      # 정본 루트
```

**적중 3건. 셋 다 이번에 새로 쓴 변경 이력 행이고, 셋 다 「이 표기는 틀렸다」고 말하는 문장이다.**

- `E-00.../DataModel_공통_기반.md:195` — 「… 포맷 예시가 `grib·nc·bin·tif·HDF5` **였다** — `grib` 은 지원하지 않고, 실제 파일은 `HDF5` 가 아니라 `HDF4` 다」
- `E-03.../Policy_데이터셋_상세.md:254` — 「… 목록에서 `GRIB`·`HDF5` 를 **걷었고** … `(nc·grib·tif)` 도 `(tif·nc)` 로 바꿨다」
- `E-04.../Policy_업로드와_계보_확정.md:282` — 「… 이 문서는 아직 `grib`·`HDF5` 를 적고 **있었다** …」

> *(정직하게)* **「0건」이 아니다.** 규범 문면(입력값·오류 안내·의존 전제·데이터 모델)에서는 **0건**이고,
> 남은 3건은 전부 「무엇을 왜 걷었는지」를 적은 변경 이력이다. 변경 이력에서까지 단어를 지우면
> **왜 바뀌었는지가 사라진다** — 그것이 이 개정이 필요했던 원인(`DR-8` 의 ✅ 가 실물과 갈린 것)이다.

```
grep -rn --include="*.md" "HDF4" .
```

**적중 9건 — 규범 문면 6 + 변경 이력 3.** 개정 전 `HDF4` 는 정본 전체에서 **0건**이었다.
규범 문면 6 = E-00 DataModel `:66` · E-03 Policy `:199` · E-04 Policy `:116`·`:241`·`:251` · E-04 PRD `:118`.

---

## 5. 임베드 재동기 · 게이트 (EVIDENCE)

**게이트를 약화시키지 않았다.** 검사기(`dev-package/tools/check-package-freshness.py`)·정규화 규칙·
블록 정규식·검사 범위에 **한 글자도 대지 않았다**(`CLAUDE.md §4`). 재green 은 **임베드를 원본과 같게 만드는 것**으로만 했다.

재동기한 블록 4개 / 파일 3개 — `E-00` `md-prd`(DataModel) · `E-03` `md-policy` · `E-04` `md-policy` · `E-04` `md-prd`.
방식 = 검사기 자신의 `normalize()` 를 불러 원본 md 를 그 함수가 보는 형태로 만든 뒤 해당 `<script>` 블록 본문에 그대로 넣었다.

### 실행 자리 — **본 체크아웃**이다 (워크트리 아님)

> *(EVIDENCE)* 검사기는 정본 루트를 **레포 루트의 형제**로 푼다. 워크트리는 `.claude/worktrees/` 아래라
> 그 경로가 안 맞아 **red 가 나고, 그 red 는 낡음이 아니라 경로 문제다.** 그래서 판정은 본 체크아웃에서만 했다.

```
./gates/run.sh planning-freshness          # 본 체크아웃에서
```

```
EPIC             BLOCK      SOURCE                VERDICT  VER  SHA8
E-00_공통_기반       md-policy  Policy_공통_기반.md       MATCH    1.4  68df5dfe
E-00_공통_기반       md-prd     DataModel_공통_기반.md    MATCH    1.9  b7516c25
E-01_역할과_권한      md-policy  Policy_역할과_권한.md      MATCH    1.3  533c6d6a
E-02_데이터_찾기      md-policy  Policy_데이터_찾기.md      MATCH    1.8  4ef1327a
E-02_데이터_찾기      md-prd     PRD_데이터_찾기.md         MATCH    1.1  4fe6f458
E-03_데이터셋_상세     md-policy  Policy_데이터셋_상세.md     MATCH    2.4  fa8e266b
E-03_데이터셋_상세     md-prd     PRD_데이터셋_상세.md        MATCH    1.2  09bc2e6c
E-04_업로드와_계보_확정  md-policy  Policy_업로드와_계보_확정.md  MATCH    2.4  ba115503
E-04_업로드와_계보_확정  md-prd     PRD_업로드와_계보_확정.md     MATCH    1.3  7c36cb74
E-05_프로젝트        md-policy  Policy_프로젝트.md        MATCH    1.6  dc30b87c
E-05_프로젝트        md-prd     PRD_프로젝트.md           MATCH    1.3  15ecab24
E-06_승인_처리       md-policy  Policy_승인_처리.md       MATCH    1.7  0ac2a5a7
E-06_승인_처리       md-prd     PRD_승인_처리.md          MATCH    1.2  746d799d
E-07_홈_대시보드      md-policy  Policy_홈_대시보드.md      MATCH    1.5  223d844f
E-07_홈_대시보드      md-prd     PRD_홈_대시보드.md         MATCH    1.3  8a05946a

planning-freshness green — 15개 임베드 블록 전부 원본과 일치.
```

**개정한 네 줄의 `VER` 가 실제로 올라가 있다** — E-03 Policy `2.4` · E-04 Policy `2.4` · E-04 PRD `1.3` ·
E-00 DataModel `1.9`. 제안 §6-2 가 세운 확인 기준(「바뀌지 않으면 임베드를 안 고친 것이다」)을 통과했다.

```
python3 dev-package/tools/check-package-freshness.py --selftest
```

```
[selftest 1] 정본 미마운트 → red OK
[selftest 2] 변조 fixture (E-00_공통_기반) → red OK
            E-00_공통_기반 / md-prd ≠ DataModel_공통_기반.md (임베드 a59725e4 vs 원본 b7516c25)
[selftest 3] 읽기 불가 fixture → red OK (진단됨)
selftest green — 검사기는 틀린 것을 틀렸다고 말한다 (fail-closed 증명).
```

---

## 6. 레포 정정 — `DR-8` · `STOP-2` 의 ✅ 를 되돌렸다

**되돌린 이유(EVIDENCE).** 그 ✅ 는 「Ted 승인 2026-08-23」을 달고 있었지만 **승인이 선 파일 목록이 틀렸다.**
실제로 고쳐진 4건은 `01 CoLAB-Plan/reference/v2_1차마일스톤_기획_260808/` 과 `01 CoLAB-Plan/planning-base/`
아래에 있었고, 그 두 폴더는 `planning/README.md §2` 가 **「이제 참조하지 않는다 — 정본은 위 한 폴더뿐이다」**로
명시적으로 배제한 옛 사본이다. **260818 정본은 한 글자도 닿지 않았다** — §2 의 grep 7건이 그 증거다.

`grep -rn "DR-8"` · `grep -rn "STOP-2"` 를 `dev-package/` 전체에 돌려 **닫힘을 주장하는 자리를 전수로 찾았다**(M-2).
지시서가 이름 댄 두 파일이 전부가 아니었다 — **다섯 자리였다.**

| # | 파일 · 자리 | 무엇을 했나 |
|---|---|---|
| 1 | `dev-package/03-HANDOFF.md §3.5` 「`〈51〉`·`㊿-F-2` 포맷 범위」 행 | 옛 ✅ 문면을 「구 기록 — 틀렸다」로 표시하고 정정 + 재개정 사실을 적었다 |
| 2 | `dev-package/03-HANDOFF.md §5.5` `DR-8` 행 | 옛 ✅ 문면을 취소선으로 보존하고 **「첫 번째 ✅ 는 무효였다」**를 머리에 세운 뒤 재개정 기록으로 닫았다 |
| 3 | `dev-package/sessions/P2.md §7` `STOP-2` | 같은 처리 — 두 문서가 **같은 말을 하도록** 문면을 맞췄다 |
| 4 | `dev-package/sessions/P2-EXEC.md §열린 것` 「`STOP-2` 정본 포맷 문구」 | 「✅ 닫힘 (2026-08-23 **재개정으로**)」 + 근거 갱신 |
| 5 | `dev-package/sessions/D2c.md §마감 4항` | 「`DR-8`·`STOP-2` 는 닫혔다」 뒤에 정정 문장을 붙였다 — 결론은 유지되지만 **근거는 재개정**이다 |

> **손대지 않은 자리 하나** — `dev-package/sessions/P2-viz-report.md §ⓐ`·`§395` 는 `STOP-2` 를
> **「닫힘인데 정본에는 옛 목록이 그대로다」**라고 **미해소로 적고 있다.** 그 서술은 당시 사실이었고
> 지금 이 개정이 그것을 해소한 것이다. **닫힘을 주장하는 자리가 아니므로 되돌릴 것이 없다.**

### `PLAN-SoT.md §9` — `〈67〉` 신설

`〈66〉` 다음에 이어 붙였다. **ⓐ 와 ⓒ 를 한 항목에 담았다**(같은 회차·같은 파일). 담긴 것:

- 정본이 못박은 규칙 셋과 **정본에 숫자를 안 넣었다는 사실**
- **운영 초기값 24시간** + 아래에서 미는 근거 · 위에서 누르는 근거 · **치르는 대가**(금요일 저녁 업로드 소실, 재업로드는 처음부터)
- **24 가 미실측 위에 있다는 고백** — `DR-11` 의 50 GB 는 **가정**이고(`P2-EXEC §8` 잔여 위험 6), 실증된 최대는 `SEED-DATA` 의 1.3 GB 단일 묶음이다. **그래서 정본이 규칙만 갖고 숫자를 안 갖는다**
- `E-03 §11`「추측으로 숫자를 박으면…」은 **선례 하나이지 정본이 선언한 규범이 아니라는** 단서 — 유추로 끌어 썼다고 명시했다(과장 금지)
- **이행 제약 둘** — ㉠ **reaper 는 처리 중 상태를 건너뛴다**(삭제 질의 조건, 산문 아님) · ㉡ **처리 중인 업로드가 만료 시각을 지나고도 살아남는다를 단언하는 시험**. ㉡ 이 없으면 음성 시험 ㉳ 가 **정상 동작에 404 를 내면서 green 을 보고한다**
- 정하지 않은 것 · DR-8 재개정 7자리 · 게이트 결과

---

## 7. 하지 않은 것 — 따로 적는다

| 안 한 것 | 왜 |
|---|---|
| **커밋** | 지시대로 하지 않았다. 메인 세션 몫이다. 워크트리는 **변경이 완결되어 열람 가능한 상태**로 남겼다 |
| **제안 §6-3 스냅샷 정오표** | **적중 0건이라 단계가 없어진 것이 아니라, 이번 회차가 정본 밖 사본 폴더에 대한 grep 을 돌리지 않았다.** 미실시로 남긴다 — 「했는데 0건」과 「안 했다」는 다르다 |
| **제안 §6-4 의 5~10 중 6·8·9·10** | 지시 범위 밖이다. `P2.md §9` 의 `NB-2` 행 닫기 · `P2-EXEC` 의 세 자리 정본 인용 교체 · `db/platform/schema.sql:d5_upload.expires_at` 주석 · **운영 설정 자리 신설(24시간의 실제 거처)** — **전부 미이행**이다. ⚠ **`〈67〉` 은 24시간을 결정으로 적었지만 그 값이 코드·설정 어디에도 아직 없다** |
| **`03-HANDOFF §1` P2 상태 갱신** | 지시 범위 밖. `§3.5`·`§5.5` 만 고쳤다 |
| **목업 HTML 대조** | `E-04 mockups/*.html` 의 S-08 문구는 정본 md 와 별개 파일이고 `planning-freshness` 시야 밖이다(WU-G1b). 문안이 「남은 시간을 숫자로 적지 않는다」로 정해진 덕에 **현행 고지문이 그대로 유효**하지만, **대조하지는 않았다** |
| **`contracts/events/core-pipeline.json` 의 `grib`·`HDF5`** | 손대지 않았다. `D2c §2-12`·§234 가 **지우지 말라**고 못박은 자리다 — 정본이 상류이고 계약은 D2c 개정 회차에 따라온다 |
| **50 GB 실측** | 못 했다. `〈67〉`·`DR-11`·`P2-EXEC` 잔여 위험 6 에 미실측으로 남는다 |
| **advisor 게이트** | 이 레인은 적용 실행만 했다. 수용 검토는 메인 세션 몫이다 |

---

## 8. 남는 위험 (INTERPRETATION — 잠정이다)

1. **24시간이 아직 어디에도 없다.** 결정만 있고 거처가 없다. `createUpload` 는 여전히 값을 못 채운다 — `expires_at` 은 `NOT NULL`·DEFAULT 없음(`P2-db-report H-1`). **다음 회차의 첫 일이 이것이다.**
2. **㉠·㉡ 이 구현 회차에 전달되지 않으면 `〈67〉` 의 보장은 종이다.** 특히 ㉡ 이 없으면 ㉳ 가 **틀린 것을 옳다고 증명한다.**
3. **정본은 여전히 버전 관리 밖이다.** 다음 개정도 사람 백업이 유일한 되돌리기다. 이번 백업 7건이 남아 있다.
4. **`E-03 Policy` 머리말 버전이 이력보다 뒤처져 있었다** — 같은 종류의 정체가 다른 문서에도 있는지는 **재지 않았다.**
