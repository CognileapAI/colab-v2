# DEPLOY 269 — `bc499c2` staging 배포 회차 (2026-09-01)

> 값·근거의 정본은 `PLAN-SoT §9 〈269〉` 다. 이 문서는 그 등재문의 작업 기록이다.

| 축 | 값 |
|---|---|
| 워크트리 · 브랜치 | `.claude/worktrees/lane-deploy2` · `lane-deploy2` |
| 배포 커밋 | `bc499c2c1ac9` (`main` · 워킹트리 변경 0건) |
| 릴리스 태그 | `bc499c2c1ac9` |
| 승인 행 | 원장 `approve` 1건 (승인자 Ted · RULING 269) — 배포 행보다 먼저 |
| 결정 등재 | `PLAN-SoT §9 〈269〉` (번호 중복 검사 = 중복 0건) |

## 1. 배포 — 순서를 지켰다

**배포 전 백업을 스택이 올라간 상태에서 먼저 받았다.** 직전 회차 중 하나가 스택을 내린 채
돌려 백업이 postgres 를 못 찾고 red 를 냈다 — 그 순서를 반복하지 않았다.

- 백업 platform **GREEN**(통과 7 · SKIP 0 · 표 23 · 행 415) · ai **GREEN**(통과 7 · SKIP 0 · 표 6 · 행 91)
- 마이그레이션은 절차대로 `deploy.sh ⑧` 의 체인별 러너가 돌렸다 — **손으로 DDL 을 치지 않았다**
- 플랫폼 체인 `0008_lineage_origin_labels` → **`0010_p6_access_request`**(중간 `0009` 포함 · 전진 전용)

## 2. 신원 판정 — 루트 200 으로 판정하지 않았다

| 항목 | 실측 |
|---|---|
| 실행 이미지 태그 | 앱 5종 전부 `colab-v2/<단위>:bc499c2c1ac9` ＝ 배포 커밋 |
| 별칭 `:i2` | **6/6** 이 `bc499c2c1ac9` 를 가리킨다 (이미지 ID 대조 완료) |
| 마이그레이션 체인 head | platform **`0010_p6_access_request`** · ai `0005_k2b_concept_graph_seed` |
| 헬스 **본문 대조** | 루트 `^ok$` ＋ 단위 5종 `"unit"` 값 일치 — **6종 200** |
| 컨테이너 | **8개 전부 healthy** |
| 호스트 노출 | `0.0.0.0` **0건** |
| 배포 판정 | **GREEN — 통과 15건 · SKIP 0** |
| **`⑩-b` 엣지 설정 바이트 대조** | **돌았다** — 레포 판 `56cc63f04dad443a` ＝ 도는 컨테이너 안의 판 `56cc63f04dad443a` |

## 3. `P6` — 배포된 스택에서 실동작을 쟀다

**양성(한 왕복)** — 잠긴 상세 `bodyAccessible=false` → 요청 **201** → `accessRequestPending=true`
→ 받은 요청 목록 **총 1** → 승인 **200**(만료 = 승인일 ＋6개월 · `2026-09-01` → `2027-03-01`)
→ 같은 상세 **`bodyAccessible=true`** → 재승인 **409**.

**음성**

| 음성 | 실측 |
|---|---|
| 남의 연구실 데이터 상세 · 요청 생성 | **404** (`없거나 연구실 경계 밖이다`) |
| 남의 연구실의 **실재하는** 요청 승인 · 거절 | **404** — 403 이 아니다(존재를 알리지 않는다) |
| 그 요청이 내 목록에 보이는가 | **0건** |
| Verified 는 교수만 | 교수 경로 **202 → 대기 1 → 승인 200** |
| **`승인 위임` 꺼진 연구원의 403** | ⛔ **`[미측정]`** — 아래 |

⛔ **배포 스택에서 못 잰 음성 하나를 감추지 않는다.** staging 주체 표의 베어러가 **하나(교수)**
뿐이고 역할 판정은 원장에서 나오므로, 연구원으로 붙을 수단이 없다. **레포 시험은 green 이지만
배포 스택 실측은 `[미측정]`** 이다. 푸는 법 = 주체 표에 연구원 베어러 1건 추가 —
**자격 파일 변경이라 배포 승인이 덮지 않는다. 별도 승인 사항으로 올린다.**

## 4. `Y-1` — 선언은 전부 섰고 왕복은 못 돈다

| 선언 | 실측 |
|---|---|
| 버스 볼륨 `events` 기동 | **떴다** — `colab-v2-staging_events` 가 두 단위에 마운트 |
| `COLAB_WORKER_EVENT_SPOOL` | **선언됐다** — `/srv/viz-events` |
| `COLAB_VIZ_TRIGGER_SPOOL` | **선언됐다** — `/srv/viz-events` |
| `COLAB_WORKER_STAGE2` | `on` |
| **트리거 왕복 1건** | ⛔ **집행 불가** |

⛔ **새 결함 — `events` 볼륨 소유권.** `infra/staging/compose.i2.yml` 의 `volume-init` 이
`uploads`·`previews` 만 `chown 10001` 하고 **신설 볼륨 `events` 를 빠뜨렸다.**
실측 = `/srv/viz-events` 가 `root:root 0755` 인데 pipeline-worker·viz-render 는 **둘 다 uid 10001**
⟹ 양쪽에서 `touch` **Permission denied**. **D5 발신도 D7 ack 도 물리적으로 못 쓴다.**

**「배선은 있는데 아무 일도 안 난다」의 실패형이다** — 그리고 헬스는 200 이다.
**고치지 않았다** — 수정은 새 커밋 ＋ 두 번째 배포이고 **이번 승인 범위 밖이다.**

## 5. 회귀 · 게이트

| 축 | 값 |
|---|---|
| `render-latency` | **p95 2.118초** · 최대 2.145초 (눈금 10초 · 상한 60초 · 표본 25 · 포맷 5종) |
| 확대·이동 | 시험 **3건 전건 통과** (상한 100 ms) |
| 갈래 기본값 | **「한 장」 그대로** — `/healthz/viz-render` 의 `tileBranch` = `꺼짐` · 홈 env 에 `COLAB_VIZ_TILE_BRANCH` 미선언 |
| 게이트 `all` | **green 35 / red 0** (병렬도 정본 `-j 2`) |
| 우회 · 비활성화 · 검사 축소 | **0건** |
| 이번 배포가 만든 red | **0건** |

## 6. 완료 정의 대조

### `P6` — **닫는다**

| 조건 | 판정 | 근거 |
|---|---|---|
| ⑴ 수용 기준 | ✅ | 위 `§3` — 배포된 스택에서 여덟 op 중 양성 왕복 ＋ 음성 넷 실측 |
| ⑵ 도메인 게이트 green | ✅ | `all` **green 35 / red 0** |
| ⑶ **staging 배포 green** | ✅ | 이 회차 — 릴리스 `bc499c2c1ac9` |
| ⑷ 목업 대비 화면 검증 | ✅ | `〈266〉` 회차 |

⟹ **`P6` = `done`.** **해제되는 것 = `P7`(연구실 대시보드)** — `P7` 의 `depends_on` 은 `[P6]`
하나뿐이라 **남은 선행이 없다.** 그 뒤 `P8` 은 `P7` 을 기다린다.

⚠ **닫으면서 세지 않은 축을 이름으로 남긴다** — 「`승인 위임` 꺼진 연구원의 403」의 **배포 스택 실측**.
완료 정의 4항이 요구하는 축은 아니지만, 다음 회차의 진입조건으로 적어 둔다.

### `Y-1` — **닫지 않는다**

| 조건 | 판정 | 근거 |
|---|---|---|
| ⓐ 무효화 대상 = 렌더 산출물뿐 | ✅ | `〈247〉`·`〈253〉` |
| ⓑ 트리거 3종이 각각 무효화를 일으킨다 | ✅ | `〈253〉` — 발신 3 · 수신 3 · 재생성 3 |
| ⓒ 수동 「다시 만들기」 흡수 | ✅ | `〈247〉` |
| ⓓ stage 1 원칙이 여기서만 뒤집힌다 | ✅ | `〈247〉` |
| ⓔ 경계 — 발신 D5 · 무효화·재생성 D7 | ✅ | `〈253〉` |
| **ⓕ staging 배포 green** | ⛔ **닫지 않는다** | 배포 자체는 green 이나 같은 항목이 진입조건에 이름으로 건 **「트리거 왕복 1건 실측」이 `§4` 로 집행 불가**다 |

⟹ **`Y-1` = `partial` 유지.** **남은 것 하나 = 트리거 왕복 1건 실측**,
그 **선행 = `events` 볼륨 `chown 10001` 수정 ＋ 재배포**.
**동작하지 않는 것을 문면으로 닫지 않는다**(`WORK-UNITS §12`).

## 7. 시험 데이터 — 만든 것과 되돌린 것

**만든 것**(전부 합성 · 실데이터 무접촉)

- 합성 데이터셋 **2건** — HYMETS 연구실 잠김 1 · A 연구실 1(경계 음성용)
- 설명 **2행** · 접근 정책(`잠김`) **2행**
- 접근 요청 **2건** — 화면 경로 1 ＋ A 연구실 직삽입 1
- 허용 줄 **1건** · Verified 검토 대기 **1건** · Verified **1건**

**되돌린 것 = 위 전부.** `DELETE` **11행** · 잔재 실측 **7 표 전부 0건** ·
접근·승인·Verified 계 표는 배포 전과 같은 **전부 0행** · 기존 `d3_dataset` **13건 무접촉**.

## 8. 다음 승인 요청 — 한 묶음으로 올린다

1. **`events` 볼륨 `chown 10001` 수정 ＋ 재배포** — 그 회차에 `Y-1` 왕복 실측 ⟹ `Y-1` 닫힘 판정
2. **staging 주체 표에 연구원 베어러 1건 추가** — 그 회차에 「`승인 위임` 꺼진 연구원 403」 실측
