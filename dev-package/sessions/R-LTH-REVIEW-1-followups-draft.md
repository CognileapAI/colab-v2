> ⚠ **정본 = 대장 `GT-1`·`LF-1`~`LF-14`(2026-09-14 등재).** 이 파일은 등재 전 초안이다.

# R-LTH-REVIEW-1 — 후속 항목 후보

> **초안 · 대장 미등재.** 등재·`stage` 판정은 오케스트레이터 ＋ Ted. 한 줄 = 무엇 · 왜 · 근거.

- 작성 2026-09-14 · 대조 트리 `integration/r-lth-1` HEAD `0427ac91`.
- 레인 보고가 기존 결함으로 적은 것은 어느 검사에 걸리는지 함께 적는다(`.claude/rules/colab-rules.md §3-3`). 걸리는 검사가 없으면 「검사 없음」.
- 계수 = A 11 · B 9 · B-2 1 · C 2 · 미회수 슬롯 2. 기준 = 표 1행 = 후보 1건(겹치는 문면은 한 행으로 합침).

## A. 판정이 유예한 것 (판정 등재문 「후속 항목 후보」 ＋ 기획자 회신)

| # | 무엇 | 왜 | 근거 |
|---|---|---|---|
| A1 | 연구실 고르개 — 관리자가 한 연구실만 골라 보는 전환(읽기 창구의 연구실 인자 ＋ 계약 판정 · L) | 이번 회차는 표기를 누를 수 없는 표시로만 바꿈 · 「경계는 요청에서 오지 않는다」 규칙 재판정 선행 | 카드 ① ⓑ · `dev-package/intent/2026-09-13-lth-shell-header.md` · 대장 `BO-3` `note:` |
| A2 | 검색 범위 고르개 — 검색창 옆 전체 / 우리 연구실(M~L) | A1 과 같은 구조 변경(요청이 조회 범위를 지정) · A1 과 한 자리에서 결정하기로 판정 | 카드 ⑧ ⓑ · `dev-package/intent/2026-09-13-lth-search-scope.md` |
| A3 | 내 계정 메뉴 — 사용자 이름 버튼에 내 계정·로그아웃 메뉴(M) | 이번 회차는 화살표만 지움 · 휴대전화 「더보기」 목록과 같은 부품으로 묶기로 판정 | 카드 ② ⓑ · 카드 ③ ⓒ · `…-lth-shell-header.md` |
| A4 | 가공 단계 사유 입력 ＋ 한 번 더 확인 절차(사유 저장 칸 ＋ 마이그레이션 1건 ＋ 계약 열쇠 · L) | 계산값과 다른 단계를 유지할 때 이유가 기록에 남지 않음 · 부모 후붙임 흐름의 확인 2회 문제 설계 선행 | 카드 ⑩ ⓑ · `…-lth-processing-level-mismatch.md` |
| A5 | 표 줄 3자리 키보드 열기 — `ProjectTable`·`CatalogTable`·`ProjectDatasetTable`(M) | 마우스 없이 표에서 상세로 가는 경로 부재 잔존 · 빠른 작업 버튼·초점 순서 재설계 동반 | 카드 ⑥ · `…-lth-a11y-empty-states.md` |
| A6 | 승인 상태 3값 — 승인됨 / 검토 대기 / 요청 없음(목록 계약 열쇠 1 ＋ 묶음 질의 1 · M) | 승인 요청이 들어온 줄이 목록에서 구분되지 않음 | 카드 ⑨ ⓑ · `…-lth-verified-chip.md` |
| A7 | 접근성 검사기 게이트 신설 | 키보드 접근·제목 단계 결함이 사람이 본 뒤에만 발견됨 · 검사 부재 자체가 결함 · 걸리는 검사 = 검사 없음 | 카드 ⑦ ⓒ · `…-lth-a11y-empty-states.md` · `colab-rules §3-3` |
| A8 | 전 화면 대표 제목 규약 적용 잔여분(화면 10+ · M) | 이번 회차 집행은 연구실 설정 한 화면 · 연구실 홈 등 같은 결함 잔존 | 카드 ⑦ ⓑ · SUMMARY §4 ⑦ |
| A9 | 본인 비활성화 확장 — 계정명 재입력 ＋ 활성 관리자 셈(M) | 활성 관리자 0명 상태 발생 여부 `[미확인]` · 확인 뒤 별 항목으로 두기로 판정 | 카드 ⑤ ⓑ · `…-lth-account-members.md` |
| A10 | 「할 일 함」 표기 변경(조건부 · M) | 기획자가 변경을 원할 때만 — 기획 정본 → 목업·계약 설명·화면 2곳을 한 회차에 | SUMMARY §5 · `dev-package/sessions/R-LTH-REVIEW-1-planner-reply-draft.md` |
| A11 | 로그인 실패 뒤 이메일 칸 관측 회수(조건부 · 재현 시 S) | 코드와 관측이 갈림 · 미회수면 `[미확인]` 로 남기고 항목화 | 라운드 `## spec 추적 및 작업 종료 기준` 6 · SUMMARY §5 |

## B. 레인 보고 발견 (보고 문면 그대로)

| # | 무엇 | 왜 | 근거 |
|---|---|---|---|
| B1 | `frontend-test` 게이트가 vitest fork worker 기동 타임아웃을 red(판정)로 계수 — red(준비)여야 함 | 준비 실패가 판정 red 로 읽혀 재실행·오판 유발 · 걸리는 검사 = `frontend-test` 판독부 자체 | 레인 보고 · 판정 등재문 「추가 확인」 동시 실행 상한 해제 항 |
| B2 | `lineage-unknown-20260907.test.tsx` 전역 스캔 시험의 5000ms 예산이 부하 시 red | 시험 결과가 호스트 부하에 의존 · 걸리는 검사 = `frontend-test` | 레인 보고 |
| B3 | 호스트 `CLOCK_REALTIME` 역행을 탐지하는 게이트 없음 | 시험 되돌리기는 기본키 스냅숏으로 교체 완료 · 다른 시각 오라클 시험 영향 `[미확인]` · 걸리는 검사 = 검사 없음 | 레인 보고 · 커밋 `1e48a64b`(`services/core-api/tests/conftest.py`) |
| B4 | `tests/fixtures/setup-db.sh` 비멱등 — 재실행 시 `schema "account_admin" already exists` | 같은 DB 에 재실행하면 준비 단계 실패 · 걸리는 검사 = 〈TBD: 이 스크립트를 부르는 게이트 이름〉 | 레인 보고 |
| B5 | 운영자 검색 카드 조립 `_compose(db)` 가 요청 트랜잭션(소속 연구실)을 써서 타 연구실 매치를 조용히 건너뜀(`row is None → continue`) | 결과 항목이 소리 없이 빠지는 green-by-skip 형태 · 이번 회차 범위 줄이 전 연구실 건수를 말하므로 카드 수와 갈릴 가능성 `[미확인]` · 걸리는 검사 = 검사 없음 | 레인 보고 · 〈TBD: 파일 앵커 `_compose(`〉 |
| B6 | 구성원 표가 구성원마다 계정 상태를 한 번씩 조회(`status_for_account`) | 구성원 수에 비례한 조회 수 · 걸리는 검사 = 검사 없음 | 레인 보고(Task 7 구성원 절) |
| B7 | `accounts.py` 주석이 운영자 지정을 「SQL 수동」으로 기재 · 화면은 이미 존재 | 주석과 실물 불일치 · 걸리는 검사 = 검사 없음 | 레인 보고 · 라운드 「그 밖」 · `services/core-api/src/colab_core/app/routes/accounts.py` |
| B8 | 계정 관리 화면이 디자인 시스템 패턴이 아니라 `login.css` 위에 구성됨 | 공용 패턴 변경이 이 화면에 전파되지 않음(이번 표 안내도 `frontend/src/auth/login.css` 에 2줄 추가 · `b3ff16e9`) · 걸리는 검사 = 검사 없음 | 레인 보고 · 라운드 「그 밖」 |
| B9 | 레인 동시 실행으로 호스트 load 14~37 → vitest 타임아웃 · 레포가 drvfs 위라 I/O ≈450배 느림(ext4 대비) | Ted 판정 = 이 회차 마감 뒤 폴더 30·31·32 를 ext4 로 이전 · 이전 뒤 env 파일·픽스처 경로·설정 바인딩 재점검 필요 | 레인 보고 · 판정 등재문 「추가 확인」 ext4 이전 항(load 20~27 · 읽기 22.9s vs 0.05s · 측정 시점 다름) |

## B-2. 수용 검토 발견 (오케스트레이터 전달)

| # | 무엇 | 왜 | 근거 |
|---|---|---|---|
| B10 | 계정 자격 행이 없는 구성원(상태를 모름)은 권한 표에서 편집 가능으로 남는다 | 비활성과 같게 잠글지 **Ted 판정 대상** · 닫는 비용 = `_editable_permissions` 1행 | Task 7 수용 검토 · `services/core-api/src/colab_core/app/routes/members.py` 앵커 `def _editable_permissions(` |

## C. 라운드 계획 「그 밖」 중 A·B 와 겹치지 않는 것

| # | 무엇 | 왜 | 근거 |
|---|---|---|---|
| C1 | 계정 표 이메일 열 고정 ＋ 모바일 카드 목록 | 이번 회차는 좁은 폭 좌우 이동 안내까지 | 라운드 `### 이번 회차에 **들어가지 않는** 후속 항목 후보` 「그 밖」 |
| C2 | 프로젝트 표·계보 노드의 `level_pair` → `level_view` 확장 | 가공 단계 불일치 표식이 데이터셋 목록 두 표에만 선다 | 같은 절 · 커밋 `0427ac91` |

## D. 미회수 슬롯

- ~~〈TBD: Task 4·6·7 레인 보고의 후속 항목〉~~ — 해소: 레인 보고 후속은 추가 절과 대장 GT-1·LF-* 에 흡수
- ~~〈TBD: Task 3 …통합 미반영〉~~ — 해소: Task 3 는 통합·main 반영 완료(14d3e063 · a07e87a4)

## 추가 (2026-09-14 · Task 3b 보고)

- 부모 선택 단계 안내 줄 — 계산값 추종 중에는 제한 안내를 숨겼다. 추종 중에 「부모를 고르면 가공 단계가 계산돼요」 같은 안내를 둘지 Ted 문면 판정 대상 · 근거 = Task 3b 레인 보고 · 판정 기록 「집행 판단」.
- 확정 부모 중 Lv 를 모르는 부모가 있으면 계산값이 없어 기본 Lv2 로 돌아가고 상한이 다시 적용된다 — 의도 여부 확인 대상.
- 부모 선택 단계의 「분류에서 바꾸기」 링크(`lin-goto-classify`)는 제한 안내 줄 안에 있어 계산값 추종 중에는 함께 숨는다. 추종 중에는 단계를 올릴 필요가 없어 동작상 문제는 없다. 추종 중 안내 문구를 정할 때 이 링크를 따로 둘지 함께 판정 · 근거 = 안내 줄 보정 레인 보고(`c10acf6a`).

## 추가 (2026-09-14 · 최종 트리 `a07e87a4` 전수)

- `service-tests-viz-render` 가 가상환경의 패키지 설치 누락(`ModuleNotFoundError: colab_viz`)을 판정 red 로 셈 — 준비 red 로 분류돼야 함 · 근거 = 전수 `dev-package/reports/lth-review-260913/full-a07e87a4/`
- dev 실화면 대비·글자 크기 계측 — 이번 회차는 frontend-visual 을 명시 면제로 넘겼고 프런트가 11개 영역 바뀌었다 · dev 배포 뒤 `COLAB_VISUAL_URLS` 로 로그인 화면·주요 화면 1회 계측 · 근거 = 게이트 ③ 지적
- EC2 에 반입 파일(285M)·`dev.env.bak-before-e9ca26b15bd1` 잔존 — 디스크 여유 4.8G · 정리 여부 판정 대상(삭제는 파괴적 조작)
