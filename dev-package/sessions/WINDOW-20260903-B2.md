# staging 단독 창 2 — 단계 B (배포) 회차 기록

> 절차서 = `notes/STAGING-WINDOW-RUNBOOK.md §2`. 값·근거의 정본은 `PLAN-SoT §9`.
> 이 회차 범위 = **B-0 승인 행 ＋ 단계 B 배포 ＋ 배포 후 게이트·현물 점검**. 단계 C~H 미착수.

## 0. 전제 실측 (착수 시점)

| # | 조건 | 실측 | 판정 |
|---|---|---|---|
| ⓐ | `main` 워킹트리 변경 0건 | `git status --porcelain \| grep -c .` = **0** | ✅ |
| ⓑ | `main` 체크아웃에서 집행 | 배포는 `main` 체크아웃에서 실행(워크트리는 이 기록 커밋 전용) | ✅ |
| ⓒ | `COLAB_STAGING_ENV` | 셸 기본 **미설정** — 배포 셸에서 홈 0600 env 파일로 export | ✅ |
| ⓓ | 스택 기동 상태 | 컨테이너 8종 healthy(배포 전 `docker ps` 실측) | ✅ |
| ⓔ | 배포 대상 커밋 | `HEAD` = `72408173be50` · `git diff --stat 8e3a0da..7240817` = **`dev-package/sessions/WINDOW-20260903-A2.md` 1파일 173줄 추가** ⟹ **코드 변경 0건** | ✅ |
| ⓕ | 승인 행이 배포 행보다 먼저 | 승인 `11:34:44` < 배포 `11:35:49` | ✅ |

## 1. B-0 — 원장 승인 행 (축자)

```
2026-09-03T11:34:44+0900	approve	72408173be50	-	-	승인자=Ted 본것=go/no-go ① GO · 짝 백업 20260903T103411 · WINDOW-20260903-A2 · Ted 「GO — 배포한다」 · 실제 배포 커밋 72408173be50 = 8e3a0daee013 + 문서 1건(dev-package/sessions/WINDOW-20260903-A2.md · 173줄 추가 · 코드 변경 0건 · git diff --stat 실측)
```

`deploy.sh` 는 `HEAD` 를 스탬프한다 ⟹ 승인 행의 sha 는 `8e3a0daee013` 이 아니라 **`72408173be50`** 이다.
둘의 차이는 문서 1건뿐이고 그 사실을 행에 축자로 박았다.

## 2. 단계 B — 배포

- 명령 = `infra/staging/deploy.sh --target staging` (플래그 없음 · 파괴 플래그 0건)
- 릴리스 id = **`72408173be50`** · 태그 = `72408173be50`

| 단 | 실측 |
|---|---|
| ① 커밋 확인 | 워킹트리 변경 0건 · sha `72408173be50` |
| ④ 이미지 보존 대조 | 6종 전부 「보존 ≠ 신규」 · 이미지 ID 동일 0건 |
| ⑤ 배포 전 백업 | platform **GREEN(통과 7 · SKIP 0)** `platform-20260903T113512.sql.gz` 31745B · ai **GREEN(통과 7 · SKIP 0)** `ai-20260903T113512.sql.gz` 5162B |
| ⑦ 롤·DB | `roles/databases: ok` |
| ⑧ 마이그레이션 | platform·ai 각 1회 실행 — **새 리비전 0건**(head 불변) |
| ⑨ 앱 롤 GRANT | `ok (colab_app@platform · colab_ai_app@ai · SELECT only)` |
| ⑩ 교체 | 5개 배포 단위 ＋ 엣지 재생성 · 엣지 설정 해시 `56cc63f04dad443a` |
| ⑩-b 엣지 바이트 대조 | **레포 판 = 도는 판** (`56cc63f04dad443a`) |
| ⑪ 배포 판정 | **GREEN — 통과 15건 · SKIP 0** |
| ⑪-b 체인 | platform `0011_lv1_drop_level_user_set` · ai `0005_k2b_concept_graph_seed` — **GREEN 2건 · SKIP 0** |
| ⑫-a 별칭 재부착 | **6/6** · 이미지 ID 대조 완료 |
| ⑫-a2 digest 이력 | 원장 append **6종**(`~/colab-v2-releases/image-digest-ledger.tsv`) |
| ⑫-b 이미지 정리 | 보존 3릴리스 + 별칭 2 · `14d3136aa55a` 6종 삭제 |

원장 마지막 배포 행 (축자) —

```
2026-09-03T11:35:49+0900	deploy	72408173be50	72408173be50	green	별칭재부착GREEN digest이력=6종 배포전백업GREEN 워킹트리변경=0
```

### 2.1 독립 재확인 (배포 스크립트 밖에서 한 번 더)

- `infra/staging/verify/verify-deploy.sh` → **배포 판정: GREEN (통과 15건 · SKIP 0)** · exit 0
- `infra/staging/verify/verify-chains.sh` → **체인 판정: GREEN (통과 2건 · SKIP 0)** · head 2건 불변
- 태그 대조 (`docker image inspect`) — `<릴리스태그>` 와 `:i2` 의 이미지 ID 가 6종 전부 동일

| 이미지 | 이미지 ID (앞 12) |
|---|---|
| core-api | `2caa4a7b480c` |
| pipeline-worker | `dc5bd80ab616` |
| viz-render | `4726a9609481` |
| ai-service | `29dcd023d81f` |
| frontend | `4142085bb697` |
| migrator | `2a89ad8e5283` |

## 3. 배포 후 게이트 — `./gates/run.sh all -j 1`

| 회 | 조건 | 결과 |
|---|---|---|
| 1회 | 게이트 입력 env **미주입** | green 36 · red(판정) 0 · **red(준비) 7** |
| 2회 | 홈 0600 게이트 입력 env 주입 후 재실행 | **green 42 · red(판정) 0 · red(준비) 1** |

- **red(판정)은 0 건이다.** 배포가 만든 red 는 없다.
- 1회의 red(준비) 7건은 전부 「입력 미선언 4 · 환경대기 3」 — `COLAB_APPLIED_DB_URL_*`·`COLAB_AUTOMETA_STAGING_DB_URL`·`COLAB_PREVIEW_TILE_DB_URL`·`COLAB_ARTIFACT_OWNER_DB_URL`·`COLAB_REFERENCE_DATA` 미주입. 2회에서 전건 green 으로 돌았다.
- ⚠ **43/43 green 은 이 회차에서 나오지 않았다.** 남은 1건 = `rls-effect-selftest` **red(준비)**.
  - 단독 재실행 **2회** — 둘 다 red(준비). 케이스 18건 중 **매번 다른 1건**이 판정 불가(1회 `rls-effect ①: 둘째 층을 PERMISSIVE 로` · 2회 `rls-effect 0: 앱 롤에 BYPASSRLS 를 붙임`).
  - 원인 = 일회용 postgres 준비 시간(`limit=60초` · 호스트 load average 1.7~2.8). **판정부가 아니라 환경이다.**
  - 상한 연장·병렬도 조정·건너뛰기로 green 을 만들지 않았다. **red 로 센다.**
  - 선례 = `WINDOW-20260902-ABC2` 2회도 같은 게이트 1건 red(준비). **이번 배포가 만든 것이 아니다.**

### 3.1 `artifact-ownership` 계수 (배포 후 실측)

```
산출물 소유 — 대상 한 벌 75 · 살아 있다 42 · 접수분에만 닿는다 14 · 고아 0 · 판정 불가 19
  세는 단위 = 한 캐시 키 아래 선 산출물 한 벌 · 자리 = <홈>/.colab-v2-staging/previews
  원장 = d3_file 130 · d5_upload_file 143 · 전수 스냅숏 156 줄
  경계 대조 — 관리자 롤 (d3_file,d5_upload_file)=(130,143) ↔ 경계 롤 'colab_app' (0,0) (갈렸다)
```

**고아 0건.** 지도 타일 1벌은 D5 산출이라 대상 밖(완료 정의 ⑷).

### 3.2 `〈297〉` 원장 기반 P8 digest 대조 (읽기 전용)

`infra/staging/restore/preflight.sh --stamp 20260903T103411 --record-digests …` — 복원은 하지 않았다.

```
digest 대조 GREEN — 10 건 전건 일치 · 면제 0 (자체=원장 회차 72408173be50 · 외부=대장 §3 · 배포되는 이미지가 전부 실제로 돌았다)
```

- **기대값 출처가 이번 배포 회차 `72408173be50` 로 자동으로 옮겨 붙었다** — `〈297〉`-㉯ 가 노린 것(「마지막 배포」가 아니라 「지금 가리키는 것」). 배포 뒤에도 `§3` 을 손으로 고칠 필요 없이 GREEN.
- 자체 6 PASS ＋ 외부 4 PASS.
- 같은 실행의 P6·P7·P9 FAIL 3건은 **복원 전용 선행조건**(env 파일 선언 · 복원 직전 백업 지목 · 복원 원인)이라 이 창(복원 미착수)과 무관하다. `사전조건 RED` 는 **복원을 막는 올바른 거부**다.

## 4. 배포 후 사용자 눈높이 현물 점검 (읽기 전용 · 상태만)

베어러 = `~/.colab-v2-staging-subjects.tokens.json` (경로만 · 값 미출력) · `User-Agent` 헤더 부착.

| 점검 | 결과 |
|---|---|
| `GET /api/v1/me` | **200** |
| `GET /api/v1/datasets?limit=20` | **200** · 13건 |
| 데이터셋 상세 `GET /api/v1/datasets/{id}` | **200** · 최상위 키에 **`projects` 있음** · **`actions.canDownload` 있음(본체 있는 데이터셋 · true)** |
| 검색 `POST /api/v1/dataset-searches` (필터 없음) | **200** · 10행 · 행에 **`summary`·`period` 키 둘 다 있음** · `degraded:false` |
| 검색 `POST /api/v1/dataset-searches` `verified:true` | **200** · **0행** — staging 의 13건 전부 `verified:false` 실측. 억지 제안 없는 **정직한 빈 상태**. ⚠ `verified` 는 **본문 필드**이지 질의 문자열 토큰이 아니다(`catalog.py:281`) — 질의문에 `verified:true` 를 넣으면 걸러지지 않는다 |
| 값 조회 `POST /api/v1/datasets/{id}/value-lookup` | **op 실재 · 501 아님.** 본체 있는 건 **200 · `available:true` · 실제 값 반환**. 산출물 없는 건 **200 · `available:false` · `unavailableReason:"자리에 산출물이 없다"`** — 값을 지어내지 않는다 |

⚠ **엣지의 UA 없는 요청 403 은 이번에 재현되지 않았다** — `User-Agent` 를 비운 `GET /api/v1/me` 가 **200** 이었다. 이 창은 그 사실만 적고 판정하지 않는다.

## 5. 단계 E 진입조건 실측

| # | 조건 | 실측 | 판정 |
|---|---|---|---|
| ㉠ | 베어러 파일 동기화 | `~/.colab-v2-staging-subjects.tokens.json` (0600 · 09-03 06:44) — 위 §4 전건이 이 토큰으로 200 | ✅ |
| ㉡ | NumPy 본체 원천 | `01.level-data/02.vegetation/02.vegetation/Lv.2/Prediction_*.npy` **31건 실재** | ✅ |
| ㉢ | 동봉 격자 짝 | ⚠ **경로가 지시와 다르다.** `Lv.2/#metadata/` 는 **없다**. 실재 자리 = `01.level-data/02.vegetation/02.vegetation/#metadata/LAT_crop.npy`·`LON_crop.npy` (**Lv.2 의 한 단 위**). 형상은 이 회차가 재지 않았다 — `[미확인]` | ⚠ 경로 정정 필요 |
| ㉣ | `〈277〉` 파서가 서빙 릴리스에 실려 있는가 | `git log --oneline 77813d391f94 -1 -- services/pipeline-worker/src/colab_pipeline/d5/parse.py` = `44bef51` ⟹ `77813d3` 시점에 이미 포함. 이번 배포 `72408173be50` 는 그 위 + 문서만 | ✅ |
| ㉤ | 단계 D(복원) 선행 | **미착수.** 런북 `§5` = E 를 D 보다 먼저 하면 적재분이 복원으로 지워진다 | ⛔ 미충족 |

**단계 E go/no-go 권고 = no-go (지금은).** 막는 것 둘 —
1. **순서** — 단계 D(`R-1` 복원 실측)가 아직 안 돌았다. E 를 먼저 하면 D 가 E 의 적재분을 지운다.
2. **격자 경로 정정** — 지시문의 `Lv.2/#metadata/` 는 실물에 없다. `02.vegetation/#metadata/` 로 고치고, `(1280,1280)` 짝 여부를 **재고** 착수한다.

## 6. 이 회차가 건드리지 않은 것

- 파괴 플래그 0건 · 컨테이너 수동 조작 0건(`deploy.sh` 가 부른 compose 외) · 운영 DB 쓰기 0건 · 자격증명 값 출력 0건(경로만).
- 마이그레이션은 `deploy.sh ⑧` 만 돌았다 — 손 DDL 0건. head 는 `0011`/`0005` **그대로**.
- 단계 C·D·E~H 미착수. 크론 셋째 줄 미설치.
- `main` 워킹트리 변경 0건 유지. 이 기록은 워크트리 `lane-window-b2`(브랜치 동명)에서 커밋한다. push 없음.
