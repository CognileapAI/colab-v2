# P3-LINEAGE-GRAPH — 계보 그래프 조회 op 를 열고, 확대·타일의 정본 침묵을 드러낸다

> 시점 = 2026-08-30 (+09:00) · 브랜치 = `wt/lane3-map` · 기점 = `eeb7816`(main · fast-forward 병합 · 충돌 0)
> 운영 스택 `colab_v2_staging_*` 은 **한 번도 건드리지 않았다**(조회 0건 · 정지·재기동·DDL·쓰기 0건 · 접속 문자열 미출력).
> **번호 발급됨 = `PLAN-SoT §9 〈227〉`** (2026-08-30 번호 발급 회차에서 등재).

---

## 1. 항목과 완료 정의 (지어내지 않았다 — 대장에 있는 것을 그대로 옮긴다)

**항목 = `P3` 「계보 그래프 · 2D 시각화 3종 · createScreenshot · 렌더 표현 확장」** (`work-items.yaml:869-878`).

**완료 정의 축자** (`work-items.yaml:876`):

> 「각 P 의 완료 판정」 4항 (`§7` 말미). 타일 서빙·확대 · `createScreenshot` 이 완료 정의로 올라왔다

그 4항의 축자 (`WORK-UNITS.md §7` 말미 · 「각 P의 완료 판정 (I2 이후 공통) — 넷 다 충족해야 닫힌다」):

> 1. 해당 단계 스토리의 수용 기준 통과
> 2. 도메인 게이트 green (계약·경계·스키마·RLS)
> 3. **staging 배포 green** — 로컬 green은 완료가 아니다
> 4. 목업 대비 화면 검증

**완료 정의는 있다.** `완료 정의 미작성` 이 아니다.

⚠ **4항 ③ 은 이 레인에서 충족할 수 없다** — 운영 접촉이다(배포 실행). 지시의 정지 조건에 걸려
**하지 않았다.** 따라서 **이번 회차에 `P3` 은 닫히지 않는다.** 상태를 `open` 그대로 둔다.

## 2. 정본이 말한 것과 침묵한 것

정본 = `<작업공간>/40 COLAB-기획/…/에픽/E-03_데이터셋_상세/`(Policy v1.8 · PRD). 목업에서 정책을 유추하지 않았다.

### ㉮ 계보 그래프 — **정본이 말한다. 축자로 열둘.**

`Policy §8`·`§5`·`§1`·`§2` — 가로축은 데이터만(프로젝트는 배지) · 원천(점선 표기) → 가공 전 →
이 데이터(굵은 테두리) → 파생 · 가공 방식은 화살표 위 코랄 라벨(✦ AI 표식) · **화면을 넘으면
가로 스크롤**(접거나 요약하지 않는다) · 데이터 노드는 눌러 그 상세로 이동 · **원천 노드는 이동하지
않는다**(연구실 밖 출처라 열 화면이 없다) · **잠긴 노드는 사라지지 않는다**(있는 줄 모르면 요청할
수조차 없다) · **묘비 노드도 사라지지 않는다**(눌러도 이동하지 않는다) · 확정된 관계만 그린다 ·
기록 없음 빈 상태는 「아직 채워지지 않은 계보예요」＋`계보 채우기`(경고가 아닌 중립 톤) ·
편집 컨트롤은 `업로드·편집` 권한자에게만 보인다.

### ㉯ 2D 지도 렌더(격자) — **정본이 말한다.** 그리고 그 자리는 이미 서 있다.

`Policy §8` — **무엇으로 그릴지는 사람이 고르지 않는다**(포맷에 따라 자동. 목업의 격자·경계·점
전환 버튼은 검토용 데모 장치라고 정본이 직접 적는다) · 값 하나 선택 · 팔레트 3종 · 구간 수 3~9 ·
**바꾼 설정은 저장하지 않는다.**
⭑ `〈204〉` 로 경계·점 둘은 이미 범위 밖이고 남은 것은 격자다. 격자 렌더는 `P2` 가 세웠다(§3).

### ㉰ 스크린샷 — **정본이 말한다.**

`Policy §8`·`§2`·`§3.2` — 「지금 장면을 **PNG** 로 저장」 · **편집 권한자에게만** ·
설정을 저장하지 않으므로 남길 장면은 여기서 뽑는다.

### ㉱ **확대·타일 — 정본이 침묵한다. 여기서 멈췄다.**

기획 정본 패키지 전체(에픽 9 · 인덱스 6)에서 **`확대`·`줌`·`zoom` 0건**이고, `타일` 6건은 전부
**프로젝트 카드의 「지표 타일」**(E-05·E-07)이라 지도와 무관하다. 데이터셋 상세 정본은 지도의
확대·팬·타일 좌표를 **한 줄도 규정하지 않는다.**

그런데 완료 정의는 「**타일 서빙·확대**가 완료 정의로 올라왔다」고 적는다(`§7` P3 행도 「확대/타일 뷰」).
**정본에 근거가 없는 완료 조건이다.** 지어내지 않는다 — `§7` Ted 판정 자리에 올린다.

## 3. 실물 대조 — 무엇이 있고 무엇이 없었나 (전건 파일·행 실측 · 2026-08-30)

| 갈래 | 뒷단 | 계약 | core-api 라우트 | 화면 | 시험 |
|---|---|---|---|---|---|
| 계보 그래프 조회 | **있음** — `routes/lineage.py:39-116` `lineage_graph()` 완성품 | **있음** — `fe-core.yaml:1067` ＋ `LineageGraph` 스키마 | **없었다** — 501 표(`not_implemented.py:70`) | **없었다** — `DatasetDetailPage.tsx` 에 계보 언급 0건 | 간접만(쓰기 3 op 응답 경유) |
| 계보 쓰기 3 op | 있음 | 있음 | 있음(`lineage.py`) | **없었다** | 있음 |
| 격자 2D 렌더 | 있음 — `viz-render` 렌더 파이프라인 | `core-viz.yaml` | 중계 있음(`createPreviewRender`·`getPreviewRender`) | 있음(단일 이미지) | 있음 |
| 지도 타일 서빙 | 있음 — `tiles.py:44 render_tile()` ＋ `renders.py:132` 라우트 | `core-viz.yaml` 만 | **없음** — `fe-core.yaml` 에 타일 0건 | **z=0 한 장을 `<img>` 로**(`preview/tiles.ts:15-18` 이 「타일 갈래는 stage 2 확대 뷰의 자리」라고 스스로 적는다) | 서명만(`test_tile_signature.py` 10) |
| `createScreenshot` | **있음** — `screenshots.py:62` ＋ `screenshot.compose` | `core-viz.yaml` 만 | **없음** — core-api·`fe-core.yaml` 양쪽에 0건 | **없음** — `frontend/src` 에 스크린샷 0건 | 있음(`test_screenshot.py` 9) |

**전제 정정 하나 — 「타일은 COG 없이 못 낸다」가 이 op 에는 걸리지 않는다.** `render_tile()` 은
저장된 `.tif` 가 아니라 렌더 작업이 메모리에 든 배열에서 픽셀을 뽑는다. 미리보기 슬롯의 `.tif` 0건은
이 갈래의 구현 여부와 무관하다. **의존은 여전히 살아 있으나(확대 뷰의 원본 해상도), 「지금 못 돈다」는 아니다.**

## 4. 이번에 세운 것 — 계보 그래프 조회 op 하나

- **새로 만든 함수 0개.** 세 쓰기 op 이 이미 `lineage_graph()` 로 답하고 있었고, 없던 것은
  **그 함수를 부를 GET 라우트 하나**였다. 조회와 쓰기가 다른 그래프를 그리면 어느 쪽이 맞는지
  아무도 모른다 — **같은 함수가 답한다.**
- **계약 개정 0건.** 계약은 처음부터 이 op 을 들고 있었다. **동결 해제를 타지 않았다.**
- **낡은 산문을 정정했다** — `not_implemented.py` 는 「이 조회 op 자체는 P1 배정이라 범위를
  늘리지 않는다」로 적었는데, **`P1` 은 `done` 인데 op 은 501 로 남아 있었다.** 원문은 지우지
  않고 취소선 ＋ 개정 표시를 붙였다. **501 표 20 → 19**(P1 계열 13 → 12).

### ㉯ 화면 — 계보 구역 (`LineageSection`)

- 자리 = `DatasetDetailPage.tsx` 의 `LockedContent` **안**. 잠긴 데이터는 서버와 같은 기준으로 막힌다.
- 신설 6파일(`graphTypes.ts`·`graphSource.ts`·`graphFixture.ts`·`useDatasetLineage.ts`·
  `LineageSection.tsx`·`lineageGraph.css`) ＋ 시험 1파일. **기존 시험 파일은 한 줄도 고치지 않았다.**
- 정본 축자를 그대로 옮긴 것 — 가로축 4칸 순서 · 원천 점선·이동 없음·hover 문구 ·
  묘비 잔존·이동 없음·hover 지운 날짜 · **잠긴 노드 잔존** · 화살표 코랄 라벨과 ✦ ·
  **가로 스크롤(접기 없음)** · 기록 없음 빈 상태 중립 톤 · 경고 칩 · 계보 상세 행(경로 플래그·확인자·
  확인일·파일명 링크) · 파생 행 읽기 전용 · **활용 배지는 노드가 아니다** · `canEdit` 로만 편집 컨트롤 존재.
- **만들지 않은 것과 이유** — ⓐ **E-04 검색 창**: 재사용 가능한 독립 컴포넌트가 없다
  (`LineageStep` 안에 업로드 전용으로 박혀 있고 `uploadId` 를 요구한다). **사본을 만들지 않았다** —
  버튼만 `canEdit` 게이트로 세우고 미연결 표시를 남겼다(`CLAUDE.md §5` 구현 두 벌 금지).
  ⓑ **활용 프로젝트 목록**(`P5` 면)이 없어 배지는 앵커로만 둔다. ⓒ 겹쳐 보기·스크린샷·확대는 범위 밖.
- **스키마가 주지 않아 대체한 것** — `LineageGraph` 에 **`lastModifiedAt` 이 없다.** 정본이 요구하는
  「이후 수정됨」을 상세 응답의 값과 `lineageConfirmedAt` 을 견줘 그렸다. **값을 지어내지 않았으나
  한 화면이 두 op 을 합쳐 판정한다** — 서버가 계보 응답에 이 표시를 실어야 하는지는 판정 사안이다(§8-4).

## 5. 세 상태 증명 — 신설 검사마다 red 를 픽스처로 못 박았다

⚠ **이 레포의 실데이터는 계보 간선 6 · 데이터셋 12 다. 대부분의 데이터셋은 부모도 자식도 0 건이다.**
「간선을 순회하며 단언한다」는 시험은 **아무것도 검사하지 않은 채 green** 이 된다. 그 자리를 막았다.

| 검사 | 대상 있음 → 검사 | 명시적으로 비어 있음 → 통과 ＋ **건수 노출** | 아무것도 못 찾음 → **red** |
|---|---|---|---|
| 계보 간선·노드 | `DSA2 ← DSA1` 로 간선·노드 종류를 센다 | — | `assert_nonempty()` 가 모수 0 을 실패로 만든다 |
| 계보 0건 데이터셋 | — | 간선 **0 건**을 `assert_counted()` 가 **건수를 출력에 적은 채** 통과. 그런데 **노드는 비지 않는다**(자기 자신이 남는다) | 노드까지 0 이면 red |
| 계약 필수 키 대조 | 필수 키를 **계약에서 읽어 와** 응답과 대조 | — | 필수 키 목록이 0 건이면 red(`assert_nonempty`) |
| 501 표 이탈 | 표를 직접 읽어 `getDatasetLineage` 부재를 단언 | — | 표 자체가 0 건이면 red |

**red 실측** — 구현 전 `pytest tests/test_lineage_graph_read.py` = **9 failed · 2 passed**.
(통과한 2 는 「계약이 이 op 을 선언한다」와 「미인증 401」 — 구현 전에도 참이라 green 이 옳다.)
구현 후 **11 passed · 0 failed.** 주장이 아니라 실측이다.

**「대상 0건도 red」를 픽스처로 실증했다** — 계보 0건 데이터셋 하나를 만들고 **같은 데이터에
두 판**을 걸었다. 몸통의 단언은 양쪽 다 「절대 참이 아닌 값」이다.

| 판 | 결과 | 뜻 |
|---|---|---|
| 모수를 안 세는 판(`for e in body["edges"]`) | **passed** | 0 건을 순회해 몸통이 한 번도 안 돌았다 — **아무것도 검사하지 않고 green** |
| 모수를 세는 판(`assert_nonempty`) | **failed** — 「검사 대상이 0 건이다」 | 같은 데이터에서 red 가 난다 |

**1 failed · 1 passed 실측**(2026-08-30). 픽스처는 증명 후 지웠다 — 커밋에 없다.
주장이 아니라 잰 값이고, 그래서 모수를 세는 줄을 단언 앞에 뒀다.

**화면 쪽 red 증명 — 구현을 위반 상태로 바꿔 실측했다(전부 원복).** 단언에 대한 단언이 아니라
정본 규칙을 하나씩 깨고 실패 건수를 셌다.

| 위반 주입 | 실패 건수 | 위반 주입 | 실패 건수 |
|---|---|---|---|
| 원천 노드를 이동 가능하게 | 1 | 「이후 수정됨」 제거 | 1 |
| 「이 데이터」 굵은 테두리 제거 | 1 | 파생 행 「여기서는 못 고쳐요」 제거 | 1 |
| 직접 연결에도 ✦ 부착 | 1 | `processed` 플래그 지어냄 | 1 |
| 관계가 있어도 빈 상태로 접음 | 1 | 활용 배지를 노드로 세움 | 3 |
| 묘비 노드 미표시 | 1 | **잠긴 노드 숨김** | **32** |
| 보기 권한자에게 계보 수정 노출 | 1 | | |

⚠ **「잠긴 노드 숨김」이 32건을 무너뜨린다** — 그 규칙이 화면 전체에 퍼져 있다는 뜻이고,
정본이 그것을 「있는 줄 모르면 요청할 수조차 없다」로 적은 이유다.

## 6. 실측 — 시험과 게이트

| 잰 것 | 값 | 시점·방법 |
|---|---|---|
| `core-api` 전량 | **488 passed · 0 failed · 0 skipped** | 일회용 postgres · 2026-08-30 |
| 그중 이번 신설(`test_lineage_graph_read.py`) | **11 passed** | 같은 실행 |
| 그 11 의 red 증명 | 라우트 추가 전 **9 failed / 2 passed** | 픽스처로 실측 |
| 기점 대비 증감 | 479 → **488** (＝ 신설 11 − 501 파라미터 시험 2) | 501 표가 20 → 19 로 줄어 파라미터 시험이 2건 준다 |
| `frontend` 전량 | **14파일 311 passed · 0 failed · 0 skipped** · `tsc --noEmit` exit 0 | 오케스트레이터 직접 재실행 · 2026-08-30 |
| 그중 이번 신설(`test/lineage-graph.test.tsx`) | **34 passed** (기점 13파일 277 → 14파일 311) | 같은 실행 |
| 그 34 의 red 증명 | **위반 주입 11종 전건 red** (아래 표) | 픽스처로 실측 · 전부 원복 |
| 게이트 전량 `all -j 2` | **green 27 / red 2** (프론트 작업 후 재실행도 동일) | 2026-08-30 · 환경 완비 후 |

**red 2 의 정체 — 기점과 같다. 둘 다 적용 DB 부재이고 red 인 것이 옳다.**
① `schema-diff` ② `autometa-loss`. **이번 변경이 만든 red 는 0 건이다.**

**도구 부재로 못 돈 것 = 0 건.** 이 워크트리는 `core-api/.venv` · `pipeline-worker/.venv` ·
`frontend/node_modules` 를 이미 들고 있었다. ⚠ **`viz-render/.venv` 는 없다** — 이번 변경이
`viz-render` 를 한 글자도 만지지 않아 돌릴 필요가 없었으나, **`viz-render` 시험을 이 회차에
재지 않았다는 사실은 남긴다**(`[미확인]` · 푸는 법 = venv 생성 후 `pytest`).

## 7. 이번에 세지 않은 판단기준 (다음 회차 진입조건)

- **staging 배포 green** — 완료 정의 4항 ③. 운영 접촉이라 하지 않았다. **이것 없이 `P3` 은 닫히지 않는다.** `[미확인]`
- **목업 대비 화면 검증** — 완료 정의 4항 ④. 화면이 서야 성립한다. `[미확인]`
- **`viz-render` 시험 전량** — 이번 변경 범위 밖. `[미확인]`
- **`createScreenshot` 의 화면 도달 경로** — 뒷단은 서 있는데 core-api 중계도 `fe-core.yaml` 항목도 없다.
  중계를 세우려면 **계약 동결 해제**가 필요하고 그것은 판정 사안이다(§8).
- **확대 뷰의 원본 해상도** — `PV-1` 의 지도 타일 산출물이 배포에 붙어야 성립한다(`PV1-CONSUMER-BUILD §5`).

## 8. Ted 판정이 필요한 자리 (이번에 결정하지 않았다)

1. **「타일 서빙·확대」의 정본 근거** — 완료 정의가 요구하는데 **기획 정본 전체에 확대·줌 규정이 0건**이다.
   정할 것 = ⓐ 지도에 확대가 있는가 ⓑ 있다면 어느 화면의 어느 컨트롤인가 ⓒ 정본에 그 행을 세울 것인가,
   아니면 완료 정의에서 이 조항을 내릴 것인가. **지어내지 않았다.**
2. **`createScreenshot` 을 화면까지 잇는 계약 동결 해제** — 뒷단(`viz-render`)과 화면 사이에
   **core-api 중계가 없다.** 정본은 스크린샷을 편집 권한자 기능으로 규정하므로 화면이 부를 표면이 필요하다.
   `fe-core.yaml` 에 op 을 여는 것은 동결 해제 절차 사안이다.
3. **`getDatasetLineage` 의 소유 항목** — 코드 산문이 이 op 을 「P1 배정」으로 적었고 `P1` 은 닫혔다.
   이번에 `P3` 으로 구현했다. **대장의 귀속을 `P3` 으로 확정할지**를 확인받는다.

4. **「이후 수정됨」을 계보 응답이 실을 것인가** — 정본은 계보 구역 맨 위에 이 표시를 요구하는데
   `LineageGraph` 에 수정 시각이 없다. 지금은 화면이 두 op 의 값을 합쳐 판정한다.
5. **정본이 침묵한 화면 갈래 넷**(지어내지 않았다) — ⓐ `processed` 관계의 경로 플래그 문구
   (정본은 `✦ AI 제안·확인됨`·`직접 연결` 둘만 준다) ⓑ **원천 행의 확인자·확인일**
   (정본은 「행마다」 확인 이력을 요구하나 원천은 관계에 짝지을 수 없다) ⓒ 관계 0건이면서
   기록 없음 표시도 없는 데이터셋의 화면 ⓓ 활용 0건일 때 배지 표기.

## `PLAN-SoT §9` 등재 완료 — **번호 `〈227〉` 발급됨 (2026-08-30 · 번호 발급 회차)**

> 정본은 `dev-package/PLAN-SoT.md §9 〈227〉` 다. 아래는 등재에 쓴 원문이고, 값의 정본이 아니다.
>
> (원 제목: `PLAN-SoT §9` 등재 문안 (번호는 오케스트레이터가 발급))

> **〈227〉 계보 그래프 조회 op 를 열고, 「확대·타일」이 정본에 없다는 사실을 드러낸다.**
> **완료 정의 = 있다**(`완료 정의 미작성` 아님) — 「각 P 의 완료 판정」 4항(수용 기준 · 도메인 게이트 ·
> **staging 배포 green** · 목업 대비) ＋ 타일 서빙·확대 · `createScreenshot`. **4항 ③ 이 운영 접촉이라
> 이 레인에서 충족 불가 → `P3` 은 닫히지 않는다. 상태 `open` 유지.**
> **정본 대조 = 계보 그래프·격자 렌더·스크린샷은 축자로 규정돼 있고, 확대·타일은 기획 정본 전체에
> 규정이 0 건이다**(`확대`·`줌` 0건 · `타일` 6건은 전부 프로젝트 카드의 지표 타일).
> **실물 대조(2026-08-30) = 계보 그래프를 그리는 함수는 이미 있었고**(`routes/lineage.py:lineage_graph`)
> **없던 것은 그 함수를 부를 GET 라우트 하나**였다 — 계약은 처음부터 그 op 을 들고 있었다.
> `createScreenshot` 뒷단·타일 서빙 뒷단도 이미 있으며, **없는 것은 화면과 core-api 중계**다.
> **집행 = ⓐ `getDatasetLineage` 라우트 신설(계약 개정 0건 · 동결 해제 미경유 · 새 함수 0개)
> ⓑ 501 표 20 → 19(P1 계열 13 → 12) ＋ 낡은 산문 정정(「P1 배정」 — `P1` 은 닫혔는데 op 은 501 이었다)
> ⓒ 세 상태 시험 11건 신설 — 간선 있는 데이터셋은 검사 · 계보 0건 데이터셋은 **건수를 드러낸 채** 통과하되
> **노드는 비지 않는다** · **모수 0 건이면 red**(간선 6 · 데이터셋 12 라 이 갈래가 다수다).**
> ⓓ 계보 구역 화면 신설 — 정본 축자 규칙 열넷을 그대로 옮기고, **E-04 검색 창은 사본을 만들지 않고
> 미연결로 남겼다**(재사용 가능한 독립 컴포넌트가 없다).
> **실측 = `core-api` 488 passed / 0 failed(신설 11 · 라우트 추가 전 9 failed 로 red 증명) ·
> `frontend` 311 passed / 0 failed ＋ `tsc` exit 0(신설 34 · **정본 규칙 위반 주입 11종 전건 red**) ·
> 게이트 `all -j 2` green 27 / red 2 — 기점과 동일하고 이번 변경이 만든 red 는 0 건이다.**
> red 둘은 전부 적용 DB 부재이고 red 가 옳은 동작이다(`schema-diff` · `autometa-loss`).
> **범위 밖 = staging 배포(운영 접촉) · 목업 대비 화면 검증 · `createScreenshot` 화면 중계(동결 해제 사안) ·
> 확대·타일(정본 침묵 · Ted 판정 대기).**
