# DR-4 — dev 무인 재생성 실행 기록 (2026-09-14)

- 성격 = `dev-package/tools/dev-reseed/reseed.sh` 1회 실행의 **생성물**. 값의 원본은 `result.json` 이다.
- 승인 = dev 한정 상시 승인(`.claude/rules/deploy.md` 11번 증보 문단). 회차별 GO 불요 · 승인 기록 = `approval-record.json`.
- 실행 자리 = `dev-package/reports/dev-reseed-runs/20260914T012505Z` · 실행 식별자 `20260914T012505Z` · 배포 대상 sha `b955bece7384`.
- 결과 = **failed**(멈춘 단계 `reset`) · 총 소요 18초.

## 1. 단계별 소요

| 단계 | 상태 | 종료코드 | 소요(초) | 로그 |
|---|---|---|---|---|
| preflight | ok | 0 | 5 | `logs/preflight.log` |
| deploy | skipped | — | — | `—` |
| reset | failed | 1 | 13 | `logs/reset.log` |
| bootstrap | skipped | — | — | `—` |
| up | skipped | — | — | `—` |
| s3 | skipped | — | — | `—` |
| prelude | skipped | — | — | `—` |
| seed | skipped | — | — | `—` |
| verify | skipped | — | — | `—` |
| report | skipped | — | — | `—` |

## 2. 계수

- 계수 없음 — verify 단계를 돌지 않았다.

- `deploy_doctor` 요약줄 축자 = `—`(도구 안에서는 미실행 · 정지 뒤 사람 실행 1회 = §7)

## 3. 미리보기 판정 표 (0 행 · 성립 0 · 판정불가 0)

| 순번 | 데이터셋 | 가공 단계 | 판정 | ms |
|---|---|---|---|---|
| — | — | — | — | — |

## 4. 차단 (1 건)

- `reset` · **reset** — 활성 트랜잭션 0 아님

---

## 5. 실행 창 (KST) — 2회차 · 사람이 채운 자리 (`WU-C4` · 작업 사본 브랜치 `lane/wu-c4-run2` · 기점 `6db560ad`)

| 구간 | 시각 | 소요 | 결과 |
|---|---|---|---|
| 0 · 자격·접속 확인 | 10:22 ~ 10:24 | — | `aws sts` 신원 = `colab-platform-s3-uploader-dev` · ssh `docker ps` 앱 4종 `:dev-b955bece7384` · `agent-browser doctor` 12 pass / 0 fail |
| 1 · `--preflight-only --target-ref b955bece` | 10:24:19 ~ 10:24:22 | 3초 | **exit 0 · `preflight — 통과 10 · 미달 0`** — 1회차 정지 원인 2건(`pf_secrets` 짝짓기 · `COLAB_DEV_SECRETS_DIR` 이름 충돌 · `main` `1b6cf870`)이 닫힌 것을 확인 · 계정 신원 = `provision-lab.sql` 에서 읽음(id `000000000000000000HYMETSP1` · `pi@hymets.invalid` · 교수) · 마운트 자리 `/etc/colab` |
| 1 · `--dry-run --from reset --target-ref b955bece` | 10:24:38 | 1초 | exit 0 · 8단계 전건 code=0 · prelude ② 건너뜀 표기 |
| 2 · 실행 `--from reset --target-ref b955bece`(단일 프로세스 · `nohup setsid`) | 10:25:05 ~ 10:25:23 | **18초** | **`reset` 에서 정지 · exit 1** — §6 |
| 2′ · 정지 뒤 복구(도구 밖 · 사람 1회) | 약 10:28 | 약 20초 | `reset` ①′ 이 정지한 앱 4종을 같은 compose·env 로 `start` — §7 |
| 2″ · `deploy_doctor` 1회(도구 밖) | 약 10:30 | — | `항목 15 — ✓ 15 · ✗ 0 · ─ 0` · ⑮ `b955bece7384 ∈ main` — §7 |
| 3 · 계정 단계 / 4 · UI 실사 | — | — | **미실행**(seed·verify 가 돌지 않았다) |

- 사람 입력 = **0회**(대화형 프롬프트 0 · BatchMode ssh · 러너 표준입력 대기 0). 도구 밖 사람 조작 = **1회**(§7 앱 재기동 · 비파괴).
- 비밀값 검사 = 실행 자리 전 파일 `grep -E 'postgresql://|PASSWORD|AKIA'` **0건** · 러너 자격 파일은 `install -m 600` 으로 두었다가 정지 확정 뒤 `shred -u`(작업 자리 잔존 0).

## 6. 정지 지점 — `reset` ①″ 「활성 트랜잭션 0 확인」 · 원인 = 도구 결함 1건(실물 결함 아님)

- 순서 = ① 실행 전 계수 ✓ → ①′ 앱 4 단위 정지 ✓ → **①″ 활성 트랜잭션 질의 ✗** → ② 두 체인 스키마 재생성 **미실행**.
- 축자(`logs/reset.log`) = `ERROR:  column "colab_platform" does not exist` · `LINE 1: ... count(*) from pg_stat_activity where datname in (colab_plat...` · `질의 결과 = <없음> (기대 0)`.
- 원인 = `dev-package/tools/dev-reseed/stages.sh` `psql_master_query` 가 원격 heredoc 안에서 `export SQL='$sql'` 로 SQL 을 **작은따옴표 한 겹**에 싣는데, 호출부(`stage_reset` ①″)의 SQL 자체가 작은따옴표를 품는다(`datname in ('colab_platform','colab_ai')` · `state <> 'idle'`). 원격 셸에서 안쪽 따옴표가 바깥 따옴표를 닫아 문자열 리터럴이 식별자로 넘어간다. 질의가 서지 않으니 값이 「없음」이고 기대 0 과 달라 `blocked_add reset "활성 트랜잭션 0 아님"` 으로 떨어졌다 — **활성 트랜잭션이 실제로 있었던 것이 아니다**(질의가 실행되지 않았다).
- 이 경로는 **실모드로 돈 적이 없다** — 2026-09-13 초기화(`DR-2-run-2026-09-13.md`)는 사람이 런북을 밟았고, `--dry-run` 은 `DRY ssh <dev> bash -s  # psql:master` 로 본문을 찍기만 한다. `tests/`(`doctor-parse.sh`·`preflight-red.sh`·`preflight-secrets.sh`) 에 `psql_master_query` 픽스처 **0건**.
- 부수 관찰 = 같은 오류 줄이 `reset.log` 에 **두 번** 찍힌다 — `ssh_script` 가 `tee -a $STAGE_LOG` 로 한 번, `psql_master_query` 가 `printf … | redact >> $STAGE_LOG` 로 또 한 번. 판정과 무관 · 로그 읽기에서 실행 2회로 오독할 자리.
- 고침 후보(코드 변경 · 이 회차 밖 · 후속 레인) = ⑴ `psql_master_query` 에서 작은따옴표를 이스케이프해 싣는다(예 `local esc="${sql//\'/\'\\\'\'}"` 뒤 `export SQL='$esc'`) 또는 SQL 을 인용 heredoc(`<<'SQL'`)으로 원격 파일에 두고 `-f` 로 준다 ⑵ `tests/` 에 픽스처 — `docker`·`ssh` 를 스텁으로 두고 `$SQL` 이 따옴표를 잃지 않는지(green) · 잃으면 red 인지 ⑶ 이중 로깅 제거. 기존 `dev-reseed-selftest` 게이트에 얹는다.
- 지시 규칙(「`reset` 재시도 없음 · 파괴 단계 재개 없음」)에 따라 **`--from` 재개 0회 · 2차 실행 0회.**

## 7. 현재 dev 상태 (정지 뒤 · 2026-09-14 10:30 KST 기준)

- 데이터 = **무변**(스키마 DROP ② 미실행). `reset` ① 실행 전 계수(연구실 경계 안 · 도구 축자) = platform 스키마 `['account_admin','public']` · 연구실 1 · `d3_dataset` **26** · `d3_file` **565** · `d6_project` **4** · `d4_lineage_edge` **18** · ai 스키마 `['public']` · S3 `uploads/` 1008 객체 · `previews/` 518 객체 · 진행 중 멀티파트 0. 정지 **뒤** 재계수 = `[미확인]`(푸는 법 = 다음 회차 `reset` ① 계수 또는 `reset:count` 본문의 읽기 전용 재실행) — ②가 돌지 않았다는 근거는 `reset.log` 순서와 `blocked.jsonl` 의 단계명뿐이다.
- 앱 = `reset` ①′ 이 4 단위를 `stop` 한 채 도구가 멈췄다(`docker ps -a` = `core_api`·`viz_render`·`ai_service` Exited(0) · `pipeline_worker` Exited(137)). 사람이 **같은 compose 파일·같은 env 파일**로 `docker compose … start core-api pipeline-worker viz-render ai-service` 1회(①′ 의 역연산 · 이미지·볼륨·데이터 무변) → 4종 `Up (healthy)` · 이미지 `:dev-b955bece7384` 그대로.
- `deploy_doctor` = 정지 뒤 `infra/ops/probes/deploy-verification.sh` **1회** — `항목 15 — ✓ 15 · ✗ 0 · ─ 0` · `전 항목 통과 (─ 0 — 15 항목이 실제로 돌았다)` · ⑮ `b955bece7384 ∈ main`. 출력에 비밀 패턴 0건. 원문 = 실행 자리 `doctor-after-stop.out`(추적 제외).
- 배포·태그 = 무변(`dev-20260914-2` · 1회차 실적용 그대로). staging·prod·`_ops/` 무접촉.

## 8. 이탈 — 지시 §4 ⑴~⑷

1. 배포 = 1회차에 release executor 로 선행(`stage_deploy` 결함 · 후속 WU) — 이 회차는 `--from reset` 이라 `deploy` skipped. 무변.
2. 계정 단계 = 별도 명령(`stage_seed` 가 `--accounts-password-file` 미전달 · 후속 WU) — **미도달**(seed 미실행). 운영자 계정 재생성 0건.
3. 사람 입력 = **0회**(§5). 단, 도구 정지 뒤 앱 재기동 1회는 사람 조작이다(§7 · 비파괴 · 도구가 `reset` 실패 시 앱을 되살리지 않는 것도 후속 고침 후보 — 실패 경로에서 ①′ 의 역연산이 없다).
4. 「한 번의 실행으로」 = **불성립**. `reset → report` 가 한 프로세스에서 끝나지 않았다(`reset` 에서 exit 1). `--from` 재개 0회.

## 9. 다음 회차 진입조건

1. `stages.sh` `psql_master_query` 따옴표 결함 고침 ＋ 픽스처(§6 고침 후보 ⑴⑵) — 별도 레인 · `dev-reseed-selftest` green.
2. `stage_reset` 실패 경로에 ①′ 역연산(앱 `start`) 추가 여부 판정(오케스트레이터) — 없으면 정지마다 dev 가 내려간 채 남는다.
3. 위 뒤 `--preflight-only` 10/10 → `--from reset --target-ref b955bece`(dev 는 그 sha 그대로 · 데이터 무변이라 전제 동일).
4. 계정 단계·UI 실사·계수표는 seed·verify 가 돈 회차에서 처음 나온다.
