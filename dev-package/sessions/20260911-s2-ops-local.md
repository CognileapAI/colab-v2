# R-S2-OPS 로컬 구현·검증

## 결과

- I4의 추적·구조화 로그·상태형 알람·레지던시 선언은 로컬 구현과 자동 검증이 끝났다.
- IS4 맨몸 복구 runner와 selftest는 섰고 실제 read-only import/plan도 수행했다. 다만 provider sensitivity metadata 때문에 원격 값 변경 0인 metadata-only plan이 남아, 원격 apply 없이 요구한 축자 No changes까지 갈 수 없었다.
- I3의 15행은 종전 판정 그대로 닫힘 11, 열림 1, 부분 3이다. 로컬 selftest가 자동 트리거·판정기 fail-closed를 다시 증명했지만 실제 무인 trigger, 실제 판정 red→green, 단회 전체 게이트, 배포 전후 health는 남았다.
- main push, 배포, 크론 설치, webhook 연결, Terraform 원격 apply는 0건이다.

## IS4

- 격리 Terraform 1.9.8 컨테이너, 레포 read-only mount, 0700 scratch, 기존 state/.terraform 비복사.
- selftest 7/7: 입력 부재 78, no-op green, metadata-only 허용 관측, delete/value update/대상 0 red, 상태 비유입.
- 실 read-only 리허설은 import 성공, resource 1, add/destroy/replace 0, 원격 값 변경 0이었다.
- scratch refresh-only 뒤에도 sensitivity metadata-only plan이 남아 runner가 78로 멈췄다. 원격 apply 0, scratch 삭제 완료.

## I4

- W3C traceparent 검증과 새 server span, 응답 traceparent, core→viz/AI 전파를 공통 stdlib 커널로 구현했다.
- core/viz/ai 요청 완료와 pipeline worker pass를 colab.ops.v1 JSON 한 줄로 남긴다. query, header, body, 이메일, credential-like 필드를 싣지 않는다.
- 공통 커널은 core 원본과 ai/viz/pipeline 복제본 3개가 byte-identical이며 codegen manifest 13/13으로 고정됐다.
- 상태형 alarm runner는 임계 전 무통지, raise 1회, 반복 무통지, recovery clear 1회, 손상 상태 red, 운영 webhook 부재 78이다.
- 레지던시는 S3/RDS 서울, CloudFront 글로벌 edge, OpenAI provider-managed와 전송/제외 범위를 TOML로 고정했다.
- ops selftest 10건과 gate trace 5, region 3, external 2, alarm 3이 green이다.

## I3 재대조

- sessions/I3.md §6의 15행 기준: 닫힘 11은 기존 배포·롤백·백업·체인·fail-closed·prod 음성·SHA/health/tag 증거를 유지한다.
- 열림 1은 사람이 명령하지 않은 5분 cron trigger의 실제 완주다.
- 부분 3은 배포 판정 단계의 실제 red 뒤 green, 한 번의 전체 게이트 준비실패 0, 작업 전후 공개 health 200→200이다.
- pipeline selftest는 54건과 내장 verify 전건 green, verify selftest는 22건 전건 green이었다.

## 승인 경계

1. Terraform 원격 metadata-only apply.
2. 실제 staging/dev 배포와 의도적 판정 red→green.
3. 호스트 cron 설치.
4. 운영 webhook 파일 배선과 실제 raise/clear 통지.

이 네 가지를 실행하기 전까지 IS4·I3·I4는 완료로 닫지 않는다.
