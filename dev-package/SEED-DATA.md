# SEED-DATA — v2 초기 데이터 인벤토리와 적재 범위

> **WU-S1 산출물** (`WORK-UNITS §8.5` T-S 트랙). 결정 `㊾`(원천 지정·적재는 화면 경로로) · `㊿`(tif 5번째 입력 포맷 · `.npy` 제외).
> **원천** = `F:\00_Project\00 CoLAB\03 Reference-Data` — **3.5 GB · 2,122 파일** (2026-08-23 실측)
> **귀속 연구실** = **고려대학교 수문학연구실 (전창현 교수)** — `〈52〉`. **v2 의 첫 실제 연구실**이고, 모든 시드 데이터셋의 `lab_id` 가 여기를 가리킨다.
> **지원 포맷** = **`NetCDF` · `Binary` · `HDF4` · `GeoTIFF` 4종** — `〈51〉` 로 **GRIB 을 범위에서 뺐다**(원천에 원파일 0건).
> ⚠ PoC 의 「4포맷」(`GRIB·NetCDF·Binary·HDF5`)과 **수만 같고 구성이 다르다.** 숫자가 아니라 목록으로 읽어라.
>
> **이 문서의 지위.** 무엇을 넣고 무엇을 넣지 않는지의 **정본**이다. S2(적재)는 이 표만 보고 움직이고, 여기 없는 것은 넣지 않는다.
> 값의 출처는 **원천 파일과 원천이 동봉한 문서**뿐이다. 원천이 말하지 않는 것은 `[원천 무근거]` 로 남기고 만들지 않는다(`㊴-②` 규율을 원천에 적용).

---

## 0. 먼저 읽을 것 — 범위를 바꾸는 발견 4건

조사 과정에서 **계획을 고쳐야 하는 사실 넷**이 나왔다. 인벤토리보다 이것이 먼저다.

| # | 발견 | 무엇이 바뀌는가 |
|---|---|---|
| **F-1** ✅**해소** | **원천에 GRIB 원파일이 0건이다.** `02.File-format/file_format_1_grib/` 에 `00.Data` 폴더가 **없고**, 남은 것은 파생물뿐이다(`.npy` 8 · `.png` 24 · `.py` 1). 전 원천을 훑어도 `*.grib`·`*.grb` **0건** | **완료 조건 7 / S3 을 원천만으로 닫을 수 없다.** 5포맷 E2E 중 **GRIB 한 칸이 빈다** → **Ted 가 GRIB 원파일을 추가로 주거나**, ERA5 에서 내려받아야 한다. → **`〈51〉` 로 닫혔다: GRIB 을 수급하지 않고 v2 범위에서 뺀다.** 지원 포맷은 `NetCDF·Binary·HDF4·GeoTIFF` 4종. **S3 차단 해제.** C3 의 GRIB 지식(ERA5 경도 0-360)은 v2 가 안 쓰므로 추출 우선순위에서 내린다(지우지는 않는다) |
| **F-2** | **`file_format_5_HDF5` 는 HDF5 가 아니라 HDF4 다.** 매직바이트 `0e 03 13 01` (HDF5 라면 `\x89HDF`). 실체 = MODIS **MOD15A2H** FPAR/LAI, Sinusoidal, 8일 합성 | **D5 의 라이브러리 선택이 바뀐다** — `h5py` 로 못 연다. HDF4 는 `pyhdf` 또는 GDAL 의 HDF4 드라이버가 필요하고, **빌드 의존성이 다르다**(컨테이너 이미지에 영향). 그리고 `PLAN-SoT §7`·C3 가 말하는 「HDF5(MODIS Sinusoidal)」도 **실은 HDF4 였을 가능성**이 있다 → C3 가 `HARVEST.md` 에서 확인한다 |
| **F-3** *(2026-08-23 전수 재측정)* | **원천 tif 62 건을 IFD 바이트 파싱으로 전수 판정했다. COG 는 포맷견본 6 건뿐이다.** `file_format_4_tif`(→`D-14`) 의 6건은 256×256 내부 타일 + **4단 오버뷰 피라미드**(3660→1830→915→458→229)를 가진 Cloud-Optimized GeoTIFF. 실체 = HLS S30(NASA/USGS). **KWRA 51 건 중 COG 는 0 건**(Input 11 = 타일만·오버뷰 없음 / Output 40 = 스트립). vegetation 5 건도 타일만 | **`DR-2` 는 살아 있되 성격이 바뀐다.** ① **긴급성은 내려간다** — 첫 적재(KWRA)에 COG 가 0 건이라 첫 업로드에서 터지지 않는다 ② **판별 규칙의 요구는 올라간다** — **타일링만 보고 「COG 다」로 판정하면 16 건**(KWRA Input 11 + vegetation 5)**을 전부 우리 산출물로 오인한다.** 오버뷰까지 봐야 하고, **「타일만 있는 것」을 어느 쪽으로 볼지 정본이 답해야 한다** |
| **F-4** | **`.bin` 의 포맷 명세는 `.ini` 에 없다.** 원천의 `.ini` **74건 전부가 Windows `desktop.ini`**(폴더 보기 설정)로 내용 가치 0. 실제 명세는 **`01.Code/Lv1_Data Processing.py` 안에** 있다 — 1024바이트 int8 헤더, int16 little-endian, `nx=2305`·`ny=2881`, `/100` 스케일, Marshall-Palmer Z→R | 명세가 **코드에만 있다.** 그 코드가 사라지면 이 포맷은 못 읽는다 → **C3 `HARVEST.md` 가 이 파일을 반드시 흡수해야 한다.** ⚠ **추가 경고**: 코드가 가정한 3블록 구성은 압축 해제 시 **~39.8 MB** 인데 `gunzip -l` 실측은 **13.28 MB** 다. **반사도 블록만 실재할 가능성이 높다** — 파서를 짜기 전에 이 불일치를 먼저 풀어야 한다 |

> **가설 하나가 틀렸다.** 조사 착수 시 「`.ini` 74건이 바이너리 스펙일 것」으로 보고 그것을 최우선 과제로 걸었는데, **전부 `desktop.ini` 였다.** 명세는 코드에 있었다(F-4).

---

## 1. 원천 실측 — 전체 구성

```
03 Reference-Data/                     3.5 GB · 2,122 파일
├── 01.level-data/          주제별 · 가공 단계별 (Lv.0 → Lv.1 → Lv.2)
│   ├── 01.precipitation/   40 파일 · 103 MB   강우
│   ├── 02.vegetation/      80 파일 · 347 MB   식생
│   └── 03.drought/          5 파일 ·  49 MB   가뭄
├── 02.File-format/         포맷별 견본 + 처리 코드
│   ├── file_format_1_grib/  39 파일 ·  49 MB  ⚠ 원파일 0건 (F-1)
│   ├── file_format_2_nc/   157 파일 ·  59 MB  GK2A AMI L2 LST · LCC · 10분
│   ├── file_format_3_bin/   47 파일 · 689 MB  HSR 레이더 합성 · 5분
│   ├── file_format_4_tif/   27 파일 · 885 MB  HLS S30 · ⚠ 6/6 이미 COG (F-3)
│   └── file_format_5_HDF5/  86 파일 · 1.3 GB  ⚠ 실체는 HDF4 · MODIS (F-2)
└── 3_KWRA_conference/    1,639 파일 · 116 MB  학회 산출물 + 다운스케일 예제
```

### 확장자 분포 (전체 2,122)

| 확장자 | 수 | 성격 |
|---|---:|---|
| `.npy` | **1,622** | NumPy 중간 배열 — **전량 제외**(`㊿`) |
| `.nc` | 184 | NetCDF4 — ⚠ 실체는 HDF5 컨테이너 (`DR-3`) |
| `.png` `.jpg` `.jpeg` `.avif` | 124 | 시각화 산출 이미지 |
| `.tif` | **62** | GeoTIFF — 6(포맷견본, 이미 COG) + 51(KWRA) + 5(기타) |
| `.gz` | 22 | `*.bin.gz` — HSR 레이더 바이너리 |
| `desktop.ini` | 74 | Windows 폴더 설정 — **내용 가치 0** (F-4) |
| `.py` `.ipynb` | 14 | 처리·시각화 코드 — **명세의 실소재** |
| `.docx` `.pptx` `.pdf` `.md` | 10 | 원천 동봉 문서 — **메타데이터의 출처** |
| `.hdf` | 8 | ⚠ HDF**4** · MODIS MOD15A2H (F-2) |
| `.gpkg` | 2 | GeoPackage — SPI/SPEI 가뭄지수 |

---

## 2. 배정 결과 — 2,122 파일 전부

**S1 완료 판정 = 미분류 0.** 아래 세 분류의 합이 2,122 여야 한다.

| 분류 | 파일 수 | 근거 |
|---|---:|---|
| **① 초기 데이터로 넣는다** | **278** | 아래 §3 의 데이터셋으로 적재 |
| **② 시험용으로만 쓴다** | **148** | 카탈로그에 안 세운다. D5 파서·감지 픽스처 |
| **③ 넣지 않는다** | **1,696** | 아래 사유별 |
| **합계** | **2,122** | ✅ **미분류 0** |

### ① 넣는다 (278)

| 묶음 | 파일 수 | 포맷 |
|---|---:|---|
| 강우 Lv.0 HSR | 10 | `.bin.gz` |
| 강우 Lv.0 RN15 | 10 | `.nc` |
| 식생 Lv.0 GK2A NDVI | 31 | `.nc` |
| 식생 Lv.1 보조입력 4종 | 4 | `.tif` 등 |
| 가뭄 Lv.1 SPI/SPEI | 2 | `.gpkg` |
| 포맷견본 nc (GK2A L2 LST) | 141 | `.nc` |
| 포맷견본 hdf (MODIS) | 8 | `.hdf`(HDF4) |
| 포맷견본 tif (HLS S30) | 6 | `.tif`(이미 COG) |
| 포맷견본 bin (HSR) | 12 | `.bin.gz` |
| **KWRA 다운스케일 Input** | 11 | `.tif` (NDVI 10 + DEM 1) |
| **KWRA 다운스케일 Output 4종** | 40 | `.tif` |
| 격자 정보 (Lat/Lon) | 2 | `.nc` |
| 기타 tif | 1 | `.tif` |

### ② 시험용 (148)

- **원천 동봉 문서 10건**(`.docx` 5 · `.pptx` 2 · `.pdf` 2 · `.md` 1) — 데이터가 아니라 **메타데이터의 출처**. 사람이 읽고 데이터셋 설명란을 채우는 데 쓴다
- **처리·시각화 코드 14건**(`.py` 13 · `.ipynb` 1) — **D5 재구현의 명세**(F-4). C3 `HARVEST.md` 입력
- **시각화 이미지 124건** — 적재 결과가 맞는지 **눈으로 대조하는 정답지**. 카탈로그에 안 세운다

### ③ 넣지 않는다 (1,696)

| 사유 | 수 |
|---|---:|
| **`.npy` — 중간 산출물** (`㊿`) | 1,622 |
| **`desktop.ini` — Windows 폴더 설정** | 74 |

> **`.npy` 제외의 근거가 조사로 굳어졌다.** 학회 폴더에서 npy 를 **생성하는** 스크립트가 없고, 유일한 관련 스크립트(`Temperature_TotalPrecipitation_Vis.py`)는 **읽기만** 하며 원본 경로가 원작자 로컬(`M:\02_ERA5_npyfile\...`·`H:\#lat_lon_info\...`)로 하드코딩돼 있다. 즉 **출처를 되짚을 수 없는 캐시**다. 카탈로그에 데이터셋으로 세우면 **계보가 시작부터 거짓**이 된다.
> **제외는 삭제가 아니다.** 원천에는 그대로 둔다.

---

## 3. 데이터셋 경계 — 폴더를 어떻게 자르는가

지배 규칙은 `DATAMODEL-BASELINE §4.3`:

> **포맷 · 변수 목록 · 좌표계 · 격자 — 모든 조각이 같아야 한다. 하나라도 다르면 조각이 아니라 다른 데이터다.**
> 기간 = 조각들의 **합집합** · 용량 = **합계** · 마지막 수정 = 조각 중 **가장 늦은 값**

그리고 두 문장이 경계를 마저 정한다:

- **「역할이 다른 파일은 한 데이터셋에 담지 않는다」** — 담으면 계보가 데이터셋 층·파일 층으로 갈라져 어긋났을 때 판정할 수 없다
- **「파일에는 계보가 없다. 계보는 데이터셋 사이에만」** — 그래서 **자르는 방식이 곧 계보의 해상도**다

### 3.1 데이터셋 목록 (제안 · 15건)

| # | 데이터셋 이름 | 주제 | Lv | 조각 | 좌표계 | 격자 |
|---|---|---|:--:|---:|---|---|
| **D-01** | GK-2A NDVI 2 km 한반도 (2024-06-15~24) | 식생·NDVI | 0 | 10 | EPSG:4326 | 852×1200 |
| **D-02** | DEM 100 m 충청권 | 지형·DEM | 0 | 1 | EPSG:4326 | 1280×1280 |
| **D-03** | GK-2A NDVI 1 km 충청권 — Nearest | 식생·NDVI | 1 | 10 | EPSG:4326 | 126×128 |
| **D-04** | GK-2A NDVI 1 km 충청권 — Bilinear | 식생·NDVI | 1 | 10 | EPSG:4326 | 126×128 |
| **D-05** | GK-2A NDVI 1 km 충청권 — IDW | 식생·NDVI | 1 | 10 | EPSG:4326 | 126×128 |
| **D-06** | GK-2A NDVI 1 km 충청권 — Co-Kriging | 식생·NDVI | 1 | 10 | EPSG:4326 | 126×128 |
| **D-07** | HSR 레이더 합성 반사도 (Lv.0) | 강우·강수 | 0 | 10 | `[원천 무근거]` | 2305×2881 |
| **D-08** | RN15 지상 강수 (Lv.0) | 강우·강수 | 0 | 10 | `[원천 무근거]` | `[원천 무근거]` |
| **D-09** | GK2A/AMI NDVI 원자료 (Lv.0) | 식생·NDVI | 0 | 31 | LCC | `[원천 무근거]` |
| **D-10** | 식생 모델 보조입력 4종 | 지형·DEM 외 | 0 | 4 | `[원천 무근거]` | `[원천 무근거]` |
| **D-11** | SPI/SPEI 가뭄지수 (L1 Calibrated, 2000-2025 주단위) | `[주제 무근거]` | 1 | 2 | WGS84 | 벡터 |
| **D-12** | GK2A AMI L2 LST 견본 | `[주제 무근거]` | 0 | 141 | LCC | `[원천 무근거]` |
| **D-13** | MODIS MOD15A2H FPAR/LAI 견본 | 식생·NDVI | 0 | 8 | Sinusoidal | `[원천 무근거]` |
| **D-14** | HLS S30 견본 (이미 COG) | 식생·NDVI | 0 | 6 | `[원천 무근거]` | 3660×3660 |
| **D-15** | HSR 레이더 견본 | 강우·강수 | 0 | 12 | `[원천 무근거]` | 2305×2881 |

> **왜 D-03~D-06 을 넷으로 쪼개는가.** 같은 날짜·같은 격자·같은 포맷이라 한 데이터셋으로 합치고 싶어 보이지만, **가공 방식이 다르다.** 합치면 「어느 픽셀이 어느 방법의 결과인가」를 말할 수 없고, 무엇보다 **계보 관계에 붙는 가공 방식이 넷인데 자식이 하나**가 되어 버린다(`§4.2` — 가공 방식은 데이터셋이 아니라 **관계에 부착**된다). **넷으로 나누고 계보로 잇는 것이 정본이 시킨 모양이다.**
>
> **왜 D-01 과 D-02 를 나누는가.** 둘 다 KWRA `Input/` 한 폴더에 있지만 **변수도 격자도 다르다**(NDVI 852×1200 vs DEM 1280×1280). 합치는 규칙이 곧바로 금지한다 — *「하나라도 다르면 조각이 아니라 다른 데이터다」*.
>
> **`[원천 무근거]` 를 채우지 않았다.** 좌표계·격자는 **파일 헤더에서 자동으로 읽는 값**이고(`DATAMODEL §4.1` — *「파일에서 자동, 사람이 타이핑하지 않는다」*), 이 환경에는 지오 라이브러리가 없어 읽을 수 없었다(§7). **S2 가 실제로 파싱할 때 채워진다.** 여기서 추정해 적으면 그 값이 정본 행세를 한다.

---

## 4. 계보 후보 — 원천이 이미 가지고 있는 것

**이 원천의 가장 큰 값은 파일이 아니라 계보다.** 세 갈래가 이미 구조로 존재한다.

### 4.1 KWRA 다운스케일 — 1:4 팬아웃 (가장 완결적)

```
D-01  GK-2A NDVI 2 km 한반도 (Lv.0)
  │
  ├─[주입력]──→ D-03  Nearest    1 km   가공 방식 = 최근린보간
  ├─[주입력]──→ D-04  Bilinear   1 km   가공 방식 = 이중선형보간
  ├─[주입력]──→ D-05  IDW        1 km   가공 방식 = IDW (k=12, p=2)
  └─[주입력]──→ D-06  Co-Kriging 1 km   가공 방식 = Regression Kriging
                        ▲
D-02  DEM 100 m 충청권 ──[보조입력]──┘   (Co-Kriging 의 보조 변수)
```

**부모 하나에 자식 넷, 관계마다 가공 방식이 다르다.** `DATAMODEL §4.2` 가 *「가공 방식을 데이터셋에 붙이면 부모가 여럿일 때 어느 부모의 가공인지 말할 수 없다」* 며 **관계에 부착**하기로 한 그 설계의 실물 예제다.

그리고 **D-02 → D-06 관계가 `보조입력`이다.** 원천 README 가 명시한다 — *「DEM: 고도 격자. 본 예시의 **Co-Kriging** 에서 보조 변수로 사용」*. 이는 Lv 계산 규칙 *「(주입력 부모 중 최대 Lv) + 1, **보조입력은 계산에서 뺀다**」* 을 검증하는 자리다 — **D-06 의 Lv 는 1 이어야 하고, DEM(Lv.0)이 그 값을 바꾸지 않아야 한다.**

> **S2·P3 의 검증 자산.** 이 묶음 하나로 카탈로그·계보 그래프·시각화·다중 부모·부모 역할 2값·가공 방식 부착이 **한꺼번에** 채워진다. **S2 는 이것을 가장 먼저 적재한다.**

### 4.2 level-data — Lv.0 → Lv.1 → Lv.2 사슬

원천 폴더가 **가공 단계를 폴더 이름으로 이미 선언**하고 있다(`Lv.0` · `Lv.1` · `Lv.2`).

| 주제 | 사슬 | 원천 문서가 밝힌 가공 |
|---|---|---|
| **강우** | Lv.0 (HSR 반사도 + RN15 지상강수) → Lv.1 → Lv.2 | WGS84 변환 · crop · U-Net 예측. *Z-R 관계식(Marshall-Palmer)의 한계를 U-Net 이 보완* |
| **식생** | Lv.0 (GK2A NDVI, LCC) → Lv.1 → Lv.2 | DQF 필터 · WGS84 변환 · crop · 월평균 · 100 m 균등분할 → U-Net 공간상세화 |
| **가뭄** | **Lv.1 만 있다** | SPI/SPEI 산정. **Lv.0 이 원천에 없다** |

**식생 사슬은 보조입력 4종이 병렬로 들어간다** — `Lv.1_(Model_Input_Data)` 의 DEM · Aspect · LULC · HLS_S30_NDVI. 이것도 `보조입력` 부모 역할이다.

> **원천 문서가 Lv.1 과 Lv.2 의 차이를 스스로 대조해 놨다** — *「Lv.1 은 값이 변하지 않는 균등분할, Lv.2 는 U-Net 으로 지형에 따라 값이 실제로 달라진다」*. 데이터셋 설명란에 그대로 쓸 문장이다.
>
> ⚠ **가뭄은 `원천` 계보 상태로 들어간다.** Lv.0 이 폴더에 없고, 문서는 *「기상관측소 강우 관측자료에서 산정」* 이라고만 말한다. 강우 폴더의 자료와 물리적으로 이어지는지 **확인할 수 없다** — 이을 근거가 없으므로 **잇지 않는다.** 계보 상태 4값 중 **`원천`** 이 정확히 이 경우다.

### 4.3 포맷 견본 — 계보 없음

`02.File-format/` 의 견본들은 **서로 이어지지 않는다.** 전부 Lv.0 · 계보 상태 `원천` 이다. 목적이 다르다 — **D5 파서가 5포맷을 실제로 여는지 보는 것**이다.

---

## 5. 온톨로지 매핑 — D9 시드 22행과의 대조

K2 가 **정본 인용만으로** 22행을 적재했고 `[정본 무근거]` 5건을 남겼다. 이제 **실데이터 쪽 어휘**가 생겼으므로 대조한다.

> **주의 — 이 절은 K2 를 자동으로 고치지 않는다.** `㊸-④-2` 가 **「문서가 정본」**이고 `㊴-②` 가 **「정본에 없으면 만들지 않는다」**로 못 박았다. 실데이터에서 어휘가 나왔다는 것은 **정본 갱신을 제안할 근거**이지, 시드에 바로 넣을 권한이 아니다. **아래는 K2 재방문 후보 목록이다.**

### 5.1 가공 방식 — 시드 13개 vs 실데이터

| 실데이터 어휘 | 출처 | D9 시드 대응 | 판정 |
|---|---|---|---|
| 최근린보간 (Nearest) | KWRA README §4 | `보간 방식(선형/최근접)` | ✅ 커버 |
| 이중선형보간 (Bilinear) | KWRA README §4 | `보간 방식(선형/최근접)` | 🟧 부분 — 「선형」에 걸치나 이름이 다르다 |
| **IDW (k=12, p=2)** | KWRA README §4 | 없음 | ⬜ **신규 후보** |
| **Regression Kriging / Co-Kriging** | KWRA README §4 | 없음 | ⬜ **신규 후보** |
| 재격자화 / 다운스케일 | 원천 다수 | `재격자화` · `다운스케일` | ✅ 커버 |
| **DQF 마스킹 / 품질검사(QC)** | 식생 처리 문서 | `품질검사` | ✅ 커버 |
| **LCC→WGS84 재투영** | 식생·강우 처리 문서 | 없음(`재격자화` 와 다르다) | ⬜ **신규 후보** |
| **ROI 크롭 / 연구영역 자르기** | 처리 문서 다수 | `유역 클리핑` · `유역 경계로 잘라냄` | 🟧 부분 — **유역이 아니라 사각 영역**이다 |
| 월평균 시간집계 | 식생 처리 문서 | `일 단위 평균` | 🟧 부분 — 축이 다르다(월 vs 일) |
| **Savitzky-Golay 필터 + 선형보간** (d=2, 23일창) | KWRA 문서 | 없음 | ⬜ **신규 후보** |
| **source thinning** (2배 thin ≈25%) | KWRA README §8 | 없음 | ⬜ **신규 후보** |
| **U-Net 공간상세화 / 예측** | 강우·식생 문서 | 없음 | ⬜ **신규 후보** — 딥러닝 가공이 시드에 아예 없다 |

### 5.2 주제 — 4값 대조

정본 4값 = `강우·강수` / `식생·NDVI` / `지형·DEM` / `토지피복·LULC`.

| 실데이터 | 배정 |
|---|---|
| NDVI · EVI · FVC · HLS NDVI · FPAR/LAI | **식생·NDVI** |
| HSR 반사도 · RN15 · Total Precipitation(tp) | **강우·강수** |
| DEM · Aspect | **지형·DEM** |
| LULC | **토지피복·LULC** |
| **SPI / SPEI (가뭄지수)** | ⛔ **4값 어디에도 안 들어간다** |
| **LST (지표면온도)** · **기온 t850** | ⛔ **4값 어디에도 안 들어간다** |

> **저장 자리는 있다** (2026-08-23 확인) — `d3_dataset_description.topic`(`db/platform/schema.sql:244`). `d3_dataset` 이 아니라 **「사람이 적는 정보」 1:1 곁테이블**이고, 이는 `DATAMODEL §4.1`(*이름·주제·설명*)이 시킨 모양 그대로다. 시스템이 채우는 값과 사람이 적는 값을 가른 것이다.
>
> ⚠ **그런데 DB 가 4값을 강제하지 않는다.** `topic` 은 **CHECK 없는 nullable text** 다. `⑲`(값 집합은 DB가 강제)가 열거한 **확정 열거값 9종에 주제가 없고**(역할·권한스위치·파일종류·부모역할·만들어진경로·계보상태·접근상태·프로젝트상태·프로젝트유형), `㊸-④-2` 의 주제 4값은 **그 뒤에 정해졌다.** 4값 CHECK 가 걸린 곳은 **`d9_topic_synonym`(ai 체인) 뿐이고 그것은 사전을 지키지 데이터셋의 주제를 지키지 않는다.**
> **S2 에 걸리는 것** — 적재가 `유출·수문` 같은 값을 넣어도 **DB 는 막지 않는다.** `D-11`·`D-12` 의 `[주제 무근거]` 는 **NULL 로 남길 수 있다**(열이 nullable 이므로 규칙 위반이 아니다). **비워 두는 쪽을 택한다** — 없는 값을 지어내는 것보다 낫고, 나중에 사람이 채울 수 있다.
> ✅ **결정됐다 — `〈55〉` 주제를 DB 열거값으로 강제한다** (2026-08-23 Ted). `topic` 에 4값 CHECK 를 걸고 **nullable 은 유지한다**(「값이 있다면 넷 중 하나」이지 「반드시 있다」가 아니다). **`D-11`·`D-12` 는 NULL 로 적재한다** — 억지로 배정하면 검색·분류가 조용히 틀리고, 4값을 늘리는 것은 정본 개정이라 여기서 할 일이 아니다.

> ⚠ **주제 4값이 실데이터를 다 담지 못한다.** 가뭄지수·지표면온도·기온이 남는다. **이건 시드 문제가 아니라 정본 문제**이고, `㊸-④-2` 가 *「목업 쪽 불일치는 정본 갱신 대상으로 표시」* 한 것과 같은 성격이다. **Ted 판단이 필요하다** — 4값을 늘릴 것인가, 이 데이터셋들을 넣지 않을 것인가, 억지로 배정할 것인가. **기본값 = 정본을 안 건드리고 `[주제 무근거]` 로 남긴다**(D-11·D-12).

### 5.3 지명 별칭 — 시드 4개 vs 실데이터

시드 = `낙동강 유역` · `한강 상류` · `금강 하굿둑` · `한강 유역` — **전부 유역·하천 이름**이다.

실데이터의 지명은 **성격이 다르다**:

| 실데이터 표기 | 출처 | 비고 |
|---|---|---|
| **충청권** | KWRA README (한글) | 좌표: 126.70°E~127.96°E, 36.08°N~37.36°N |
| **southern Gyeonggi and Chungcheong regions** | KWRA docx (영문) | **위와 같은 영역을 다르게 부른다** |
| 한반도 전역 | KWRA README | 852×1200 격자 |
| Korea | 폴더명 | |

> ⚠ **같은 영역에 이름이 둘이다** — `충청권` 과 `southern Gyeonggi and Chungcheong regions`. **`d9_place_alias` 가 정확히 이런 것을 위한 표**인데, 시드 4개는 전부 유역 이름이라 **행정권역 별칭이 한 건도 없다.** K2 재방문의 가장 분명한 후보다.
>
> 또한 **시드의 유역 이름 4개는 이 원천에 한 건도 나타나지 않는다.** 시드와 실데이터가 **서로 다른 공간 어휘 체계**를 쓰고 있다는 뜻이다 — K3(계보 제안)·K4(검색)가 실데이터 위에서 돌 때 **사전이 헛돌 수 있다.**

### 5.4 출처·기관 어휘 (시드에 자리가 없다)

`GK-2A(천리안위성2A호)` · `NMSC(국가기상위성센터)` · `기상청 API허브` · `MODIS MOD15A2H` · `HLS S30(NASA/USGS)` · `ERA5` · `한국수자원학회(KWRA)`

> 이들은 `원천 표기`(연구실 밖 출처 이름 · 계보 그래프의 **점선 노드**) 필드에 들어갈 값이다. **D9 시드 3표에는 이 범주가 없다** — `㊸-④-4` 가 시드 테이블을 3개(가공방식·주제동의어·지명별칭)로 못 박았기 때문이다. **정상이다.** 출처는 D4 의 저장 필드이지 D9 사전이 아니다.

---

## 6. S2 적재 순서 (권고)

| 순서 | 대상 | 왜 이 순서인가 |
|:--:|---|---|
| **1** | **KWRA 다운스케일 D-01~D-06** | 계보 1:4 팬아웃 · 보조입력 · 가공 방식 4종이 **한 묶음에 다 있다.** 여기가 green 이면 계보 기능 전체가 증명된다. ✅ **`DR-2` 는 여기서 터지지 않는다** — 전수 측정 결과 이 묶음의 tif 51 건 중 **COG 는 0 건**이다. 다만 **Input 11 건이 「타일만 있고 오버뷰 없음」**이라 **판별 규칙이 허술하면 여기서 오인한다** |
| **2** | **level-data 3주제 (D-07~D-11)** | Lv 사슬과 `원천` 상태(가뭄)를 동시에 실측 |
| **3** | **포맷 견본 (D-12~D-15)** | D5 파서 검증. **GRIB 은 여기서 빠진다**(F-1) |

---

## 7. D5·C3 로 넘기는 것

| # | 넘기는 것 | 받는 곳 |
|---|---|---|
| **DR-2** | tif 는 입력이자 출력이다. 감지가 *이미 COG* 와 *일반 GeoTIFF* 를 구분 못 하면 **계보가 오염된다.** **픽스처가 원천에 세 부류로 다 있다**(전수 측정): ① **진짜 COG** = 포맷견본 6 건 ② **타일만·오버뷰 없음 16 건**(KWRA Input 11 + vegetation 5) — **여기가 급소다** ③ **평범한 스트립 GeoTIFF** = KWRA Output 40 건. **①만으로는 규칙을 못 짠다** — ②를 어느 쪽으로 볼지가 규칙의 실제 내용이다 | D5 (구분 규칙 + **음성 시험**) · **정본**(②의 판정) |
| **DR-3** | **확장자가 거짓말을 한다** — `.nc` 가 실제로는 HDF5 컨테이너. **감지는 매직바이트 기준이어야 한다** | D5 |
| **F-2** | **`.hdf` 는 HDF4 다.** `h5py` 로 안 열린다 → `pyhdf`/GDAL HDF4 드라이버. **컨테이너 빌드 의존성이 바뀐다** | D5 · C3 |
| **F-4** | **`.bin` 명세는 코드에만 있다** (`01.Code/Lv1_Data Processing.py`). ⚠ **헤더 가정과 실제 크기가 안 맞는다**(39.8 MB 계산 vs 13.28 MB 실측) | C3 `HARVEST.md` · D5 |
| **F-1** | **GRIB 원파일이 없다** → S3(5포맷 E2E)를 닫을 수 없다 | **Ted** (추가 제공 or ERA5 수급) |

---

## 8. 조사 방법과 한계 — 무엇을 못 봤는가

**정직하게 적는다. 이 문서는 파일을 연 것이 아니라 겉을 본 것이다.**

- **지오 라이브러리가 이 환경에 없다** — `rasterio` · `netCDF4` · `h5py` · `xarray` · `cfgrib` · `numpy` · GDAL CLI(`gdalinfo`·`ncdump`·`h5dump`) **전부 미설치**. 그래서 판정은 **파일명 · 매직바이트 · `strings` · 원천 동봉 문서 · 처리 코드**에서 나왔다
- **따라서 좌표계·격자·변수 목록·기간의 상당수가 `[원천 무근거]` 다.** 이 값들은 `DATAMODEL §4.1` 상 **파일에서 자동으로 읽는 값**이라 **S2 가 실제로 파싱할 때 채워진다.** 여기서 추정해 적으면 그 추정이 정본 행세를 한다
- ✅ **tif 는 전수 측정했다**(2026-08-23 재측정) — 62 건 전부 TIFF IFD 를 바이트 단위로 파싱해 태그 322/323(타일) vs 278(스트립) + IFD 개수(오버뷰)로 판정했다. **미확인 0 건 · BigTIFF 0 건.** 두 세션이 각자 독립 구현으로 같은 수를 얻었다. **이 항목만은 추정이 아니다**
- **`레이더합성자료포맷정보.pdf` 를 못 읽었다** — CID 인코딩이고 `pdftotext`(poppler)가 없다. 그림 3장만 확인. **바이너리 크기 불일치(F-4)의 답이 이 PDF 에 있을 수 있다**
- **파일 수 배정(§2)은 확장자·경로 기준 집계다.** 개별 파일을 하나하나 열어 확인한 것이 아니다

> **S1 을 닫는 조건은 「미분류 0」이지 「전부 해독」이 아니다.** 해독은 S2 가 파서를 돌리며 한다. 다만 **F-1(GRIB 부재)·F-2(HDF4)** 는 해독 이전의 사실이라 지금 확정으로 올린다.

---

## 9. 연구실 귀속 — 닫혔다

`〈52〉` **고려대학교 수문학연구실 (전창현 교수).**

S1 은 이 칸을 채우지 못해 `🟧 부분` 으로 서 있었다. 데이터셋은 `연구실 1:N` 이라 **연구실 행이 먼저 있어야** 귀속이 성립하는데 받을 연구실이 정해지지 않았고, 없는 값을 지어내면 그 값이 정본 행세를 하므로 비워 뒀다. **2026-08-23 Ted 가 답해 닫혔다.**

**원천 스스로도 같은 말을 하고 있었다** — 가뭄 자료의 `[Data Info]SPI-4weeks.docx` · `[Data Info]SPEI-4weeks.docx` 메타데이터 카드에 생산자가 **`차호영` · `고려대학교`** 로 적혀 있다. 사람이 준 답과 원천의 기록이 일치한다.

### 따라오는 것 — 이것이 v2 의 첫 실제 연구실이다

지금까지 staging 의 연구실은 **P0 이 세운 시험용**이었다. 그래서 이 값은 이름 하나가 아니라 **테넌시의 실체**다.

- 시드 데이터셋 **15건 전부의 `lab_id`** 가 이 연구실을 가리킨다
- **RLS 경계가 처음으로 실데이터 위에서 의미를 갖는다** — `D3b` 의 음성 시험이 픽스처가 아니라 **진짜 자료**를 지키게 된다
- `연구실 정보 5필드` · `데이터 공개 범위(기본값)` 에 실제 값이 필요해진다 (S2 착수 시)

### 적재 계정 — 닫혔다

`〈53〉` **초기 계정 4명을 PoC 에서 이어받는다. `올린 사람` = 전창현(`jch@korea.ac.kr`).**

출처 = `00 CoLAB-PoC/backend/alembic/versions/0005_add_users_and_upload.py:57-61` 시드. **전원 `organization = 'Korea University HyMetS'`** — `〈52〉` 의 고려대 수문학연구실이 PoC 에도 같은 실체로 있었다.

| 이메일 | 이름 | 역할(v2 2값) |
|---|---|---|
| **`jch@korea.ac.kr`** | **전창현** | **교수** |
| `sy@korea.ac.kr` | 구서영 | 연구원 |
| `jj@korea.ac.kr` | 백종진 | 연구원 |
| `jy@korea.ac.kr` | 변종윤 | 연구원 |

**교차 검증** — KWRA 학회 폴더의 작성자 서명 `jbyun`·`syku` 가 **변종윤·구서영**이다(§4 조사). 계정 명단과 실데이터가 서로를 뒷받침한다.

**옮기는 것은 이름·메일·역할뿐이다.** PoC 는 `password_hash` 열에 **평문**이 들어 있고 **4명이 같은 값**이다 — **인증 정보는 옮기지 않는다.** v2 인증은 새로 세운다.

**`jybyun@korea.ac.kr` 는 쓰지 않는다.** 초안 목업 문서에만 나오고 시드에 없다. 변종윤의 계정은 `jy@korea.ac.kr` 다 — `UNIQUE (lab_id, email)` 아래서 주소가 둘이면 **다른 사람 둘**이 된다.

### `올린 사람` 과 `소유자` 를 갈라 둔다

| 필드 | 값 | 왜 |
|---|---|---|
| **올린 사람** | **전창현 고정** | **바뀌지 않는 값**(정본 §4.1 「승계해도 그대로」)이라 **적재 주체 하나**로 고정하는 게 맞다 |
| **소유자** | 실제 생산자로 **승계** | 승계 가능한 값이다. 그래야 「누가 넣었나」와 「누구 자료인가」가 **각자 참말**이 된다 |

> ⚠ **미결 1건 — 차호영 계정이 없다.** 가뭄 자료(`D-11` SPI/SPEI)의 생산자가 `[Data Info]` 문서에 **`차호영`·`고려대학교`** 로 적혀 있는데 **PoC 4명에 없다.**
> ✅ **닫혔다 — (나) 채택** (2026-08-23). Ted 가 **모르는 사람**이라고 답했다. 계정을 만들지 않고 `D-11` 소유자를 전창현 교수로 둔다.
> **다만 출처는 버리지 않는다** — `d3_dataset.source_label`(**원천 표기** · 계보 그래프의 **점선 노드**, 정본 §4.1)에 **`차호영(고려대학교)`** 를 적는다. 연구실 밖 출처를 적는 자리가 정확히 이것이라, **계정을 지어내지 않으면서 자료의 출처도 잃지 않는다.**

