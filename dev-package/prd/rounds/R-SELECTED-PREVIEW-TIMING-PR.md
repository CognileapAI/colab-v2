# 선택한 파일만 미리보고 요청별 소요 시간을 확인합니다

Plan-Ref: dev-package/prd/rounds/R-SELECTED-PREVIEW-TIMING.md
Head-SHA: 미커밋 — 게시 전 실제 커밋 SHA 입력
검증 상태: 로컬 검증 통과, 원격 CI 미실행

## 목적
상세 조회만으로 렌더가 쌓이지 않게 하고 선택한 미리보기 한 건의 시간을 확인합니다.

## 범위
‘보기’ 명시 실행, 중복 요청 방지, 요청별 시간 기록, 재적재 검증 도구와 타일 높이 수정. 좌표 오류 수정·배포·초기화는 제외합니다.

## 계획
구현·수용 검토·통합 시험·로컬 브라우저 확인을 마쳤습니다. 게시와 병합은 사용자가 진행합니다.

## 결정
기존 API·로그를 사용합니다. 응답을 잃으면 새 요청 대신 기존 작업을 확인합니다. DB 변경은 없습니다.

## 검증
선택 전 실행 0건 → 보기 후 선택 파일 1건. 그림 표시 뒤에만 시간 표시.
화면 1,522건·서버 541건·재적재 자체시험 104건·관련 도구 시험 58건 통과. 타입 오류 0, 시각 검사 1페이지 통과. 필수 게이트 각각 성공 1 / 판정 실패 0 / 준비 실패 0.

Evidence-Ref: /home/ttlhi10/.local/state/colab/selected-preview-20260916/evidence-index.json
Evidence-SHA256: f8af5db07f6b38678f826a194bb41b4b03e9f0869b5c63e3bcab6f0ec7d34765
CI-Ref: 로컬 검사 근거는 Evidence-Ref 참조. 원격 CI 미실행

## 남은 제약
아직 미커밋·미배포입니다. 브라우저 검증은 모의 응답을 쓰므로 실제 자료 렌더 속도를 의미하지 않습니다. 변경 검토·커밋 후 Head-SHA를 채우고, develop 대상 PR을 게시합니다. 병합 후 dev에서 확인합니다.
로컬 글꼴 파일 로딩이 차단돼 시각 검사는 대체 폰트 기준입니다.
