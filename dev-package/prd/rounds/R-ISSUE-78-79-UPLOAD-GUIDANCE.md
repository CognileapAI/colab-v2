> spec: dev-package/prd/specs/2026-09-16-issue-78-upload-input-guidance.md
# #78·#79 업로드 입력 안내·등록 흐름 실행 계획

**Goal:** #79 선택 제거를 유지하며 #78의 안내·필수 조건을 정렬한다.
**Architecture:** 기존 입력 컴포넌트·등록 검증기를 확장한다. DB 변경 없이 등록 계약의 필수만 개정한다.
**Tech Stack:** React/TypeScript, Python core-api, OpenAPI, Vitest/pytest, agent-browser.
**Spec:** 위 링크 및 승인된 #79 spec.

## Global Constraints
- 사용자 명시 승인으로 관측 간격·Lv0 출처 필수화. 이전 판정은 이력 보존.
- 서버 구현자는 별도 사본에서 서버/계약/서버시험만, 부모는 주 사본에서 프런트/문서/통합 검증을 소유한다. 동일 체크아웃 병렬 쓰기 금지.
- 생성 타입은 생성기로 만든다. 새 의존성/CSS/DB 마이그레이션 없음.
- 기존 #79 로컬 변경을 승인된 입력으로 보존하고 함께 검증한다.

## 단계
- [x] 첨부 및 기존 필수/선택 판정 대조, 사용자 결정 확정.
- [x] 계획 수용 검토 후 서버 레인 준비.
- [x] 실패 시험: 신규 등록의 관측 간격·Lv0 출처 누락 거절, 프런트 누락 시 요청0회와 안내.
- [x] 서버/계약 필수 조건 및 생성 타입, 관련 정상 fixture 정렬.
- [x] 프런트 배지·필수 검증·7개 예시/문면 및 단계별 안내. 기존 성공시험 fixture에 실제 필수값 보완.
- [x] 서버 레인 독립 수용 검토 및 19파일 hash 일치 회수. 신규 등록 하위 null 제약과 기존 null 자료 PATCH 회귀 포함.

## 통합 검증·인계 단계
다음의 현재 판정은 아래 task runtime 및 RESULT.md에 기록한다. 이 계획의 중간 수치를 최종 결과로 대체하지 않는다.
1. 계약·전체 프런트·서버 선언 8게이트 실행.
2. #78/#79 실제 브라우저 등록·재조회 및 시각 검사.
3. 최종 intent 미달/초과 대조, 현재 파일 보고서 검증과 결과 인계.

## 검증/진행 원본
구체 명령과 종료코드·계수는 task runtime 및 로컬 evidence에 기록한다. 준비 실패나 이전 결과를 현재 실행 성공으로 세지 않는다.

### 중간 확인
- 프런트 전체 125파일/1,506시험 통과. 서버 통합 후 선언 게이트를 다시 실행한다.
- 프런트·문서 독립 사전 검토 approve, intent 미달/초과 0. 서버 수용 검토도 approve로 완료했다. 통합 후 재검증 판정은 runtime을 따른다.
- 최종 진행/검증 원본: task `392d4e784eb74870920e42b187af1bb3`의 현재 run 및 `.git/colab-evidence/issue-78-79/RESULT.md`.
