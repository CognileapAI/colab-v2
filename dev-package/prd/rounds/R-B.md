# R-B · 등록 화면 묶음 — WU-B1 · B2 · B3 · B4 · B5 · B6 · B7 · B8 · B10 · B11 (10건)

> ⭑ **이 파일은 아직 한 덩어리다.** R-A 가 `main` 에 병합되는 시점에 **R-A 와 같은 방식으로 4개로 쪼갠다** —
> `R-B-1-db.md`(B1·B2·B4·B6 의 DB·마이그레이션) · `R-B-2-server.md`(B5·B7·B8 의 계약·서버) · `R-B-3-frontend.md`(B3·B10) · `R-B-4-verify.md`(B11 ＋ R-B 종료 검증).
> 지금 쪼개지 않는 이유 = **R-A 가 계약·스키마를 먼저 움직이므로 R-B 의 `현재 코드 path:line` 이 그때 달라진다.** 지금 옮겨 적으면 두 달 못 가 거짓이 된다.
> ⛔ **R-A 종료 전에 이 파일로 세션을 열지 않는다.** 진입조건 = R-A 병합 완료 ＋ WU-A11 판정표 회수.

---

## 0. 읽기 규칙 — 이 파일이 유일한 부트스트랩

> ⛔ **아래 4개를 통째로 열지 않는다.** 세션이 느려지는 원인이 이것이다.
> `dev-package/03-HANDOFF.md`(607 KB) · `dev-package/PLAN-SoT.md`(1.17 MB) · `dev-package/work-items.yaml`(513 KB) · `dev-package/WORK-UNITS.md`(138 KB)

- **허용된 접근은 아래 세 줄뿐이다.**
  1. 결정 번호 최대값 — `bash dev-package/prd/tools/max-decision.sh`
  2. 대장에서 항목 하나 — `grep -n -A14 '^  - id: WU-B1' dev-package/work-items.yaml`
  3. 게이트 이름 확인 — `sed -n '12,30p' gates/run.sh` (`ALL_GATES` 배열)
- `03-HANDOFF.md` · `CLAUDE.md` · `RESTART.md` 는 **머리 부분만** 읽는다. 본문 통독 금지.
- 요구사항 정본은 이 파일과 `dev-package/prd/PRD-260905-적용전기획.md` 다. **이 파일이 아직 요구 문면을 축자로 담지 않은 자리는** PRD 사본에서 **해당 `#### PRD-xx` 절만** 읽는다 — 절 하나가 20~40행이다. 파일 전체를 열지 않는다.
- **코드 파일은 고칠 때만 연다.** 현황 정찰·grep 스윕·다수 파일 읽기는 **서브에이전트에 위임**하고 결론만 회수한다.
- 못 읽으면 `[미상]` 이고 실패다. 지어내지 않는다.

---

## 1. 확정 결정 — 다시 열지 않는다 (PRD §1 · Ted 2026-09-05)

미결 18건이 전부 닫혔다 = 확정 16 ＋ 해소 1(미결-8) ＋ 개발 실측 1(미결-10). **기획자에게 받아야 하는 답은 0건이다.**
아래 16줄이 요구사항이다. 다르게 구현할 사유를 찾으면 **고치지 말고 보고한다.**

- 미결-1 ⓐ — 공개 범위는 **연구실 내부 3값**(`연구실 구성원 전체`/`나만 보기`/`지정한 사람만`), 기본 선택 `연구실 구성원 전체`. 저장은 `열림`·`잠김`·`지정 공개`. RLS 경계를 열지 않고 PRD-37·WU-C5 를 열지 않는다
- 미결-2 ⓐ — 가공 단계를 **사람이 고르고**, 계보 계산값과 어긋나면 **경고만** 낸다(등록을 막지 않는다). `processing_level_user_set` 재신설 ＋ 원장에 `〈194〉` 반전 기재
- 미결-3 ⓐ — 기존 13행의 3축은 **전 행 NULL**, 자동 매핑 없음. 표기 「분류를 아직 안 골랐어요」, `topic` 열은 남긴다
- 미결-4 ⓐ — 관측 간격은 **선택 입력**. 저장은 수치＋단위 두 칸, 표기는 기간 뒤 괄호 (R-A 에서 닫힘)
- 미결-5 ⓐ — 설명 필수화 후 빈 기존 행은 **그대로 두고** 그 행을 수정할 때 채우게 한다 (R-A 에서 닫힘)
- 미결-6 ⓐ — 확정 부모 1건 이상이면 체크박스 **잠금＋사유 한 줄**. 라벨 = `가공 전 데이터를 못 찾았어요 — 기록 없이 등록할게요`
- 미결-7 ⓐ — 가공 단계 **Lv0~Lv3 네 단**(CHECK 4값 · 계약 enum 4값 · 화면 칩 4단)
- 미결-9 ⓑ — 상세는 **한 페이지 스크롤 유지 ＋ sticky 구역 메뉴** (R-A WU-A8 이 최종형)
- 미결-11 ⓐ — 원천 표기(`sourceLabel`)는 **Lv 무관 상시 노출**, `sourceUrl`·`sourceDownloadedOn` 두 칸만 Lv0 게이팅
- 미결-12 ⓐ — 유형별 주의 문구는 **선택기 아래 보조 문구 ＋ 설명란 힌트**(저장 칸 없음 · 표시층 문자열)
- 미결-13 ⓐ — 분류·유형은 **표시만 국문＋영문 병기**(`기상·기후 인자 (Meteorological & Climatic Factors)`), 저장·CHECK·필터·색인은 국문 단일
- 미결-14 ⓐ — 유형↔가공 단계 **제약 없음**(조합 검증을 만들지 않는다)
- 미결-15 ⓐ — 종료 모달은 조건만 고치고 문면 유지 (R-A 에서 닫힘)
- 미결-16 ⓐ — 등록 ③ 의 쓰임 한 줄은 **받지 않는다**(PRD-36 · WU-C4 범위 밖)
- 미결-17 ⓐ — pdf 항목 8 잘린 1행은 원문 요청 중. **회신 대기가 WU-B2 착수를 막지 않는다** — 변수 표 본문은 `T-38`~`T-44` 로 확보돼 있다. ⛔ **추정 전사를 하지 않는다**
- 미결-18 ⓐ — 기간은 시각값 저장 유지, 화면만 구조화 (R-A 에서 닫힘)
- ⭑ **미결-10 = 개발 실측** — R-A 의 **WU-A11 판정표가 WU-B11 의 범위다**. 미결-8 = 해소(값 표가 pdf 원문).

---

## 2. 범위 — WU 10건

> 각 행의 요구 문면·`현재 코드 path:line`·수용 기준은 **PRD 사본의 해당 `#### PRD-xx` 절**에 있다. **그 절만** 읽는다.
> 순서·의존·게이트·레인은 `dev-package/prd/개발계획서-260905.md` §2 R-B · §3 표가 정본이다.

| 순 | WU | 담는 PRD | 계층 | 크기 | 의존 | 레인 | 쪼갤 때 갈 파일 |
|---|---|---|---|---|---|---|---|
| 1 | WU-B1 분류 3축 스키마·계약 | 01 · 02 · 03 | DB · 계약 · 서버 | L | — | `p3-axes-schema` | R-B-1-db |
| 2 | WU-B2 변수 행 표 | 16 | DB · 계약 · 서버 · FE | L | B1 | `p3-variable-rows` | R-B-1-db |
| 3 | WU-B3 등록 3단계 재구성 ＋ 값 안내 | 12 · 04 · 13 · 33 | FE | L | B1 · B2 | `p3-register-steps` | R-B-3-frontend |
| 4 | WU-B4 공개 범위 3값 | 11 | DB · 계약 · 서버 · FE | M | B3 | `p3-visibility-3` | R-B-1-db |
| 5 | WU-B5 Lv 연결 규칙 | 07 · 08 · 09 · 10 | 계약 · 서버 · FE | L | B1 · B3 | `p3-lv-rules` | R-B-2-server |
| 6 | WU-B6 Lv0 출처 칸 | 19 | DB · 계약 · 서버 · FE | M | B1 · B3 | `p3-lv0-source` | R-B-1-db |
| 7 | WU-B7 목록 필터·상세 3행 | 05 · 06 | 계약 · 서버 · FE | M | B1 | `p3-axis-filters` | R-B-2-server |
| 8 | WU-B8 계보 상태 판정식 ＋ 기록 없음 체크박스 | 27 | 계약 · 서버 · FE | M | B1 · B3 | `p3-lineage-unknown` | R-B-2-server |
| 9 | WU-B10 상세 계보 수정·추가 | 31 | FE | M | B5 · B8 | `p3-lineage-fix` | R-B-3-frontend |
| 10 | WU-B11 디자인 검수 수정 | 29 후속 | FE | `[미상]` | **WU-A11** | `p3-design-fix` | R-B-4-verify |

**WU 별로 놓치면 안 되는 것 — PRD 절을 읽기 전에 알아야 하는 조항**

- **WU-B1** — 5값·6값·4값 CHECK · 계약에 세 열쇠 · 값 집합 밖은 **400(500 아님)** · 이관된 3쌍이 실측으로 맞고 `지형·DEM` 만 NULL. `processing_level_user_set` 재신설은 마이그레이션 `0011` 의 반전이다 — ⛔ **삭제 근거 주석과 `〈194〉`·`〈276〉` 문면을 남긴 채 반전을 덧붙인다**(지우지 않는다).
- **WU-B2** — 새 표 `d3_dataset_variable` ＋ RLS ＋ 대표 1개 부분 UNIQUE. 배열 이관이 **순서대로** 되고 **첫 행이 대표**. **변수명 검색이 종전과 같이 잡힌다** · **cross-tenant 음성 0건**. ⛔ `d3_dataset_variable` 은 **D3 소유**다 — D2 가 D3 표를 FK 하지 않는다.
- **WU-B3** — 표시기 = `① 분류 · ② 메타데이터 입력 · ③ 연결` 이고 **눌러서 어느 단계로나 간다**(rev1 `UI-003`) · ① 세 값에 기본 선택값 · **15개 값 전부에 정의·예시 한 줄** · 유형 6값에 `참고 · 특이사항` · 설명란에 분류·단계별 힌트 · **모달 재오픈이 늘 ①**. ⛔ **WU-A3 이 세운 골격을 재작성하지 않고 필드만 늘린다.**
- **WU-B4** — M-4 는 **두 표**다(`d2_dataset_access.state`(`schema.sql:174`) ＋ `d1_lab_profile.default_visibility`(`schema.sql:104-105`)). 한쪽만 넓히면 연구실 기본값이 표현 못 하는 상태가 생긴다. **상태 전이표가 선다** — `잠김` ∧ 유효 grant ≥1 인 행이 **어느 시점에도 0건**. **cross-tenant 음성 여전히 0건**(RLS 경계 무개방 증명).
- **WU-B5** — `부모 Lv ≤ 자기 Lv` 가 **서버 400** 으로 선다 · 찾기 모달이 초과 후보를 **보이되 못 고르게** 하며 **사유 대비 4.5:1 이상** · 사후 충돌이 연결을 **지우지 않고** `확인 필요` 로 막는다 · 불일치는 **경고만**.
- **WU-B6** — M-8. **두 칸은 선택 입력이다** — Lv0 에서 비운 채 등록해도 **성공**하고 Lv1 이상에서 값을 실어도 **거절하지 않고 저장한다**. ⛔ **종전 완료 판정(「Lv0 이면 두 칸 필수·400」·「Lv1 이상 값 전송 시 400」)은 폐기됐다.** 근거 = pdf 항목 3 축자. **Lv 로 갈리는 것은 두 칸의 화면 표시뿐**이고 `sourceLabel` 은 **늘 보인다**.
- **WU-B7** — 3축 필터가 **AND** 로 걸리고 각 축에 **`미지정` 항목**이 있어 재선택 대상을 찾을 수 있다 · 상세에 3행이 이 순서로 뜬다. ⭑ **여기 뒤에 `M-10` 이 붙는다** — R-A 에서 이월된 PRD-21 「`nc` 로도 찾는다」가 **이 자리에서 닫힌다**.
- **WU-B8** — **스키마 변경 0**. 저장 플래그는 이미 있는 `d4_lineage_unknown` 행이고 바뀌는 것은 **그 행을 언제 쓰는가**다. `d3_catalog.py` `lineage_state()` 를 PRD-27 의 **6항 판정식**으로 대체하고 `ingestion.py` 의 자동 `mark_unknown` 을 걷는다. 부모 0건·체크 안 함·Lv1↑ = `확인 필요` · 체크함 = `기록 없음` · **Lv0 = `원천`**(제외 조항). 부모 1건이면 체크박스 비활성 ＋ 사유 한 줄. **라벨 축자 = `가공 전 데이터를 못 찾았어요 — 기록 없이 등록할게요`** 이고 **종전 문면 문자열이 코드에 없다**. **부모 ≥1 경로의 판정이 종전과 같음을 회귀로 증명한다.** **홈 대시보드 모수 불일치도 같이 닫는다**(`SummaryTiles.tsx:52` ↔ `dashboardSource.ts:20`).
- **WU-B10** — 상세에 `계보 수정 · 추가` 진입 · `기록 없음` 상세가 `계보 채우기` 빈 상태를 그린다 · 모달이 **등록 ③ 과 같은 규칙**(PRD-07·08·09)을 재사용했음을 **diff 로 보인다**. **`가공 방식` 칸은 새 칸이 아니라 이미 있는 `d4_lineage_edge.method` 를 여는 것**(부록 A `H-50`) — 이 WU 는 **FE 진입만** 만든다. 파생 행은 **읽기 전용**.
- **WU-B11** — **WU-A11 이 `있음` 으로 판정한 항목만** 고친다. 대비 3건(**Lv 칩 · 연결 불가 후보 · 그래프 라벨**)이 접근성 합격선이라 **먼저 돈다**. ⛔ **판정 없이 고치지 않는다.**

### 마이그레이션 — 한 head

`M-1`·`M-2`·`M-3`(WU-B1) · `M-5`(WU-B2) · `M-4`(WU-B4 — **두 표**) · `M-8`(WU-B6) · **`M-10` 전부**.
**WU-B1 에서 한 head 로 묶고, WU-B2·B4·B6 은 그 head 를 잇는다.**
⭑ **`M-10` 은 라운드에서 한 번만 돈다.** 색인 재정의(`topic`→`category` · `format`＋`file_extension`) ＋ **`d3_dataset_variable` → `autometa.variables` 미러 트리거 신설**을 **한 마이그레이션에** 담아 생성 컬럼 재계산·GIN 재생성을 **1회로** 끝낸다. 자리는 **WU-B2 뒤** — 변수 행 표가 서야 트리거를 걸 대상이 있다.
⚠ 「생성 컬럼이라 자동 재계산」은 **변수명 경로에는 거짓**이다 — 생성 컬럼은 같은 행의 열만 참조하므로 새 표를 색인식에 넣을 수 없다.
⭑ 종전 목록의 `M-11`(`d4_lineage_edge.transform_note`)은 **컬럼이 이미 있어**(`db/platform/schema.sql:555` `method`) 부록 B 에서 지웠다.
⛔ **`topic`·`variables`·`format` 컬럼을 지우지 않는다** — 되돌림 경로이자 이관 대조 근거다. 삭제는 다음 릴리즈의 별건이다.

### 열지 않는 WU

- **WU-B9**(연결별 `가공 방식`) — 지웠다. `d4_lineage_edge.method` 와 계약 열쇠·등록 화면·상세 표시가 **전부 이미 있다**(부록 A `H-50`). 레인 `p3-transform-note` 도 지웠다.
- **WU-C3**(상세 화면 구조 개편) · **WU-C4**(등록 ③ 쓰임 한 줄) · **WU-C5**(연구실 밖 공개) — 미결-9 ⓑ · 16 ⓐ · 1 ⓐ 확정으로 **착수 조건 자체가 사라졌다.**
- **WU-C2**(대표 그림 저장 경로) — 별건 1건. 이미지 보관 도메인 결정에 걸려 있어 **라운드를 만들지 않는다.**

---

## 3. 지켜야 하는 규약 — 명령으로

### ㉮ 워크트리 레인

- WU 하나에 레인 하나. 레인 이름은 §2 표의 마지막 열. 각 레인은 `origin/main` 에서 딴 자기 워크트리에서 돈다.
- 병합은 **ff-merge**, 병합 뒤 워크트리·로컬/원격 브랜치를 정리한다. 한 레인 = 한 WU.

### ㉯ 착수 전 — `work-items.yaml` 등재가 먼저다

⛔ **원장 행 없이 마이그레이션을 만들지 않는다.** 순서 = 대장 등재 → 마이그레이션 원장 등재 → 스키마.
`dev-package/work-items.yaml` 의 `items:` 리스트 **끝에** WU 마다 아래 모양으로 덧붙인다(들여쓰기 2칸). `<…>` 는 §2 표에서 옮긴다.

```yaml
  - id: WU-B1
    name: 분류 3축 스키마·계약 (PRD-01·02·03)
    status: in_progress
    stage: stage2
    owner: "T-R"
    entry_conditions: ["R-A 병합 완료", "계약 동결 해제 20차 승인 (등급 ㉯)"]
    depends_on: []
    completion_def: "M-1·M-2·M-3 적용. 5값·6값·4값 CHECK 가 서고, 계약에 세 열쇠가 있고, 값 집합 밖은 400(500 아님). 이관된 3쌍이 실측으로 맞고 지형·DEM 만 NULL"
    evidence: "dev-package/sessions/p3-axes-schema-<YYYYMMDD>.md"
    deadline: null
    note: "processing_level_user_set 재신설 = 마이그레이션 0011 의 반전. 삭제 근거 주석과 〈194〉·〈276〉 문면을 남긴 채 반전을 덧붙인다. R-B head 를 여기서 만든다"
    sources: ["dev-package/prd/rounds/R-B.md", "PRD-01", "PRD-02", "PRD-03", "부록 B M-1·M-2·M-3"]

  # WU-B2 · B3 · B4 · B5 · B6 · B7 · B8 · B10 · B11 도 같은 8필드 모양으로 이어 붙인다.
  # entry_conditions 에 계약 20차 승인이 필요한 WU = B1 · B2 · B4 · B6 · B8.
  # WU-B11 의 entry_conditions 에는 "WU-A11 판정표 회수" 를 반드시 적는다.
```

### ㉰ 계약 동결 해제 — **20차 · 등급 ㉯ · Ted 승인 필수**

근거 문서 = `dev-package/sessions/X2-FREEZE-PROTOCOL.md` §5. **R-A 가 19차를 썼으므로 이 라운드는 20차**다. 회차 번호는 `PLAN-SoT §9` 가 **유일한 발급처**이고, 새 항목은 **직전 해제 항목을 인용해 번호를 잇는다.**
**여는 값** — `WU-B1` 세 축 열쇠 ＋ required 확장 · `WU-B2` `variables` 를 **문자열 배열에서 객체 배열로** · `WU-B4` `accessState` · `WU-B6` 출처 2칸 · `WU-B8` `lineageUnknown`.
㉯ 인 사유 = **파괴적 변경** ＋ **마이그레이션 ≥1**(M-1~M-5·M-8·M-10) ＋ **설계 판단 다수**(기각한 대안이 있다 — PRD-16 ⓑ · PRD-27 판정식).

```bash
# ⑴ 파괴 판정을 실행 출력으로 낸다 (주장하지 않는다 — §5-㉱-1)
./gates/run.sh contract-breaking
# ⑵ 소비자 수를 grep 출력으로 낸다 (§5-㉱-3)
grep -rn 'variables\|accessState\|lineageUnknown\|category' contracts/ services/ frontend/src | wc -l
# ⑶ 마이그레이션 건수 = 7 (M-1·M-2·M-3·M-5·M-4·M-8·M-10, head 1개)  ⑷ 되돌림 경로
```
⛔ **승인 없이 `contracts/` 를 고치지 않는다.**
⛔ **§5-㉰-4(집행 없는 신설) 금지** — 계약만 열고 서버 수용 목록(`_ALLOWED_CREATE_FIELDS`)을 다음 회차로 미루지 않는다. **계약 · 서버 · 화면 · 시험을 한 회차에 세운다.** `〈280〉` 이 그 반례를 닫은 자리다.
⛔ **§5-㉰-6(묶음 쪼개기) 금지** — 한 라운드의 계약 변경을 WU 별로 나눠 각 조각을 ㉮ 로 통과시키지 않는다. **목적 단위로 판정한다.**

### ㉱ 결정 번호 〈N〉 — 예약하지 않는다

```bash
git fetch origin main && bash dev-package/prd/tools/max-decision.sh   # 병합 직전에 다시 잰다
```
2026-09-05 실측 최대 = **〈326〉**(참고값). **R-A 가 번호를 여럿 쓰므로 R-B 착수 시점에 반드시 다시 잰다.**
`PLAN-SoT §9` 표에 아래 행을 **병합 직전에** 덧붙인다 — 필드 8개(X2 §5-㉲). **`〈194〉` 반전은 별도 한 행으로 적는다.**

```
| 〈N〉 | **R-B 등록 화면 묶음 — 계약 동결 해제 20차 · 분류 3축 ＋ 변수 행 표 ＋ 공개 범위 3값 ＋ 출처 2칸 ＋ lineageUnknown** | **집행 (2026-MM-DD · 워크트리 `<레인>` · 병합 `<sha>`).** ①회차 = **20차**(직전 19차 = R-A) ②값 = `category`·`dataType`·`processingLevelUserSet` ＋ required 확장 · `variables` 객체 배열 · `accessState` · `sourceUrl`·`sourceDownloadedOn` · `lineageUnknown` ③근거 = PRD-01·02·03·11·16·19·27 (`dev-package/prd/PRD-260905-적용전기획.md`) ④가·파 판정 = **파괴** · `contract-breaking` 출력 = `<축자>` ⑤소비자 = `<n>` 건 · 측정법 = `<grep 명령 축자>` ⑥마이그레이션 = **7건 · head 1개**(M-1·M-2·M-3·M-5·M-4·M-8·M-10) ⑦승인 = Ted · `<일자>` ⑧이번에 세지 않은 축 = `<…>` `[미측정]` |
| 〈N+1〉 | **`〈194〉`·`〈276〉` 반전 — 「레벨은 언제나 계보에서 나온다 — 예외 없음」을 되돌린다. 사람이 Lv 를 고르고 불일치는 경고만 낸다** | **반전 (미결-2 ⓐ · Ted 2026-09-05).** ⛔ **원래 근거 문단과 마이그레이션 `0011` 의 삭제 근거 주석을 지우지 않는다** — 남긴 채 이 반전 항목을 덧붙인다. 근거 = 부모를 나중에 붙이는 흐름에서 불일치는 정상 중간 상태이고, 차단하면 등록을 못 끝내며 덮어쓰면 고른 값이 사라져 선택 칸이 무의미해진다 |
```
⛔ **HANDOFF 에는 값을 적지 않는다**(`CLAUDE.md §6-3`).

### ㉲ 게이트 — 작업 중엔 단독, 병합 전엔 전건

```bash
# 작업 중 (해당 WU 것만 하나씩)
./gates/run.sh schema-diff                 # B1 · B2 · B4 · B6
./gates/run.sh migration-single-head       # B1 · B2 · B4 · B6
./gates/run.sh contract-lint               # B1 · B2 · B5 · B6 · B7 · B8
./gates/run.sh contract-breaking           # 같음
./gates/run.sh generated-up-to-date        # B1
./gates/run.sh service-tests-core-api      # B1 · B2 · B4 · B5 · B6 · B7 · B8
./gates/run.sh rls-coverage                # B2
./gates/run.sh rls-effect                  # B2 · B4
./gates/run.sh autometa-loss               # B2
./gates/run.sh ai-no-lineage-write         # B5
./gates/run.sh frontend-typecheck          # FE 가 닿는 전부
./gates/run.sh frontend-test               # 같음
./gates/run.sh frontend-fixture-reach      # B3 · B10
# 라운드 끝에 한 번
./gates/run.sh all -j 1
```
⛔ **게이트를 끄거나 검사 대상을 줄이지 않는다.** **green 으로 시작한 테스트는 오라클이 아니다** — 실패 테스트 red 를 먼저 확인한다.

### ㉳ 커밋 문면

```
R-B <WU-키> — <한 줄 요지>

- <마이그레이션 / 계약 / 서버 / 화면 중 이 커밋이 움직인 것>
- 계약 동결 해제 20차 · 등급 ㉯ · Ted 승인 <일자> · PLAN-SoT §9 〈N〉 등재
- RED 선실측 → GREEN: <시험 파일>:<건수>
```

### ㉴ 금지

- ⛔ `main` 에 직접 push. ⛔ staging DB 직접 쓰기(변경은 마이그레이션 경유로만). ⛔ 원장 행 없이 마이그레이션.
- ⛔ `topic`·`variables`·`format` 컬럼 삭제. ⛔ `M-10` 을 두 번 돌리기(라운드에서 **한 번**).
- ⛔ `〈194〉`·`〈276〉` 원문 문단과 `0011` 삭제 근거 주석 지우기 — **반전은 덧붙이는 것**이다.
- ⛔ WU-B6 에서 두 칸을 필수로 잠그거나 Lv1 이상 값을 400 으로 거절하기(**폐기된 판정**).
- ⛔ WU-B4 에서 RLS 경계 넓히기(연구실 밖 공개는 PRD-37 · **범위 밖**). ⛔ 유형↔가공 단계 조합 검증 신설.
- ⛔ WU-B3 에서 WU-A3 골격 재작성. ⛔ WU-B11 을 WU-A11 판정표 없이 착수.
- ⛔ 미결-17 의 잘린 1행을 **추정 전사**하기. ⛔ `40 COLAB-기획/` 문서 수정. ⛔ 문서·주석에 절대경로.
- ⛔ 이 세션이 `03-HANDOFF.md` 를 직접 고치기 — §4 의 5줄만 넘긴다.

---

## 4. 산출물과 근거

| 무엇 | 어디 |
|---|---|
| 마이그레이션 | `db/` — **head 1개**(WU-B1 이 만들고 B2·B4·B6 이 잇는다) ＋ `M-10` 1회 ＋ 마이그레이션 원장 행 |
| 계약 | `contracts/seams/fe-core.yaml` — 세 축 열쇠 · `variables` 객체 배열 · `accessState` · 출처 2칸 · `lineageUnknown` |
| 서버 | `services/core-api/.../routes/catalog.py`·`ingestion.py` · `services/pipeline-worker/.../d3_catalog.py`(`lineage_state()`) |
| 프론트 | 등록 3단계 재구성 · 변수 표 · 3축 필터·상세 3행 · 계보 수정 모달 · 디자인 수정 |
| 세션 노트 | `dev-package/sessions/<레인>-<YYYYMMDD>.md` — WU 마다 하나 · **각 ≤ 60행** |
| 대장 | `dev-package/work-items.yaml` — WU 10건. 완료 시 `status: done` ＋ `evidence` 갱신 |
| 원장 | `PLAN-SoT §9` — 20차 해제 1행 ＋ `〈194〉` 반전 1행 · **병합 직전** |

**오케스트레이터에 넘기는 HANDOFF 갱신문 — 5줄 이하. 세션이 `03-HANDOFF.md` 를 직접 고치지 않는다.**

```
R-B <WU-키> 완료 — 레인 <레인>, 병합 <sha>
<한 줄 요지 · 무엇이 서고 무엇이 안 열렸는가>
계약 동결 해제 20차 승인 <일자> · PLAN-SoT §9 〈N〉(＋〈194〉 반전 〈N+1〉) 등재 · 마이그레이션 <n>건 head 1개
게이트: <단독 게이트 목록> green / 라운드 끝 ./gates/run.sh all -j 1 = green <n>/<n>
근거: dev-package/sessions/<레인>-<YYYYMMDD>.md
```

---

## 5. 완료 판정

- **WU 별 수용 기준** — §2 표의 「놓치면 안 되는 것」 ＋ PRD 사본의 해당 `#### PRD-xx` 절 `수용 기준` 이 완료 판정이다. **둘 다 충족해야 닫는다.**
- **R-A 에서 이월된 1건** — PRD-21 「`nc` 로도 찾는다」가 **WU-B7 뒤 `M-10` 으로 닫힌다.** 라운드 종료 보고에 **닫혔음을 명시**한다.
- **경계 증명 2건** — `cross-tenant 음성 0건`(WU-B2 · WU-B4) · `state='잠김' ∧ 유효 grant ≥1 인 행이 어느 시점에도 0건`(WU-B4).
- **회귀 증명 3건** — 변수명 검색이 종전과 같이 잡힌다(B2) · 매핑 후 기존 허용자의 접근이 종전과 같다(B4 · 양성·음성 둘 다) · 부모 ≥1 경로의 계보 판정이 종전과 같다(B8).
- **게이트** — 위 ㉲ 목록의 해당 게이트가 WU 마다 green, 그리고 라운드 끝에 `./gates/run.sh all -j 1` green ＋ **staging 배포 green**(`CLAUDE.md §0`).
- **절차** — 20차 승인이 `contracts/` 첫 수정보다 먼저 있었음이 커밋 순서로 보인다 · `〈N〉` 이 병합 직전 실측값이다 · `〈194〉` 반전이 **원문을 지우지 않고** 덧붙여져 있다.

---

## 6. 이 파일을 쪼개는 시점과 방법

**언제** — R-A 4개 파일이 전부 `main` 에 병합되고 WU-A11 판정표를 회수한 직후. **그 전에 쪼개지 않는다**(R-A 가 `path:line` 을 바꾼다).

**어떻게** — `R-A-1-db.md` 를 형판으로, WU 를 **계층 기준**으로 넷에 나눈다(계층이 겹치면 **가장 이른 계층**에 넣고 나머지 파일에서 상호 참조한다).

| 새 파일 | WU | 비고 |
|---|---|---|
| `R-B-1-db.md` | B1 · B2 · B4 · B6 | head 를 B1 이 만든다. `M-10` 은 B2 뒤 |
| `R-B-2-server.md` | B5 · B7 · B8 | B7 뒤에서 R-A 이월분(`nc` 검색)이 닫힌다 |
| `R-B-3-frontend.md` | B3 · B10 | WU-A3 골격 위에 필드만 늘린다 |
| `R-B-4-verify.md` | B11 ＋ R-B 종료 검증 | 진입조건 = WU-A11 판정표 |

각 파일은 **§0 읽기 규칙 → §1 확정 결정 16줄 → §2 범위(요구 문면 축자 ＋ `path:line`) → §3 규약(명령) → §4 산출물·HANDOFF 5줄 → §5 완료 판정** 순서를 그대로 지키고 **≤ 300행**을 넘기지 않는다.
쪼갠 뒤 `dev-package/prd/README.md` 의 상태표에 4행을 더하고 이 파일은 **지우지 말고** 「쪼갬 — 위 4개로 대체」 한 줄을 머리에 달아 남긴다.
