# R-B-3 · 프론트 계층 — WU-B3 · WU-B10

> 이 파일 하나로 세션을 시작한다. 라운드 = **R-B** · 계층 = **FE 단독** · WU **2건**.
> 이 파일은 `dev-package/prd/rounds/R-B.md` §6 의 분할표대로 잘라 낸 것이다.
> 라운드 의도 = `dev-package/intent/2026-09-07-r-b.md` (**미승인 초안** — 판정 입력으로 쓰지 않는다).
> ⛔ **이 파일의 두 WU 는 마이그레이션도 계약 개정도 하지 않는다.**

---

## 0. 읽기 규칙 — 이 파일이 유일한 부트스트랩

> ⛔ **아래 4개를 통째로 열지 않는다.** 세션이 느려지는 원인이 이것이다.
> `dev-package/03-HANDOFF.md` · `dev-package/PLAN-SoT.md`(1.17 MB) · `dev-package/work-items.yaml`(513 KB) · `dev-package/WORK-UNITS.md`(138 KB)

- **허용된 접근은 아래 세 줄뿐이다.**
  1. 결정 번호 최대값 — `bash dev-package/prd/tools/max-decision.sh`
  2. 대장에서 항목 하나 — `grep -n -A14 '^  - id: WU-B3' dev-package/work-items.yaml`
  3. 게이트 이름 확인 — `sed -n '150,175p' gates/run.sh` (`ALL_GATES` 배열)
- `03-HANDOFF.md` · `CLAUDE.md` · `RESTART.md` 는 **머리 부분만** 읽는다. 본문 통독 금지.
- 요구사항 정본은 이 파일과 `dev-package/prd/PRD-260905-적용전기획.md` 다. 이 파일에 옮겨 적힌 문면이 우선이고, 더 필요하면 PRD 사본에서 **해당 `#### PRD-xx` 절만** 읽는다.
- **코드 파일은 고칠 때만 연다.** 현황 정찰·grep 스윕·다수 파일 읽기는 **서브에이전트에 위임**하고 결론만 회수한다.
- 이 파일의 `path:line` 은 **integration/r-b HEAD 에서 실측한 값**이다. PRD 사본에 적힌 옛 행 번호와 갈리면 **이 파일이 이긴다.**
- 못 읽으면 `[미상]` 이고 실패다. 지어내지 않는다.

---

## 1. 확정 결정 — 다시 열지 않는다 (PRD §1 · Ted 2026-09-05)

미결 18건이 전부 닫혔다 = 확정 16 ＋ 해소 1(미결-8) ＋ 개발 실측 1(미결-10). **기획자에게 받아야 하는 답은 0건이다.**
아래 16줄이 요구사항이다. 다르게 구현할 사유를 찾으면 **고치지 말고 보고한다.**

- 미결-1 ⓐ — 공개 범위는 **연구실 내부 3값**(`연구실 구성원 전체`/`나만 보기`/`지정한 사람만`), 기본 선택 `연구실 구성원 전체`. 저장은 `열림`·`잠김`·`지정 공개`. RLS 경계를 열지 않고 PRD-37·WU-C5 를 열지 않는다
- 미결-2 ⓐ — 가공 단계를 **사람이 고르고**, 계보 계산값과 어긋나면 **경고만** 낸다(등록을 막지 않는다). `processing_level_user_set` 재신설 ＋ 원장에 `〈194〉` 반전 기재
- 미결-3 ⓐ — 기존 13행의 3축은 **전 행 NULL**, 자동 매핑 없음. 표기 「분류를 아직 안 골랐어요」, `topic` 열은 남긴다
- 미결-4 ⓐ — 관측 간격은 **선택 입력**. 저장은 수치＋단위 두 칸, 표기는 기간 뒤 괄호 (R-A 에서 닫힘)
- 미결-5 ⓐ — 설명 필수화 후 빈 기존 행은 **그대로 두고** 그 행을 수정할 때 채우게 한다 (R-A 에서 닫힘)
- 미결-6 ⓐ — 확정 부모 1건 이상이면 체크박스 **잠금＋사유 한 줄**. 라벨 = `가공 전 데이터를 못 찾았어요 — 기록 없이 등록할게요`
- 미결-7 ⓐ — 가공 단계 **Lv0~Lv3 네 단**(CHECK 4값 · 계약 enum 4값 · 화면 칩 4단)
- 미결-9 ⓑ — 상세는 **한 페이지 스크롤 유지 ＋ sticky 구역 메뉴** (R-A WU-A8 이 최종형)
- 미결-11 ⓐ — 원천 표기(`sourceLabel`)는 **Lv 무관 상시 노출**, `sourceUrl`·`sourceDownloadedOn` 두 칸만 Lv0 게이팅
- 미결-12 ⓐ — 유형별 주의 문구는 **선택기 아래 보조 문구 ＋ 설명란 힌트**(저장 칸 없음 · 표시층 문자열)
- 미결-13 ⓐ — 분류·유형은 **표시만 국문＋영문 병기**(`기상·기후 인자 (Meteorological & Climatic Factors)`), 저장·CHECK·필터·색인은 국문 단일
- 미결-14 ⓐ — 유형↔가공 단계 **제약 없음**(조합 검증을 만들지 않는다)
- 미결-15 ⓐ — 종료 모달은 조건만 고치고 문면 유지 (R-A 에서 닫힘)
- 미결-16 ⓐ — 등록 ③ 의 쓰임 한 줄은 **받지 않는다**(PRD-36 · WU-C4 범위 밖)
- 미결-17 ⓐ — pdf 항목 8 잘린 1행은 원문 요청 중. **회신 대기가 착수를 막지 않는다.** ⛔ **추정 전사를 하지 않는다**
- 미결-18 ⓐ — 기간은 시각값 저장 유지, 화면만 구조화 (R-A 에서 닫힘)
- ⭑ **미결-10 = 개발 실측** — WU-A11 판정표는 `R-B-4-verify.md` 의 WU-B11 몫이다. 미결-8 = 해소(값 표가 pdf 원문).

---

## 2. 범위 — 이 파일의 WU 2건

### WU-B3 · 등록 3단계 재구성 ＋ 값 안내 (PRD-12 · 04 · 13 · 33 · 40) — 계층 FE · 크기 L · 레인 `p3-register-steps`

- **의존**: WU-B1(세 축 계약 열쇠) · WU-B2(변수 행 표 계약). ⚠ **이 WU 는 계약 동결 해제 20차를 쓰지 않는다** — 계약을 여는 것은 B1·B2·B4·B6·B8 이다. **진입조건 = B1·B2 의 계약 열쇠가 이미 서 있을 것**이고, 서지 않았으면 구현하지 말고 보고한다.
- **현재 코드 (실측)**
  - 단계 라벨 — `frontend/src/components/upload/RegisterArea.tsx:55-57` = `1: '① 자동 메타데이터 확인', 2: '② 소속 프로젝트 지정', 3: '③ 계보 확정',`. 단계 수는 같고 내용물이 전부 다르다.
  - 파일 크기 — `RegisterArea.tsx` **868행** · `UploadModal.tsx` **1007행**(PRD 사본의 「556행」은 낡았다).
  - step 상태 — `frontend/src/components/upload/UploadModal.tsx:117` `const [step, setStep] = useState<Step>(1);`(PRD 사본의 `:72` 는 낡았다).
  - Esc 계층 — `ESC_LAYER_ATTR = 'data-esc-layer'` 정의 `UploadModal.tsx:53` · 사용 `:528` = `if (document.querySelector(...)) return;`(위 계층이 있으면 Esc 를 삼키지 않는다). 주석 `:50`·`:518` 이 우선순위 **확장보기 → 찾기 → 계보 수정 → 닫기 확인 → 업로드** 를 못 박는다.
  - 닫기 경로 — `requestClose` 정의 `UploadModal.tsx:509`.
  - 확장보기 — **전용 컴포넌트가 없다**(grep 0건 · 문자열 언급만 `frontend/src/components/common/toastCopy.ts:26`). 이 WU 가 만든다.
  - 공개 범위 표시 — `frontend/src/components/detail/DetailHeader.tsx:93` = `{d.accessState === '잠김' ? <span className="chip chip--warning">잠김</span> : null}` (두 갈래 칩). 값 칸 미존치 근거 주석 `frontend/src/components/detail/UsageSection.tsx:33` · 편집 필드 자리 주석 `frontend/src/components/detail/editFields.ts:5` · 왕복 재동기 주석 `frontend/src/components/detail/useDatasetEdit.ts:92`.
- **요구 문면 축자 (PRD-12)** — 「분류 체계와 자유 입력은 성격이 다르고, 가공 단계가 뒤 논리를 좌우해 가장 먼저 정해야 한다.」 단계 = `① 분류 · ② 메타데이터 입력 · ③ 연결`.
  - ① 분류 — 분류(필수)·유형(필수)·가공 단계(필수), 각 셀렉트 아래 정의·예시 한 줄. 카드 부제 = `목록 필터가 이 세 축을 그대로 받아요`.
  - ② 메타데이터 입력 — 이름·설명·기간·좌표계·격자·확장자(자동)·용량(자동)·변수 표·부가 정보(관측 간격·공개 범위)·대표 그림. 카드 부제 = `파일에서 읽는 값은 확장자·용량뿐이에요`.
  - ③ 연결 — 계보 부모 연결 ＋ 원천 표기(＋Lv0 전용 2칸) ＋ 연관 프로젝트·논문 등록.
  - 이동 규칙 축자(rev1 `UI-003`) — 「등록 단계 표시기 · 언제나 · 눌러서 아무 단계로나 이동」. rev1 JS 주석 축자 — 「한 번에 한 단계만 보인다. 막지는 않는다」. **`다음` 은 순차 이동, 표시기는 임의 이동**이고 빈 값은 마지막 게이트(`데이터셋 만들기`)가 잡는다.
- **요구 문면 축자 (PRD-12 2026-09-06 개정 · 모달 2장면)** — 장면1 = 파일 없음, 드롭존만 크게 보이고 등록 폼·미리보기가 화면에 없다. 장면2 = 파일 있음, 좌측 미리보기 ＋ 우측 등록 3단계. 되돌아가는 길은 파일 배지의 `×` 하나이고 그 순간 입력값이 사라진다는 고지를 띄운다(PRD-39 ③ · PRD-43 문면). ⛔ 2단 등록 게이트·기준 격자 파일 흐름·이어올리기 배너는 **걷지 않는다**(§0.3-5 존치 규칙).
- **요구 문면 축자 (PRD-13)** — 「단계가 둘일 때는 안 드러났고 셋이 되며 나왔다.」 모달 open 전이마다 step 을 1 로 되돌린다. 언마운트 여부에 의존하지 않는다.
- **요구 문면 축자 (PRD-04)** — 세 셀렉트 각각 바로 아래 한 줄. 문면 형식 = `<b>{정의}</b> · 예: {예시}`, 가공 단계는 뒤에 줄을 바꿔 `{부가 안내}` 를 덧붙인다. 값 사전은 아래 세 표를 **FE 상수로 그대로** 옮긴다(rev1 `AXIS_DEF` 와 축자 일치).

**분류(Data Category) 5값 표 (PRD-01 원문 축자)**

| 카테고리(국문) | 영문 병기 | 설명 | 대표 인자 예시 | 활용 예시 | 메타데이터 항목 |
|---|---|---|---|---|---|
| 수문 인자 | Hydrological Factors | 유역 내 수문 순환과 직접적으로 관련 인자 | 유량, 수위, 적설량 | 수문모형 입력, 홍수/가뭄 분석 | 유역코드, 관측 해상도 |
| 기상·기후 인자 | Meteorological & Climatic Factors | 기상 조건 및 기후변동성과 관련 자료 | 강수량, 기온 | 강우 이벤트 분석, 기후 트렌드 평가 | 공간 해상도, 시간 간격 |
| 식생·탄소 인자 | Vegetation & Carbon Factors | 생태계 반응, 생물활동, 탄소 순환과 관련 요소 | NDVI, GPP | 생태계, 기상 상호작용 연구 | 위성플랫폼, 연산 방식 |
| 사회·경제 인자 | Socio-Economic Factors | 인간 활동 및 사회적 구조와 관련 지표 | 인구밀도, 토지이용 | 재해취약성 평가, 도시유역 재해 영향 분석 | 통계 출처, 행정구역 단위 |
| 환경 인자 | Environmental Factors | 물, 공기, 토양 등의 환경 상태와 직접 관련 요소 | 대기오염농도, 토양오염지수 | 환경건강 위험도 분석, 수질과 강우의 연관성 평가 | 측정 기준, 샘플링 위치 |

- 기본 선택값 = `기상·기후 인자`(rev1 `<option selected>` · rev1 Policy `VAL-004` 축자 「기본값 기상·기후 인자」).

**유형(Data Type) 6값 표 (PRD-02 원문 축자)**

| 데이터 유형(국문) | 영문 병기 | 정의 / 설명 | 대표 예시 | 특이사항 및 주의점 |
|---|---|---|---|---|
| 지상관측자료 | In-situ Observation | 지표면 또는 근처에서 센서를 통해 직접 측정하거나 원격 감지한 데이터 | AWS 기온/강수, 수위/유량/적설 | 레이더도 지상 설치된 경우 포함, 포인트 또는 범위관측 |
| 위성자료 | Satellite-based Data | 인공위성에 탑재된 센서를 통해 원격탐사 방식으로 수집된 자료 | MODIS NDVI, LST, GPM, Himawari | 해상도, 궤도 정보 명시 필요 |
| 재분석자료 | Reanalysis Data | 관측자료 + 수치모형을 동화한 격자 기반 통계-물리 결합 자료 | ERA5, NCEP/NCAR, KMA LDAPS, KLAPS | 시계열 일관성 우수, 시간 지연(latency) 존재 |
| 수치모형자료 | Numerical Model Output | 수치모델을 통해 특정 현상을 예측하거나 재현한 시뮬레이션 결과 | WRF, SWAT, DSSAT | 초기조건 및 설정 파라미터 기재 필요 |
| 합성자료 | Fused / Blended Data | 복수 자료를 융합하여 도출한 결과로, 관측 기반 또는 시뮬레이션 포함 가능 | 레이더+위성 기반 강수 추정, NWP+위성 융합 예측 | 생성 방식, 융합 기법의 상세 설명 필요 |
| 관측 기반 산출물 | Derived Observation Products | 관측자료를 활용하여 계산된 파생 인자 | SPI, ET₀, NDVI 기반 생물계절지표 | Level 2와 밀접하게 연계됨 |

- 기본 선택값 = `재분석자료`(rev1 Policy `VAL-004b` 축자 「기본값 재분석자료」). 「관측 기반 산출물 ↔ Level 2」 상호 제약은 걸지 않는다(미결-14 ⓐ).

**가공단계(Lv) 4값 표 (PRD-03 원문 축자)**

| 저장값 | 화면 라벨 | 설명 | 데이터 예시 | 활용 예시 | 메타데이터 필수 항목 |
|---|---|---|---|---|---|
| Lv0 | Lv0 · 원시 데이터 | 원시 데이터 | ERA5 원자료, 기상청 레이더 NetCDF | 연구의 추적성 확보, 원자료 백업 | 출처(Source URL), 다운로드 일자 |
| Lv1 | Lv1 · 기초 전처리 완료 자료 | 기초 전처리 완료 자료 | NAN 보간, 좌표계 변환 등 | 모델 입력용, 기초 통계 분석 | 보간방법, 좌표계 유형 |
| Lv2 | Lv2 · 도출된 2차 산출물 | 도출된 2차 산출물 | SPI, PDSI, NDVI 변형 | 모델 훈련, 논문 Figure 생성 | 산출 방법(논문/수식) |
| Lv3 | Lv3 · 유형화 및 고차 분석 결과 | 유형화 및 고차 분석 결과 | 재해 유형별 군집화 | 정책 제안, 재해 대응 매뉴얼 작성 | 유형화 기준, 클러스터 기반 리스크 매핑 |

- 기본 선택값 = `Lv2`(rev1 `<option selected>`). 상한 Lv3(미결-7 ⓐ). Lv0 은 별도 칸 두 개(PRD-19 · WU-B6), Lv1~Lv3 은 설명란에 적는다.
- **국문＋영문 병기 표기 예시 (PRD-02 원문 축자)** — `재분석자료 (Reanalysis Data)` 형태. 셀렉트 옵션·상세 칩에 적용하고 CHECK·필터·색인은 국문 단일이다.

- **요구 문면 축자 (PRD-33)** — 붙는 자리 세 곳. ⑴ 유형 셀렉트 아래 보조 문구 `참고 · {특이사항 및 주의점}` (PRD-04 정의 줄 다음 줄, 회색 한 줄). ⑵ ② 단계 설명 칸 아래 힌트 = 고른 분류의 `메타데이터 항목` ＋ 고른 가공 단계의 `메타데이터 필수 항목`(Lv1~Lv3). Lv0 은 별도 칸 소관이라 이 줄에 넣지 않는다. ⑶ 유형이 `위성자료`·`수치모형자료`·`합성자료` 이면 힌트를 `필요` 어조 그대로 보인다 — `해상도, 궤도 정보 명시 필요` / `초기조건 및 설정 파라미터 기재 필요` / `생성 방식, 융합 기법의 상세 설명 필요`. **저장을 막지 않는다.** `활용 예시` 열은 등록 화면에 넣지 않고 목록·상세 툴팁으로만 쓴다.
- **요구 문면 축자 (PRD-40 · 판정 ⓐ 비움 허용)** — 화면 입력에서만 비우고 저장은 `period_end = period_start` 로 채운다. 종료 칸 필수 표시를 걷고 안내 한 줄 `한 시점이면 비워 둬요` 를 붙인다. 표시는 시작=끝이면 한 값으로 그리고 관측 간격 괄호 병기(PRD-35)를 그대로 붙인다. **스키마·계약·CHECK 무변.**
- **변경 — 프론트만.** `RegisterArea.tsx` 의 단계 라벨·구성 교체 · `UploadModal.tsx` 의 장면 분기와 step 리셋 · 값 사전 상수 신설 · 확장보기 오버레이 신설 · 기간 달력 팝오버 · 공개 범위 값 칸 재동기 확인. ⛔ **WU-A3 이 세운 골격을 재작성하지 않는다 — 필드만 늘린다.**
- **변경 — DB·계약·서버: `DatasetCreate.required` 에 `category`·`dataType` 승격 ＋ 서버 미입력 400 「분류를 골라 주세요」(20차 승인 패키지 ⑴ · PRD-01·02 수용 기준 · FE 기본값 송신과 동시 · 마이그레이션 0). B1 advisor ② 로 이관·소유 지정(오케스트레이터 지정 · Ted 추인 대기).**
- **기존 데이터 처리**: 해당 없음(표시·입력층 전용). 기존 13행의 3축은 전 행 NULL 이고 `분류를 아직 안 골랐어요` 로 그린다(미결-3 ⓐ).
- **수용 기준 — 요구 본체**
  - Given 파일을 놓고 등록 진입, When 화면 확인, Then 표시기가 `① 분류 · ② 메타데이터 입력 · ③ 연결` 이고 ① 이 열려 있다.
  - Given ① 의 분류·유형 중 하나가 비어 있음, When `다음`, Then 막힌다(가공 단계는 기본값 `Lv2` 라 빈 상태가 없다).
  - Given ③ 진입, When 화면 확인, Then 계보 카드와 연관 프로젝트·논문 카드가 같은 단계 안에 있다.
  - Given ③ 에 있음, When 표시기의 ① 을 클릭, Then 조건 없이 ① 로 간다.
  - Given ② 의 이름·설명이 빈 채로 있음, When 표시기의 ③ 을 클릭, Then 조건 없이 ③ 으로 간다(rev1 `UI-003`). 막히는 자리는 `데이터셋 만들기` 하나다.
  - Given ③ 까지 갔다가 모달을 닫음, When 다시 열기, Then ① 분류다. 모달이 DOM 에 남아 있는 구현이어도 같다.
  - Given 세 셀렉트의 15개 값(5+6+4) 전부, When 값을 바꿈, Then 아래 한 줄이 그 값의 정의·예시로 바뀐다. 정의가 빈 값이 하나도 없다.
  - Given 유형 `위성자료` 선택, When 화면 확인, Then 정의 줄 아래에 `참고 · 해상도, 궤도 정보 명시 필요` 가 읽힌다.
  - Given 분류 `수문 인자` ＋ 가공 단계 `Lv1`, When ② 단계 설명 칸 확인, Then 힌트에 `유역코드, 관측 해상도` 와 `보간방법, 좌표계 유형` 이 함께 보인다.
  - Given 가공 단계 `Lv0`, When ② 단계 설명 칸 확인, Then 힌트에 `출처(Source URL), 다운로드 일자` 가 없다.
  - Given 15개 값 전부, When 각각 선택, Then 부가 문구가 빈 값이 하나도 없다.
- **수용 기준 — R-A′ 이관 6건** (출처 `dev-package/sessions/R-A2-ROUND-20260907.md` §5)
  - ㈎ **확장보기 오버레이** — Given 확장보기가 열려 있음, When 배경 클릭, Then 닫힌다. **닫기는 `requestClose`(`UploadModal.tsx:509`)를 경유한다** — ⛔ 직접 close 금지. Given 확장보기가 열려 있는 동안, When DOM 확인, Then 그 층에 `data-esc-layer="확장보기"` 표식이 붙어 있고(`ESC_LAYER_ATTR` 정의 `:53`) 업로드 모달의 Esc 처리(`:528`)가 삼켜지지 않는다. ⚠ **전용 확장보기 컴포넌트가 아직 없다 — 이 WU 가 만든다.**
  - ㈏ **WU-A6 기간 달력 팝오버** — R-A′ 에서 이관됨(판정-2 무변 · 담는 WU 만 바뀜). Given 기간 입력, When 위젯 확인, Then 달력 팝오버이고 최소 단위(PRD-18)까지만 칸이 열린다.
  - ㈐ **등록 3단계 재편** — Given 현재 라벨 `RegisterArea.tsx:55-57`, When 변경 후 확인, Then 세 라벨이 `① 분류 · ② 메타데이터 입력 · ③ 연결` 이고 표시기로 어느 단계로나 이동하며 모달 재오픈이 늘 ① 이다. ⛔ **WU-A3 골격 재작성 금지 — 필드만 늘린다.**
  - ㈑ **모달 2장면 구조** — Given 파일 0개, When 모달 열기, Then 드롭존만 보이고 등록 폼·미리보기가 DOM 에 없다. Given 파일 1개, When 확인, Then 장면2 이고 좌측 미리보기 ＋ 우측 ① 분류다. Given 장면2, When 파일 배지 `×`, Then 장면1 로 돌아가고 입력값 초기화 고지가 뜬다. Given 장면2, When 확인, Then 2단 등록 게이트·이어올리기 배너·기준 격자 첨부 자리가 여전히 도달 가능하다.
  - ㈒ **PRD-40 종료 비움 조립** — Given 시작 `2020-06-01`·종료 비움·단위 `일`, When 저장 후 재조회, Then 저장값이 `period_start = period_end = 2020-06-01 00:00` 이고 화면이 `2020-06-01` 한 값을 보인다. Given `period_end IS NULL` 인 기존 행, When 상세 조회, Then 종전과 같이 「무기한·진행 중」이다. Given 시작보다 앞선 종료 입력, When 저장, Then 400 이다.
  - ㈓ **공개 범위 값 재동기 재검증** — R-A′ 는 상수 문장 표시 여부까지만 검증할 수 있었다(값 칸이 R-B 몫). Given 3값 중 하나로 저장, When 저장 뒤 상세 재조회, Then 헤더 칩(`DetailHeader.tsx:93` 의 두 갈래가 **세 갈래**가 된다)과 공개 범위 설명이 **같은 값**으로 다시 그려진다. ⚠ 값 칸 자체는 WU-B4 가 세운다 — 이 WU 는 **재동기 재검증**을 소유한다.

### WU-B10 · 상세 계보 수정·추가 (PRD-31) — 계층 FE · 크기 M · 레인 `p3-lineage-fix`

- **의존**: WU-B5(Lv 연결 규칙) · WU-B8(계보 상태 판정식). **규칙 자체는 `R-B-2-server.md` 의 WU-B5 가 세운다** — 이 WU 는 그것을 재사용한다. 계약 동결 해제 20차를 쓰지 않는다.
- **현재 코드 (실측)**
  - `d4_lineage_edge.method` = `db/platform/schema.sql:670` — **이미 있는 칸을 여는 것**이고 이 WU 는 **FE 진입만** 만든다(부록 A `H-50`).
  - 등록 화면 입력 열쇠 — `frontend/src/components/lineage/LineageStep.tsx:45-46` (`/** 사람이 직접 적은 가공 방식 → 요청의 `method`. */` · `method: string;`). ⚠ PRD 사본이 가리킨 `upload/LineageStep.tsx:350-356` 은 **경로가 낡았다**(파일은 `components/lineage/` 아래에 있다).
  - 상세 표시층 — `frontend/src/components/lineage/LineageSection.tsx:187`(계보 행 `가공 방식: …`) · `:307`(`data-testid="lin-method"`) · `:311`·`:313`(그래프 엣지 라벨). **표시층은 이미 서 있다**(PRD-30).
  - 계약 열쇠 — `contracts/seams/fe-core.yaml:3357` · `:4108` · `:4160` (`method:`). ⚠ PRD 사본의 `:2679`·`:3305` 는 실측과 갈린다.
  - 상세에 계보 편집 진입점은 없다.
- **요구 문면 축자 (PRD-31)** — rev1 `H-46`·`H-47` 축자 — 「가공 전 데이터를 추가했어요. 직접 연결로 남아요」 / 「아직 채워지지 않은 계보예요」 / 「원자료(Lv0)라 부모가 없다면 그대로 두어도 괜찮아요.」 파생 행 문면 = `여기서는 못 고쳐요`(rev1 `H-45`).
- **변경 — 프론트만.** ⑴ 상세 계보 구역 헤더에 `계보 수정 · 추가` 버튼. 누르면 모달이 열리고 **등록 ③ 과 같은 찾기·연결 UI** 를 쓴다(PRD-07·08·09 규칙이 그대로 적용). `가공 방식` 칸은 새 칸이 아니라 `d4_lineage_edge.method` 를 여는 것이다. 추가 항목은 `직접 연결` 로 남고 토스트 `가공 전 데이터를 추가했어요. 직접 연결로 남아요` 를 낸다. ⑵ 계보가 `기록 없음` 인 상세는 빈 상태 3문면 ＋ 버튼 `계보 채우기`. ⑶ 파생 행은 읽기 전용이며 `여기서는 못 고쳐요` 를 붙인다.
- **변경 — 계약·서버: 없음.** 계보 확정·수정 경로가 이미 있고 상세에서 오는 수정도 같은 경로를 쓴다 — **새 op 를 만들지 않는다.** 권한 게이트 = `업로드·편집`. **변경 — DB: 없음.**
- **PRD-22 편집 대상 확장** — 편집 대상 목록에 계보 부모 연결을 더하되 편집 화면 안에 계보 표를 그리지 않고 이 모달로 보낸다(두 곳이 같은 규칙을 각각 구현하지 않게 한다).
- **기존 데이터 처리**: 해당 없음(화면 경로다).
- **수용 기준**
  - Given 계보가 `기록 없음` 인 데이터셋, When 상세의 계보 구역 확인, Then 빈 상태 3문면이 보이고 `계보 채우기` 가 눌린다.
  - Given `계보 수정 · 추가` 로 부모 1건 추가, When 저장 후 재조회, Then 그 부모가 `직접 연결` 로 보이고 계보 상태가 `확인 필요` 또는 `확정` 으로 바뀐다.
  - Given 자기 Lv 보다 높은 부모를 모달에서 고르려 시도, When 확인, Then 등록 ③ 과 같은 사유 문구로 막힌다 — **재사용을 diff 로 보인다**(같은 컴포넌트·같은 상수를 쓴 것이 diff 에 나타난다).
  - Given `업로드·편집` 권한 없는 계정, When 상세 조회, Then 버튼이 보이지 않고 API 직접 호출도 거절된다.
  - Given 파생 행, When 확인, Then 읽기 전용이고 `여기서는 못 고쳐요` 가 읽힌다.
  - Given 상세에서 `가공 방식` 을 채움, When 재조회, Then `LineageSection.tsx:187` 의 계보 행과 `:313` 의 엣지 라벨이 그 값을 그대로 읽는다.

### Ted 판정 대기 — 착수 차단 아님

> 출처 = `dev-package/sessions/R-A2-ROUND-20260907.md` §4 의 12건 중 **이 파일이 소유하는 8건**. ⛔ **판정 전에 임의로 정하지 않는다.** 판정이 오면 해당 수용 기준을 그때 덧붙인다.

- **1** — `frontend/src/components/lineage/LineageSection.tsx` 문면(현재 「가공 방식은 화살표 라벨 · 데이터 상자를 누르면 그 상세로 가요」 ↔ PRD-43 축자 「각 데이터를 누르면 그 상세로 가요」). 코드 유지 / PRD 축자 채택.
- **2** — D-07 같은 해 생략 표기를 단위 `일` 에도 적용할지(적용 시 `test/interval-period-20260906.test.tsx:311` 개정 필요).
- **3** — rev1 `pbStatus` 문면 **4개** ↔ 화면 상태 **3개**. 4단계로 늘릴지 / 3단계 유지할지.
- **4 · 7** — ⑫ 할 일 안내 축자 「분류를 고르고…」 ↔ 코드의 ① 단계명 `자동 메타데이터 확인` 불일치. `FOOT_HINTS[1]` 존치 여부가 같은 사안이다. ⚠ 이 WU 가 ① 단계명을 `분류` 로 바꾸므로 **판정이 오면 문면이 저절로 맞을 수 있다** — 그래도 임의 확정하지 않는다.
- **5** — `다음` 차단 조건 `ready` 가 격자 검증을 포함해 격자 검증 중에도 `다음` 이 막힌다. 수용 여부.
- **6** — ⑩ 접근 구역의 공개 범위 **값 칸**(현재는 출처 문장만 단독 노출). 그대로 둘지 / 문장도 숨길지.
- **10** — PRD-42 「빈 이름 → 종전 문면」이 빠른 생성 칸에서 무반응(실측 `frontend/src/components/upload/RegisterArea.tsx:495` `quickCreate` · 호출 `:624`). 고칠 WU 지정.
- **12** — 상세 편집 중 `GridAttachEntry` 숨김 여부(실측 `frontend/src/components/upload/GridAttachEntry.tsx:31` · 사용 `frontend/src/routes/DatasetDetailPage.tsx:253`).

---

## 3. 지켜야 하는 규약 — 명령으로

### ㉮ 워크트리 레인

- WU 하나에 레인 하나. 레인 이름 = `p3-register-steps`(WU-B3) · `p3-lineage-fix`(WU-B10).
- 각 레인은 `origin/main` 에서 딴 자기 워크트리에서 돈다. 병합은 **ff-merge**, 병합 뒤 워크트리·로컬/원격 브랜치를 정리한다.
- 한 레인 = 한 WU. 두 WU 를 한 브랜치에 섞지 않는다. **두 WU 가 `LineageSection`·`RegisterArea` 를 함께 건드리지 않으므로 병렬 가능하나, 의존이 다르므로 B3 → B10 순서를 지킨다.**

### ㉯ 착수 전 — `work-items.yaml` **등재 확인**

⭑ **등재는 이미 끝났다**(WU-B3·B10 포함 10건). 이 세션이 하는 것은 **등재 확인**이다.

```bash
grep -n -A14 '^  - id: WU-B3' dev-package/work-items.yaml
grep -n -A14 '^  - id: WU-B10' dev-package/work-items.yaml
```
- 확인 항목 = `status` · `entry_conditions`(B3 = R-A 병합 완료 ＋ B1·B2 계약 열쇠) · `depends_on` · `completion_def`. 갈리면 **대장이 이긴다**(`CLAUDE.md §6`).
- 완료 시 `status: done` ＋ `evidence` 갱신. ⛔ 없는 행을 새로 덧붙이지 않는다(중복 id 는 `work-item-consistency` red).

### ㉰ 계약 동결 해제 — **이 파일은 쓰지 않는다**

- **여는 것은 `R-B-1-db.md`(B1·B2·B4·B6)와 `R-B-2-server.md`(B8)** 이고 회차는 **20차 · 등급 ㉯** 다. 근거 문서 = `dev-package/sessions/X2-FREEZE-PROTOCOL.md` §5.
- 이 파일의 두 WU 는 **계약 파일을 고치지 않는다.** 진입조건으로만 건다 — **B1·B2 의 계약 열쇠가 서 있어야 WU-B3 을 착수하고, B5·B8 의 규칙·판정식이 서 있어야 WU-B10 을 착수한다.**
- ⛔ `contracts/` 를 이 레인에서 수정하면 그 자체가 절차 위반이다. 필요가 생기면 **멈추고 보고한다.**

### ㉱ 결정 번호 〈N〉 — 예약하지 않는다

```bash
git fetch origin main && bash dev-package/prd/tools/max-decision.sh   # 병합 직전에 다시 잰다
```
2026-09-07 실측 최대 = **〈372〉**(참고값 · 병합 직전 재실측). **병합 직전 `origin/main` 최대 ＋ 1** 이 이번 번호다.
- 이 파일의 두 WU 는 **계약을 열지 않으므로 해제 회차 행을 쓰지 않는다.** 새 결정이 생기면(예 Ted 판정 8건의 회신 반영) `PLAN-SoT §9` 에 한 행씩, 필드 8개(X2 §5-㉲)로 적는다.
- ⛔ **HANDOFF 에는 값을 적지 않는다** — 두 곳에 적으면 갈라진다(`CLAUDE.md §6-3`).

### ㉲ 게이트 — 작업 중엔 단독, 병합 전엔 전건

```bash
# 작업 중 (단독 게이트만 하나씩)
./gates/run.sh frontend-typecheck
./gates/run.sh frontend-test
./gates/run.sh frontend-fixture-reach       # B3 · B10
# 병합 직전 한 번
./gates/run.sh all -j 1
```
⛔ **게이트를 끄거나 검사 대상을 줄이지 않는다.** 미구현 게이트의 red 는 버그가 아니다. **green 으로 시작한 테스트는 오라클이 아니다** — 실패 테스트 red 를 먼저 확인한다.
⚠ 워크트리는 `node_modules` 를 승계하지 않는다 — `frontend` 는 `npm ci` 를 먼저 돌린다(미구성 red 는 준비 red 다).

### ㉳ 커밋 문면

```
R-B FE 계층 — 등록 3단계 재편 · 값 안내 15값 · 모달 2장면 (WU-B3)

- 표시기 ① 분류 · ② 메타데이터 입력 · ③ 연결 · 표시기 임의 이동 · 재오픈 늘 ①
- R-A′ 이관 6건 동봉 — 확장보기 오버레이 · 달력 팝오버 · 2장면 · 종료 비움 · 공개 범위 재동기
- 계약 무변(20차는 B1·B2·B4·B6·B8 몫) · RED 선실측 → GREEN: <시험 파일>:<건수>
```

### ㉴ 금지

- ⛔ `main` 에 직접 push. ⛔ 이 레인에서 `contracts/`·`db/`·`services/` 수정(FE 단독 계층이다).
- ⛔ **WU-A3 골격 재작성**(필드만 늘린다). ⛔ 2단 등록 게이트·기준 격자 흐름·이어올리기 배너 걷어내기.
- ⛔ 확장보기에서 `requestClose` 를 우회한 직접 close. ⛔ `data-esc-layer` 표식 누락.
- ⛔ 유형↔가공 단계 조합 검증 신설. ⛔ 도움말 팝업 신설(배치는 「선택기 아래 보조 문구 ＋ 설명란 힌트」로 확정 · 미결-12 ⓐ).
- ⛔ 표시기 이동에 검증 붙이기(rev1 `UI-003` — 막는 자리는 `데이터셋 만들기` 하나다).
- ⛔ Ted 판정 대기 8건을 임의로 확정하기. ⛔ 미결-17 잘린 1행 추정 전사.
- ⛔ `40 COLAB-기획/` 문서 수정. ⛔ 문서·주석에 절대경로. ⛔ 이 세션이 `03-HANDOFF.md` 를 직접 고치기 — §4 의 5줄만 넘긴다.

---

## 4. 산출물과 근거

| 무엇 | 어디 |
|---|---|
| 프론트 — 등록 | `frontend/src/components/upload/RegisterArea.tsx`(단계 라벨·구성) · `UploadModal.tsx`(장면 분기 · step 리셋 · 확장보기 Esc 계층) |
| 프론트 — 값 사전 | 분류 5값 · 유형 6값 · Lv 4값 ＋ 부가 열 3종을 담는 **FE 상수 한 곳**(PRD-04·33 이 같은 사전을 쓴다) |
| 프론트 — 계보 | `frontend/src/components/lineage/LineageSection.tsx`(진입 버튼 · 빈 상태) ＋ 등록 ③ 찾기·연결 UI **재사용** |
| 마이그레이션 · 계약 · 서버 | **없음**(이 파일의 두 WU 는 FE 단독) |
| 세션 노트 | `dev-package/sessions/p3-register-steps-<YYYYMMDD>.md` · `dev-package/sessions/p3-lineage-fix-<YYYYMMDD>.md` — **각 ≤ 60행** |
| 대장 | `dev-package/work-items.yaml` — WU-B3 · WU-B10 **기등재**. 완료 시 `status: done` ＋ `evidence` 갱신 |
| 원장 | `PLAN-SoT §9` — 새 결정이 생길 때만 · **병합 직전** |

**오케스트레이터에 넘기는 HANDOFF 갱신문 — 5줄 이하. 세션이 `03-HANDOFF.md` 를 직접 고치지 않는다.**

```
R-B-3(FE) 완료 — WU-B3 · WU-B10, 레인 p3-register-steps · p3-lineage-fix, 병합 <sha1>·<sha2>
등록 표시기 ① 분류 · ② 메타데이터 입력 · ③ 연결 · 15값 정의·예시 · 상세 계보 수정·추가 진입
R-A′ 이관 6건 동봉 — 확장보기 오버레이 · 달력 팝오버 · 등록 3단계 재편 · 모달 2장면 · 종료 비움 · 공개 범위 재동기
게이트: frontend-typecheck · frontend-test · frontend-fixture-reach green / 병합 직전 all -j 1 green <n>/<n>
Ted 판정 대기 — 이 파일 소유분 8건(1·2·3·4·5·6·7·10·12) — 착수 차단 아님. 근거: dev-package/sessions/p3-*-<YYYYMMDD>.md
```

---

## 5. 완료 판정

- **WU-B3** — 표시기 세 라벨이 `① 분류 · ② 메타데이터 입력 · ③ 연결` · 눌러서 어느 단계로나 이동 · ① 세 값에 기본 선택값(`기상·기후 인자`·`재분석자료`·`Lv2`) · **15개 값 전부에 정의·예시 한 줄이고 빈 값 0** · 유형 6값에 `참고 · 특이사항` · 설명란에 분류·단계별 힌트 · **모달 재오픈이 늘 ①** · WU-A3 골격이 재작성되지 않았음이 diff 로 보인다.
- **WU-B3 이관 6건** — 확장보기가 배경 클릭으로 닫히되 `requestClose` 를 경유하고 `data-esc-layer="확장보기"` 를 단다 · 기간 달력 팝오버가 선다 · 장면1/장면2 전환과 존치 3종 도달 가능 · 종료 비움이 `period_end = period_start` 로 조립되고 화면이 한 값을 보인다 · 저장 뒤 헤더 칩과 공개 범위 설명이 **같은 값**으로 다시 그려진다(세 갈래).
- **WU-B10** — 상세에 `계보 수정 · 추가` 진입 · `기록 없음` 상세가 빈 상태 3문면과 `계보 채우기` 를 그린다 · **등록 ③ 규칙 재사용이 diff 로 보인다** · `가공 방식` 이 `d4_lineage_edge.method` 로 저장되고 계보 행·엣지 라벨이 그 값을 읽는다 · 파생 행 읽기 전용 ＋ `여기서는 못 고쳐요` · 권한 없는 계정은 버튼 미노출 ＋ API 거절.
- **게이트** — `frontend-typecheck` · `frontend-test` · `frontend-fixture-reach` 전건 green, 그리고 병합 직전 `./gates/run.sh all -j 1` green.
- **절차** — 대장 등재 확인이 첫 커밋보다 먼저 있었음이 보인다 · `contracts/`·`db/`·`services/` 에 이 레인의 변경이 **0건**임이 diff 로 보인다 · Ted 판정 대기 8건이 **임의 확정되지 않았음**이 보인다.

---

### 다음 파일

`dev-package/prd/rounds/R-B-4-verify.md`(WU-B11 ＋ R-B 종료 검증 · 진입조건 = WU-A11 판정표).
⚠ **이 파일의 WU-B3 은 `R-B-1-db.md`(B1·B2)가 병합된 뒤에, WU-B10 은 `R-B-2-server.md`(B5·B8)가 병합된 뒤에 착수한다.**
