# Spec: 하네스 개선 총괄 계획 — PR 1 이후 전 단계(E0 · S-red · PR 2 · Ted · effort · PR 3 · PR 4 · E1 · X · M · S)
출처 intent: `dev-package/intent/2026-09-25-harness-improvement.md`
출처 절: 「판정 기록」 1–7라운드 · 7라운드 Ted 원문 "전부 권고대로 엄격하게 다시짜" · 설계 원칙 2026-09-26(「동작과 제약은 시스템으로」).
근거(저장소 밖): `~/.claude/reports/harness-state-20260925/`{system-first-recut/recut.md · playbook-gap-20260926/direction.md · opus55-prompt-audit/cross.md · findings-verified.md · master-plan-20260926/verify-master.md(gate-1 판정 · 차단급 4 · 개선 10 반영)}. 줄 번호 기준 = 브랜치 `claude/harness-improvement` HEAD `27f7fb6c`(develop `67a03a05` + intent/spec + PR 1 병합 `d97141fb`) · 2026-09-26 재열람.
관계: phase 1 spec `dev-package/prd/specs/S-HARNESS-IMPROVEMENT-20260925.md`(PR 1 완료)의 phase 2/3 stub 을 이 문서가 대체한다. E0 · S-red 는 spec 상세가 있다(`S-HARNESS-E0-EVAL-GATE-20260926.md` · `S-HARNESS-SRED-REDRUN-20260926.md` — 단위 ID · 결정은 그 spec 이 정본 · 이 문서 §2 는 요약). PR 2 이후는 착수 전 이 문서의 해당 절을 spec 상세(V-id · 시험 · 레인 지시)로 확장한다. 커밋 트레일러 `Intent-Ref: dev-package/intent/2026-09-25-harness-improvement.md`(게이트 `intent-ref`). T 번호는 이 문서 Ted 행동표가 정본(두 spec 도 같은 번호).

## 0. 출처 · 원칙
- 순서(7라운드 확정): PR 1 병합 → Ted(T1 ruleset · T11 PAT) → E0 → S-red → PR 2 → effort 실측(Q6) → PR 3 → PR 4 → E1 · X · M · S. 대안 「S-red → E0」 는 §6 Q2(Ted).
- playbook 의존 순서: ⑴ evals 가 CLAUDE.md/skills/hooks 변경을 gate 한다 → E0 가 PR 2 보다 앞 ⑵ approval gate(사람 병합 · ruleset)가 CI 자동화보다 앞 → T1 이 PR 2 open 보다 앞.
- 시스템 우선: 규칙(must/never)은 hook · gate · `permissions` · frontmatter · lifecycle · CI · ruleset 으로 강제한다. 강제 불가 규칙은 지우거나 「이유 1줄 + 장치 위치」 포인터로만 남긴다. 병합 · 배포 · 삭제는 사람이 하고 git-guard · ruleset · PAT · TTY 토큰이 받친다.
- 단위 규칙: 모든 단위는 ① 강제 장치(파일 · 이벤트/검사 · FC=fail-closed/FO=fail-open · Claude/Codex 적용) 또는 「산문 · 맥락만」 ② 증명 시험(파일 + 사례)과 실행 게이트 ③ 병합 조건(PR 블록에 1회) ④ 지우는 산문 ⑤ 소유 PR ⑥ 출처 를 갖는다. 장치 없는 단위는 강제 장치 열에 「산문 · 맥락만」 을 적는다. Ted 행동은 T-번호 · 명령/UI 단계 · 증거 기록 자리를 갖는다.
- 게이트 종료코드 0/1/78(AGENTS.md:45) · 훅 정의(`.claude/settings.json` hooks · `.codex/hooks.json`) 무변경 유지(재신뢰 0) · 승인 intent append-only · ADR 이력 무수정(새 규칙 = 새 ADR).
- eval 해시 집합 = Q-A ⓐ 확정(`.claude/**` · `.agents/**` · `gates/**` · hook 스크립트 · 재개봉 없음) · 정본 = E0 spec 4.1 `eval/harness/config-paths.txt`. 회차는 병합 직전 head 에서 1회 · 그 뒤 해시 집합 파일 push 금지 · Update branch(develop 병합)는 집합 파일을 안 건드리면 해시 불변이라 허용 · 게이트 출력에 `hash(head)=hash(회차)` 두 값.

## 1. 순서표
| # | 단계 | 선행 조건 | 병합/완료 조건 | Ted 행동 | 측정 지점 |
|---|---|---|---|---|---|
| 1 | PR 1(A1–A7 · C12) | — | 게시 · CI green · Ted 병합 | T13(병합) | agent-bridge 시험 수 기준값(spec §5) |
| 2 | T1 ruleset · T11 PAT | PR 1 병합 · develop pull | `gh api …/rulesets` 에 develop 규칙 1건 · 에이전트 토큰 `gh pr merge` 403 | T1 · T11 | ruleset JSON 스냅샷(→ PR 3 3-5) |
| 3 | E0 eval gate PR | T1 적용 | E0-1~E0-7 green · 실측 1회 결과 커밋 · ci.yml `harness-eval` 잡 MATCH(해시 일치) green | T12(실측) · T16(게시) · T17(분류 판정) · T13 | eval 회차 #5 · 실패 4건 분류 |
| 4 | S-red 소형 PR | E0 병합 | `test_task_runtime.py` ①–⑨ · `test_harness_lifecycle_contract.py` ⑩·⑫ green · agent-bridge · `harness-eval` MATCH green(S-red head 회차) | T12 · T16 · T13 · T18(병합 뒤 fix 레인 실측) | fix 레인 hook exit 2 관측(V-S10) |
| 5 | PR 2(B · L · P-roles · G1 G2 G6 P2 P7 VERDICT at) | S-red 병합 · L8 스모크 기록 | §2 PR 2 블록 병합 조건 | T12(PR 2 head eval) · T16 · T13 · T8(prune 첫 실행) | audit.jsonl 시작 · handoff `at` 시작 |
| 6 | effort 실측(Q6) | PR 2 병합 · 첫 레인 1회 관측 | 보고서 `dev-package/reports/harness/<날짜>-opus55-effort/` | T15(스폰 비용 승인) | 3계수 · 턴 · 토큰 · 벽시계 |
| 7 | PR 3(C · P-rules · permissions · G3 P8 P6 · ruleset 스냅샷 · X 측정) | Q6 결과(audit.jsonl ≥1주 계수는 3-8 만의 조건) | §2 PR 3 블록 병합 조건 | T12 · T16 · T13 · T14(bypass-deny 실측) · T3 · T5 | 1차 CI 통과율 · eval 추이 표 |
| 8 | PR 4(skills 산문 · G7 · advisor 보안 pass · ADR-0011 · effort 값) | PR 3 병합 · Q6 값 | §2 PR 4 블록 병합 조건 · 회차 = PR 4 병합 전 1회 | T12 · T16 · T13 | PR 4 head 회차 |
| 9 | E1 · X · M · S(S-auth · S-dep) | PR 4 병합 | E1 = E0 대비표 · 실패 4건 해소 판정만(회차는 행 8) · 나머지 각 블록 조건 | T16 · T13 | metrics.py 첫 산출 |

## 2. 단위표
열 = ID · 내용 · 강제 장치(파일 · 이벤트/검사 · FC/FO · Claude/Codex) · 시험 · 게이트 · 지우는 산문 · 출처.

### E0 — eval gate 기제 PR(단독 lane · 실측 = 레인 먼저 · 중첩 `claude -p` 거부 시 T12 · 정본 = E0 spec §4)
| ID | 내용 | 강제 장치 | 시험 · 게이트 | 지우는 산문 | 출처 |
|---|---|---|---|---|---|
| E0-1 | 해시 정본 `eval/harness/config-paths.txt`(`AGENTS.md` · `CLAUDE.md` · `.claude/**` · `.agents/**` · `gates/**` · `scripts/harness/hooks/**` · 자기 자신 · 제외 = `results/**` · `.codex/**` · `scripts/harness/*.py` · `eval/harness/**` 는 §6 Q3) + 계산기 `eval/harness/config_hash.py`(`git ls-files` 추적 + 비무시 미추적 · 파일 sha = `git hash-object` · symlink = 링크 텍스트 · 정렬 연결 sha256) | 계산기(장치 없음) · Claude/Codex 공통 | `scripts/tests/test_harness_eval_gate.py` ⑴–⑷(+ symlink) · 게이트 `harness-contract-selftest`(`run.sh:357` 등록) | 없음 | direction 격차 1 · Q-A ⓐ · E0 spec 4.1 |
| E0-2 | `eval/harness/run.sh` 가 회차마다 `results/<run>/config-hash.json` + `summary.md` 「설정 해시」 1줄(`:76-78` 뒤 · `:259-270` 요약 블록) · 해시 실패 = 78(`ready_red` `:37-44` · 해시 없는 결과 생성 금지) | 러너 · 회차마다 · FC · 도구 무관 | `eval/harness/tests/run-selftest.sh` ⓛⓜ · CI `harness-runner-selftest`(ci.yml:694) | 없음 | E0 spec 4.2 |
| E0-3 | MATCH 판정: `gates/tools/harness-eval.sh:87-96` 면제 분기가 `config_hash.py verify` 호출 — 현재 해시와 일치하는 전수 결과(선택 실행 아님 · 준비 0 · 과제 수 = N) 없음 = 78(`missing=eval-result:<hash>`) · 일치 결과 green 집합이 직전 결과보다 줄면 1(회귀) · 일치 + 무회귀 = 0(run id · 계수 · `hash(head)=hash(회차)` 출력) · env(`COLAB_HARNESS_EVAL_EXEMPT=1`) · 등록부 · `record` 무변경 · 회차 무효화 규칙 = §0 마지막 항 | 게이트 + CI 잡 `harness-eval`(`if: needs.changes.outputs.harness == 'true'` :683) · FC · 도구 무관 · 병합 차단 효력 = T1 뒤 | `gates/tools/harness-eval-selftest.sh` ⓛ–ⓡ(없음 78 · 일치 0 · 회귀 1 · 선택 실행 78 · 준비>0 78 · 해시 불일치 78 · 직전 없음 0) · CI `harness-judge-selftest`(:695) | `harness-eval.sh:8-10` 「승격 전」 머리말 · `gates/README.md:47,62-71` 「건수만」 → 해시 일치 규칙 | direction 격차 1 · Ted 7라운드 B · E0 spec 4.3 |
| E0-4 | ci.yml `changes.harness` 필터(:119-128)에 `.claude/settings.json` · `.claude/rules/**` · `gates/**` · `eval/harness/**` 추가(4줄 · results 포함 = 결과 커밋 PR 도 게이트를 깨움) · `gates/tools/ci-filter-check.py` `WANT_PATTERNS` → 정본 읽기 ∪ `FILTER_ONLY`(`.codex/**` · `scripts/harness/**` · `scripts/agent-bridge.py` · `eval/harness/**`) · ㈖ 정본 ⊆ 필터 · `:641-650` 주석 → `:681` 잡 위로(E0 소유 · C3 는 `gates/README.md:245` 표만) | CI 필터 · FC(ci-filter-check 축자 대조) | `scripts/tests/test_ci_eval_policy.py` 변이 2건(`harness-eval-selftest.sh:159-169` 안에서 실행 · PyYAML 잡) | ci.yml:641-650 「승격 전 면제」 10줄 → 4줄 | direction 격차 1 · E0 spec 4.4 |
| E0-5 | `scripts/harness/check.py` `check_eval_freshness`(최신 회차 > 30일 → `warning:` 1줄 · exit 불변 · results 부재 = 78) · 상수 1곳 `check.py`(`harness.yaml` 무변경) | harness-contract 출력 · FO(경고) | `test_harness_eval_gate.py` ⑸ · 게이트 `harness-contract` | 없음 | direction 격차 1 · E0 spec 4.5 |
| E0-6 | 실측 1회(E0 최종 코드 상태 · `COLAB_EVAL_TIMEOUT=93 COLAB_EVAL_BUDGET=2.01` · 레인 먼저 · 거부 시 T12) · `results/<run>/` 커밋 · 실패 4건(H14 · H15 · H16 · H18 · `results/20260912-211809/summary.md:22-26`) 분류(하네스 문안 결함 2 · 겹침 2 · 확정은 재실측 뒤)를 intent 「확인」 절에 5줄 · expect/task 수정은 T17 뒤 별건 | 「산문 · 맥락만」(기록 · 판정은 E0-3 회귀 규칙) | 시험 없음 · 조건 = 기록 존재 + 결과 커밋 + `harness-eval` 0 | 없음 | direction 격차 1 · 6라운드 실패 4건 메모 · E0 spec 4.6 |
| E0-7 | 문서: `eval/harness/README.md` 「결과」 여섯 종 · 새 절 「설정 해시 · 면제 조건」 · `gates/README.md:47-48,62-71,293` · `dual-agent.md:192` 1구 | 「산문 · 맥락만」 | V-E8 grep 0건 | 「다섯 중 셋」 · 「건수만」 · `harness-eval.sh:8-10` 3줄 | E0 spec 4.7 |
- E0 병합 조건: E0-1~7 시험 green · `harness-eval` 잡 MATCH green(E0 head 회차 커밋 · 그 뒤 집합 파일 push 0) · `harness-contract` · `intent-ref` · `exec-bit` green · 훅 정의 · 등록부 · `verify_evidence.py` diff 0 · 게시 T16 · Ted 병합(T13).

### S-red — red-run 소형 PR(N-5 · Q-B ⓐ · 정본 = S-red spec §4)
| ID | 내용 | 강제 장치 | 시험 · 게이트 | 지우는 산문 | 출처 |
|---|---|---|---|---|---|
| S-1 | `lifecycle begin --role lane-worker --fix --red <path>[::case]`(반복)가 시험을 실제 실행해 rc 1 일 때만 task 를 열고 `task.json.fix.red[]`{spec · path · case · blob(`git hash-object`) · runner · rc · output_sha256 · log} 기록 · 러너 표 `RED_RUNNERS` 1곳(unittest 경로 + `-k` · vitest · pytest · bash) · rc 0(이미 green) · rc ∉ {0,1} · 미지원 경로 · `--fix` ⊻ `--red` · 비-lane-worker · `--legacy` = 78 · 마커 `<git-common-dir>/colab-harness/red-locked/<task_id>`(`lifecycle_contract.py` `begin` :210 · CLI :542-550) | lifecycle CLI · FC(거절 78 · task 미생성) · Claude/Codex 공통 | `scripts/tests/test_task_runtime.py` ①–④ · 게이트 `agent-bridge`(`run.sh:351`) | 없음 | direction 격차 2 · S-red spec 4.1 |
| S-2 | `gates/run.sh task`(`run_gates` :466-481)가 red 항목마다 행 `fix-red:<spec>`(blob 불변 ∧ rc 0 = green · blob 변경 = red_판정 · rc 1 = red_판정 · 그 외 red_준비) · `verify_task_report`(:328 · :335)가 행 전부 green + `verify_red_locked`(현재 blob == 기록 blob) 요구 · `stop()` :350 경유 · 위반 사유에 출구 | lifecycle CLI + H7(`lane-gate-summary.sh`) · FC · CLI 공통 | `test_task_runtime.py` ⑤–⑨ · `agent-bridge` | `lane-worker.md:44` 「red 를 실제로 확인 … red 로그 한 줄 인용」 → 「`begin --fix --red` 가 기록 · 인계가 요구」 포인터(편집은 S-5) | direction 격차 2 · 2-8 · S-red spec 4.2 |
| S-3 | `test-file-guard.sh` `:42` 앞 red-locked fast path(bash · 마커 없고 env 없으면 exit 0) → 잠금 상태면 `file_path` 가 속한 checkout(`FTOP`) 기준 `lifecycle_contract.py red-locked --checkout` 조회 → 기록 red 경로 Edit/Write = exit 2(agent_id · env · cwd 무관 · `COLAB_ALLOW_TEST_EDIT` 무효 · 부모 세션이 lane worktree 의 잠금 경로를 고쳐도 차단) · 훅 정의 무변경 | PreToolUse Edit\|Write hook 본문 · FC(잠금 상태 lookup 실패 = 2) · Codex = bridge `apply_patch` 페이로드 같은 스크립트 | `test_harness_lifecycle_contract.py` `RedLockedGuardTests` ⑩-a…j · Codex ⑫ · `agent-bridge` · 병합 뒤 fix 레인 1회 실측(V-S10 · T18) | `test-file-guard.sh:12-14,23-29` 「Claude lane 통과」 · `design-review/SKILL.md:101-102,107` · `dual-agent.md:59-60` 「편집 시점 차단 없음」 | A2 ⓐ 선행 · S-red spec 4.3 |
| S-4 | CLI `handoff` 통과 시 `task.json.handed_off = {mode, run_id, at}`(L1 「인계됨」 첫 단) · 마커 제거 · 「닫힘」 · prune · `blocked` 값은 2-4 | 기록(판정 아님 · S-3 잠금 해제의 입력) | 시험 ⑤ · ⑨ · `agent-bridge` | 없음 | intent L1 · S-red spec 4.4 |
| S-5 | 문서: `lifecycle-evidence.md` 「fix 레인」 절(정본) · `design-review` · `dual-agent` · `lane-worker.md:38,44` · `test-file-guard.sh` 머리말 · `README.md` A3 blockquote 1구 | 「산문 · 맥락만」 | V-S7 grep 0건 | 위 「지우는 산문」 전부 | S-red spec 4.5 |
- S-red 병합 조건: ①–⑫ red→green 로그 · `agent-bridge` · `harness-contract` · `intent-ref` · `exec-bit` green · `harness-eval` MATCH green(S-red head 회차 · T12 · 면제 선택지 없음 · 순서 대안은 §6 Q2) · 훅 정의 diff 0 · 게시 T16 · Ted 병합(T13).

### PR 2 — B · L · P-roles · G1 G2 G6 P2 P7 VERDICT · handoff `at`(lane 1 · 커밋 묶음 B → L → P)
| ID | 내용 | 강제 장치 | 시험 · 게이트 | 지우는 산문 | 출처 |
|---|---|---|---|---|---|
| 2-1a | B1: `verify_evidence.py:58` 오류 문구에 「Update branch(develop 병합 push)가 새 run 을 만든다 · Re-run 은 계속 red」 · `colab-v2-work` PR 절차 1줄 | 기존 검사 유지(FC) · 산문 1줄 | `test_harness_evidence.py:164` 문구 단언 추가 · `harness-contract-selftest` | 사용자 메모리 절차(저장소 밖) | intent B1 3라운드 |
| 2-1b | B2 ⓓ: `changes` 잡이 등록부를 읽어 `run-service-tests-<svc>` 출력 → 서비스 시험 잡 `RUN`(ci.yml:491) · `if:` 가 그 출력만 읽음 · 등록부 filters 에 ai-service · pipeline-worker 추가 | CI + `verify_evidence.py applicable`(신설 하위명령) · FC | `test_harness_evidence.py` filters {ai-service:true} + core-api 증거 → green · `test_ci_required.py` · `harness-contract-selftest` | ci.yml:167-171 「골격」 주석(C3 ①) | intent B2 2라운드 · 4라운드 C3 ① |
| 2-1c | B3: 판정 우선(1 > 78 > 0) 통일 — `lifecycle_contract.py:499-500` `run_gates` · `gates/run.sh` `all`(:965-981 계수 · :1010 `exit $rc`) · `record` 명령 불일치(`verify_evidence.py:288-293`) → 1 · `ci` JSON 해독 실패 → 78 · lifecycle 판정성 raise 에 `JudgementError(ValueError)` → 1(`:611-613` 분기) · 새 ADR-0010 | 집계기 4곳 · FC · CLI 공통 | 입력 조합 {1 · 78 · 1+78 · 111 · 표식만} 표를 `test_task_runtime.py` · `test_harness_evidence.py` · `scripts/tests/test_gates_run_args.py`(all 요약 fixture)에 고정 · scope 위반 handoff → 1(`test_task_runtime.py:379-380` 갱신) · `adr-records` | `gates/README.md` 집계 서술 → ADR-0010 포인터 | intent B3 2라운드 |
| 2-1d | B4 + L6 ⓐ: `harness-contract` · `agent-bridge` 를 ci-producers 생산자로 등록 · ci.yml 새 잡에서 `record` 실행 · `check.py:113` 에 「`gates.required`(harness.yaml:16-29) ⊆ 등록부 `checks[*].gates`」 대조 · `agent-bridge.yml` → `workflow_dispatch` 전용 · ci.yml agent-bridge 잡의 `if:` 필터에 `scripts/tests/**` 추가(E0-4 필터 집합에 동기) 또는 무조건 실행 | harness-contract gate + CI · FC | `test_harness_config.py` fixture(생산자 없는 required → red) · `test_harness_evidence.py:203` 등록부 전수 · `harness-contract-selftest` · `scripts/tests/**` 만 바꾼 PR 에서 agent-bridge 잡 실행 관측 1 | `agent-bridge.yml:3-34` 경로 필터 · `colab-v2-work:73` 「못 돌았음≠통과」→ 포인터 | intent B4 · L6 ⓐ 3라운드 · recut 2-1 |
| 2-1e | B5: `ci-required` 인라인 판정(ci.yml:848-872)을 `scripts/harness/ci_required.py`로 · intent-ref `skipped` 허용식 = `event_name != 'pull_request' \|\| base_ref == 'product'`(job `if` 와 거울상) · harness-contract 가 두 식 일치 대조 | CI + harness-contract · FC | `scripts/tests/test_ci_required.py:39` 확장(develop PR + skipped → 1 · push → 0 · product → 0) | 없음 | intent B5 2라운드 |
| 2-1f | B6 + G1 + G6 + 2-2 VERDICT: 새 workflow `.github/workflows/pr-contract.yml`(`pull_request` opened · edited · synchronize · reopened · `pr_contract.py --mode draft`) · placeholder = 줄/칸 전체 `<…>` 만(`pr_contract.py:47-48`) · Head-SHA 불일치 = `::warning::` 0 · 신설 검사 ⑴ `Plan-Ref` 파일 head 존재 ⑵ 그 spec 의 「출처 intent」 == 커밋 `Intent-Ref` 집합 ⑶ `Scope:` 행 glob ⊇ `git diff --name-only base..head` ⑷ `Advisor-1-Ref:` · `VERDICT: ACCEPT\|ACCEPT-WITH-CHANGES` + `Advisor-2-Ref: runtime:artifacts/advisor-<n>.md sha256=<…>` 행 필수(CI = 형식 · 로컬 실행 = 파일 sha256 실체 대조) · spec V-id ⊆ PR 「검증」 절 | CI workflow(required 지정은 T1 후속 판정 · §6 Q9) + 로컬 CLI · FC(구조) · FO(Head-SHA) | `scripts/tests/test_pr_contract.py:101` 확장(인라인 `<task_id>` 통과 · 템플릿 원문 실패 · Plan-Ref 부재 · intent 불일치 · scope 밖 파일 · VERDICT 행 부재 · sha256 불일치) · `harness-contract-selftest` | `verification-before-completion:51-61` → 1줄 · `colab-v2-work:54-66` ② 생략 금지 → 「② 는 pr_contract 가 VERDICT 행으로 판정」 · 「advisor ② 판정문 미보존」 관행 | intent B6 2라운드 · recut 2-2 · direction 격차 3 · 6 |
| 2-1g | B7: probe 파일명 = slug 40자 + `-` + sha256 12자(`live_audit.sh:29,35`) · 선언 URL(중복 제거) 수 ≠ 페이지 수 → red(`frontend-visual.sh:108-113`) · probe 전체 배열 + `counts` 대조(`live_probe.js:56` 80 절단 제거) · 시작 시 이전 probe/png/index 삭제(`:75`) · `@layer`/`@media` 재귀(`live_probe.js:39`) | 게이트 `frontend-visual` · FC | `gates/tools/frontend-visual-selftest.sh` 사례 4(앞 60자 동일 URL 2 → probe 2 · 수 불일치 → 1 · 81번째 위반 → 1 · 잔존 probe 미계수) · agent-browser 1회 `activeRules > 0` | 없음 | intent B7 2라운드 |
| 2-1h | B8: `_pg.sh:193` trap 을 `pg_on_cleanup` 등록 목록 + 멱등 trap 1개로 · `service-tests.sh:82` · `operator-notifications.sh:7` 치환 · `operator-notifications.sh:8-14` 5곳 `exit 78` 에 `_readiness.sh` 표식 | 게이트 스크립트 · FC | `gates/tools/service-tests-selftest.sh`(docker stub · 두 정리 실행) · grep 「표식 없는 exit 78」 0건을 `operator-notifications-selftest.sh` 에 · 로컬 1회 `/tmp/service-tests-*` 수 불변 | 없음 | intent B8 2라운드 |
| 2-3 | `_lock.sh:80-112` `flock -w` 상한 초과 = 78 유지 + `gates/run.sh:835-837` `-j` > `harness.yaml gates.max_jobs`(신설) = 78 | 게이트 스크립트 · FC | `test_gates_run_args.py`(`all -j 9` → 78) · `gate-host-mutex-selftest.sh:97` | `lane-worker.md:52` 대기 금지 · `:55` 전수 두 벌 · `colab-rules §3-1` `-j 2` 수치 → 이유 1줄(지목만 · 편집은 2-11) | recut 2-3 |
| 2-4 | L1 + N1 + N8 + T8: task.json 종료 기록 2단(「인계됨」 `handed_off` 는 S-red S-4 가 기록 · 2-4 는 「닫힘」= stop 통과 또는 메인 `lifecycle close` + `handed_off.mode = blocked`) · `lifecycle prune`(dry-run 기본 · 「닫힘」 + 14일 · 체크아웃 부재 = 닫힘 상당 기록 · `--apply` 는 Ted 지시) · `handoff --mode blocked`(`--summary` 필수 · scope 안 미커밋 허용 · dirty 목록 + hash 기록 · gate 불요 · red-locked 마커 해제) · `stop()` 반송 메시지에 출구 명령 · 닫힌 task 재사용 거절 · Stop payload `agent_id` 정보 필드 | lifecycle CLI + H6/H7 hook · FC · CLI 공통 | `test_task_runtime.py` ① 닫힘 기록 ② stop 실패 시 기록 없음 ③ prune dry-run 목록 · 파일 무삭제 ④ `--apply` 후 부재 ⑤ blocked 3건(요약 없음 거절 · scope 밖 dirty 거절 · scope 안 dirty 기록) ⑥ 닫힌 task handoff 거절 · `agent-bridge` | `lane-worker.md:5-8` 정지 목록 · `:42` · `researcher.md:75` 「질문 3개 분할」 · `lifecycle-evidence.md:35-37`(지목만 · 편집은 2-11) | intent L1 3라운드 · T8 · recut 2-4 · Q5 ⓐ |
| 2-5 | L2 + N2 + G2: 명시 `begin --role researcher` 는 같은 checkout 에 「닫힘」 없는 researcher task 의 baseline 대비 변경이 있으면 거절(출구 = `lifecycle close <옛 task>`) · SubagentStart 자동 begin 은 가장 오래된 열린 task baseline 승계 · 옛 스키마 task 제외 · lane-worker `begin` 은 `--scope`(`**` 명시) · `--spec <경로>` · `--expect-head <sha>` 필수 · `--spec` 검사 = 파일 존재 + 「출처 intent」 승인 표기(`scripts/harness/intent_ref.py` 판별) + 「우려 항목」 절에 `〈판정 대기〉` 0 + 「정책 대조」 절 존재 · sha256 기록 · `--shared-checkout --reason` | lifecycle CLI · FC(거절) · CLI 공통 | `test_task_runtime.py` ① 편집 → 새 begin 거절 ② `--agent-id` 변형 거절 ③ 변경 없는 artifact 추가 begin 통과 ④ 출구 명령 포함 ⑤ 옛 스키마 통과 ⑥ `runtime:artifacts` 선행 쓰기 통과 ⑦ `--scope`/`--spec`/`--expect-head` 부재 거절 ⑧ 미승인 spec · 판정 대기 spec 거절 · `agent-bridge` · probe ①–⑤ 격리 clone 재실행 | `lane-worker.md:27` 병합 문장 → 「`--expect-head` 가 대조한다」 · `:38` scope · 「승인 없는 커밋 금지」(Q3 ⓐ) · `lifecycle-evidence.md:16-17` · `:36` 「재설정 금지」 → 장치 위치(지목만 · 편집은 2-11) | intent L2 3라운드 · recut 2-5 · direction 격차 3 |
| 2-6 | N3 + `at`: handoff JSON 을 CLI 가 채움 — `checkout` · `branch`(detached = SHA) · `report` · `counts` · `spec_sha` · `exempt` · `at`(ISO UTC) · `stop()` 이 checkout/branch/spec_sha 대조 · 옛 형식 = schema 키로 거절 | lifecycle CLI + H6/H7 · FC | `test_task_runtime.py`(필드 채움 · 대조 실패 거절 · 옛 형식 거절) · `agent-bridge` | `lane-worker.md:28` `WORKTREE=/BRANCH=` · `:72-73` ≤15행 · gate-summary 경로 · `researcher.md:87` | recut 2-6 · direction §3 |
| 2-7 | 변경 경로 → 필수 게이트 대조: handoff 가 baseline 대비 변경 파일 × ci.yml paths-filter(`gates/tools/ci-filter-check.py` 파서 재사용) → 등록부 게이트 ∩ 선언 게이트 · 첫 회차 경고 모드(`COLAB_HANDOFF.missing` 필드 · 차단은 다음 PR) · `--exempt-gate --reason` 건수 노출 · 등록부 부재 = 78 | lifecycle CLI · FO(경고 · Q4 ⓐ) → 후속 FC | `test_task_runtime.py`(missing 계산 · exempt 건수 · 등록부 부재 78) · `agent-bridge` | `AGENTS.md:44-46` 「변경에 맞춰」 → 포인터 · L6 「agent-bridge 선언」 문장 | recut 2-7 · Q4 ⓐ |
| 2-8 | A2 ⓐ + P7 + 역할 분기(`test-file-guard.sh` · S-red S-3 red-locked 블록 뒤 · `:42` `COLAB_FIX_LANE` 조기 exit **앞** — payload 읽기(:45)를 :42 앞으로 옮기고 비-lane 세션은 그 뒤 즉시 exit 0): ⓐ `agent_type=lane-worker` ∧ (payload cwd toplevel == `$CLAUDE_PROJECT_DIR` toplevel ∨ 브랜치 ∈ develop/product/main) → exit 2(열린 task `shared_checkout` 이면 통과) ⓑ `agent_type=researcher` ∧ WATCH · `runtime:artifacts` 밖 → exit 2 ⓒ lane + 열린(`handed_off` · 「닫힘」 없는) scoped task + 보호 경로 4종(`:81-87`) scope 밖 → exit 2 ⓓ 열린 task 에 `fix`(S-red 기록) 있으면 보호 경로 4종 전부 exit 2(scope 안이라도) — ⓓ 는 `:42` env 검사와 OR 로 한 분기(deny 문구 · audit rule 명 `fix-lane-test-edit` 하나) · task.json `fix` 가 정본 · env 는 폴백 · `COLAB_ALLOW_TEST_EDIT` 는 fix task 에 무효 · S-red 창(PR 2 전)은 S-3 red-locked 경로 차단(기록 red 경로만) + `:42` env 검사(보호 4종) + handoff blob 대조(S-2)로만 막힌다 · `agent_type` 부재 = 통과 · `scripts/agent-bridge.py` `--worker` 에 `COLAB_AGENT_TYPE` 주입(:278 controls 목록) | PreToolUse Edit\|Write hook 본문(정의 무변경) · FC · Codex = bridge env | `test_harness_lifecycle_contract.py` fixture 매트릭스(lane+루트 → 2 · lane+worktree → 0 · researcher+develop → 0 · 필드 없음 → 0 · scope 밖 보호 경로 → 2 · fix task 보호 경로 → 2(env 없이) · env 만 → 2(문구 동일) · 종료 기록 있는 task 만 → 0 · 기존 env 시험 green · S-red ⑩ 무변경 green) · `test_agent_bridge.py` env 주입 · `agent-bridge` · 실 Claude lane 1회 편집 시점 차단 관측 | `lane-worker.md:19-21` · `:31` · `researcher.md:27-28` · `:30` 「Edit 비활성」 · `dual-agent.md:58-62` Codex/Claude 분기 → 장치 위치 · `design-review/SKILL.md:101,107` A2 ⓒ 문장 → ⓐ 동작 | intent A2 ⓐ · recut 2-8 · direction 격차 2 |
| 2-9 | `git_guard_parse.py` 규칙 추가 + P2 + ⑴ 전원: ⑴ 보호 브랜치 push 를 `agent_id` 무관 전원 차단(`rule_push` :448 · Q-D ⓐ · 메인 세션 ff push 도 T1 이후 422) ⑺ `agent_id` 있는 `gh pr create/edit/comment/review/close/ready` · `gh issue *` · `gh api` POST/PATCH/DELETE · `gh release` · `--edit-last` 차단(`rule_gh` :423 · Q1 ⓐ) ⑻ subagent `push --tags` ⑼ role ∈ {lane-worker, researcher, measurement-lane} `git push` 전면(`COLAB_AGENT_TYPE`/`agent_type` 결합) ⑽ subagent `lifecycle close\|prune --apply\|stop` ⑾ `git commit` 메시지 마지막 문단 `Intent-Ref` 트레일러 검사(파서 폴백 FO · 대상 경로 = `intent-ref` 게이트와 동일) · P2 `audit.jsonl`(git-common-dir `colab-harness/audit.jsonl` · ts · hook · tool · agent_id · cwd · verdict allow/deny/fallback · rule · deny 원문 · allow sha256 · 쓰기 실패 = 무시)을 git-guard · test-file-guard · decision-number-guard 가 append · 게시 = T16(Ted): ⑺ 은 subagent 만 · 3-1 은 전 세션 → hook 상 PR 2~PR 3 사이 메인 세션은 게시 가능 · PR 3 부터 deny · 단 T11 PAT(`pull_requests:read`) 뒤 에이전트 셸은 `gh pr create` 자체가 403 → 실제 게시는 T11 이후 전부 Ted | PreToolUse Bash hook 본문 · FC(⑴⑺⑻⑼⑽) · FO(⑾ · audit) · Codex = bridge `guard-command` 동일 스크립트 | `scripts/tests/test_git_guard.py` rc 표(각 규칙 차단 · 허용형 0 · 메인 `push origin feature` 0 · 메인 `push origin develop` 2) · audit 줄 append 단언 · `test_harness_lifecycle_contract.py` 3 hook audit · `agent-bridge` | `lane-worker.md:29` · `researcher.md` 게시 문장 · `issue-before` 게시 절 → 「게시 단계는 메인 세션 · T16」 1줄 · `README.md:73` 「⑴ 은 agent_id 있는 호출만」 → 전원 | recut 2-9 · direction 격차 4 · Q-D ⓐ · Q1 ⓐ · 6라운드 트레일러 결함 |
| 2-10 | L4 · L5 · L6 · L7: `design-review/SKILL.md:63,65` → `runtime:artifacts` 쓰기 + 「모든 레인 인계 뒤 반입 · 커밋」 · `researcher.md:27` 범위 갱신 · L5 glob 접두 검사(`lifecycle_contract.py:81-92` · 「첫 와일드카드 앞 dirname 존재」 · 출구 2개) + 문서 순서 「scoped task 열린 동안 병합 금지」 · L6 `check_gate_parallelism`(`check.py:25`) fixture 시험 4종 + 역할 문서 1줄 · L7 재현 3회 기록 + `colab-v2-work` 「`VERDICT:` 텍스트 현행 관행」 1줄 | L5 = lifecycle CLI FC · L6 = 시험 · L4/L7 = 「산문 · 맥락만 + 관측 1회」 | `test_task_runtime.py`(없는 접두 거절 · 기존 디렉터리 아래 새 파일 통과 · begin 뒤 병합 파일 범위 밖 고정) · `test_harness_config.py` parallelism 4종 · researcher 2건 `runtime:artifacts` H6 통과 관측 1회 | `researcher.md:69-83` H6 절차 · `lane-worker.md:68-69` H7 서술 → `lifecycle-evidence.md` 포인터 · `:51` `COLAB_GATE_REPORT_DIR` | intent L4–L7 3라운드 · recut 2-10 · T10 입력 |
| 2-11 | 역할 · 정본 산문 재작성(장치 없음 · 「산문 · 맥락만」) — 역할 파일 산문 편집의 소유는 2-11 하나 · 2-3/2-4/2-5 는 지울 문장을 지목만 하고 편집은 2-11 커밋(P 묶음)에서 1회: AGENTS.md:17-19 에 조기 종료 4패턴 정본 3행(Q2 · 120행 상한 `harness.yaml:98`) · `lane-worker.md:13-14` · `:38↔:52,:70`(Q3 ⓐ 한 원칙) · `:72` ≤15행 삭제 · `researcher.md:15-16` · `:75` 두 건 · `:87` · `design-review:69` 모델 문장 · §2-2 항목 10 + 메인용 1행 · L3 「(자동)」 3곳(`executing-plans:17` · `writing-plans:166` · `VENDORED:87`) → 「Workflow `agent()` 스폰은 isolation 명시」 · `lane-worker.md:27` 이유 문장(C9 ①) · `lifecycle-evidence.md` measurement-lane 절(C8 ③) · `colab-rules §2-3` 레인 시작 절차는 PR 3 | 산문 · 맥락만 | `harness-contract`(adapter · 120행) · `python3 scripts/agent-bridge.py check` · grep 「(자동)」 0건 | 위 표 「지울 문장」 전부 · `lane-worker.md:10-11` · `researcher.md:12-13` 은 PR 4(프로브 뒤) | recut 2-11 · cross.md 역할 11건 중 8 · intent L3 · 4라운드 반입 5건 |
| 2-12 | 착수 전: phase 2 spec 상세(V-id · 시험 목록 · 레인 지시 · `begin --scope`/`--spec` 인자) · advisor ① 대조 · L8 스모크 결과 반영 | 「산문 · 맥락만」(메인 세션 절차) | spec gate ① 판정문 `runtime:artifacts/advisor-1.md` | — | intent 구현 형태 · 4라운드 「PR 2 반입 5건」 |
- PR 2 병합 조건: agent-bridge · harness-contract(-selftest) · intent-ref · exec-bit · adr-records green(lane `task` 1회) · `pr-contract.yml` green(PR 2 본문 자체가 VERDICT 행 · Advisor-Ref 포함) · `harness-eval` MATCH green(PR 2 병합 직전 head 회차 · T12 · 그 뒤 집합 파일 push 0) · 2-8 fixture 매트릭스 · Codex `--worker` env 실측 · 2-9 rc 표 · 2-4 시험 · `begin --scope` 없는 호출 거절 관측 1 · B1 Update branch 뒤 green 관측 1 · 게시 T16 · Ted 병합(T13). 병합 뒤: 메인 pull → 첫 Bash 통과 확인 · researcher 스모크 · T8 prune dry-run 건수 기록.

### Ted 행동(T · 명령/UI 단계 · 증거 자리 = intent 「판정 기록」 절 줄 추가)
| ID | 행동 | 명령 / UI | 증거 |
|---|---|---|---|
| T1 | develop ruleset(PR 1 병합 뒤 · PR 2 open 전): 대상 `refs/heads/develop` · `pull_request`(승인 0) · `required_status_checks`(strict true · `ci-required`) · `bypass_actors: []` · enforcement active | GitHub → Settings → Rules → New branch ruleset(또는 `gh api -X POST repos/CognileapAI/colab-v2/rulesets --input <json>`) | `gh api repos/CognileapAI/colab-v2/rulesets` · `gh api repos/CognileapAI/colab-v2/branches/develop/protection` 출력 요지 1줄 → intent · JSON 원문 → PR 3 3-5 |
| T11 | 에이전트 전용 fine-grained PAT(`contents:write` · `workflows:write` · `pull_requests:read`) 발급 · 에이전트 세션 셸에만 `GH_TOKEN` 주입(`gh auth setup-git`) · Ted 터미널은 개인 토큰 유지 | GitHub → Settings → Developer settings → Fine-grained tokens · 에이전트 셸 프로파일 | 두 셸의 `gh auth status`(토큰 값 제외) · 에이전트 토큰 `gh pr merge <n>` → 403 실측 1줄 |
| T12 | eval 실측 승인 · 실행(회차 ≈32 USD · E0 · S-red · PR 2 head · PR 3 head · PR 4 head = E1 · 면제 선택지 없음) — 레인이 중첩 `claude -p` 를 못 돌리면 Ted 가 실행 | 해당 worktree 루트 일반 터미널 `COLAB_EVAL_TIMEOUT=93 COLAB_EVAL_BUDGET=2.01 bash eval/harness/run.sh` → `results/<run>/` 커밋(트레일러 포함) 또는 레인 재개 | `results/<회차>/summary.md` 경로 · `config-hash.json.hash` · USD 합 |
| T3 | 동시 작업 clone 30·32·33 은 작업 시작 전 `git pull --ff-only origin develop`(열린 task 없는 시점) | 각 clone 에서 명령 1줄 | 각 clone HEAD SHA |
| T5 | Codex `/hooks` 재신뢰 = T3 갱신 직후 1회(Claude 는 불요 · 정의 무변경) | Codex CLI `/hooks` review | 날짜 · PC · 신뢰 정의 목록(dual-agent.md C8 자리) |
| T8 | 첫 `lifecycle prune --apply`(PR 2 병합 뒤 · dry-run 건수 확인 뒤) | `python3 scripts/agent-bridge.py lifecycle prune --apply` | 삭제 건수 · 남은 task 수 · `.git/colab-harness` 용량 |
| T13 | 각 PR 병합(E0 · S-red · PR 2 · 3 · 4 · S-auth · S-dep) | GitHub UI Merge(ruleset 이 ci-required 를 요구) | 병합 SHA |
| T14 | PR 3 permissions 반영: 새 Claude 세션 · bypass 세션(Workflow 레인 · `--dangerously-skip-permissions`)에서 `gh pr merge 1` · `cat ~/.ssh/id_ed25519` · 중간 와일드카드 `Bash(git push * develop*)` 시도 결과 | 세션 재시작 뒤 명령 3개 | deny 발동 O/X 표 → PR 3 병합 조건 · 와일드카드 통과 시만 3-1 에 추가 |
| T15 | Q6 effort 실측 스폰 승인(레인 2과제 × 2레벨 + researcher 2 × 2) | 메인 세션 스폰 | 보고서 경로 |
| T16 | PR 게시(E0 · S-red · PR 2~4 · S) — 메인 세션이 저장소 밖 초안(`~/.claude/pr-bodies/` · `pr_contract.py --mode draft` 로컬 통과) 제공 · Ted 가 `gh pr create` | Ted 터미널 `gh pr create --base develop --body-file <초안>` | PR 번호 → intent 「확인」 |
| T17 | 실패 4건 분류 판정 — H15·H16 expect 갈래(`::error:: … exit 1` 수용) · H14·H18 재실측 red 시 task.md 규약 1줄 · 측정 도구 변경 별건 PR go/no-go | 대화 판정 | intent 「판정 기록」 |
| T18 | S-red 병합 뒤 fix 레인 1회 실측 지시(V-S10 · 대상 red 과제 지정) | 메인 세션에 과제 지정 | intent 「확인」(hook exit 2 관측) |

### effort 실측(Q6 · PR 2 병합 뒤 · 보고서 `dev-package/reports/harness/<날짜>-opus55-effort/`)
| ID | 내용 | 장치 | 채택 규칙 | 출처 |
|---|---|---|---|---|
| Q6-1 | `.claude/agents/lane-worker.md:5` high vs medium — 최근 병합 lane 과제 2건 · 같은 base 신선 워크트리 · 레벨당 1회 · 턴 · 출력 토큰 · 벽시계 · 3계수 · maxTurns 도달 | 측정(장치 없음) | 3계수 동일 → `medium` · 값 반영 = PR 4 4-5 | cross.md §3 · Q6 |
| Q6-2 | `.claude/agents/researcher.md:5` medium vs low — 정답이 저장소에 있는 조사 2건 · `[미확인]` 건수 · `파일:행` 정확률 | 측정 | 정확률 동일 → `low` | cross.md §3 |
| Q6-3 | 조건부 3건: 「주장마다 감사」 절차 삭제(`lane-worker.md:10-11` · `researcher.md:12-13`) · writing-plans 「결정 코드만」(`:131-139`) · `pasted_content` 태그(`colab-v2-work:38`) 각 레인 1~2건 | 측정 | 오류 · 과잉 조심 증가 없으면 PR 4 4-6 적용 | cross.md §3 |
| Q6-4 | `.claude/settings.json:2` `effortLevel` — 레인 결과 뒤 메인 작업 1건 medium 반복 | 측정 | 레인 결과 우선 · 반영 PR 4 | cross.md §3 |

### PR 3 — C · P-rules · permissions · G3 P8 P6 · ruleset 스냅샷 · X 측정(lane 1 + measurement-lane 1)
| ID | 내용 | 강제 장치 | 시험 · 게이트 | 지우는 산문 | 출처 |
|---|---|---|---|---|---|
| 3-1 | `.claude/settings.json` `permissions`(현재 키 없음 · 102행) deny: `Bash(gh pr merge)` · `Bash(gh pr merge *)` · `Bash(gh pr create *)`(Q1 ⓐ) · `git push origin develop` · `develop *` · `product*`(T1 뒤) · `Read/Edit(~/.config/colab-platform/**)` · `Read(~/.ssh/**)` · `Read(~/.aws/**)` · `Read(//etc/colab/**)` · 종전 ask 후보 7종(`*infra/dev/ship.sh*` · `*reset_dev_environment.py*` · `*dev-reseed/reseed.sh*` · `*lifecycle prune --apply*` · `git push * --tags*` · `Edit(.claude/settings.json)` · `*purge_datasets.py*--yes-delete*`)도 deny(Q-C ⓐ · 사람 인가 = S-auth TTY 토큰) · force 3종(`git push --force*` · `-f *` · `--force-with-lease *`)은 deny 목록에서 제외 — 브랜치 인지 차단은 git-guard `rule_push`(PR 1 A1) + T1 ruleset 담당 · T14 에서 중간 와일드카드 `Bash(git push * develop*)` 가 실측 통과하면 그 형태만 추가 | Claude Code permissions · FC · Claude 전용(Codex = git-guard + PAT + S-auth) | 3-2 fixture · T14 실측 표 | `colab-v2-work:129` · `executing-plans:35` · `design-review:102` · `dev-reseed:140-143` → 「파일 주입은 deny」 · `s3-upload.md:23` → 「Read · cat · 리다이렉션 deny · grep -r · 서브프로세스 미대상」 · `deploy.md:40-45` → 포인터 | recut 3-1 · Q1 · Q-C · gate-1 차단급 4 |
| 3-2 | harness-contract 확장(`config.py:55` · `check.py` · `harness.yaml`): `permissions.required_deny` ⊆ live settings · `publication.*.enforced_by`(harness.yaml:116-130) 비면 red · settings `env` 에 `COLAB_*` 0건 · N9 `hygiene.forbidden_phrases`(roots 한정 · 취소선/「종전」 제외 · 패턴 = `(자동)` · `origin/main` · `main 병합` · Playwright 반복) · N-7 spec/round 구조 lint(변경 파일 한정 · 면제 건수) · N-8 의존 denylist(`package.json` + `requirements*` · playwright/puppeteer) · `eval.hash_exclude` 명시 목록 존재 노출(§6 Q1 결과에 따라) | 게이트 `harness-contract` · FC · 도구 무관 | `test_harness_config.py` fixture(deny 1건 제거 → red · enforced_by 공백 → red · 금지 구문 1건 → red · 오탐 0 = 패턴 좁힘) · `harness-contract-selftest` | `colab-rules:84-89` §3-2 · 「예정」 3곳 · C1·C2·C8·C9·C10 「grep 0건」 완료기준 → 상시 gate · `to-spec:23` · `writing-plans:16-17,131-139` · `design-review:18,90,92` Playwright 반복 | recut 3-2 |
| 3-3 | C6 + C7 + C1 + G3: adapter 17개 `disable-model-invocation` 승계 · `agent-bridge.py:98-110` 루프에 역방향 존재 + 플래그 동일 + description 동일성 2조건 · `harness.yaml` 역할 기대값(model · effort · maxTurns · tools · disallowedTools · isolation) ↔ `.claude/agents/*.md` frontmatter(`check_contract` `config.py:125`) ↔ `.codex/agents/*.toml`(`test_agent_bridge.py:35-59` 는 harness.yaml 을 읽어 단언) · `measurement-lane.md:13,18` · `measurement-lane.toml:12` 전제 문장 교체(ADR-0005) | `agent-bridge check` + harness-contract · FC | 부정 fixture(플래그 불일치 · 본문 없는 adapter · frontmatter 값 1개 변경 → red) · `gates/run.sh agent-bridge` · 새 세션 스킬 목록에서 4종 제외 관측 1회 | `colab-rules:25-29` 모델 배정 · 529 · `:62` 「강제 예정」 · `VENDORED:5` 「8종 명시 호출」 · `:98` 경로 · 「뮤텍스가 없다」 문장 | intent C1 · C6 · C7 4라운드 · recut 3-3 · direction 격차 5 |
| 3-4 | C11 + N11: `bootstrap-diet.sh` mtime 후보 출력 삭제 · `HEAD..origin/develop` 뒤처짐 N 커밋 안내(SessionStart stdout) · 종속 시험 2곳 · `agent-bridge.py` Codex 문장 | SessionStart hook 본문 · FO(안내) | `test_task_runtime.py` bootstrap 사례(`mtime` 0건 · 뒤처짐 문구) · `agent-bridge` | `AGENTS.md:23` · `colab-v2-work:18` · T3 pull 안내 산문 | intent C11 · recut 3-4 |
| 3-5 | N10 + C5: `docs/development/github-ruleset.json`(현 `refs/heads/main` :6) 을 T1 실제 상태(ruleset 형식)로 교체 · CI `repo-hygiene` 단계: `gh api repos/…/rulesets` live ↔ JSON 대조(`vars.COLAB_RULESET_APPLIED` 스위치 · 토큰 읽기 불가 = 78 · 78 도 실패 집계 주석) · `release-evidence.md:43,45` 정정 | CI 잡 + 게이트 스크립트 · FC | `scripts/tests/test_harness_release_evidence.py` 대조 함수 fixture(불일치 → 1 · 응답 없음 → 78) · N10 `GITHUB_TOKEN` 읽기 권한 실측 | `product.md:159` → ruleset 포인터 · `harness-transition-handoff.md:191` 「원격 미적용」 | intent C5 4라운드 · recut 3-5 |
| 3-6 | harness-eval 증거 JSON 에 `provider/model` 기록(러너 `modelUsage` 실측값 · `config-hash.json` `claude_version` 옆) · `verify_evidence.py` 가 역할 선언 모델과 대조(S-A13) | 게이트 + CI · FC | `test_harness_evidence.py`(모델 불일치 → 1) · `harness-eval-selftest.sh` | 없음(맥락 + 포인터) | recut 3-6 · AGENTS.md:47 |
| 3-7 | 규칙 문서 재작성 + C2 + C3 + C4 + C8 + C9 + C10 + P6 + S1: `AGENTS.md:40-41`(guard 명시 호출 → 「trust 없으면 guard 없음」) · `:48-49` · `colab-rules:4,27-29,62,65,120` · §1-2 legacy 한정(Q5 ⓐ) · `product.md:3,138` · `dual-agent.md` C8(개수 → 정본 참조 · Codex 재신뢰 3정의 · Claude 불요 기록 · `:121` effort 재측정 문장 · `:172-177`) · `README.md` 훅 표 삭제 + 정본 링크 + `:74` `origin/main` + COLAB_HOOKS 절(3-8 반영 · H6/H7 예외) · `gates/README.md` CI 표 = ci.yml 잡 집합(B4 뒤 · `:245` 행은 C3 몫) · C4 `harness-transition-handoff.md` 「이후 이력」 절 + `AGENTS.md:18` 포인터 + S1 전환 종료 조건 1줄 · C9 문장 5종 · C10 `migration-guard.sh` 머리말 · `worktree-setup.sh:262-268` P-E 문단 삭제 + `COLAB_TEST_ENV_FILE` · P6 `work-items.yaml` 항목 ↔ intent 링크 1조건 + 날짜 컷오프(`work_item_consistency.py`) · 「Read deny 는 Claude 만」 1행 | P6 = 게이트 `work-item-consistency` FC · 나머지 「산문 · 맥락만」(N9 가 이후 drift 를 잡음) | `harness-contract` · `agent-bridge check` · C10 `COLAB_TEST_ENV_FILE` 시험 1건 · P6 fixture(링크 없는 신규 항목 → red) | 위 열거 전부 · 판정 질문 6번째 「강제 장치 없는 must/never 인가」 · `README.md:105` | intent C2·C3·C4·C8·C9·C10 4라운드 · recut 3-7 · cross.md 상시 로드 PR 3 5건 · direction 격차 7 |
| 3-8 | P8: `COLAB_HOOKS=0` 조기 exit(`git-guard.sh:2` · `test-file-guard.sh:2` · `decision-number-guard.sh:2`) 삭제 — PR 3 마지막 단독 커밋 · 조건 = audit.jsonl ≥1주 계수 기록(PR 본문) · 계수 미달이면 3-8 만 제외하고 병합 · 3-8 은 소형 후속 PR(Ted 확인 · Q-F ⓐ) · `migration-guard.sh:2` 는 §6 Q7 · 복구 경로 = Edit 도구 · `git revert` | PreToolUse hook 본문 · FC 강화 | `test_harness_lifecycle_contract.py`(`COLAB_HOOKS=0` env 에서 `push --force origin develop` → 2) · `agent-bridge` | `README.md:79-114` 「전부 끄는 법」 → 남는 hook 만 · `git-guard.sh:25,86` 킬스위치 언급 | direction 격차 4 · Q-F ⓐ · gate-1 개선 3 |
| 3-9 | X 측정(measurement-lane · 보고서 `dev-package/reports/harness/<날짜>-metrics/`): ① 1차 CI 통과율(병합 PR 별 첫 push 의 `ci-required` 결과 · `gh api`) ② eval 추이(`results/*/summary.md` green/과제) + T6 표(세 PR 동안 역할별 spawn n · 한도 도달 수) | 측정(장치 없음) | 보고서 존재 · 계수 기준 명기 | 없음 | direction 격차 7 · intent T6 4라운드 |
- PR 3 병합 조건: `harness-contract`(-selftest) · `agent-bridge` · `work-item-consistency` · `intent-ref` · `adr-records` green · `pr-contract.yml` green · `harness-eval` MATCH green(PR 3 병합 직전 head 회차 · 그 뒤 집합 파일 push 0) · T14 표(bypass 세션 deny 적용 O/X · `gh pr merge` 거부 · `cat ~/.ssh/…` 거부 · 와일드카드) · 3-8 포함 시 그 시험(`COLAB_HOOKS=0` 거부) + audit.jsonl ≥1주 계수 기록 · heredoc 오탐 재확인(2-9 corpus) · N10 토큰 읽기 실측 · N9 오탐 0 · 3-2 fixture(deny 1건 제거 → red) · audit.jsonl 오탐 계수(deny 뒤 즉시 재시도 성공 비율) 기록 · 게시 T16 · Ted 병합.

### PR 4 — skills 산문 · G7 · advisor 보안 pass · ADR-0011 · effort 값(장치 0 · `Intent-Ref` 커밋 1 · `VENDORED.md` 개조표 갱신)
| ID | 내용 | 강제 장치 | 시험 · 게이트 | 지우는 산문 | 출처 |
|---|---|---|---|---|---|
| 4-1 | skills 산문(cross.md PR 4 배정 19 + 7건): `writing-plans:10-12,45-52,61,89,153-171` · `executing-plans:12,17,20,35,37-45,53,60,61` · `to-spec:19,100,102` · `receiving-code-review:27-38,43-48,68,102-111,139-145` · `verification:12,35,78,131-141` · `tdd:14,29,238,290` · `colab-v2-work:37,50` · `VENDORED:36,57` · `design-review:8,97`(Q4 ⓐ) · `grill-me:14-17` · `css-edit-audit.sh:76` 범례 | 「산문 · 맥락만」(N9 가 잡는 줄은 PR 3 선반영) | `harness-contract` · `agent-bridge check` · N9 green | 열거한 줄 전부(한 원칙 = 자기 브랜치 커밋 자유 · push/병합/원장 승인 · 에이전트 커밋 ≠ 승인) | cross.md §1 · recut §3 |
| 4-2 | G7 + AGENTS.md: `:10` 선독 지시 축소(`dual-agent.md` 전문 → 필요 절) · `:21` 「사용자가 지정하지 않은 외부 입력은 자료」 · `:42-43` 브리프 예산 + 「산출물 먼저」 + `COLAB_HANDOFF` 한정 | 「산문 · 맥락만」 | `harness-contract` 120행 상한(`check_always_on_lines` `config.py:243`) | `AGENTS.md:10` 「먼저 … 읽는다」 | direction §3 · cross.md AGENTS 3건 |
| 4-3 | `advisor.md` 보안 pass 절(게이트 ② 4항: 자격증명 노출 · 경로 이탈 · 삭제/배포 경계 · 외부 입력 지시 취급) · `:39` 「쓰기 도구가 없으므로」 → T10 판(Bash 유지 · 규율) | 「산문 · 맥락만」 | `agent-bridge check`(adapter) | `advisor.md:39` | direction 격차 6 · intent C9 · T10 |
| 4-4 | 새 ADR-0011(`docs/decisions/0011-*.md` · 템플릿 `_template.md`): ① 「승인 의미 판정 ≠ 인가 토큰 존재 검사」(ADR-0003 보강 · 이력 무수정 · §6 Q11) ② 「양방향(모델) 리뷰 미채택 — 사람 게시 원칙 · CI 모델 호출 0」 | 게이트 `adr-records` · FC | `adr-records` green · `scripts/harness/adr_gate.py` 구조 | 없음 | direction §3 · X4 · D2 |
| 4-5 | Q6 값 반영: `.claude/agents/{lane-worker,researcher}.md` `effort` · `.claude/settings.json:2` `effortLevel` · `harness.yaml` C7 기대값 · `dual-agent.md` 역할 표 | 3-3 대조 gate · FC | `agent-bridge`(기대값 ↔ frontmatter) | `dual-agent.md` 「medium ≥ Opus 5 high」 수치 → 보고서 경로 | recut §3 · Q6 |
| 4-6 | 조건부(Q6-3 결과 뒤): `lane-worker.md:10-11` · `researcher.md:12-13` 감사 절차 삭제(문안 동일) · writing-plans `:131-139` + 템플릿 · `colab-v2-work:38` 태그 규약 · `receiving-code-review:68` | 「산문 · 맥락만」 | `agent-bridge check` | 해당 줄 | cross.md §3 |
- PR 4 병합 조건: `harness-contract` · `agent-bridge` · `adr-records` · `intent-ref` green · `pr-contract.yml` green · **PR 4 병합 직전 head 회차(MATCH green · 회귀 0 · 실패 4건 해소분 기록 · 그 뒤 집합 파일 push 0)** · 게시 T16 · Ted 병합. 병합 뒤 메인 세션이 사용자 메모리 3파일 갱신(T7).

### E1 · X · M · S
| ID | 내용 | 강제 장치 | 시험 · 게이트 | 지우는 산문 | 출처 |
|---|---|---|---|---|---|
| E1 | PR 4 head 회차(PR 4 병합 조건에서 이미 실행)를 E0 회차와 대조한 표(과제별 판정 · USD · p50/p95) · 실패 4건 해소 판정(T17 분류 확정) — 별도 회차 없음 | E0-3 MATCH · FC(회차는 PR 4) | `harness-eval` 잡 · 대조표 커밋 | 없음 | direction §3 · Q-A ⓐ · gate-1 개선 4 |
| X | 3-9 의 2지표를 PR 3 병합 뒤 1회 더(전후 대조) · 나머지 4지표(재작업 횟수 · hook 오탐률 · 레인 처리시간 · 탐지→intent lead time)는 audit.jsonl · `at` 축적 뒤 | 측정 | 보고서 | 없음 | direction 격차 7 |
| M | `scripts/metrics/metrics.py`(해시 집합 · `harness` 필터 밖 위치 · 입력 = `gh api` PR/check-runs · `results/*/summary.md` · audit.jsonl · task.json `at`) · 출력 표 1개 · tier/cron 없음 · intent 후보로 PR 4 뒤 착수 | 스크립트(장치 아님) | `scripts/tests/test_harness_metrics.py` fixture · `agent-bridge` 목록 등록 | 없음 | direction §3 M |
| S-auth | `services/core-api/ops/purge_datasets.py`(`--yes-delete` :99) · `reset_dev_environment.py` · `infra/dev/ship.sh` 에 TTY + 1회용 토큰(`[ -t 0 ]`·`[ -t 1 ]` · nonce 를 터미널에만 출력 · 되받아 대조 · 선례 `deploy.md:69` reseed) + 실행 기록 `authorized_at` · sha256(token) · `tag-release.sh` 제외 · deploy.md 선독 | 스크립트 자체 · FC(비 TTY · 토큰 불일치 = 거부) · 도구 무관(Claude · Codex · `bash -c`) | `scripts/tests/test_ops_authorization.py`(stdin 비 TTY → 거부 · 토큰 불일치 → 거부 · 기록 필드) · `harness-contract-selftest` 등록 · `infra/dev/tests` | `deploy.md:40-45` 「명시 GO 없이 실행하지 않는다」 → 「TTY 토큰이 요구한다」 1줄 · `dev-reseed:120-123` 승인 절 → 포인터 | recut Q6 ⓐ · Q-E ⓐ · direction 격차 4 |
| S-dep | CI 잡 `dependency-audit`(`pip-audit` × requirements 5벌 · `npm audit --audit-level=high` frontend) · 생산자 등록 · path filter · high/critical = 1 · 면제 파일(CVE id + 만료일) · 등록부(`.agents/ci-producers.json`) 편집은 해시 집합 안 → 회차 1회 또는 `harness.yaml eval.hash_exclude`(§6 Q1) | CI 잡 + `required-gates` 대조 · FC | `test_harness_evidence.py:203` 등록부 전수 · 잡 green 1회 | 없음 | direction 격차 7 · P5 |
- S 병합 조건: 각 PR 단독 lane · 해당 게이트 green · `pr-contract.yml` green · 게시 T16 · Ted 병합. S-auth 는 배포 스크립트라 `.agents/rules/deploy.md` 선독 · dev 1회 실측(ship.sh 반입 1건 · purge dry-run 1건).

## 3. 파일 소유표(같은 파일은 순차 PR 만 · 동시 lane 0)
| 파일 | 소유 PR(순서) | 충돌 해소 |
|---|---|---|
| `eval/harness/run.sh` · `config_hash.py` · `config-paths.txt` · `results/**` · `tests/run-selftest.sh` · `gates/tools/harness-eval.sh` · `harness-eval-selftest.sh` · `gates/tools/ci-filter-check.py` · `scripts/tests/test_ci_eval_policy.py` · `test_harness_eval_gate.py` | E0 → S-red · PR 2 · 3 · 4(`results/**` 회차만) → 3-6(PR 3) → E1(대조표만) | E0 가 러너 · 게이트 소유 · PR 3 는 증거 필드만 |
| `.github/workflows/ci.yml` | E0(:116-128 필터 · :641-650 주석 이관 · :697-700 무변경) → PR 2(2-1b · 2-1d · 2-1e · C3 주석) → PR 3(3-5) → S-dep | 순차 · 각 PR 은 자기 잡/필터 블록만 |
| `.github/workflows/agent-bridge.yml` · 새 `pr-contract.yml` | PR 2 | — |
| `.agents/ci-producers.json` | PR 2(2-1b · 2-1d) → S-dep | 순차 · E0 diff 0 |
| `scripts/harness/hooks/lifecycle_contract.py` · `scripts/harness/task_state.py` · `scripts/tests/test_task_runtime.py` · `docs/development/lifecycle-evidence.md` | S-red(S-1 · S-2 · S-4 · S-5) → PR 2(2-1c · 2-4 · 2-5 · 2-6 · 2-7 · 2-10) | S-red 먼저 병합 · PR 2 lane 은 병합 뒤 develop 기준 |
| `scripts/harness/hooks/test-file-guard.sh` · `scripts/tests/test_harness_lifecycle_contract.py` | S-red(S-3) → PR 2(2-8 · P2) → PR 3(3-8) | 순차 · 2-8 은 S-3 블록 뒤에 분기 추가 |
| `git-guard.sh` · `git_guard_parse.py` · `decision-number-guard.sh` · `scripts/tests/test_git_guard.py` | PR 2(2-9 · P2) → PR 3(3-8 · C10 decision 머리말 잔여) | 순차 |
| `scripts/harness/hooks/migration-guard.sh` · `worktree-setup.sh`(:262-268) · `bootstrap-diet.sh` | PR 3(C10 · 3-4) | cross.md 의 worktree-setup PR 4 배정을 C10 소유(PR 3)로 통합 |
| `scripts/harness/hooks/css-edit-audit.sh:76` | PR 4 | — |
| `scripts/harness/verify_evidence.py` · `scripts/tests/test_harness_evidence.py` | PR 2(2-1a · 2-1b · 2-1c) → PR 3(3-6) → S-dep(등록부 시험) | 순차 · E0 diff 0 |
| `scripts/harness/pr_contract.py` · `scripts/tests/test_pr_contract.py` | PR 2(2-1f) | — |
| `scripts/harness/{check.py,config.py}` · `.agents/harness.yaml` · `scripts/tests/test_harness_config.py` | E0(E0-5 `check.py` 만 · `harness.yaml` 무변경) → PR 2(2-1d · 2-3 · L6) → PR 3(3-2 · 3-3) → PR 4(4-5 값) | 순차 |
| `gates/run.sh` · `gates/tools/_lock.sh` · `scripts/tests/test_gates_run_args.py` | E0(`run.sh:357` 등록 · `:510-513` 주석) → PR 2(2-1c · 2-3) | 순차 |
| `gates/tools/_pg.sh` · `service-tests.sh` · `operator-notifications.sh` · `frontend-visual.sh` · `frontend-visual-selftest.sh` · `.agents/skills/design-review/scripts/*` | PR 2(2-1g · 2-1h) | — |
| `gates/README.md` | E0(:47-48 · :62-71 · :293) → PR 3(:245 C3 · 3-7) | 순차 |
| `README.md` · `docs/development/github-ruleset.json` · `release-evidence.md` · `harness-transition-handoff.md` · `dev-package/prd/rounds/R-HARNESS-PR-CENTRIC.md` | S-red(`README.md` A3 blockquote 1구) → PR 3(3-5 · 3-7) | 순차 |
| `scripts/agent-bridge.py` · `scripts/tests/test_agent_bridge.py` | PR 2(2-8 env) → PR 3(3-3 · 3-4) | 순차 · E0 · S-red diff 0 |
| `.agents/roles/lane-worker.md` · `researcher.md` | S-red(`lane-worker.md:38,44`) → PR 2(2-11 · 2-4~2-10 산문 지목) → PR 4(4-6 조건부) | 순차 |
| `.agents/roles/measurement-lane.md` · `.codex/agents/*.toml` · `.claude/skills/*/SKILL.md`(17) · `.agents/skills/VENDORED.md:5,98` | PR 3(3-3) | `VENDORED.md:36,57,87` 은 PR 2(:87) · PR 4(:36,:57) |
| `.agents/roles/advisor.md` · `docs/decisions/0011-*.md` | PR 4 | ADR-0010 은 PR 2 |
| `AGENTS.md` | PR 2(:17-19 4패턴 · :44-46 포인터) → PR 3(:18 · :23 · :40-41 · :48-49) → PR 4(:10 · :21 · :42-43) | 순차 · 매 PR 120행 상한 gate |
| `.agents/skills/design-review/SKILL.md` | S-red(:101-102 · :107) → PR 2(:63,:65,:69 · §2-2) → PR 3(:18,:90,:92 N9) → PR 4(:8 · :97) | 순차 |
| `.agents/skills/colab-v2-work/SKILL.md` | PR 2(:48 · :54-66 · :73 · L5/L6/L7 1줄) → PR 3(:18 · :129) → PR 4(:37 · :38 · :50 · :33 · :93 · :116-122) | 순차 |
| `.agents/skills/{executing-plans,writing-plans}/SKILL.md` | PR 2(:17 · :166 「(자동)」 = L3) → PR 4(나머지) | intent L3(PR 2) 가 recut N9 「PR 3 당김」보다 우선 |
| `.agents/skills/{to-spec,receiving-code-review,verification-before-completion,test-driven-development,grill-me}/SKILL.md` | PR 4 | — |
| `.claude/settings.json` | PR 3(`permissions`) → PR 4(`effortLevel`) | hooks 블록 무변경(모든 PR) |
| `.claude/agents/*.md` | PR 4(effort) | PR 3 gate 가 값을 읽기만 |
| `.agents/rules/{colab-rules,product,deploy,s3-upload}.md` · `docs/development/dual-agent.md` | E0(`dual-agent.md:192` 1구) → S-red(`:59-60`) → PR 2(`dual-agent.md:58-62` · Q1 ⓐ 예외 · 설계 원칙 「장치와 산문 같은 PR」 우선(2-8)) → PR 3(3-1 · 3-7) → S-auth(`deploy.md` 1줄) | 순차 |
| `services/core-api/ops/{purge_datasets,reset_dev_environment}.py` · `infra/dev/ship.sh` | S-auth | 제품 코드 무변경 원칙의 예외 = ops/infra 스크립트(intent 영향 범위 밖 → §6 Q10) |
| `scripts/metrics/metrics.py` · `dev-package/reports/harness/<날짜>-{opus55-effort,metrics}/` | M · Q6 · 3-9 | 보고서는 생성물 · 커밋 대상 · 해시 집합 밖 |
| `dev-package/prd/specs/S-HARNESS-IMPROVEMENT-20260925.md` · 이 문서 · E0 spec · S-red spec | 메인(각 PR 착수 전 상세화) | lane 은 spec 을 쓰지 않음 |

## 4. 측정표
| playbook 지표 | 데이터 원천 | 계산 가능 시점 | 소유 |
|---|---|---|---|
| eval 추이(green/20 · USD · p95) | `eval/harness/results/*/summary.md` + `config-hash.json` | E0 뒤(회차 #5 부터) | E0 · E1 · 3-9 |
| 1차 CI 통과율(first-pass merge share) | `gh api repos/…/pulls?state=closed` × check-runs(첫 push 의 `ci-required`) | 지금(PR #130 이후 표본) | 3-9 · M |
| 재작업 횟수(PR 당 synchronize 수 · VERDICT ACCEPT-WITH-CHANGES 수) | PR timeline + `pr-contract.yml` VERDICT 행 | PR 2 병합 뒤 | M |
| hook 오탐률(deny 뒤 즉시 재시도 성공 / deny) | `audit.jsonl`(P2) | PR 2 병합 뒤 ≥1주 · PR 3 3-8 조건 | 3-9 · M |
| 레인 처리시간 · 턴 | task.json begin 시각 · handoff `at`(2-6) · SubagentStop 기록 | PR 2 병합 뒤 | M |
| 한도 도달률(T6 · #131 원한 결과 8) | 세 PR 동안 spawn 기록(역할별 n 공개) | PR 3 | 3-9 |
| effort A/B(Q6) | `<날짜>-opus55-effort/` | PR 2 병합 뒤 | Q6 |
| 탐지 → intent lead time | eval red · issue 일자 ↔ intent 메타 작성일 | M 뒤 | M |
| 의존성 CVE 건수 | `dependency-audit` 잡 출력 | S-dep 뒤 | S-dep |

## 5. 위험 · 롤백
- E0: 회차 비용 ≈32 USD(`results/20260912-211809` 31.55) · 해시 집합 = Q-A ⓐ 확정(재개봉 없음) · MATCH 뒤 회차가 필요한 PR = E0 · S-red · PR 2 · PR 3 · PR 4(5회 ≈160 USD) + S-dep(회차 1회 또는 `harness.yaml eval.hash_exclude: [.agents/ci-producers.json]` · §6 Q1) · M 은 `scripts/metrics/metrics.py` 로 집합 밖 · Ted 추정 ≈64 USD 초과 → §6 Q1 · 회차 무효화 = §0 규칙(회차 뒤 집합 파일 push 금지 · Update branch 는 집합 무변경이면 허용 · 건드리면 재실측) · 실패 4건은 직전 기준(green 16) 안이라 회귀 규칙으로 허용하되 전수 · 준비 0 · 과제 수 대조로 green-by-skip 차단 · 롤백 = 커밋 ③(게이트 verify) revert 1커밋(러너 해시 줄 · 필터 · 경고는 무해).
- S-red: begin/handoff 거절이 늘어 레인이 78/1 로 멈출 수 있음 → 거절 메시지에 출구(`--red` 재선언 · red 실행 명령 · blob 복원) · 버려진 fix task 는 worktree 제거가 출구(2-4 `blocked` 전) · 롤백 = 커밋 ③(hook) · ②(대조) revert(fixture 시험이 기존 동작 고정).
- PR 2: fail-closed 면 확대 — test-file-guard 가 매 Edit 마다 task 조회(성능 · 조회 실패 = 통과 FO 로 설계 · S-3 잠금 상태는 FC) · git-guard ⑴ 전원은 메인 Claude 세션의 develop ff push 도 막음(Q-D ⓐ 의도) · Ted 터미널은 hook 밖이며 T1 ruleset(PR 필수 · 우회자 0)이 막는다 · 출구 = PR · `pr-contract.yml` 이 게시된 PR 을 red 로 낼 수 있음(구조 실패만 · Head-SHA 는 경고) · B2 ⓓ 는 `changes` 잡 실패 시 서비스 시험 전부 skip → `required-gates` 가 「path filter producer did not succeed」 78(`verify_evidence.py:187`)로 잡음 · 메인 checkout 은 병합 · pull 뒤 새 hook 적용(`worktree-setup.sh:31-38` 전제) · 롤백 = 커밋 묶음(B/L/P) 단위 revert · 정지 시 복구 = Edit 도구(matcher 밖) 또는 `git revert`.
- PR 3: `permissions.deny` 가 메인 세션의 정상 작업(예: `git push --force-with-lease` 기능 브랜치)을 막을 위험 → 3-1 은 force 3종 제외 · 보호 브랜치 · 비가역 명령 한정 · 브랜치 인지 차단은 git-guard `rule_push` + T1 ruleset · T14 실측 표 없이는 병합 금지 · 3-8 은 마지막 단독 커밋(계수 미달 = 제외 · 후속 소형 PR) · 킬스위치 제거 뒤 hook 결함 = Bash 정지 → `bash -n` 시험 + parser fallback(PR 1) + Edit 복구 경로 유지 · 3-5 는 토큰 권한 부재 시 78 → `COLAB_RULESET_APPLIED` 스위치로 격리 · 롤백 = 단위 커밋 revert(3-1 · 3-8 은 각각 단독 커밋).
- PR 4: 장치 0 · effort 하향이 레인 품질을 낮추면 4-5 커밋 단독 revert · PR 4 head 회차 회귀(green 감소)면 병합 보류.
- S-auth: 무인 실행 경로(`infra/dev/install-cron.sh` 대상 스크립트에 ship.sh 포함 여부 미확인)가 TTY 요구로 깨질 수 있음 → S-auth spec 에서 cron 대상 grep 후 확정 · 롤백 = 스크립트 3개 revert. S-dep: 감사 소음 → 면제 파일(만료일 필수) · 잡은 자기 PR 에서만 required.
- 공통: 여러 PR 이 같은 파일(`lifecycle_contract.py` · `test-file-guard.sh` · `ci.yml` · `AGENTS.md`)을 순차 편집 → 각 lane 은 앞 PR 병합 뒤 develop 기준 `--expect-head` 로 시작 · 게이트 lane 은 호스트에 하나(postgres 슬롯 4 · `_lock.sh` host mutex).

## 6. 미해결(Ted 판정 · 프로브 필요 · 담당)
- **8라운드 판정(2026-09-26 · Ted 원문 「권고대로」): 아래 1–12 전부 권고안 = 결정.** 실측 항목(6 · 8 · 9 · 10 일부)은 실행 결과를 intent 「확인」 절에 적고, 10 의 S-auth 는 별도 intent 로 발행한다. E0 spec 4.1 정본에 `eval/harness/**`(results 제외)를 넣었다(#3 · #5).
1. E0 회차 비용 · 범위: 해시 집합은 Q-A ⓐ 확정(재질문 없음). 회차 = E0 · S-red · PR 2 · PR 3 · PR 4 각 1회(5회 ≈160 USD · Ted 「≈64 USD · PR 2 전 1회 + PR 4 뒤 1회」 초과) + S-dep 의 `.agents/ci-producers.json` 편집 = 회차 1회 또는 `harness.yaml eval.hash_exclude: [.agents/ci-producers.json]`(명시 목록 · harness-contract 가 목록 존재를 노출) — 5~6회 승인인지 · hash_exclude 허용인지 Ted.
2. S-red 순서 대안(문서 간 충돌 · 미해결): gate-1 master 검증자 = S-red 를 E0 보다 먼저 병합(EXEMPT 모드 창 · 회차 불요 · E0 head 회차가 S-red 를 덮음 · 1회 ≈32 USD 절감) vs 7라운드 확정 E0 → S-red(E0·S-red 검증자 확인 · S-red 회차 1회). 이 문서와 두 spec 은 E0 → S-red 기준 · 권고 = 확정 순서 유지(S-red 가 편집 시점 차단을 E0 게이트 아래에서 검증) — 변경 여부 Ted.
3. `eval/harness/**`(results 제외)의 해시 포함 여부(문서 간 충돌 · 미해결): master 검증자 ⓐ 포함(해시 = 필터 집합 · expect.sh/러너 수정으로 만든 green 도 재실측 강제) vs E0 spec 4.1 ⓑ 제외(측정 도구 · 자기 참조 · 필터에만) — 권고 ⓐ(엄격 · 정본 1집합) · 7라운드 목록에 없는 추가이므로 Ted 판정(E0 spec 우려 #5).
4. `AGENTS.md` · `CLAUDE.md` 를 해시 정본에 추가(모델 입력 정본 · Ted 목록 밖 · `.claude/rules` 는 이미 목록 안) — 수용?(E0 spec 우려 #4)
5. 회귀 규칙(일치 결과의 green 집합이 직전보다 줄면 1 · 실패 4건은 직전 기준 안이라 허용)이 「pass rate 로 gate」 원칙을 만족하는지 — 분류는 E0 lane · 판정 T17(E0 spec 우려 #2) · CI 해시 = 머지 커밋 트리 기준(base 가 집합을 바꾸면 재실측 · E0 spec 우려 #7) 수용?
6. `permissions.deny` 가 bypass 세션(Workflow 레인 · `--dangerously-skip-permissions`)에도 적용되는지 · 중간 와일드카드 `Bash(git push * develop*)` 통과 여부 — T14 실측(Ted) · 결과가 X 면 Q-C ⓐ 의 deny 이전은 유지하되 인가 경계는 S-auth 로만.
7. 3-8 킬스위치 제거 대상에 `migration-guard.sh:2` 포함 여부(direction 「3훅」) · audit 계수 미달 시 3-8 을 PR 3 에서 제외해 후속 소형 PR 로 두는 것 — PR 3 spec 상세 때 Ted.
8. SubagentStart/SubagentStop `agent_id` 일치 · A7 additionalContext 도달(L8 스모크 · PR 1 병합 뒤 메인 세션) — 결과가 2-5 자동 begin 승계 · 2-8 `agent_type` 의존을 정한다.
9. `pr-contract.yml` 을 T1 required check 에 넣을지(현재 T1 = `ci-required` 1개 · 넣으면 PR 본문 형식 오류가 병합을 막는다) · `gh api rulesets` 읽기에 `GITHUB_TOKEN` 권한이 충분한지(3-5) — PR 2 병합 뒤 Ted · PR 3 lane 실측.
10. 2-9 ⑼ 「role 결합」은 bridge `--worker` 가 서브에이전트 전용인지 확인 뒤(`agent-bridge.py` 실측 · PR 2 lane) · ⑾ 트레일러 검사의 대상 경로 판정을 hook 안에서 어떻게 얻는지(스테이징 목록 `git diff --cached --name-only`) · S-auth 가 손대는 `services/core-api/ops/*.py` · `infra/dev/ship.sh` 는 intent 「영향 범위 · 제품 코드 0」 밖 — 범위 확장 줄을 intent 판정 기록에 추가할지 별도 intent 로 뺄지 Ted · recut §3 「F7·F8·F9 조정문」은 cross.md 에 항목이 없어 미반영.
11. ADR 2줄을 ADR-0003 편집이 아니라 새 ADR-0011 로 두는 것(ADR 이력 무수정 제약) 수용 여부.
12. S-red 잠금 = checkout 결속(`file_path` 기준 · 신원·env·cwd 무관 · 부모 세션의 lane worktree 편집도 차단) · 버려진 fix task 의 출구를 PR 2 `handoff --mode blocked` 까지 「worktree 제거」로만 두는 것 · RED 조건 rc == 1 만 — 수용?(S-red spec 우려 #1–#3)
