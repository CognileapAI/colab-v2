용도 — `/to-spec` 산출물의 집. 승인된 `dev-package/intent/*.md` 를 재인터뷰 없이 합성한 회차 spec. 양식은 `TEMPLATE.md`.
명명 — `<회차>.md`(예 `R-C.md`). 라운드 파일 `prd/rounds/R-*.md` 이 첫 줄에서 이 파일을 링크한다.
우선순위 — spec 은 「무엇을·왜·어떤 결정으로」의 불변 기록, 라운드 파일은 실행 뷰. 어긋나면 spec 이 우선한다.
