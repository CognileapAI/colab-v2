Per-directory counts in the table were taken from the earlier full `find` listing (4+5+7+9+9+8+10+5+7+10+2+12 = 88), so no further verification is needed.

**File:** `<repo>/.claude/worktrees/harness-improvement/dev-package/prd/specs/S-HARNESS-TEAM-B-APPROVAL-20260926.md` — 184 lines.

**Decisions made where the source was silent (5):**
1. Transition rule = 「형식 OR (legacy ∧ 동결 스냅샷)」 with the snapshot read as the union of base and head commits, snapshot-name removal = `red(판정)`, snapshot generated by a new `intent_ref.py --freeze-legacy <sha>` option (byte-reproducible); current base `5bb3d6fe` measures 84 intent files = approved 63 · unapproved 15 · no-meta 6 · new form 0 (the "62" in the task is the v1 fixture size, not today's count).
2. The v1 answer table `gates/fixtures/intent-ref/intent-meta-classification.json` stays byte-identical because `gates/**` is in the eval hash set (`config-paths.txt:13`) and B must not need a 회차; the v2 table and new red-first cases go under `scripts/tests/fixtures/intent-ref/` (outside the set); the running gate is `harness-contract-selftest` (`gates/run.sh:356-357`).
3. `dev-package/intent/README.md:1-2` is included in B-3 alongside `TEMPLATE.md` (D2/D7 assign it to this PR and line 2 「승인 = 커밋(별도 승인 표기 없음)」 contradicts B-1); the master plan `:4` pointer is a 메인-session commit, not a lane edit, per the ownership table `:170`; the master plan's referenced §10 does not exist yet and is left to PR 2.
4. Import: `intent-draft/` (8 files, not in the task's enumeration) is included so the copy is 1:1 (88 files · 1.6 MB); path substitution is 3 literal `sed` passes (worktree root → `<repo>/.claude/worktrees/harness-improvement`, clone root → `<repo>`, remaining `<home>` → `<home>`), while the 40 `~/.claude/...` relative references and the one `/home/user/` allow-list citation are kept verbatim.
5. ADR handling recommended as a dated append line in ADR-0007 (ADR-0003 `:35` precedent) rather than a new ADR-0013, to keep 0011/0012 reserved for PR 2/4; handle checking recommended as format-only (GitHub login regex · no network · ADR-0003), and `2026-09-08-harness-evals.md` stays intentionally unprotected — all six concerns are tabled as 〈판정 대기〉.
