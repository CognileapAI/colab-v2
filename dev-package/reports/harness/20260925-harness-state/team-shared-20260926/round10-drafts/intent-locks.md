<repo>/.claude/worktrees/harness-improvement/dev-package/intent/2026-09-26-portable-gate-locks.md — 82 lines (within 80-130).

Decisions made where the source was silent:
1. Lock lifetime model: python3 runs as a short-lived child that calls `fcntl.flock` on the fd bash already opened with `{VAR}>` (`_lock.sh:98` · `_pg.sh:95`) and exits; the lock stays on bash's open file description, so exec survival, kernel release on death, and `gate_mutex_spawn` fd-closing (selftest ⑴⑵) are unchanged. Rejected `fcntl.lockf` (per-process, not fork-inherited) and mkdir locks (no kernel release).
2. Wait-limit implementation split: host mutex = one python3 call with blocking `LOCK_EX` + `signal.alarm` whose handler raises (PEP 475 EINTR retry must be broken); pg slots keep the existing bash `LOCK_NB` + `sleep 1` loop (`_pg.sh:98-108`), with per-second python3 spawn cost logged as 미해결 5.
3. Helper placement: one file `gates/tools/_flock.py` (`<fd> --nb | --wait <초>`, stdlib-only) shared by `_lock.sh`, `_pg.sh`, and both selftest fixtures, not inline `python3 -c`; the "python3 부재 → 78" branch is kept (not dropped) because `_lock.sh`/`_pg.sh` are sourced directly outside `run.sh:190`.
4. Selftest scope: regime unchanged, fixture ported (`flock -x` holders, `/proc/$$/fd` → `/dev/fd` + `os.fstat` inode compare, `mktemp -d -p` → explicit path, PATH surgery target `flock` → `python3`), plus a recommended new case ⓘ "flock binary absent + python3 present → green" so CI `ubuntu-latest` proves the macOS shape every PR; Windows parity = no native branch because `dev.ps1:119-122` routes gates through WSL.
5. Governance: recommend a new short ADR (ADR-0005 `:52-54` explicitly planned a "3상태 면제 변수" for this day; this intent takes a different path, so a record is needed, 0005 unmodified; number = next free at merge, skipping reserved 0011/0012 — flagged 미해결 3), rollout as a standalone small PR after PR A/B and before the PR 2 head 회차 (hash set: `config-paths.txt:13` still contains `gates/**` today, so landing before PR A would cost one 회차), and `ops-schedule-selftest.sh:127` flock use explicitly placed out of scope (미해결 4).
