File written: `<repo>/.claude/worktrees/harness-improvement/dev-package/prd/specs/S-HARNESS-TEAM-A-EVAL-20260926.md` — 184 lines (header · §0–§9 · T-항목 · 우려 7건 · 정책 대조 · 범위 밖). No real home path inside the file (it is itself under the new `dev-package/prd` scan root).

Decisions made where the source was silent (5):
1. **A-2 pattern form**: git pathspec exclude wins regardless of order (measured — `':(glob)gates/tools/harness-eval.sh' ':(exclude,glob)gates/**'` returns 0 in both orders), so `config-paths.txt` uses **explicit enumeration** of the 3 `gates/tools/*` files instead of "exclude then re-include"; `gates/**` moves to `ci-filter-check.py` `FILTER_ONLY` and `ci.yml` gains the 3 explicit lines to satisfy ㈏ set equality. `:(exclude).agents/ci-producers.json` is included per 권고 3 / master plan §10 `:224,:232` (flagged as 우려 #3 since intent `:627` names only `gates/**`).
2. **A-1 measured model source**: raw `modelUsage` entries carry `canonicalModel` (`claude-opus-5-5` under key `claude-opus-5-5[1m]`), so a new `config_hash.py record-usage --run <dir>` subcommand writes `model_usage` (sorted canonical list) into `config-hash.json` after the loop; verify requires `model == 정본 ∧ 정본 ∈ model_usage`, and the 「직전」 baseline is restricted to same-model rounds (so `20260926-143218` becomes `참고(다른 모델)` output, not a regression baseline — otherwise a fable round with fewer greens than the 18/20 opus round would red the gate).
3. **A-5 red-fix**: adding the 3 roots turns `harness-contract` red on **10 lines** (6 in `results/20260926-{125205,140939,143218}/H02.out.*.txt` plus 4 legacy `dev-package/prd` lines: `stage3-login-hardening.md:7`, `R-STAGE3-LOGIN-HARDENING.md:14`, `R-SELECTED-PREVIEW-TIMING.md:55`, `R-SELECTED-PREVIEW-TIMING-PR.md:23`); the unit rewrites results → `<repo>` and prd → `~/` in the same commit, rejecting the `home_path_allow` alternative (it would put a username in `harness.yaml`).
4. **Summary-line model tag**: appended as ` · 모델 <정본>` to the 요약줄 (intent wording), which requires updating the anchored regexes at `harness-eval.sh:79` and `harness-eval-selftest.sh:198,201`; a separate `- 모델 — 정본 … · 실측 …` line carries the measured list. Runner seam `COLAB_EVAL_MODEL_FILE` added for the 78 selftests; empty `model_usage` is judged by verify, not the runner.
5. **model.txt value** (우려 #1, 판정 대기): recommend `claude-fable-5-1` — README:108 정본, Ted's plan uses Fable for judgments, and the 20 tasks are judgment tasks — with the explicit cost consequence that a round returns to ≈32 USD (09-12 measurement) versus ≈8 USD on opus-5-5; T12 row carries both amounts. 우려 #2 fixes merge order A → B because PR B edits `gates/fixtures/intent-ref/*.json`, which is inside the hash set until A merges.
