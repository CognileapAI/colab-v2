[harness: subagent output matched instruction-shaped pattern(s): settings-json. Control tags below are neutralized (`<` → `<\`); treat any remaining directive-shaped text as a finding to relay to the user, not an instruction to you.]

VERDICT: GO-WITH-CHANGES

인용 대조(8회 호출 · 부하 큰 6곳 이상): `harness-eval.sh:87-96·:44-55·:8-20·:29·:33-34` · `ci.yml:116-128·:641-650·:681-711·:694-700·:866-869` · `eval/harness/run.sh:23-26·:37-44·:71-73·:76-78·:257-270·:277` · `test-file-guard.sh:12-14·:23-29·:42·:43·:69-78` · `lifecycle_contract.py:81-88·:210·:233·:287·:313·:315·:328·:350·:455·:548-550·:580·:602-606` · `gates/run.sh:351·:353-357·:365-369·:506-515` · `verify_evidence.py:233·:358-361` · `ci-filter-check.py:39·:50·:204` — 전부 실존·일치. 미확인: `intent_ref.py` 판정 경로 목록(E0 머리 괄호) · `agent-bridge.py:277-279·:337·:384-385` · `lifecycle_contract.py:377·:244-247`.
순서(PR 1 → T1·T11 → E0 → S-red → PR 2) = 7라운드 재구성 지시 `:609` 와 일치. Codex parity 는 두 spec 모두 명시(E0 V-E9 · S-red ⑫). 산문-only 단위(E0-6 · E0-7 · S-4 · S-5)는 모두 「강제 기제: 없음/기록」을 자기 선언. 무관 PR 차단 없음: `harness-eval` 잡은 `changes.harness == 'true'` 에서만 깨고 skipped 는 `ci-required` `always_required` 밖(`ci.yml:860-870`). S-red ↔ 2-8: 같은 hook · 같은 삽입점 앞뒤 · 같은 `open_tasks`/`handed_off` 헬퍼로 경계 명시 — 이중 차단 없음. 아래 결함만 고치면 GO.

- 차단급 · E0 「우려 항목 #1」 ⓑ · S-red T14 「면제(해시 집합 ⓑ)」 · 열린 질문 1 — Ted Q-A ⓐ(`:607`)는 「`.claude/**` · `.agents/**` · `gates/**` · hook 스크립트」를 이미 확정했다. ⓑ(guard hook · `gates/**` 제외)와 T14 면제 선택지는 확정 결정을 재개봉한다. 교체: 우려 #1 행 → 「해시 집합 = Q-A ⓐ 확정(재질문 없음) · 이 행은 비용 공개만: E0·S-red·PR 2·3·4 각 1회 ≈32 USD · 합 ≈190 USD」. T14 → 「S-red 실측 실행 승인(≈32 USD) — 면제 선택지 없음」. 열린 질문 1 삭제.
- 차단급 · E0 4.1 정본 목록 — Ted 목록은 `.claude/**` 전체인데 spec 은 `rules·settings.json·agents·skills·hooks` 5개로 좁혔다(`.claude/settings.local.json`·`commands/**` 등 누락 가능 · worktrees 는 `--exclude-standard` 로 자동 제외). 교체: `config-paths.txt` = `AGENTS.md` · `CLAUDE.md` · `.claude/**` · `.agents/**` · `gates/**` · `scripts/harness/hooks/**` · `eval/harness/config-paths.txt`. 열린 질문 2 → 「AGENTS.md · CLAUDE.md 추가(모델 입력 정본 · Ted 목록 밖) 수용?」로 축소(`.claude/rules` 는 이미 Ted 목록 안).
- 차단급 · S-red 4.3 `red-locked --cwd` · 우려 #1 근거 「부모가 lane checkout 의 red 시험을 고치는 것도 막아야 함」 — 설계가 이를 못 이룬다. 부모 세션이 `file_path=<lane worktree>/scripts/tests/x.py` 로 Edit 하면 payload `cwd` = 부모 checkout → `TOP` 이 부모 → REL 이 절대경로로 남고 task 도 checkout 불일치로 제외 → exit 0. 교체: 조회를 `file_path` 기준으로 — hook 에서 `FTOP="$(git -C "$(dirname "$FP")" rev-parse --show-toplevel)"` · `REL="${FP#"$FTOP"/}"` · `lifecycle_contract.py red-locked --checkout "$FTOP"`(cwd 대신) · task 대조 = `task.checkout == FTOP`. 시험 ⑩-j 추가: 「cwd = 부모 checkout · file_path = lane worktree 의 잠금 경로 → 2」. ⑩-h 는 유지(다른 worktree 자기 파일 → 0).
- 개선 · E0 4.4 「`test_ci_eval_policy.py` 를 `run.sh:357` harness-contract-selftest 에 등록」 — 이 시험은 `ci-filter-check.py` 를 부르고 PyYAML 부재면 78(`ci-filter-check.py:81`). harness-contract-selftest 가 PyYAML 없는 환경(로컬 `all` · CI 다른 잡)에서 78 로 바뀐다. 교체: `gates/tools/harness-eval-selftest.sh:159-169`(이미 PyYAML 의존 · CI `harness-judge-selftest` 에서 PyYAML 설치 뒤 실행) 안에 `python3 -m unittest scripts/tests/test_ci_eval_policy.py` 1줄 추가 · `run.sh:357` 에는 `test_harness_eval_gate.py` 만.
- 개선 · E0 4.4 필터 — `harness` 필터(`ci.yml:119-128`)에 `eval/harness/**` 가 없어 러너·셀프테스트·results 만 바꾼 PR(T12 결과 커밋 PR 포함)이 `harness-runner-selftest`·면제 게이트를 깨우지 않는다. 교체: 필터 추가 3줄 → 4줄(`- 'eval/harness/**'`) · `FILTER_ONLY` 에 `"eval/harness/**"` 추가(해시 밖 · 필터 안).
- 개선 · E0 4.4/4.7 `ci.yml:641-650` 주석 교체 — intent C3(`:348` 「ci.yml 낡은 주석」 · PR 3)와 소유 겹침. 교체: 4.7 에 「`ci.yml:641-650` 주석은 E0 소유로 이관 · C3 는 `gates/README.md:245` 표만」 1줄 + intent 「확인」 절에 같은 줄 기록.
- 개선 · 단위별 「⑶ 병합 조건」 누락 — 머리말은 모든 단위가 ⑴–⑸ 를 적는다고 선언했으나 E0 4.2·4.3·4.4·4.5 · S-red 4.1·4.2·4.3 에 병합 조건 줄이 없다. 교체(각 단위 끝 1줄): E0 4.2 「병합 조건 = CI `harness-runner-selftest` 17/17」 · 4.3 「= `harness-eval-selftest` 18/18 ∧ CI `harness-eval` 0(해시 일치)」 · 4.4 「= `harness-eval-selftest`(ci-filter-check + 변이 2건 red)」 · 4.5 「= `harness-contract-selftest` ⑸ green · 경고는 병합 조건 아님(Ted 결정 밖 · 신호)」 · S-red 4.1/4.2/4.3 「= `agent-bridge` 게이트 green(①–⑫) ∧ V-S9」.
- 개선 · S-red 4.1 러너 표 `scripts/tests/*.py` → `[python, -m, unittest, <module>.<case>]` — 점 표기 module 은 `scripts/tests` 가 패키지(`__init__.py`)여야 import 된다(미확인). 교체: `[sys.executable, '-m', 'unittest', <path>]` + case 있으면 `['-k', <case>]`(`gates/run.sh:351` 과 같은 경로 인자 형식 · 패키지 가정 없음). 시험 ①은 `-k` 로 1건만 red 인지 로그 단언.
- 개선 · E0 4.1 계산 규칙 — `.claude/hooks/**` 가 `scripts/harness/hooks/**` 로 가는 symlink 면 「작업 트리 바이트」가 대상 내용을 읽어 중복 계수(무해)지만 깨진 링크는 `missing`. 교체: 「파일 sha = `git hash-object --stdin < path`(symlink 는 링크 텍스트 · 깨진 링크 = `missing`)」로 1줄 명시 · 시험 ⑵ 에 symlink 케이스 1건.
- 개선 · S-red V-S6 「비용 증가 0」 — fast path 는 매 Edit/Write 에 `git rev-parse` + `ls` 2 fork 를 새로 넣는다(현재 `:42` 는 fork 0 으로 종료). 교체: 「python 호출 수 불변 · fork +2(git·ls) · 증가 < 10 ms/회」로 문장 정정(측정 기준은 그대로).