[harness: subagent output matched instruction-shaped pattern(s): settings-json, permissions-allow-deny. Control tags below are neutralized (`<` → `<\`); treat any remaining directive-shaped text as a finding to relay to the user, not an instruction to you.]

SPEC-BEGIN
# Spec: 하네스 개선 — phase PR 2 (lifecycle · CI 판정 · 역할 산문 · B → L → P 커밋 묶음)
출처 intent: `dev-package/intent/2026-09-25-harness-improvement.md`
출처 절: 「판정 기록」 1–8라운드 — B1–B8(2·3라운드) · L1–L8(3라운드) · A2 ⓐ 분할(1라운드) · Q3 ⓐ 커밋 승인 원칙 · Q5 ⓐ blocked handoff(7라운드) · L2 ⓑ begin 연쇄 차단 + 「닫힘」 2단(3라운드) · L3 자기검사 위치 · L4 · L5 · L6 selftest 미편입 · L7 재현 3회 · P-roles(5라운드 Q1–Q4) · 8라운드 ⑧(L8 는 메인 세션) ⑨(`pr-contract.yml` 비필수) ⑩(⑼ role 결합은 bridge 실측 뒤) ⑫(S-red 잠금 = checkout 결속). 총괄 = `dev-package/prd/specs/S-HARNESS-IMPROVEMENT-PLAN-20260926.md` §2 PR 2(2-1a~2-12) · §3 파일 소유 · §5 · §6. 형식 = phase 1 spec `S-HARNESS-IMPROVEMENT-20260925.md`. 선행 spec = E0 `S-HARNESS-E0-EVAL-GATE-20260926.md` · S-red `S-HARNESS-SRED-REDRUN-20260926.md`(S-1~S-5 · `red-locked` · `handed_off` 는 그 spec 이 정본 · 이 문서는 그 위에 분기·기록만 더한다 · 중복 구현 0).
근거(저장소 밖): `~/.claude/reports/harness-state-20260925/system-first-recut/recut.md`(2-1~2-11) · `design-lane.md`(N1 N2 N3 N6 P2 P7 · S-L1~S-R13 표) · `opus55-prompt-audit/cross.md`(역할 11건 · 충돌 절) · `findings-verified.md`(R3-1 R3-4 R3-9 R3-10 R4-11 R4-13 R4-15 R4-judges-missed). 줄 번호 기준 = 브랜치 `claude/harness-e0-eval-gate` HEAD `e1e34ed6`(= develop `e4bd039a` + intent 기록) · 2026-09-26 재열람. ⚠ `lifecycle_contract.py`(616행) · `task_state.py` · `test-file-guard.sh`(92행) · `test_task_runtime.py`(600행) · `test_harness_lifecycle_contract.py`(460행)는 S-red 가 먼저 고친다 → 아래 앵커는 함수명 우선 · 레인은 S-red 병합 뒤 develop 에서 재열람.
새 사실(L8 스모크 2026-09-26 · PR 1 병합 뒤 메인 세션): researcher-task hook 의 additionalContext 가 첫 턴에 도달(task_id · run_id · agent_id 노출) · `--agent-id <그 id>` 로 연 artifact task 인계가 H6 통과 → SubagentStart/SubagentStop `agent_id` 일치 관측 1회. 결과: 2-5 자동 begin 승계 · 2-8 `agent_type` 분기의 전제 성립 · L2 ⓐ 는 §4.5 결정(PR 3 이관). T11(에이전트 PAT)은 Ted 보류 → 이 PR 의 어떤 단위·병합 조건도 T11 에 의존하지 않는다(2-9 ⑺ 는 hook 판정 · PAT 는 별건).
트레일러: 모든 커밋 메시지 **마지막 문단 하나**에 `Intent-Ref: dev-package/intent/2026-09-25-harness-improvement.md` + `Co-Authored-By: …` + `Claude-Session: …` 세 줄(빈 줄로 분리하지 않는다 · 2-9 ⑾ 이 이 형태를 검사 · 6라운드 트레일러 결함 재발 방지). 훅 정의(`.claude/settings.json:7-96` · `.codex/hooks.json`) 무변경 → `/hooks` 재신뢰 0.
단위 규칙 = 총괄 §0: 단위마다 ⑴ 강제 장치(파일 · 이벤트/검사 · FC/FO · Claude/Codex) 또는 「산문 · 맥락만」 ⑵ 시험(파일 + 사례)과 실행 게이트 ⑶ 병합 조건 ⑷ 지우는 산문(포인터화) ⑸ 출처. 역할 파일(`.agents/roles/*.md` · `AGENTS.md` · `lifecycle-evidence.md` · 스킬 산문)의 편집은 **P 묶음 커밋 1회**(§8) — B·L 단위는 지울 문장을 지목만 한다.

## 0. 출처 · 순서
- 순서(7·8라운드): PR 1 병합(#173 `86bbaeea`) → T1 적용(ruleset 24031394 · 2026-09-26 12:30) → E0(진행 중 · 별도 worktree · 이 문서는 건드리지 않음) → S-red → **PR 2(이 문서)** → Q6 effort 실측 → PR 3 → PR 4. PR 2 lane 은 S-red 병합 뒤 develop 기준 `--expect-head` 로 시작.
- 선행 조건: S-red 병합 · L8 스모크 기록(위 새 사실 · intent 「확인」 절 줄) · advisor ① 판정문 `runtime:artifacts/advisor-1.md`(2-12).
- 파일 소유(총괄 §3): 이 PR 은 `lifecycle_contract.py` · `task_state.py` · `test-file-guard.sh` · `git_guard_parse.py` · `git-guard.sh` · `decision-number-guard.sh` · `verify_evidence.py` · `pr_contract.py` · `check.py` · `config.py` · `harness.yaml` · `run.sh` · `ci.yml` · `agent-bridge.yml` · `ci-producers.json` · `agent-bridge.py` · 역할 2파일 · `AGENTS.md` 의 순차 소유자. `colab-rules.md`(2-3 이 지목한 §3-1 `:223` `-j 2`) · `verification-before-completion:51-61`(2-1f 지목) · `measurement-lane.md:13-18` 은 각각 PR 3 · PR 4 · PR 3 소유 → PR 2 는 지목만(§10 #6).

## 1. 문제 진술
- CI 판정 정확도(B): `verify_evidence.py:56-58` 병합 부모 대조 오류 문구에 출구 없음(#141 · #151 · #160 · #173 재발). `ci.yml:491` `RUN` 식 ≠ 등록부 `service-tests-core-api` filters(`ci-producers.json:400-402` core-api · contracts 뿐) → ai-service 단독 PR 잠복 red. 집계 우선순위 불일치 — `lifecycle_contract.py:500` `78 if red_준비 else 1`(준비 우선) · `run.sh:936-941` `all` 은 모든 비0 → `rc=1`(78 없음) · `run.sh:760-771` selftest · `verify_evidence.py:137` `verdict` 는 판정 우선 · `record` 명령 불일치(`:289` · `:293`)가 `main:406` 에서 78 · `ci` 의 OSError/JSONDecodeError(`:362`)가 1 · lifecycle 모든 ValueError → 78(`:611-613` · `test_task_runtime.py:380` 이 scope 위반 78 고정). `harness.yaml:16` `gates.required` 는 `config.py:71-73` 중복 검사 + `check.py:113` 개수뿐 · `harness-contract` · `agent-bridge` 생산자 0(`ci-producers.json` grep 0) · `agent-bridge.yml:3-34` 경로 필터 전용. `ci.yml:843-870` `ci-required` 인라인 python 이 `always_required`(`:860`) 밖 `skipped` 를 사유 없이 허용 · `intent-ref` 잡 `if`(`:666`) 와 거울상 없음. `pr_contract.py` 는 로컬 CLI 뿐 · `:37,:47` `<[^>]+>` 가 정상 본문을 placeholder 로 오판. `live_audit.sh:29` `cut -c1-60` 덮어쓰기 · `frontend-visual.sh:108` 파일 수 계수 · `:75` `mkdir -p` 잔존 · `live_probe.js:56` 80 절단 · `:39` 최상위 `cssRules` 만. `_pg.sh:193` trap 대체 → `service-tests.sh:81-82` `SVC_CLEAN` 소실 · `operator-notifications.sh:7` 동일 · `:8-14` 표식 없는 `exit 78` 6곳.
- lifecycle 증거 정확도(L): 종료 기록 0(S-red S-4 `handed_off` 만 예정) · `close` · `prune` 없음(`main:541-569` 하위 명령 8개) · handoff `choices` 에 `blocked` 없음(`:566`) → 정당한 정지가 H7 8회 상한까지 차단. `stop()` 은 최종 메시지의 task_id 로만 판정(`:365-369`) → 편집 뒤 새 `begin --role researcher` 가 baseline 재설정(probe 재현). lane `begin` 은 `--scope` 선택(`:549`) · `--spec` · `--expect-head` 없음. handoff JSON 은 task_id · mode · summary · artifacts · run_id 뿐(`:601-604`) → checkout · branch · 계수 · spec 이 산문. `check_scope_declarations:81-92` 는 오타 glob 통과. `test-file-guard.sh:42` env 부재 exit 0 · `:81-86` 보호 4종 · 역할 분기 0 · 페이로드에 `agent_type` 미사용. `git_guard_parse.py` ⑴ 은 `subagent` 한정(`rule_push:490-492`) · `rule_gh:423` 은 `pr merge` · `api PUT merge` 만 · push `--tags` · lane push 전면 · `lifecycle close|prune --apply|stop` · 커밋 트레일러 검사 없음 · 감사 기록 0(`audit` grep 0). `check_gate_parallelism`(`check.py:25`) 직접 시험 0(`test_harness_config.py:240` lambda).
- 역할 산문(P): `lane-worker.md:5-8,13-14,19-21,23,27,28,29,31,38,42,51,52,55,68-69,72,73` · `researcher.md:15-16,27-28,30,69-83,75,87` 이 장치가 대신하거나(H6/H7 · scope · git-guard) 낡았거나(`origin/<default>` · `COLAB_GATE_REPORT_DIR` · ≤15행) 조기 종료를 유발(「승인 없는 커밋 금지」 · 「질문 3개 분할」). `AGENTS.md`(55행)에 조기 종료 4패턴 정본 없음. `executing-plans:17` · `writing-plans:166` · `VENDORED.md:87` 「isolation: worktree 자동」(Workflow `agent()` 부모 worktree 실행 관측 1회).

## 2. 원한 결과 (V-id)
- V-P1 B1: `verify_evidence.py` 부모 불일치 문구에 「Update branch 가 새 run · Re-run 은 계속 red」 · `test_harness_evidence.py:164` 문구 단언 · 실 PR 1건 Update branch 뒤 green 관측 1.
- V-P2 B2 ⓓ: `changes` 잡이 `verify_evidence.py applicable` 로 `run-service-tests-<svc>` 출력 · `RUN`/`if:` 가 그 출력만 읽음 · 등록부 filters 갱신 · filters {ai-service:true} + core-api 증거 → green(`test_harness_evidence.py`).
- V-P3 B3: 입력 조합 {1 · 78 · 1+78 · 111 · 표식만} 에서 `run_gates` · `run.sh all` · selftest · `verdict` 가 같은 값(1 > 78 > 0) · `record` 불일치 → 1 · `ci` 해독 실패 → 78 · lifecycle 판정성 raise → 1 · scope 위반 handoff → 1 · ADR-0011 `adr-records` green.
- V-P4 B4+L6 ⓐ: `harness-contract` · `agent-bridge` 가 등록부 생산자 · ci.yml 새 잡 `harness-gates` 에서 `record` 실행 · `check.py` 가 `gates.required ⊆ ∪ checks[*].gates` 대조(fixture red) · `agent-bridge.yml` 은 `workflow_dispatch` 전용 · `scripts/tests/**` 만 바꾼 PR 에서 잡 실행 관측 1.
- V-P5 B5: `ci-required` 판정 = `scripts/harness/ci_required.py` · intent-ref `skipped` 허용식 = `event_name != 'pull_request' || base_ref == 'product'` · harness-contract 가 `ci.yml:666` `if` 와 거울상 일치 대조 · `test_ci_required.py` 3사례.
- V-P6 B6+G1+G6+VERDICT: `.github/workflows/pr-contract.yml` 가 `pr_contract.py --mode draft` 실행 · placeholder = 줄/칸 전체 `<…>` 만 · Head-SHA 불일치 = `::warning::` 0 · Plan-Ref head 존재 · spec 「출처 intent」 == 커밋 `Intent-Ref` 집합 · `Scope:` glob ⊇ diff · `Advisor-1-Ref:` · `VERDICT:` · `Advisor-2-Ref: … sha256=` 행 필수 · spec V-id ⊆ 「검증」 절 · `test_pr_contract.py` 7사례.
- V-P7 B7: probe 이름 = slug 40자 + `-` + sha256 12자 · 선언 URL(중복 제거) 수 ≠ 페이지 수 → 1 · 전체 배열 + `counts` 대조 · 시작 시 이전 probe/png/index 삭제 · `@layer`/`@media` 재귀 · `frontend-visual-selftest` 4사례 · agent-browser 1회 `activeRules > 0`.
- V-P8 B8: `pg_on_cleanup` 등록 목록 + 멱등 trap 1개 · 호출자 2곳 치환 · `operator-notifications.sh` 표식 없는 `exit 78` 0건 · service-tests 1회 뒤 `/tmp/service-tests-*` 수 불변.
- V-P9 2-3: `all -j N` 에서 N > `harness.yaml gates.max_jobs` → 78(`test_gates_run_args.py`) · `_lock.sh:80-123` 무변경.
- V-P10 2-4: task.json `closure`(open/closed · via stop|close|checkout-missing · at · stop agent_id 정보) · `lifecycle close` · `lifecycle prune`(dry-run 기본 · `--apply`) · `handoff --mode blocked` · stop 반송 메시지에 출구 명령 · 닫힌 task 재사용 거절 · 시험 ①–⑥.
- V-P11 2-5: 명시 researcher begin 연쇄 차단 · 자동 begin(`--spawn`) 승계 · 옛 스키마 제외 · lane begin `--scope` · `--spec` · `--expect-head` 필수 · `--spec` 4검사 + sha256 · `--shared-checkout --reason` · 시험 ①–⑧ · 격리 clone probe ①–⑤ 재실행 ③ 거부.
- V-P12 2-6+2-7: handoff JSON = task_id · mode · summary · artifacts · run_id · checkout · branch · report · counts · spec_sha · exempt · missing · at · stop() 대조 · 옛 형식 거절 · 변경 경로 → 필수 게이트 `missing` 경고 · `--exempt-gate --reason` 건수 · 등록부 부재 78.
- V-P13 2-8+P7: `test-file-guard` 역할 분기 ⓐⓑⓒⓓ fixture 매트릭스 10 · `COLAB_AGENT_TYPE` bridge 주입 · Codex ⑫′ · S-red ⑩ 무변경 green · 실 Claude lane 편집 시점 차단 관측 1.
- V-P14 2-9+P2: git-guard 규칙 ⑴전원 ⑺⑻⑼⑽⑾ rc 표 · `audit.jsonl` 3 hook append · 메인 `push origin develop` 2 · `push origin feature` 0.
- V-P15 2-10 L5: 없는 접두 glob 거절 · 기존 디렉터리 아래 새 파일 통과 · begin 뒤 병합 파일 범위 밖 고정 · L6 fixture 4종 · L4 `runtime:artifacts` researcher 2건 H6 통과 관측 1 · L7 재현 3회 기록.
- V-P16 2-11: `harness-contract` green(adapter · 120행) · `agent-bridge check` green · `grep -nE 'isolation: worktree.{0,4}자동' .agents/skills` 0건 · §4.12 지우는 문장 grep 0건.
- V-P17 공통: `harness-eval` MATCH green(PR 2 head 회차 · T12) · 훅 정의 diff 0 · `bash -n` 3 .sh · `exec-bit` · `intent-ref` 0.

## 3. 해법 개요
- 코드: lifecycle CLI 확장(closure · close · prune · blocked · begin 인자 · handoff JSON · 판정 예외 클래스 · `open-tasks` 조회) · 집계기 4곳 통일 · CI 잡 2개 신설(`harness-gates` · `pr-contract.yml`) + `applicable` · `ci_required.py` · `change_gates.py` · `hook_audit.py` 신설 · hook 본문 3개(test-file-guard 역할 분기 · git_guard_parse 규칙 · decision-number-guard audit) · gate 도구 5개(B7 · B8 · run.sh max_jobs).
- 산문: 역할 2파일 · `AGENTS.md` · `lifecycle-evidence.md` · 스킬 6파일 · `dual-agent.md` · `gates/README.md` → 장치 이름 + 위치 포인터(P 커밋 1회).
- 새 ADR = **ADR-0011**(B3 집계 우선순위 · `docs/decisions/0010-search-rationale-policy-override.md` 가 2026-09-26 proposed 로 이미 존재 → 총괄 §2 「ADR-0010」 · PR 4 「ADR-0011」 번호는 +1 · §10 #1).

## 4. 구현 결정

### 4.1 B3 집계 우선순위 통일(2-1c · 첫 커밋 · 3라운드 「B1 · L 보다 먼저」)
- 장치: `lifecycle_contract.py` 에 `class JudgementError(ValueError)` — 판정성 raise 지점만 교체: `validate_report:313,315`(`required gates are missing` · `gate failures remain`) · `verify_task_report:343,346` · `stop():385-402` 변경·증거 불일치 · `:422` scope 위반 · S-red `verify_red_locked` blob 변경 · 2-4 blocked 조건 위반. `main:613` → `JudgementError` = 1 · 그 외 ValueError/OSError = 78 · `stop`/`validate-input` = 2 유지. `run_gates:500` → `1 if red_판정 else (78 if red_준비 else 0)`. `run.sh:936-941`(`all`) → 판정 red 있으면 1 · 없고 준비 red 있으면 78 · 아니면 0(`:1010 exit $rc` 그대로) · `:760-771` selftest 는 이미 판정 우선(무변경 · 시험만). `verify_evidence.py`: `record_command:289,293` 을 `EvidenceJudgementError(EvidenceError)` 로 → `main:406` 에서 1 · `ci_command:362` 의 OSError · JSONDecodeError · SubprocessError → 78(`EvidenceError` 는 1 유지). FC · CLI 공통.
- 시험·게이트: `test_task_runtime.py` 새 표 {1 · 78 · 1+78 · 111 · 표식만}(run_gates) · `:380` 78 → 1 갱신 · S-red ⑥ ⑦ 의 handoff 78 단언 → 1 갱신(S-red 는 78 로 작성됨 · B3 가 뒤집는다) · `test_gates_run_args.py` `all` 요약 fixture(`COLAB_GATE_SUMMARY_CHILD` 스텁 gate 2개 · 1+78 → 1 · 78 만 → 78) · `test_harness_evidence.py` record 불일치 → 1 · ci 해독 실패 → 78 · 게이트 `agent-bridge` · `harness-contract-selftest` · `adr-records`.
- 병합 조건: 위 시험 green · ADR-0011(`docs/decisions/0011-gate-aggregation-judgement-first.md` · `_template.md` · 「입력을 못 읽음 = 78 · 읽었는데 위반 = 1」 · ADR-0004 보강 · 이력 무수정). 지우는 산문: `gates/README.md` 집계 서술(`:39` 류 계수 설명은 유지 · 우선순위 문장은 ADR 포인터 1줄). 출처: intent B3 · R3-9 · R4-15 · R4-judges-missed.

### 4.2 B1 · B2 ⓓ · B4 · B5 — CI 증거 사슬(2-1a · 2-1b · 2-1d · 2-1e)
- B1(2-1a): `verify_evidence.py:58` 문구 → `PR merge commit parents differ from event base/head — exit: GitHub 「Update branch」(develop 병합 push)가 새 이벤트 run 을 만든다 · 기존 run Re-run 은 계속 red` · 코드 로직 무변경(FC 유지). 시험 `test_harness_evidence.py:164` 에 문구 `assertIn`. `colab-v2-work` PR 절차 1줄은 P 커밋. 출처 B1 3라운드.
- B2 ⓓ(2-1b): `verify_evidence.py` 새 하위 명령 `applicable --filters-json <changes 출력> --producer-prefix service-tests-` → 등록부 filters(`:188-190` 검증 재사용)로 `run-service-tests-<svc>=true|false` 를 `$GITHUB_OUTPUT` 에 기록 · 등록부 `service-tests-core-api.filters`(`ci-producers.json:400-402`)에 `ai-service` · `pipeline-worker` 추가 · `ci.yml:47` `changes` 잡에 step 추가 + `outputs` 4개 · `:491` `RUN: ${{ needs.changes.outputs[format('run-service-tests-{0}', matrix.service)] == 'true' }}` · `:526` `if:` 동일. FC(`changes` 실패 = `:187` 「path filter producer did not succeed」 78). 시험 `test_harness_evidence.py`: filters {ai-service:true, 나머지 false} + core-api 증거 → green · `applicable` 출력 표(4 서비스 × 필터 조합) · `test_ci_required.py` 무영향 확인. 지우는 산문: `ci.yml:168-169` 「WU-D3 · 골격」 주석(C3 ① 반입).
- B4+L6 ⓐ(2-1d): `ci-producers.json` 에 생산자 `harness-gates`(job `harness-gates` · filters `harness` · checks `harness-contract`{gates:[harness-contract]} · `agent-bridge`{gates:[agent-bridge]}) · `ci.yml` 새 잡 `harness-gates`(`needs: changes` · `if: needs.changes.outputs.harness == 'true'` · PyYAML 등 `agent-bridge.yml:51-56` 설치 스텝 이관 · `record --producer harness-gates --check harness-contract -- bash gates/run.sh harness-contract` · 같은 형태 `agent-bridge` · 증거 artifact 업로드 · `required-gates:806` · `ci-required:848,859` needs 에 추가) · `changes.harness` 필터(`:119-128` · E0-4 가 4줄 추가한 상태)에 `scripts/tests/**` 추가(L6 ⓐ 조건 「`scripts/harness/**` · `scripts/tests/**` 변경에도 실행」 · `ci-filter-check.py` `FILTER_ONLY` 에 동기). `agent-bridge.yml:3-34` → `on: workflow_dispatch` 만(잡 본문 유지). `check.py` 새 `check_required_gates_have_producers(root, value)`: `∪ checks[*].gates` ⊉ `gates.required` → 판정 red(json 만 · PyYAML 0). FC. 시험 `test_harness_config.py` fixture(생산자 없는 required → red) · `test_harness_evidence.py:203` 등록부 전수(새 생산자 포함) · `harness-contract-selftest` · `scripts/tests/**` 만 바꾼 PR 에서 `harness-gates` 실행 관측 1(PR 2 자체가 그 PR). 지우는 산문: `colab-v2-work:73` 「못 돌았음≠통과」→ 「`required-gates` 가 skip 사유를 대조한다」 포인터(P). 출처 B4 · L6 ⓐ 3라운드 · recut 2-1.
- B5(2-1e): `ci.yml:843-870` 인라인 python → `scripts/harness/ci_required.py`(`CI_RESULTS` · `GITHUB_EVENT_NAME` · `GITHUB_BASE_REF` env · `INTENT_REF_SKIP_ALLOWED = "github.event_name != 'pull_request' || github.base_ref != 'product'"` 의 부정형 상수 = `:666` `if` 거울상 · `skipped` 허용: 등록 생산자 잡은 `required-gates` 가 대조하므로 그대로 · `intent-ref` 는 허용식 참일 때만 · 그 외 skipped = 1). `check.py` 새 검사: `ci.yml` 원문에서 정규식 `^  intent-ref:\n(?:.*\n)*?\s+if: (.+)$` 로 문자열 추출 → `ci_required.INTENT_REF_JOB_IF` 와 축자 일치(PyYAML 0 · 불일치 = 판정 red). FC. 시험 `test_ci_required.py`(현재 `:18-26` 이 ci.yml step 의 `run` 을 bash 로 실행 → step 이 `python scripts/harness/ci_required.py` 가 되므로 같은 헬퍼가 그 명령을 그대로 실행) `:39` 확장: develop PR + intent-ref skipped → 1 · push + skipped → 0 · product PR + skipped → 0 · 기존 3사례 green. 게이트 `harness-contract-selftest`(`run.sh:357` 에 `test_ci_required.py` 추가 — PyYAML 의존이므로 `harness-eval-selftest.sh:159-169` 안에 넣는 E0-4 방식과 같이 처리 · §10 #7). 출처 B5 2라운드.

### 4.3 B6 · G1 · G6 · VERDICT — `pr-contract.yml`(2-1f)
- 장치: 새 workflow `.github/workflows/pr-contract.yml`(`pull_request: types [opened, edited, synchronize, reopened]` · `permissions: contents: read, pull-requests: read` · `gh pr view --json body,headRefOid` → `python scripts/harness/pr_contract.py <body> --head <sha> --mode draft --base <base sha> --stale-head warn`). `pr_contract.py` 변경: `:37,:47` placeholder = 줄 전체 또는 표 칸 전체가 `<…>` 인 경우만(정규식 `^\s*<[^>]+>\s*$` · 칸 `\|\s*<[^>]+>\s*\|`) · `--stale-head warn` = Head-SHA 불일치를 `::warning::` + step summary · exit 0(구조 실패는 1) · 신설 검사 ⑴ `Plan-Ref` 파일이 head 트리에 존재(`git cat-file -e <head>:<path>`) ⑵ 그 spec 의 「출처 intent:」 줄 경로 집합 == `git log <base>..<head> --format=%(trailers:key=Intent-Ref,valueonly)` 집합 ⑶ `Scope:` 행(줄 1개 · 공백 구분 glob) ⊇ `git diff --name-only <base>..<head>`(`lifecycle_contract` 의 glob 규칙 재사용 · `**` 만 디렉터리 횡단) ⑷ `Advisor-1-Ref:` · `VERDICT: ACCEPT|ACCEPT-WITH-CHANGES` · `Advisor-2-Ref: runtime:artifacts/advisor-<n>.md sha256=<64hex>` 행 존재(CI = 형식 · 로컬 `--artifact-root <git-common-dir>/colab-harness` 시 파일 sha256 실체 대조) ⑸ Plan-Ref spec 의 `V-[A-Z]*[0-9]+` 집합 ⊆ 「검증」 절 본문. `.github/pull_request_template.md` 에 `Scope:` · `Advisor-1-Ref:` · `VERDICT:` · `Advisor-2-Ref:` 4행 추가. FC(구조) · FO(Head-SHA) · 도구 무관. required check 지정 없음(8라운드 ⑨).
- 시험: `test_pr_contract.py:101` 확장 — 인라인 `<task_id>` · `Array<string>` 통과 · 템플릿 원문 실패 · Plan-Ref 부재 · intent 집합 불일치 · scope 밖 파일 · VERDICT 행 부재 · sha256 불일치(로컬 모드) → 7사례 · fixture repo 는 `test_task_runtime.py:18-26` 꼴 · 게이트 `harness-contract-selftest`. 병합 조건: PR 2 본문 자체가 이 형식으로 `pr-contract.yml` green(관측 1). 지우는 산문: `colab-v2-work:54-66` ② 절 「② 생략 금지」 → 「② 판정문은 `Advisor-2-Ref` + `VERDICT` 행 · `pr_contract` 가 판정」(P) · `verification-before-completion:51-61` 은 PR 4 소유(§10 #6). 출처 B6 2라운드 · recut 2-2 · direction 격차 3 · 6.

### 4.4 B7 · B8 · 2-3 — 게이트 도구(2-1g · 2-1h · 2-3)
- B7: `live_audit.sh:29` `SLUG="$(… | cut -c1-40)-$(printf '%s' "$URL" | sha256sum | cut -c1-12)"` · `index.md` 에 URL↔파일 대응 열 · `live_probe.js:56` 절단 제거(전체 배열) · `:39` 루프를 `walk(rules)` 재귀(`CSSGroupingRule.cssRules` 있는 규칙 = `@layer` · `@media` · `@supports`) · `frontend-visual.sh:75` 뒤 `rm -f "$OUT"/*.probe.json "$OUT"/*.png "$OUT"/index.md` · `:108-113` 뒤 `declared = len(set(URLS.split()))` · `pages != declared` → `::gate-failure::` + exit 1 · `:126-127` 앞 `len(res['small']) == res['counts']['small']` 대조 불일치 → 1. FC. 시험 `frontend-visual-selftest.sh`(실브라우저 없음 · `live_audit.sh` 스텁) 4사례: 앞 60자 동일 URL 2 → probe 2 · 선언 3 페이지 2 → 1 · 81번째 위반만 비허용 → 1 · 잔존 probe 미계수 → 페이지 수 = 선언 · 게이트 `frontend-visual-selftest`. 병합 뒤 agent-browser 1회 `frontend/src` 화면 `activeRules > 0`(메인 세션 · PR 본문 「남은 제약」). 출처 B7 2라운드.
- B8: `_pg.sh` 새 `pg_on_cleanup <fn>`(배열 등록 · 첫 호출이 `trap pg_cleanup EXIT INT TERM` 설치 · 멱등) · `pg_cleanup:168` 이 등록 목록 역순 실행 뒤 컨테이너·슬롯 정리 · `pg_start:193` trap 줄을 `pg_on_cleanup` 호출로 · `service-tests.sh:81-82` → `pg_on_cleanup SVC_CLEAN` · `operator-notifications.sh:7` → `pg_on_cleanup` · `:8,:9,:10,:11,:12,:14` `exit 78` → `_readiness.sh` `ready_red` 표식(6곳). FC. 시험 `service-tests-selftest.sh`(docker 스텁 · 호출자 정리 + `pg_cleanup` 둘 다 실행 기록) · `operator-notifications-selftest.sh` 에 `grep -cE '\|\| exit 78$' operator-notifications.sh` = 0 단언 · 로컬 1회 `ls -d /tmp/service-tests-* | wc -l` 전후 동일. 출처 B8 2라운드.
- 2-3: `harness.yaml` `gates.max_jobs: 4`(신설 · `config.py:71` 옆 정수 ≥1 검증) · `run.sh:835-837` 뒤 `[ "$jobs_n" -le "$MAX_JOBS" ] || gate_args_fail "-j $jobs_n > gates.max_jobs $MAX_JOBS" "-j $jobs_n"`(78 · `MAX_JOBS` 는 python3 json 1회) · `_lock.sh:80-123` `flock -w` 상한 78 무변경(유지). 시험 `test_gates_run_args.py` `all -j 9` → 78 + stderr `max_jobs` · `all -j 2` 는 기존 경로 · `gate-host-mutex-selftest.sh:97` 무영향. 지우는 산문: `lane-worker.md:52` 대기 금지 · `:55` 전수 두 벌(P) · `colab-rules:223` `-j 2` 는 PR 3(지목만). 출처 recut 2-3.

### 4.5 L1 · N1 · N8 · T8 — 종료 기록 · close · prune · blocked(2-4)
- 장치(`lifecycle_contract.py` · FC · CLI + H6/H7 공통): begin 이 `task['closure'] = {state:'open'}` 기록(PR 2 이후 스키마 표지 · 없으면 옛 스키마). `stop()` 통과 시 hook 경로(`main:568` `stop` 하위 명령)만 `closure = {state:'closed', via:'stop', mode, run_id, at, stop_agent_id}`(정보 · 대조 없음) 저장 · CLI handoff 는 S-4 `handed_off` 만. 새 하위 명령 `close --task <id> [--reason]`(메인 전용 · 서브에이전트 차단은 §4.10 ⑽ · `closure.via='close'` · red-locked 마커 제거) · `prune [--apply] [--days 14]`(대상 = `closure.state=='closed'` ∧ `at` + 14일 경과 ∨ `checkout` 경로 부재(dry-run 이 `closure={state:'closed', via:'checkout-missing'}` 를 기록한 뒤 다음 회차 대상) · dry-run 기본 = 목록 + 건수 + 용량 출력 · 삭제 0 · `--apply` = 삭제 + `<common>/colab-harness/prune.log` 1줄). `handoff --mode blocked`(`:566` choices 추가 · lane-worker · measurement-lane 만 · `--summary` 필수 · baseline 대비 변경 파일 ⊆ scope(scope 없으면 변경 0) 아니면 `JudgementError` + 출구 2 · gate 불요 · `handed_off.mode='blocked'` + `dirty[]{path, sha256}` 기록 · red-locked 마커 제거 · H7 라벨 `H7-blocked`). `stop()` 반송 메시지 끝에 출구: `handoff --task <id> --mode complete|blocked` · `lifecycle open-tasks --checkout <root>`. `closure.state=='closed'` task 의 handoff/stop → `JudgementError('task is closed — begin a new task')`. Codex = bridge lifecycle 위임(`agent-bridge.py:384-385`) 동일.
- 시험 `test_task_runtime.py`: ① hook stop 통과 뒤 `closure.closed` · `stop_agent_id` == payload ② stop 실패 시 closure 무변경 ③ prune dry-run 이 checkout 부재 task 나열 · 파일 무삭제 ④ `--apply` 뒤 디렉터리 부재 + prune.log ⑤ blocked 3건(요약 없음 78 · scope 밖 dirty 1 · scope 안 dirty 기록 0) ⑥ 닫힌 task handoff 1 · 게이트 `agent-bridge`. 병합 뒤 T8: 메인 `prune` dry-run 건수 → intent 「확인」. 지우는 산문: `lane-worker.md:5-8` 정지 목록 · `:42` · `researcher.md:75` 「질문 3개 분할」 · `lifecycle-evidence.md:35-37`(P). 출처 intent L1 3라운드 · T8 · Q5 ⓐ · recut 2-4 · design-lane N1 N8.

### 4.6 L2 ⓑ · N2 · G2 — begin 연쇄 차단 · 필수 인자(2-5)
- 장치(`begin:210` · FC · CLI 공통): ⓐ 명시 `begin --role researcher`(`--spawn` 없음): 같은 checkout 의 `closure.state=='open'` researcher task 중 baseline 대비 변경(`snapshot:137` 차집합 · `runtime:artifacts` 는 common dir 이라 제외) 이 있으면 `ValueError('open researcher task <id> has unhanded changes — exits: lifecycle close <id> (main) · handoff that task first')` 78 · `--agent-id` 변형도 동일 · 옛 스키마(`closure` 키 없음) 제외. ⓑ 자동 begin: `researcher-task.sh:53` `BEGIN` 에 `--spawn <agent_id>` 추가(hook 본문 · 정의 무변경) → 가장 오래된 열린 researcher task 의 `baseline` · `started_identity` 를 승계해 새 task 생성 · `spawn_agent_id` 정보 필드(대조 없음 · L2 ⓐ 입력 데이터) · `:56` 폴백 명령은 `--spawn` 없이(명시 begin 규칙 적용 · 출구 문구 포함). ⓒ `--role lane-worker`: `--scope`(`**` 명시 필수) · `--spec <저장소 상대 경로>` · `--expect-head <40hex>` 필수 → 부재 = `ValueError` 78 + 예시 · `--expect-head` ≠ `rev-parse HEAD` = 78 · `--spec` 검사 = 파일 존재 · 첫 10줄의 「출처 intent:」 경로가 `intent_ref.classify:41` 로 승인 · 「우려 항목」 절(`^## .*우려 항목`) 안 `〈판정 대기〉` 0 · 「정책 대조」 제목(`^#{2,3} .*정책 대조`) 존재 → 위반 = 78 + 사유 · `task['spec'] = {path, sha256}` · `--shared-checkout --reason <r>` → `task['shared_checkout'] = r`(2-8 ⓐ 통과 조건). `--legacy` 와 `--spec`/`--expect-head` 동시 = 78. `lifecycle-evidence.md:16-17` 「사람 절차」 → 이 검사로 대체(P). L2 ⓐ(spawn 결속 대조)는 **PR 3 이관**: 관측 1회(n=1)로 H6 실패 방향을 바꾸지 않는다 · ⓑ 가 재현된 우회를 agent_id 없이 막는다 · PR 2 가 `spawn_agent_id` · `stop_agent_id` 두 정보 필드를 쌓아 PR 3 spec 이 일치율로 결정(§10 #3).
- 시험 `test_task_runtime.py` ①–⑧(총괄 2-5 열거 그대로 · ⑦ `--scope`/`--spec`/`--expect-head` 부재 각 78 · ⑧ 미승인 spec · 「판정 대기」 spec · 「정책 대조」 없는 spec 거절) + `test_task_runtime.py:464-517` researcher hook 시험에 `--spawn` 승계(두 번째 스폰이 첫 task baseline 승계) · 게이트 `agent-bridge` · 격리 clone probe ①–⑤ 재실행 → ③ 거부(메인 세션 · PR 본문). 지우는 산문: `lane-worker.md:27` 병합 문장 → 「`--expect-head` 가 대조」 · `:38` scope · 「승인 없는 커밋 금지」(Q3 ⓐ) · `lifecycle-evidence.md:16-17` · `:36` → 장치 위치(P). 출처 intent L2 3라운드 · recut 2-5 · design-lane N2 · direction 격차 3.

### 4.7 N3 · `at` · 2-7 — handoff JSON · 변경 경로 대조(2-6 · 2-7)
- 2-6(`main:601-604` · `stop:350` · FC): CLI 가 채우는 키 추가 — `checkout`(identity key) · `branch`(`git branch --show-current` · 비면 SHA) · `report`(run 보고서 절대경로 · lane 만) · `counts`{green, red_판정, red_준비} · `spec_sha`(task.spec) · `exempt[]` · `missing[]`(2-7) · `at`(ISO UTC). `stop()`: `checkout` ≠ 현재 identity → `JudgementError` · `branch` ≠ 현재 → 거절 · `spec_sha` 는 task 에 spec 이 있을 때만 대조(PR 2 전 begin task 허용) · `at`·`checkout` 키 부재 = 옛 형식 → `ValueError('handoff schema: missing checkout/at — regenerate with lifecycle handoff')`. 정보 전달용 `counts` 는 report 와 대조(불일치 = 판정). Codex 동일(CLI 산출).
- 2-7(FO 경고 · Q4 ⓐ): 새 `scripts/harness/change_gates.py` — `load_filters(ci_yml_text)`(`ci-filter-check.py:106-128` 의 filters 파싱을 함수로 분리 · `importlib` 파일 로드 · PyYAML 필요) · `glob_to_re`(`:84`) 재사용 · `required_for(changed_paths, filters, registry) -> set(gate)` = 매치된 필터 이름 → 등록부 `checks[*].gates` 합집합. handoff(`complete`)에서 baseline 대비 변경 파일 × 위 함수 → `필수 − task['gates'] − exempt` = `missing`; 첫 회차 = `COLAB_HANDOFF.missing` 기록만(차단은 PR 3) · `--exempt-gate <g> --reason <r>`(반복) 건수·사유 stdout · 등록부 부재 = 78 · PyYAML 부재 = `missing: null` + `reason: 'pyyaml-unavailable'`(경고 · §10 #4). 시험 `test_task_runtime.py`(필드 채움 · checkout 위조 거절 · 옛 형식 거절 · missing 계산 fixture · exempt 건수 · 등록부 부재 78) · 새 `scripts/tests/test_change_gates.py`(순수 함수 표 6건 · `run.sh:351` 등록) · 게이트 `agent-bridge`. 지우는 산문: `lane-worker.md:28` `WORKTREE=/BRANCH=` · `:72-73` · `researcher.md:87` · `AGENTS.md:44-46` 「변경에 맞춰」 → 포인터(P). 출처 recut 2-6 · 2-7 · design-lane N3 · Q4 ⓐ.

### 4.8 A2 ⓐ · P7 · 역할 분기 — `test-file-guard.sh`(2-8)
- 장치(PreToolUse `Edit|Write` 본문 · `settings.json:69-81` 정의 무변경 · FC · Codex = bridge 같은 스크립트): 순서 = S-3 red-locked 블록(S-red 가 `:42` 앞에 삽입) → **payload 읽기(`:45-49` validate-input)를 `:42` env 검사 앞으로 이동** → `agent_type`(payload) 부재 ∧ env 없음 ∧ 잠금 없음 → exit 0(메인 스레드 · 비용 = python 1회 추가 없음: validate-input 은 이미 잠금·env 경로에서 도는 호출 · 메인은 `agent_type` 판별을 bash `grep -q '"agent_type"'` 로 선별) → `agent_type ∈ {lane-worker, researcher, measurement-lane}` 이면 `lifecycle_contract.py open-tasks --checkout "$FTOP"`(신설 · S-red `red-locked` 와 같은 자리 · `{tasks:[{id, role, scope, fix, shared_checkout, red_paths}]}` 1회) → ⓐ `lane-worker|measurement-lane` ∧ (`FTOP` == `$CLAUDE_PROJECT_DIR` toplevel ∨ 현재 branch ∈ develop/product/main) ∧ 열린 task 에 `shared_checkout` 없음 → exit 2 ⓑ `researcher` ∧ REL ∉ WATCH 4경로(`lifecycle_contract.py:26`) ∧ 절대경로가 `<common>/colab-harness/` 밖 → exit 2 ⓒ lane ∧ 열린 scoped task ∧ REL ∈ 보호 4종(`:81-86`) ∧ scope 밖 → exit 2 ⓓ 열린 task 에 `fix` 있음 ∧ REL ∈ 보호 4종 → exit 2(scope 안이라도 · `COLAB_ALLOW_TEST_EDIT` 무효) — ⓓ 와 기존 `:42` env 검사는 OR 한 분기 · deny 문구 · audit rule `fix-lane-test-edit` 하나. `agent_type` 부재 = 통과(FO · 메인). S-red 창(PR 2 전)은 S-3 + env + S-2 blob 대조로만. Codex: `agent-bridge.py:246 codex_payloads` 에 `agent_type = os.environ.get('COLAB_AGENT_TYPE')` · `:175-176 --worker` 에 동일 · `:278` 통제 env 목록 + `scripts/dev.ps1:102` 목록에 `COLAB_AGENT_TYPE` 추가(`test_agent_bridge.py:268,494` 목록 단언 갱신). `--worker` 는 `guard-command`/`guard-edit`(`:371,:374`) 전용 = 서브에이전트 경로 확인(8라운드 ⑩ 실측 = 코드 확인 · 레인이 1회 재확인).
- 시험 `test_harness_lifecycle_contract.py` 새 클래스 `RoleGuardTests`(`:431` 직접 호출 꼴 · `CLAUDE_PROJECT_DIR` 주입): lane+루트 → 2 · lane+worktree(`git worktree add`) → 0 · lane+worktree 위 develop → 2 · `shared_checkout` task → 0 · researcher+`dev-package/sessions/x.md` → 0 · researcher+`frontend/src/x.ts` → 0(WATCH 밖 아님 = 감시 경로는 H6 몫 · 쓰기 허용 아님 → 2 로 판정: WATCH 4경로 · `runtime:artifacts` 밖 전부 2) · 필드 없음 → 0 · scope 밖 보호 경로 → 2 · fix task 보호 경로(env 없이) → 2 · env 만 → 2(문구 동일) · 닫힌 task 만 → 0 · S-red ⑩ a–j 무변경 green · `test_agent_bridge.py` env 주입 · Codex ⑫′(`apply_patch` + `COLAB_AGENT_TYPE=lane-worker` + 부모 cwd → 2) · 게이트 `agent-bridge`. 병합 뒤 실 Claude lane 1회 편집 시점 차단 관측(T18 과 같은 회차). 지우는 산문: `lane-worker.md:19-21` · `:31` · `researcher.md:27-28` · `:30` · `dual-agent.md:58-62` Codex/Claude 분기 → 장치 위치 · `design-review/SKILL.md:101,107`(P). 출처 intent A2 ⓐ · recut 2-8 · design-lane N4 · direction 격차 2.

### 4.9 (예약 없음 — 4.10 으로 계속)

### 4.10 git-guard 규칙 · P2 audit(2-9)
- 장치(PreToolUse Bash 본문 · `git_guard_parse.py` · FC ⑴⑺⑻⑼⑽ · FO ⑾ · audit · Codex = bridge `guard-command` 동일): `judge:521` 시그니처에 `role`(payload `agent_type` → 없으면 env `COLAB_AGENT_TYPE`) 추가. ⑴ `rule_push:490,492` 의 `and subagent` 제거 → 보호 브랜치 push 전원 차단(메인 ff push 포함 · Q-D ⓐ · T1 ruleset 이 원격에서 같은 결론) ⑺ `rule_gh:423`: `subagent` ∧ (`pr create|edit|comment|review|close|ready|merge` · `issue *` · `api` `-X|--method` ∈ {POST,PATCH,DELETE} · `release *` · `--edit-last`) → deny ⑻ `subagent` ∧ push `--tags` → deny ⑼ `role ∈ {lane-worker, researcher, measurement-lane}` ∧ `git push` → deny(대상 무관 · 출구 「push 는 오케스트레이터」) ⑽ `subagent` ∧ argv 에 `lifecycle` 뒤 `close|stop` 또는 `prune --apply`(`agent-bridge.py lifecycle …` · `lifecycle_contract.py …` 두 형태) → deny ⑾ `git commit` ∧ `-m` 토큰으로 메시지 복원 가능 ∧ 대상 dir 의 `git diff --cached --name-only` 가 `intent_ref.SUBJECT_PREFIXES`(`intent_ref.py:31-32` · `sys.path` 로 import) 와 교차 ∧ 마지막 문단에 `Intent-Ref:` 없음 → deny(출구 = 마지막 문단에 세 트레일러) · `-F`/편집기/`--amend` 무 `-m` = 판정 불가 통과(FO). P2 `scripts/harness/hooks/hook_audit.py`(신설 · `.py` 보조 = `git_guard_parse.py` 선례 · 등록 불요): `append(root, hook, tool, agent_id, agent_type, cwd, verdict, rule, detail)` → `<git-common-dir>/colab-harness/audit.jsonl`(ts ISO · deny 원문 · allow 는 명령 sha256 · 쓰기 실패 무시) · 호출 = `git_guard_parse.run:552`(판정마다 allow/deny · bash 폴백 시 `fallback`) · `git-guard.sh:206 deny()` 폴백 경로(python 1회 · deny 만) · `test-file-guard.sh` deny 4분기 + S-3 deny · `decision-number-guard.sh:124` deny — allow 는 python 이 이미 돈 경로만 기록(메인 Edit 비용 0). 게시 규칙: ⑺ 은 subagent 만 → 메인 세션 `gh pr create` 는 PR 3 3-1 deny 까지 가능 · T11 미적용과 무관.
- 시험 `test_git_guard.py` 새 클래스 `RuleExtensionTests`(fixture `:61`): 각 규칙 차단 · 허용형 0 · 메인 `push origin feature` 0 · 메인 `push origin develop` 2 · lane role `push origin feature` 2 · subagent `gh pr view` 0 · `gh api -X POST` 2 · `lifecycle prune`(dry-run) 0 · `prune --apply` 2 · 트레일러 없는 커밋(scope 안 스테이징) 2 · 트레일러 있는 커밋 0 · `-F` 커밋 0 · `AuditTests`: 각 판정 뒤 `audit.jsonl` 마지막 줄 필드 · 쓰기 불가 dir 에서도 exit 불변 · `test_harness_lifecycle_contract.py` 3 hook audit 줄 · `test_agent_bridge.py:505-509` 기존 green · 게이트 `agent-bridge`. 지우는 산문: `lane-worker.md:29` · `researcher.md:64` 게시 문장 · `issue-before:37-44` 게시 절 → 「게시 단계는 메인 세션 · T16」 1줄 · `README.md:73` 「⑴ 은 agent_id 있는 호출만」 → 전원(P). 출처 recut 2-9 · design-lane N6 P2 · Q-D ⓐ · Q1 ⓐ · 6라운드 트레일러 결함.

### 4.11 L4 · L5 · L6 · L7(2-10)
- L5(FC · `check_scope_declarations:81-92`): 첫 와일드카드 앞 구간의 `dirname` 이 작업 트리에 없으면 `ValueError('lane scope <glob>: directory <d> does not exist — exits: fix the prefix · or create the directory first')` · 기존 디렉터리 아래 새 파일 glob 통과. 시험 `test_task_runtime.py`(없는 접두 거절 · 통과 · begin 뒤 `git merge` 커밋 → 병합 파일이 범위 밖 목록에 포함(현행 고정)) · `agent-bridge`. `lifecycle-evidence.md:87-97` 순서 문장 「scoped task 열린 동안 병합 금지 → Update branch 는 인계 뒤 → 불가피하면 병합 뒤 새 task」(P).
- L6(시험만): `test_harness_config.py:240` lambda 대체 → fixture 4종(미선언 gate · `ALL_GATES` 부재 · 없는 gate 선언 · serial/parallel 외 값) · selftest 편입 없음(3라운드) · 게이트 `harness-contract-selftest`. 역할 문서 1줄 「lifecycle 파일 변경 lane 은 `agent-bridge` 선언」(P · `lane-worker.md` 게이트 절).
- L4(「산문 · 맥락만」 + 관측 1): `design-review/SKILL.md:63` → 「쓰기 = `runtime:artifacts/design-review-L<n>.md`」 · `:65` → 「모든 레인 인계 뒤 메인이 hash 대조로 반입 · 커밋」 · `researcher.md:27` 범위에 `runtime:artifacts` 추가 · 격리 스폰 시 기준 브랜치 확인 1줄(P). 관측: 병합 뒤 researcher 2건 같은 checkout · `runtime:artifacts` · H6 통과(메인 · PR 본문).
- L7(「산문 · 맥락만」 + 재현 3회 · 메인 세션 · 레인 밖): ① advisor+schema ② measurement-lane+schema ③ measurement-lane+schema+도구 허용목록 · 결과(StructuredOutput 호출 · 도구 수 · 종료 사유)를 `runtime:artifacts/l7-probe.md` → intent 「확인」 · 현행 우회 `VERDICT:` 텍스트는 `colab-v2-work` 「현행 관행」 1줄(P · 결과 무관). 출처 intent L4–L7 3라운드 · recut 2-10 · T10 입력.

### 4.12 역할 · 정본 산문 재작성(2-11 · P 커밋 · 「산문 · 맥락만」)
- `AGENTS.md:17-19` 아래 3행: 조기 종료 4패턴 명명(다음 단계 예고 · 계속 여부 질문 · 비차단 결정 나열 · 이정표 정지) · 「종료 = `COLAB_HANDOFF` 뒤 또는 승인된 정지(`handoff --mode blocked`)」 · 비가역 확인 유지(Q2 정본 · 58행 ≤ 120 `harness.yaml:98`).
- `lane-worker.md`: `:5-8` → 정지 = `handoff --mode blocked` 1줄 · `:13-14` → AGENTS 4패턴 포인터 + 허용 정지 목록 · `:19-21` → 「시험·gates·contracts 편집은 scope 가 허용한 task 에서만(test-file-guard)」 · `:23` 삭제 · `:27` → 「기준 = `worktree.baseRef`(ADR-0001) · 기대 HEAD 는 `begin --expect-head` 가 대조 · 첫 동작 = `git rev-parse --show-toplevel` 이 지시문의 부모 checkout 과 같으면 구현 전 정지(L3)」 · `:28` 삭제(JSON `branch`) · `:29` → 「push·병합은 git-guard ⑴⑼·ruleset 이 막는다 · 레인은 브랜치 이름만」 · `:31` → 「checkout 밖 편집은 hook 이 거절」 · `:38` → 「`--scope`·`--spec`·`--expect-head` 는 begin 필수 · 자기 브랜치 커밋 자유 · push·병합·원장 등재는 승인 범위 · 에이전트 커밋 ≠ 승인」(Q3 ⓐ 한 원칙 · `:52,:70` 과 통일) · `:42` → 「미충족 = blocked」 · `:51` 삭제 · `:52` → 「대기는 host mutex 몫 — 대기 루프 금지(턴 소모)」 · `:55` 삭제 · `:68-69` → `lifecycle-evidence.md` 포인터 1줄 · `:72` ≤15행 삭제(항목 순서만) · `:73` 삭제 · 게이트 절에 L6 1줄. `:10-11` 은 PR 4(Q6-3 뒤).
- `researcher.md`: `:15-16` → 4패턴 포인터 + 「차단 질문은 `[미확인]` 로 최종 메시지에」 · `:27` L4 · `:30` 삭제(frontmatter 정본) · `:64` 게시 문장 → 포인터 · `:69-83` → 「자동 task: hook additionalContext 의 task_id 로 handoff · artifact task 는 그 agent_id 로 begin · 상세 = `lifecycle-evidence.md`」 3줄 · `:75` → 「우선순위 순으로 답하고 남은 것은 산출 파일에 `미조사` · 산출 파일은 8번째 호출 전(턴 한도 절단 대비)」 · `:87` → 「경로 + 요지」. `:12-13` 은 PR 4.
- `lifecycle-evidence.md`: `:16-17` → `begin --spec` 검사 · `:35-37` → closure · begin 연쇄 차단 위치 · `:42` → L8 결과(일치 관측 1회 · 대조는 PR 3 판정) · 새 절 「종료 · prune」 · 「blocked」 · 「handoff JSON 키」 · 「measurement-lane」(C8 ③ · `MEASURING_ROLES:55` · `:335` allow_red).
- 스킬: `executing-plans:17` · `writing-plans:166` · `VENDORED.md:87` 「자동」 → 「Workflow `agent()` 스폰은 `isolation: "worktree"` 명시 · 관측 `design-fix-followups-20260925-E.md:5`」(L3) · `colab-v2-work:48` 같은 문장 · `:54-66` ② → VERDICT 행 · `:73` 포인터 · L5/L6/L7 각 1줄 · B1 절차 1줄 · `design-review:63,65,69`(모델 문장 = 「`researcher` 기본값 · 계수·추출만 `model: sonnet`」) · §2-2 항목 10(「산출 파일 먼저 · 턴 한도 시 남은 목록 파일 끝」 포인터) + 메인용 1행(「파일 없이 텍스트만 온 레인은 진행 보고 · 좁혀서 새 세션」) · `:101,107` → 2-8 동작 · `issue-before:37-44` 1줄 · `dual-agent.md:58-62` → 「편집 시점 차단 = test-file-guard 역할 분기(Claude·Codex 동일)」 · `README.md:73` 전원 · `gates/README.md` ADR-0011 포인터.
- 시험·게이트: `harness-contract`(adapter · 120행 · `check_always_on_lines` `config.py:243`) · `python3 scripts/agent-bridge.py check` · grep 표(§7 V-P16). 출처 recut 2-11 · cross.md 역할 11건 중 8 · intent L3 · 4라운드 반입 5건(C3 ① · C8 ③ · C9 ① · T8 · T10).

## 5. 시험 결정 (TDD · red-first)
1. B1 커밋 전: `test_task_runtime.py` 집계 표 + `:380` 1 + S-red ⑥⑦ 1 · `test_gates_run_args.py` all fixture · `test_harness_evidence.py` record/ci 분류 → `bash gates/run.sh agent-bridge` · `harness-contract-selftest` RED 기록(78 ≠ 1 · rc=1 ≠ 78).
2. B2: `test_harness_evidence.py` applicable · filters · required⊆registry(`test_harness_config.py`) · `test_ci_required.py` 3사례 → RED(하위 명령 없음 · `ci_required.py` 부재).
3. B3: `test_pr_contract.py` 7사례 → RED(`<task_id>` 실패 · 신설 검사 없음).
4. B4: `frontend-visual-selftest.sh` 4 · `service-tests-selftest.sh` · `operator-notifications-selftest.sh` grep · `test_gates_run_args.py` max_jobs → RED.
5. L1–L5: `test_task_runtime.py` ①–⑥(2-4) · ①–⑧(2-5) · 2-6/2-7 · L5 · `test_change_gates.py` · `test_harness_lifecycle_contract.py` `RoleGuardTests` · `test_git_guard.py` `RuleExtensionTests` · `AuditTests` · `test_harness_config.py` L6 4종 · `test_agent_bridge.py` env → 각 커밋 직전 RED 기록.
6. 구현 커밋마다 GREEN · 시험 수 증가분 기록(기준값 = S-red 병합 직후 `agent-bridge` 수 · `harness-contract-selftest` 수).
- 게이트 배정: `agent-bridge`(`run.sh:351` — `test_change_gates.py` 추가) · `harness-contract-selftest`(`run.sh:357` — `test_ci_required.py` 는 PyYAML → §10 #7 판정 전까지 `harness-eval-selftest.sh:159-169` 방식) · `frontend-visual-selftest` · `service-tests-selftest` · `operator-notifications-selftest` · `harness-contract` · `adr-records` · `intent-ref` · `exec-bit` · `harness-eval`(회차 뒤). CI = `harness-gates`(신설 · 자기 적용) · `pr-contract.yml`(자기 적용) · `agent-bridge.yml` 은 dispatch 만.
- green-by-skip 방지: `unittest` skip 수 기준값(현재 10) 불변 단언을 PR 본문 계수에 · 새 시험 파일은 `run.sh` 목록 등록 없이는 수가 늘지 않음이 드러남 · fixture 부재 = 78(skip 금지) · `test_harness_evidence.py:61` 「skipped 시험 거부」 규칙 유지.
- seam: fixture repo(`test_task_runtime.py:18-26`) · hook 직접 호출(`test_harness_lifecycle_contract.py:431` · `test_git_guard.py:61`) · tempdir 복사 fixture(`:400-410`) · 제품 hook 에 새 env seam 0(`COLAB_AGENT_TYPE` 은 제품 입력이지 시험 seam 아님).

## 6. 위험 · 롤백
- fail-closed 면 확대: test-file-guard 가 서브에이전트 Edit 마다 python 1회(`open-tasks`) · 조회 실패 = 통과(FO · 잠금 상태만 FC) · 메인 스레드 비용 0. git-guard ⑴ 전원 → 메인 세션 develop ff push 차단(의도 · 출구 = PR · T1 이 원격에서 같은 결론) · ⑾ 오탐 = `-m` 다중 토큰 결합 순서 — 시험 corpus 로 고정 · FO. `pr-contract.yml` 이 게시 PR 을 red 로 낼 수 있음(구조만 · 비필수 check · Head-SHA 는 경고).
- B3 가 S-red 시험 2건의 기대값을 78 → 1 로 바꾼다(의도 · ADR-0011). 78 을 해석하는 호출부(`stop()` 반송 · Workflow 스크립트)는 1 도 차단으로 읽으므로 동작 변화 = 분류만.
- B2 ⓓ: `changes` 잡 실패 시 서비스 시험 전부 skip → `required-gates` 78(`:187`) 로 잡힘. `harness-gates` 잡 신설 → PyYAML 설치 시간 +1분.
- 2-5 필수 인자: 옛 지시문·Workflow 스크립트가 `--scope` 없이 begin → 78 + 예시 문구 · 병합 뒤 첫 레인 1회 관측 · 이 PR 의 spec 이 `--spec` 검사 대상이므로 §10 「판정 대기」 0 이어야 레인이 시작된다(의도된 자기 적용). 자동 begin 승계: 열린 researcher task 가 여럿이면 가장 오래된 것 — 남은 task 는 `close`/prune 이 정리.
- 2-6 옛 형식 거절: PR 2 병합 전에 begin 한 task 의 handoff 는 새 CLI 가 JSON 을 만들므로 영향 0 · 손으로 쓴 `COLAB_HANDOFF` 만 거절.
- lane 자기 검증 한계: hook 은 `${CLAUDE_PROJECT_DIR}`(메인 checkout) 구버전이 판정(`worktree-setup.sh:31-38`) → 레인의 Bash/Edit 는 새 규칙을 라이브로 못 본다 · H7 은 구 stop() 이 새 JSON(추가 키)을 읽음(무시 · 통과) · 검증 = worktree 스크립트 직접 호출(§7) · 병합·pull 뒤 메인 관측.
- E0 규칙: `.agents/**` · `.claude/**` · `gates/**` · hook 본문 변경 → PR 2 최종 head 회차 1회(T12 · ≈32 USD) · 회차 뒤 집합 파일 push 금지 · 리뷰 수정 시 재실측.
- 롤백 = 커밋 묶음 단위 revert(B1 · B2 · B3 · B4 · L1 … L5 · P) · 정지 시 복구 = Edit 도구(matcher 밖) 또는 `git revert` · 킬스위치 `COLAB_HOOKS=0` 은 PR 3 까지 유지.

## 7. 검증표
| V | 명령(worktree 루트) | 기대 | 완료 기준 |
|---|---|---|---|
| V-P1 | `python3 -m unittest scripts/tests/test_harness_evidence.py -k parents` · 병합 뒤 실 PR Update branch | 문구 단언 green · green 관측 1 | 4.2 |
| V-P2 | `python3 scripts/harness/verify_evidence.py applicable --filters-json '{"ai-service":"true",…}'` · unittest | `run-service-tests-core-api=true` · fixture green | 4.2 |
| V-P3 | `COLAB_TASK_ID=<id> bash gates/run.sh agent-bridge` · `harness-contract-selftest` · `adr-records` · 수동: 스텁 gate 2개로 `run.sh all` | 표 5조합 일치 · `:380` 1 · ADR-0011 green | 4.1 |
| V-P4 · V-P5 | `bash gates/run.sh harness-contract` · `python3 scripts/harness/ci_required.py`(env 3사례) · `python3 gates/tools/ci-filter-check.py` | required⊆registry · 거울상 일치 · 필터 = 정본 ∪ FILTER_ONLY | 4.2 |
| V-P6 | `python3 scripts/harness/pr_contract.py ~/.claude/pr-bodies/PR-BODY-harness-improvement-PR2.md --head <sha> --mode draft --base <develop sha> --artifact-root <common>/colab-harness` | 0 · 게시 뒤 `pr-contract.yml` green | 4.3 |
| V-P7 · V-P8 · V-P9 | `bash gates/run.sh frontend-visual-selftest` · `service-tests-selftest` · `operator-notifications-selftest` · `TMPDIR=$(mktemp -d) bash gates/run.sh all -j 9` | 4사례 · 정리 2회 · 표식 0건 · 78 + `max_jobs` | 4.4 |
| V-P10~V-P12 · V-P15 | `agent-bridge`(①–⑥ · ①–⑧ · 2-6/2-7 · L5 · L6) · 수동: `python3 scripts/agent-bridge.py lifecycle prune` · `handoff --mode blocked --summary x` · `begin --role lane-worker --gate agent-bridge` (인자 없이) | dry-run 목록 · blocked 기록 · 78 + 예시 문구 | 4.5–4.7 · 4.11 |
| V-P13 | `agent-bridge`(`RoleGuardTests` · S-red ⑩ · ⑫′) · 수동: fixture repo 에서 `printf '%s' '{"tool_name":"Edit","agent_type":"lane-worker","cwd":"<parent>","tool_input":{"file_path":"<parent>/frontend/src/x.ts"}}' \| CLAUDE_PROJECT_DIR=<parent> bash .claude/hooks/test-file-guard.sh` | 2 + 「격리 아님」 문구 · 매트릭스 green | 4.8 |
| V-P14 | `agent-bridge`(`RuleExtensionTests` · `AuditTests`) · 수동: 위 꼴로 `git push origin develop`(메인 payload · develop fixture) · `git status` → `tail -1 <common>/colab-harness/audit.jsonl` | 2 · 0 · 줄 2개 verdict deny/allow | 4.10 |
| V-P16 | `bash gates/run.sh harness-contract` · `python3 scripts/agent-bridge.py check` · `grep -nE 'isolation: worktree.{0,4}자동' .agents/skills -r` · `grep -nE '≤15행|COLAB_GATE_REPORT_DIR|승인 없는 커밋|질문이 3개' .agents/roles/lane-worker.md .agents/roles/researcher.md` | green · green · 0 · 0 | 4.12 |
| V-P17 | `COLAB_HARNESS_EVAL_EXEMPT=1 bash gates/run.sh harness-eval`(회차 뒤) · `git diff develop -- .claude/settings.json .codex/hooks.json` · `bash -n` 3 .sh · `exec-bit` · `intent-ref` | 0 + run id + 두 해시 동일 · diff 0 · 0 | 공통 |

## 8. 레인 지시 (PR 2 · lane-worker 1개)
- 스폰: `Agent(subagent_type: "lane-worker", isolation: "worktree")` 명시(Workflow `agent()` 도 동일 · 부모 worktree 실행 관측 1회). 기준 = S-red 병합 뒤 develop · 지시문에 기대 HEAD 기재 · 첫 행동 = `git rev-parse --show-toplevel` 이 지시문의 부모 checkout 경로와 같으면 구현 전 정지 · `git merge --ff-only develop` · `git rev-parse HEAD` 대조.
- begin(worktree 루트 · S-red 시점 CLI 라 `--spec`·`--expect-head` 는 아직 없음 → 기대 HEAD 는 지시문 대조 · 이 명령 그대로): `python3 scripts/agent-bridge.py lifecycle begin --role lane-worker --gate agent-bridge --gate harness-contract --gate harness-contract-selftest --gate adr-records --gate intent-ref --gate exec-bit --gate frontend-visual-selftest --gate service-tests-selftest --gate operator-notifications-selftest --gate harness-eval --scope 'scripts/harness/hooks/lifecycle_contract.py' --scope 'scripts/harness/task_state.py' --scope 'scripts/harness/hooks/test-file-guard.sh' --scope 'scripts/harness/hooks/git-guard.sh' --scope 'scripts/harness/hooks/git_guard_parse.py' --scope 'scripts/harness/hooks/decision-number-guard.sh' --scope 'scripts/harness/hooks/hook_audit.py' --scope 'scripts/harness/hooks/researcher-task.sh' --scope 'scripts/harness/verify_evidence.py' --scope 'scripts/harness/check.py' --scope 'scripts/harness/config.py' --scope 'scripts/harness/ci_required.py' --scope 'scripts/harness/change_gates.py' --scope 'scripts/harness/pr_contract.py' --scope 'scripts/agent-bridge.py' --scope 'scripts/dev.ps1' --scope 'scripts/tests/test_task_runtime.py' --scope 'scripts/tests/test_harness_lifecycle_contract.py' --scope 'scripts/tests/test_harness_evidence.py' --scope 'scripts/tests/test_harness_config.py' --scope 'scripts/tests/test_gates_run_args.py' --scope 'scripts/tests/test_git_guard.py' --scope 'scripts/tests/test_pr_contract.py' --scope 'scripts/tests/test_ci_required.py' --scope 'scripts/tests/test_agent_bridge.py' --scope 'scripts/tests/test_change_gates.py' --scope 'gates/run.sh' --scope 'gates/README.md' --scope 'gates/tools/_pg.sh' --scope 'gates/tools/service-tests.sh' --scope 'gates/tools/service-tests-selftest.sh' --scope 'gates/tools/operator-notifications.sh' --scope 'gates/tools/operator-notifications-selftest.sh' --scope 'gates/tools/frontend-visual.sh' --scope 'gates/tools/frontend-visual-selftest.sh' --scope 'gates/tools/ci-filter-check.py' --scope '.agents/skills/design-review/scripts/live_audit.sh' --scope '.agents/skills/design-review/scripts/live_probe.js' --scope '.agents/skills/design-review/SKILL.md' --scope '.agents/skills/colab-v2-work/SKILL.md' --scope '.agents/skills/executing-plans/SKILL.md' --scope '.agents/skills/writing-plans/SKILL.md' --scope '.agents/skills/VENDORED.md' --scope '.agents/skills/issue-before/SKILL.md' --scope '.agents/roles/lane-worker.md' --scope '.agents/roles/researcher.md' --scope '.agents/harness.yaml' --scope '.agents/ci-producers.json' --scope '.github/workflows/ci.yml' --scope '.github/workflows/agent-bridge.yml' --scope '.github/workflows/pr-contract.yml' --scope '.github/pull_request_template.md' --scope 'AGENTS.md' --scope 'README.md' --scope 'docs/development/lifecycle-evidence.md' --scope 'docs/development/dual-agent.md' --scope 'docs/decisions/0011-gate-aggregation-judgement-first.md' --scope 'eval/harness/results/**'`(`dev-package/reports/**` 기본 허용 · intent 는 메인이 줄 추가 · `gates/tools/_lock.sh` · `colab-rules.md` · `measurement-lane.md` · `verification-before-completion` 은 편집 금지).
- 게이트: `COLAB_HARNESS_EVAL_EXEMPT=1 COLAB_TASK_ID=<id> bash gates/run.sh task` 1회 · 회차 커밋 뒤(E0 규칙 · 그 전엔 `harness-eval` 78) · 호스트 단독. 레인은 자기 hook 을 라이브로 못 쓴다 → hook 검증은 §7 직접 호출 · 시험만.
- 커밋 단위(각 트레일러 문단): **B1** 4.1 + ADR-0011 + `gates/README.md` 포인터 → **B2** 4.2(B1 · B2 ⓓ · B4 · B5 · `ci.yml:168-169` 주석) → **B3** 4.3 → **B4** 4.4 → **L1** 4.5 + `researcher-task.sh --spawn` → **L2** 4.6 + L5 → **L3** 4.7 + `test_change_gates.py` + L6 fixture → **L4** 4.8 + bridge/dev.ps1 env → **L5** 4.10 + `hook_audit.py` → **P** 4.12 + 2-10 L4/L7 산문(역할·AGENTS·lifecycle-evidence·스킬·dual-agent·README 1회) → **E** `results/<run>/`(T12 · 그 뒤 집합 파일 무변경). 되돌림 단위 = 각 묶음.
- 인계: `lifecycle handoff --task <id> --mode complete --summary 'red→green 계수 · V-P 표 · 시험 수 증가분 · run id · hash'`(새 CLI 가 JSON 을 채움 · `missing` 은 경고 필드) · push · 게시(T16) · 병합(T13) = Ted.

## 9. PR 본문 계획 (`pr_contract.py` 형식 · 4.3 신설 행 포함)
- 파일 `~/.claude/pr-bodies/PR-BODY-harness-improvement-PR2.md` · 검증 V-P6 · 게시 = T16.
- 첫 줄: 「하네스 개선 PR 2 — lifecycle 이 정지·종료·범위·인계를 값으로 기록하고 CI·hook 이 판정 우선순위·게시·편집 시점을 강제하며 역할 산문은 장치 포인터가 된다」.
- `Plan-Ref: dev-package/prd/specs/S-HARNESS-PR2-LIFECYCLE-CI-20260926.md` · `Head-SHA` · `Scope:` = §8 scope 목록 · `Advisor-1-Ref: runtime:artifacts/advisor-1.md` · `VERDICT: <advisor ② 값>` · `Advisor-2-Ref: runtime:artifacts/advisor-2.md sha256=<…>` · `검증 상태: 미검증` → CI(`ci-required` · `harness-gates` · `pr-contract.yml` · `harness-eval` MATCH) green 뒤 「검증됨」.
- 목적 = §2 V-P1–V-P17 · 범위 = §8 커밋 묶음 · 제외 = §11 · 결정 = ADR-0011 · L2 ⓐ PR 3 이관 · 2-7 경고 모드 · 검증 = §7 표를 「원한 결과 ↔ 실제 ↔ 근거 ↔ 가치 상태」 행 · 3계수 · skip 수 · 커밋별 시험 수 · 남은 제약 = 병합 뒤 관측 6건(B1 Update branch · `harness-gates` 실행 · 편집 시점 차단 · researcher `runtime:artifacts` H6 · prune dry-run 건수 · agent-browser `activeRules`) · L7 재현 결과 · lane 은 자기 hook 을 라이브로 못 씀 · T11 미적용.
- 병합 뒤: 메인 pull → 첫 Bash 통과 확인(⑴ 전원 · `git status` 0) → researcher 스모크 2건(L4) → T8 prune dry-run → Q6 effort 실측 착수 · T7 메모리 3파일 갱신(`required-gates-stale-pr-base` · `lifecycle-handoff-shared-worktree-collision` · `workflow-advisor-model-and-schema`).

## 10. 우려 항목
| # | 항목 | ⓐ | ⓑ | 권고 | 판정 |
|---|---|---|---|---|---|
| 1 | ADR 번호 — `0010` 이 이미 존재(search rationale · proposed) | B3 = ADR-0011 · PR 4 ADR = 0012(총괄 §2 번호 +1) | 총괄 번호 유지(충돌) | ⓐ | 〈판정 대기〉 |
| 2 | git-guard ⑴ 전원 = 메인 세션 develop ff push 도 hook 차단(T1 이 원격에서 이미 422) | 전원(Q-D ⓐ 그대로 · 출구 PR) | 메인 ff 예외 유지 | ⓐ | 〈판정 대기〉 |
| 3 | L2 ⓐ spawn 결속 대조 시점 | PR 3(PR 2 는 `spawn_agent_id` · `stop_agent_id` 정보 필드 축적 · 일치율로 결정) | PR 2 에서 즉시 대조(n=1 관측 위에 H6 fail 방향 변경) | ⓐ | 〈판정 대기〉 |
| 4 | 2-7 필터 로딩의 PyYAML 의존(lifecycle CLI 는 시스템 python3) | 경고 모드 유지 · PyYAML 부재 = `missing: null` + reason(첫 회차 계수에 노출) | `ci.yml` 필터를 JSON 정본으로 복제(정본 2벌) | ⓐ(FC 전환 PR 3 때 정본 1벌 결정) | 〈판정 대기〉 |
| 5 | 2-5 `--spec` 검사가 이 spec 자체에 적용 → §10 판정 완료 전 레인 시작 불가 | 의도(자기 적용) · Ted 판정 뒤 착수 | 첫 회차 면제 플래그 | ⓐ | 〈판정 대기〉 |
| 6 | 파일 소유 충돌 — 2-3 `colab-rules:223` · 2-1f `verification:51-61` · 2-11 `measurement-lane:13-18` | 총괄 §3 소유 우선(PR 3 · PR 4 · PR 3) · PR 2 는 지목 | 2-3/2-1f 문장만 PR 2 편입 | ⓐ | 〈판정 대기〉 |
| 7 | `test_ci_required.py`(PyYAML) 실행 게이트 | `harness-eval-selftest.sh:159-169` 안(E0-4 방식) | `harness-contract-selftest` 목록(PyYAML 없는 환경 78) | ⓐ | 〈판정 대기〉 |
| 8 | 2-8 ⓑ researcher 쓰기 범위 = WATCH 4경로 밖 · `runtime:artifacts` 밖 전부 exit 2(`dev-package/sessions/**` 등 산문 허용 경로는 통과) | 그대로 | 산문 경로 3종(`researcher.md:27`)만 허용 | ⓐ(H6 가 잡는 범위와 동일) | 〈판정 대기〉 |
### 정책 대조
- CLAUDE.md/AGENTS.md 저촉: 없음 — 종료코드 0/1/78 정의를 집계기에 적용(ADR-0011) · 제품 코드 0 · `contracts/**` 0 · PR 게시·병합 = 사람(AGENTS.md:27 · hook ⑺ 은 subagent 만).
- 「절대 하지 않는 것」: 훅 정의 변경 0 · ADR 이력 수정 0(ADR-0004 · 0005 보강은 새 ADR) · 승인 intent 줄 추가만 · `.claude/settings.json permissions` 무변경(PR 3).
- 계약 동결 해제: 아니오 — envelope(`lifecycle_contract.py:503-535`) · `colab-task/2` 스키마 id 무변경(키 추가만) · handoff JSON 은 키 추가 + 필수 키 2개(`checkout` · `at`).
- 디자인 제약: 해당 없음.

## 11. 범위 밖
- L2 ⓐ 대조(PR 3) · 2-7 fail-closed 전환(PR 3) · `permissions.deny`(PR 3 3-1) · `COLAB_HOOKS=0` 제거(PR 3 3-8) · C1 `measurement-lane.md` · `colab-rules` · `dual-agent.md` C8 개수 · `README.md` 훅 표(PR 3) · skills 산문 19+7 · `lane-worker.md:10-11` · `researcher.md:12-13`(PR 4 · Q6-3 뒤) · effort 값(Q6 · PR 4) · T11 PAT(별건 · 이 PR 무의존) · `task.json` 저장 형식 축소 · 현재 store 의 prune `--apply`(T8 · Ted) · Bash 쓰기 차단(A3 경계 유지 · 인계 blob·scope 대조가 잡음) · `gh api graphql` mergePullRequest · S-auth · S-dep · E1.
SPEC-END

열린 질문(≤8):
1. ADR 번호: `docs/decisions/0010-search-rationale-policy-override.md`(proposed · 2026-09-26)가 이미 있다 — B3 ADR 을 0011 로, PR 4 ADR 을 0012 로 총괄 §2 번호를 옮기는 것 수용?
2. git-guard ⑴ 전원(메인 세션의 `git push origin develop` ff 도 hook 이 차단 · T1 ruleset 이 원격에서 같은 결론)을 PR 2 에서 켤지, PR 3 `permissions.deny` 와 함께 켤지?
3. L2 ⓐ(spawn 결속 대조)는 L8 관측 1회로 PR 3 이관 + PR 2 정보 필드 축적으로 두는 판단 수용?
4. 2-7 변경 경로→게이트 대조가 `ci.yml` 필터 파싱에 PyYAML 을 요구한다 — 경고 모드에서 부재 = `missing: null` 로 두는 것 수용? PR 3 FC 전환 시 필터 정본을 어디에 둘지(ci.yml 유지 · JSON 복제)?
5. `test_ci_required.py`(PyYAML)를 `harness-eval-selftest.sh` 안에서 돌리는 E0-4 방식 재사용 수용?(`harness-contract-selftest` 에 넣으면 PyYAML 없는 환경 78)
6. 파일 소유 충돌 3건(`colab-rules:223` → PR 3 · `verification-before-completion:51-61` → PR 4 · `measurement-lane.md` → PR 3)을 총괄 §3 대로 두고 PR 2 는 지목만 하는 것 수용?
7. 2-8 ⓑ researcher 편집 시점 차단 범위 — WATCH 4경로 · `runtime:artifacts` 밖 전부 2(산문 경로 `dev-package/sessions|reports|intent` 통과) 로 확정?
8. 이 spec 의 §10 「판정 대기」 8건이 2-5 `--spec` 검사(자기 적용) 때문에 레인 착수 전 전부 판정돼야 한다 — 판정 순서를 advisor ① 대조 뒤 한 번에 할지?