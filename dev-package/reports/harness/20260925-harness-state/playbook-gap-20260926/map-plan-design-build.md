[harness: subagent output matched instruction-shaped pattern(s): settings-json. Control tags below are neutralized (`<` → `<\`); treat any remaining directive-shaped text as a finding to relay to the user, not an instruction to you.]

# 플레이북 대조 — STAGE GROUP A (Plan · Design · Build)

경로 표기: 저장소 상대 경로는 모두 `"<repo>/.claude/worktrees/harness-improvement"` 기준(이하 `WT/`). 저장소 밖 보고서는 절대경로. 줄 번호는 2026-09-26 현재 브랜치 `claude/harness-improvement` 실측.

## 1. 대조표

| # | 플레이북 요구 | 현재 하네스 | PR 2/3/4 | 격차 | 격차의 결과 |
|---|---|---|---|---|---|
| A-1 | Stage 1 intent.md = problem · proposed outcome · affected users/systems · constraints · open questions | **있음** — `WT/dev-package/intent/TEMPLATE.md:1-32`(문제 · 원한 결과 · 가치 가설 · 영향 범위 · 제약 · 설계트리 · 미해결 질문 · 범위 밖 · 확인 · 참조). 초안 생성은 `/grill-me` 종료 절차 `WT/.agents/skills/grill-me/SKILL.md:10-17`(미해결 0건 · Ted 원문 그대로) | 없음(변경 불요) | 형식 격차 없음. 「가치 가설 · 확인 방법」은 플레이북보다 넓음 | — |
| A-2 | intent 승인 = product owner 승인 · commit = audit trail · 승인된 intent 는 재작성 금지 | **부분** — 승인 판별은 `WT/scripts/harness/intent_ref.py:41-49`(메타 줄 「승인」/「미승인」 문자열) · append-only 판정 `:148-175` · 게이트 `WT/gates/run.sh:365` · CI `WT/.github/workflows/ci.yml:665-679` · ADR `WT/docs/decisions/0007-…md`. 승인 근거 = 에이전트가 적은 Ted 원문 인용(`WT/dev-package/intent/2026-09-25-harness-improvement.md:2` · `:541`). `WT/dev-package/intent/README.md:2` 「승인 = 커밋(별도 승인 표기 없음)」은 `intent_ref.py:47-49` 판별식과 모순 | 2-1(intent-ref 사슬 = B5 skip 사유 대조만) · 3-7 문서 재작성(README.md:105 만 대상) | ⑴ 「누가 승인했나」가 기계 판독 불가 — 커밋 author 는 git user Ted, 실제 작성자는 에이전트 · ⑵ intent README:2 정정 미계획 | 승인 문자열을 에이전트가 넣어도 게이트가 「승인됨」으로 본다. 감사 추적(Layer 4)이 산문 인용에 의존 |
| A-3 | Stage 2 spec.md — 승인 intent 를 조직 skill(brand · security · compliance · UX)로 제약해 합성 · 우려 항목 flag | **부분** — `WT/.agents/skills/to-spec/SKILL.md:9`(승인 intent 없으면 정지 · 산문) · 템플릿 `:57-92` 정책 대조(CLAUDE.md §2·§3·§5) + 디자인 제약 표(게이트 `frontend-design-lint` a~h) · 우려 항목 표 `:87-89`. security/deploy 는 `WT/AGENTS.md:33-35` 「변경 전 `deploy.md`·`s3-upload.md` 읽기」 산문뿐 · spec 템플릿에 절 없음 | 3-2 N-7 「spec/round 구조 lint(변경 파일 한정)」 | spec 템플릿의 제약 절이 UX 만 게이트 연결 · security/deploy/s3 제약 절 없음 · 구조 검사는 PR 3 이후 | `services/core-api/**`·`infra/**` 를 건드리는 spec 이 s3-upload/deploy 규칙 대조 없이 레인으로 간다 |
| A-4 | flagged concerns 를 policy owner 와 해소한 뒤 엔지니어링 착수 · human sign-off before build | **부분(산문)** — `to-spec/SKILL.md:23` 「두 절이 비면 advisor ① 에 올리지 않는다」 · spec `WT/dev-package/prd/specs/S-HARNESS-IMPROVEMENT-20260925.md:200-206` 우려 항목 「판정 완료」 표 · Ted 승인은 `:2` 원문 인용. 레인 착수를 막는 기계 검사 0 | 2-5 `begin --spec` sha256 기록(신원만 · 내용 미검사) · 3-2 N-7(PR 3) | `lifecycle begin --role lane-worker` 가 「spec 의 우려 항목에 빈 판정 칸」·「출처 intent 미승인」을 보지 않는다 | 미해소 우려를 가진 spec 으로 레인이 돌아 rework 발생. 사람 sign-off 가 대화에만 남는다 |
| A-5 | Stage 3 plan.md — 파일 · 순서 · 시험을 적은 계획을 코드 **전에** 커밋 | **부분** — `WT/.agents/skills/writing-plans/SKILL.md:19-22`(rounds `R-*.md` ≤300행 · spec 링크) 규약 있음. 이번 intent 는 R- 파일 없음(`WT/dev-package/prd/rounds/` 목록에 `R-HARNESS-*` 부재) · **spec 이 plan 을 겸함**: `S-HARNESS-IMPROVEMENT-20260925.md:4` 「PR 1·2·3 의 Plan-Ref 공통」 · §8 `:177-185` 레인 지시 = scope glob 18개 · 커밋 단위 ①–⑥ · V1–V17 시험표(`:156-175`). 순서: spec 커밋 `f3cfb4ab` 2026-09-26 00:12 · 개정 `901eb02a` 08:15 → 레인 병합 `222e685e`(정확 시각 [미확인]) | 2-2 pr_contract 「spec V-id ⊆ PR 검증 절」 | 「plan 이 코드보다 먼저 커밋됐다」와 「Plan-Ref 파일이 head 에 존재한다」를 검사하는 장치 0 — `WT/scripts/harness/pr_contract.py:36-38` 은 placeholder 여부만 | Plan-Ref 에 없는 경로 · 뒤늦게 쓴 계획도 통과. merged diff ↔ plan 대조(플레이북 측정 3)가 불가능 |
| A-6 | plan 이 interrogated · refined (plan mode · design review before code generation) | **부분(산문)** — advisor ① `WT/.agents/skills/colab-v2-work/SKILL.md:69-75` · spec gate ① 결과는 `S-HARNESS-…md:2` 「GO-WITH-CHANGES」 산문 · 원문은 저장소 밖 `~/.claude/reports/harness-state-20260925/fable-recheck-r3/` | 2-2 `VERDICT:` 행 = advisor **②** 증거만 | advisor ①(설계 검토) 실행 증거가 PR 본문 · 저장소 어디에도 기계 판독 형태로 없음 | 「검토했다」가 보고 문장. Layer 4 감사에서 ① 생략을 못 잡는다 |
| A-7 | CLAUDE.md ≤ one page · session start 에 읽힘 · 코드처럼 유지 | **부분** — `WT/CLAUDE.md` 6행 · `WT/AGENTS.md` 55행 · `WT/.claude/rules/colab-rules.md` 5행 · 예산 `WT/.agents/harness.yaml:98-100` `always_on_max_lines: 120` · 검사 `WT/scripts/harness/config.py:243-257` ← `WT/scripts/harness/check.py:91`(harness-contract gate). **그러나** `AGENTS.md:10-11` 이 첫 독서로 `docs/development/dual-agent.md`(303행) + `.agents/rules/product.md` §0·§3·§5·§6·§10(192행 파일)을 지시하고 `colab-v2-work` SKILL(131행)이 「반드시 먼저 호출」 | 3-4(SessionStart behind-N 안내) · 3-7 문서 재작성 · PR 4 AGENTS.md +4행 | 예산이 재는 파일 집합(3개)과 실제 세션 시작 독서 집합이 다름 → 「한 페이지」는 측정상 성립 · 실효상 미성립 | 낡은 지식이 매 세션 컨텍스트를 점유(플레이북 anti-pattern) · 예산 게이트가 실제 부담을 못 잡는다 |
| A-8 | 「실수 2회 → CLAUDE.md」 | **있음(변형)** — 사건 기록은 `colab-v2-work/SKILL.md` 각 절(§0 · §1-b · §3 · §4 · §6 「실제 사건」) · eval 규칙 `WT/eval/harness/README.md:8` 「사고 1건 = eval 1건」 | PR 4 산문 축소(colab-v2-work:33,50,74,93,116-122 포인터화) | 자리가 CLAUDE.md 가 아니라 skill — 플레이북과 위치만 다름 · 결함 아님 | — |
| A-9 | skills = 버전 관리된 `.claude/skills/<name>/SKILL.md` · trigger 조건 + enforcement 지시 | **부분** — 본문 `WT/.agents/skills/*`(19) · Claude adapter `WT/.claude/skills/*`(17). adapter 는 description 을 「Claude에서 공통 to-spec 절차를 연결한다」로 평탄화(`WT/.claude/skills/to-spec/SKILL.md:3`) — 원본 trigger 문(`WT/.agents/skills/to-spec/SKILL.md:3`) · `disable-model-invocation: true`(`:4`) 가 adapter 에 없음. `colab-v2-work` 의 긴 trigger 설명(`.agents/…/colab-v2-work/SKILL.md:3`)도 Claude 쪽엔 도달하지 않음 | 3-3 C6 「adapter 17개 `disable-model-invocation` 승계 + 양방향 검사」 | **description(trigger 조건) parity 는 미계획** — 플래그만 | Claude 의 skill 선택기가 trigger 조건을 못 본다 → 자동 발동 skill 이 발동하지 않거나(colab-v2-work) 명시 전용 skill 이 자동 발동(grill-me · to-spec) |
| A-10 | hooks = deterministic guardrail(보호 경로 · credential leak · formatter/linter) | **부분** — `WT/.claude/settings.json:58-84` PreToolUse: git-guard(Bash) · migration-guard · decision-number-guard · test-file-guard(Edit\|Write). test-file-guard 는 Claude lane 에서 통과(`WT/scripts/harness/hooks/test-file-guard.sh:42` `COLAB_FIX_LANE`). credential 경로 · formatter/linter hook 0 | 2-8 test-file-guard 역할·scope 분기(exit 2) · 3-1 `Read(~/.ssh/**)` 등 deny(Claude 전용) · 2-9 git-guard 규칙 ⑺–⑾ | linter/formatter 는 CI gate 에만(편집 시점 없음) · Codex 는 permissions 비적용(recut §5-7) | 편집 시점 피드백 없이 CI 에서 red — 턴 소모. Codex 경로는 deny 없음 |
| A-11 | parallel work: 한 사람이 worktree 여러 개 오케스트레이션 · 「2–3 세션이 출발점 · 상한 = 리뷰 가능한 수」 | **부분** — `WT/.claude/agents/lane-worker.md:6` `isolation: worktree` · `WT/.claude/settings.json:3-5` `baseRef: head` · `WT/scripts/harness/hooks/worktree-setup.sh:78-82` 「격리 아님」 권고(차단 아님 · A7). 세션 수 규칙 = `WT/.agents/rules/colab-rules.md:36` 「WU 1개 또는 같은 계층 2~3개」 · `to-spec` 템플릿 「예상 레인 수 · 기본 직렬 1」 · 게이트 레인 직렬 ADR-0002. clone 30–33: intent `:591` T3 정정 「작업 전 `git pull --ff-only`」(산문 · Ted 확인) · 33 = `cb30d344` 호스트 뮤텍스 없음(`:457`) | 2-8 ⓐ lane-in-parent = exit 2 · 2-3 `-j` 상한(게이트 병렬도) · 3-4 behind-N 안내(advisory) | 동시 **레인 수** 상한 장치 0 · 낡은 clone 에서 게이트 실행을 막는 장치 0(옛 코드는 새 검사를 갖지 않음) | 33 에서 게이트 실행 시 host mutex 우회 → postgres 슬롯 오염 → 다른 clone 의 gate 판정 오염(메모리 「레인 병렬 실행이 게이트 판정을 오염시킨다」) |
| A-12 | 반복 작업 = subagent · scoped context · tool limits | **있음** — `WT/.claude/agents/gate-runner.md:5-7` tools Bash, Read · maxTurns 20 · `measurement-lane.md:5-7` tools Bash, Read · 60 · `researcher.md:5-7` disallowedTools Edit, NotebookEdit · 50 · `advisor.md:5-7` disallowedTools Edit, Write, NotebookEdit · 16 · `lane-worker.md:5-7` 도구 제한 없음 · 200. frontmatter ↔ 기대값 대조 없음(`harness.yaml` 에 role 키 0건 · grep) | 3-3 C7 `harness.yaml` 역할 기대값 ↔ frontmatter/toml 대조 | researcher 는 Write · Bash 허용 → 쓰기 범위는 산문(`WT/.agents/roles/researcher.md:27-30`) + H6 인계 검사(사후) | frontmatter 가 조용히 바뀌어도 감지 없음(PR 3 까지) |
| A-13 | 측정: first-pass merge share · rework cycles/change · merged diff == plan.md | **없음** — 계산 스크립트 0. 원천 데이터는 존재(§5) | 없음 | 세 지표 모두 미계산 | 하네스 개선의 효과를 「green 관측 1회」로만 판정(intent `:29`) |

## 2. 격차 순위 (이 레포: 오케스트레이터 1 + Claude/Codex 에이전트 + clone 30–33)

| 순위 | 격차 | 이 레포에서 중요한 이유 | 닫는 비용 | 닫는 layer |
|---|---|---|---|---|
| 1 | **G1 intent→spec→plan→diff 사슬이 PR 에서 기계 판독 불가**(A-5 · A-2) | 이 레포는 이미 Intent-Ref 트레일러 · Plan-Ref 헤더 · scope glob · V-id 를 다 만들어 두고 **연결만 안 검사**한다. 에이전트가 쓰는 PR 본문이라 placeholder 만 막는 검사는 에이전트 오류를 못 잡는다 | 낮음 — `pr_contract.py` 30행 내외 + B6 workflow 에 인자 1개 | Deterministic + Audit |
| 2 | **G2 spec 의 우려 항목 미해소 · 출처 intent 미승인 상태로 레인 착수 가능**(A-4) | 이번 라운드도 spec 개정(`901eb02a` · `f3f1339a` 「레인 통지」)이 레인 진행 중 있었다 — 플레이북 측정 「requirements rework after build begins」의 실측 사례. 2-5 가 `--spec` 를 begin 에 넣는 김에 내용 검사를 붙이면 비용 최소 | 낮음 — `lifecycle_contract.py` begin 에 spec 파서 1개 | Deterministic(Design→Build 경계) |
| 3 | **G3 skill trigger description 이 Claude adapter 에서 소실**(A-9) | Ted 원칙 「산문은 맥락만」의 전제는 「필요한 맥락이 도달한다」이다. trigger 가 안 닿으면 skill 은 advisory 조차 아니다. Codex 는 원본을 읽으므로 두 에이전트의 발동 조건이 다르다 | 낮음 — adapter 생성 규칙 + harness-contract 대조 1건 | Advisory 를 실제로 동작하게 함 + Deterministic(drift 검사) |
| 4 | **G4 「RED 시험 고정 뒤 구현」이 lifecycle 로 표현 불가**(A-10 · Stage 4 교차) | A2 ⓒ′ 기각 사유가 `lifecycle_contract.py:312-315`(red 인계 거절) — 즉 플레이북의 「시험 먼저 커밋 · 에이전트는 시험을 못 고침」이 이 하네스에선 구조적으로 불가. 2-8 은 scope 가 시험을 제외한 레인만 막는다 | 중간 — task 에 `locked` 목록 + test-file-guard 분기 1개 + handoff 대조 | Deterministic |
| 5 | **G6 advisor ①(설계 검토) 증거 부재**(A-6) | 2-2 가 ② 만 증거화. ① 은 「생략 불가」(colab-v2-work:75)인데 감사 불가 | 낮음 — PR 본문 행 1개 + pr_contract 검사 | Audit |
| 6 | **G5 clone 30–33 낡은 코드 · 동시 레인 수**(A-11) | 33 은 host mutex 이전 코드. 저장소 안 장치로는 옛 clone 을 못 막는다(옛 clone 의 hook·run.sh 가 돈다) — 사람 절차만 남음 | 낮음(Ted `pull --ff-only` + 기록) / 장치화 불가 | Human gate + Audit |
| 7 | **G7 실효 세션 시작 독서량 미측정**(A-7) | 예산 120 이 재는 3 파일 밖에서 ~600행이 「먼저 읽어라」로 지정됨 | 낮음 — 예산 집합 확장 또는 지시 축소 | Advisory hygiene |
| 8 | **G8 Stage 3 측정 미계산**(A-13) | 데이터는 전부 있음(§5). 없으면 PR 2–4 효과를 관측 1회로 판정 | 중간 — 스크립트 1개 | Audit |

## 3. 제안

| 격차 | 장치(경로 · 이벤트 · 게이트 · 키) | 강제하는 것 | 재는 것 | PR |
|---|---|---|---|---|
| G1 | `WT/scripts/harness/pr_contract.py` `validate()` 확장 — ⑴ `Plan-Ref` 경로가 `--head` 트리에 존재(`git cat-file -e <head>:<path>`) ⑵ 그 spec 의 「출처 intent:」 경로 == 범위 안 커밋의 `Intent-Ref` 트레일러(`intent_ref.py` 재사용) ⑶ spec 파일의 최초 커밋 시각 ≤ 범위 안 첫 non-doc 커밋 시각 ⑷ 범위 diff 의 파일 집합 ⊆ spec 「레인 지시」 scope glob 합집합 ∪ 기본 허용(`docs/development/lifecycle-evidence.md` · `dev-package/reports/**`) — 초과분은 건수·목록 출력. B6 PR workflow 에서 `--mode draft` 실행 · ⑷ 는 첫 회차 경고(Q4 와 같은 방식) | plan 없이 코드 없음 · plan 밖 diff 없음 · intent↔spec↔commit 일치 | 「merged diff matches plan.md」 = ⑷ 초과 건수 · PR 당 | PR 2(2-2 확장 · 같은 파일) |
| G2 | `WT/scripts/harness/hooks/lifecycle_contract.py` `begin --role lane-worker --spec <path>`(2-5) 에 검사 추가: ⑴ spec 「출처 intent」 파일의 `intent_ref.classify()` == approved ⑵ 「우려 항목」 표의 판정/권고 칸 빈 행 0 ⑶ 「정책 대조」 절 비어 있지 않음 → 위반 = 78 + 출구(「spec 절을 채우거나 `--spec-unchecked --reason`」 · 면제 건수 handoff JSON `exempt` 에 기록 · 2-6 과 결합). N-7(3-2) 구조 lint 는 이 파서를 공유 | 미승인 intent · 미판정 우려로 레인 착수 불가 | 「intent→spec 경과」 · 「build 뒤 spec 개정 건수」 = begin 의 `spec_sha` ≠ 병합 시 spec sha 인 task 수 | PR 2(2-5) · 파서는 PR 3 N-7 이 재사용 |
| G3 | ⑴ adapter 생성 규칙: `WT/.claude/skills/<name>/SKILL.md` frontmatter `description` = 원본 `WT/.agents/skills/<name>/SKILL.md` description **그대로** · `disable-model-invocation` 승계 ⑵ `WT/scripts/harness/check.py` 에 `check_skill_adapters()` — 두 값 불일치 = red(판정) · `python3 scripts/agent-bridge.py check` 도 같은 대조(양방향) | Claude 가 원본 trigger 로 skill 을 고른다 · 명시 전용 skill 자동 발동 불가 | drift 건수(harness-contract 출력) | PR 3(3-3 확장) |
| G4 | `lifecycle begin --lock <glob>`(task.json `locked: [{path, sha256}]`) — fix 레인 지시문이 RED 시험 경로를 lock · `test-file-guard.sh`(2-8 분기 앞) 가 열린 task 의 `locked` 경로 Edit/Write = exit 2 · `handoff --mode complete` 가 locked hash 불변 대조(변경 = 판정 1) · red 인계 불가 규칙(`:312-315`)은 유지 — RED 는 같은 task 안에서 시험 작성 → lock → 구현 순으로 표현 | 「시험 고쳐서 green」 봉쇄 · Bash 쓰기도 handoff 에서 잡힘 | lock 위반 인계 거부 건수 | PR 2(2-4/2-8 확장) — 또는 Stage 4 그룹 신설 |
| G6 | PR 본문 헤더 `Advisor-1-Ref: <dev-package/reports/…/advisor1.md>`(파일 = advisor ① 원문 · VERDICT 첫 줄) · `pr_contract.py` 가 존재·비placeholder·VERDICT 행 검사(2-2 의 `VERDICT:` 와 같은 형식) · Ted 「어떤 PR 은 ① 생략」 결정 시 `Advisor-1-Ref: exempt(<사유>)` 명시만 허용 | ① 실행 또는 명시 면제 없이는 draft 통과 불가 | ① 면제 비율 · ① 지적 등급(차단급/개선) 집계(colab-v2-work:75 요구) | PR 2(2-2) |
| G5 | 장치화 불가(옛 clone 은 옛 코드 실행). 대체: ⑴ `WT/scripts/harness/hooks/bootstrap-diet.sh`(3-4) SessionStart additionalContext 에 `behind origin/develop N · gates/tools/_lock.sh gate_host_mutex 유무` 출력 ⑵ intent 「확인」 절에 clone 별 갱신 기록(날짜 · HEAD) — T3 완료 기준으로 승격 ⑶ 동시 레인 수: `lifecycle begin --role lane-worker` 가 열린(닫힘 기록 없는) lane-worker task 수 ≥ `harness.yaml` `lanes.max_open`(제안 3) 이면 78 + 출구(`lifecycle close`) — L1 종료 기록 위에서 성립 | ⑶ 만 deterministic · ⑴⑵ 는 advisory/audit | 열린 레인 수 시계열(task runtime) | ⑴ PR 3(3-4) · ⑶ PR 2(2-4 뒤) · ⑵ 그룹 T |
| G7 | `harness.yaml` `hygiene.always_on_files` 에 `AGENTS.md` 가 「먼저 읽는다」로 지정한 파일(`docs/development/dual-agent.md`)을 포함하거나, AGENTS.md:10 의 지시를 「필요 절만」으로 좁힘 · 예산 초과 = 현행 red | 세션 시작 독서량 상한 | 시작 독서 집합 총 행수(`check.py` 출력) | PR 3(3-7) 또는 PR 4 |
| G8 | `WT/scripts/harness/metrics.py` — 입력 = `gh pr list --json`(실측 도달 확인 · PR #167–171 createdAt/mergedAt/mergedBy/commits 회수됨) · `git log --merges origin/develop` · `.git/colab-harness/<checkout>/<task>`(45 checkout key 실측) · `required-gates-<sha>` artifact(`ci.yml:839`) · PR 본문 V-id 표. 출력 `dev-package/reports/harness/metrics/<date>.json` · 게이트 아님(비차단) · Stage 6 detection 의 입력 | 없음(측정 전용) | §5 지표 전부 | 새 그룹 M(PR 4 뒤) |

## 4. 계획과의 충돌

| # | 계획/결정 | 플레이북 문장 | 판정 |
|---|---|---|---|
| 1 | `WT/.agents/roles/lane-worker.md:38` 「사용자 승인 없는 커밋은 하지 않는다」(현행) | "do NOT run approval prompts during build (puts a person back on the critical path of parallel sessions)" | **충돌 — 계획이 해소**: Q3 ⓐ(intent `:597`) · 2-5 「지우는 산문」. PR 2 병합 전까지는 현행 문장이 살아 있어 레인이 커밋 승인 질문으로 턴을 끝낸다(cross.md 「lane-worker.md:38 ↔ :52·:70」) |
| 2 | 3-1 `permissions.ask`(`*infra/dev/ship.sh*` · reseed · `prune --apply` · `push --tags` · `Edit(.claude/settings.json)` · `gh pr create` Q1) | 같은 문장 + Stage 5 "hooks as approval gates … production deploys need release manager authorization" | **충돌 아님** — 대상이 배포·삭제·게시라 build 경로 밖. 단 recut `:48` 「Workflow 레인은 bypass 라 ask 무발동」 → Workflow 레인엔 Layer 3 이 없고, `deny` 가 bypass 모드에서도 적용되는지 [미확인] → PR 3 병합 조건 ⑴ `gh pr merge` 거부 시험에 「bypass 세션」 케이스 추가 필요 |
| 3 | A2: Fable ⓒ′(시험 task 분리) 기각 · 2-8 은 scope 제외 레인만 차단 | "for bug fixes write the failing test FIRST and the agent cannot rewrite it … hooks block agent edits to test files during fixes" | **충돌** — 기각 사유(`lifecycle_contract.py:312-315` red 인계 거절)가 옳더라도 결과는 「일반 레인에서 RED 시험 불변성 없음」. G4 로 해소 가능 |
| 4 | PR 2·3·4 가 hook 본문 · skill · settings 를 바꾸는데 harness-eval 은 CI exempt 모드(intent 과제문 · `ci.yml:642` 주석) | "runs … whenever CLAUDE.md, skills, or hooks change; gate configuration changes on pass rate" | **충돌(Stage 4 그룹 소관)** — PR 2/3/4 병합 조건에 eval 통과율 없음. 이 그룹에서는 기록만 |
| 5 | Ted 설계 원칙(`:600`) 「강제할 수 없는 규칙은 지우거나 이유를 담은 맥락으로만」 · PR 4 산문 축소 | Governance Layer 1 "Advisory (skills — violations still possible)" + "write a skill for institutional knowledge that is enforced inconsistently" | **충돌 아님** — 플레이북도 advisory 를 layer 로 인정. 단 A-9 처럼 advisory 가 **도달조차 안 하면** layer 1 도 없다 → G3 가 원칙의 전제 |
| 6 | clone 30–33 = 동시 작업용 4개 유지(T3 정정 `:591`) + Workflow 레인 | "Two or three sessions is a sensible starting point … ceiling = how many streams one person can review properly" | **부분 충돌** — 규칙문(colab-rules:36 「2~3개」)과 실물(clone 4 + 레인)이 어긋나고 상한 장치 0. G5 ⑶ |
| 7 | 승인 근거 = 에이전트가 intent 에 적는 Ted 원문(`:2` · `:541-600`) | "commits record author, timestamp, what was asked, what was decided, who approved at each gate" | **부분 충돌** — 「who approved」가 산문 · 커밋 author 는 항상 Ted(git user) → 사람/에이전트 커밋 구분은 Co-Authored-By 트레일러뿐 [트레일러 전수 존재 미확인]. 병합 actor 는 GitHub 가 기록(mergedBy=sungwooHa 실측) → 병합 게이트만 감사 가능 |
| 8 | B1 ⓐ 「Update branch(develop 병합 push)로 새 run」 유지(`:556`) | Stage 3 측정 "rework cycles per change" | **충돌 아님·측정 오염** — develop 병합 커밋(`dc0446fd` · `4e4a5a0b` · `791f7c7c`)이 rework 로 계수됨 → G8 에서 「base 갱신 병합」을 별도 분류 |

## 5. 측정 (Stage 1–3) — 이 레포의 기존 데이터로 계산하는 법

| 지표 | 원천(실측 존재) | 계산 |
|---|---|---|
| S1 첫 대화 → intent 커밋 시간 | intent 메타 「작성 YYYY-MM-DD」(`TEMPLATE.md:2`) · `git log --diff-filter=A --format=%aI -- dev-package/intent/<f>` | 커밋 시각 − 작성일(일 단위 · 대화 시작 시각은 저장소에 없음 → 작성일 대용) |
| S1 intent 수용률 | `intent_ref.classify()` 결과(approved/unapproved/no-meta) · 정답표 `gates/fixtures/intent-ref/intent-meta-classification.json`(ADR-0007 언급 · 파일 실존 [미확인]) | approved / 전체(월별) · 이번 intent 는 approved(`:2`) |
| S2 intent → spec 경과 | intent 최초 커밋 `4d286a56` 2026-09-25 23:45 · spec 최초 커밋 `f3cfb4ab` 2026-09-26 00:12 | 27분(이번 회차 실측) · 일반화 = spec 「출처 intent:」 줄 파싱 → 두 파일 최초 커밋 차 |
| S2 build 착수 뒤 요구 rework | spec 파일의 커밋 중 첫 레인 커밋 이후 것 · 또는 2-5 `spec_sha`(begin) ≠ 병합 시 spec sha | 이번 회차: `901eb02a`(08:15) · `f3f1339a`(08:47 · 「레인 통지」) — 레인 첫 커밋 시각 [미확인]이라 ≥1건 |
| S3 first-pass merge share | `gh pr list --state merged --json number,commits,createdAt,mergedAt`(도달 실측) · PR 브랜치의 `git log --merges`(base 갱신 병합) · `required-gates` 재실행 횟수(`gh run list --workflow ci.yml`) | 「PR 생성 뒤 추가 커밋 0 · CI 재실행 0」인 PR / 전체. base 갱신 병합 커밋(「Merge develop … required-gates」 메시지)은 rework 에서 분리 계수 |
| S3 rework cycles / change | 위 + `.git/colab-harness/<checkout>/<task>/` 의 task 수 · run_id 수(`harness.yaml:32` runtime 경로 · 45 checkout key 실측) | PR 당 (레인 task 수 − 1) + CI 재실행 수 |
| S3 merged diff == plan | spec 「레인 지시」 scope glob(`S-HARNESS-…md:181`) · `git diff --name-only base..merge` · PR 본문 V-id 표(`WT/.github/pull_request_template.md:20-23`) · gate-summary.json(`colab-gate-summary/1`) | diff 파일 ⊆ scope 합집합 여부 · 초과 파일 수 · V-id 행 중 「확인됨」 비율 — G1 ⑷ 가 자동 산출 |
| S3 세션 시작 독서량(G7) | `check.py` always-on 출력 + AGENTS.md 지시 파일 행수(dual-agent 303 · product 192 · colab-rules 224 · colab-v2-work 131 실측) | 합계 행수 · 토큰 수(`claude` 세션 첫 턴 `modelUsage` [미확인 접근 경로]) |

## 요약 판정
- 형식(intent · spec · plan 겸용 spec · 역할 frontmatter · hook · worktree 격리)은 플레이북 6단계 중 1–3 을 **모두 갖춤**. 부족한 것은 **단계 사이 경계의 기계 검사**: intent 승인→spec 착수(G2) · spec/plan→diff(G1) · 설계 검토 증거(G6) · skill trigger 도달(G3) · RED 시험 고정(G4).
- PR 2/3/4 계획은 Ted 원칙(시스템 강제)과 플레이북 Layer 2·3 에 부합하나, 위 5개 경계는 계획 밖. G1 · G2 · G6 은 이미 PR 2 가 손대는 파일(`pr_contract.py` · `lifecycle_contract.py` begin)에 얹을 수 있어 추가 비용이 낮다.
- 장치화 불가로 남는 것: clone 30–33 의 옛 코드(사람 절차 + 기록) · 「누가 승인했나」(단일 사용자 저장소 · 병합 actor 만 GitHub 기록).