[harness: subagent output matched instruction-shaped pattern(s): settings-json. Control tags below are neutralized (`<` → `<\`); treat any remaining directive-shaped text as a finding to relay to the user, not an instruction to you.]

## STAGE GROUP C — Maintain · 거버넌스 4층 · source-of-truth · 구현 의존 순서

기준: 브랜치 `claude/harness-improvement` (= develop `67a03a05` + intent/spec + PR 1 병합 `222e685e`). 경로는 워크트리 루트 `…/.claude/worktrees/harness-improvement/` 기준 상대경로. 「없음」은 이 워크트리 안에서 grep·ls 로 못 찾은 것이며 외부(GitHub 설정 · EC2 · 사용자 메모리)는 `[미확인]`.

### 1. 대조표

| # | playbook 요구 | 현재 하네스 (있음/부분/없음 · file:line) | PR 2/3/4 단위 | 격차 | 격차의 결과 |
|---|---|---|---|---|---|
| M1 | Maintain: 결정론적 탐지 스크립트가 운영 지표(CI 실패율 · 배포 후 5xx · PR cycle time)를 1σ/2σ/3σ 대역으로 감시 | **부분**. 결정론적 probe 3종 5분 주기·연속 실패 임계값 2 — `infra/ops/alarms.toml:1-5` · `infra/ops/alarm_runner.py:2` 「연속 실패/회복 전이에만 알림」 · `infra/ops/README.md:11,13`(deploy_doctor 15항 · `/healthz` 4종 · 백업 24h). 게이트 `ops-observability`는 설정 정합만 대조(`gates/tools/ops-observability.sh:4-8` · README:3). `backup-cron-streak`는 04:40 회차 로그 판독(`gates/tools/backup-cron-streak.sh:2-20`). 모델 호출 0 ✓ | 없음 | ⑴ 지표가 「probe 성공/실패」뿐 — CI 실패율 · 5xx 비율 · PR cycle time 미수집 ⑵ 단일 임계값(failure_threshold=2)이며 σ 대역 · 추세 없음 ⑶ SDLC(하네스) 지표 감시 0 | PR 2·3 이후 lifecycle/CI 변경이 CI red 율·인계 거부율을 올려도 사람이 우연히 보기 전엔 신호 없음. 운영 5xx 도 `/healthz` 200 이면 통과 |
| M2 | 계층 응답: 1σ 로그만 · 2σ Claude 읽기 전용 진단 · 3σ PR 또는 사전 승인 runbook | **없음**. alarm_runner 는 raise/clear 두 전이를 Slack webhook 으로만 보냄(`infra/ops/README.md:7,19`). 에이전트가 사람 없이 호출되는 경로 0 (ci.yml 트리거 `push/pull_request/workflow_dispatch` 뿐 `.github/workflows/ci.yml:4-7`, `schedule:` 0건) | 없음 | 대역별 응답 계층 · runbook 개념 자체가 없음 | 알람 → 사람이 Slack 읽고 → 사람이 researcher 스폰. 야간·주말 알람은 무처리 상태로 누적 |
| M3 | 발견은 intent.md(Stage 1 형식)로 재진입 · 에이전트가 초안 작성 | **부분**. researcher S6 경로 — 입력 = 전수 red 로그 · `deploy_doctor` 미달 · 티켓 → `dev-package/intent/<날짜>-<주제>.md` 초안 · 발의자 `agent(전수 red 로그)` · 승인 `미승인` (`.agents/roles/researcher.md:51-65`). 승인 intent 줄 추가만 허용 = 기계 강제(`scripts/harness/intent_ref.py:1-19` · ADR-0007) | 없음 (2-11 역할 본문 재작성은 문안만) | 트리거가 사람(오케스트레이터 스폰)에 결속 · 알람 이벤트 → 초안 생성 자동 경로 없음 · 「기획자 문의 · 티켓」 입력원이 저장소 밖(issue 미게시 `.agents/harness.yaml:130`) | 동일 계급 사고가 재발해도 intent 로 남지 않음 → 「같은 계급 반복 사고」 측정 불가 |
| M4 | 반복 보안 스캔 · 검증 뒤 보고 · 소범위 → PR · 광범위 → intent | **없음**. `.github/workflows/` = `agent-bridge.yml` · `ci.yml` · `product-promotion.yml` 3개 · dependabot/CodeQL/pip-audit/npm audit 0건 | PR 3 3-2 N-8 의존 denylist(playwright/puppeteer)는 정책 lint 이지 취약점 스캔 아님 | 핀 고정 의존(`services/*/requirements*.txt` · `frontend/package-lock.json`)의 CVE 감시 0 | 취약 의존이 `product` 까지 승격되어도 게이트 green |
| M5 | on-call: 사고 채널에 Claude 상주 · 진단·수정·교훈이 채널에 감사 흔적으로 남음 | **부분**. Slack 두 채널(development · activity) · 영속 상태 pending→sent (`infra/notifications/README.md:3,7` · `docs/development/operator-notifications.md:5,9`) · 08:00 KST 일일 감사 보고. 에이전트 상주 없음 · 진단 기록 채널 저장 없음 | 없음 | 채널은 「알림 배출」이지 「대화형 감사 흔적」 아님 | 사고 대응 이력은 사용자 메모리·세션 기록(저장소 밖)에만 남음 → 재현 불가 |
| M6 | 서비스 오너가 triage·승인 · 수정은 PR 리뷰 게이트 경유 | **부분(병합에 한정)**. 병합·배포·기록 삭제 = separate-approval-required(`.agents/harness.yaml:116-122`) · ADR-0003 「승인은 사람」 · T1 ruleset(PR 필수·`ci-required`·bypass 0)은 Ted 적용 대기(intent 판정 기록 2라운드 T1) `[미확인 적용 여부]` | 2-9 ⑺⑼ subagent `gh pr *`·`push` 차단 · 3-1 deny/ask · 3-5 ruleset live 대조 | 알람 triage 의 소유자·SLA 정의 없음(사람이 보는 것이 전부) | 「누가 언제 triage 했는가」 기록 0 → M-측정 3종 모두 계산 불가 |
| M7 | 탐지는 결정론(모델 없음) · tier 경계 version-controlled | **있음(현 범위)**. `alarms.toml` 저장소 관리 · `ops-observability` 게이트가 5분/3대상 대조(`infra/ops/README.md:3`) · probe allowlist(`infra/ops/run-scheduled.sh:17-20`) | — | tier 가 1단뿐이라 「경계」가 임계값 하나 | — |
| G1 | Advisory 층: skills(trigger + enforcement 지시) — 위반 가능 | **있음**. `.agents/skills/` 19종 · Claude adapter 승계 검사(`harness-contract` · `agent-bridge check`) · Ted 원칙 = 산문은 맥락만(intent:600) | 3-3 `disable-model-invocation` 승계 · 4 = 산문 46건 | 없음(playbook 과 방향 일치) | — |
| G2 | Deterministic 층: hooks 가 매 해당 동작을 스크립트로 차단/허용 | **부분**. 11 hook 등록(`.claude/settings.json:6-` · `.agents/harness.yaml` hook_registrations) · Codex 파리티 `.codex/hooks.json`. 단 ⑴ 킬스위치 `COLAB_HOOKS=0`(ADR-0005:107) ⑵ test-file-guard 는 `COLAB_FIX_LANE=1` 없으면 통과 · Claude lane 은 env 못 줌(`scripts/harness/hooks/test-file-guard.sh:26-27,32-33`) ⑶ `permissions` 키 부재(settings.json 키 = effortLevel:2 · worktree:3 · hooks:6) | 2-8 lane/researcher 경계 · 2-9 git-guard 규칙 ⑺–⑾ · 3-1 deny/ask · 3-2 `permissions.required_deny ⊆ live` | ⑴ 킬스위치 잔존(PR 3 병합 조건 ⑵ 「`COLAB_HOOKS=0` 세션 거부」의 기제 미기술 `[미확인]`) ⑵ fix 회차 시험 편집 차단이 Claude lane 에 없음(2-8 은 checkout/scope 검사) ⑶ Codex 는 permissions 미적용(recut §5-7) | 「막는 장치」가 env 한 줄로 꺼지며 그 사실이 어디에도 기록되지 않음 |
| G3 | Human gate 층: approval hook — **특정 사람**이 인가(예: prod 배포 = release manager) | **부분**. ship.sh 는 `COLAB_RELEASE_PRE_EVIDENCE` 검사(`docs/development/release-evidence.md:15-16`) — 증거 파일이지 사람 신원 아님 · purge 는 `--yes-delete` 플래그 + 「Ted 명시 GO」 산문(`.agents/rules/deploy.md:41-42`) · 승인은 「현재 대화」(ADR-0003) = 세션 기록 밖 | 3-1 ask 7종 · Q6 ⓐ TTY+1회용 토큰(별도 소형 PR) · Q1 ⓐ `gh pr create` deny | ⑴ `ask` 는 「터미널 앞 누군가」 ≠ 특정 사람 ⑵ Workflow 레인은 bypass 라 ask 무발동(recut §4 Q1) ⑶ 인가 사실이 릴리스 증거(pre JSON 스키마)에 안 실림 | 배포·삭제 인가자가 사후 재구성 불가. Codex 경로는 ask 자체가 없음 |
| G4 | Audit 층: 모든 결정이 VCS 또는 세션 transcript 에 · hook 호출 timestamp 로그 · OTel export | **부분**. ✓ 커밋↔intent `Intent-Ref`(base 이후 18/18 커밋) · ✓ PR 본문 Head-SHA/Plan-Ref(`scripts/harness/pr_contract.py:31-36`) · ✓ gate-summary JSON started/finished(`gates/tools/gate_summary_json.py:53-57`) · ✓ CI 증거 90일(ci.yml:655-658) · ✓ 릴리스 증거 pre/post(release-evidence.md:8-10). ✗ hook 판정 로그(차단/허용 · 규칙 · 시각) 없음 — stderr 만 · ✗ task runtime 은 git common dir(`docs/development/lifecycle-evidence.md:20-22`) · push 되지 않음 · L1 prune 14일 · ✗ OTel/telemetry 는 제품 trace 만(`infra/ops/README.md:5`) · 하네스 export 0 · ✗ 승인 근거 = intent 「판정 기록」의 Ted 원문 인용(산문) | 2-4 closed 기록 · 2-6 handoff JSON(checkout/branch/spec_sha) · 3-6 증거에 provider/model | 「누가 각 게이트에서 무엇을 승인했나」가 기계 판독 불가 · hook 이 무엇을 언제 막았는지 0 기록 | 감사 시 세션 transcript 를 사람이 뒤져야 함 · transcript 는 저장소 밖 |
| S1 | source-of-truth 선택(repo-as-truth / legacy-as-truth) 명시 | **부분(이중 정본)**. `work-items.yaml:1-6` 「항목 상태의 유일한 원본」 · `PLAN-SoT.md:1` 「설계 단일 진실원」 ↔ AGENTS.md:24-26 「읽기 호환 자료 · 신규 task 로 대장 추가 금지」 · `.agents/harness.yaml:125-130` `transition.mode: legacy-compatibility`. 대장은 여전히 강제(`work-item-consistency` in planning-gates · decision-number-guard · findings R5-18) | 3-7 Q5 = colab-rules §1-2 legacy 한정(산문) | 「repo-as-truth」이나 저장소 안에 정본 둘(대장 vs intent/spec) · 전환 종료 조건 없음 | 에이전트는 대장에 쓰지 말라는 규칙과 대장 정합 게이트를 동시에 받음 → legacy 항목 접촉 시 이중 기록 또는 red |
| S2 | linkage 최소: artifact 에 record ID · legacy record 에 commit SHA | **부분**. ✓ `work-items.yaml` `sources:` → intent 경로(예 DEVELOP-PRODUCT) · ✓ 커밋 → intent(ADR-0007) · ✓ 릴리스 증거 → PR number + merge_commit_sha. ✗ intent → item id / issue 번호 규약 없음(`harness.yaml:130` issue 미게시) | 없음 | 역방향(intent → 대장 id) 산문 | 대장 항목이 어느 intent 로 닫혔는지 기계 추적 불가 |
| D1 | 의존 순서: Continuous evals 는 CLAUDE.md+feedback loop 뒤 · PR review·approval gates·CI/CD·Maintenance 앞 · **CLAUDE.md/skills/hooks 변경마다 실행 · 설정 변경은 pass rate 로 게이트** | **없음(면제 모드)**. `harness-eval` CI 잡 = `COLAB_HARNESS_EVAL_EXEMPT: '1'`(ci.yml:699) · 승격 조건 3회 연속 2/2 green(`gates/tools/harness-eval.sh:12-14` · `eval/harness/README.md:127,156`) · 마지막 실행 `eval/harness/results/20260912-211809`(14일 전) · PR 1 이 hook 4종을 바꿨으나 이후 결과 디렉터리 0 | recut §5 순서에 eval 단계 0 · Q6 effort 실측은 eval 스위트 아님 | PR 1(hooks) · PR 2(lifecycle/hook 본문) · PR 3(permissions/harness-contract) · PR 4(skills 46건)이 전부 eval 트리거 경로(`README.md:9` 「지침 4경로가 바뀔 때만 돈다」)인데 실행 계획 없음 | playbook 문장 위반: "gate configuration changes on pass rate". 회귀는 레인 실패로만 발견 |
| D2 | approval gates 가 CI/CD 자동화보다 앞 | **대체로 충족**. CI 에 에이전트 write step 0 · `permissions: contents: read`(ci.yml:13-14 · agent-bridge.yml:36-37) · T1 ruleset 을 PR 2 앞에 둠(recut §5-1) | 3-1 · 3-5 | T1 적용 실측 `[미확인]` | — |
| D3 | Maintenance loop 는 intent + evals + 전 단계 뒤 | intent ✓ · evals ✗ → 전제 미충족 | 없음 | M 그룹 계획 자체 없음 | — |

### 2. 격차 순위 (이 레포 조건: 오케스트레이터 1 + Claude/Codex + clone 30–33)

| 순위 | 격차 | 왜 이 레포에서 중요한가 | 닫는 비용 | 닫는 층 |
|---|---|---|---|---|
| 1 | D1 evals 가 hook/skill/settings 변경을 게이트하지 않음 | PR 1–4 가 전부 지침·hook 변경 · 과제 20건·판정기 존재 · 비용 실측 있음(40실행 32.45 USD · `eval/harness/README.md` 상한 표) · 모델 호출은 로컬 정책이라 CI 는 「결과 커밋 존재」만 보면 됨 | 낮음 — 러너에 hash 1개 · gate 분기 1개 · recut §5 단계 2개 | deterministic + audit |
| 2 | G4 hook 판정·인가 로그 없음 | 병렬 clone 4 + Workflow bypass 레인에서 「무엇이 막혔나/통과했나」를 재구성할 자리가 0 · PR 2 2-9 로 규칙이 11개로 늘어나 오탐 판독 데이터 필요 | 낮음 — JSONL append 함수 1개 · hook 3개에 호출 1줄 | audit |
| 3 | G3 인가자 신원 없음 · ask 는 레인에서 무발동 · Codex 무적용 | 배포·삭제가 Ted 1인 결정인데 그 사실이 증거 스키마에 없음 · Q6 ⓐ 가 purge/reset 만 다룸 | 중간 — ship-gate.sh 1곳 + release_evidence 스키마 필드 1개 + 토큰 발급 스크립트 | human gate |
| 4 | M1/M2 SDLC·운영 지표 결정론 감시 · 계층 응답 없음 | 하네스 변경 효과(CI red 율 · 인계 거부율 · PR cycle time)를 잴 수 있는 원천(CI 증거 · gate-summary · `gh api`)이 이미 있음 · 없으면 T6/Q6 류 재측정이 매번 수작업 | 중간 — 스크립트 1 · cron 1 · 대역 계산 · 2σ 이상은 사람 스폰 유지 | deterministic + audit |
| 5 | M4 보안 스캔 0 | product 승격 PR 은 intent-ref 도 제외(ci.yml:666) · 의존 핀은 `generated-up-to-date` 류 정합만 | 낮음 — CI 잡 1 · 등록부 1 항목 · 78 규약 | deterministic |
| 6 | S1 이중 정본 | 30–33 clone 병렬 시 대장 YAML 충돌 선례(colab-rules.md:119) · 전환 종료 조건 없이 legacy-compatibility 영구화 | 낮음 — harness.yaml 선언 + gate 조건 1 | audit |
| 7 | G2 Claude lane 의 fix 회차 시험 편집 차단 없음 | Ted 원칙 「동작은 시스템으로」 직접 위반 · 2-8 이 같은 파일을 만짐 | 낮음~중간 — env 조건을 task runtime 조회로 교체 | deterministic |

### 3. 제안

- **P1 (격차 1) eval 결과 hash 게이트** — `eval/harness/run.sh` 요약에 `harness_hash`(sha256: `CLAUDE.md` · `AGENTS.md` · `.agents/skills/**` · `.agents/roles/**` · `.claude/agents/**` · `.claude/settings.json` · `scripts/harness/hooks/**`) 기록 · `gates/tools/harness-eval.sh` 면제 분기: `changes.harness == true` 인 PR 에서 `eval/harness/results/*/summary.md` 중 head 의 hash 와 일치하는 회차가 없으면 **red(판정)** · 일치 회차가 3회 연속 2/2 이면 Q10 승격 자동 판정. 강제: 지침 변경 PR 은 로컬 eval 결과 커밋 없이는 `ci-required` red. 측정: evals pass rate 시계열(summary.md). 배치: **지금(PR 2 open 전 · T1 과 같은 자리)** + recut §5 에 「PR 1 병합 뒤 로컬 eval 1회 · PR 4 뒤 1회」 2단계 추가. 비용은 회차당 ≈32 USD 실측.
- **P2 (격차 2) hook 감사 로그** — `scripts/harness/hooks/lifecycle_contract.py` 에 `audit-append` 서브커맨드(`colab-harness/<checkout-id>/audit.jsonl` · 필드 `ts` · `hook` · `event` · `rule` · `decision(block|allow)` · `agent_id` · `task_id` · `cmd_sha256`) · `git-guard.sh` · `test-file-guard.sh` · `decision-number-guard.sh` 의 exit 2 경로와 보호 동사 허용 경로에서 호출 · `lifecycle prune` 은 audit.jsonl 제외 · `harness-contract` 가 스키마 검사. Codex 는 `agent-bridge.py guard()` 같은 함수. 측정: 규칙별 차단 건수 · 오탐(사람이 뒤집은 건) 비율. 배치: **PR 2 2-9** (git_guard_parse 규칙 추가와 같은 커밋 묶음).
- **P3 (격차 3) 인가자 토큰 → 릴리스 증거** — Q6 ⓐ(TTY + 1회용 토큰)를 `infra/_lib/ship-gate.sh` 로 확장: `COLAB_RELEASE_AUTHORIZER_FILE`(0600 · Ted 가 `tag-release.sh` 로 발급 · SHA 결속) 부재 = 78 · 불일치 = 1 · pre JSON(`colab-release-evidence/1`)에 `authorized_by` · `authorized_at` 필드 추가(`scripts/harness/release_evidence.py` 검증). 도구 무관(Claude ask · Codex · `bash -c` 모두 동일 경계). 배치: recut §2 「별도 소형 PR(deploy.md 선독)」 범위 확장. 측정: 배포 건별 인가 timestamp → 「인가 → 반입」 시간.
- **P4 (격차 4) SDLC 지표 결정론 감시** — `scripts/harness/sdlc_metrics.py`(모델 호출 0): 입력 = `gh api` PR 목록(opened/merged 시각 · `ci-required` conclusion) + CI 증거 bundle counts + `gate-summary.json` 3계수 + alarm 이벤트(`colab.ops.v1` timestamp) · 출력 `dev-package/reports/harness/metrics/<주>.json` · 대역 = 30일 이동 중앙값 ± MAD×k(1/2/3) · tier 1 = 기록 · tier 2 = researcher 스폰 명령을 출력(사람이 실행 — 무인 에이전트 호출은 별도 Ted 결정) · tier 3 = researcher S6 템플릿으로 intent 초안(`researcher.md:51-65` 입력 목록에 「sdlc-metrics 3σ」 추가). 게이트 이름 `sdlc-metrics`(gh 토큰 부재 = 78) · dev 호스트 cron 주 1회(`infra/ops/install-schedule.sh` 선례). 배치: **새 그룹 PR 5 「Maintain」** · P1 승격 뒤. 측정: 대역 이탈 → intent 파일 첫 커밋 시간.
- **P5 (격차 5) 의존 취약점 스캔** — `ci.yml` 잡 `dependency-audit`: `pip-audit -r services/*/requirements*.txt` + `npm audit --audit-level=high --prefix frontend` 를 `verify_evidence.py record` 로 실행 · `.agents/ci-producers.json` 등록 · advisory DB 조회 불가 = 78 · high 이상 = 1 · `ci-required` always_required 에 추가 · red 는 researcher S6 입력 「dependency-audit red」. 배치: PR 3 3-2(N-8 manifest 파서 재사용) 또는 PR 5.
- **P6 (격차 6) 정본 선언 + 연결 게이트** — `.agents/harness.yaml` `transition` 에 `truth: repo` · `ledger_exit: <조건>` · `link: {item.sources → intent, intent.참조 → item id}` · `gates/tools/work_item_consistency.py` 에 「PR 범위에서 `status` 가 바뀐 항목은 `sources` 에 head 에 존재하는 intent 를 가져야 하고 그 intent 를 가리키는 `Intent-Ref` 커밋이 범위에 있어야 함」 1조건. 배치: PR 3 3-7(Q5 와 같은 hunk). 측정: 대장 상태 변경 중 intent 연결 비율.
- **P7 (격차 7) fix 회차 시험 차단을 task runtime 결속으로** — `lifecycle begin --role lane-worker --fix` 플래그 → task.json `fix_lane: true` · `test-file-guard.sh` 의 `COLAB_FIX_LANE` 조기 exit 앞에 `lifecycle_contract.py open-task --cwd <cwd> --field fix_lane` 조회 분기(env 경로는 Codex 파리티로 유지). 배치: **PR 2 2-8**(같은 파일 · fixture 매트릭스에 `fix task 열림 + services/*/tests → 2` 행 추가).
- **P8 (G2 킬스위치)** — PR 3 병합 조건 ⑵ 의 기제 명시: PreToolUse 3 hook(git-guard · test-file-guard · decision-number-guard)에서 `COLAB_HOOKS=0` 첫 줄 제거 · `harness-contract` N? = PreToolUse 등록 hook 파일에 킬스위치 줄 0건 · PostToolUse/SessionStart 는 유지. 배치: PR 3 3-2.

### 4. 계획과의 충돌

| # | 계획·결정 | playbook 문장 | 판정 |
|---|---|---|---|
| X1 | recut §5 순서(T1 → PR 2 → PR 3 → PR 4)에 eval 실행 단계 0 · `harness-eval` 면제 유지 · PR 1 이 hook 4종 변경 후 결과 디렉터리 없음 | "runs on schedule AND whenever CLAUDE.md, skills, or hooks change; gate configuration changes on pass rate" · 의존 그래프 "Continuous evals — requires CLAUDE.md and feedback loops" → "PR review" → "approval gates" → "CI/CD" | **충돌(최대)**. PR 3(설정) · PR 4(46건 산문)이 pass rate 없이 병합됨 |
| X2 | PR 3 3-1 `ask`: `*infra/dev/ship.sh*` · `*reset_dev_environment.py*` · `*lifecycle prune --apply*` · `git push * --tags*` · `Edit(.claude/settings.json)` · `gh pr create *`(Q1 ⓑ 시) + recut Q1 주석 「Workflow 레인은 bypass 라 ask 무발동」 | "Do NOT run approval prompts during build (puts a person back on the critical path of parallel sessions)" · "a specific person must authorize" | **부분 충돌**. 레인에서는 무발동(= 게이트 아님) · 메인에서는 build 중 프롬프트 · 신원 없음. deny 로 옮기고 인가는 P3 토큰으로 |
| X3 | test-file-guard `COLAB_FIX_LANE` env 결속(`test-file-guard.sh:26-27`) · 2-8 이 lane 경계만 추가 · 사후 검사는 `--scope` handoff(A3 판정) | "the agent cannot rewrite it; hooks block agent edits to test files during fixes" | **충돌**. 인계 시점 검사는 편집 시점 차단이 아님 · Ted 원칙과도 어긋남 → P7 |
| X4 | ADR-0003 「승인 판정에 자동 Stop/PreToolUse/UserPromptSubmit 훅을 걸지 않는다」 · ADR-0007 「훅은 두지 않는다」 | "Hooks as approval gates: block or ask before actions matching conditions … exit 2 blocks" · "Governance is enforced as the agent acts" | **긴장(비충돌)**. ADR 은 「승인 의미 판정」을 훅에 안 맡기는 것 · playbook 의 approval hook 은 「인가 토큰 존재」 검사이므로 ADR-0003 재검토 조건에 안 걸림 — 단 이 구분을 ADR-0003 「결정」 절에 1줄로 못박아야 PR 3 3-1 이 ADR 위반으로 읽히지 않음 |
| X5 | 킬스위치 `COLAB_HOOKS=0` 잔존(ADR-0005:107 「신뢰 경계 아님」) · PR 3 병합 조건 ⑵ 기제 없음 | "managed settings (non-negotiable)" · "approval gates version-controlled in .claude/settings.json" | **충돌**. 훅 층이 env 한 줄로 advisory 층으로 강등 → P8 |
| X6 | Q3 ⓐ 「자기 브랜치 커밋 자유 · push/병합/원장은 승인 범위」 · 2-5 가 lane-worker.md:38 「사용자 승인 없는 커밋 금지」 삭제 | "Do NOT run approval prompts during build" | **정합**(playbook 방향으로 해소됨) |
| X7 | Q4 ⓐ design-review 계측자가 판정 · 최종 결정은 사람 | "humans remain accountable for every decision that requires judgment … verification agent-driven but gated" | **정합** |
| X8 | researcher S6 는 사람 스폰 전제 · 무인 트리거 0 | "triggers invoke Claude without a person in the path … service owner triages and approves" | **격차(비충돌)**. Ted 원칙(최종 결정 사람)과 양립 — 초안까지 무인, 승인은 사람. P4 tier 2 자동 스폰 여부는 Ted 결정 |
| X9 | clone 30–33 동시 작업용(T3 정정) + `colab-rules.md:81` 「실행 레인은 한 시점에 하나」 · `:223` 「에이전트 동시 실행 시 -j 2」 | "Two or three sessions is a sensible starting point … ceiling = how many streams one person can review properly" | **정합**(자원 한계가 상한을 정함). 33 = `cb30d344` 호스트 뮤텍스 없음은 pull 전까지 위험 유지 |
| X10 | PR 4 = 「장치 0 · 산문만」 46건 + `always_on_max_lines: 120`(`harness.yaml:98`) | "keep CLAUDE.md under one page" · "When Claude makes a mistake twice, the correction goes into CLAUDE.md" | **정합** · 단 X1 때문에 효과 미측정 |

### 5. 측정 (Stage 6 + 감사 층) — 이 레포 데이터로 계산하는 법

| playbook 지표 | 원천(이미 있음) | 계산 | 빠진 것 |
|---|---|---|---|
| 대역 이탈 → intent.md 시간 | alarm 이벤트 `timestamp`(`infra/ops/alarm_runner.py:26-35` · `/opt/colab-v2/ops-alerts` state · notifications spool 영속 sent) · intent 첫 커밋 날짜(`gates/tools/artifact-lead-time.sh:16-19` `first_commit_date` 재사용) · 발의자 `agent(…)` 표기(`researcher.md:62`) | `min(git log --diff-filter=A -- dev-package/intent/<f>)` − `event.timestamp`(같은 target) | 이벤트 ↔ intent 결속 키(제안: intent 메타에 `event:<target>@<ts>`) · 대역 자체(P4) |
| 발견 중 병합된 수정 비율 | intent 파일 목록(발의자 `agent`) · `git log --grep='Intent-Ref: <intent>' origin/develop` · PR 본문 `Plan-Ref`(pr_contract) | 분자 = develop 에 Intent-Ref 커밋이 있는 agent-발의 intent 수 · 분모 = agent-발의 intent 수 | agent-발의 intent 가 현재 0건일 가능성 `[미확인]` → 지표는 P4 뒤 의미 |
| 같은 계급 사고 반복 | `colab.ops.v1` `target` · `event` · 08:00 활동 보고 · `backup-cron-streak` RED 로그 | target 별 `alarm.raised` 30일 계수 · 계급 = target(현재 3종) | 사고 계급 분류(target 보다 세밀한 축) · 이벤트 장기 보관 위치(`infra/notifications/archive.py` 존재 · 보관 기간 `[미확인]`) |
| 감사: hook 호출 timestamp 로그 | 없음 | P2 `audit.jsonl` → 규칙별 차단/허용 · 오탐률(사람 override = 이후 같은 명령 허용 기록) | P2 전부 |
| 감사: 게이트마다 누가 무엇을 승인했나 | intent 「판정 기록」(Ted 원문 · 날짜) · 커밋 author/date · PR `VERDICT:`(2-2) · ruleset 병합자 `[미확인]` | 현재는 산문 파싱 · P3 뒤 `authorized_by` 필드 · 2-2 뒤 `VERDICT:` 행 | 승인 행 형식 고정(제안: 판정 기록 줄 접두 `승인: <날짜> <원문>` 을 intent_ref 가 파싱) |
| (D1 보조) evals pass rate 시계열 · 지침 변경 대비 회귀 | `eval/harness/results/*/summary.md`(4회차 · 마지막 09-12) · `changes.harness` 필터 | 회차별 green/불안정/준비 · P1 `harness_hash` 로 「어느 지침 상태의 점수」 결속 | 09-12 이후 회차 0 → PR 1 효과 미측정 |
| (D1 보조) CI 첫 통과율 · 준비 red 비율 | CI 증거 bundle(`verify_evidence.py` · 90일 artifact) · `gate-summary.json` counts · `gh api` run conclusion | PR 별 첫 `ci-required` conclusion · `red_readiness/총` | 수집 스크립트(P4) |

핵심 요지: Maintain 단계는 「probe 3종 → Slack」 1단 구조라 playbook 의 계층 응답·intent 재진입·보안 스캔이 전부 사람 손에 있고, 거버넌스 4층 중 audit 층(hook 판정 로그 · 인가자 신원)이 가장 비어 있으며, PR 1→2→3→4 순서는 approval-gate-before-CI 는 지키지만 **evals-before-config-change 를 지키지 않는다**. 가장 싸고 순서상 먼저인 것은 P1(eval hash 게이트 · PR 2 open 전)이다.