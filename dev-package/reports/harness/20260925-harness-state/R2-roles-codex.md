# R2-roles-codex — agent roles, model tiering, Claude/Codex parity, skills layer
Repo: develop @ 67a03a05. Read-only analysis 2026-09-25. Paths repo-relative.

## 1. Components (status)
| Component | Status | Evidence |
|---|---|---|
| Claude role adapters `.claude/agents/*.md` (frontmatter + 2-line pointer) | implemented (values), tested (pointer body) | `.claude/agents/*.md:1-15`; `scripts/harness/config.py:140-166`; `scripts/tests/test_harness_config.py:92` |
| Shared role bodies `.agents/roles/*.md` (5) | implemented | advisor 49 · gate-runner 40 · lane-worker 74 · measurement-lane 63 · researcher 91 lines |
| Codex role configs `.codex/agents/*.toml` (5) | tested | `scripts/tests/test_agent_bridge.py:35-59`; `scripts/agent-bridge.py:79-92`; spawn evidence `dev-package/reports/harness/20260924-agent-model-tiering/X-codex-smoke.md:99-113` |
| Codex hook bridge `.codex/hooks.json` → `scripts/agent-bridge.py codex-event` | tested (unit), measured once per doc 2026-09-15 | `.codex/hooks.json`; `scripts/agent-bridge.py:246-357`; tests `test_agent_bridge.py:101-217,547`; `docs/development/harness-transition-handoff.md:51-66` |
| `agent-bridge check` (role/skill/hook parity) | tested | `scripts/agent-bridge.py:82-135`; `test_agent_bridge.py:221` |
| Skills: 18 bodies `.agents/skills/*` + 17 Claude adapters `.claude/skills/*` (slack-completion Codex-only) | tested (parity) | `.agents/harness.yaml:63`; `agent-bridge.py:97-111` |
| VENDORED.md upstream pinning | documented | `.agents/skills/VENDORED.md:105-133` (no script references the hashes) |
| Always-on line cap (120) | tested | `.agents/harness.yaml:98-99`; `scripts/harness/check.py:91`; `scripts/harness/config.py:243-268`; `test_harness_config.py:180` |
| Codex model-eval runner | implemented (does not select role/model) | `scripts/codex-harness-eval.py:73` |
| Model tiering intent/spec | approved; C1/X1-X3 implemented; outcome 8 pending | intent `dev-package/intent/2026-09-24-agent-model-tiering.md:24-38`; spec `dev-package/prd/specs/S-AGENT-MODEL-TIERING-20260924.md:11-59` |

## 2. Known-context check
- CONFIRMED: advisor fable/high/maxTurns 16 (`.claude/agents/advisor.md:4-7`); researcher opus/medium/50 (`researcher.md:4-7`); lane-worker opus/high/200 isolation: worktree (`lane-worker.md:4-8`); measurement-lane sonnet/low/60 (`measurement-lane.md:4-7`); gate-runner haiku/low/20 (`gate-runner.md:4-7`). Matches spec C1 table S-AGENT-MODEL-TIERING:12-18.
- Codex: advisor gpt-6-astra/high/read-only; lane-worker gpt-5.6-sol/high; researcher gpt-5.6-sol/medium; measurement-lane gpt-5.6-terra/low; gate-runner gpt-5.6-luna/low. Matches spec X3 (:53-59) and dual-agent.md:113-121. Spec X1 (gpt-6-sol/luna) is the "target" column; those models give HTTP 400 on this account.
- Workflow `agent({agentType:'lane-worker'})` ignoring frontmatter isolation: NOT warned in any repo doc. Only in user memory `workflow-lane-worker-runs-in-parent-worktree.md`. Repo docs claim isolation is automatic (see R2-2).

## 3. Findings
R2-1 drift/medium/implemented — Host gate mutex exists in code (`gates/run.sh:152-191`, `gates/tools/_lock.sh:80`, commits 9ece9104, b549d75d), but `.agents/roles/measurement-lane.md:15-19`, `.codex/agents/measurement-lane.toml:11-12` and `docs/decisions/0002-gate-running-lanes-run-alone.md:12` still say `gates/run.sh` has no cross-process mutex; meanwhile `.agents/roles/lane-worker.md:52` says the host mutex handles gate waiting. Two role bodies contradict each other. ADR-0002's own revisit trigger (:32) has half-fired. User memory `gate-lanes-cannot-run-in-parallel.md` is also stale. Effect: measurement-lane still refuses/pre-checks processes on a premise the code has removed; lane-worker trusts a mutex the ADR says is absent.

R2-2 risk/high/documented — Repo docs state lane isolation is automatic: `.agents/skills/executing-plans/SKILL.md:17` ("lane-worker 는 isolation: worktree(자동)로 뜬다 — 이미 격리돼 있는지 확인만 한다"), `.agents/skills/writing-plans/SKILL.md:166`, `.agents/skills/VENDORED.md:87`, `.agents/skills/design-review/SKILL.md:106`, `.agents/rules/colab-rules.md:62`. Observed 2026-09-25: Workflow `agent()` with agentType lane-worker ran in the parent worktree. The warning exists only in user memory (outside repo); residuals intent H1-H15 does not list it. `.agents/roles/lane-worker.md:27` checks expected HEAD only, not "am I in a non-primary worktree", so a parallel Workflow fan-out can put two writers in one checkout (violates colab-v2-work/SKILL.md:47 "체크아웃 하나에 쓰기 에이전트는 하나").

R2-3 drift/medium/implemented — Explicit-invocation policy not carried to Claude. Bodies `agent-browser`, `apple-design`, `grill-me`, `to-spec` have `disable-model-invocation: true`; 0 of 17 `.claude/skills/*/SKILL.md` adapters do (e.g. `.claude/skills/grill-me/SKILL.md:1-4`). Claude registers the adapter, so these are model-invocable on Claude (this session's available-skills list shows grill-me, to-spec, apple-design, agent-browser). `VENDORED.md:5` claims "전 8종 명시 호출 전용" — false even for bodies (5 superpowers bodies + grilling lack the flag). Codex side keeps `allow_implicit_invocation: false` only for grill-me/to-spec via `agents/openai.yaml` (dual-agent.md:41). `agent-bridge.py:101-111` does not compare invocation flags.

R2-4 gap/medium/implemented — Claude adapter descriptions are generic ("Claude에서 공통 X 절차를 연결한다", e.g. `.claude/skills/verification-before-completion/SKILL.md:3`) while the body carries the trigger text (`.agents/skills/verification-before-completion/SKILL.md:3` "Use when about to claim work is complete…"). Relevance-based auto-triggering on Claude is lost for TDD, verification-before-completion, receiving-code-review. `lane-worker` frontmatter `skills:` (`.claude/agents/lane-worker.md:7`) preloads only the 9-line adapter pointers, so the "injected" skills are one more read away.

R2-5 gap/medium/documented — No automated check on Claude role frontmatter values. `scripts/harness/config.py:158-166` strips frontmatter and compares only the body; `agent-bridge check` (`agent-bridge.py:82-92`) validates only Codex toml; Codex model/effort is asserted by `test_agent_bridge.py:35-59`. Spec test decision C1 is a manual grep (`S-AGENT-MODEL-TIERING-20260924.md:62`). A silent edit of model/effort/maxTurns/isolation/tools in `.claude/agents/*.md` passes every gate — asymmetric with Codex.

R2-6 strength/high/tested — Codex role tiering matches the approved spec and is enforced: `.codex/agents/*.toml` model/effort values = spec X3; `test_role_models_are_explicit_and_do_not_claim_parent_inheritance` (`scripts/tests/test_agent_bridge.py:35`) asserts all 5 roles and the selectable-model set; 5-role spawn record in `X-codex-smoke.md:99-107` (child `turn_context` = toml). Caveat `X-codex-smoke.md:113`: roles were read from the cwd worktree's `.codex/agents`, not the trusted main checkout.

R2-7 strength/medium/implemented — Claude frontmatter = spec C1 exactly (see §2). Behaviour rules from spec C2 landed: `.agents/roles/advisor.md:49` (16 turns, ≤8 tool calls), `.agents/roles/lane-worker.md:52`, `.agents/roles/gate-runner.md:5,8`, `.agents/roles/measurement-lane.md:50`, `.agents/skills/colab-v2-work/SKILL.md:39`, `.agents/skills/design-review/SKILL.md:69`, `dev-package/prd/rounds/R-CODEX-PARITY.md:4`.

R2-8 strength/high/tested — Adapter/body split is enforced, no duplication: `.claude/agents/*.md` and `.claude/rules/*.md` bodies must equal the exact 2-line pointer; Codex toml must name `.agents/roles/<name>.md` (`scripts/harness/config.py:140-172`); `CLAUDE.md` must equal the exact adapter text (`config.py:132-139`). Tests: `test_harness_config.py:27` test_product_adapter_cannot_fork_or_lose_shared_body, `:92` test_rejects_missing_or_forked_rule_and_role_sources.

R2-9 strength/high/tested — Codex has automatic enforcement when trusted, not only explicit guards: `.codex/hooks.json` wires 6 events / 9 entries to `agent-bridge.py codex-event`; fail-closed (`agent-bridge.py:290-293` exit 2 on any error; `:313-314` COLAB_HOOKS=0 → deny); apply_patch paths incl. move destinations are guarded (`test_all_targets_including_move_destination` :101, `test_malformed_codex_event_is_blocked` :547, `test_codex_patch_delete_hits_existing_fix_guard` :537). Live block of git push and migration edit documented 2026-09-15 on main 8da27769 (`harness-transition-handoff.md:51-66`). Parity check `agent-bridge.py:113-133` requires same events/matchers as `.claude/settings.json`.

R2-10 drift/medium/implemented — Codex trust docs are stale vs config: `docs/development/dual-agent.md:70` "5개 이벤트", `:83,:85` "7개 등록 항목" vs `.codex/hooks.json` 6 events / 9 entries (SubagentStart researcher added 2026-09-24, Stop/slack). New definitions need per-PC `/hooks` re-trust (`dual-agent.md:87-89`); residual H10 "미해소(기록 0)" (`dev-package/intent/2026-09-25-harness-design-round-residuals.md:121-122`). No code can detect whether Codex trust is active, so "automatic" Codex enforcement is conditional and unverified after 2026-09-15. `harness-transition-handoff.md:162,175` still say native hook allow/block "미확인", contradicting its own :51-66. `AGENTS.md:39` says hooks/permissions/isolation are not auto-applied in Codex — true for `.claude/*`, but a reader can miss that `.codex/hooks.json` does auto-apply.

R2-11 risk/medium/implemented — Write-restriction parity is uneven and mostly instruction-level. Codex advisor has OS `sandbox_mode = "read-only"` (`.codex/agents/advisor.toml:5`, asserted `agent-bridge.py:91-92`); Claude advisor only disallows Edit/Write/NotebookEdit but keeps Bash (`.claude/agents/advisor.md:6`). Claude researcher allows Write (`.claude/agents/researcher.md:6`); Codex researcher path limit is "an instruction, not a filesystem sandbox guarantee" (`researcher.toml`). gate-runner/measurement-lane "no edit" on both sides = instruction (+Bash). Codex lane-worker gets no isolated checkout: SubagentStart hook says "it does not create an isolated checkout" (`agent-bridge.py:340-342`, `dual-agent.md:94-96`).

R2-12 gap/medium/implemented — Harness model evals do not exercise roles or the current tiering. `scripts/codex-harness-eval.py:73` runs `codex -C <fixture> exec` without `-m`/role → global config (astra). Re-run excluded by Ted 2026-09-25 (spec :51, intent :28). Latest results: Codex 2026-09-09, Claude 2026-09-12 (`eval/harness/results/20260912-211809`, 20 tasks, green 16, fail 4 H14/H15/H16/H18 per commit 355fd9dc) — before #130/#131/#140/#144. AGENTS.md:47 requires model evals with the actual model; none exist for current role configs.

R2-13 pending/low/documented — Intent outcome 8 (post-merge re-measure of limit-hit rates for researcher 50, measurement-lane 60, advisor 16) has no record; `PR-BODY.md:39` marks it follow-up. Needed to validate maxTurns raises (intent :15-19 baseline 58%/40%/30%).

R2-14 drift/low/implemented — `docs/development/dual-agent.md:54` says lane-worker has 4 frontmatter skills; `.claude/agents/lane-worker.md:7` has 5 (colab-ponytail added). `.codex/agents/lane-worker.toml` lists 4 skill bodies (no colab-ponytail); Codex relies on the `ponytail-inject.sh` PostToolUse hook, which needs trust.

R2-15 drift/low/documented — VENDORED.md stale: `:1` "10종" vs `:5`/`:34`/`:50` "8종"; `:98` diff target `.claude/skills/<name>/SKILL.md` is now a 9-line adapter (body in `.agents/skills`). Upstream SHAs `:105-133` and Ponytail body SHA `:13` are not verified by any script (grep for the hash under scripts/gates = 0 hits).

R2-16 gap/low/tested — Line cap covers pointers only: 120-line cap on AGENTS.md(55)/CLAUDE.md(6)/.claude/rules/*.md(5-12). `AGENTS.md:10` mandates reading `docs/development/dual-agent.md` first (300 lines) and CLAUDE.md sends to `.agents/rules/product.md` (192); neither is budgeted. The check is honest but the budget does not measure the per-session read load.

R2-17 gap/low/tested — Skill parity check is one-directional: `agent-bridge.py:101-111` walks bodies → requires adapter; an orphan `.claude/skills/<x>` without body, or a `skills:` name in agent frontmatter that doesn't resolve, is not detected. Current state is clean (17 adapters = 18 bodies − slack-completion; `dual-agent.md:39` matches). `test_all_source_skills_and_hook_matchers_are_checked` (`test_agent_bridge.py:221`) only runs check() on the live repo (no negative fixture for orphan).

R2-18 drift/low/documented — Stale model/turn guidance outside the tiering spec: `.agents/rules/colab-rules.md:25` cites memory `model-roles-fable-advisor.md` that does not exist; `:28` "Sonnet 하강 없이 Fable 로 재시도" for lanes vs lane-worker opus frontmatter; `:62` "P-A … 강제 예정" although frontmatter already has isolation. User memory `subagent-turn-limits-truncate-results.md:3,8-9` still says advisor 12 / researcher 30 (now 16 / 50).

## 4. Open questions
- Does Claude Code's Agent tool (not Workflow agent()) currently honour frontmatter `isolation: worktree` when the caller omits it? Only Workflow agent() was observed failing.
- Is Codex project trust + per-definition `/hooks` trust currently active for the 9 entries on this PC (H10)? Not checkable from code.
- Should `measurement-lane` keep the "one gate lane per host" rule now that run.sh has a host mutex (ADR-0002 revisit condition 2, pg slot keyed by repo root, status unknown)?
- Should adapters carry `disable-model-invocation` and the body's trigger `description`, or should agent-bridge check copy them?
- Residual H8 (Workflow advisor/measurement-lane + schema → StructuredOutput failure) is draft in residuals intent; relevant to the same Workflow agentType behaviour.
