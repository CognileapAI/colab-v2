VERDICT: GO-WITH-CHANGES

- 차단급 · `gates/run.sh:748-779 · ci.yml:448-450,548` — §4.4 ⓐ(`ALL_GATES` 정적 배열 = KNOWN) 로는 CI 가 호출하는 `service-tests-${{ matrix.service }}`(ci.yml:548) · `stage2-markers`/`stage2-markers-selftest`(ci.yml:448-450) 를 판정 못 한다. 단순 라벨 grep(`^  [a-z-]+\)`)에 이 이름들이 안 잡힘 = glob/복합 라벨(`service-tests-*)` 류)로 존재. 배열 멤버십 검사면 required CI 가 78 로 red. 수정: 알려진 gate 판정을 `case` 패턴(글롭 포함)으로 하거나, 「run.sh 의 모든 case 라벨(글롭 포함)이 검사를 통과한다」 시험을 `test_gates_run_args.py` 에 넣고 lane 확인 항목을 「ALL_GATES 밖 이름」에서 「글롭·동적 라벨」로 바꾼다. §11 #3 권고 ⓐ 단독은 불충분.
- 차단급 · `decision-number-guard.sh:60-73` — A6 「origin/develop 부재 → exit 2」가 현재 구조(`:67` 기준 조회가 `:75` 내용 판정 **앞**)에 그대로 들어가면 결정 번호와 무관한 모든 Edit/Write 가 origin/develop 없는 checkout(원격 이름 다른 clone · 오프라인 첫 fetch 전)에서 차단된다. spec 에 「readiness 실패는 payload 가 결정 번호 후보(〈N〉 신규)를 담을 때만 낸다(경로·내용 선별 뒤)」를 명시하고, `test_harness_lifecycle_contract.py:319-327` Read→0 · 비결정 편집→0 가 ref 부재 fixture 에서도 green 인 케이스를 추가.
- 개선 · A6 CI 영향 — decision-number-guard 는 CI 에서 직접 실행되지 않음(ci.yml 에 참조 0 · `fetch-depth: 0` 은 :188,573,671 만). 시험은 temp repo fixture(`update-ref refs/remotes/origin/develop`)라 checkout depth 무관. 무영향 확인됨.
- 개선 · run.sh exit 2 의존 — `returncode == 2` 기대는 전부 hook 시험(`test_agent_bridge.py:418-550` · `test_harness_lifecycle_contract.py:53,57,249,316`) · `test_operator_runtime_cron.py:106` 은 다른 스크립트. CI 의 run.sh 호출은 전부 단일 인자(ci.yml · agent-bridge.yml:61-62). `:971-977` 2→78 무영향 확인. 단 `all -j`(인자 2개 · 현행 jobs_n=2 기본) 가 78 로 바뀌는 동작 변화를 `gates/README.md:78` 1줄에 포함.
- 개선 · spec 앵커 정정 — `ALL_GATES` 는 `:284`(spec 「:296-306」 아님). 상단 이동은 안전(사용처 `:705,820,830,896,919,966` 전부 뒤). `selftest)` 구성원 추출(`:688,705`)도 배열만 참조.
- 개선 · exec-bit — `run.sh:749-753` 주석·`gates/tools/exec-bit.sh` 는 `.sh` 인덱스 모드만 검사. `lifecycle_contract.py` 100644 가 현재 green 이므로 비실행 `git_guard_parse.py` 호환. §4.1 「lane 1회 실행」은 유지해도 되나 결과 예측 가능.
- 개선 · §8 scope 누락 후보 — `scripts/tests/test_agent_bridge.py`(A6 「decision 관련 시험 재확인」에서 fixture 에 origin/develop 이 없으면 편집 필요 → 조건부 선언). 나머지 편집 파일(README:73/75/115-117 · gates/README:78 · dual-agent:59-60 · SKILL:101,106 · run.sh:308 · 두 hook · 새 .py · 새 시험 2 · lifecycle_contract 시험)은 scope 목록에 있음.
- 개선 · §8 commit ①·③ 이 `run.sh:308` 을 각각 건드림 — 순차라 충돌 없음. 다만 ③ 에서 시험 수 증가를 PR 「검증」 3계수로 적을 때 ① 뒤 기준값을 따로 기록.
- A7 흡수 가능성(scope) — 대상 3 hook(`researcher-task.sh` · `worktree-setup.sh` · `css-edit-audit.sh`)은 PR 1 파일과 겹치지 않음. 추가 필요: `--scope` 3 hook + `scripts/tests/test_task_runtime.py`(`:398,515,538` 이 평문 stdout 단언 → 변경 필수 · harness-contract 게이트) + 커밋 단위 ⑤ + `dual-agent.md:78-80`(「Claude 는 평문」 문장 교정) + `README.md:72`.
- A7 파일 충돌 — 동일 파일 공유는 `dual-agent.md`(PR 1 :59-60 ↔ A7 :78-80) · `README.md`(PR 1 :73-75,:115-117 ↔ A7 :72 · 인접 hunk). 한 lane 순차 커밋이라 merge 충돌 없음. bridge 는 이미 hook 이 낸 `hookSpecificOutput` JSON 을 본문만 재적재(`dual-agent.md:77` · `test_agent_bridge.py:163-174`) → Codex 경로 무영향.
- A7 완료 기준 보강 — SubagentStart 가 additionalContext 를 존중하는지 문서 미확정. unit test 만으로 닫지 말고 「병합·pull 뒤 researcher 1회 스폰 → 첫 턴에 task id 노출」 스모크를 완료 조건에 넣고, 미존중 시 대안(오케스트레이터가 프롬프트에 task id 명시)을 남은 제약에 기록. hook 이 `${CLAUDE_PROJECT_DIR}` 스크립트를 쓰므로 lane 자체 검증 불가(§6 과 동일).
- §7·§9 — V 표는 §2 V1–V14 전부 대응 · pr_contract draft 필수 절(목적/범위/계획/결정/검증/남은 제약 · Plan-Ref · Head-SHA)이 §9 에 있음. 사실 (4) 의 실패 원인(절 누락·Plan-Ref)은 §9 로 커버. 변경 없음.