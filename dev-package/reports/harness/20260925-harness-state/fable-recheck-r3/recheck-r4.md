### Q6
VERDICT: REVISE
- 최종 권고: ⓐ + ⓑ 유지하되 ⓐ 에 두 조건 추가 — ① `design-review/SKILL.md:65`(항목 7 「메인이 순차로 커밋」) 을 「모든 레인 handoff 뒤 반입·커밋」 으로 함께 고침 ② researcher 본문의 artifact task 개설은 `--agent-id` 없이 researcher 가 직접 `begin --role researcher --artifact runtime:artifacts/<파일>` 실행으로 적음 · ⓑ 에는 「worktree 기준 브랜치 확인(baseRef)」 1줄
- 반박 시도: ⓐ 전제(runtime 이 snapshot 밖)는 버팀 — `lifecycle_contract.py:142–143` `git ls-files` 는 checkout 루트 기준이라 `.git/colab-harness` 는 절대 안 잡힘. ⓒ 기각도 버팀 — `:391–394` 전체 트리 대조가 H6 계약 자체. 무너진 곳 둘: (1) `:384–385` HEAD identity 검사는 mode 와 무관하게 적용 → 현행 §2-2 항목 7 대로 메인이 레인 1 산출을 같은 checkout 에 커밋하면 열려 있는 레인 2 의 handoff 가 거부됨(`colab-v2-work/SKILL.md:37` 이 이미 경고). 항목 5 만 고치는 현 ⓐ 완료 기준(`intent:287`)은 이 충돌을 못 막음. (2) VERIFIED FACT (2): 훅 stdout 이 subagent 에 안 닿으므로 `lifecycle-evidence.md:43` 의 「출력된 agent_id 로 begin」 경로는 Workflow 스폰 researcher 에서 실행 불가 → ⓐ 문서가 그 경로를 전제하면 A6/T1 에 숨은 의존이 생김. `:377` 은 task 에 agent_id 가 있을 때만 대조하므로 `--agent-id` 생략 begin 은 합법.
- 바뀐 점: ⓐ 의 수정 범위를 항목 5 → 항목 5+7(+§2-3 이후 경로 참조) 로 넓히고, artifact task 개설 문장을 훅 출력 비의존으로 고정 · 완료 기준의 재현 1회는 「메인 커밋 없이 researcher 2건 동시 open → 각각 `handoff --mode artifacts` 통과 → 그 뒤 메인 반입·커밋」 순서로 명시. ⓑ 는 메모리 `worktree-baseref-must-match-working-branch` 대로 격리 스폰 시 기준 브랜치 확인을 넣지 않으면 researcher 가 main 기준 낡은 트리를 읽음.
- 위험: 항목 7 개정으로 design-review 취합이 「레인 전부 종료 뒤」로 직렬화돼 벽시계가 늘어남 — 레인 수가 많으면 ⓑ(레인별 worktree)로 넘어가는 기준을 스킬에 적어야 함.

### Q7
VERDICT: KEEP
- 최종 권고: ⓐ — 판정 기준 「첫 와일드카드 앞 구간의 dirname 이 작업 트리에 존재」 · 거부 메시지 출구 2개 = (1) 오타 정정 재-begin (2) 새 디렉터리면 `mkdir` 뒤 begin 또는 부모 glob · 문서 문장은 「scoped task 열린 동안 병합 금지 · develop 반영(update branch)은 handoff 뒤 PR 브랜치에서 · task 중 병합이 불가피하면 병합 뒤 새 task」 순서로
- 반박 시도: 검사 위치 버팀 — `:217` begin 에서 `check_scope_declarations(scope, root)` 로 root 전달됨. 새 디렉터리 거부 우려는 출구가 성립 — 빈 디렉터리는 `ls-files --others` 에 안 잡혀 baseline 불변(`:142–143`), `:91` 디렉터리 거절은 와일드카드 없는 glob 에만 적용되므로 `mkdir` 뒤 `newdir/**` 통과. ⓑ 무너짐 확인 — 병합 파일은 snapshot(`:406–407`) 과 `diff started..HEAD`(`:416`) 두 원천에 잡혀 started..HEAD 필터만으로는 못 뺌(judge-g5 와 일치). 숨은 의존 하나 발견: Ted 확정 develop protection 의 strict 는 병합 전 「branch up to date」 를 강제 → develop→PR 브랜치 병합이 정규 절차가 됨(judge-g5 뒤집힐 조건의 절반). 그러나 lane handoff(H7 `:403–427`) 는 HEAD identity 를 안 보고 scope 만 보며, update branch 는 handoff 뒤 PR 단계에서 일어나고 병합 트리는 ci-required 가 다시 검사하므로 「병합은 handoff 뒤」 순서 문장 하나로 흡수됨 — ⓑ 재설계 조건(scoped task 중 병합 월 수 회)은 아직 미충족(2/204 · literal 경로). 더 싼 대안 없음 — ⓒ(문서만)는 오타 glob 1건당 lane 전체 + gate 재실행을 낭비하고 ⓐ 비용은 수 줄 + 시험 1개.
- 위험: scoped task 가 계속 literal 경로만 쓰면 접두 검사 가치가 0 에 수렴 — 다음 회차 scoped task 의 와일드카드 사용 건수를 확인 항목(`intent:29`)에 넣지 않으면 판단 근거가 안 쌓임.