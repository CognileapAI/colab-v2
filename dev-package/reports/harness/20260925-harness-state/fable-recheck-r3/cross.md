CROSS: ISSUES 7
- specB#1 ↔ Q11(run.sh ⓐ) — specB 「차단급: `service-tests-*`·`stage2-markers` 가 ALL_GATES 밖」은 오류. 실측 `gates/run.sh:284-305` ALL_GATES 에 `service-tests-core-api/ai-service/viz-render/pipeline-worker` · `stage2-markers` · `stage2-markers-selftest` 전부 포함, `:649` 는 literal 대안 라벨(`a|b|c|d`)이지 glob 아님 — Q11 ⓐ 유지, specB 이 항목은 「개선: 앵커 `:296-306`→`:284` 정정」으로 강등. 「case 라벨 전수가 KNOWN 통과」 unit 1건은 싸므로 `test_gates_run_args.py` 에 추가해도 됨(spec §4.4 lane 확인 항목 대체).
- Q11(seam 제거) ↔ spec §4.1 `:52`·`:76`·`:78`·`:85`·`:121`·V5b `:140`·§11#2 ⓐ · specA「(d)(e) fault-injection」 — Q11 REVISE 채택 시 7곳이 전부 env seam 을 전제하므로 spec 을 tempdir 복사본 방식으로 일괄 개정해야 함(specA 의 (d)(e) 도 복사본 스텁으로 표현) · 단 specA 「종료 marker 부재 → 폴백」은 Q11 의 「parsed 1행 스텁 통과」 공격을 부분 봉쇄하므로 두 권고는 상보 · 병기.
- specA「adapter 전제 미검증 · 파서 부재 = 상시 폴백」 — 오류. 실측 `.claude/hooks/git-guard.sh` = `exec bash "$(dirname "${BASH_SOURCE[0]}")/../../scripts/harness/hooks/git-guard.sh"` 이므로 본체의 `BASH_SOURCE[0]` 은 `scripts/harness/hooks/` 로 풀림 · 파서 동거 경로 정상. 「시험은 `.claude/hooks/git-guard.sh` 경유로 고정」(spec `:78` 이미 명시)만 남기고 lane 1순위 항목에서 뺌 · 폴백 stderr 관측 1줄은 유지.
- Q2(A7 재구성) ↔ specB「A7 흡수」 ↔ spec `:11`·`:50`(PR 2 공유 파일 면 분리) — Q2 주경로 「lifecycle CLI 로 열린 task 조회·재사용」은 CLI 에 조회가 없으면 `lifecycle_contract.py` 또는 runtime 파일 추가 · specB 흡수안은 `test_task_runtime.py:398,515,538` 수정 필수 → 둘 다 PR 2 공유 파일을 PR 1 에 끌어옴 = Ted 미승인 범위. A7 을 PR 1 에 넣더라도 조건 「`lifecycle_contract.py`·`test_task_runtime.py` 무접촉」 고정 · 충족 불가면 A7 은 PR 2 로 — Ted 판정 항목에 이 조건을 붙여 올림.
- Q2 부경로 ↔ Q3 ④ ↔ Q5 ⑵ — hook 자동 begin 의 운명이 세 갈래(Q2: smoke 실패 시 끔 · Q3: PR 2 에서 끄거나 재사용 · Q5: 유지하고 baseline 상속) · Q5 ⑵ 는 자동 begin 존재를 전제하므로 Q2 의 「끔」 이 발동하면 Q5 ⑵ 대상 소멸, 반대로 유지하면 Q2 「열린 task 조회 → 재사용」은 T_old·T_auto 두 열린 task 중 어느 것을 재사용할지 미정 → PR 2 spec 상세화 때 단일 결정(권고: 자동 begin 유지 + 상속(Q5 ⑵) · researcher 재사용 대상 = 자기 `agent_id` 또는 최신 자동 task 1건 · Q2 「끔」 조항 삭제).
- Q8 ↔ spec §5 `:122`·§9 `:162` — Q8 실측대로 `agent-bridge` gate 가 CI 어디서도 안 돌면 PR 1 신규 시험 2파일(`test_git_guard.py`·`test_gates_run_args.py` · `run.sh:308` 등록)은 B4(PR 2) 전까지 CI 미실행 · §9 「검증 상태: 미검증(CI 뒤 갱신)」 은 CI 로 갱신 불가. PR 1 본문 「남은 제약」에 「agent-bridge 시험은 lane 로컬 실행 로그가 유일 근거 · CI 편입은 B4」 명기 · V1–V5·V10 근거 열 = lane 로그 경로.
- specB#2(A6 readiness 조건부) ↔ V14 `:36`·§4.6 `:115` ↔ Ted 2라운드 A6 「권고대로」 — specB 「결정 번호 후보 payload 에서만 exit 2」는 방향 유지·범위 축소라 Ted 판정과 충돌 없음, 단 V14·`:115`·시험 케이스 「ref 부재 → 2」 문구를 「결정 번호 포함 편집 + ref 부재 → 2 · 비결정 편집 → 0」 2건으로 고쳐야 정합. 부수: Q8 ② 의 intent `:22`·`:304`·`:305` 정정은 spec §10 `:168` 「승인 intent 본문 변경 0 · 줄 추가만」에 따라 편집이 아니라 판정 줄 추가로.

인용 확인 4건: `gates/run.sh:284-305,649`(specB#1 반증) · `.claude/hooks/git-guard.sh` 3행 shim(specA adapter 반증) · `lifecycle_contract.py:350,575,607`(Q4/Q5 `stop()` 경로 일치 · 정합) · `.agents/roles/lane-worker.md:27` 첫 줄 = `git merge --ff-only`(Q3 ① 정합 · Q3 인용은 `.claude/agents/` 가 아닌 `.agents/roles/` 경로로 적어야 함).
충돌 없음 확인: Q1(T1 strict 정합) · Q10 · Q4·Q5 상호(`closed` 2단화 + `stop/close/prune --apply` 규칙 동일 방향) · Q6·Q7·Q9(PR 2/3 · PR 1 파일 무접촉) · specA 나머지(heredoc 미종결·레코드 개행·fixture 모순·V7 grep) 은 라운드 3 판정과 독립.