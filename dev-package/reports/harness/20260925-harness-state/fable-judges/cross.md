CROSS: ISSUES 9
- A2 ⓒ′ · L(handoff 계약) — ⓒ′ 「시험 작성 task → handoff」는 성립 불가: `lifecycle_contract.py:311-315` handoff 는 선언 gate 비어 있으면 거절 · red 있으면 거절, `:335` allow_red 는 measurement-lane 뿐(확인) → RED 시험을 들고 lane-worker task 1 을 닫을 수 없다 — ⓒ′ 를 PR 1 에서 빼거나 「task 1 gate = 새 시험을 돌리지 않는 gate 만 선언」을 명문화
- C3 · B4 · B5 · L6 — C3 사실 오류: `agent-bridge.yml:32` 가 `.github/workflows/ci.yml` 을 path filter 에 포함(확인) → 「ci.yml 만 바꾼 PR 에서 harness-contract 안 돈다」는 거짓이라 C3 ⓑ 기각 근거 소멸(B5 의 check.py drift 대조도 같은 전제로 유효) · B4 ⓐ 채택 시 harness-contract 는 record 생산자가 되어 L6 「ci.yml 은 agent-bridge 를 어디서도 안 돌린다」도 PR 2 안에서 거짓이 된다 — C3 는 ⓑ 재검토, L6·C3 사실란을 B4 ⓐ 이후 상태로 갱신
- B4 ⓐ · L6 ⓐ — B4 가 `agent-bridge` gate 를 ci.yml required 경로에 올리면 lifecycle 시험 60건은 이미 required 에 들어가 L6 selftest 편입은 CI 이중 실행이고 L6 자신의 뒤집힐 조건이 발동 — L6 는 fixture 시험 4종 + 문서만, selftest 편입은 B4 ⓐ 기각 시에만
- T5 · L8 · context §15 — g5·g8 모두 T5 를 「지금」으로 옮겨 고정 순서(T5 = PR 3 뒤)를 깨며, 서로 순서가 반대(L8 「스모크 먼저 · 훅 없으면 T5」 vs T5 「L8 은 재신뢰 뒤에만」) — L8 순서 채택(스모크 → 미발화 시 재신뢰 → 재실행) · context T5 시점을 「PR 2 lane 전 1회 + 정의 변경 PR 뒤 1회」로 개정해 Ted 판정
- B1 ⓒ · T1 ⓐ(strict) · L5 — T1 strict 가 켜지면 B1 ⓒ 는 불필요(B1 자인) · B1 은 「T1 판정 뒤 착수」인데 context 는 T1 적용을 PR 2 뒤로 둬 순환 · L5 ⓐ 는 B1 ⓒ 전제 — T1 strict 여부를 PR 2 phase 착수 전 Ted 결정으로 당기고, strict 면 B1 = ⓐ(문서) 만 · L5 는 문장 흡수
- C5 · A1 · A5 · C2 — decision-number-guard 실패 방향(fail-open→78)·기준 변경은 Ted 분류상 blocking guard(PR 1) 인데 PR 3 에 있고 C2 README:75 가 여기 묶임 · `git-guard.sh:271` 거부 문구 `main|master`(집합은 develop·product 포함 · 확인)는 A1 판정에 없음 — guard 본체·:271 문구는 PR 1 A1/A5 편집에 편입, ruleset JSON·README 만 PR 3
- A5 · C10 — 두 판정이 같은 `git-guard.sh` 머리말·`:90` 삭제를 PR 1/PR 3 양쪽에 두고 문장도 다름(A5 「이 hook 은 exit 1 을 내지 않는다」 vs C10 「exit 1 통과 유지 + envelope 뒤 판정 불가=통과 2단」) — git-guard = PR 1(A1 편집 안, C10 2단 규칙 문장 채택) · 나머지 3 guard + worktree-setup = PR 3(L3 ⓓ 가 PR 2 에서 같은 worktree-setup.sh 본문을 먼저 고침) 을 spec 에 명시
- A2 ⓐ · L2 · L8 — A2 ⓐ 는 PreToolUse payload agent_id == task agent_id 대조(`:517→:532` 훅 payload 경로 확인)로 열린 lane task 를 찾는데, lane task 가 `--agent-id` 로 열리고 그 값이 PreToolUse 값과 같다는 실측이 없고 `test_task_runtime.py:467-475` 는 불일치 통과를 고정(확인) — A2 ⓐ 선결을 「L1 ⓐ」만이 아니라 「L8 스모크 일치 확인」으로 적고, 불일치 시 폴백은 L2 ⓑ 식 checkout 단위 대조
- C5 · T1 — C5 는 ruleset required check 를 `required-gates` 로 전제하나 T1 은 `ci-required`(required-gates 는 always_required 로 흡수·중복) 로 결론 — C5 JSON 값은 `ci-required` 로 확정하고 T1 결론 참조로 통일