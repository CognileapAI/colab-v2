CROSS: ISSUES 10
Q10·미해결4 — 같은 시험의 자리가 둘로 갈린다: Q10 은 `scripts/tests/test_ops_authorization.py` → `agent-bridge`(시스템 python3 · venv 없음 · ship-gate.sh 도 그 파일이 subprocess) · 미해결4 는 `services/core-api/tests/` → `service-tests`(venv) + ship-gate.sh → `dev-reseed-selftest` CASES. Q1 의 「venv 없이 python3 만으로 돈다를 시험이 고정」은 Q10 쪽에만 성립 — 제안: Python 시험 = `scripts/tests` · `agent-bridge`(stdlib 제약 증명 · `harness.yaml:26` required 확인) · ship-gate.sh = `dev-reseed-selftest` CASES(pty 선례 자리) · 총괄 계획 S-auth 행 정정을 spec 항목으로.
Q3·Q5·Q8 — purge 검사 자리 충돌: Q5 「`:129` connect 앞」 vs Q3 지문 = dry-run 산출 바이트(`_counts` `:135` · connect 뒤). 실측: dry-run·실집행 모두 `:129` 접속 → `:135` 계수 → `:150` 분기. 제안: TTY·토큰 존재 검사 = `:129` 앞(부작용 0 · Q8) · 결속 대조 = 계수 뒤 DELETE 앞 · challenge 발급은 `--yes-delete`+TTY+토큰 부재일 때만(플래그 없는 dry-run 은 발급 0 — 에이전트 dry-run 이 nonce 를 오염하지 않게).
Q1·Q5②·미해결7 — reset 도구가 무엇을 검사하는지 셋이 다르다: Q1 「도구는 결속 토큰 일치만 · isatty 0」 · Q5 「전달값→인가 기록 존재 검사 · 직접 호출은 도구 자체 TTY+토큰 경로」(도구 안 isatty 필요 → Q1 과 모순) · 미해결7 「reseed 발급 토큰을 그대로 받는다」. 제안: 도구 입력 = 토큰 env(`docker run -e`) + 마운트된 challenge 디렉터리 · 검증 = sha256(계수‖nonce)==토큰 · isatty 없음 · 직접 호출 런북(`SKILL.md:148`)은 `reseed --from reset` 경유 또는 `docker run -it` 래퍼 중 하나로 확정(「또는」 금지).
Q4·미해결7 — reset ack 불일치는 오늘 `_PRECONDITION=3`(`:567-575` 실측) · Q4 는 인가 거부 전부 77 · 미해결7 은 그 경로를 「확장」으로 그대로 씀. 제안: reseed 경유 불일치를 3 유지(기존 guard 시험 보존)할지 77 로 옮길지 결정 · 77 이면 `test_reset_dev_environment_guard.py` 기대값 변경을 spec 에 명시 · ⑷ 는 stages.sh 자체 검증이 먼저 거부해야 무변.
Q3·미해결7 — s3-apply 토큰: reseed s3 단계 `stages.sh:667` 은 `--phase s3-apply --plan-sha256` 만 · ack 없음(실측). 미해결7 의 `(target,"s3-apply",plan sha256)` 토큰은 reseed 에 두 번째 사람 회차(새 challenge)를 추가 — Q3 「reset = 기존 계수 바이트 무변(토큰 1개)」과 충돌 · 「확장」 범위 초과. 제안: 인가 기록에 planSha256 을 계수 sha 에 체인해 reset 회차 토큰 하나로 s3-apply 까지 덮거나, s3 사람 회차를 명시 추가하고 reseed 문서·시험 변경을 범위에 넣거나 — 택일.
Q2a·Q7 — Q2a′ 「로컬 `~/.local/state` 없음」 vs Q7ⓒ 로컬 `authorizations.jsonl` 필수(쓰기 실패 = 77). purge·reset 은 EC2 컨테이너(`--rm` · root)라 「운영자 기계」가 정의되지 않고 마운트 없이는 항상 77. 제안: jsonl 도 행위 호스트 root 0600 auth 디렉터리(미해결1 nonce 와 같은 backend)에 두어 backend 1개 · 로컬 사본은 폐기 또는 비차단 선택.
Q7·미해결6 — Q7 「doctor 소비는 15/15 별도 판정」 vs 미해결6 「편입 안 함」이라면서 ⑮ 에 AUTHORIZATION.json 정보 줄 추가(= `deploy_doctor.py` 변경 · `DeployReport.line` 형식은 spec 으로 미룸). 제안: doctor 무변경으로 통일(반입 증거는 PR 요약의 ssh 실측 줄) · 정보 줄을 원하면 범위 안 doctor 변경으로 선언하고 형식을 spec 에서 확정.
Q2·Q5③·미해결2 — semantics 묶음 「Q5③ 확정 전 Q2 를 닫지 않는다」 vs mechanism Q2 확신 높음 닫힘 · 미해결2 는 executor pty 중계로 닫되 「실제 dv 계획의 ship.sh 포함 여부는 spec 에서 Ted 가 grep」 — 저장소 밖 사실에 걸린 위장 보류. 제안: spec 착수 전 Ted 가 계획 파일 1개를 확인 · 미포함이면 executor 변경 0 + `DEPLOY.md:536` 정정만으로 축소.
Q1·Q6-1·미해결5 — Q1′ 「토큰 논리는 `human_auth.py` 1벌 · bash 는 TTY·env·호출만」 vs Q6-1/미해결5 「인가 함수 = `infra/_lib/ops-auth.sh`」(논리 보유로 읽힘) · 모듈의 컨테이너 마운트·`ops-bundle.sh` 편입은 「spec」으로 보류. 제안: 「논리 = `human_auth.py` 만 · `ops-auth.sh` = 래퍼」 1줄 명시 · `reset_docker_prefix` `-v` 1줄(`stages.sh:38-46` 현재 `-it`·모듈 마운트 없음 실측)과 purge 런북 마운트를 필수 항목으로.
Q1·Q2 — purge TTY 확보가 「`docker run -it` 필수 또는 호스트 래퍼」로 열려 있고 런북(`DEPLOY.md §6-1` · docstring)에 `-it` 없음 — 「또는」이 남은 보류. 제안: `docker run -it -v <auth dir>` 로 런북 고정 · 호스트 래퍼(두 번째 진입점) 금지 · `-it` 누락 시 토큰 미출력 = 안전 실패임을 시험 문장에 기록.