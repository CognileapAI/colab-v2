### C5
VERDICT: REVISE
- 최종 권고: PR 3 · `github-ruleset.json` 은 손으로 고친 「제안」이 아니라 T1 적용 뒤 실제 상태 기록으로 바꾼다 — `gh api repos/CognileapAI/colab-v2/branches/develop/protection`(GET · git-guard 허용) 출력을 그대로 담거나, ruleset 형식 유지 시 `ref_name` = `refs/heads/develop` · `required_status_checks[].context` = `ci-required` · `strict` 유지 · `pull_request` rule(승인 0) · `bypass_actors` 빈 배열 · `name` 의 「proposal only」 삭제. `release-evidence.md:45` 「미적용」 → 「적용 · 날짜 · T1」, `:43` 「dev/main」 → `develop/product`(`harness.yaml:6-7`). T1 이 PR 3 착수 시점에 미적용이면 폴백 = 제안 형식 retarget + 「미적용」 유지.
- 반박 시도: `github-ruleset.json:2` name 「main PR and required-gates (proposal only)」 · `:6` `refs/heads/main` · `:9` context `required-gates` + strict true(T1 strict 와 이미 일치) — 이름만 `ci-required` 로 바꾸면 되나, T1 완료 기준(intent `:450`)은 「`gh api …/protection` 출력에 이름이 보이고 C5 JSON 이 같은 내용을 기록」 → 제안 JSON 과 적용 상태가 두 형식(branch protection vs ruleset)으로 갈라져 다시 drift 하는 구조. 적용 상태를 그대로 기록하면 사본이 하나가 된다. `ci.yml:844` job name 은 `pull_request` 에서만 `ci-required`(push 는 `ci-push-summary`) → PR check context 로 `ci-required` 유효. PR 1 겹침 없음(PR 1 은 README·hook·SKILL·dual-agent 만 · spec §4 전수) · README:75 · guard 는 A6 로 이관 완료(spec §4.6 · V14). 흡수 아님.
- 위험: 이 JSON 을 인용하는 시험 유무 미확인(`scripts/tests` grep 못 함 · 예산) — 형식을 바꾸면 lane 이 먼저 grep. PR 3 착수 시 T1 미적용이면 폴백 형식으로 남고 T1 완료 기준의 「같은 내용 기록」은 다시 열린다.

### C6
VERDICT: KEEP
- 최종 권고: PR 3 · ⓐ+ⓒ 그대로 — adapter 4종(`agent-browser`·`apple-design`·`grill-me`·`to-spec`)에 `disable-model-invocation: true` 승계 · `agent-bridge.py:101-111` 검사에 (adapter → 본문 존재) + (플래그 값 동일) 2조건 · `VENDORED.md:5`·`:98` 정정 · description 복사 안 함.
- 반박 시도: 현상 실증 — 이 세션의 skill 목록에 grill-me · to-spec · apple-design · agent-browser 가 모델 호출 후보로 올라 있음(unscoped·scoped 양쪽) → adapter 가 플래그를 안 싣는 것이 실제 동작. 본문 플래그는 `VENDORED.md:57`·`:64`·`:65` 로 확인. 더 싼 대안 검토: (1) adapter 를 symlink 로 — Windows checkout(`scripts/dev.ps1`)·`agent-bridge.py:98-111` 길이 비교 로직과 충돌 → 기각 (2) 본문에서 플래그 제거로 「불일치」 해소 — 명시 호출 정책 자체를 버리는 것 → 기각. 판정 위험 「agent-bridge check 가 path-filter 된 `agent-bridge.yml` 에서만 돈다」(judge-g6 C6)는 B4(PR 2 · `agent-bridge` 를 ci-producers 등록 · `agent-bridge.yml` 폐지 · 판정 기록 `:549`)가 흡수 → PR 3 시점엔 ci.yml 에서 record 로 돈다. PR 1 겹침 없음(`.claude/skills` · `agent-bridge.py:98-111` · VENDORED.md 는 PR 1 대상 밖).
- 위험: adapter 4종이 Claude 자동 목록에서 빠짐 — `design-review` 는 apple-design 을 경로로 읽으므로 무영향(`VENDORED.md:64`) · 완료 기준의 「새 세션 목록에서 4종 제외 관측」은 병합 뒤 오케스트레이터가 system prompt skill 목록으로 확인.

### C7
VERDICT: REVISE
- 최종 권고: PR 3 · Fable ⓐ 채택(Opus ⓑ 기각) — `.agents/harness.yaml` 에 `sources.hook_registrations`(`:50-62`) 와 같은 꼴로 `role_registrations`(가칭) 블록: 역할 5종(`:46`) × {claude: model·maxTurns·isolation·tools·disallowedTools / codex: model·model_reasoning_effort·sandbox_mode} **스칼라 키만** 선언 · `check.py` 가 `.claude/agents/*.md` frontmatter(`key: value` 1행 파서) 와 `.codex/agents/*.toml` 을 대조 · `test_agent_bridge.py:35-59` 의 Codex 하드코딩 dict 는 harness.yaml 을 읽어 단언(선택 가능 모델 집합 단언은 시험에 유지). 값 확정 시점 = PR 2 L7 결과(advisor frontmatter 변경 가능 · 판정 기록 `:564`)와 T 그룹 C9 ⓑ(advisor Bash 제거 여부) 뒤.
- 반박 시도: Opus ⓑ 의 근거 「선언 구조 신설이라 범위가 늘어난다」는 `harness.yaml:50-62` `hook_registrations` 로 반박 — settings.json 의 event·matcher 기대값을 harness.yaml 에 선언하고 check 가 대조하는 패턴이 **이미** 있다(judge-g6 C2 「검사되는 정본 = harness.yaml」). 역할 기대값도 같은 패턴의 두 번째 블록이지 새 구조가 아니다. harness.yaml 은 순수 JSON(`:1-132` 전수) → PyYAML 불요. ⓑ 를 택하면 Claude 기대값이 시험 코드 한 곳 더(Codex dict 와 별개)에 흩어지고 C8·C9 문서가 참조할 정본이 없다. 강제력은 동등(`harness-contract`·`agent-bridge` 둘 다 `gates.required` `:26-27`). PR 1 겹침 없음(check.py·config.py·harness.yaml 은 PR 1 대상 밖 · spec §4 전수) — 단 PR 2 가 B4(check.py 「gates.required ⊆ 등록부」)·L6(`check_gate_parallelism` fixture 시험)로 같은 파일을 먼저 손댐 → PR 3 순차라 병합 뒤 기준.
- 위험: `config.py` 로더가 미지 키를 거부하는지 미확인(`:71-73` 만 인용됨 · 못 읽음) — 거부하면 로더 수정 1건 추가. `lane-worker.md:7` `skills` 는 list 형이라 1행 파서 밖 → 선언 대상에서 제외(문서 `dual-agent.md:54` 「스킬 N개」 수치와 무관하게 둠).

### C8
VERDICT: REVISE
- 최종 권고: PR 3 · ⓑ 유지 + 3건 조정 — ① 완료 grep 패턴 확장: 현행 `[0-9]+개 (이벤트|등록 항목)|스킬 [0-9]+개` 는 `dual-agent.md:70` 「기존 **11개 셸 훅**의 5개 이벤트」 의 앞 수치와 `:39` 「공통 스킬 **17개** … 어댑터 **17개**」 를 못 잡거나(11개) 잡아서 dated 실측 문장을 오탐(17개·「2026-09-25 실측」) → `:70` 은 「`harness.yaml sources.hook_names` 의 훅」 참조로, `:39` 는 이력 표기(날짜 유지)로 grep 제외 규칙 명시 ② 재신뢰 대상 3 commit 목록은 **Codex `.codex/hooks.json` 한정**으로 적고 Claude 는 「이 PC · hook 등록 무변경 → 재신뢰 불요(task 45fad2b0 · e4797bd0)」(판정 기록 `:565` T5) 를 같은 자리에 기록 ③ `lifecycle-evidence.md` measurement-lane 절은 PR 2 로 이관 — L1 종료 기록 2단·prune(`:559`)·L2·L5 가 같은 문서의 역할·인계 절을 다시 쓰므로 그때 `MEASURING_ROLES`(`lifecycle_contract.py:55`)·`allow_red`(`:335`) 문장을 한 번에 넣는다(PR 2 spec 상세화 시 항목 추가). PR 3 의 C8 은 `dual-agent.md` 만.
- 반박 시도: grep 실측 — `dual-agent.md:54` 「스킬 4개」 · `:70` 「11개 셸 훅의 5개 이벤트」 · `:83` 「7개 등록 항목 … 실측했다」(dated · 유지 맞음) · `:85` 「7개 정의를 검토한다」(현재형 · 교정 대상) 확인. ⓐ(수치 교정)로 되돌릴 근거 없음 — 검사기 없는 수치는 3/3 회 미갱신(judge-g7). PR 1 겹침: A2 ⓒ 가 `dual-agent.md:59-60`, A7 이 `:78-80` 을 고침(spec §4.2 · §4.7) → C8 대상 줄(`:54`·`:70`·`:85-89`)과 다른 줄 · 순차라 충돌 없음, PR 3 은 병합 뒤 줄 번호 재확인. 흡수 여부: A7 은 SubagentStart 출력 문장만 → C8 미흡수 · T5 판정은 ②의 입력값.
- 위험: ③ 이관은 PR 2 spec 이 stub 이라 누락 가능 — phase 2 상세화 체크리스트에 「lifecycle-evidence measurement-lane 절(C8 이관)」 1줄을 지금 적어 두지 않으면 PR 3 에서 다시 잡아야 한다.