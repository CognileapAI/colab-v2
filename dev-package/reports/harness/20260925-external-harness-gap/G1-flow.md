# G1 흐름 메커니즘 격차 — 외부 `sungwooHa/ai-sdlc-harness` 대 CoLAB

- 외부 루트 `E` = `/home/ttlhi10/.claude/jobs/d406d55c/tmp/ext/sungwooHa-ai-sdlc-harness-78b2d0f/` (읽기 전용 데이터)
- 우리 경로 = 저장소 루트 상대 경로. 대조 커밋 `ff3498e5`(claude/agent-model-tiering, PR #130 포함 브랜치 HEAD)
- 판정 기호: 있음 / 부분 / 없음. 권고: 채택 / 변형 / 불채택

## (a) 산출물 체인

| # | 외부(무엇 · 언제 발화 · 차단/안내 · 파일:행) | 우리(상태 · 파일:행) | 차이 | 권고 + 이유 | 비용 |
|---|---|---|---|---|---|
| a1 | intent/spec/plan/pr 4종 템플릿. intent 6절(문제·바라는 결과·가치 가설·제약·열린 질문·비범위) `E/docs/changes/_templates/intent.md:6-28`; spec 은 User Stories·Testing Decisions·Agreement Drafts `spec.md:14-39`; plan 은 합의할 결과 표(V1/US1 · 구현 확인 · 사용 후 측정)·DoD 체크 9항 `plan.md:9-49`. 발화 없음(문서 규약) · 안내 | 부분. intent 템플릿 `dev-package/intent/TEMPLATE.md:1-29`(문제·원한 결과·영향 범위·제약·설계트리·미해결·범위 밖·확인), spec 템플릿 `.agents/skills/to-spec/SKILL.md:9,28,55-77`(화면별 토큰 표 포함). plan 템플릿·pr 템플릿 없음. 누적 intent 61개·spec 57개 파일 | 우리 intent 에 「가치 가설」 절이 없음. 우리에게 없는 것: 가치 식별자(V1/US1)가 intent→spec→plan→pr 로 이어지는 연결, 「사용 후 측정할 것」 열. 우리에게만 있는 것: 설계트리(Q/A)·Ted 확인 원문·계약 파괴 여부(Ted 서명) `TEMPLATE.md:10,13-16,21-24` | 변형 — intent 에 `가치 가설` 1절, spec 에 식별자(V1…)만 추가. plan.md 는 불채택: 우리는 라운드 파일 `dev-package/prd/rounds/R-*.md`·advisor ① 이 계획 역할을 이미 함 | 새 파일 0 · 훅 0 · 재신뢰 0 · 템플릿 2개 수술 편집. 기존 61개 intent 소급 불필요 |
| a2 | 변경 폴더 로컬 전용 규칙: `docs/changes/<YYMMDD_NN>-<slug>/` 는 커밋 금지, 이력은 PR 설명란·첨부가 담음 `E/docs/changes/README.md:3-6,35`; `.gitignore:18`; fast guard 가 pre-commit·CI 에서 스테이지된 이력 파일 거절(README:5-6) · 차단 | 없음(반대 정책). intent·spec·보고서를 커밋함: intent README 「승인 = 커밋」 `dev-package/intent/README.md:2`, 「개정 금지」 `:3`; 보고서 `dev-package/reports/harness/20260924-*/` 커밋됨 | 외부는 저장소=규칙만, 이력=PR. 우리는 저장소=규칙+이력(intent·spec·보고서) 이며 intent 커밋 자체가 승인 증거 | 불채택 — 우리 승인 증거(`README.md:2`)와 advisor ② 「intent 대조」(`.agents/skills/colab-v2-work/SKILL.md:61`)가 커밋된 intent 에 의존. PR 게시를 Ted 가 수행(AGENTS.md)하므로 PR 설명란만으로 이력을 넘기면 에이전트가 이력을 읽을 경로가 사라짐 | 채택 시: 기존 흐름 정면 충돌(승인=커밋 규칙 폐기, 118개 파일 이동) |
| a3 | `Plan-Ref: <YYMMDD_NN>-<slug>` 커밋 트레일러 + `E/scripts/harness/check-plan-artifact.sh:24-43`. CI 에서 base 대비 diff 가 `apps/*` 2개 이상 또는 `packages/*` 를 건드리면 트레일러 존재 확인. 기본 경고(exit 0) `:14,38-42`, `PLAN_GATE=enforce` 에서 exit 1. 같은 누락 3회 포착 후 enforce 승격 규칙 `:7-9` | 없음. `.github/workflows/{ci,agent-bridge,product-promotion}.yml` 및 `gates/`·`scripts/` 에 `Plan-Ref`·trailer 검사 0건(grep). 연결은 intent 본문 참조와 커밋 메시지 한국어 규칙(`colab-v2-work/SKILL.md:129`)뿐 | 커밋 → intent 역추적이 사람 판독에 의존. 외부는 기계 판독 가능한 1줄로 연결 | 변형 — 트레일러 `Intent-Ref: dev-package/intent/<파일>.md` 로 이름·값을 우리 경로에 맞추고, 조건을 「`services/**` 2개 이상 또는 `contracts/`·스키마 변경」으로 바꿔 ci.yml 에 경고 전용 1 step. 차단 승격은 외부의 3회 규칙을 그대로 차용 | 새 파일 1(검사 스크립트, 약 40행) · 훅 0 · 재신뢰 0 · ci.yml step 1. 충돌: 없음(경고 전용). 참조 대상이 커밋된 파일이라 파일 실존까지 검사 가능 |
| a4 | UI/여러 앱 변경에 「Spec + HTML 초안」: `required_when` 이 설명서(eli7)·mockup·flow·architecture 4종 HTML 을 plan 규모 또는 UI 경로일 때 요구 `E/.agents/harness.yaml:51-58`. 구현 전 초안, 구현 후 실제 결과로 갱신, 합의본은 보존 `E/docs/standards/AGREEMENT_REVIEW.md:9-28,71-74`. Stop 에서 pr-body-gate 가 누락 HTML 차단 `E/scripts/pr-body-gate.py:219-226` | 부분. spec 의 화면별 토큰·캡처 장면 표 `to-spec/SKILL.md:58-77`; 시각 회귀 `frontend/scripts/visual-baseline/scenes.json`(to-spec:72 참조); 완료 보고 HTML 은 Ted 선호(메모리 `ted-reporting-preferences`)이나 구현 **전** 목업·흐름 HTML 요구 규칙 없음 | 외부는 구현 전 시각 초안을 합의 대상에 포함. 우리는 구현 전 합의 대상이 텍스트 spec 뿐이고 시각 검증은 구현 후(visual-baseline·agent-browser) | 변형 — UI spec 에 한해 `mockup.html` 1종만 선택 항목으로 추가(4종 불채택: flow·architecture 는 archify/explain-visually 명시 호출 전용 규칙과 겹침). 차단 훅 불채택 | 새 파일 0 · 훅 0 · spec 템플릿 1절. 충돌: 전역 「스킬 명시 호출 전용」과 겹치지 않게 수기 HTML 로 한정 |

## (b) 합의 흐름

| # | 외부 | 우리 | 차이 | 권고 + 이유 | 비용 |
|---|---|---|---|---|---|
| b1 | 챌린지 기반 승인: `agreement-gate.py prepare` 가 scope·초안 hash 로 64자 challenge 생성 `E/scripts/agreement-gate.py:138-156`. 사용자가 `/<prefix>-agreement approve <challenge>` 를 입력하면 UserPromptSubmit 훅이 처리 `:411-417`. Skill 호출·문서의 「승인」 문자열은 무효 `:320-323`, `AGREEMENT_REVIEW.md:44-45`. 에이전트 셸에서 approve 불가 `:388` · 차단 | 부분. 승인 = intent 「확인」 절에 Ted 원문 인용 `dev-package/intent/TEMPLATE.md:21-24`, 예 `dev-package/intent/2026-09-24-harness-lane-hygiene.md:97-99`; 승인 = 커밋 `intent/README.md:2`. UserPromptSubmit 훅 없음(`.claude/settings.json` hooks 키: SessionStart·SubagentStart·SubagentStop·PreToolUse·PostToolUse) | 우리 승인 기록은 에이전트가 원문을 옮겨 적는 방식이라 출처가 에이전트 필기. 외부는 호스트 훅이 사용자 입력을 직접 관측해 기록(단, 외부도 「읽고 이해했음」은 증명 못 한다고 명시 `AGREEMENT_REVIEW.md:114`) | 불채택(현 단계) — Ted 는 자연어로 승인(「전부 권고대로 할게」)하며 64자 hex 입력을 요구하면 대화 흐름과 충돌. 대안 변형: UserPromptSubmit 훅이 Ted 프롬프트 원문+시각+session_id 를 task runtime 에 기록만(차단 없음) → intent 「확인」 원문 인용의 대조 근거 | 원형 채택: 새 파일 2(gate 522행+snapshot 127행) · 훅 3 이벤트 · 재신뢰 3 · 모든 Edit/Bash 경로에 fail-closed(아래 b3). 변형: 새 파일 1 · 훅 1 · 재신뢰 1 |
| b2 | 합의 리비전 hash 보존: 승인 시 intent/spec/plan/scope.json/HTML 을 `agreements/<NNN>/` 로 복사하고 sha256 manifest `snapshot.json` 기록 `E/scripts/harness/agreement-snapshot.py:61-103`; 동일 리비전 재생성 거절 `:78-79`; 검증 `:36-58`. 승인 후 planning 입력 수정 차단 `agreement-gate.py:335-338`. 개정은 새 리비전 prepare 로 이전 grant 폐기 `AGREEMENT_REVIEW.md:50-53` | 부분. 「승인된 intent 는 고치지 않고 신규 intent 발행」 규칙 `intent/README.md:3`(문서 규칙, 검사기 없음). 커밋 이력이 사실상 hash 보존 역할. 산출물 회수 시 hash 확인은 `colab-v2-work/SKILL.md:41`·`docs/development/lifecycle-evidence.md:82`(task artifact 대상) | 우리 intent 개정 금지는 훅·게이트 없이 규칙만. 단 intent 가 커밋되므로 `git log -- <intent>` 로 사후 검출 가능. 외부는 로컬 전용이라 hash 보존이 필수, 우리는 Git 이 대체 | 변형 — 사전 차단 대신 CI 경고 1줄: 「이미 승인 표기가 있는 `dev-package/intent/*.md` 의 본문이 PR 범위에서 수정되면 경고」. a3 스크립트에 합침 | 새 파일 0(a3 에 병합) · 훅 0 · 재신뢰 0 |
| b3 | 파일 범위 + 검증 기록 강제: PreToolUse 에서 승인 scope 밖 Edit/Write 차단 `agreement-gate.py:340-342`, 복합 셸·비선언 셸 차단 `:348-349,398`, 미지 도구 차단 `:402`. `verify` 가 선언 명령 실행 후 소스 hash 에 종속된 기록 `:212-232`; 소스 변경 시 기록 무효. Stop·pre-commit 에서 scope drift·stale 검증·pr.md 부재·Agreement-Ref 불일치 차단 `:253-274,442-463`, `E/.husky/pre-commit`(check-staged). 상태 없음 = 모든 소스 편집 차단(`approved()` `:174-177`) | 부분. 게이트 증거 hash 대조는 레인 단위 H7(`lifecycle-evidence.md:54,67`: 필수 게이트·3계수·종료코드·검사 중 파일 변경 시 차단). 범위 제한은 워크트리 격리 + 레인 1건 규칙(`.claude/agents` lane-worker). 파일 경로 단위 scope 선언·차단 없음. pre-commit 훅 체계 없음(`.husky`·`.githooks`·pre-commit 설정 부재) | 우리 H7 은 「게이트가 현재 소스에 대해 green 이었는가」를 이미 검사(외부 verify+stale 과 같은 축). 없는 것은 「승인한 파일 목록 밖 수정 차단」. 외부 PreToolUse 는 `;&|><` 포함 셸 전부 차단 `:348` — 우리 레인의 docker·pytest·npm 파이프 사용과 직접 충돌 | 불채택(PreToolUse 원형). 변형 채택 후보: lane `begin` 에 `--scope <glob>` 선언 → handoff `complete` 에서 변경 파일 ⊄ scope 이면 차단(H7 확장). 이유: 사전 셸 차단 없이 사후 drift 만 잡음 | 원형: 훅 3 · 재신뢰 3 · 레인 셸 전면 충돌. 변형: 새 파일 0 · `scripts/agent-bridge.py` 수정 · 훅 0 · 재신뢰 0 |
| b4 | pause/resume 사용자 제어 `agreement-gate.py:418-437`; 합의 대기 상태의 Stop 은 「구현·커밋 차단 유지」 안내 후 통과 `:447-455` | 부분. researcher `handoff --mode draft-return`(미승인 초안 반환) `lifecycle-evidence.md:75`; 「승인 대기와 독립인 작업은 계속」 `AGENTS.md` | 외부는 대기 중 구현을 기계 차단, 우리는 모드 구분만 | 불채택 — b1 원형 미채택 시 대상 상태가 없음 | — |
| b5 | 승인 게이트 위치 = 구현 직전 1회(사용자) | 있음(다른 축). advisor ①②③ `colab-v2-work/SKILL.md:54-66`: ① 대규모 위임 전 계획 공격, ② 산출물 수용 전 intent 대조, ③ 비가역 go/no-go. ① 생략 금지 근거 「6단계 전부 결함, 정정 49건」 `:64` | 외부에 독립 반론 에이전트 게이트 없음(사용자 승인 + 코드 리뷰 `AGREEMENT_REVIEW.md:67-69`). 우리는 사람 승인 전후에 모델 반론 3회 | 유지 — 외부 대비 우리 우위 항목. 채택 대상 없음 | — |

## (c) PR 가치 검토

| # | 외부 | 우리 | 차이 | 권고 + 이유 | 비용 |
|---|---|---|---|---|---|
| c1 | pr.md 템플릿: 한 문장 제목(≤60자)·Plan-Ref·Agreement-Ref·그림·세 상자(합의 기대/실제/근거, ≤3행)·가치 확인(확인됨/부분 확인/미검증/미달 + 지표·담당·시점)·볼 곳(≤3)·증거(≤10줄) `E/docs/changes/_templates/pr.md:1-23`, 상한 `E/.agents/harness.yaml:66-76`; 상태 정의 `AGREEMENT_REVIEW.md:78-90` | 부분. PR-BODY.md 관행 6건(`find dev-package/reports -name PR-BODY*`), 예 `dev-package/reports/harness/20260924-lane-hygiene-review/PR-BODY.md` 57행, 절: 무엇을 모았나·무엇이 바뀌나·검증·미검증·근거·게시 절차·병합 뒤(`:1-53`). 템플릿 파일 없음 | 우리에 「미검증」 절은 있으나 「합의 기대 ↔ 실제」 대응표와 가치 상태 4분류 없음. 우리에게만 있는 절: 게시 절차(사용자)·병합 뒤 재신뢰(`:45-53`) — Ted 가 PR 을 게시하는 구조 반영 | 변형 — `PR-BODY` 템플릿 1개 신설: 세 상자 표(intent 「원한 결과」 행 ↔ 실제 ↔ 근거) + 가치 상태 4분류 + 기존 게시 절차·병합 뒤 절 유지. 행 상한은 적용하지 않음(우리 PR 은 여러 레인 합본) | 새 파일 1(템플릿) · 훅 0 · 재신뢰 0. verification-before-completion 의 intent 대조(`SKILL.md:51-54`)와 같은 입력을 쓰므로 추가 작업량 작음 |
| c2 | `pr-body-gate.py`: PreToolUse Write 로 pr.md 형태 위반 차단(exit 2) `E/scripts/pr-body-gate.py:279-285`; PostToolUse 로 기억 `:288`; Stop 에서 재검증 + 필수 HTML 부재 차단 `:5-11,219-246`. 파싱 오류 시 fail-open `:18`. 형태만 검사, 가치 진위는 미검사 `:13-14` | 없음. `.github/workflows/*.yml`·`gates/`·`scripts/` 에 PR 본문 검사 0건(grep `PR-BODY|pr_body|pull_request.body`). ci.yml 의 pull_request 참조는 base sha 뿐(`:229-245`) | 우리 PR 본문은 형태 검사 없음 | 불채택(훅) — 6건/누적 규모에서 형태 훅 1개당 재신뢰 비용 > 이득. 대신 c1 템플릿 + advisor ② 고정 3항의 intent 대조로 내용 검사. 형태 결함이 3회 반복되면 그때 경고 스크립트(외부 3회 규칙 `check-plan-artifact.sh:7-9`) | 원형: 새 파일 1(315행) · 훅 3 이벤트 · 재신뢰 3 |

## CoLAB 에 중요한 G1 격차 (우선순위)

1. **c1 가치 대응표** — advisor ② 와 verification 의 「intent 대조」(`colab-v2-work/SKILL.md:61`, `verification-before-completion/SKILL.md:51-54`)가 이미 같은 대조를 하는데 결과가 PR-BODY 에 정형으로 남지 않음(`20260924-lane-hygiene-review/PR-BODY.md:36` 「미검증」 자유서술). 템플릿 1개로 기존 산출을 PR 로 옮기는 비용이 최소.
2. **b3 변형(레인 scope 사후 검사)** — lane-worker 200턴 절단 사례에서 원인 (a) 범위 2건(P2a·P2b) `dev-package/intent/2026-09-24-harness-lane-hygiene.md:20`; 선언 범위와 실제 변경을 handoff 에서 대조하면 범위 초과를 기계로 드러냄. H7 에 붙이므로 훅·재신뢰 증가 0.
3. **a3 변형(Intent-Ref 트레일러, 경고 전용)** — 커밋 → intent 역추적을 기계화. b2(승인 intent 수정 경고)를 같은 스크립트에 합침.
4. 불채택 핵심: b1 원형(64자 challenge 승인) · a2(로컬 전용 이력)는 Ted 의 자연어 승인·「승인 = 커밋」(`intent/README.md:2`)과 정면 충돌. b3 원형은 레인의 복합 셸 사용 전면 차단(`agreement-gate.py:348`).
5. 비용 참고: 외부 원형 전부 채택 시 훅 이벤트 추가 = UserPromptSubmit·Stop·PreToolUse(agreement) + PreToolUse/PostToolUse/Stop(pr-body) + pre-commit 체계 신설, 스크립트 약 1,000행(522+127+315+44).
