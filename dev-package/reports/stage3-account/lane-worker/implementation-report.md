# Stage 3 계정 발급 — 로컬 구현·검증 인계

- 작업 사본: `/home/ttlhi10/colab-stage3-plan`, 브랜치 `codex/stage3-plan`.
- 기준 커밋: `c329c32d2f6d469deb12d64f6cb89e60994b1d3d`. 변경은 미커밋이며 push·배포 없음.
- 승인 범위: 서비스 운영자만 생성, 기존 연구실 하나와 교수/연구원 역할 지정, 중복 이메일 거절, 운영자가 초기 비밀번호 전달, 첫 로그인 비밀번호 변경 필수.

## 구현

계정관리 화면과 서버 API, DB 자격정보 저장, 운영자 권한 검사를 구현했다. 기본 역할은 연구원이다. 기존 이메일을 덮어쓰지 않으며 기존 로그인 호환을 유지한다. 초기 비밀번호로 발급한 세션은 비밀번호 변경 전 일반 데이터 및 계정관리 API를 사용할 수 없다. 비밀번호 변경 후 이전 세션과 초기 비밀번호는 거절된다. 운영자 등록은 일반 계정 생성 권한과 분리했다.

## 검증 근거

- [작업 게이트](gate-summary.json): green 14 / red(판정) 0 / red(준비) 0. 부모가 할당 작업 사본에서 task identity·14개 게이트 선언·현재 파일 hash를 함께 대조해 통과했다.
- 서버: 1,082 passed / 0 failed / 0 skipped / 6 deselected. 프론트: 95개 파일, 1,206 tests passed. migration drift 20/20.
- 작업 대장: 180건, 불일치 0건. 파싱 범위 밖 9건은 별도 표시했다.
- 부모 추가 인증 시험: 격리 DB 환경에서 5 passed. 최초 환경 미설정 실행의 준비 오류를 통과로 세지 않았다.
- 실제 브라우저: 운영자 로그인 → 계정 생성 → 새 사용자 로그인 → 비밀번호 변경 강제 → 변경 후 연구실 이동 → 새로고침 → 로그아웃 → 초기 비밀번호 거절 → 새 비밀번호 재로그인을 확인했다. 중복 이메일 거절, 비운영자 관리 API 거절, 비밀번호 변경 전 데이터 API 403과 이전 세션 401도 확인했다. 모의 응답 0건이며 시험 DB만 사용했다.
- [추가 시각 게이트](visual/gate-summary.json): green 1 / red(판정) 0 / red(준비) 0. 계정관리 1페이지, 13px 미만 0건, 대비 4.5 미만 0건, 허용 예외 0개, 라이트·다크 스크린샷 2장. [화면 근거](visual/frontend-visual/index.md).
- [정적 CSS 계측](visual/css_audit.md): 새 account 규칙에서 정적 결함을 발견하지 못했다는 독립 검토 승인. 기존 다른 CSS의 지적은 남아 있으므로 전체 CSS 무결함을 주장하지 않는다.
- 코드 및 RLS 독립 검토 승인. git diff --check 통과.

## 남은 범위와 한계

개발 서버 적용·배포 후 확인이 남아 BO-1은 대장에서 open 상태다. Stage 3의 다른 기능은 이번 구현에 포함하지 않았다. 운영 환경과 운영 데이터 변경은 없다.

서브에이전트 자동 종료 훅은 원본 체크아웃에서 실행돼 할당 워크트리의 작업 기록을 찾지 못하고 exit 2를 반환했다. 이를 통과로 처리하지 않았으며, 부모가 올바른 작업 사본에서 명시적 보고 검증을 수행해 인계를 수용했다.
