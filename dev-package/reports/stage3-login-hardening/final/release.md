# 로그인 신뢰성과 복구 — 구현 및 WSL stage 검증

2026-09-12. 작업 사본 `codex/stage3-staging-deploy`, 기준 HEAD `b63c9e8a0d40`와 미커밋 변경. 코드·계약·DB 구현과 WSL stage 적용 완료. main 병합·커밋·push·AWS 배포 없음.

## 검사 결과

- 단일 최종 실행: **14개 중 13 green / 판정 실패 1 / 준비 실패 0**, 실행 종료코드1. 서버 시험1111 통과·skip0·E2E 제외6(실제 브라우저 검사는 별도 수행).
- 실패는 기존 수용한 `POST /sessions`의 `request-body-wrapped-in-one-of` 1 error/0 warning/0 info와 같다. 검사기·baseline 우회 없음. 공식 verifier 역시 `gate failures remain`으로 종료1. 자동 전체 green 또는 lifecycle complete로 표시하지 않는다.
- gate-summary의 실행 전후 snapshot은 동일하고, 상태 문서 갱신 직전 현재 checkout과도 일치했다. 이후 변경은 본 실행 결과와 대장/인계/계획의 상태 기록이다. release-evidence의 제품 소스350파일 hash와 이미지·HTTPS 번들을 별도로 대조했다. 상태 문서 갱신 뒤 옛 전체 snapshot을 현재 전체 트리 검증이라고 부르지 않는다.
- 실제 브라우저에서 발견한 늦은 종료·저장 실패 토큰 복원·401 탭 전파·다른 계정의 이전 화면 데이터 잔존을 추가 회귀시험과 구현으로 해결했다. 마지막 r5 브라우저에서는 A→B 전환 후 A 자료0, B 자료 표시를 확인했다.

## 의도 및 사양 대조

| 승인 약속 | 구현과 검증 근거 |
|---|---|
| 입력 비밀 제거·두 로그인 형태 | validation privacy/server 시험, strict oneOf 계약; 입력/ctx/msg 반사 제거 |
| 계정 생성→최초 비밀번호 변경 | 격리 운영자 UI 발급, stage 전용 계정 실제 로그인·변경; 새 값+확인, 10~512 코드포인트 |
| 현재 브라우저의 모든 탭·서버 세션 종료 | stage 실제 두 탭 로그인 화면, 해당 bearer401; 별도 브라우저 새로고침 및 bearer200 |
| 고정12시간·최초 변경 원자성 | 서버 경계/동시성 DB 시험; 실제 첫 변경 전후 sessionId/expiresAt 동일. 실제 12시간 대기는 수행하지 않았으며 시계 경계는 격리 시험으로 판정 |
| 작성·업로드 중 계정 전환 차단 | 모든 작성 표면의 dirty/inFlight 등록 시험, 실제 두 탭의 로그아웃 확인·취소 및 다른 계정 폐기 취소/확정 |
| 만료 후 같은 계정 복구 | 실제 브라우저 만료/401 주입 후 초안과 동일 File 참조 유지; 이미 접수된 업로드 상태 조회, 자동 재업로드/등록0 |
| 오프라인 로컬 종료·서버 미확인 | 실제 offline 설정 후 로그인 화면·미확인 안내·큐1; 재접속 뒤 옛 세션401/새 세션200 |
| 변경 응답 유실 수동 복구 | commit 이후 응답 손실과 요청 전 실패를 각각 주입; 자동 재실행0, 새 값/초기값 직접 재로그인 확인 |
| 이전 응답이 새 상태를 침범하지 않음 | 늦은401/logout/me, 저장 실패, 준비 중 inert, 저장 이벤트 선행 읽기 회귀시험과 최종 계정 전환 실측22222222222222222222222 |

승인된 기능 결과의 미달0·초과0. 최소 점검용 복구 화면은 승인 계획의 호환 이미지 요건을 충족하기 위한 운영 산출물이다. 각 경계의 단위·DB 시험과 브라우저 장애 주입을 실제12시간 관찰 또는 모든 오류의 stage 장애 주입으로 확대하지 않는다.

## 배포와 복구

- HTTPS stage `https://www.colab-hydro.com/`; API와 FE만 고유 이미지로 교체, platform0026 적용 및 기존 인증 롤 권한 재적용. 나머지6개 컨테이너 ID 동일, 전체8개 healthy.
- 기존 계정7행·자격1행·운영자1행의 원문 행 일치. 실제 사용자 암호 변경0. 임시 연구실에 전용 비운영자 시험 계정1개만 추가했고 시험 종료 후 활성 세션0, 제품 자료 생성/삭제0. 시험 계정 행은 보존했다.
- stage 실측은 브라우저를 통해 비로그인401/회수401/별도 세션200/비운영자403. 셸 HTTPS 요청은 모두403이라 인증 결과로 사용하지 않았다. 실제 서빙 JS hash는 최종 이미지 번들과 같다.
- stage 백업35+6표의 복원을 대조했고, 배포 직전 migration 버전표를 제외한40표의 최신 데이터가 여전히 같았다. 새 세션 원장의 별도 복원에서는 회수/활성 상태와 제한 권한을 확인했다.
- 전체 FE→호환 점검 FE 실제 이미지 전환: 첫 변경의 sid/만료 유지→오프라인 큐1→재접속 종료204→옛 세션401/별도200. 점검 FE에서도 첫 변경 성공. 서버는 같은 검증된 이미지다.
- **복구 이미지는 자료 작업을 멈추는 점검 화면이며 인증 코드 자체를 이전 버전으로 되돌리지 않는다.** 운영 장애가 발생하지 않아 stage에서 추가 되돌림은 실행하지 않았다. DB downgrade 및 옛 무상태 이미지 복귀 금지.
- 자동배포 watcher cron 한 줄을 원본 보관 후 hold했고 pipeline lock을 잡아 선택 배포했다. main이 ss1 미호환이므로 hold를 유지한다. 백업·기타 정기 작업은 그대로다. 호환 main 릴리스가 준비된 뒤 watcher 재개를 조정한다.
- 기존 발급 다운로드 URL은 별도10분 capability로 만료하며 세션 로그아웃과 동시에 회수되지 않는다. 회수 세션의 신규 티켓 발급은401이다. 앱을 닫으면 종료 재시도를 보장하지 않고 원래12시간 만료가 상한이다.

세부 이미지 ID·소스 hash·검사 계수는 `release-evidence.json`, stage 결과는 인접 `stage-*.json`을 본다. 운영자용 백업·이미지 아카이브·선택 배포/복구 override·cron 원본은 WSL의 보호된 릴리스 디렉터리에 보관했다. 비밀 파일은 저장소 산출물에 포함하지 않는다.

## 후속 디자인 배포와 현재 stage (06:26KST 이후)

다른 디자인 작업이 FE를 `design-st-r5-20260912-c329c32d-dirty`로 교체했다. 위 `index-BA9RpWX4.js`와 r5 FE ID는 본 작업의 배포 직후 증거다. 현재 API는 동일하고 FE는 디자인 버전이며 이를 되돌리지 않았다.

디자인 작업 사본의 세션 저장/조정/종료 큐/작업 보호/최초 변경 코드는 본 작업 최종본과 같다. LoginPage 차이는 ThemeSwitcher 추가다. 현재 실제 HTTPS 번들 `index-YdOxNYzp.js`와 컨테이너 파일 hash가 같고, 현재 화면에서도 두 브라우저 로그인→한 브라우저 두 탭 로그아웃→회수401/별도200 및 별도 브라우저 새로고침 유지를 재검증했다. 세부 결과는 `../closeout/design-coexistence-result.json`을 본다. 디자인 전체의 품질 검사는 이 로그인 작업의 검증 범위에 합산하지 않는다. watcher hold는 유지되며 시험 세션은 다시 모두 종료했다.
