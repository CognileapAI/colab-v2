# dev 재적재 기간·보조입력 처리

근거: 2026-09-16 사용자 승인 및 `.agents/skills/dev-reseed/SKILL.md`의 식생 모델 입력 규칙.
목표: 승인된 5계정과 정본 자료를 날짜·파일역할·계보가 맞게 dev에 재적재한다.
실제 화면 개발과 product 재개는 제외한다.

## 순서와 수용 조건

- [x] 최신 origin/develop 위에 기존 5계정/격자 개선을 충돌 없이 이식한다.
  최신 release-plan, 원격 디스크, 트랜잭션 검사, doctor 종료코드 처리를 유지한다.
- [x] 모든 자료의 기간 입력을 계획 생성과 러너에 연결한다.
  기존 파일/문서 근거와 DEM·Aspect의 사용자 제공 2023-05 월 단위를 사용한다.
  누락·잘못된 기간은 삭제 전에 실패한다. 월을 일 정밀도로 바꾸지 않는다.
- [x] DEM·Aspect는 독립 본체로 등록하고 해당 NDVI Lv.2의 보조입력으로 연결한다.
  기존 공식 API를 사용하고 중복 연결을 만들지 않는다. 저장 기간·설명·연결 역할을 검증한다.
- [x] 실패 시험 후 구현하고 관련 pytest, dev-reseed-selftest, 실제 정본 계획 검사를 실행한다.
  독립 검토 후 부모가 검증한다. 통과/판정 실패/준비 실패를 구분한다.
- [ ] 사용자 게시용 PR 요약·검증 근거를 준비한다. PR 게시는 사용자 담당이다.
  dev 배포는 원격 develop에 포함된 SHA 및 그 SHA의 병합 PR·CI 증거가 필요하다.
- [ ] 조건 충족 후 같은 버전의 보호된 실행 계획으로 준비 검사와 리허설을 수행한다.
  삭제 대상 목록과 입력을 고정하고 advisor go/no-go 후 초기화한다.
- [ ] 계정 5명 로그인/역할/소속, 모든 자료의 등록·기간·파일·계보·미리보기와 배포 사후 검증을 확인한다.

## 시작 시 실제 확인

- origin/develop: `8c063cf2`, dev CURRENT_SHA 및 컨테이너 4개: `857edc15af9b`.
- root HEAD: `51a9c384`. 기존 미커밋 작업이 있어 root를 배포 사본으로 쓰지 않는다.
- 직전 preflight: 9 통과, 2 미달(git dirty, dev-sha), 변경 단계 0.
  해당 결과는 과거 측정이며 새 실행 전 다시 검사한다.
- 구현 작업은 격리 사본에서 수행한다. 삭제·배포는 아직 실행하지 않았다.

## 구현 수용

최신 develop `8c063cf2` 기준 격리 사본에서 구현했다. 검증 근거는 `dev-reseed-period-role-validation.md`다.
기존 관계의 역할이 다르면 자동 삭제·재연결하지 않고 중단한다. 화면에서 역할을 바꾸는 기능은 추후다.
PR 병합·동일 SHA CI와 새 실행 준비 증거가 없으므로 실제 배포·초기화는 아직 진행하지 않았다.
