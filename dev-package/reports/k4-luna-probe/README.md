# K4 골든 12건 — `gpt-5.6-luna` 질의 해석 실측 (2026-09-22 · 해석 절반)

intent: `dev-package/intent/2026-09-22-k4-luna-interpreter-probe.md` (미승인) · 러너: `eval/k4-search/llm_interpreter_probe.py`
task: `24cfc3a8acef4298a9960f451872c9af` (researcher) · 원시 결과: `interp-01.json`

**이 문서는 해석 절반만 담는다.** 검색 절반(해석 결과 → dev D3 조회 → retrieval 판정)은
dev 플랫폼 DB 가 비어 있어(아래 §블로커) 이번 회차에 실행하지 못했다. retrieval pass/fail·정답 순위는 **미실측**이다.

## 무엇을 돌렸나
- 모델 `gpt-5.6-luna` · 제품 `LlmQueryInterpreter` 그대로(`SYSTEM_PROMPT`·`response_format json_object`·고정 seed·timeout 8초) · 파서 `_read` 그대로(세 값만).
- 골든 12문항 × 2회 = 24회 호출. 사전·그래프 확장·API·UI 없음. 제품 코드·설정 무변경.
- 비교 대상 literal = `LiteralInterpreter`(현 dev 운영 모드 `COLAB_AI_QUERY_INTERPRETATION=literal`).

## 결과 요약
| 항목 | 값 | 판정 기준(intent) 대비 |
|---|---|---|
| 응답 읽기 실패 → literal 폴백 | **0 / 24** | — |
| 기능어 혼입(자료·데이터·찾아줘 류) | **0 / 12** 문항 | 기준 0건 충족 |
| 질문에 없는 낱말 날조 | **0 / 12** 문항 | 기준 0건 충족 |
| topic | 12/12 유효값. 식생 7 · 강우 2 · 가뭄 3 | 골든셋에 topic 정답이 없어 정오 판정은 하지 않음 |
| isDataQuery | 12/12 true (010 「없는 산출물」 문항도 true — 프롬프트 지시대로) | — |
| 지연 | 최소 1.42 · 중앙 2.74 · 최대 5.79 초 · 8초 초과 0/24 | 기준(8초 안) 충족 — 단 ⚠ 아래 |
| 결정성 | 2회 terms 집합이 갈린 문항 **4/12** (002·004·007·008) | 기록만(보증 대상 아님) |

⚠ **지연의 단서 하나.** 같은 날 첫 실행(`run-01`, D3 단계에서 실패해 JSON 미보존)에서는 24회 중 **2회가 8초 timeout** 을 넘겨 literal 로 폴백했다(로그 `event=search.interpreter.unreachable exc=TimeoutError`). 두 번째 실행은 0회. 표본 48회 중 2회(4%)가 제품 timeout 을 넘긴 셈이며, 폴백 경로가 설계대로 동작한 것도 함께 확인됐다.

## 문항별 terms — luna 1회차 vs literal
| # | luna terms | literal terms | 차이의 성격 |
|---|---|---|---|
| 001 | 2023년·5월·경기 남부·충청권·식생·월평균·위성 | 2023년·5월·경기·남부와·충청권의·식생·상태를·살펴볼·수·있는·월평균·위성·자료·찾아줘 | 조사·기능어 제거, **복합어 결합** |
| 002 | 2023년 5월·경기 남부·충청권·식물·푸른지·월평균 (2회차: 「식물이 얼마나 푸른지」한 덩이) | … 식물이·얼마나·푸른지·월평균으로·볼·수·있는·자료·찾아줘 | 서술구를 어디까지 한 덩이로 보느냐가 회차마다 다름 |
| 003 | 2023년 5월 1일·식생지수·U-Net | 2023년·5월·1일의·식생지수를·U-Net·예측한·자료·찾아줘 | 조사 제거(「식생지수를」→「식생지수」) — literal 은 못 함 |
| 004 | GK2A_NDVI_mean_202305.tif·천리안·(202305) | GK2A_NDVI_mean_202305·tif·만드는·데·쓴·천리안·원자료·찾아줘 | luna 는 파일명을 온전히 유지, literal 은 `.` 에서 자름. **「원자료」를 luna 가 버림** |
| 005 | U-Net·식생지수·지형·토지피복·검증용·식생 | U-Net·식생지수·모델에·쓴·지형·토지피복·보조자료와·검증용·식생자료·찾아줘 | 「지형·토지피복」을 둘로 나눔(literal 은 한 토큰) |
| 006 | 레이더 반사도·강우 추정·검증·강수 | 레이더·반사도로·만든·강우·추정·결과와·검증에·쓴·강수·자료·찾아줘 | 복합어 결합 |
| 007 | WGS84·연구대상지·15분·누적·강수 (2회차 +잘라 둔·표본) | WGS84·바꾸고·연구대상지에·맞춰·잘라·둔·15분·누적·강수·표본·찾아줘 | 「표본」 포함 여부가 회차마다 다름 |
| 008 | 2000년·2025년·대한민국·시군구·4주·가뭄지수·주간 (2회차: 「2000년부터 2025년까지」·「4주 가뭄지수」) | 2000년부터·2025년까지·대한민국·시군구의·4주·가뭄지수를·주간으로·비교할·자료·찾아줘 | 기간 표현 분해 방식이 회차마다 다름 |
| 009 | 시군구별·주간·SPI-4weeks·SPI | 시군구별·주간·SPI-4weeks·자료·찾아줘·SPEI·말고·SPI·필요해 | **「SPEI 말고」의 부정을 이해해 SPEI 를 뺌** — literal 은 SPEI 를 검색어로 던짐 |
| 010 | U-Net·가뭄지수 | 검색·대상·자료·중·U-Net·예측한·가뭄지수·찾아줘 | 기능어 전부 제거 |
| 011 | 결측률·0%·검증된·2023년·5월·식생 | 결측률이·0%로·검증된·2023년·5월·식생·자료·찾아줘 | 조사 제거 |
| 012 | 100m·직접 관측한·천리안·월평균·NDVI | 100m로·직접·관측한·천리안·월평균·NDVI·자료·찾아줘 | 조사 제거 |

## 검색 절반에서 반드시 재봐야 할 자리 (해석만으로는 판정 불가)
1. **복합어는 D3 에서 구 질의가 된다.** `d3_catalog.py:719` — 낱말마다 `phraseto_tsquery` 를 태우고 끝에 `:*` 를 붙인다. 「경기 남부」는 `'경기' <-> '남부':*` 로 나가 **인접**을 요구한다. literal 의 「경기」「남부와」 두 접두 질의와 매칭 규칙이 다르므로, luna 가 복합어를 낸 문항(001·002·006·007·008·012)의 retrieval 은 해석의 좋고 나쁨과 별개로 갈릴 수 있다. 이건 프롬프트가 아니라 **해석기 ↔ D3 계약**의 문제다.
2. **004 「원자료」 탈락.** 골든 004 는 월평균의 원자료를 찾는 문항이라 「원자료」가 정답을 가르는 낱말일 수 있다. luna 가 이를 기능어로 본 것이 pass/fail 을 바꾸는지 실측이 필요하다.
3. **009 SPEI 제외**는 해석 품질의 명백한 이득이지만, 결과 집합이 실제로 줄어드는지는 자료가 있어야 보인다.

## 블로커 — dev 플랫폼 DB 가 비어 있다 (2026-09-22 확인)
core-api 컨테이너가 보는 RDS `colab_platform` 에서 `d3_dataset` 0건 · `d1_account` 0건 · `d1_lab` 1건 · `/etc/colab/subjects.json` 3바이트(`{}`).
그래서 `golden_baseline.py --mode literal` 도 지금은 `subject absent` 로 준비 실패(78)한다. 2026-09-15 보고서의 「살아 있는 28개 자료」 상태가 아니다.
복구는 제품 데이터를 만드는 작업이라 이 task(researcher·읽기 전용)의 권한 밖이며 Ted 판단 대상이다. 절차는 `.agents/skills/dev-reseed/SKILL.md`.
복구 뒤 실행 명령(같은 러너, `--skip-remote` 만 뺀다):
```
COLAB_DEV_KEY_FILE=~/.config/colab-platform/dev-key.pem ~/.config/colab-platform/with-dev-env.sh \
  python3 eval/k4-search/llm_interpreter_probe.py --repeats 2 --output dev-package/reports/k4-luna-probe/run-02.json
```

## 이 실측이 말하지 않는 것
- luna 가 「충분」한가(결정 ㊷ 잔여 조건)의 최종 답. retrieval 없이는 반쪽이다.
- 프롬프트를 고치면 어떻게 되는가. 제품 프롬프트 그대로만 쟀다.
- Haiku 등 다른 모델과의 비교. Ted 가 「gpt로 하자」로 닫았다.
