# dev 시딩 후 Ted 피드백 2건 조사 (읽기 전용)

- 조사일 2026-09-14 · 근거 커밋 `7446eb7d` · dev 실물 조회 = UI 읽기(계정 `pi@hymets.invalid`) · 코드 변경 0 · 커밋 0.

## Item 1 — 「가뭄 데이터가 없다」 (SPI/SPEI `.gpkg` 등록 불가)

### 결론

- 원인 = **미리보기 분석 실패가 등록을 막는 프런트 결합**이다. 서버 정책과 어긋난다.
- `.gpkg`·GeoPackage·SQLite 는 **코드 전역 0건**이다(`services/**`·`frontend/src/**`·`contracts/**`·`db/**` grep, 매치 0). 벡터 판독기 자체가 없다.

### 근거

- ㉮ 프런트 비활성 조건 — `frontend/src/components/upload/UploadModal.tsx:1527`
  > `disabled={gridReuseBusy || !uploadId || !status?.ready || Boolean(status?.failure) || Boolean(statusIssue) || Boolean(intakeError)}`

  `data-testid="reg-open"`(「다음 →」)이 `status.failure` 하나로 잠긴다.
- ㉯ 업로드 안내 문면 — `frontend/src/components/upload/FileDropCard.tsx:41`
  > `'어떤 포맷이든 올려요 · 같은 확장자면 여러 개를 한 데이터셋으로 묶어요'`

  같은 파일 `:44` = `NOT_PREVIEWABLE_NOTICE = '이 확장자는 지도로 못 그려요'` · `:47-50` `PREVIEWABLE_EXTENSIONS` 14값에 `gpkg` 없음.
- ㉰ 서버 정책은 반대다 — `services/core-api/tests/test_dataset_registration.py:257-268`

  요지: 감지 실패·헤더 실패·미리보기 실패 3종 전부에서 등록 201 을 요구한다.
  > `test_a_failed_pipeline_does_not_block_registration`
  > `f"실패({reason})한 업로드가 등록을 막았다 — 정본 :192 위반이다."`

  정본 근거 = `Policy_업로드와_계보_확정:192` · `P2.md §2-12`·`§2-21`.
  [추론] 프런트 `:1527` 의 `Boolean(status?.failure)` 항은 **정본 :192 를 프런트가 위반**하는 자리다 — 의도된 정책이 아니다.
- ㉱ 분석 경로 — `services/pipeline-worker/src/colab_pipeline/d5/detect.py:112`
  > `return None, None, "알려진 매직바이트 없음"`

  감지 목록 = GRIB·HDF4·HDF5·NetCDF(CDF)·GeoTIFF·NumPy·HSR Binary(`detect.py:96-112`). SQLite 매직 미등재.
  → `services/pipeline-worker/src/colab_pipeline/domains/d5_ingestion.py:400-403`
  > `reason="형식 인식 실패", klass="영구"` / `detail="본체 전건이 알려진 매직바이트가 아니다"`

  계약 열거 `frontend/src/generated/fe-core.ts:4197` `FailureReason` 8값에 `형식 인식 실패` 포함.
- ㉲ 미리보기 판독 대상 — `services/viz-render/src/colab_viz/domains/d7_visualization/readers.py:44-45`

  `SUPPORTED_FORMATS` 7값(NetCDF·Binary·HDF4·GeoTIFF·NumPy·GRIB·HDF5). 벡터 없음.
- ㉳ 「보기만 할게요」 — `frontend/src/components/upload/UploadModal.tsx:874-897` `viewOnly()`

  요지: `createDataset` 을 부르지 않고 접수된 업로드 주소(S-08 미등록 파일 미리보기)로 이동만 한다.
  > `여기서 만드는 사실은 **없다** — 'createDataset' 을 부르지 않고, 이미 접수된 업로드의 주소로 이동할 뿐이다.`

  근거 문서 = 정본 §7.2 전이표. **데이터셋이 안 생기는 것은 사양대로다** — 등록 창구가 아니다.

### 선택지와 비용

| 안 | 변경 | 계약·DB | 비용 | 비고 |
|---|---|---|---|---|
| ① 등록 허용(인식 실패 = 미리보기 없음) | `UploadModal.tsx:1527` 에서 `Boolean(status?.failure)` 항 제거 ＋ `:1357-1364` 실패 배너를 「등록은 됩니다」 문면으로 · 프런트 시험 신규 1건 | **계약 0 · 마이그레이션 0 · 서버 0** | 최소. 서버는 이미 201 을 낸다(㉰) | 정본 :192 와 화면을 일치시킨다 |
| ② gpkg 판독기 추가 | `d5/detect.py` SQLite 매직 ＋ GeoPackage 판정 · `d5/formats.py` · `viz-render/readers.py` 벡터 경로 신설 · `renderableFormats` | 계약 개정 有(포맷 목록) · 마이그레이션 0 | 최대. 래스터 전제(격자·좌표 배열)가 벡터에 없어 `coords.py`·`downsample.py` 재설계 | 벡터 심볼로지가 미정. `POL-021`(타일 서버·바탕 지도 미사용)과 별개 판정 |
| ③ 그대로(형식 미지원 고지) | 문면만 | 0 | 0 | 「어떤 포맷이든 올려요」(㉯)와 실동작이 계속 어긋난다 |

### 권고

- **① 채택.** 근거 = 서버·정본이 이미 ① 이고 프런트만 어긋나 있다(㉰). 비용이 세 안 중 가장 작고 계약 변경이 0 이다.
- ② 는 별도 intent 로 분리한다 — 벡터 미리보기는 `stage 2` 이후 범위 판정 사안이다. **이 조사에서 결정하지 않는다.**

## Item 2 — 「모든 데이터셋 계보가 레벨 2 라고 나온다」

### 결론

- 「레벨」 = **가공 단계 Lv** 이고 접근 레벨·공개 단계가 **아니다**.
- 성격 = **(a) 등록 폼의 사람 입력 칸**이다. 계보 깊이 파생값(b)은 따로 있으나 **표시에서 사람 값이 이긴다.**
- 원인 = 등록 폼 기본값 `Lv2` 를 러너가 바꾸지 않아 26건 전부 `processingLevelUserSet='Lv2'` 로 저장됐다. **제품 계산 버그가 아니다.**

### 근거

- ㉮ 기본값 — `frontend/src/components/upload/axisDict.ts:183`
  > `export const DEFAULT_PROCESSING_LEVEL = 'Lv2';`

  상태 초기화 `frontend/src/components/upload/UploadModal.tsx:206` `useState(DEFAULT_PROCESSING_LEVEL)` · 전송 `:1012` `if (level) out.processingLevelUserSet = level;`
  셀렉트 자리 = `frontend/src/components/upload/RegisterArea.tsx:216`(`PROCESSING_LEVELS` 4값 `Lv0`~`Lv3`).
- ㉯ 파생값 — `services/core-api/src/colab_core/domains/d3_catalog.py:1440-1456` `processing_level()`

  요지: 부모 없으면 0 · 있으면 (주입력 부모 최대 Lv)+1 · 상한 `LV_CAP`.
- ㉰ 표시 규칙(사람 값 우선) — 같은 파일 `:1497-1518` `level_view()`
  > `"processingLevel": derived if human is None else human,`

  화면 쪽 같은 규칙 = `frontend/src/components/common/processingLevel.ts:38-40`.
- ㉱ 러너 — `runner.py` 에 `가공`·`Lv0`~`Lv3`·`UserSet`·`reg-level` 매치 **0건**(`level` 매치 2건은 `:856`·`:935` 의 「자기 Lv 를 넘는 부모」 오류 문구뿐). `upload-plan.json` 에도 가공 단계 필드 없음 — **축 3개를 한 번도 건드리지 않았다.**
- ㉲ dev 실물(2026-09-14 · UI 조회 · 26건)
  - 카탈로그 필터 축자 — `가공 단계` 셀렉트: `Lv0 · 원시 데이터 (0)` · `Lv1 · 기초 전처리 완료 자료 (0)` · **`Lv2 · 도출된 2차 산출물 (26)`** · `Lv3 · 유형화 및 고차 분석 결과 (0)` · `미지정 (0)`
  - 표 열 이름 = `Level`, 셀 축자 = `Lv2`(`frontend/src/components/catalog/CatalogTable.tsx:167` `Lv{displayLevel(row)}`)
  - 상세 3건 축자

    | 데이터셋 | 상세 「가공 단계」 | 불일치 줄 축자 |
    |---|---|---|
    | HSR 레이더 반사도 원자료(루트) | `Lv2` | `고른 가공 단계는 Lv2이고, 연결한 데이터로 계산하면 Lv0이에요. 그대로 두어도 등록돼요.` |
    | hsr_sample(자식 1단) | `Lv2` | `고른 가공 단계는 Lv2이고, 연결한 데이터로 계산하면 Lv1이에요. 그대로 두어도 등록돼요.` |
    | pred_sample(자식 2단) | `Lv2` | 없음(파생도 Lv2 라 일치) |

  - 계보 그래프 노드도 전부 `Lv2` 로 선다(`Lv2 가공 전 HSR 레이더 반사도 원자료 ›` 등).
  - 불일치 줄 출처 = `frontend/src/components/detail/DetailHeader.tsx:135-139`(`data-testid="dh-lv-mismatch"` · 경고만 · 등록·저장을 막지 않는다).
  - 스크린샷 = 러너 scratch `shots/lv-mismatch-root.png`(레포 밖 · 커밋 대상 아님).

### 고치는 길

- ㉳ 러너 쪽 — 데이터셋별 `가공 단계` 셀렉트를 계획값(원본 경로 `Lv.0`/`Lv.1`/`Lv.2`)대로 고르게 한다. `upload-plan.json` 에 가공 단계 필드를 더하고 `RegisterArea` 셀렉트를 조작한다.
- ㉴ 제품 쪽(선택) — `DEFAULT_PROCESSING_LEVEL` 을 `Lv2` 로 둘지 「미지정」으로 둘지가 판정 사안이다. 현재 기본값이 `Lv2` 라 **사람이 안 고른 것과 Lv2 로 고른 것이 구분되지 않는다**(`services/core-api/src/colab_core/app/routes/catalog.py:208` 의 `미지정` 이 영원히 0건이 된다). ⚠ 계약 산문은 「안 골랐으면 `None`」을 전제한다(`d3_catalog.py:1458-1470`).
- ㉵ 기존 26건 정정 가능 여부
  - **UI 로는 불가.** 상세 편집 폼 `frontend/src/components/detail/editFields.ts:85-95` `TEXT_FIELDS` 7칸에 분류·유형·가공 단계 3축이 **없고** `toPatch()`(`:204-229`)도 싣지 않는다.
  - **API 로는 가능.** `PATCH /datasets/{datasetId}`(`services/core-api/src/colab_core/app/routes/catalog.py:1210`)의 `_UPDATE_FIELDS`(`:793-804`)에 `processingLevelUserSet` 이 있고, 쓰기 대응표 `d3_catalog.py:1235` 가 `d3_dataset.processing_level_user_set` 으로 잇는다. 값 CHECK = `Lv0`~`Lv3`(`db/platform/schema.sql:421-423`).
  - 26건 재시딩 대신 PATCH 26회로 닫을 수 있다. **비가역 조작이 아니므로 Ted 판정 후 실행한다.**

## 후속 항목 (이 조사에서 고치지 않은 것)

1. `UploadModal.tsx:1527` 의 `Boolean(status?.failure)` 제거 — 정본 :192 위반 해소.
2. 「어떤 포맷이든 올려요」 문면 ↔ 실동작 불일치(`FileDropCard.tsx:41`).
3. `DEFAULT_PROCESSING_LEVEL = 'Lv2'` 의 존치 여부 — 「미지정」 표현 불가.
4. 상세 편집 폼에 분류 3축 부재 — 서버는 PATCH 를 받는데 화면 창구가 없다.
5. GeoPackage·벡터 미리보기 — 별도 intent.

## [미확인]

- `.gpkg` 업로드 화면의 실제 재현(dev 에서 직접 올려 보지 않았다 — 읽기 전용 범위).
- `d5_upload` 의 `failure_reason` 원장 실측(DB 직접 조회를 하지 않았다).
- 「가뭄」 주제 데이터셋의 dev 계수(카탈로그 26건의 주제별 계수를 재지 않았다).
