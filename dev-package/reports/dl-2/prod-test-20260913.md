# `DL-2` prod 임시 검증 — 2026-09-13 (KST)

⛔ **완료 판정이 아니다.** 이 회차는 **레인 커밋을 prod 스택에서 굽고 삭제를 실제로 눌러 본 것**이고,
`CLAUDE.md §0` 의 완료 조건(「dev 배포 green ＋ `deploy_doctor` 15/15 를 한 번의 실행으로」)과는
다른 물건이다. 아래 §4 의 되돌림을 마치기 전에는 prod 가 정상 릴리스 상태가 아니다.

---

## 1. 어떻게 띄웠나 — 그리고 **무엇을 명시로 우회했나**

| 축 | 값 |
|---|---|
| 빌드 대상 | **레인 커밋**(`lane/wu-dl-2`) — prod 브랜치의 `infra/prod` 도구로 굽되 오버레이는 **미추적 복사**로 얹었다 |
| 로컬 임시 태그 | `prod-20260913-dl2-test`(`fc75de17`) · `prod-20260913-dl2-test2`(`55df17b0`) — **로컬 전용 · 원격 push 0** |
| 조상 검사 | **명시 우회** `COLAB_SHIP_ALLOW_NONMAIN=1` · 기록 축자 `MAIN_SHA ancestor=bypass` |
| `deploy_doctor` | **⑮ ✗ 가 정답이다** — ⑮ 는 「실행 sha ∈ `main`」을 재는 항목이고, 이 회차의 실행 sha 는 병합 전 레인 커밋이다. **15/15 를 주장하지 않는다** |
| 마이그레이션 | `up.sh` **0건** |
| 유닛 | **4 유닛 healthy** |
| 웹 | 웹 실행기 **`pr` 대상**으로 배포 · 자산 해시 일치 · 실행기 **78**(＝ Slack 파일 부재 · 배포 실패 아님) |
| 계정 | prod 운영자·교수 계정 1건을 `create_account` 트랜잭션 ＋ `provision-service-operator.sql` 로 심었다(계정 주소는 이 문서에 적지 않는다 — 문서 규약) |

**왜 우회했는가** — `DL-2` 는 아직 `main` 에 없고, 이 회차가 재려던 것은 「병합해도 되는가」다.
조상 검사를 통과시키려면 **먼저 병합해야** 하는데 그러면 재는 순서가 뒤집힌다.
그래서 검사를 **끄지 않고 명시로 우회**하고(`ancestor=bypass` 가 기록에 남는다) ⑮ 의 ✗ 를
**정답으로** 읽는다. ⛔ 이 우회는 **이 임시 검증 한정**이고, 정상 릴리스 경로는
`docs/BRANCHING.md` 규칙 1(「`main` 이 유일한 배포 원천」) 그대로다.

---

## 2. 1차 — `fc75de17` · 21:35

**대상** `LON_crop`.

| 축 | 결과 |
|---|---|
| 묘비 전환 | ✓ |
| `d3_file` 행 | 0 |
| S3 원장 키 | 부재 |
| 운영자 감사 | 2행 ✓ |

⚠ **여기서 결함 3건이 드러났다**(전부 `D9` 로 고쳤다 · `lane-report.md §12`).

1. **렌더-삭제 경합** — 등록 직후 **자동 렌더**가 삭제와 겹쳐
   `RENDER_UNKNOWN_ERROR` ＋ `FileNotFoundError: …webp` 로 죽었다.
2. **고아 표식 10개** — 그 렌더가 `sink.index` 로 **먼저** 써 둔 표식이 가리킬 산출물 없이 남았다.
3. **회수 계수가 로그에 0줄** — `_deletion_log` 가 배포 로깅에 붙어 있지 않았다.

---

## 3. 2차 — `55df17b0`(D9 포함) · 22:05

### 3-1. 삭제 2건

| 대상 | 회수 로그 한 줄 | 표식 |
|---|---|---|
| `LAT_HSR` | `dataset.previews_reclaimed stale=4 kept=0 orphanIndex=0 removed=16` | 12 삭제 |
| `[검증] 강수 WGS84 crop 표본`(사용자가 이어서 삭제) | `removed=48`(12벌) | — |

- `removed` 의 이름은 **S3 에 실존하던 객체 전부**와 일치했다 — 「지웠다」가 계수만의 주장이 아니다.
- `orphanIndex=0` — D9 의 고아 정리는 **이번 회차에 걷을 것이 없었다**(1차가 남긴 10개는 이미 지워진
  데이터셋의 것이라 이 문의 방아쇠가 닿지 않는다 · §4-③).

### 3-2. 원장·버킷 계수

| 축 | 값 |
|---|---|
| `d3_file` 행 | **−4** |
| S3 원장 키 | **−4** |
| `previews/` | 36 → 16 (**−28** · 시험 중 새 렌더 **+8**) |
| `preview-index/` | 23 → 46 (**−12** · **+35**) |
| `uploads/` | **+14** — 미등록 반복 업로드 조각(`U-2` 소관) |

### 3-3. 잔여물이 무엇인가 — **지운 데이터셋 소유분은 0**

| 잔여 | 수 | 왜 남는가 |
|---|---|---|
| 미등록 업로드(15 파일) 소유 미리보기 | 6벌 · 표식 28 | **데이터셋이 아니다.** 이 문의 입력은 「지워진 `d3_file.id`」이고 미등록 업로드에는 그 행이 없다 |
| `DL-2` 이전 고아 | 미리보기 2벌 · 표식 18(1차의 10 포함) | 표식이 없던 시절의 산출물 ＋ 1차 경합 잔여 — 백필(ⓓ5)과 §4-③ 의 몫 |

⟹ **삭제한 데이터셋이 소유한 산출물·표식은 하나도 남지 않았다.** `DL-2` 가 닫으려던 것이 그것이다.

---

## 4. 되돌림 계획 (prod 를 정상 릴리스 상태로)

① `prod.env` 의 `COLAB_IMAGE_TAG` 를 **`prod-aa8bee981ff5`** 로 되돌린다
② `up.sh` 재적용 ③ 웹 재배포 ④ `deploy_doctor` **15/15 를 한 번의 실행으로** 확인
(⑮ 가 ✓ 로 돌아오는 것이 되돌림의 판정이다)
⑤ 로컬 임시 태그 둘(`prod-20260913-dl2-test`·`-test2`) 삭제 — **원격에는 없다**

### 남는 일

- **③ 고아 표식 18 ＋ 옛 미리보기 2벌** — 삭제의 방아쇠가 닿지 않는 자리다.
  `ops/backfill_preview_index.py` **dry-run 계수 → Ted ⓓ5 GO → `--apply`** 순서로 처리한다
  (백필은 표식을 **놓는** 도구이므로, 걷는 쪽은 별도 판정이 필요하다 — 이 문서가 그 물음을 연다).
- **로거** — `colab_core.*` 나머지 5개가 같은 구멍 위에 있다(`lane-report.md §12-4`).
- **`uploads/` +14** — 미등록 반복 업로드 조각. `U-2` 소관.

---

## 5. 이 회차가 **재지 않은** 것

- `deploy_doctor` 15/15 · dev 배포 판정 · 병합 뒤 회귀 — 전부 **완료 판정의 자리**이고 여기가 아니다.
- CloudFront `/previews/*` 300초 캐시 만료 뒤의 404 — 이 회차에서 대기·재확인하지 않았다(`[미측정]`).
- 고아 표식 18 의 실제 정리 — 계획만 세웠고 집행 0건.
