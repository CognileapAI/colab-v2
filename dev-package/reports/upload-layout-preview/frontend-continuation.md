# 업로드·상세 UI 계속 작업 — 2026-09-09

## 범위·환경
- 사본 `/home/ttlhi10/colab-ui-frontend`, 브랜치 `codex/upload-preview-frontend`, 출발 `8855102`.
- 승인 정본: `dev-package/intent/2026-09-09-upload-layout-preview.md`, `dev-package/prd/specs/R-UPLOAD-PREVIEW.md`.
- 비교 원본: `/mnt/f/00_Project/00 CoLAB/40 COLAB-기획/10_적용전/업로드_계보_260905_rev2_이태헌.html`.
- Node 22.22.1, npm ci, 전용 Vite 127.0.0.1:43220, agent-browser session `ui-layout`.
- 이 보고서의 브라우저는 `audit-upload.html` 모의 출처를 사용한다. 실제 업로드·등록·파일 읽기·저장 성공 증거가 아니다. 부모 사본의 실제 E2E와 분리한다. 운영 서버 접근·main 병합·배포 없음.

## 이번 변경
1. 처음/분석 화면 폭 720→620px. 파일을 놓아도 분석 장면을 유지하고, 분석 완료 뒤 사용자가 다음을 눌러야 전폭 입력으로 바뀐다. 실패/분석 중 진입 비활성.
2. 입력 화면 좌 미리보기:우 폼 2:3. 원본의 카드 테두리·제목줄·본문 여백·흰 입력 표면을 복원했다. 파일 관리는 왼쪽 하단 펼침에 보존했다.
3. 하단 행동줄을 모달 전폭 바닥에 고정. 단계 변경 시 본문 처음으로 스크롤. 파일 배지에서 묶음 전체 빼기 제공. 개별 파일 추가/종류변경/빼기는 파일관리에서 유지.
4. 등록 장면 첫 미리보기 자동 요청. 같은 업로드에서 자동 요청은 한 번이고 실패 시 자동 무한 반복하지 않는다. 수동 설정/대표그림은 `details.up-preview-options > summary`에서 펼친다. 실제 저장되는 대표그림 기능은 추가하지 않았다.
5. 파일 제거 후 이전 설명·주제·변수·좌표계·출처·관측간격·공개범위·계보 미확인 상태를 초기화. 등록 요청 중 중복제출 차단과 저장 중 안내 추가.
6. 상세 읽기 순서를 정보→설명→변수표→소유/파일→계보→미리보기→활용으로 변경. 다운로드/수정 행동은 정보 위. 수정 시 기존 정보 자리가 편집 폼으로 바뀌며 취소하면 기존 정보를 복원.
7. 기간이 없는 데이터도 저장된 관측간격을 별도 행으로 표시. 기간이 있으면 기존 괄호 병기 유지.
8. 실제 계측에서 발견된 작은 라벨과 낮은 대비 수정. 공유 계보 파일은 수정하지 않고 상세 화면 범위의 스타일로만 적용.

## 직접 재현한 RED→GREEN
- 분석 중 입력 진입 차단, 분석 완료 후 명시적 다음(2건).
- 파일 제거/재첨부 후 이전 설명 유출(1건).
- 입력 진입 시 첫 이미지 자동 요청·표시(1건).
- 배지의 묶음 제거, 단계 전환 스크롤 복귀(2건).
- 상세 읽기 순서 3건(기본정보/설명/변수/소유, 상단 행동, 계보/미리보기/활용).
- 수정 중 정보 자리 교체/취소 복원(1건).
- 등록 응답 대기 중 중복제출 비활성(1건).
- 기간 없는 관측간격 표시(1건).
- 초기 12건 + 후속 14건 + 미지원 슬롯 1건 = 신규 27건을 실패 원인 확인 후 구현했다. 기존 구명세 테스트는 승인 rev2와 충돌하는 순서·진입조건·CSS 컨테이너 이름만 변경. 배지 버튼 추가로 모호해진 파일빼기 선택자는 정확한 버튼으로 지정했다.
- 이전 상세 좌우배치 CSS 문자열 시험 7건은 새 읽기순서/앵커 행동 시험 4건으로 대체. 치수는 아래 실제 브라우저에서 확인하며 CSS 문자열 통과를 시각 검증으로 주장하지 않는다.

## 검증 결과
- `frontend-typecheck`: exit 0, 오류 0.
- build: exit 0. 기존 번들 500kB 초과 경고는 남음(분할작업 범위 밖).
- `frontend-test`: exit 0, 83파일 1115건 통과/실패0. green1/red판정0/red준비0. 이후 GRIB 미지원 슬롯 신규1건 RED→GREEN, 관련 회귀 결과는 아래 추가.
- `frontend-visual`: 초기 업로드+상세 fixture 2페이지, exit 0. 글자13px 미만0, 대비4.5 미만0, 허용목록 면제0, light/dark 캡처4장. `/tmp/ui-gates/frontend-visual`.
- 입력 메타데이터 장면 직접 `live_probe.js`: 텍스트73, 작은글자0, 낮은대비0. `/tmp/ui-register-final-probe.json`.
- CSS 사후훅 정적 결과: upload.css 작은글자0(기존20→0), detail.css 원문 작은글자8(실화면 우선순위 스타일로 덮인 기존 선언 포함). computed 결과와 구분한다. 미정의 토큰 후보는 기존 원문 upload9/detail3이며 이번 새 토큰 선언 없음.
- 변경 전 실제 migration/decision/test-file guard를 JSON stdin으로 실행. 시험 작성 단계와 `COLAB_FIX_LANE=1` 구현 단계를 구분. CSS 사후훅 실행. 자동 훅 전체 적용을 주장하지 않는다.

## 시각 치수·캡처
화면 확대율100%, 데스크톱1440×900. 실제 코드와 원본 샘플은 데이터 내용/미리보기 결과가 다르므로 구성·폭·하단 위치만 직접 비교한다.

| 상태 | 원본 | 구현 | 판정 |
|---|---|---|---|
| 파일 선택 모달 | 폭620px, 높이379.92px | 폭620px | 폭 일치, 안내 내용 차이로 높이는 가변 |
| 분석완료 다음 전 | compact 유지 | 폭620px, 높이389.70px, scene=analyze | 사용자 클릭 전 전폭 확장 없음 |
| 입력 좌우 | 본문1360px, 좌537.59/우806.39px | 같은 2:3, 세로스크롤 생기면 가용폭15px 감소 | 비율 일치, 긴 내용은 스크롤 |
| 입력 하단 | y841, 높이59px, 폭1440px | y841, 높이59px, 폭1440px | 일치 |
| 좁은 화면390×844 | 원본 반응형 참고 | 가로넘침0(body/modal390), 한 열343px, 하단y785/h59 | 기본 반응형 확인 |

캡처: `screens/continuation/reference-pick.png`, `reference-classify.png`, `actual-pick.png`, `actual-analyzed.png`, `actual-classify.png`, `actual-metadata.png`, `actual-connections.png`, `actual-detail.png`, `actual-edit.png`, `actual-narrow.png`.

## 남은 검증·제한 (전체 완료 아님)
- 65개 정책 전수 통합표·실파일 저장/편집/관계/다운로드는 부모 작업에서 대조 필요. 이 레인은 fixture 화면을 실제 저장 증거로 확대하지 않는다.
- 부모가 수행한 실제 파일 E2E는 이 diff 통합 후 핵심 경로를 재실행해야 한다(자동 첫미리보기/파일관리 위치/상세 순서가 변경됨).
- 대표그림 교체는 기존 로컬 objectURL에만 반영. 저장 계약 연결은 이번 변경에 없음.
- 후속 인라인 편집에서 기존 지원필드는 표시 셀 안에서 입력으로 교체한다. 이름은 헤더, 좌표계·기간/관측간격·원천·설명은 기존 정보 셀에 남고 분류·격자·변수·소유·파일은 그대로 표시한다. 분류/변수 등 API 범위 확대는 부모의 사용자 범위확인에 따르며 이 레인에서 구현하지 않았다.
- 후속 보완: 빠른 프로젝트 생성 취소 시 이름·유형·오류 초기화, 빈 이름 안내, 생성 뒤 후보 갱신. 전역 드롭에서도 같은 확장자 제한·제외 안내 적용. 파일 별칭 TIFF/NC4/H5/HDF5/NPY/BIN.GZ는 파일 구조·좌표에 따라 미리보기 가능하다고 안내. 분석 실패 원인과 같은 파일 재전송 복구 제공. 등록 취소도 입력 보호 확인을 공유하고 변수 추가 시 새 이름 칸으로 초점 이동. 이 14건은 각각 구현 전 RED 확인 후 구현했다.
- 계보 후보 오류/수정 기능은 별도 레인 소유. 이 레인은 `frontend/src/components/lineage/**` 무수정.
- 초과: 제품 기능 추가 없음. audit 진입점에 상세 fixture 장면만 추가했고 production index 진입과 연결하지 않는다.

## 인계 파일
- 1차 `/tmp/ui-first.patch` 및 `/tmp/ui-first-snapshot/manifest.json`.
- 후속 코드 `/tmp/ui-followup.patch`는 1차 스냅샷 대비 10파일 차이만 포함. 부모 통합본의 계보 수정은 포함하지 않는다.
- 최종 보고서/추가 캡처는 별도 `/tmp/ui-evidence-final.patch`로 제공한다. 추가 캡처 `screens/continuation/followup-pick.png`.

## 미지원 슬롯 후속
- 분석 완료 `renderable=false`이면 미리보기 기본 슬롯이 failed 상태와 지도 미지원 안내를 보인다. 등록·설정에서 수동 시도는 유지한다. 미확인 null과 false를 구분한다.
- `/tmp/ui-followup-tests.patch`: 기존 빈 이름/취소 시험2파일을 승인 동작으로 갱신. 기준격자 선택 검증은 유지.
- `/tmp/ui-unsupported.patch`와 `/tmp/ui-unsupported-null.patch`: 미지원 슬롯 보완 및 null 정규화.
- 최종 증분 검증: 업로드·분석복구·미리보기fallback·격자 4파일157건 통과/실패0. typecheck exit0, build exit0(기존500kB 경고). 전체1115건 이후 추가된 미지원 슬롯1건은 이157건에 포함한다. 마지막 통합본 전체검증은 부모 실행 범위다.

## 상세 셀별 편집 보완
- 기존 `useDatasetEdit` 초안·취소·저장·공개범위 확인을 재사용한다. 공통 입력 렌더러를 필드별로 사용해 같은 데이터를 두 상태로 관리하지 않는다.
- 이름을 헤더에서 편집하고 이름 제목은 화면읽기 도구에 유지한다. 공개범위는 헤더 태그 옆에서 수정한다. 좌표계·기간/간격·원천3항목·설명은 표시 셀 안에서 수정한다. 나머지 정보 셀은 계속 보인다.
- 셀 유지/지원필드 배치/취소 시험 RED 확인 후 GREEN. 관련 상세·수정·Lv0출처·공개범위 4파일62건 통과. 기존 이름·설명 저장왕복, 서버확정값 반영, 실패rollback, 취소 후원값 시험 유지.
- 타입·빌드 exit0. 시각계측 데스크톱1440×900/모바일390×844 모두 텍스트123건, 작은글자0/대비미달0. 필수 배지9px 문제를13px로 수정했다.
- 모바일 정보격자 최소콘텐츠폭 때문에 오른쪽 셀이 잘리는 현상을640px이하 한열로 보완. 본문327px, 셀325px, 오른쪽350px. 편집 input/textarea/select 중 화면 밖으로 나간 항목0.
- 캡처 `screens/continuation/inline-edit.png`, `inline-edit-narrow.png`. fixture 화면이며 실제 저장증거는 부모 통합 E2E에 따른다.
- 코드 `/tmp/ui-inline.patch`(직전 인계완료 스냅샷 대비6파일), 추가 반응형 `/tmp/ui-inline-narrow.patch`.
- 최종 전체 frontend-test:83파일1116건 통과/실패0, exit0; green1/red판정0/red준비0. `/tmp/ui-inline-gates/gate-summary.json`, `/tmp/ui-inline-full.log`.
