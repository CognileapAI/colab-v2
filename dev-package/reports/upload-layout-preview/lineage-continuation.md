# 계보 직접 찾기·수정 후속 구현 — 2026-09-09

기점 `8855102`, 레인 `codex/upload-preview-lineage`. 부모와 분리된 사본, 소유 `frontend/src/components/lineage/**`와 관련 시험. main 병합·배포·운영 환경 접촉 없음.

## 구현과 계약

- 등록 단계 직접 찾기/기존 후보 수정: 560px 중앙 모달, 이름 검색·실제 주제 필터·단계 서버 필터·후보 정보. 선택한 뒤 `이 데이터로 연결`을 눌러야 카드에 반영. 취소/Esc/닫기/배경 클릭은 기존 입력을 바꾸지 않는다. 초점 진입·순환·복귀 적용.
- 상세 추가: 동일 검색 컴포넌트와 선택 표시 재사용. `연결 추가`가 저장 확정이며 원본 API 응답 그래프로 교체한다.
- 초과 단계 후보는 보이되 선택 불가. 행 전체 흐리게 하지 않고 사유 유지. 새 결과 영역의 12px Lv와 낮은 비활성 이름 대비를 실제 시각검사 결과에 따라 개선.
- 공통 후보 조회는 로딩/빈 결과/통신 오류를 구분한다. 명시적 재시도, 이전 필터 응답 및 언마운트 응답 무시. 기존 목록 cursor를 끝까지 읽어 첫 페이지 이후의 이름도 찾는다.
- 상세 기존 가공 전 관계에 `가공 방식 수정`/`연결 제거`. 저장 중 중복/상충 작업 차단, 오류 시 입력 유지. 파생 관계/편집권한 없는 경우 작업 버튼 없음.
- 가공법 수정은 기존 PATCH 계약, 빈 값은 `method: null`. 삭제는 기존 DELETE 204 후 GET 재조회. 재조회 실패는 제거 성공과 읽기 실패를 구별하여 새로고침 안내. 데이터셋 자체를 지우지 않음.
- 닫힌 상세 추가 화면의 저장 응답은 다음 화면을 변경하지 않는다.

## TDD·검증 근거

- 새 시험 `frontend/test/lineage-continuation.test.tsx`: 검색 선택확정/조회오류 복구/이전 필터 응답 차단 3건 RED→GREEN; 관계 수정·제거/실패 입력유지·권한/실제 API 직렬 요청 3건 RED→GREEN; 다음 cursor 후보 누락 1건 RED→GREEN; 닫힌 화면 저장응답 1건 RED→GREEN. 최초 개별 실행 8통과/0실패. 이후 동일 데이터셋 그래프 재조회가 열린 모달을 닫는 경합도 RED→GREEN 추가, 신규9건.
- 계보·업로드 관련 6개 파일 회귀: 181통과/0실패(페이지·닫기 회귀 추가 전). 기존 시험은 후보 선택 후 확정 클릭만 추가했고 기존 단언을 제거하지 않았다.
- frontend-typecheck 게이트: green 1 / 판정 실패 0 / 준비 실패 0, 종료 0(초기 6개 신규시험 시점). 최종 빌드는 아래 갱신한다.
- 시각 컴포넌트 하네스: 전용 포트 43231·agent-browser session `ui-lineage`, 실제 제품 컴포넌트에 격리된 합성 후보 2건. 1280px 뷰포트에서 모달 폭 560px, 모바일 390×844에서 좌우16px·가로 넘침 없음. 입력→선택→확정으로 닫기 확인. 하네스 파일은 제거.
- frontend-visual: RED 글자 크기 2건/대비 2건 → 수정 후 GREEN 페이지1/13px미만0/대비미달0/면제0/스크린샷2. green 1 / 판정 실패 0 / 준비 실패 0. **제품 로그인·실제 저장 E2E 증거가 아니다.** 부모의 통합 E2E가 필요.
- 정적 CSS audit·css-edit-audit 훅 실행. 기존 전체 lineage.css에 남은 13px 미만/미정의 fallback 토큰 보고를 전 파일 해결로 확대하지 않는다. 새 모달의 실제 렌더 시각검사만 위 결과로 확인했다.
- guard: 사본 `.claude/settings.json`의 Edit/Write migration/decision-number/test-file 훅을 JSON stdin으로 명시 실행. 제품 파일은 `COLAB_FIX_LANE=1`, 시험 작성 단계는 정상 TDD 단계. CSS 사후 훅도 실행.
- 전체 frontend-test 첫 실행은 진행 중 추가 시험이 수집된 중간 상태이므로 최종 수용 근거에서 제외. 1100통과/2실패 중 하나는 페이지 RED, 하나는 기존 `register-steps-20260907`의 reg-gate 부재. 소스 고정 뒤 낮은 병렬도로 다시 실행.

## 통합 주의

- `frontend/test/upload.test.tsx`의 후보 선택 4곳 뒤 `await click(screen.getByRole('button', { name: '이 데이터로 연결' }));`만 추가. 다른 레인 변경과 합칠 때 전체 파일 덮어쓰기 금지.
- `lv-rules-20260907.test.tsx` 4곳, `lineage-unknown-20260907.test.tsx` 1곳도 같은 확정 단계 추가.
- 관련 시험 내용·계약·서버·생성 타입 변경/삭제 없음. 새 테스트9건 및 기존 사용자 단계 추가만 수행.

## intent 대조 — 미달·초과

이번 독립 레인의 완료 단위는 계보 UI와 기존 계약 연결이다. 전체 intent 충족/배포 준비 완료를 선언하지 않는다.

- 미달: 원본 찾기의 **파일명·관측기간·포맷·분류 메타정보**가 DatasetRow에 없으므로 표시/검색 지원을 꾸미지 않았다. 현재는 실제 묶음 이름·주제·프로젝트·파일 수·업로더를 이용한다. 원본처럼 하려면 후보 계약과 서버의 파일명/기간/포맷 값 및 질의 확장 필요. 부모에게 전달.
- 미달: 실제 등록→재접속→계보 추가/수정/삭제→노드 이동 통합 저장 E2E는 부모의 독립 서버에서 검증한다. 본 레인의 합성 시각시험으로 대체하지 않는다.
- 초과: 0. PATCH/DELETE UI는 승인 spec 사용자 스토리15/최종 계보 수정 검증의 기존 계약 연결이며 새 권한/정책 없음.

## 최종 실행 결과

- 전체 frontend-test 낮은 병렬도 최종 실행: 1101통과/2실패, 83파일. green0/판정실패1/준비실패0. 계보 모달 재조회 경합1건과 `register-steps-20260907` reg-gate 부재1건.
- 계보 경합은 같은 데이터셋의 새 그래프 수신과 데이터셋 이동을 분리하여 수정했다. 새 회귀 RED1/기존8통과→GREEN. 수정 후 관련 6파일 **184통과/0실패**, 최종 타입 포함 build **종료0**. 큰 JS 번들 경고1건은 남는다.
- 전체 frontend-test 재실행은 UI 레인과 합친 부모 통합본에서 수행한다. 따라서 본 사본 전체게이트 GREEN을 주장하지 않는다.
- 소유 source의 마지막 변경은 LineageSection: 새 그래프 수신은 saved만 초기화, datasetId 변경만 모달닫기. 부모는 이 최신 파일과 신규9번째 테스트를 회수한다.
