5회차 값을 반영해 6절 전문을 다시 출력한다. 파일 쓰기는 하지 않았다. 5회차 수치는 `dev-package/sessions/DR-4-run-20260914T050442Z.md`가 아직 `main`에 반입되지 않아 오케스트레이터 제공값으로 표시했다.

---

# R-DATA-CANON 마감 초안 — 오케스트레이터 커밋용

- 작성 = researcher 위임 · 2026-09-14(5회차 반영 개정). 미승인 초안이다.
- 5회차 근거 = `dev-package/sessions/DR-4-run-20260914T050442Z.md` — **아직 `main` 미반입, 값은 오케스트레이터 제공값**으로 표시(`[미병합]`). 병합 뒤 이 자리를 세션 파일 경로로 확정 인용한다.
- 입력 = `dev-package/prd/rounds/R-DATA-CANON.md`(§5·§11·§12) · `dev-package/intent/2026-09-14-data-canon-and-reseed.md`(원한 결과 ⓐ~ⓕ · 작업 단위 완료 정의) · `dev-package/sessions/DR-4-run-20260914T0{10023,12505,20005,22417}Z.md` ＋ `[미병합]` 5회차 · `dev-package/work-items.yaml` `DR-4` 블록 · `dev-package/reports/r-data-canon/{wu-c3,wu-c1b,wu-c2a,wu-c2c}/NOTE.md` · `git log --oneline`.

---

## 1. intent 대조표

| 항목 | 원한 결과 요지 | 판정 | 근거 |
|---|---|---|---|
| ⓐ | 유형별 설명 정본이 참조자료 폴더 안 `DATASETS.md` 4건(강수·식생·가뭄·포맷 확인용)에 있고 Ted 확정 표시가 문서 안에 있다 | 충족 | `dev-package/reports/reference-data/datasets-md/{01.level-data/01.precipitation,01.level-data/02.vegetation,01.level-data/03.drought,02.File-format}/DATASETS.md` 4건 존재(레포 사본 자리) · 커밋 `c49a1e40`·`2d5d6422` |
| ⓑ | 실행 계획(`plan-manifest.yaml`·`upload-plan.json`)이 정본 md 의 fenced yaml 블록에서 생성되고 어긋나면 비영 종료 | 충족 | `dev-package/tools/dev-seed/build_plan.py` · 커밋 `c49a1e40` · 게이트 `seed-plan-drift`·`seed-plan-drift-selftest` 신설(`gates/README.md:45-46`) |
| ⓒ | 명령 하나(`reseed.sh` ＋ `/dev-reseed`)가 10단계를 사람 입력 0회로 끝까지 돈다 | 미달 | 5회 실행 전부 「한 번의 실행」 불성립 — 1회차 `secrets` 정지 · 2회차 `reset` ①″ 정지(따옴표 결함) · 3회차 `s3` ② 정지(소유자 단언) · 4회차 `--from s3` 재개 · 5회차 `[미병합]`도 회차 5개로 분산 완주(정본 일치는 달성했으나 재개 횟수 자체가 판정 대상). 도구 결함 고침 5건(커밋 `5843d3e4`·`aad4d2b5`·`1a834b6a`·`09a32b16`·`18f0b1be`)으로 재개 지점은 좁아졌으나 **단일 프로세스 완주 실행은 5회 중 0회.** |
| ⓓ | 재생성 뒤 데이터셋 28 · 프로젝트 4 · 간선 18 · 가공 단계 정본 일치(「미지정」 0) | 충족(5회차 확정) | `[미병합]` 5회차 verify = 데이터셋 **28** · 프로젝트 **4**(drought 2 연결) · 간선 **18** · 「미지정」 **0** · 프로젝트 미연결 **0** · 가공 단계 Lv0 10 · Lv1 16 · Lv2 2(정본 분포와 대조는 병합 뒤 세션 파일로 재확인) |
| ⓔ | 가뭄 자료가 「미리보기 없음」으로 등록되고 안내 문면이 「등록은 됩니다」로 바뀐다 | 충족(5회차 확정) | `[미병합]` 5회차에서 가뭄 2건(seq 13·14 `.gpkg`) 화면 등록 성공 — WU-C2a·WU-C2c(커밋 `57314fcb`·`d5cd6ca9`)가 연 등록 카드 잠금 해제가 실사로 확인됨 |
| ⓕ | 투입 실적과 정본의 차이가 항목명·사유로 결과 JSON·보고에 남는다(차단 조용히 넘어가지 않음) | 부분 | 데이터·간선 계수는 정본과 일치해 차단 항목 자체는 소멸. 다만 **미리보기 판정 표가 「성립 28」로 나온 것 자체가 새 결함** — 순회기가 렌더 완료가 아니라 요청 시점만 측정해 「성립」으로 적음(green-by-skip 계열). 실제 렌더 성립은 미측정 → `PV-2` 후속(§3 `PV-2-EXT`) |

- 판독 기준 개정 = ⓒ 는 **재개 횟수 자체**(5회)를 미달 근거로 잡는다 — 정본 수치가 맞아도 「명령 하나가 끝까지 돈다」는 별개 계수다. ⓕ 는 계수 일치와 계수 신뢰도(측정 방식 결함)를 나눠 판정한다.

## 2. DR-4 최종 상태 권고

**권고 = `partial`(🟧) — 5회차 반영 후에도 유지.**

완료 정의(`dev-package/work-items.yaml` DR-4 `completion_def`) ⑴~⑸ 대조 —

| 번호 | 정의 | 판정 |
|---|---|---|
| ⑴ | 진입점 하나가 초기화와 재투입을 사람 손조작 없이 이어 부른다 | 미달 — 5회차까지 누적 재개 4회(2·3·4·5회차가 각각 앞 회차 정지·중단 지점에서 재개) |
| ⑵ | 중단 지점에서 상태 파일 기준으로 재개할 수 있다 | 충족 — `--from`·`--from-seq` 로 재개 실증(4회 실측) |
| ⑶ | 승인 게이트가 절차 안에 있다(dev 한정 상시 승인) | 충족 — `.claude/rules/deploy.md` 〈395〉 개정 · WU-C0 커밋 |
| ⑷ | 화면단 원칙 유지(재투입은 브라우저 화면 조작) | 충족 — seed 단계는 agent-browser 화면 조작, DB 직접 쓰기는 prelude SQL 선행 4단계뿐 |
| ⑸ | 실행 결과가 계수표 한 장으로 나온다(프로젝트·데이터셋·간선·미리보기·차단 이름) | 충족(수치는 확정) — 다만 미리보기 칸의 측정 방식 결함은 별도 결함으로 기록(§1 ⓕ) |

⑴ 미달이 `partial` 사유다. 5회차로 데이터 정합(⑵~⑸ 실체)은 완결됐으므로, 남은 결함은 **도구가 단일 실행으로 끝까지 도는가**로 좁혀졌다.

**닫는 조건 — 후속 WU `DR-4b`(신설 제안, 착수 진입조건 충족)**
- 이름 = 「reseed 배포·계정 단계 통합 ＋ 단일 실행 증명」.
- 진입조건 = **충족** — DR-4 5회차 종료, ⓓ·ⓔ 확정됨.
- 완료 정의 = ㉮ `main` 최신 tip 기준 10단계를 **재개 0회 · 사람 입력 0회**로 단일 프로세스가 완주 ㉯ 완주 실행의 verify 계수가 28/4/18/0 과 일치 ㉰ 계정 단계가 같은 단일 실행 안에 포함 ㉱ 미리보기 판정 표가 렌더 완료 기준으로 측정됨(현재의 요청 시점 측정 결함을 고치거나, 고치기 전까지는 「측정 방식 미확정」으로 명시) ㉲ 이 실행의 결과 JSON·세션 기록 1건이 「한 번의 실행」 성립을 계수로 증명.

**새 블로커** — 4회차 gitignored 산출물(러너가 회전한 관리자(PI) 비밀번호의 유일 사본 · 상태 파일)이 워크트리 정리로 유실됨. **「PI 계정 비밀번호 재설정 필요(Ted 관리자 계정으로)」**를 DR-4b 착수 전 선행 항목으로 기록.

## 3. 후속 등재 목록 (대장 신설 후보) — ≤8건

| id 제안 | 이름 | 진입조건 | 완료 정의 | 관련 |
|---|---|---|---|---|
| `DR-4b` | reseed 배포·계정 단계 통합 ＋ 단일 실행 증명 | 충족(DR-4 5회차 종료) | 위 §2 ㉮~㉲ | 이슈 없음(DR-4 파생) |
| `DR-4-TOOLING` | reseed 도구 잔여 결함 묶음 — 소유권·네이밍·기록 오염·산출물 보존 7건 | DR-4b 착수 전 아무 때나(도구 코드만) | 일곱 각각 fail-closed 픽스처 또는 실측 1건으로 닫힘 — ⑴ dev-seed README 종료코드 4·5 문서화 ⑵ 계정 생성 폼 칸별 `data-testid` 신설 ⑶ `COLAB_DEV_URL` 이름 통일 ⑷ `verify` 세션 이름을 agent-browser 가 읽는 `--session` 으로 전달(4회차 green-by-skip) ⑸ `lib.sh` 로그 `tee` 이중 기록 잔존분 점검 ⑹ `.work/new-password.txt` custody 확인·Ted 판정 ⑺ **신설 — 실행 산출물(gitignored 상태 파일·회전 비밀번호)을 워크트리 밖(`$COLAB_JOB_DIR/tmp` 또는 홈)에 두도록 실행 자리 변경**(5회차 유실 원인) | #49 · #50 |
| `OPS-PI-PW` | PI 관리자 계정 비밀번호 재설정 | 즉시 착수 가능(Ted 관리자 계정 권한 필요) | Ted 관리자 계정으로 PI 계정 비밀번호 재설정 완료 ＋ 새 비밀번호를 워크트리 밖 0600 파일로 보관 | DR-4 5회차 파생(신규 블로커) |
| `GATE-46` | migration-drift 오라클 정비 | 없음(착수 후보) | 기동 실패의 red 위장·바닥값 미달·이름표 어긋남 셋을 각각 실패 픽스처로 고정 후 green | #46 |
| `S3-PROC` | S3 계획 검토 절차 정비 | 없음 | `stage_s3` ② 계획 검토 컨테이너 통합 이후 남은 검토 순서 문서화 · `--rehearse` 자리와 일치 | #49 |
| `DEPLOY-PRIM` | `--rehearse` 원시동작을 배포 1차 요소로 승격 | 없음 | `docs/DEPLOY.md`·`.claude/rules/deploy.md` 에 원시동작 10종을 `deploy_doctor` 와 별개 절로 등재 | 없음 |
| `PV-2-EXT` | PV-2 증보 — 미리보기 「성립」 측정 방식 결함 ＋ viz-render 재기동 관측 | 없음(착수 후보) | ⑴ 순회기가 렌더 완료가 아니라 요청 시점만 재는 것을 고치거나 「측정 미완」으로 명시 표기(5회차 「성립 28」 전건이 이 결함의 실례) ⑵ 4회차 관측(`viz-render` `RestartCount` 5 · oom=false · exit=0)을 진입조건에 추가 | #42 |
| `OPS-EC2` | EC2 잔존물 처분 — Ted 판정 대기 | 없음(판정 먼저) | `dev.env.bak-*` · 이미지 tar · 로컬 web-backup 디렉터리의 보존/삭제를 Ted 가 정하고 값을 등재 | #48 |

- 8건 한도를 지키려고 `GATE-PARALLEL`(신규 게이트 병렬 안전성 미선언)은 `GATE-46` 안건과 별도 표기를 생략했다 — 필요 시 `GATE-46` 완료 정의에 병렬 안전성 항목을 합쳐 넣는다.
- 이슈 매핑 — `#40`(gpkg 등록 차단) = WU-C2a·WU-C2c ＋ 5회차 실사로 **closed-by** 성립(닫기는 Ted) · `#44`(레벨 전건 Lv2) = WU-C1b ＋ C4 재생성(5회차 Lv0 10·Lv1 16·Lv2 2 분포 확인)로 **closed-by** 성립(닫기는 Ted) · `#46` = `GATE-46` · `#48` = `OPS-EC2` · `#49` = `S3-PROC`·`DR-4-TOOLING` · `#50` = `DR-4-TOOLING` ⑴⑵.
- `dual-agent.md:26` 「기존 12개 스킬」은 실물 13개(`ls .claude/skills/`)와 어긋남 — 문서 한 줄 정정 권고(코드 변경 0).
- `gates/README.md:45` 의 `COLAB_REF_ROOT` 3상태는 이미 구현됨 — 후속 등재 대상 아님.
- `lifecycle_contract` 측정 레인·`harness-eval-selftest` `expect()` 구멍은 이번 조사에서 원출처를 찾지 못함 — `[미확인]`.

## 4. PLAN-SoT 〈398〉 증보문 초안 (㉿ 이하)

> ㉿ **5회차 마감(2026-09-14)** — WU-C4 는 5회 실행 전부 「한 번의 실행」 불성립으로 종료(1회차 `secrets` 정지 · 2회차 `reset` ①″ 따옴표 결함 정지 · 3회차 `s3` ② 소유자 단언 정지 · 4회차 `--from s3` 재개 · 5회차 `[미병합]` dev `d5cd6ca9` 배포(태그 `dev-20260914-3`)로 가뭄 2건 화면 등록 완주). 5회차 verify = 데이터셋 **28** · 프로젝트 **4**(drought 2) · 간선 **18** · 「미지정」 **0** · 미연결 **0** · Lv0 10 · Lv1 16 · Lv2 2 · `deploy_doctor` **15/15**. 정본 일치(intent ⓓ·ⓔ)는 확정, 단 미리보기 판정 「성립 28」은 순회기가 렌더 완료가 아닌 요청 시점만 재는 측정 결함으로 렌더 실성립은 미측정(`PV-2` 후속). DR-4 상태 = `partial`(🟧) 권고 — 완료 정의 ⑴(사람 손조작 없는 단일 진입점 완주) 미달, ⑵~⑸ 충족. 새 블로커 = 4회차 gitignored 산출물(러너 회전 PI 비밀번호 유일 사본 · 상태 파일)이 워크트리 정리로 유실 → PI 계정 비밀번호 재설정 필요(Ted 관리자 계정). 후속 = `DR-4b`(진입조건 충족) ＋ `DR-4-TOOLING`·`OPS-PI-PW`·`GATE-46`·`S3-PROC`·`DEPLOY-PRIM`·`PV-2-EXT`·`OPS-EC2`(§3) 신설 제안.

## 5. HANDOFF 5줄 초안

1. 최종 갱신 = 2026-09-14(R-DATA-CANON 5회차 완주 · dev `d5cd6ca9`/`dev-20260914-3` 배포 · 정본 일치 28/4/18/0 확정).
2. 현재 단계 = DR-4 데이터 정합 완료, 단일 실행 증명 미달로 `partial`(🟧) 전환 대기 — `DR-4b` 착수 준비.
3. 다음 WU = `DR-4b`(reseed 배포·계정 단계 통합 ＋ 단일 실행 증명) — 착수 전 `OPS-PI-PW`(PI 비밀번호 재설정) 선행 권고.
4. 새 블로커 = PI 관리자 계정 비밀번호 유실(4회차 gitignored 회전값이 워크트리 정리로 소멸) — Ted 관리자 계정으로 재설정 필요.
5. 다음 세션 진입조건 = 5회차 세션 파일(`DR-4-run-20260914T050442Z.md`)이 `main` 에 반입되고 이 초안의 `[미병합]` 표시를 확정 경로 인용으로 교체한 뒤, 대장 `DR-4` 상태를 `partial` 로 갱신.

## 6. §5-b 후보 (3건 — 코더 지시로 1건 추가)

1. 「실모드로 돈 적 없는 원격 줄에서 정지」×3(1회차 `secrets` · 2회차 `reset` 따옴표 · 3회차 `s3` 소유자 단언) → **원격 원시동작은 리허설로 먼저 증명한다.** `--rehearse` 10원시동작 신설이 이 교훈의 직접 반영.
2. 「검사기가 판정 대상을 놓친 채 통과를 보고」×2(4회차 verify 로그인 27건 green-by-skip · WU-C2a 기존 시험이 결함 전제 고정) → **검사기를 새로 붙일 때 기존 시험이 결함을 전제로 굳혀 두지 않았는지 먼저 확인한다.**
3. 「실행 산출물을 워크트리 안 gitignored 자리에 두고 워크트리를 지워 유실」(4회차 PI 회전 비밀번호·상태 파일 소멸) → **실행 자리는 워크트리 밖(`$COLAB_JOB_DIR/tmp` 또는 홈)으로 둔다.**

---

## 참조

- `dev-package/prd/rounds/R-DATA-CANON.md` §5·§11·§12
- `dev-package/intent/2026-09-14-data-canon-and-reseed.md`
- `dev-package/sessions/DR-4-run-20260914T0{10023,12505,20005,22417}Z.md` ＋ `[미병합]` `DR-4-run-20260914T050442Z.md`
- `dev-package/work-items.yaml` `DR-4` 블록
- `dev-package/reports/r-data-canon/{wu-c3,wu-c1b,wu-c2a,wu-c2c}/NOTE.md`