# CoLAB v2 — 상태 지도 (실측 2026-09-05)

조사 범위 = `dev-package/03-HANDOFF.md`(머리말·§2·§4·§4.5·§5.5) · `dev-package/work-items.yaml` 전수 · `CLAUDE.md §4` · `gates/README.md` · `40 COLAB-기획/README.md` ＋ `20_검토/260905_적용현황조사/` 2건 · claude-mem 관측 8797~8801.
레포 무편집 · 기획 폴더 무편집.

---

## §A 실측 스냅샷

- **main** = `b0c1f34`(「원장 등재 2건 — BF-12 코드 집행〈325〉 · 기획 정본 폴더 이동 대응〈326〉」). 직전 `ed8ceb3` · `14db1a8`(BF-12 앱 로그 설정).
- **작업트리** = `git status --short` 출력 0행. `git stash list` 0건.
- **브랜치** = 로컬 `main` 하나. 원격 `origin/main`·`origin/gh-pages`·`origin/urgent-upload-lineage-rev1`·`origin/feature/rtf400_deploy`. PRD 작업이 실린 미병합 브랜치 없음.
- **게이트 선언 수 50** — 두 기록이 다르다. 둘 다 적는다.
  - `03-HANDOFF` 머리말(2026-09-05): 병합 트리 `ed8ceb3` 기준 **두 실행에 걸쳐** 선언 50 · green 50 · red(판정) 0 · red(준비) 0. 첫 실행 `all -j 1` = green 43 · red(판정) 1(`planning-freshness`, 원인은 기획 정본 폴더 이동 · `370ed99` 로 닫힘) · red(준비) 6(셸 입력 미선언). `source ~/.colab-v2-test.env` 뒤 재실행 10종 전건 green. 머리말 축자 「**한 번에 50 green 이 아니다 — 두 실행에 걸친 셈이다**」.
  - `03-HANDOFF §2`(2026-09-03 재개정 · `§9 〈307〉`-㉵): 병합 트리 `./gates/run.sh all -j 1` 판정 = green 50 · red(판정) 0 · red(준비) 0.
  - 게이트 실행법 = `gates/run.sh all [-j N]`. 병렬 안전성 정본 = `gates/config/parallelism.toml`(단독/병렬/미선언 3상태) · `db-selftest` 는 병렬 금지 · 재현은 `COLAB_GATE_JOBS=1`.
- **staging** = 서빙 릴리스 `e37808cace69`(배포 2026-09-05 13:22:17 KST) · 컨테이너 8/8 healthy · `verify-deploy` GREEN(통과 15 · SKIP 0) · 체인 platform `0011` · ai `0005`.
- **이 회차(BF-12 병합 회차)** = staging 무접촉 · 배포 0 · 삭제 0 · 소유 계수 무변(대상 136 · 살아 있다 117 · 접수분 0 · 고아 0 · 판정 불가 19).
- ⚠ 소유 계수의 유효성에 이의 기록 있음 — `§4.5`(`〈317〉`⑶): 자리 파일이 236 → 285 로 늘었고 쓴 주체 `[미확인]` ⟹ 「판정 불가 19」는 더 이상 실측값이 아니다. 배포 창에서 `artifact-ownership` 을 재굽기 **전에** 먼저 돌린다.
- **진입조건 규약** = `CLAUDE.md §4` — 순서(진입조건 확인 → 계약 동결 → 실패 시험 red → 구현 → 게이트). 축자 「**미충족이면 구현하지 말고 보고한다**」. 게이트 우회·검사 대상 축소 금지.

---

## §B 열린 항목 표

대장 = `dev-package/work-items.yaml`. `done`·`deferred` 제외 전수.

### stage2 (지금 단의 항목)

| id | 기능 설명 | status | 차단 유형 | 무엇이 막는가 |
|---|---|---|---|---|
| `BF-12` | 지도 타일 회수 루프의 INFO 요약이 로그로 나오게 한다 | open | 자격·환경 필요 | 완료 정의 ⑴⑵⑷ 충족 · **⑶ staging 배포 뒤 첫 바퀴 요약 1줄 실물** 미충족. 배포 창 6 ＋ 8/8 healthy 뒤 ≥60분 대기 필요 |
| `TL-2` | 구판 미리보기 산출물 회수 — 소유 판정에 등급 2종(판정 불가 · 원천 원장 부재) 추가, 세기만 하고 지우지 않는다 | open | 선행 대기 | 진입조건 「`BF-12` 닫힘」 미충족. 관측 계수가 로그로만 나오는데 그 로그가 아직 staging 에서 확인 안 됨 |
| `CR-2` | 코드리뷰 회차 후속 5건(계약 델타 · 로그인 제한 XFF 버킷 · 재굽기＋소유 재실측 · tolerate 기한/`_FILE`/DLQ · CI 인프라 red 둘) | open | 판정 대기(Ted) ＋ 일부 배포 선행 | 진입조건 「없음 — 판정만 있으면 착수」. ⑴ 은 18차 해제로 부분 집행(브랜치 `lane-r18` 병합 여부 `[미확인]` — §F). ⑶ 만 staging 배포 선행 |
| `IS4` | terraform state 보관·복구 | partial | 자격·환경 필요 | 맨몸 호스트 복구 리허설. `R-1` 과 재료 공유 |
| `I3` | 배포 자동화 파이프라인 | partial | 자격·환경 필요 | 완료 정의 15행 중 닫힘 9 · 열림/부분 6 |
| `I4` | 운영 준비 — 추적·알람·복구 리허설 | open | 선행 대기 | 진입조건 `I3`. `R-1` 리허설 결과를 받아 쓰고 다시 돌리지 않는다 |
| `J-1` | 편의 기능 묶음 9건(격자 가져오기·연구실 기본 격자·격자 불일치 경고·예상 영역·「지도 없음」 배지·팔레트 선택 등) | open | 선행 대기 | 진입조건 「stage 2 마지막」. 완료 정의는 초안 상태(`completion_def_draft`) |
| `PA-G` | 구글 IdP 어댑터(비밀번호 발급 경로 제거) | open | 선행 대기 | 개시 조건 = 대장의 `stage1`·`stage2` 미완이 **0건**이 되는 시점(`deferred`·자기 자신 제외). 지금 미완 다수 |

### stage1 잔여 (버그 수정 `BF-` 계열)

| id | 기능 설명 | status | 차단 유형 | 무엇이 막는가 |
|---|---|---|---|---|
| `BF-7` | 스크린샷 다운로드가 크롬에서 취소된다 — 객체 URL 즉시 revoke | open | 지금 착수 가능 | 진입조건 「없음」 ＋ 「통합 브랜치 `bugfix/20260903-ui-12` 병합 뒤」. 그 브랜치가 로컬·원격 목록에 없다(병합 추정 · §F) |
| `BF-8` | 연구실 화면(S-01) 뿌리 여백·최대폭 규칙 부재 | open | 지금 착수 가능 | 진입조건 「없음」 ＋ `BF-5` 병합 뒤. `BF-5` 는 대장에 열려 있지 않다 |
| `BF-9` | core-api 계보 그래프 — 원천 → 루트 edge 부재 | open | 지금 착수 가능 (조건부) | 진입조건 「없음」. 단 계약 `LineageEdge` 가 원천 끝점을 허용하는지 `[미확인]` ⟹ 계약 변경 필요면 멈추고 보고(동결 해제 회차) |
| `BF-10` | 바탕지도 대안 — 경위도 격자선·눈금 옵션 | open | 판정 대기(Ted) | Ted 판정 Q4 = 이번 회차 미착수 · 다음 회차 후보 등록. **착수는 Ted 가 회차를 연다**. 선행 판정 하나 더 — 화면 오버레이와 서버 렌더 PNG 의 불일치를 받아들일지 |
| `BF-11` | `project.css` 미정의 토큰 4종 → `tokens.css` 정본 치환 ＋ backlink 공용화 | open | 지금 착수 가능 | 진입조건 「없음」 ＋ `BF-5` 병합 뒤 |

### after_stage2 (단계 대기 — 지금 열지 않는다)

| id | 기능 설명 | status | 차단 유형 |
|---|---|---|---|
| `C1` | 5 repo 푸시 확인 | open | 단계 대기 (진입조건 자체는 없음) |
| `C2` | v1 이관(`20 CoLAB-v1`) | open | 선행 대기 (`C1`) |
| `C3` | PoC 지식 추출 `HARVEST.md` | open | 선행 대기 (`C1`) |
| `C4` | v1 방법론 추출 `METHOD.md` | open | 선행 대기 (`C1`) |
| `C3·C4` | 위 둘을 한 행으로 묶은 항목 | open | 선행 대기 |
| `R2` | v1 레포 5종 archive | open | 선행 대기 (`C4`) |
| `G1b` | E-04 · E-02 목업·화면 최신성 | open | 선행 대기 (`P2`·`P4`) |
| `DP-1` | 데이터 프로세스 — 가공 결과를 새 데이터셋으로 저장 | open | 단계 대기 (Ted 판정 `〈189〉`-㉰ 「stage 2 다음 · 지금 만들지 않는다」) |
| `T-1` | `ts_config` 한국어 재작성 | open | 단계 대기 |
| `P4` | 검색 히어로(S-01) · 검색 결과(S-06) | open | 단계 대기 (Ted 판정으로 항목 전체가 stage 3 으로) |
| `K3` | 계보 제안 서비스 | open | 선행 대기 (`P2`) |
| `K4` | 자연어 검색 서비스 | open | 선행 대기 (`T-1`·`G8b` Q1) |
| `K5` | 제안 원장 | open | 선행 대기 (`K3`) |
| `LV-3` | 「CoLAB에서 가공됨」 표식 | open | 선행 대기 (`DP-1`·`P3`) |
| `LV-4` | 계보 관계 삭제 시 부모 이름을 상수로 남긴다 | open | 단계 대기 (저장 자리 설계 `[미확인]`) |

---

## §C 기획 적용 트랙

### C-1. `10_적용전` 3 문서 상태 (정본 = `40 COLAB-기획/README.md` 입력물 상태표)

| 문서 | 작성자 | 받은 날 | 상태 |
|---|---|---|---|
| `업로드 목업 기획 피드백_260826_이태헌.pdf` | 이태헌 | 08-26 | 미적용. 10항목 = 적용 3 · 부분 1 · 미적용 3 · 충돌 3 |
| `업로드_계보_260826_rev1_이태헌.html` | 이태헌 | 08-26 | 미적용. 변경점 30 = 적용 4 · 부분 1 · 미적용 21 · 충돌 4 |
| `260903_기획안에 대한 의견 정리_조성진.docx` | 조성진 | 09-03 | 미적용. 12항목 = 적용 3 · 부분 2 · 미적용 5 · 충돌 1 · 결정필요 1 |

폴더 규칙 — `10_적용전` 은 원본 무수정 보관, 반영 완료 시 `30_적용완료/적용일_원파일명` 으로 이관. 지금 `30_적용완료` 로 넘어간 것 0건.

### C-2. 260905 조사 2건 요지

**`_조사_적용현황_SoT_260905.md`**(948행 · 기획 정본 ↔ v2 코드 전수 대조)
- 항목 604 = 적용 447 · 부분 49 · 미적용 67 · 상이 41. 적용률 74.0%.
- 에픽별 미적용 최다 = E-03 데이터셋 상세 17 · E-04 업로드와 계보 12 · P1 20.
- 미적용 67 중 다수는 대장에서 `stage2`/`after_stage2` 로 의도적으로 미룬 것이고 각 표 비고에 stage 를 적었다.
- P1(계정과 연구실 소속)은 `CLAUDE.md §5` 가 v2 범위 밖으로 선언한 에픽 ⟹ 미적용 20 은 그 선언의 결과.
- 조사 문서가 스스로 지목한 핵심 = **상이 41건** — 개발이 사양보다 앞서 임의로 정한 자리 ＋ 후행 정본(260825·v2.1)을 아직 흡수하지 못한 자리 두 종류.

**`_조사_적용현황_적용전문서_260905.md`**(156행 · 3 문서 ↔ 코드 대조)
- 추출 한계 명시 — docx 삽입 이미지 7개 텍스트 추출 불가 · PDF 표 3개가 이미지로 박혀 읽히지 않음(Lv별 필수 메타 표 · 고려대 Level/Category/Type 표 · 다중 입력 방향 표).
- 정면 충돌 5건 — C-1 분류 체계(고려대 3축 분류5·유형6·가공단계4 ↔ 코드 `topic` 4값 CHECK) · C-2 가공 단계 Lv(사람 선택 ↔ 계보 파생값 · `0011` 마이그레이션이 `processing_level_user_set` 컬럼을 삭제) · C-3 공개 범위(docx 외부 축 3값 ↔ rev1 내부 축 3값 ↔ 코드 2값 열림/잠김) · C-4 등록 단계 구성(분류/메타/연결 ↔ 자동메타/프로젝트/계보) · C-5 포맷 표기(확장자 ↔ 판별 포맷).
- 두 기획자의 3값이 서로 다르다 — docx D-4 기준은 「연구실 외부」, rev1 R-02 기준은 「연구실 내부」. docx 안은 RLS 경계 정책 자체를 손대야 하고, rev1 안은 표기 교체로 근사 가능.
- 결정필요 1건 = docx D-6-1 「'못 찾은 것이 있어요' 체크박스」 — 코드에 해당 체크박스 없음.
- rev1 이 30건 변경점을 자체 데이터(`CHG` 배열)로 내장 — 근거·화면 좌표·선택자까지 문서가 스스로 명시.

### C-3. PRD 작업의 실제 존재 여부 = **미발견**

- `40 COLAB-기획/20_검토/260905_PRD/` = **빈 디렉터리**(`ls -la` 결과 `.`·`..` 뿐 · 폴더 mtime 2026-09-05 15:56).
- `find "00 CoLAB" -iname "*PRD*"` 전수 — 나온 것은 전부 무관한 선행물이다: `00 CoLAB-PoC/docs/PRD_CO_LAB_HYDRO.md`(및 그 워크트리 사본 다수) · `99 temp/정본-백업-20260822/에픽/*/documents/PRD_*.md`(2026-08-22 정본 백업) · `40 COLAB-기획/99_archive/backup/_backup_260823_NB2-DR8/E-04_PRD.md.bak`. **`PRD_적용전기획_260905.md`·`개발계획서_260905.md` 는 어디에도 없다.**
- 최근 24시간 수정 `*.md`/`*.html`(기획 폴더 ＋ `99 temp`) = `40 COLAB-기획/README.md` · `10_적용전/업로드_계보_260826_rev1_이태헌.html` · `20_검토/260905_적용현황조사/` 2건 · `99 temp/pr1-review/41-deep-review-fable.md`. PRD 산출물 없음.
- `30 CoLAB-v2/planning/` = `README.md` 하나. `dev-package/` 최근 24시간 변경분에 PRD 계열 파일 없음.
- git — 작업트리 clean · stash 0 · PRD 작업이 실린 미병합 브랜치 없음.

### C-4. S1396 이 남긴 것

claude-mem 관측 8797~8801(세션 `93b33938…` · 2026-09-05 06:54~06:57 UTC).

- **8797(decision)** — Ted 가 결정 규칙을 세우고 비동기 에이전트를 띄웠다. 규칙 = ⑴ 기획자 문서가 임의 개발 코드보다 우선 ⑵ 기획자끼리 갈리면 RLS 보안 모델을 안 바꾸는 쪽(rev1 내부 3값)을 기본으로 두고 `미결` 표시 ⑶ Lv 은 분류 단계의 사람 선택을 기본으로 하고 계보 파생값은 경고만(차단 없음) ⑷ 스키마·enum 변경마다 기존 행 이행 규칙(자동 매핑/재선택)과 과도기 표기 명시 ⑸ 추적 키(D-/P-/R-/C-/E-)로 항목 손실 0 ⑹ 개발계획서는 라운드(R-A 즉시 · R-B 판정 게이트)로 묶는다. **선언한 산출 경로 2개가 위 미발견 파일이다.**
- **8798** — 조사 범위(604항목)·원장 규약(`PLAN-SoT §9` 최대 `〈324〉`)·개발세션 프롬프트 규약 확인. 기획 문서 도착 이전부터 있던 버그 2건 지목 — `d2_access.py:76-81` 권한 스위치 기본값 역전(`stored.get(s, False)`) · `routes/preview.py:96-132` 미리보기가 업로드·편집 게이트와 `require_body_access` 를 안 본다.
- **8799** — 추적 사슬 확정. 코드 주장 6건 실측 검증 · 260826 라운드가 이미 정면 충돌 4건과 착수 전 판정 14건(9건 Ted 요구)을 기록해 두었음을 확인.
- **8800** — rev1 HTML 이 `AXIS_DEF`(분류5·유형6·가공단계4 정의·예시)와 Lv 규칙 함수·Lv0 조건부 필드를 내장한 실행 가능한 사양임을 확인. 충돌 6건·선행 판정 9건 정리.
- **8801** — `topic` 4값 ＋ NULL 허용 구조 확인(계약층 무제약 · 서버 400 · DB CHECK). rev1 3축으로 가려면 스키마 재편 ＋ 기존 NULL 행 이행 경로 ＋ 계약 재생성 ＋ FE 상수 재생성이 필요.

⟹ **S1396 이 남긴 것은 조사·판정 규칙·관측 기록이고, PRD·개발계획서 문서 실물은 남기지 못했다.** 8797 의 `files_modified` 는 의도 선언이며 파일시스템 실측과 어긋난다.

---

## §D 의존 도식

```
[배포 창 6]  ── Ted go/no-go ① ─┐
                                 │
                                 ▼
                    BF-12 ⑶ (8/8 healthy 뒤 ≥60분 · 회수 요약 1줄)
                                 │  ⟹ BF-12 open → done
                                 ▼
                    TL-2 ⑴⑸ (등급 2종 신설 · 관측 전용 계수 · 삭제 0 확인)
                                 │
                                 ▼
                    Ted 별도 go ── TL-2 apply (상한 20/바퀴 · fail-closed)

CR-2 ⑴⑵⑷⑸ ──── Ted 판정 4갈래 (배포 불요)
CR-2 ⑶      ──── staging 배포 선행 ── 재굽기 ＋ 소유 네 등급 재실측
                                        ▲
                          artifact-ownership 을 재굽기 *전에* 먼저 (〈317〉⑶)

BF-7 · BF-8 · BF-11  ── 선행 없음 (프론트 · staging 무접촉) ── 지금 착수 가능
BF-9                 ── 선행 없음 · 단 계약 변경 필요 판정 시 중단·보고
BF-10                ── Ted 가 회차를 열어야 착수 (＋ 화면/PNG 불일치 수용 판정 선행)

I3(partial) ──▶ I4 ──▶ (운영 리허설)
IS4(partial) ─── R-1 리허설 재료 공유

stage1·stage2 미완 = 0  ──▶ PA-G (구글 IdP · 비밀번호 경로 제거)
                              ▲
        지금 미완 = BF-7·8·9·10·11 · BF-12 · TL-2 · CR-2 · IS4 · I3 · I4 · J-1

[기획 적용 트랙]
10_적용전 3문서 ──▶ 260905 조사 2건(완료) ──▶ PRD·개발계획서 [미발견]
                                                    │
                                                    ▼
                                    Ted 판정 (C-1 분류체계 · C-2 Lv · C-3 공개범위
                                              · C-4 등록단계 · C-5 포맷표기)
                                                    │
                                                    ▼
                                    대장 신규 항목 등재 ──▶ 코드 적용 라운드
```

---

## §E 착수 후보 순서

**Ted 접촉 없이 · staging 무접촉으로 지금 시작 가능한 것**

1. **`BF-11`** — `project.css` 미정의 토큰 치환. 선행 없음 · 프론트 단독 · 정적 대조 시험으로 오라클이 선다.
2. **`BF-8`** — S-01 뿌리 여백·최대폭. 선행 없음 · `BF-5` 가 세운 관례를 그대로 따른다.
3. **`BF-7`** — 스크린샷 revoke 타이밍. 선행 없음 · 서버 경로 무접촉. ⚠ 완료 정의 ⑶ 이 크롬 실측 1회를 요구한다(브라우저 접근 필요).
4. **`BF-9`** — 계보 원천 → 루트 edge. 선행 없음. ⚠ 계약 `LineageEdge` 확인이 먼저이고, 개정이 필요하면 `CLAUDE.md §4` 대로 멈추고 보고한다.
5. **PRD·개발계획서 재작성** — 입력(조사 2건 · 3 문서 · 260826 결정 기록)이 전부 서 있고 산출물만 없다. 문서 작업이라 코드·staging 무접촉.

**Ted 판정이 있어야 움직이는 것**

6. **`CR-2` 판정 4갈래**(⑴ 계약 델타 · ⑵ 로그인 제한 XFF 버킷 · ⑷ tolerate 기한/`_FILE`/DLQ · ⑸ CI 인프라 red 둘) — 배포 불요 · 판정만 있으면 착수.
7. **배포 창 6 go/no-go** — 열려야 `BF-12` ⑶ 이 닫히고 그 뒤에야 `TL-2` 가 열린다. 창의 내용물은 `BF-12` 로깅 하나(＋문서)로 못 박혀 있다. ⚠ 다른 것을 이 창에 붙이지 않는다.
8. **기획 정면 충돌 5건 판정**(C-1~C-5) — 판정 전에는 3 문서의 코드 적용이 시작될 수 없다. C-1·C-2 는 DB 마이그레이션 ＋ 계약 개정 ＋ 기존 행 이행 규칙을 동반한다.
9. **`BF-10`** — Ted 가 회차를 열어야 착수.

**단계 대기(지금 열지 않는다)** — `J-1`(stage 2 마지막) · `PA-G`(미완 0) · `after_stage2` 15항목.

---

## §F 미확인

| # | 미확인 | 푸는 법 |
|---|---|---|
| 1 | **게이트 판정 기록 2건이 다르다** — 머리말(2026-09-05)은 「두 실행에 걸쳐 50 green · 한 번에 50 green 이 아니다」, `§2`(2026-09-03 `〈307〉`-㉵)는 「`all -j 1` 판정 = green 50 · red 0 · red 0」. 승자를 고르지 않았다 | `source ~/.colab-v2-test.env` 후 `./gates/run.sh all -j 1` 1회 실행해 실측 |
| 2 | **`lane-r18` 병합 여부** — `CR-2` ⑴ 18차 해제(커밋 `4e714ea`)가 `§4.5` 에서 「미병합」이고 「다음 병합 = `lane-r18`」인데, 로컬·원격 브랜치 목록에 `lane-r18` 이 없다 | `git log --oneline main | grep -i r18` · `git branch -a --contains 4e714ea` |
| 3 | **`bugfix/20260903-ui-12` 병합 여부** — `BF-7` 진입조건이 그 병합을 요구하는데 브랜치 목록에 없다(병합 후 삭제로 추정) | 위와 같은 방법 |
| 4 | **`BF-5` 상태** — `BF-8`·`BF-11` 진입조건인데 대장 미완 목록에 없다(`done` 추정) | `work-items.yaml` 의 `BF-5` 블록 직접 확인 |
| 5 | **`PA-G` 개시 계수** — 대장 note 축자로 앞 회차 19(`deferred` 제외 18)와 실측 17(자기 제외 16)이 다르고 정의 차인지 시점 차인지 그 회차가 재지 않아 `[미확인]` 으로 남겼다. 이 지도의 stage1·stage2 미완 12건도 세는 규칙이 다르면 달라진다 | 대장에서 `deferred`·`PA-G` 제외 후 `status != done` 전수를 한 회차에 세고 셈 근거를 남긴다 |
| 6 | **소유 계수 「판정 불가 19」의 유효성** — `§4.5`(`〈317〉`⑶)가 자리 파일 236 → 285 증가를 근거로 실측값이 아니라고 적었는데, `§2` 스냅샷은 여전히 19 를 적는다. 쓴 주체도 `[미확인]` | 다음 창에서 재굽기 **전에** `artifact-ownership` 실행 |
| 7 | **`〈304〉` 스파이크 원인**(1,164 / 2,111 ms) — `〈312〉`·`〈318〉` 두 회차 연속 재현 실패 ⟹ 후보 ⓓ(재기동 첫 접촉)로 설명되지 않는다. 첫 접촉 서버 안 비용이 `〈312〉` 의 2배 이상인 것도 원인 `[미확인]` | 재기동이 있는 배포 창 6 에서 첫 접촉 4건 재채집 |
| 8 | **PRD 산출 실패의 원인** — 8797 이 `files_modified` 에 2 파일을 적었으나 파일시스템에 없다. 에이전트가 쓰기 전에 종료됐는지, 다른 경로에 썼는지 구분되지 않는다 | S1396 세션 트랜스크립트 확인, 또는 재작성으로 대체 |
| 9 | **`CR-2` ⑴ 의 완료 정의 문면과 실측이 어긋난다** — 완료 정의는 세 op 의 410/413/415/422(12쌍)인데 실측상 실제로 나는 것은 2쌍뿐이고 나머지 10쌍은 생산자가 0건이다. 대장은 문면을 고치지 않은 채 evidence 에 그 사실을 적었다 | Ted 판정으로 완료 정의 문면을 실측에 맞출지 결정 |
| 10 | **`gates/README.md` 의 게이트 이름 수와 `run.sh ALL_GATES` 의 50** — README 표는 이름을 묶어 적은 행(`service-tests-*` 4종을 한 행)이 있어 표 행 수와 게이트 수가 일치하지 않는다 | `grep -c` 로 `run.sh` 의 case 전수를 센다 |
| 11 | **지시문 ↔ 레포 충돌 없음** — 지시문이 지목한 `dev-package/CLAUDE.md` 는 별도로 열지 않고 레포 루트 `CLAUDE.md §4`(작업 규약)를 근거로 썼다. 대장·게이트·`03-HANDOFF` 가 「진입조건 미충족이면 구현하지 말고 보고한다」를 `CLAUDE.md §4` 로 일관되게 인용한다 | `dev-package/CLAUDE.md` 존재 여부와 §4 대조 |
