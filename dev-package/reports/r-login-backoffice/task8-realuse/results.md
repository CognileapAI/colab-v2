# R-LOGIN-BACKOFFICE 작업 8 — dev 실사용 검증 (로그인 · 운영자 백오피스)

- 실행 2026-09-12 ~ 09-13 · 대상 `https://d31zgpff2091oh.cloudfront.net` (AWS dev)
- 배포 sha = `71ee15757737` (EC2 `CURRENT_SHA` · 컨테이너 4종 `colab-v2/*:dev-71ee15757737` healthy)
- 도구 = `agent-browser` 0.27.0 · 화면 조작과 화면 판독만 사용. 스크린샷 30장 = `shots/`
- 시험 계정 2개(전부 `@colab.test`)를 새로 만들었고 **Ted 계정(`ttlhi10@gmail.com`)은 읽기 외에 건드리지 않았다** —
  비밀번호·상태·운영자 등기 어느 것도 변경하지 않았다.
- 비밀번호는 0600 파일에서만 읽어 표준입력·DOM 주입으로 넘겼다. argv·로그·이 보고서에 원문이 없다.

## 0. 시험 자료

| 항목 | 값 |
|---|---|
| QA 운영자 | `qa-operator-20260912@colab.test` · `QA 운영자` · `01M2B24P6DV1RMRNM83MQSFC3X` · 연구실 A(`0000000000000000000000000A` 고려대학교 수문학연구실) · 역할 `연구원` |
| QA 사용자 | `qa-user-20260912@colab.test` · `QA 시험 사용자` · `01M2B2CPC1GBTKZKWX7QSH9Y0S` · 연구실 A · 역할 `연구원` |
| QA 운영자 발급 경로 | 컨테이너 `colab_v2_dev_core_api` 안에서 `accounts.py::create_account` 와 같은 트랜잭션 1건(`operator=True` 포함). 운영자 0명 상태에서 자기 자신을 만들 수 없어 제품 API 를 쓸 수 없다 — `task1-deploy/operator.md §3` 과 같은 이유 |
| 연구실 B | `0000000000000000000000000B` `B 연구실` 실재. **서비스 계정은 0건** — 계정 목록의 연구실 필터 B 는 항상 0행이다. 자료 쪽 픽스처는 있다(데이터셋 1건 · 업로더 `TEST-stage12-labB-20260911`) |

## 1. 단계별 결과

### 1 — 로그인 → 첫 비밀번호 변경 강제 → 랜딩

- 기대 = 로그인 화면 → 초기 비밀번호로 로그인 → 변경 강제 화면 → 새 비밀번호 → 랜딩
- 실측 = 전부 성립. `POST /api/v1/sessions` **201**, 변경 뒤 `/lab` 로 이동.
  - 로그인 화면 안내문 = 「운영자에게 받은 이메일과 초기 비밀번호를 넣어 주세요.」
  - 변경 화면 표제 = 「비밀번호 변경」 · 안내문 「처음 로그인하셨습니다. 사용할 새 비밀번호로 바꿔 주세요.」
  - 랜딩 GNB = `전체 연구실` · `연구실` · `프로젝트` · `데이터셋` · `업로드` · **`계정 관리`** · `QA 운영자` · `로그아웃`
- 뒷받침 = `shots/01-login-page.png` · `shots/02-forced-password-change.png` · `shots/03-landing-lab.png`
- 추가 = 뒤처리 단계에서 **바뀐 비밀번호로 재로그인 1회 성립**(변경이 실제로 저장됐다는 증거)

### 2 — `/account-admin` 목록과 필터

- 기대 = 열 6개(이메일·이름·역할·연구실·상태·최근 로그인) ＋ 필터 4종
- 실측 = 열은 **8개**다 — `이메일` `이름` `역할` `연구실` `상태` **`관리자`** `최근 로그인` **`행 동작`**.
  지시문의 6개는 부분집합이고 초과 2열은 결손이 아니라 이번 회차 추가분이다(`AccountAdminPage.tsx` 의 `<thead>`).
- 필터 실측 —

| 조작 | 요청 | 코드 | 행 수 |
|---|---|---|---|
| 없음 | `GET /api/v1/admin/accounts` | 200 | 2 → (계정 추가 뒤) 3 |
| 연구실 = B 연구실 | `?labId=0000000000000000000000000B` | 200 | **0** · 「조건에 맞는 계정이 없어요.」 |
| 상태 = 비활성 | `?status=inactive` | 200 | 0 (그 시점 비활성 계정 없음) |
| 역할 = 교수 | `?role=%EA%B5%90%EC%88%98` | 200 | 0 (교수 계정 없음) |
| 이메일 = `ttlhi` | `?email=ttlhi` | 200 | 1 (부분 일치 동작) |
| 연구실 = 고려대…＋이메일 | `?labId=…A&email=ttlhi` | 200 | 1 (두 필터 동시 적용) |
| 연구실 = 고려대… | `?labId=…A` | 200 | 2 |

- 뒷받침 = `shots/04-account-admin-list.png` · `shots/05-filter-lab-b-empty.png` · `shots/06-filter-email-substring.png`

### 3 — 계정 추가

- 기대 = `qa-user-20260912@colab.test` 추가 → 목록에 등장 · 상태 `첫 로그인 대기`
- 실측 = `POST /api/v1/admin/accounts` **201** · 안내 「qa-user-20260912@colab.test 계정을 추가했어요.」
  - **상태 표기는 `첫 로그인 대기` 가 아니라 `사용 중`** 이다. 화면은 `login_credential.status`(`active`/`inactive`)만 보여 주고
    `must_change_password` 를 상태 열에 반영하지 않는다(`AccountAdminPage.tsx` `STATUS_LABEL`). 첫 로그인 여부는
    **`최근 로그인` 열의 `기록 없음`** 으로만 드러난다.
  - 관리자 열 = `아니요`(「관리자로 등록」 미체크 → 요청에 `operator` 를 아예 싣지 않는다)
- 뒷받침 = `shots/07-account-create-form.png` · `shots/08-account-created.png`
- ⚠ **도구 쪽 걸림돌 1건(제품 결함 아님)** — `agent-browser click` 은 대상이 뷰포트 아래(버튼 y=795 · 뷰포트 h=577)면
  클릭 좌표가 문서 밖이라 **아무 일도 일어나지 않고 `✓ Done` 을 낸다**. `elementFromPoint` 가 `null` 이었다.
  `scrollintoview` 를 먼저 부른 뒤에야 제출됐다. 처음 두 번의 「제출 실패」는 이 원인이고 화면 쪽 문제가 아니다.

### 4 — 비밀번호 재설정 → 그 사용자로 로그인

- 기대 = 재설정(새 값 ＋ 확인) → 성공 안내 → 별도 브라우저에서 새 비밀번호로 로그인 → 변경 강제 화면
- 실측 = `POST /api/v1/admin/accounts/01M2B2CPC1GBTKZKWX7QSH9Y0S/password-reset` **200** ·
  안내 「qa-user-20260912@colab.test 의 비밀번호를 재설정했어요.」
  - 대화상자 = 표제 「비밀번호 재설정」 · 칸 「새 초기 비밀번호」 「새 초기 비밀번호 확인」 · 단추 「그대로 두기」 「재설정」.
    두 칸이 같아지기 전까지 「재설정」은 `disabled`.
  - 별도 브라우저 세션 로그인 = **201** → 「비밀번호 변경」 강제 화면. 그 이상 진행하지 않고 세션을 닫았다.
  - 응답에 비밀번호 필드 없음(계약대로).
- 뒷받침 = `shots/09-password-reset-dialog.png` · `shots/10-password-reset-done.png` · `shots/11-qauser-forced-change.png`

### 5 — 비활성화 → 로그인 거절 문구 대조 → 재활성화

- 기대 = 비활성화 → 상태 변경 · 새 세션 로그인 시 **비밀번호 틀림과 같은** 오류 → 재활성화
- 실측 —

| 시도 | 비밀번호 | 계정 상태 | 코드 | 화면 문구 |
|---|---|---|---|---|
| ㉮ | 틀린 값 | 사용 중 | **401** | 「계정 또는 비밀번호가 맞지 않아요. 비밀번호 변경 중 연결이 끊겼다면 기존 초기 비밀번호도 직접 시도해 보세요.」 |
| ㉯ | 맞는 값 | **비활성** | **401** | ㉮ 와 **한 글자도 다르지 않다** |

- 비활성화 = `POST …/status` **200** · 안내 「qa-user-20260912@colab.test 을 비활성화했어요.」 · 상태 열 `비활성`
- 재활성화 = **200** · 안내 「…을 다시 활성화했어요.」 · 상태 열 `사용 중`
- 대화상자 표제는 상태에 따라 「계정 비활성화」/「계정 재활성화」로, 행 단추는 「비활성화」/「재활성화」로 바뀐다.
- 뒷받침 = `shots/12-wrong-password-401.png` · `shots/13-deactivate-dialog.png` · `shots/14-status-inactive.png` ·
  `shots/15-inactive-login-401.png` · `shots/16-status-reactivated.png`

### 6 — 운영자 지정 · 해제 · 거절

- 기대 = 지정 → 해제 → 자기 자신 해제 거절 · 「마지막 한 명」 가드 확인
- 실측 —

| 조작 | 요청 | 코드 | 결과 |
|---|---|---|---|
| `qa-user` 관리자 지정 | `POST …/01M2B2CPC1GBTKZKWX7QSH9Y0S/operator` `{operator:true}` | **200** | 관리자 열 `아니요` → `관리자` · 안내 「…을 관리자로 지정했어요.」 |
| `qa-user` 관리자 해제 | 같은 경로 `{operator:false}` | **200** | 관리자 열 `관리자` → `아니요` · 안내 「…의 관리자 권한을 해제했어요.」 |
| **자기 자신 해제** (화면) | — | — | 자기 행의 「관리자 해제」 단추가 **`disabled`** — 누를 수 없다 |
| **자기 자신 해제** (서버) | `POST …/01M2B24P6DV1RMRNM83MQSFC3X/operator` `{operator:false}` | **400** | `{"code":"BAD_REQUEST","message":"자기 자신의 관리자 권한은 해제할 수 없다. 다른 관리자에게 요청한다."}` |
| **자기 자신 비활성화** | `POST …/01M2B24P6DV1RMRNM83MQSFC3X/status` `{status:"inactive"}` | **400** | 화면 안내 「자기 계정은 비활성화할 수 없다.」 |

- 화면의 `disabled` 가 서버 가드를 대신하지 않는다 — 둘 다 있고 문구도 서로 다르다(화면은 누르지 못하게, 서버는 사유를 말한다).
- **[미확인] 「마지막 한 명」 가드(운영자가 1명만 남는 해제)는 재현하지 못했다.** 재현하려면 운영자 2명 중
  Ted 의 등기를 지우거나 Ted 로 로그인해야 하고, 둘 다 Ted 계정 조작이라 이번 지시가 금지한 범위다.
  자기 자신 해제가 400 으로 먼저 막히므로 **QA 운영자만으로는 이 가드에 도달하는 경로가 없다.**
- 뒷받침 = `shots/17-operator-designate-dialog.png` · `shots/18-operator-granted.png` · `shots/19-operator-revoked.png` ·
  `shots/20-self-revoke-disabled-400.png` · `shots/30-self-deactivate-400.png`

### 7 — 전 연구실 읽기

- 기대 = GNB 에 「전체 연구실 (읽기 전용)」 · 타 연구실 데이터셋 1건 이상 노출 · 다운로드 거절(404) · 편집 동작 부재/비활성
- 실측 —
  - **GNB 표기 두 겹** — 화면에 보이는 글자는 **`전체 연구실`** 이고, 단추의 `aria-label` 이
    **`연구실 전환 · 전체 연구실 (읽기 전용)`** 이다(`frontend/src/shell/Gnb.tsx:76,79`).
    「(읽기 전용)」은 **눈에 보이지 않는다.**
  - 이 단추는 **눌러도 아무 일이 없다** — 전환 목록이 없고 `▾` 도 일부러 달지 않았다(같은 파일의 주석이 그 판정을 적어 둔다).
  - 데이터셋 목록 = **26건**. 그 안에 **연구실 B 의 데이터셋 1건**이 보인다 —
    `TEST Stage12 LabB Binary Grid Candidate 20260911` · 업로더 `TEST-stage12-labB-20260911`.
  - 그 데이터셋 상세(`/datasets/01M27S31B82M6BG21HFK368R42`) 진입 성공.
  - **다운로드 = `GET /api/v1/datasets/01M27S31B82M6BG21HFK368R42/download` → 404**,
    화면 문구 「이 파일은 더 이상 없어요.」
  - 같은 화면에서 `POST /api/v1/previews` **404** · `POST /api/v1/preview-target-descriptions` **404**,
    문구 「지금 미리보기를 만들 수 없어요. 잠시 뒤 다시 시도해 주세요. / 다운로드·계보 확인은 그대로 할 수 있어요.」
  - 프로젝트 = 1건 노출(`TEST Stage12 Palette Usage 20260911`) · 「닫힘 1건은 숨겨져 있어요」
  - 연구실 화면(`/lab`) = 데이터 맵·최근 활동·할 일 함 전부 렌더.
- ⛔ **어긋남 — 편집 동작이 부재도 비활성도 아니다.** 타 연구실(B) 데이터셋 상세에서 다음 단추가 **전부 보이고 활성**이다 —
  `수정` · `기준 격자 추가` · `파일 관리` · `파일 추가`(파일 선택 입력) · `계보 수정 · 추가` · `계보 채우기`.
  화면은 「읽기 전용」이라고 적어 두고 편집 진입점을 그대로 둔다.
  - **이 어긋남이 걸리는 검사 = 없다.** `gates/run.sh` 에 화면 권한 표기를 대조하는 게이트가 없고,
    `deploy_doctor` 15 항목에도 없다. 즉 이 상태는 어떤 자동 검사도 통과한다.
  - **[미확인] 서버가 그 편집을 실제로 거절하는지는 재지 않았다** — 확인하려면 타 연구실 자료에 쓰기를 시도해야 하고,
    이번 지시의 범위(읽기 확인)를 넘는 비가역 조작이라 실행하지 않았다.
- 뒷받침 = `shots/21-datasets-all-labs.png` · `shots/22-labb-dataset-detail.png` · `shots/23-labb-download-404.png` ·
  `shots/24-projects.png` · `shots/25-gnb-all-lab-readonly.png`

### 8 — 한 탭 로그아웃 → 다른 탭도 로그아웃

- 기대 = 탭 2개 → 한 탭에서 로그아웃 → 다른 탭도 로그아웃
- 실측 = 탭 t1(`/lab`) · t2(`/datasets`) 를 띄운 뒤 t1 에서 「로그아웃」 →
  t1 이 로그인 화면. t2 로 전환하니 **URL 은 `/datasets` 그대로인데 화면이 로그인 화면**이다(새로고침 없이).
- 뒷받침 = `shots/26-two-tabs-tab2.png` · `shots/27-logout-tab1.png` · `shots/28-logout-propagated-tab2.png`

## 2. 합격 표

| 단계 | 판정 | 비고 |
|---|---|---|
| 1 로그인 → 변경 강제 → 랜딩 | 통과 | 201 · 변경 뒤 재로그인도 성립 |
| 2 계정 목록 · 필터 4종 | 통과 | 열 8개(지시문 6개의 상위집합) |
| 3 계정 추가 | 통과(표기 차이) | 상태 = `사용 중` · 첫 로그인 여부는 `최근 로그인 = 기록 없음` |
| 4 비밀번호 재설정 → 그 계정 로그인 | 통과 | 200 → 201 → 변경 강제 |
| 5 비활성화 · 거절 문구 · 재활성화 | 통과 | 비활성 401 문구가 오답 401 과 동일 |
| 6 운영자 지정 · 해제 · 자기 해제 거절 | 통과 | 「마지막 한 명」 가드는 [미확인] |
| 7 전 연구실 읽기 | **부분 미달** | 타 연구실 데이터셋 노출·다운로드 404 는 통과 · **편집 단추 활성이 어긋남** |
| 8 탭 간 로그아웃 전파 | 통과 | 새로고침 없이 전파 |

## 3. 뒤처리 실측

| 대상 | 처리 | 확인 |
|---|---|---|
| `qa-user-…` | 화면에서 비활성화 | `login_credential.status = inactive` · `session_version 8` |
| `qa-operator-…` | 운영자 등기 삭제 ＋ 비활성화(DB 직접 — 자기 해제·자기 비활성화가 400 으로 막힌다) | `status = inactive` · `session_version 3` |
| 운영자 수 | 삭제 전 「나 말고 1명」 확인 뒤 삭제 | `service_operator` **1행** (Ted) — 0명이 되는 경로를 취하지 않았다 |
| Ted 계정 | 변경 없음 | `ttlhi10@gmail.com` `active` · `session_version 4`(이번 회차 변경 0) |
| 계정 행 | **삭제 0건** — `d1_account`·`d2_member_role`·`login_credential` 행은 남는다 |
| 비밀 파일 | `shred -u` | 실행 뒤 부재 확인 |
| 서버 임시 스크립트 | `/tmp` · 컨테이너 `/tmp` 양쪽 삭제 | `No such file or directory` |

## 4. 후속 항목

1. **타 연구실 데이터셋 상세의 편집 진입점** — 운영자 전 연구실 열람에서 `수정`·`파일 관리`·`파일 추가`·
   `기준 격자 추가`·`계보 수정 · 추가`·`계보 채우기` 가 활성이다. 표기(「읽기 전용」)와 화면이 어긋난다.
   **어느 게이트에도, `deploy_doctor` 에도 걸리지 않는다** — 검사가 없는 것 자체가 항목이다.
2. **「마지막 운영자 해제」 가드의 시험 경로가 없다** — 자기 해제가 먼저 400 이라, 운영자 2명 중 하나로는
   도달할 수 없다. 시험하려면 운영자 3명 또는 서비스 시험(`test_account_backoffice.py`) 쪽 오라클이 필요하다.
3. **계정 목록의 상태 열이 「첫 로그인 대기」를 표현하지 않는다** — `must_change_password` 가 목록 응답에 없다.
   현재는 `최근 로그인 = 기록 없음` 으로 간접 추정한다.
4. **연구실 B 에 서비스 계정이 0건** — 계정 관리 화면의 연구실 필터·전 연구실 목록을 실계정으로 교차 검증할 자료가 없다.
   자료(데이터셋) 쪽 픽스처만 있다.
