⛔ `gates-all-w9-ABORTED.txt` 는 **중단된 전수다 — 판정이 아니다.**

Ted 판정 2026-09-06 11:35 (축자) — 「이 레인에서 전수를 돌리지 않는다.
다음 세션이 `main` ff 전에 `-j 4` 전수를 돌린다」.

시작 2026-09-06 11:21:50 (KST) · 중단 11:37 경 · **결과 줄이 없다.**
돌던 트리 = `70d1157f940adad1c714edaf3ae20d2daf72b077` (커밋 `20b3715d1db0`) —
그것도 **최종 트리가 아니다**(그 뒤 원장·산문 커밋이 더 얹혔다).

⛔ 이 파일을 「green」으로도 「red」로도 읽지 않는다. **돌다 만 것이다.**
   판정 근거로 쓰지 않는다 (`CLAUDE.md §4` — 검사를 못 한 것은 통과가 아니다).

이 레인이 실제로 돌린 것 = **좁은 문서 게이트 셋뿐**이고 그 로그는
`narrow-doc-gates.txt` 다. 그 셋은 전수를 갈음하지 않는다.
