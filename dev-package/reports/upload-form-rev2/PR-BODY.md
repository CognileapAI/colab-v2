# 업로드 모달 입력을 기획서 rev2 ＋ 기획자 9/13 피드백에 맞춘다 — Ted 판정 대기

## 무엇이 달라지는가

**업로드 모달의 등록·연결·미리보기 세 단계가 최근 기획서(rev2)와 기획자 2026-09-13 구두 피드백을
따라 다시 짜였다.** 계약·서버·데이터베이스는 건드리지 않았다 — 화면과 화면 시험만 바뀐다.

### ② 메타데이터 단계

- 사람이 적는 칸의 순서가 목업 순서로 바뀐다 — 이름 → 기간 → 관측 간격 → 좌표계 → 격자 → 설명 →
  변수 표 → 공개 범위.
- 변수 표가 선이 있는 표로 바뀌고, 행 삭제 버튼이 글자 `삭제` 대신 `×` 로 바뀐다. 표 아래에
  `+ 변수 추가` 가 그대로 남는다.
- 필수·선택 표시가 알약 배지에서 라벨 옆 작은 글자(붉은 글자 `필수` · 흐린 글자 `선택`)로 바뀐다.
  사용처·`data-testid` 는 그대로다.
- `주제` 입력 칸이 사라진다. 읽는 쪽(목록·상세·필터)과 데이터베이스는 그대로다.
- 데이터셋 등록 버튼을 누를 때 검증이 한 곳(등록 게이트 6단)으로 모인다 — 이름·설명·분류·유형·
  기간 시작·관측 간격·Lv0 출처 두 칸 중 처음 걸리는 하나만 토스트로 알리고 그 입력 단계로 이동한다.
- 격자 칸이 여러 줄 입력에서 한 줄 입력으로 바뀌고, 칸 아래 안내 문단이 사라진다.
- 기간 안내 문단은 문면은 그대로이고 자리만 종료 칸 옆으로 옮겨진다.

### ③ 연결 단계

- 이어받을 데이터를 추가하는 버튼 문구가 `앞 데이터 직접 추가` 에서 `+ 가공 전 데이터 추가` 로 바뀐다.
- 연결한 데이터가 하나도 없을 때 `아직 연결한 가공 전 데이터가 없어요` 한 줄이 버튼 위에 뜬다.
- 후보를 고르는 창이 라디오 단일 선택으로 바뀐다 — 이름·가공 단계·분류·기간 네 정보만 보이고,
  파일이 여럿이면 `대표 파일명 외 N개` 로 줄여 표시한다. 목록은 높이가 고정되고 스크롤이 생기며,
  하단에 선택 요약·가공 방식 입력·`취소`/`이 데이터로 연결` 버튼이 고정된 영역으로 붙는다.
- 후보를 찾는 검색 칸이 하나로 줄어든다 — 분류·주제·가공 단계·기간 두 칸이 사라지고 검색어 한 칸만
  남는다.
- 원천(연구실 밖에서 가져온 자료) 정보를 적는 칸은 **원시 자료이거나 이어받은 데이터가 하나도 없을
  때만** 보인다. 종전에는 항상 보였다.
- 「AI 가 앞 데이터를 찾아 추천해 준다」 버튼과 그 호출이 화면에서 사라진다. 앞 데이터는 사람이
  직접 고르는 것 하나만 남는다.

### 좌측 미리보기

- 업로드 화면에서만, 변수가 하나뿐이거나 시각이 하나뿐이면 그 고르는 칸이 사라진다(후보가 둘 이상일
  때만 뜬다). 데이터셋 상세 화면과 확장보기 화면은 그대로다.
- `.npy` 파일의 변수 이름과, 거절된 파일의 이름이 이제 업로드할 때 붙인 원래 파일 이름으로 보인다
  (종전에는 서버 내부 식별자로 보였다).

## 바꾸지 않은 것

- 데이터베이스 스키마 변경 0건.
- 화면-서버 약속 문서(계약) 변경 1건 — `RenderTarget` 에 선택 필드 `fileNames` 를 추가했다. 기존
  호출은 그대로 컴파일되고 `contract-breaking` 게이트가 green 이다(파괴적 변경 없음).
- 서버 쪽 검증은 그대로다 — 관측 간격과 원시 자료 출처 정보는 **화면만** 필수로 막고, 서버는 지금도
  값 없이 등록을 성공시킨다. 화면 경로와 다른 경로(직접 호출)의 비대칭이 남는다.
- 데이터셋 상세·확장보기 화면의 고르는 칸 동작은 그대로다.
- AI 서비스 자체는 손대지 않았다 — 제안 엔드포인트와 중계 코드, 자동 생성 타입은 남아 있고
  호출하는 화면 자리만 없어졌다.

## Ted 판정이 필요한 것 (병합 조건 · 8건)

여덟 건 모두 화면이 이미 그 방향으로 바뀐 상태이고, 승인·기각 어느 쪽도 정본·기획서만으로는
확정할 수 없어 판정을 기다린다. 판정문 전문은 근거 절 참조.

| 항목 | 기존 판정 | 새 근거 | 지금 화면 상태 | 권고 |
|---|---|---|---|---|
| 계보 확정 화면의 AI 추천 버튼 | 없음(신규 물음) | 이번 기획서·기획자 구두 의견 — 이 화면에 AI 요소를 두지 않는다 | **이미 제거됨** — 계약·자동 생성 타입·AI 서비스는 무변경. 인터페이스 멤버 하나는 시험 15개 파일의 스텁이 구현하고 있어 걷으면 전부 실패해 존치(후속 항목). 음성 시험 3건 신설. 관련 화면 시험의 판정 자리 1건이 없어졌다(사후 경고는 다른 시험이 계속 잰다) | 판단 자료 부족 — 확정하거나 되돌리거나, 근거상 자동으로 정해지지 않는다 |
| 등록 필수 항목 확대 | 없음(신규 물음) | 화면-서버 약속 문서의 필수 목록과 실제 화면 요구값의 차이 | 서버는 이름·설명·분류·유형만 요구한다. 화면은 그보다 더 요구한다 | 아래 두 항목 승인 시 함께 계약·서버를 넓힌다 |
| 원천 정보 칸 노출 조건 | 확정(2026-09-05, 「항상 보인다」) | 기획서 rev2 ＋ 기획자 9/13 구두 피드백 「원시 자료이거나 연결이 0건일 때만」 | **이미 반대 방향으로 구현됨** — 종전 확정 판정과 반대다 | 승인 시 재판정 완료로 기록, 기각 시 되돌린다 |
| 관측 간격 필수화 | 확정(2026-09-05, 「선택 입력」) | 기획자 9/13 구두 피드백 「기간과 같은 시간축 정보라 간격을 모르면 그 기간이 몇 장인지 읽을 수 없다」 | **화면만 이미 구현** — 서버는 여전히 선택으로 저장한다 | 화면·서버 동시 승인이거나 전면 기각 — 화면만 승인하면 직접 호출 경로가 뚫린 채로 남는다 |
| 원시 자료 출처 주소·내려받은 날 필수화 | 확정(기존 계획서, 「선택 입력」) | 기획자 9/13 구두 피드백 ＋ rev2 목업 배지 채택 | **화면만 이미 구현** — 서버는 여전히 값 없이 저장한다 | 위와 같은 갈래 |
| 연결 0건 빈 상태·「기록 없음」 라벨 | 확정(2026-09-05, 라벨 문면 못박음) | rev2 목업 축자 — 다만 지시문이 지목한 자리와 목업 실물의 요소가 다르다(체크박스 라벨 vs 0건 빈 상태) | **기존 확정 축자 유지**(체크박스 미변경) · 0건 빈 상태는 rev2 축자로 별도 신설 | rev2 축자로 체크박스까지 바꿀지, 기존 확정 유지할지 |
| 후보 찾기 창의 가공 단계 필터 제거 | 확정(2026-09-05 감사로 요구 항목 등재) | 기획자 9/13 피드백 「핵심 정보만 · 필터는 검색어 하나」 | **이미 제거됨** — 죽은 연결 하나(`onLevelFilterChange`)가 남는다 | 승인(죽은 연결 정리) / 기각(필터 복원) |
| 등록 화면 안내 문단 배치 | 확정(2026-09-12 spec 수용 기준 — 격자 칸 안내 위치 무회귀, 안내 문단 순서 규칙) | rev2 목업 축자 — 격자 칸에는 안내 문단이 없고, 기간 안내 문단은 자리만 옮긴다 | **화면만 이미 구현** | 판단 자료 부족 — 승인(spec 개정) / 기각(격자 칸 안내 문단 복원, 기간 안내 문단 자리 복귀) |

여덟 건의 문제 서술·선택지별 대가·설계 결정 근거는 판정문
`dev-package/intent/2026-09-13-upload-form-rev2.md` 에 있다. 승인 자리는 다음과 같다.

- 기획서 5곳(관측 간격·원천 조건·원시 자료 두 칸·후보 필터·등록 폼 표) — `dev-package/prd/PRD-260905-적용전기획.md`.
- 존치 목록 1곳 — `dev-package/prd/specs/R-A2.md`.
- 상시 지침 1곳 — `CLAUDE.md §0` 「AI 두 지점」 행.
- 대장 1곳 — `dev-package/work-items.yaml` `LV-2` 항목의 개정 표시.

## 바꾸지 않은 것을 지키는 게이트

조립한 트리에서 각 게이트를 1회씩 실행했다(부분 실행 합산이 아니다).

- 계약 3종 — `contract-lint` · `contract-breaking` · `generated-up-to-date`, 전부 green(파괴적 변경
  없음 · 자동 생성물 등기부와 일치).
- 프론트 3종 — `frontend-typecheck` · `frontend-test` · `frontend-fixture-reach`, 전부 green(형제
  레인 동시 실행 구간의 흔들림은 단독 재실행에서 전건 통과로 확인, 근거는 각 레인 보고서).
- 데이터 시각화 서비스 시험(`service-tests-viz-render`) green.
- 대장 정합(`work-item-consistency`) green.
- `service-tests-core-api` 는 이 조립 자리에서 **준비 red** 다 — 이 기계에서 일회용 데이터베이스가
  네트워크로 닿지 않는 환경 문제이고(원인·대체 측정 결과는 `dev-package/reports/upload-form-rev2/lane-B.md`
  7절), 코드 결함이 아니다. 대체로 돌린 미리보기 관련 시험은 전건 통과했다. 최종 판정은 PR CI 의
  서비스 시험 잡에서 받는다.
- 전수 게이트(`gates/run.sh all`)는 사용자 터미널 실행분 자리를 비워 둔다 — `[미확인 · 실행 뒤 기입]`.

## 대장

- `UF-1` — `blocked`(위 여덟 건 판정 대기가 병합을 막는다).
- `UF-2` — `partial`.
- `UF-3` — `open`(아래 후속 항목이 이 상태를 남긴다).
- 결정 번호는 임시 〈397〉로 적었고, 병합 직전 `origin/main` 최대 번호로 재실측한다.

## 후속 (판정 뒤 진행)

- 서버·계약 필수 목록 확대 — 관측 간격·원시 자료 출처 두 칸을 서버 400 거절 대상에 넣는 작업.
  계약 동결 해제 20차 뒤에 얹을지 21차를 새로 열지는 판정 뒤에 정한다.
- 사용하는 곳이 없는 인터페이스 멤버(`LineageSource.suggestions`) 정리 — 시험 15개 파일의 스텁을
  같은 커밋에서 함께 고쳐야 한다.
- `dev-package/prd/PRD-260905-적용전기획.md` 의 후보 찾기 창 필터 항목(PRD-08)에 개정 표시 마무리.
- 화면-서버 약속 문서 산문에 거절 파일 이름 표시 규칙을 추가.
- 호스트 환경 결함 목록(각 레인 보고서에서 수집) — 호스트 부하에서 화면 시험이 흔들리는 문제,
  이 기계에서 서비스 시험용 일회용 데이터베이스가 닿지 않는 문제, 업로드 화면 CSS 의 다크 테마
  대응을 재는 검사가 없는 문제 등. 어느 검사에도 걸리지 않는 항목은 각 보고서에 그 사실과 함께
  적혀 있다.

근거 — 커밋 17건(`git log --oneline b5e8e79f..HEAD`), 레인 보고서
`dev-package/reports/upload-form-rev2/lane-A1.md`·`lane-A2.md`·`lane-A3.md`·`lane-A4.md`·`lane-B.md`·`lane-C1.md`,
판정문 `dev-package/intent/2026-09-13-upload-form-rev2.md`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01E843FPmKzSLnStzS9YCx97
