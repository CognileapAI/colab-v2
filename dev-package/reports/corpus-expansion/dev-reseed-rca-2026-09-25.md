# dev 재시드 반복 실패 — 근본 원인 분석 (2026-09-24~25)

메타 — 가지 `k3-resume`(`f890d58a` 위) · task `3a07efeaa9564053908b2c6e369dddef` · **dev·컨테이너 무접촉**(dev 로그 수치는 오케스트레이터·조사 레인의 읽기 전용 실측 인용 · 저장 파일 없음).
file:line — 제품 = `ea21d8c2aa54`(dev 배포본 · `git show ea21d8c2aa54:<path>`) · DR-4 대조 = `d5cd6ca9` · 도구 = `f890d58a`(도구 트리 = `c24c03d4`).
run-dir = `…/agent-a9c33a67acf039b61/dev-package/reports/dev-reseed-runs/wu2-20260924T0834Z/`. 입력 = 문제 기록 `dev-reseed-issues-2026-09-24.md`(이하 「문제 #n」) ·
실행 보고 `wu2-reseed-2026-09-24.md` · 인계서 `HANDOFF-20260924-k3-k4-corpus.md` §8 · 조사 메모 R1~R3(추적 제외). **결정·코드 변경 없음.**
추기 2026-09-25 — task `770eaed017dd41fbb756ac88fdf88b5d` · 주장 검증(C1~C10 · 16 관점)과 사고 조사(reset 직전 BYPASSRLS 백업 행 분석 · S3 삭제 표지 HEAD 표본 · 읽기 전용 재계수)를 반영했다.
정정한 서술은 제자리에서 고쳤다. 새 결정은 §0′-6 의 사용자 결정만 적는다 · 코드 변경 없음.

## 0′. 사고 — 9-24 reset 이 dev 데이터를 지웠다

### 0′-1 무엇이 지워졌나
- 1차 `reset` 08:33:26Z 계수(연구실 경계) = 데이터셋 35 · 파일 585 · 프로젝트 6 · 계보 22 · S3 `uploads/` 1,028 · `previews/` 750(`logs/reset.log` 7행)
  → 08:33:39Z 두 체인 `DROP SCHEMA` → 08:34:12–17Z `s3-apply` 1,778 키 삭제(실패 0).
- 07:40:53Z 백업(BYPASSRLS) = **4,189행** — 계정 8 · 데이터셋 35(살아 있음 33) · 파일 585(3,947,014,464 B · 전부 S3) · 프로젝트 6 · 계보 22 · 업로드 147 ·
  autometa 35(format 31 · crs 25 · grid 30 · period 35). reset 계수와 같다 — 07:40 → 08:33Z 사이 사라진 것은 없다.
- 데이터 행은 전부 2026-09-16 에 생겼다.
  - ① 시드 코퍼스(재시드 `d81e4387`) — 데이터셋 28 · 프로젝트 4 · 계보 18 · 계정 5. 이번 계획과 바이트가 같다(3,641,736,593 B). 이번 재시드가 새 id·키로 다시 만들었다 — 잃은 것은 id·키와 부가 상태다.
  - ② 팀원 QA 계정 3 의 QA 자료(12:57–15:15Z) — 살아 있는 데이터셋 5 · 프로젝트 2(「테스트」 · 「검증용 프로젝트 2026-09-16」) · 계보 4 · 다운로드 2.
    파일 16 중 13 은 시드 파일과 바이트가 같고 고유 내용은 PNG 3(≈2.2 MB)이다. 관련 GitHub 이슈 #89–#99 는 모두 닫혔고 증거는 GitHub 에 남아 있다.
- 9-16 15:15Z → 9-24 07:40Z 데이터 변경 0 — 9-18·9-24 백업의 표별 행 집합이 같다(alembic 0032 → 0033 만 다름).
- S3 1,778 = `uploads/` 1,028(1,024 를 DB 가 참조 · 등록 파일 585 전부 · 만료 미등록 업로드 111세션 포함) ＋ `previews/` 750(재생성 가능 산출물). 참조 없는 키는 4건뿐이다.

### 0′-2 거짓 0 — 전제가 선 자리
- 전제 「dev 플랫폼 DB = 0」은 계수 세 번에서 왔다 — 2026-09-22 14:12Z · 09-24 06:43:57Z · 06:57:10Z(WU0 `wu0-precheck-2026-09-24.md` §2-1 · intent `2026-09-24-corpus-expansion-dev-reseed.md` 17행).
  셋 다 core-api 컨테이너에서 앱 롤 `colab_app`(NOBYPASSRLS)으로 `app.current_lab` 없이 `count(*)` 를 냈다.
- 센 표는 전부 FORCE RLS · 정책 `lab_id = current_lab_id()` — GUC 가 없으면 NULL → 0. RLS 가 없는 `d1_lab` 만 1 로 보였다. 「연구실 1 · 나머지 0」이 거짓 0 의 표지다.
- 같은 방식으로 다시 세면 지금도 0, BYPASSRLS 롤로 세면 28 이다. 같은 실패가 `.agents/rules/deploy.md` 10번 증보 〈363〉(2026-09-06)에 이미 적혀 있다 — **재발**.
- 반증은 손에 있었다 — 07:40Z 백업 219,290 B 는 빈 상태 기준치 20,983 B 의 10배다. 비교하지 않았다(`wu2-prereq-2026-09-24.md` §2-1). `stage_reset` 계수 줄은 레인이 `tail -30`·좁은 grep 만 봐서 그날 어느 기록에도 없다.

### 0′-3 공동 원인
| # | 원인 | 빠진 것 |
|---|---|---|
| ① | 거짓 0 전제 | 「없다」를 경계 없는 앱 롤 계수로 판정 · 재측정 없이 intent·게이트까지 전파 |
| ② | reset 도구 fail-open | `stage_reset` 은 `count-before.json` 이 있는지만 본다(`stages.sh:190-191` `test -s`) · `_phase_count` 는 보고만 하고 0 반환(`reset_dev_environment.py:279-307`) → 35·585 를 세고 13초 뒤 DROP |
| ③ | 재측정 없는 게이트 ③ advisor | 07:33Z go 논거 첫 줄 = 「DB 0 · S3 1,778 = 고아」 · 판정문에 스스로 「원격 사실은 재측정하지 않음」 |
| ④ | 비어 있음 조건 없는 상시 승인 | dev 상시 승인(`deploy.md` 11번 개정 〈398〉)의 조건은 게이트 넷(대상 식별)뿐 |

- 거짓 0 은 go 판단의 필요조건이었지만 단독 원인은 아니다 — ②가 계수를 기대값과 비교했으면 35·585 에서 멈췄다.
- S3 계획은 접두사 전수 나열이고 DB 참조를 대조하지 않으며(`reset_dev_environment.py:357-380`) DROP 뒤에 돈다 — 「고아」 판정은 구조상 검증할 수 없었다. 관련 열린 이슈 #59(S3 계획 검토를 reset 앞으로) · #49 ⑶(계수 0).

### 0′-4 세 번째 반복
- reset 이 비영 계수를 보고도 기존 자료를 지운 것이 이번이 세 번째다.
  9-15 05:59:27Z · 06:05:22Z 재시드 `a7d9a6d8`(9-14 시드 · 계수 30·567·4·18 · S3 1,271 키) → 9-16 02:03:32Z 재시드 `d81e4387`(9-15 저녁 사람 테스트 · 데이터셋 3 · 계수 3·3·6·0 · S3 141 키) → 9-24.
- 계수 순서 = 데이터셋 · 파일 · 프로젝트 · 계보. K4 골든 id 부재(wu0 §2-1)는 결론만 맞다 — 골든 id 는 9-13 세대 교체 때 이미 없었다(07:40Z 백업에도 9건 중 0).

### 0′-5 복구 가능 창
- DB — 07:40:53Z 백업에 4,189행 전부. `_ops/backups/` 수명주기 backups-30d → ≈2026-10-24 만료.
- S3 — versioning Enabled · 삭제는 delete marker(표본 HEAD 로 확인) · expire-noncurrent-30d → ≈2026-10-24 부터 비현행 버전 영구 삭제.
  1,778 키 전부에 비현행 버전이 있는지는 목록 권한이 없어 미확인이다.
- 시드 28 은 이번 재시드가 같은 바이트로 다시 올렸다. 고유 손실 = QA 자료(고유 PNG 3 ≈2.2 MB) · 팀원 QA 계정 3 · dev 재현 상태. 만료 미등록 업로드 111세션(≈4.19 GB)도 지워졌다(이미 회수 대상이었을 가능성).

### 0′-6 결정 (2026-09-25 · 사용자)
- QA 자료는 복원하지 않는다. 팀 공지는 사용자가 한다.
- 백업을 로컬에 따로 보존하지 않는다(창이 지나면 소멸한다).
- **dev 재시드는 reset 게이트를 고칠 때까지 동결한다** — DROP 직전 계수가 0 이 아니면 멈춘다 · 계수는 BYPASSRLS 롤 또는 API 로만 센다.

### 0′-7 이 문서의 나머지와의 관계
- §1~§3 은 시간 손실만 센다. 이 사고는 데이터 삭제 사고라 시간 순위 밖에 둔다. 예방 후보는 §7 「§0′」 행.
- 인계서 §3-1 「dev DB 0 · S3 1,778 = 고아」와 §3-3 「행만 지워짐 · reset 도구 없음 · 9-18 이후 재공백화」는 틀렸다 — 인계서에서 정정했다.

## 0. 결론

- **사고** — 1차 `reset`(08:33Z)이 거짓 0 전제 아래 비어 있지 않은 dev(4,189행 · S3 1,778 키)를 지웠다(§0′). dev 재시드는 reset 게이트 수선까지 동결.
- 세 검사(preflight 11 · doctor 15 · `--rehearse` 10)는 1·2차 무렵 green 이었고 3차 이후 회차는 preflight 만 다시 돌았다. 어느 검사도 **대상 sha 에서 seed 화면을 걷지 않는다**. 1차 08:33Z → 3/3 16:37Z ≈ **8h04m** · dev 가동 ≈ **2h11m**.
- 시간 순위는 지표에 따라 바뀐다(§3). 벽시계로는 **러너↔제품 계약 드리프트 · 대조 게이트 없음**이 1위(≈5h04m · 재시드 활동 0 인 ≈3h 포함) — 구간 안 드리프트는 1차(`bafae4a7`)·3차(`6705675d`·`e171c5c2`)이고 2차·L1 은 1차 우회의 2차 효과다.
  실작업 기준으로는 드리프트 ≈2h05m > **agent-browser 성공 의미론**(rc 0 을 반영으로 셈 · 09-13 #50 ⑵ 재발) ≈1h14m(6차~3/3 전 구간 상한 ≈2h). dev 가동으로는 드리프트가 가장 작다(342s).
- 8h04m 중 가장 큰 단일 구간은 사람 결정 대기 2h41m(09:23→12:04Z)이다 — 원인 순위 밖.
- `cf07186d`(09-16) 한 커밋이 상세 미리보기 흐름(보기 단추의 `!description` 게이트) · 러너 verify 흐름 · 계획 기대 열을 함께 바꿨고 로컬 대역·모의 응답으로만 검증됐다(dev·전체 재시드 미실행).
  6차 이후 멈춘 결함 5종 중 4종(#17·#18·#20·#21)이 이 커밋에서 나왔고, #23 의 측정 단위(전체 데이터셋 자동 렌더 → 선택 파일 1건)도 이 커밋이 바꿨다.
- 최종 잔여 3행은 러너 결함이 아니다 — 제품 결함 2건(GeoPackage describe 415 막다른 길 · 경도 0–360 격자 거절) ＋ 계획 어휘(「미판정 대상」 미처리). 재시드가 드러낸 제품 결함 5건(§5 · P3 는 「조용한 정체」에서 「배너 문구 모순」으로 좁힘) · 이슈 초안 5건(미등재).

## 1. 타임라인·손실 (UTC · 2026-09-24)

| 회차/시도 | 시각 | dev 가동 | 멈춘 자리 | 직접 원인 | 도입 변경(커밋·날짜 KST) | 손실(멈춤 → 다음 시도) |
|---|---|---:|---|---|---|---|
| 1차 | 08:33:22–08:36:04 | 162s | seed·projects 「대상 연구실을 선택해 주세요.」 | 운영자 주체 생성 4종에 대상 연구실 헤더 요구 · 러너는 교수를 임시 운영자로 걷는다 | `bafae4a7`(09-17) `target_scope.py` 신설 | 36m43s |
| 2차 | 09:12:47 · 09:13:42–09:14:04 | 25s | projects 가 로그인 화면 | 운영자 해제 = 세션 무효화 · 수선 ㈏ 가 결합을 몰랐다 | 제품 설계(DR-4 에도 있음) × 수선 `0d6a27b6`(09-24) | 7m36s(→ `c4cdd5ea` 09:20:23) ＋ 재시드 활동 0 구간 2h59m37s(사람 결정 대기 2h41m12s 포함) |
| L1(로컬) | 12:20–12:33 | 0 | accounts 「account-create 가 열리지 않았다」 | 재개 회차 교수 = 비운영자 | 러너 순서(화면 대기가 「이미 생성됨」 판정 앞) | 3차 전 수선 `71a1d95c` · 12:20 → 3차 24m53s |
| 3차 | 12:44:53–12:47:28 | 155s | datasets seq 1 「② 메타데이터 입력」 | 업로드 필수 칸 4종이 러너·계획·정본에 없음 | `6705675d`(09-17) · `e171c5c2`(09-16) | 50m03s(L2·제안표·서명) |
| 4차 | 13:37:31–14:18:59 | 2,488s | datasets seq 18 격자 판정 846s | 전체 파일 렌더 `RENDER_TIMEOUT` · 화면 판정 미결 · 러너는 성공 표식만 대기 | 제품 한계(기한 120s) × 러너 대기 | 헛대기 1,566s + 1m49s |
| 5차 | 14:20:48–14:36:58 | 970s | 4차와 같음 | 판별 시험(무변경) · **중단 기준 없음** — 원인 ≈14:23Z · 수선 14:29:58Z 뒤에도 dev 점유 | — | 970s 전부 + 15s |
| 6차 | 14:37:13–15:15:22 | 2,289s | 러너 verify seq 15 120s | 같은 파일 재선택 → 설명 비움 → 비활성 보기 click rc 0 | 러너 verify 흐름 `cf07186d`(09-16) · dev 첫 주행 | 헛대기 840s + 1m09s |
| 7차 | 15:16:31–15:19:45 | 194s | 6차와 같음 | **무변경 재시도** | — | 194s + 24m54s(`f938198c`·`7ba6cdaf`) |
| 1/3 | 15:44:39–16:00:57 | 978s | 러너 5행 `client_no_request` · 재시드 verify 28행 판정불가 | 좌표 click 이 화면 밖 보기 단추를 스크롤 없이 누름 · rc 0 | 러너 click `cf07186d` × 상세 배치(보기 단추 접힌 선 아래) | 978s + 18m02s(`008ad5ab`·`74612bca`·`aa75b0e9`) |
| 2/3 | 16:18:59–16:22:42 | 223s | 러너 seq 25 `server_no_terminal` | 「그려짐」을 창 교차 이미지로만 셈 | 러너 측정 `cf07186d` | 223s + 8m17s(`c24c03d4`) |
| 3/3 | 16:30:59–16:37:12 | 373s | 재시드 `stage_verify` exit 1 → **최종화 미실행** | seq 13·14 describe 415 막다른 길 · seq 16 경도 0–360 bbox 거절 · 「미판정 대상」 판정 | FE `cf07186d` · viz-render 격자 경로(정규화 없음) · 계획 어휘 | **미종결** |

- 합 — 벽시계 08:33:22 → 16:37:12 = **8h03m50s** · dev 가동 7,857s = **2h10m57s** · 고정 대기 초과 ≈ 3,372s(문제 기록 머리말).
- 구간(UTC) — A 1차 → 2차 39m25s · B 2차 → `c4cdd5ea` 7m36s · C 09:20:23 → 12:20 L1 2h59m37s(재시드 커밋·실행 0 · 09:23:07Z 「needs input」 → 12:04:19Z 사람 결정 대기 2h41m12s · 12:04–12:12Z 세션 전환) ·
  D L1 → 3차 24m53s · E 3차 → 4차 52m38s(L2 · 서명 대기 ≈19m `d03b744e` 13:17:44Z → `b210ce5c` 13:36:32Z) · F 4차 41m28s · G 4차 끝 → 3/3 끝 2h18m13s. A~E = 5h04m09s · C 제외 2h04m32s · F+G = 2h59m41s.
- 1차 `reset` 은 비어 있지 않은 dev 를 지웠다(§0′). 이 표는 시간 손실만 센다.
- 멈춤 → 다음 시도 구간에는 같은 시간대 다른 가지 작업이 섞였다(예: 09:08~09:57Z 디자인 구조 P5 커밋 · 12:11Z 인계 `4b162d54` · 12:14~12:37Z staging·infra 곁일). 벽시계 손실은 **상한**이다.
  입력 문서는 KST(러너 로그·mtime·커밋)와 UTC(단계 로그)를 섞어 쓴다 — 이 문서는 UTC 로 환산했다.
- 최종 상태 — 러너 verify 5/5 그려짐(seed exit 0) · `stage_verify` 성립 25 · 미성립 1(16) · 판정불가 2(13·14) → exit 1(`stages.sh:1141`) · `account_finalize`(`:1144`) 미실행.
  dev = 데이터셋 28 · 프로젝트 4 · 계보 18 · autometa 28/28.

## 2. 직접 원인 확정 근거

### 2-1 권한·세션 계약 — 1차 · 2차 · L1
- 1차 — 운영자 주체이고 조작이 `_CREATIONS`·`_CONTEXT` 이면 헤더 필수 `target_scope.py:8-10`·`:42-43` · 비운영자는 통과 `:31-34` · 화면 `ProjectFormModal.tsx:86`. `target_scope.py` 는 `bafae4a7` 가 낳았고 `d5cd6ca9` 에 없다.
- 2차 — 세션 확인이 `service_operator` 를 매 요청 재조회해 토큰 주장과 어긋나면 거절 `kernel/login_sessions.py:193-194`·`:226-228`. **DR-4 `d5cd6ca9` 에도 같은 검사**(`:188-189`·`:219-220`) — 드리프트가 아니라 수선 ㈏(`0d6a27b6`)가 실물 주행 없이 dev 로 간 것.
- L1 — 앞 회차가 이미 해제한 교수로 `--from seed` 재개 → 계정 관리 화면 대기가 「이미 생성됨」 판정보다 먼저(문제 #3) → `71a1d95c`.

### 2-2 업로드 필수 칸 — 3차
- `RegisterArea.tsx:1164`(`classifyBlocked`)·`:1299` · 서버 `routes/ingestion.py:610`(`_validate_create_required_metadata`) · 계약 `contracts/seams/fe-core.yaml:4854`(`DatasetCreate.required`).
- `6705675d`(분류·유형 기본값 제거) · `e171c5c2`(관측 간격·Lv0 출처 필수). 값이 러너·`upload-plan.json`·정본 md·스냅샷 어디에도 없었다 → 제안표 → 사용자 서명 `b210ce5c`.

### 2-3 전체 파일 렌더 시간 초과 · 판정 미결 — 4·5차
- seq 18(GK-2A LST 변환 결과 · npy 143 파일 463,338,304 B ＋ 격자).
- 정정 — `RENDER_TIMEOUT` 은 재생성 구간 전체에서 4건이고 전부 143 파일 묶음이다. seq 17(nc 143 · 격자 없음) 139,478 ms(14:02:03Z) ·
  seq 18 격자 부착 재업로드(145 = 143 ＋ 격자 2) 147,334 ms(4차) · 137,794 ms(5차) · 138,776 ms(6차). 종전 「139,478 = seq 18」 귀속은 틀렸다.
  같은 seq 18 을 격자 없이 올린 전체 렌더는 세 번 모두 성립했다(32.7~34.0s).
- 기한 `services/viz-render/src/colab_viz/kernel/config.py:72` 120s(환경변수 없음) · 검사는 단계 경계에서만 `domains/d7_visualization/jobs.py:554-558`(READ `:569` · DRAW `:608` · LEGEND `:621`) → 120s 를 넘긴 단계가 끝나는 138~147s 에 실패.
- 정정 — 시간을 지배하는 것은 바이트가 아니라 **파일 수 × 파일별 좌표 처리**(조각마다 격자 재로딩 `jobs.py:342` · 투영 좌표 계산)다.
  로컬 재현(마감 해제) — nc 143 은 87~117s, 격자 붙인 npy 143 은 11.7~19.5s · 최대 RSS ≈2.9 GB(dev viz-render 1.5 GiB · 1 CPU → swap 압박 추정), 격자 없는 npy 143 은 0.8~3.1s.
  6차 seq 20(12 파일 · 637 MB)은 시간 초과가 아니라 500 MiB 한도의 413(`config.py:65`)이다.
- 화면 — 실패는 200 응답의 `failure` 로 FE 에 닿는다. 미리보기 칸에 `up-preview-error`(role=alert) 「그리는 데 너무 오래 걸려요. 조각 하나나 좁은 기간으로 다시 해 보세요.」 ＋ 「미리보기 그리기」 재시도(`PreviewPanel.tsx:268`·`:580-584`) — **조용한 정체가 아니다**.
  결함은 배너다 — 수렴 배너는 성공 결과에서만 갱신 `UploadModal.tsx:558-572`·`PreviewPanel.tsx:270-273` → 기본 문구 「전체 파일 기준으로 미리보기를 다시 확인해요.」 고정 `UploadModal.tsx:1491-1495` → 오류와 모순. 창 577 에서는 오류 문구가 칸 스크롤 아래에 있다.
  `gridState` 에 TIMEOUT 갈래 없음 → null `gridFlow.ts:244-270` → 격자 블록이 제목·판정 없이 「예상 영역」만 `GridUploadBlock.tsx:57`·`:68-80`. 「위치 확인」은 결과가 서야 `gridFlow.ts:263-264`.
  조각 하나를 골라 다시 그려 성립하면 배너가 「전체 파일 기준 미리보기 확인을 마쳤어요」로 바뀐다 — `onPreviewResult` 가 fileIds 를 보지 않는다(`UploadModal.tsx:563`).
- 러너 — 성공 표식만 846s(`runner.py:1286-1311`@`b210ce5c`). 5차는 같은 입력이라 결정적 재현. 6차는 `d3f7a222` 2단 대기로 실패 ≈1s 뒤 `up-preview-error` 를 읽었다 — 화면은 오류를 즉시 띄웠고 4·5차 러너가 보지 않았다.
- DR-3(09-13)에서는 seq 18 격자 칸 미출현(`reference-data/datasets-md/02.File-format/DATASETS.md:18`) — 09-24 에 격자 칸이 선 경위는 미특정(§8).

### 2-4 누름·측정 의미론 — 6·7차 · 1/3 · 2/3
- 6·7차 — 러너가 이미 선택된 파일을 다시 select(`cf07186d` 의 `request_selected_preview`) → `DatasetPreviewSection.tsx:347-351` 설명 비움 · describe 는 `selectedFileId` 변화에만 `:169-183` → 보기 영구 비활성 `:355` → agent-browser 0.27.0 click rc 0 · POST 0건 · 120s.
  재선택은 **러너 전용**이다(Chromium 키보드·팝업 경로 재현) — 네이티브 `<select>` 는 같은 값 재선택(키보드·팝업)에 `change` 0건, agent-browser `select` 는 값·disabled 와 무관하게 `change` 를 낸다(단일 파일이라 disabled 인 고르개 포함).
  멈춘 뒤 파일이 2개 이상이면 다른 파일로 갔다 오거나 새로고침으로 풀린다 · 1개면(seq 15) 새로고침뿐. 팝업 항목 마우스 클릭과 Firefox·Safari·모바일 피커는 미실측.
  #17·#18 인과 미분리 — `preview-judgment.tsv`(3/3 판정 기록) 28행 전부 y≈1630~2424(창 577) — 6·7차에도 같은 배치였을 가능성. 1/3 은 재선택을 없앴는데도 5/5 요청 0, 2/3 은 재선택 제거와 초점·Enter 를 함께 넣어 효과를 가를 수 없다(2×2 재현 미실시).
- 1/3 — 보기 중심 y≈1630~2424 · 창 1280×577(`preview-judgment.tsv` 비고) → 좌표 click 이 스크롤 없이 화면 밖을 눌렀다. `resume-drafts`(「올리다 만 것이 있어요」) 덮임은 **로컬 대역 페이지에서만** 관측 · dev 는 스크롤 뒤 28행 전부 「= 보기 단추」.
- 1/3 오귀속 — 러너는 click rc 0 을 누름 완료로 받고 120s 표시 대기로 넘어갔다. 시간 초과를 제품 쪽(`client_no_request · 제품 결함 추적`)으로 돌렸고 verify 판정은 「전건 통과」였다(`.work/logs/run-20260925-004502.log`).
- agent-browser 0.27.0 재현 — disabled(`pointerdown` 만 도착 · 처리기 0) · 화면 밖(스크롤 없이 창 밖 좌표) · 고정 층 덮임(덮은 층이 눌림) 모두 rc 0 · `success:true`.
  CSS 선택자뿐 아니라 `@ref`·`find testid … click` 도 같다. `is enabled` 는 false 를 돌려주면서 click 은 성공으로 보고한다. `scrollintoview` 뒤에는 누름이 들어간다. dev 사례는 활성 버튼 ＋ 화면 밖이었다.
- 2/3 — 측정이 `box.top < window.innerHeight` 창 교차만 셌다(`cf07186d` 도입 · `c24c03d4` 이전) → 접힌 선 아래 지도 0.
- 선례 — #50 ⑵(09-13 · 「+ 추가」 click 성공인데 onClick 무반응 · 화면 밖 좌표). 우회(`activate` = 초점＋Enter)는 자리별로만 들어갔고 `spot()` 기본은 click(`runner.py:585`·`:624-628`).

### 2-5 seq 13·14 GeoPackage 판정불가 — 3/3
- 관측 — 45s 정착 미확인 · 보기 활성 false · 미지원 표시 0 · `preview-unavailable` 0 · 누름 없음(`logs/verify.log:788`·`:797`·`:1063`·`:1072`).
- dev 실측 — `POST /api/v1/preview-target-descriptions` **415** ×2(16:34:01.758Z · 16:34:48.427Z) · viz-render `/viz/v1/target-descriptions` 415(같은 span). **백엔드는 옳게 거절했다.**
- 사슬 — viz-render 읽을 조각 0 → 415 `NOT_RENDERABLE` ＋ `renderableFormats`(`app/routes/describe.py:85-87`) → core-api 그대로 통과(`app/routes/preview.py:44-55`·`:233-235`) →
  FE `datasetPreviewSource.ts:77-86` `describe()` 는 413 만 분기하고 415 는 `PreviewUnavailable`(`:84`) — `create()` `:116-121`·`get()` `:129-134` 는 415 → `NotRenderableError`(**비대칭**) →
  `DatasetPreviewSection.tsx:179-181` catch → `loadError` → `:340` alert ＋ 「다시 불러오기」(같은 415 반복) · `description` 미도착 → `:355` 보기 영구 비활성.
- **필요 원인 둘** — ⑴ `describe()` 415 미매핑 ⑵ describe catch(`:179-181`)가 오류 종류를 가리지 않고 `loadError` 만 세운다. 로컬 재현(배포본 코드 · describe 415 대역)에서 ⑴ 만 고치면 여전히 alert 로 떨어졌다 — 수정은 두 곳 모두 필요하다.
- 미지원 안내 `dt-preview-unsupported`(`:362-366`)는 「그릴 수 없음」 단계에서만, 열자마자 서는 갈래(`:154-156`)는 renderable 0 일 때만 — GeoPackage 본체는 `kind === '본체'` 라 renderable(`preview/pick.ts:77` `RENDERABLE_KIND` · `:83-85`).
  범위는 「단일 본체」보다 넓다 — 선택된 본체 조각(기본 = 첫 본체)이 미지원이거나 `FieldReadError` 를 내거나 변수가 0 이면 같은 경로다(viz-render `describe.py:64-87`).
- 화면 — alert 문구는 봉투 message 「이 형식은 아직 지도로 못 그려요.」(`preview/requestError.ts:6-7`)다. 로컬 재현 DOM 과 업로드 화면 덤프(`.work/fail/13-analysis.txt`)로 확인했고 dev 상세 화면에서는 판독하지 않았다.
  사용자는 미지원이라는 말은 본다. 잃는 것 = 지원 형식 목록(`renderableFormats`) · 「등록·계보 확정·다운로드는 그대로」(`PreviewPanels.tsx:123-133`) · 미지원 표지 · 재시도가 무익하다는 신호.
- 업로드 쪽도 같다 — `preview/previewSource.ts:118-127` · `upload/previewSource.ts:65-70` 의 describe 에도 415 매핑이 없고, 업로드 화면 덤프에 같은 문구 ＋ 「다시 불러오기」가 있다.
- 도입 — `cf07186d`(09-16 · 「상세 조회의 자동 렌더를 제거」 · 보기 단추에 `!description` 게이트). DR-4 `d5cd6ca9` 의 보기 단추는 `disabled={start.phase === '시작하는 중'}` 뿐이고, describe 실패를 삼키고(`:131-136`) 자동 `create()` → 415 → 미지원 안내(`:175-182`·`:284-289`).
- 시험 공백 — `frontend/test/dataset-preview.test.tsx:63` 대역 describe 가 항상 성공 · 미지원 시험(`:178-195`)은 `create()` 415 만. 실서버 순서(describe 가 먼저 415)는 재현되지 않는다.
- 파생 「가공 단계 불일치 · 가공 단계 판정불가 · 계수 판정불가 13,14」 = 판정불가 갈래가 읽은 값을 `?` 로 적은 결과(`stages.sh:981` → `:821-826` · `:1076-1077`). 별개 결함 아님.

### 2-6 seq 16 ERA5 변환 결과 미성립 — 3/3
- 관측 — 적중 「= 보기 단추」 · focus+Enter · slot drawing → 1,909 ms 뒤 failed(`logs/verify.log:1126`·`:1130` · `preview-judgment.tsv` 16).
- dev 실측 — render `01M3A4BPGT27PQHZWE45V571BB` · `status=failed` · `failure_code=MAP_BOUNDS_IMPLAUSIBLE` · `processing_ms=436`.
- 격자 실측 — `02.File-format/file_format_1_grib/04.Lat_Lon_info/lon2d.npy` (721, 1440) f8 · **0.000–359.750** · `lat2d.npy` −90–90 · 본체 72 파일 (721, 1440) f4. 격자는 정상 첨부됐다(`grid_skip=false`).
- 사슬 — npy 는 좌표 없음 → 짝 격자 `jobs.py:340-354` → `grid.py:213-262`(짝·축 판별만 · 경도 정규화 없음 · `|값|>90` 이면 경도 `:150-177`) →
  warp 전 1차 검사 `preview.py:261` → `check_bbox_4326` `:100-101` 「경계가 지구 밖이다: (0.25, -85.0, 359.75, 85.0)」(1024 솎기 · Mercator ±85.05° 절단 뒤 값) → `jobs.py:668-673` → `MAP_BOUNDS_IMPLAUSIBLE`(`failures.py:27`·`:52-53`).
  예외 문구는 로컬 재현으로 얻었다 — dev 로그에는 `failure_code` 만 남는다(`render_failed reason=none`). 경도에서 360 만 빼면 같은 배열이 성립한다.
- 대조 — GRIB 원자료 seq 15 도 앱 코드에 경도 정규화가 없다. GDAL GRIB 드라이버가 −180..180 으로 보고하고 `readers.py:302-303` 이 clamp 할 뿐이다(첨부 격자는 안 쓴다 · 배지 「투영 계산 격자」) → 성립.
- 범위는 npy ＋ 2D 격자보다 넓다 — viz-render 어디에도 경도 정규화가 없다(`git grep` 결과 타일 수식 1건뿐).
  경도 0–360 인 1D/2D NetCDF(ERA5 CDS 기본 산출물 포함 · 합성 재현)와 180° 를 넘는 지역 격자(예 120..240)도 같은 이유로 실패한다. 등록 전 업로드 미리보기(72 파일 · 13:57:56Z)도 같은 원인으로 실패했다.
- 실패 시 썸네일도 잃는다 — `build_map_layer` 예외가 `_build_artifacts` 안에서 올라와(`jobs.py:637`) `job.artifacts` 가 대입되지 않는다 → 실패 봉투에 `thumbnailUrl`·`valuePreviewUrl` 이 없다(①② 파일은 디스크에 있다).
  `jobs.py:669` 주석 「지도형만 실패하고 ①②는 남는다」와 달리 미리보기 전체가 실패로 보인다.
- 선행 관측 — DR-3(09-13) 등록 때 같은 실패 「격자 경계 위생 실패 — 지도형 미생성」(`sessions/DR-3-run-2026-09-13.md:140`) → 정본 md 비고 「전지구 격자라 한반도 경계 밖」(`DATASETS.md:52`) → 기대 「미판정 대상」.
  업로드 화면 문구가 그 오진을 부른다 — `gridFlow.ts:81-84` 「결과 위치가 한반도 밖으로 나왔습니다」(`:247`). 09-16 라운드는 「ERA5 좌표 오류는 별도 해결 대상」이라 적고(`prd/rounds/R-SELECTED-PREVIEW-TIMING.md:56`) 대장·이슈에 올리지 않았다.
- 러너 — `stages.sh:929` 만 특별 분기(`미성립(포맷 미지원 …)`) · 「미판정 대상」 행은 일반 경로 → `:1094-1096` 「예상 밖 미성립」 → exit 1. **계획-러너 의미 불일치.**

### 2-7 기대값 표류 — 문제 #23
- 기대 — 15·17 `렌더 성립(3패스 중 1회)`(`plan-manifest.yaml:241`·`:274`) · 19·21·25 `'**렌더 미성립(3패스 전부)**'`(`:305`·`:336`·`:398` — md 강조 기호까지 값에 들어갔다).
- 출처 — 정정: 수동 관측이 아니다. DR-3(2026-09-13 22:32 ~ 09-14 00:35 KST) agent-browser 자동 러너(`2d5d6422` `runner.py` `phase_verify`)의 3패스 측정이다(#42 표) —
  누름 없이 상세 진입 뒤 고정 6초 대기 · img·`preview-unavailable` 계수. → 정본 md 판정 ㈎ 「ⓑ 지금 계수를 정본으로 · 뒷단 해소는 `PV-2`」(사용자 확정 09-14 · `DATASETS.md:162-166`) → `build_plan.py` 가 그대로 복사(`c49a1e40` 09-14 · 열 파싱 `cf07186d`).
  정본 md 안에서도 grib 비고(3패스 중 3패스)와 표(3패스 중 1회)가 어긋난다 — DR-3 원문은 3패스째만 그려짐.
- 낡음 — `660854bb`(09-15 · 미리보기 준비 비동기·큐·창 단위 읽기)가 09-13 의 503·OOM 기제를 고쳤다 · #42 종료(09-17) · 대장 `PV-2` 는 open(`work-items.yaml:3653-3655`). 재측정 트리거 없음.
  `cf07186d`(09-16)가 측정 단위도 바꿨다 — 「상세 진입 시 전체 데이터셋 자동 렌더」 → 「선택 파일 1건 명시 렌더」. 이번 5행 2.3~3.5s(러너 화면 표시 · `stage_verify` 로는 seq 17 4.1s)는 09-13 의 20~38s 와 같은 단위가 아니다.
  다만 업로드 단계 전체 파일 렌더도 bin 7.0s · tif 3.5s · hdf4 4.6s 로 성립했고 첫 렌더도 1.7~2.7s 다(캐시 아님) — 서버는 이제 이 형식들을 그린다.
- 판정 — **stale-product-changed**(`660854bb` ＋ `cf07186d`). 이번 러너 결함(09-24~25)은 09-13 측정에 끼어들 수 없다.
  그러나 09-13 러너의 고정 6초 대기가 bin 행(문구·이미지 0 · 당시 진단 「대기 중」 · S3 산출물은 그 뒤 존재)에 개입했을 여지는 남는다. grib·nc·tif·hdf4 는 서버 503 문구로 뒷받침된다.
  PV-2 는 다 풀리지 않았다 — relay 10s 503 7건 · 143 파일 `RENDER_TIMEOUT`(§2-3 · P4 · P5).

## 3. 근본 원인 — 배타 손실 순

손실 = 그 원인을 주 원인으로 멈춘 회차의 dev 가동 ＋ 멈춤 → 다음 시도 벽시계(상한). 한 회차는 한 원인에만 넣고 겹침은 따로 적는다. 구간 A~G 는 §1.
순위는 벽시계 기준이다. 실작업(재시드 활동 0 인 C 제외) 기준으로도 1·2위 순서는 같고(드리프트 ≈2h05m > agent-browser ≈1h14m · 6차~3/3 전 구간 상한 ≈2h), dev 가동 기준이면 드리프트가 가장 작다(342s · 이후 원인 합 7,515s).
§0′ 사고(데이터 삭제)는 시간 순위 밖이다.

| 순위 | 근본 원인 | 주 원인 회차 | 벽시계(상한) | 실작업 | dev 가동 | 관여(겹침) |
|---|---|---|---|---|---|---|
| 1 | 러너↔제품 계약 드리프트 · 대조 게이트 없음 | 1·3차(드리프트) · 2차·L1(1차 우회 `0d6a27b6` 의 2차 효과) | 5h04m09s(A~E · C 포함) | 2h04m32s | 342s | 2차 = 드리프트 수선의 미리허설(6) · E 에 서명 대기 ≈19m |
| 2 | agent-browser 성공 의미론(rc 만 · 사후조건 없음) · 뷰포트 측정 | 6차 verify · 1/3 · 2/3 · 7차 뒤 수선 | ≈1h14m | ≈1h14m | ≈22m | 3차 비활성 click 15s×4 · L2 R2 · #17·#18 인과 미분리(§2-4) |
| 3 | 고정 시간 대기 · 상태 판독 없음 | 4차 · 6차 | ≈40m | ≈40m | 헛대기 ≈38m | 5·7차 헛대기 ≈16m(4) · 합 ≈56m |
| 4 | 상태·기록·프로세스(판별 시험 중단 기준 없음 · 무변경 재시도 · 인계 입력 오류) | 5·7차 | ≈20m | ≈20m | ≈19m | 손 정리 8회 · 되돌림 1회 · 9가지 사슬 · 가드·훅 거절 44건 |
| 5 | 기대값 출처·의미 | 3/3 | 6m ＋ **최종화 미실행**(미종결) | 6m | 373s | 1/3 오표기 · #23 혼선 · 제품 결함 P1 과 겹침 |
| 6 | 사전 검사 커버리지 공백 | — | 배타 0 | 0 | 0 | 1~3차 · 1/3 이 dev 에서 처음 드러남 — 예방 효과는 가장 크다 |
| — | 사람 결정 대기·세션 전환(원인 아님) | C 구간 | 2h59m37s(결정 대기 2h41m12s) | — | 0 | 8h04m 중 가장 큰 단일 구간 |

### 3-1 계약 드리프트 · 게이트 없음
- 왜 1·3차가 멈췄나 → 서버가 새 규칙(대상 연구실 헤더 · 필수 칸 4종)을 요구했고 러너는 몰랐다.
  구간 안 계약 드리프트는 이 둘뿐이다 — 1차 `bafae4a7`(09-17 · `target_scope.py` 신설) · 3차 `e171c5c2`(09-16)·`6705675d`(09-17).
  2차의 해제 = 세션 무효화는 DR-4(`d5cd6ca9` `db_credentials.py:222`·`:258`)에 이미 있던 설계이고 1차 우회 `0d6a27b6` 이 그 경로를 새로 밟았다. L1(#3)은 그 우회가 남긴 재개 전제 문제다 — 둘 다 1차의 2차 효과.
- 왜 몰랐나 → 규칙이 DR-4 뒤 4일 새 들어왔다: `d5cd6ca9..ea21d8c2aa54` 230 커밋(비병합 192) · 러너가 밟는 경로(pathspec ①) 비병합 **24 커밋 · 49 파일 · +2,196/−795**.
  러너가 의존하는 `frontend`·`services/core-api` 전체로는 비병합 **63 커밋 · 248 파일 · +14,297/−3,018**, 소스만(`frontend/src`·`services/core-api/src`) **41 커밋 · 116 파일 · +5,123/−1,796**.
- 왜 러너가 따라가지 않았나 → 러너는 09-15~16 에 3회 갱신됐다(`660854bb` +44 · `a3e68b06` +272 · `cf07186d` +62). 마지막 갱신(09-16 13:08Z)이 `bafae4a7`(09-16 17:47Z)·`6705675d`(09-17 01:55Z)보다 앞서 분류·간격·출처 입력은 한 번도 들어가지 않았다.
  제품 PR 완료 조건에 러너 갱신이 없고, 계약(`_CREATIONS`·`_CONTEXT` · `DatasetCreate.required` · 러너 testid)과 러너를 대조하는 게이트가 없다(`wu2-reseed-2026-09-24.md` §5-2 · §10 ⑴).
- 마지막 green(LKG) = `d5cd6ca9` 에는 단서가 있다 — DR-4 는 5회 재개의 partial 이었고 `d5cd6ca9` 에서는 프로젝트 생성·로그인·데이터셋 24건 화면을 다시 걷지 않았다. 전체 걷기는 `b955bece7384`(계약 차이 2 파일 +80/−3)에서 했다.
- 왜 게이트가 없나 → 러너는 dev-package 도구라 제품 게이트 밖이고, `dev-reseed-selftest`·`product-reseed-selftest` 는 dev 무접촉 대역이다.
- 왜 늦게 드러났나 → 재시드는 드물게 돈다(DR-3 09-13 · DR-4 09-14 · 이번 09-24). 러너는 10일간 대상 제품에 한 번도 실행되지 않았다.
- ① pathspec = `frontend/src/components/{upload,datasetpreview,preview,project}` · `frontend/src/auth` · core-api `app/target_scope.py`·`app/routes/ingestion.py`·`kernel/` (시험 제외).

### 3-2 agent-browser 성공 의미론 · 뷰포트 측정
- 왜 6차가 120s 를 버렸나 → 비활성 보기를 누르고 rc 0 을 「눌렀다」로 셌다.
- 왜 rc 0 을 믿었나 → 러너 판정 = 명령 rc(`runner.py:624-628` 캐시 · 문제 #5·#6) · 사후조건(slot 이 idle 을 떠남 · POST 발생) 없음. agent-browser 0.27.0 의 click/select 는 「이벤트 발송」 성공이지 「동작 반영」이 아니다.
- 왜 사후조건이 없었나 → 09-13 #50 ⑵ 에서 같은 계열을 봤으나 우회를 자리별로만 넣었다(`activate` · 기간 단추 `02f5ab1f` · 보기 `008ad5ab`). 표준 누름 원시동작이 없다.
- 왜 측정도 틀렸나 → 「그려짐」을 창 교차로 정의(`cf07186d`) — dev 창 1280×577 과 상세 배치(보기 단추 y≈1630~)를 재현한 적이 없다.
- 왜 시험이 못 잡았나 → 러너 시험 대역이 `ab` 를 항상 rc 0 성공으로 둔다(`dev-seed/tests/test_runner_grid_wait.py:46` · `test_runner_plan_mapping.py:370`·`:506`) · `cf07186d` 검증은 로컬 모의 응답 · dev·전체 재시드 미실행(`prd/rounds/R-SELECTED-PREVIEW-TIMING.md:55-56`).

### 3-3 고정 시간 대기 · 상태 판독 없음
- 왜 4차가 846s 를 기다렸나 → 성공 표식(「위치 확인」)만 기다렸고 서버는 147s 에 이미 실패했다(5차 138s).
- 왜 실패를 못 읽었나 → 같은 대기에서 실패 표식(`up-preview-error`)·격자 상태(`data-grid-state`)를 읽지 않는다. 격자 칸 180s ×4(4·6차)도 「부재」라는 정상 갈래를 상한으로만 판정(`runner.py:1295`).
- 왜 → 러너가 화면 상태 기계(`gridState`)를 모르는 행복 경로 스크립트이고, 계획에 행별 기대(격자 부착/좌표 보유/대용량 렌더 실패)가 없다.
- 왜 끝이 없었나 → 화면은 오류(`up-preview-error`)를 즉시 띄웠지만 러너가 읽지 않았다. 제품은 실패를 수렴 배너·격자 판정에 전하지 않아 성공 표식만 보는 대기에는 끝이 없다(§5 P3).

### 3-4 상태·기록·프로세스
- 왜 5차가 dev 를 16분 점유했나 → 5차는 14:21:11Z 보고에서 「성공하면 일시적 · 같은 자리 실패면 결정적」 판별 시험으로 명시됐고 원인 조사·수선 레인이 함께 떴다.
  원인은 ≈14:23Z, 수선 `d3f7a222` 는 14:29:58Z 에 나왔는데 5차는 14:36:58Z 까지 돌았다 — 결함은 **판별 시험에 사전 중단 기준이 없던 것**이다.
  `reseed.sh` 에 신호 trap 이 없어 중단하면 `result.json`·회차 기록이 남지 않는다(문제 #12) — 중단 비용이 계속 돌리는 쪽을 택하게 했다.
- 왜 7차가 같은 자리에서 멈췄나 → 결정적 실패를 원인 확인 없이 무변경 재시도(문제 #16). 도구는 같은 도구 sha·같은 입력 재개를 거절하지 않는다. 사용자 규칙(무변경 금지 · 최대 3회)은 7차 뒤 15:20:51Z 에 나왔다.
- 왜 입력이 틀렸나 → 인계·레인 지시가 결함원이었다. 인계서 §3-1 「dev 계정 0건」(거짓 0)을 전제로 한 계정 작업이 대상 환경을 잘못 짚었다(12:18Z · 12:23Z 정정) ·
  인계서 §4(b) 재개 명령에 `COLAB_SEED_WORK_DIR` 가 없었다(어드바이저 발견 · 문제 #8) · 지정 감싸개가 `COLAB_DEV_SECRETS_DIR` 를 풀지 않는다(문제 #11) ·
  `wu2-prereq` §7-2 파일럿 SQL 이 소유자 URL(거짓 0 이면 건강한 실행을 멈춘다 · 정정은 인계서에만) · L1 지시에 참조자료·`COLAB_REF_ROOT` 가 없었다(문제 #13) ·
  클릭 수선 레인 지시 4건에 dev 창 1280×577 이 없어 #18 을 로컬에서 못 잡았다.
- 왜 격리가 마찰이 됐나 → 12:12~16:52Z 가드·훅 거절 44건(워크트리 격리 37 · lifecycle 훅 7). 상태 자리(`.work`·run-dir)가 1차 워크트리에 묶여(문제 #8) 격리 레인이 그 경로를 만질 때마다 거절됐다 — 시간 비용 미계측.
- 왜 재개가 무거웠나 → 상태 `.work` 가 체크아웃 local(`reseed.sh:247`) · run-dir 가 회차를 섞는다(문제 #8) · 회차 기록이 체크아웃을 dirty 로(`stages.sh:1352-1355`) → preflight:git(`preflight.sh:43-45`) → 손 정리 8회 · 추적 기록 7건 휩쓸림 1회(문제 #24).
- 왜 로그를 못 믿었나 → 단계마다 로그 비움(`lib.sh:133`) · `redact` 블록 버퍼(`lib.sh:18-22`·`:80`).
- 왜 인계 비용이 컸나 → 수선마다 새 가지 — 9가지 선형 사슬(문제 #25).
- 왜 반복되나 → 재시드는 매번 다회 실행으로 끝났다(DR-4 5회 `03-HANDOFF.md:24` · 이번 10회) — 도구가 「한 번 도는 스크립트」 전제라 재개·다회차가 1급 개념이 아니다.

### 3-5 기대값 출처·의미
- 왜 seq 16 이 verify 를 막았나 → 「미판정 대상」 행도 성립/미성립으로 판정했다(`stages.sh:929`·`:1094-1096`).
- 왜 → 기대 어휘 6종(`미측정` · `미측정(판정 5종 밖)` · `미성립(포맷 미지원 …)` · `렌더 성립(3패스 중 1회)` · `렌더 미성립(3패스 전부)` · `미판정 대상`) 중 코드가 뜻을 아는 것은 2종 —
  러너 `preview_required`(`runner.py:96-98` · 「렌더 성립」 접두) · 재시드 verify(`미성립(포맷 미지원 …)`). 「3패스 전부 미성립」 행이 실제로 실패하면 오히려 「예상 밖 미성립」 red 다.
- 왜 → 값이 정본 md 의 산문을 그대로 복사한 것이다(enum 아님 · `build_plan.py` · 강조 기호 `**` 까지 값에 남음).
- 왜 낡았나 → 09-13 관측이 PV-2 서버 한계 아래였고 md 스스로 「해소는 PV-2」로 임시값임을 적었다. 측정일·측정 sha·귀책 이슈 필드가 없어 `660854bb`·#42 종료가 재측정을 부르지 않았다.
- 왜 제품 결함이 기대값이 됐나 → seq 16 「경계 위생 실패」를 화면 문구대로 「한반도 밖」으로 읽고 결함 판정 없이 「미판정 대상」에 넣었다. 13·14 의 기대 표식에는 발생 조건(열자마자/누른 뒤)이 없다(문제 #19).

### 3-6 사전 검사 커버리지 공백
- 왜 green 세 검사가 못 막았나 → preflight 11 = 환경(git · dev-sha · aws · qemu · 잔여 · 참조자료 · agent-browser doctor · secrets · 자원 · 계획 생성) · doctor 15 = 배포 건강 ·
  rehearse 10 = 원격 원시동작 ＋ ⑻ 러너 `--phase report`(계획 표 출력뿐 · `stages.sh:1319`) ＋ ⑼ 정적 HTML 제목 `Co-Lab`(`:1329` · `frontend/index.html:6` — SPA 가 안 떠도 통과). **seed 화면 주행이 없다.**
  3차 이후 회차는 preflight 만 다시 돌았다 — doctor 15/15 · rehearse 10/10 은 1·2차 무렵 값을 이어 쓴 것이다.
  「green 인 채 네 번 멈춤」은 본 실행 1·2·3·4차다 — L1 의 #3 은 로컬에서 먼저 red 로 잡혀 dev 를 멈춘 적이 없다.
- 왜 → 리허설은 「아무것도 바꾸지 않는 실모드」(`stages.sh:1148`)로 설계됐고, 대상 sha 일회용 스택 end-to-end 는 도구 명령이 아니라 매번 손으로 짠 레인(L1·L2)이었다.
- 왜 로컬 레인도 못 잡았나 → L1 은 참조자료를 받지 못해 datasets·verify·report 를 면제했고(문제 #13), 면제가 dev go 조건에서 빠지지 않았다. L2 는 28행 중 4행만 밟았다.
- 왜 시험 대역도 못 잡았나 → 러너 대역은 agent-browser 를 이상화(rc 0 = 반영 · `verify-session.sh` click → 즉시 drawn · 누르지 않아도 뜨는 미지원 표시) · FE 대역은 describe 성공을 가정 · 대역 페이지는 dev 창 크기가 아니었다.
  #1~#4 는 대역 의미론보다 **selftest 에 제품이 없고 러너 seed 국면을 부르지 않는 것**이 원인이다. 예외 — #1 수선 픽스처 `operator-window.sh` 의 `run(){ …; return 0; }` 가 #2 를 낳은 순서(해제 → projects)를 green 으로 통과시켰다.

## 4. 사전 검사가 못 잡은 이유

| 실패 | 잡을 수 있었던 검사 | 실제 검사가 보는 것 | 공백 |
|---|---|---|---|
| 1차 reset 이 비어 있지 않은 dev 삭제(§0′) | DROP 직전 계수 = 기대(0) 대조 · BYPASSRLS 계수 · S3 계획의 DB 참조 대조 | 사전대조 = 앱 롤 경계 없는 `count(*)` · `stage_reset` = 보고서 파일 존재만 · 게이트 ③ = 재측정 없음 · 리허설 ⑸ S3 키 수 · ⑹ `select 1` | 「비어 있다」를 판정하는 경로 없음 |
| 1차 대상 연구실 헤더 | 권한 계약 ↔ 러너 국면 주체 대조 · 대상 sha seed 주행 | preflight = 환경 · doctor = 배포 건강 · rehearse ⑻ 계획 표 출력 · ⑼ 정적 제목 | 러너 화면 경로 미주행 |
| 2차 해제 = 세션 무효화 | 대상 sha 로컬 seed(해제 → projects) | `operator-window.sh` = 국면 순서만(러너 호출을 rc 0 으로 가림) | 수선이 실물 미검증으로 dev 행 |
| 3차 필수 칸 | `DatasetCreate.required` ↔ 계획 행 대조 · L1 datasets 국면 | preflight ⑽ build-plan = md → 28/18 계수 · L1 datasets 면제 | 계약 required 미대조 · 면제가 go 조건 밖 |
| 4차 seq 18 렌더 시간 초과 | 대용량 행 실물 리허설 | L2 = seq 1·2·3·13 | 24행 미답 |
| 6·7차 재선택·비활성 click | 대상 sha 실브라우저 verify | 러너 대역 `ab` rc 0 고정 | agent-browser 의미론 이상화 |
| 1/3 화면 밖 click · 2/3 창 교차 측정 | dev 창 크기 대역 페이지 | 로컬 jsdom · 대역 페이지(창 크기 다름) | 뷰포트 미재현 |
| 3/3 seq 13·14 | FE 시험에 describe 415 대역 | `dataset-preview.test.tsx:63` describe 항상 성공 | 실서버 호출 순서 미재현 |
| 3/3 seq 16 | 격자 위생 실패 행의 결함 판정 · 기대값 재측정 | DR-3 관측을 기대값으로 흡수 | 결함·기대 경계 없음 |

## 5. 재시드가 드러낸 제품 결함 (확정 5 · file:line = `ea21d8c2aa54`)

⭑ 2026-09-25 GitHub 이슈로 등록 — P1 #133 · P2 #134 · P3 #135 · P4 #136 · P5 #137.

| # | 결함 | 근거 | 사용자 영향 | 선행 기록 |
|---|---|---|---|---|
| P1 ([#133](https://github.com/CognileapAI/colab-v2/issues/133)) | 미지원 형식 본체(GeoPackage) 상세 미리보기 막다른 길 — `describe()` 415 미매핑 ＋ describe catch 가 「그릴 수 없음」으로 보내지 않음(필요 원인 둘) | `datasetPreviewSource.ts:77-86`(vs `:116-121`·`:129-134`) · `DatasetPreviewSection.tsx:179-181`·`:340`·`:355`·`:362-366`·`:154-156` · `preview/pick.ts:77`·`:83-85` · 업로드 쪽 `preview/previewSource.ts:118-127` · `upload/previewSource.ts:65-70` · 도입 `cf07186d` | 미지원 문구(「이 형식은 아직 지도로 못 그려요.」)가 오류 틀(alert)로 뜨고 무익한 「다시 불러오기」가 붙는다 · 보기 영구 비활성 · 지원 형식 목록·「등록·계보 확정·다운로드는 그대로」(`PreviewPanels.tsx:123-133`) 미표시. 선택된 본체 조각이 미지원·읽기 실패·변수 0 인 데이터셋 전부 상시 재현 · 업로드 화면도 같다 | `FMT-GPKG`(`work-items.yaml:3665` open) · #40 · #53 ⑶ |
| P2 ([#134](https://github.com/CognileapAI/colab-v2/issues/134)) | 경도 0–360 격자 미리보기 거절 · 실패 시 썸네일 소실 | `grid.py:81-90`·`:180-210`(정규화 없음) → `preview.py:261` → `:100-101` → `jobs.py:668-673` → `failures.py:52-53` · 썸네일 `jobs.py:637`(`_build_artifacts` 안 예외 → `job.artifacts` 미대입) · 업로드 문구 `gridFlow.ts:81-84`·`:247` · 대조 GRIB = GDAL 보고 ＋ `readers.py:302-303` clamp | npy ＋ 2D 격자 · 1D/2D 경도 0–360 NetCDF(ERA5 CDS 기본 산출물) · 180° 를 넘는 지역 격자 모두 지도형 불가 · 업로드 화면은 「한반도 밖」으로 오안내 · 상세는 0.4s 뒤 실패 · 값 미리보기·썸네일까지 응답에서 빠져 미리보기 전체가 실패로 보인다. 격자를 −180..180 으로 고쳐 올리는 것 말고 길이 없다 | DR-3(09-13) 관측 · `R-SELECTED-PREVIEW-TIMING.md:56` · 이슈·대장 없음 |
| P3 ([#135](https://github.com/CognileapAI/colab-v2/issues/135)) | 업로드 전체 파일 렌더 실패 뒤 배너가 오류와 모순 · 격자 판정 미결(「조용한 정체」 아님) | `UploadModal.tsx:558-572`·`:563`·`:1491-1495` · `PreviewPanel.tsx:268`·`:270-273`·`:580-584` · `gridFlow.ts:244-270` · `GridUploadBlock.tsx:57`·`:68-80` | 오류 문구(재시도 안내 ＋ 「미리보기 그리기」)와 진행형 배너 「다시 확인해요」가 동시에 선다 · 격자 「맞습니다」가 안 선다 · 조각 하나로 다시 그려 성립하면 배너가 「전체 파일 기준 확인을 마쳤어요」로 오표시 | 문제 #15 · 이슈 없음 |
| P4 ([#136](https://github.com/CognileapAI/colab-v2/issues/136)) | 중계 10s 고정 < viz-render describe 실소요 | `app/relay.py:28`(모듈 상수 · 설정 불가 · 재시도 없음)·`:87`·`:96-97` → `routes/preview.py:236-240` 503 · viz-render `describe.py:56-60`(전 조각 준비) · 503 7건(13:58~14:40Z) · core 10,030~10,041 ms 에 끊김 · viz 11.8~21.7s 에 200 | 파일 수가 많은 업로드(143~145)의 변수·시각 고르개가 「그리는 서버에 연결하지 못했다: timed out」(`PreviewPanel.tsx:144-147`) · 지연은 바이트가 아니라 파일 수를 따른다(72 파일 ≈8.9s · 31 파일 4.3~4.7s) · viz-render 는 끝까지 계산(헛계산). 같은 상수가 create·palettes·reclaim·lookup·screenshot·get 과 ai-service 검색·계보 제안 중계에도 걸린다 | `PV-2`(open) · #42(닫힘 09-17 · 렌더 경로만 비동기화 `660854bb`) |
| P5 ([#137](https://github.com/CognileapAI/colab-v2/issues/137)) | 좌표·격자가 있는 다중 파일(≈143) 업로드의 지도형 전체 파일 미리보기 `RENDER_TIMEOUT` | `kernel/config.py:72` 120s 하드코딩(환경변수 없음 · 「정본 무근거」) · `jobs.py:554-558`(단계 경계 검사 → 138~147s) · `jobs.py:342`(조각마다 격자 재로딩) · `failures.py:49` · 4건 전부 143 파일(nc 143 · 격자 붙인 npy 143) · 격자 없는 npy 143 은 34s 성립 · 500 MiB 초과는 413(`config.py:65`) | 파일 수 × 파일별 좌표 처리가 시간을 지배 — 바이트가 아니다 · 격자 붙인 143 은 최대 RSS ≈2.9 GB 로 메모리 한도도 넘는다(추정) · 기한 뒤까지 계산 · 안내는 「조각 하나나 좁은 기간으로」뿐. 마감만 올리면 nc 의 CPU 비용은 흡수하지만 격자 143 의 메모리 문제는 남는다 | `PV-2` · #42 |

- 제외 — 같은 파일 재선택이 설명을 비움(`DatasetPreviewSection.tsx:347-351`): **러너 전용**(Chromium 키보드·팝업 경로 재현 — 네이티브 select 는 같은 값 재선택에 change 0건 · 팝업 항목 마우스 클릭·타 브라우저 미실측).
- 제외 — 보기 단추가 접힌 선 아래(y≈1630~2424 · 창 577): 사용성 항목 · #125(OPEN · 보기 단추를 패널 위로)와 겹친다.
- 초안 — 레포 밖 `gh-issue-drafts.md`(5건 · 주장 검증 반영 · 공개 레포 규칙 적용 · 미등재). 등재 여부는 사용자.

## 6. 설계 질문 — 화면 걷기 · API 로더 · 혼합 (판정하지 않는다)

- 현행 채택 기록 = 사용자 결정 2026-09-13 「agent browser … 이걸로하자」 · 화면단 원칙(백엔드 시딩·API 직접 호출 금지 · `work-items.yaml:3627`) · 러너 성격 「사람 체크리스트를 같은 화면 경로로」(`dev-seed/README.md:3-4`).
  UI 대 API 의 득실 비교 기록은 미발견. autometa 는 업로드 분석 사건으로만 들어온다(문제 기록 §5 · `apply_autometa`).

| 안 | 얻는 것 | 치르는 것 | 이번 실패 중 소거 |
|---|---|---|---|
| ⓐ 화면 걷기 유지 ＋ 대상 sha 로컬 end-to-end | 사람 경로와 같은 화면 · FE 결함(P1·P3·P2 문구)을 걷다가 본다 | 화면 개정마다 러너 수선 · 느림(4차 2,488s) · agent-browser 의미론 비용 · 리허설 수십 분·4.4 GB | 1~3차(4차는 자원이 dev 와 같을 때)를 dev 전에 · 6차 이후는 누름 표준화가 함께 있어야 |
| ⓑ 공개 API 로더(업로드 → 전송 → 분석 → 등록 · 계보) | DOM·click 의미론 소거 · 계약이 변경을 알린다 · 상태를 API 로 판독 · 빠름 | 헤더·필수 칸을 러너가 재현 · FE 결함은 못 본다 · 분석 사건 경로 실측 필요 · 화면단 원칙 개정 필요 | 4차 대기 · 6차~2/3 |
| ⓒ 혼합 — 대량은 API · 표본만 화면(권한 전환 · 업로드 필수 칸 · 미리보기 5종) | 속도·안정 ＋ 화면 핵심 경로 확인 | 두 경로 유지 · 표본 선정이 발견률을 좌우 · 원칙 부분 개정 | 대량 행의 누름·대기 결함 |

- 어느 안과도 겹쳐 쓰는 것 = 러너↔제품 계약 게이트 · 기대값 enum · 실행 자리 분리(§7).

## 7. 재발 방지 후보 (결정 없음 · 꼬리표 = gate/runner/preflight/process/product)

| 근본 원인 | 후보 | 꼬리표 |
|---|---|---|
| 3-1 드리프트 | `_CREATIONS`/`_CONTEXT` ↔ 러너 국면 주체 · `DatasetCreate.required` ↔ 계획 행 · 러너 testid ↔ 대상 sha 번들 대조 — 제품 PR 에서 red | gate |
| 3-1 | 러너 의존 화면·권한·필수 칸 변경 PR 의 완료 조건에 러너 갱신(또는 면제 사유) | process |
| 3-1 | 대상 sha 와 러너 마지막 green sha 사이에 러너 의존 경로 변경이 있으면 78 | preflight |
| 3-2 | 누름 원시동작 하나(적중 검사 → 초점·Enter → 사후조건 → JS 폴백)로 `spot()` 포함 전 자리 대체 · select 는 값이 다를 때만 | runner |
| 3-2 | 대역에 「rc 0 인데 무반응」 red 픽스처(비활성 · 화면 밖 · 덮임) · 대역 페이지 창 = dev 창(1280×577) | gate |
| 3-2 | agent-browser 판 고정 ＋ 의미론 스모크 | preflight |
| 3-3 | 성공·실패·부재 표식 동시 판독(`up-preview-error` · `data-grid-state` · slot) · 상한은 안전망만 · 행별 기대를 계획에 | runner |
| 3-3 | 실패를 수렴 배너·격자 판정에 전하기(P3) | product |
| 3-4 | 무변경 재개 거절(도구 sha·입력 동일 → 78) · 「바꾼 것」 필수 · 시도 횟수를 도구가 셈 | process |
| 3-4 | 상태·결과·회차 기록을 체크아웃 밖·회차별로 · 로그 누적·줄 버퍼 · 회차 단위 가지 하나 | runner/process |
| 3-4 | 사람 계정과 시드 계정 분리(최종화 차단 · 문제 #14) | process |
| 3-5 | 기대값 enum(성립 필수 · 미성립 기대 · 관찰만) — `미판정 대상`·`미측정` 은 비차단 관찰 · 발생 조건 동반 | runner |
| 3-5 | 기대값에 측정일·측정 sha·방법·귀책 이슈 → 이슈가 닫히면 stale · 재측정 | process |
| 3-5 | 제품 실패 문구를 기대값으로 흡수하기 전 결함 판정 절차 · 「별도 해결 대상」은 대장·이슈로 | process |
| 3-6 | 대상 sha 일회용 스택 end-to-end 명령(참조자료 필수 · 없으면 78) · 면제는 dev go 조건에서 뺀다 | preflight |
| 3-6 | FE 시험 대역에 실서버 호출 순서(describe → create) · describe 415 대역 | gate |
| §5 | P1 두 곳(415 매핑 ＋ describe catch 분기 · 업로드 쪽 포함) · P2 경도 정규화(NetCDF 포함) ＋ 실패 시 썸네일 보존 · P3 배너가 실패 반영 · P4 relay 한도 설정화·비동기 · P5 마감 설정화 ＋ 단계 안 검사 ＋ 메모리 | product |
| §0′ | `stage_reset` 이 DROP 직전 계수를 선언된 기대(빈 dev = 0)와 비교해 다르면 멈춘다 · 계수는 BYPASSRLS 롤 또는 API(`d1_account`·무소속 행 포함) | runner/gate |
| §0′ | S3 계획을 DROP 전에 DB 참조 키와 대조(#59) · selftest 에 비영 계수 red 픽스처 | gate |
| §0′ | preflight 에 최신 BYPASSRLS 백업의 크기·행 수 대 빈 상태 기준치 · 보고서가 `count-before` 값을 인용 | preflight |
| §0′ | 「없다·비었다」는 BYPASSRLS 롤·API 로만 판정 — 레인 지시 틀과 게이트 ③ 체크리스트에 · advisor 가 전제를 재측정하지 않았으면 판정 보류 · 상시 승인에 「비어 있음」 조건 | process |
| 3-4 | 판별 시험에 사전 중단 기준 · `reseed.sh` 신호 trap · 레인 지시에 참조자료·창 크기·상태 자리·대상 환경 필수 칸 | process |
| 한계 | 회차 창의 core-api·viz-render 로그를 run-dir 에 보존(즉석 `docker logs` 단일 출처 제거) | runner |

## 8. 미확정·한계

- 벽시계 손실은 병행 작업을 포함한 상한이다. 재시드 활동 0 인 C 구간(사람 결정 대기 2h41m12s 포함)을 빼면 드리프트 실작업은 ≈2h05m. R3 의 드리프트 5h46m(→4차 멈춤)은 4차 시간 초과 41m28s 를 포함 — 본 문서는 4차 시작까지 5h04m.
  R3 의 「846s×2 ＋ 격자 1566s」는 이중 계산이다 — 1,566s 가 846s 를 포함한다(180×4 ＋ 846).
- R3 집계 「24 커밋 · 35 파일 · +1451/−659」는 경로 필터 미기재로 재현 불가 — 본 문서 수치는 pathspec ① 재계산(커밋 수 24 는 일치)과 `frontend`·`services/core-api` 실측(§3-1). 원 수치는 드리프트를 3배 넘게 작게 적었다.
- seq 13·14 alert 문구는 로컬 재현 DOM 과 업로드 화면 덤프로 확인했다 — dev 상세 화면에서는 판독하지 않았다(러너가 `role="alert"` 를 읽지 않음). 415 귀속은 시각·trace 대조이고 target_id 로 묶지는 않았다.
- 재선택은 Chromium 키보드·팝업 경로에서 러너 전용으로 확인 · 팝업 항목 마우스 클릭과 Firefox·Safari·모바일 피커는 미실측 · #17·#18 인과 미분리(§2-4). GRIB 경로의 clamp(`readers.py:302-303`)가 0–360 원자료를 정확히 얹는지 미검증 · 0–360 NetCDF 는 합성 재현만.
- 4차 seq 18 두 대상 — 격자 없는 첫 업로드(143 · 34s 성립)와 격자 부착 재업로드(145 · 147s 실패)로 확정. DR-3 에서 격자 칸 미출현이던 seq 18 에 09-24 격자 칸이 선 경위는 미특정. 격자 붙인 143 의 dev 지연이 swap 압박 때문인지는 추정이다.
- 서버 근거는 즉석 `docker logs --since/--until` 단일 출처이고 저장 파일이 없다(415 · `MAP_BOUNDS_IMPLAUSIBLE` · `RENDER_TIMEOUT` · relay 503 · seq 15 요청 0). 컨테이너가 재생성되면(다음 reset·배포) 재검증할 수 없다.
- 사고(§0′) — 1,778 키 전부의 비현행 버전 존재는 목록 권한이 없어 표본 HEAD 로만 확인 · 팀원 QA 가 직접 조작인지 도구인지 미확인 · 9-13 세대 교체 주체 미확인.
- 최종화 — 계정 최종화 미실행 — 재시드 재개 전 계정 설계 재검토 필요(문제 #14). 재시드는 reset 게이트 수선까지 동결(§0′-6).
- 제품 결함 5건은 미수선 · 이슈 미등재(초안만). 설계 질문(§6)·예방 후보(§7)는 dev-reseed 개선 intent 의 입력이다.
