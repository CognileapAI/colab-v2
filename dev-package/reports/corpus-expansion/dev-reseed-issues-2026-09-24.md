# dev 재시드 문제 기록 — 2026-09-24 (1~7회차 · 수선 시도 3회 · 로컬 검증 2건)

메타 — 기준 `b210ce5c`(corpus-upload-classify) · 가지 `corpus-reseed-issues` · task `199ecf26fcde4293808329dd19531aa8` ·
**dev 무접촉**(실행 자리 로그·`state.json`·덤프만 읽었다). file:line 은 `b210ce5c` 기준이고, 인용한 제품 파일은
dev 가 도는 `ea21d8c2aa54` 와 줄이 같다(`routes/ingestion.py` 만 달라 sha 를 붙였다).
추기 2026-09-25 — 가지 `corpus-reseed-final`(`c24c03d4` 위) · task `6ee643917e0e444ab7e9c914256a7044` · dev 무접촉.
§4 이후 도구 file:line 은 `c24c03d4`, 제품 file:line 은 `git show ea21d8c2aa54:<path>` 로 대조했다.
run-dir 인용(`logs/verify.log` 등)은 `…/agent-a9c33a67acf039b61/dev-package/reports/dev-reseed-runs/wu2-20260924T0834Z/` 기준.
추기 2026-09-25 RCA — 근본 원인 분석 `dev-reseed-rca-2026-09-25.md`(같은 폴더) · #21·#22 **원인 확정** · #23 대부분 확정(bin 행 미확정) · 제품 결함 5건 확정(RCA §5).
추기 2026-09-25 사고 — 1차 `reset` 이 비어 있지 않은 dev(4,189행 · S3 1,778 키)를 지웠다 → **#26** · RCA §0′. dev 재시드는 reset 게이트 수선까지 동결.
추기 2026-09-25 재개 경로(가지 `reseed-verify-resume` · 사용자 결정 「실패한 꼬리만」) — #21·#22 세 행(seq 13·14 #133 · 16 #134)은 `dev-package/tools/dev-reseed/known-defects.json` 로 대조에서 면제(행·판정·비고 머리 일치만 · 그 밖은 종전대로 실패) · `reseed.sh --from verify --verify-from <3/3 실행 자리>` 가 상세 화면 순회 없이 3/3 판정표로 대조 → record-details → 계정 최종화 → report 를 잇는다 · 소유자 관리 운영자는 프로필 `ownerManaged` 로 초기 자격 조건만 건너뛴다(`SKILL.md` 「실패한 꼬리만 잇기」).
주장 검증 정정(RCA 가 우선) — #21 필요 원인 둘(describe() 415 미매핑 ＋ describe catch) · #15 139.5s 는 seq 17 · 원인은 파일 수 × 파일별 좌표 처리 · 637 MB 는 413 · #17 재선택은 러너 전용 · 파일 2개 이상이면 다른 파일로 복구 ·
#22 거절 지점은 warp 전 검사 `preview.py:261` · #23 09-13 측정은 자동 러너(고정 6초 대기 · bin 행 오염 가능) · §7 은 RCA §5 P1~P5 로 다시 맞췄다(P3 조용한 정체 아님 — 배너 문구 모순 · P4 503 7건 · 11.8~21.7s · 재선택·보기 단추 위치는 제외).

- **목적** — `dev-package/tools/dev-reseed`(+ `dev-package/tools/dev-seed/runner.py`) 개선 intent 의 입력.
  dev-reseed 는 **검증용 데이터를 세우는 도구**다. 이 문서는 무엇이 시간을 먹었고 무엇을 바꿀 후보인지만 적는다.
  intent·결정·코드 변경은 하지 않는다.
- **범위** — dev 1차(08:33Z) · 2차(09:12Z·09:13Z 두 시도) · 3차(12:44Z) · 4차(13:37Z) ＋ 로컬 검증 2건
  (L1 12:20Z 재로그인·재개 · L2 12:58Z 업로드 필수 칸). 회차 상세의 정본은 `wu2-reseed-2026-09-24.md` §1–32.
  추기 — 5차(14:20Z) · 6차(14:37Z) · 7차(15:16Z) · 수선 시도 1/3(15:44Z) · 2/3(16:18Z) · 3/3(16:30Z). 상세는 §4.
- **시간** — 1차 시작 08:33Z → 4차 멈춤 14:18:59Z ≈ **5시간 46분**. dev 가 실제로 돈 시간은 ≈ 47분
  (1차 162s · 2차 3s+22s · 3차 ≈155s · 4차 2,488s). 나머지는 멈춘 자리 분석·수선·로컬 재현·서명 대기다.
  4차 seed 2,484s 중 **고정 대기 초과가 ≈ 1,566s**(격자 칸 180s × 4 · seq 18 격자 판정 846s).
  4차 멈춤 시점 dev 의 데이터셋 = **17/28**(`state.json` · done 15 · registered_no_preview 2).
  추기 — 3/3 종료 16:37:12Z 까지 ≈ **8시간 04분** · dev 가 돈 시간 ≈ **2시간 11분**(5차 970s · 6차 2,289s · 7차 194s ·
  1/3 978s · 2/3 223s · 3/3 373s 추가). 5~7차 고정 대기 초과 ≈ 1,806s(seq 18 846s · 격자 칸 180s × 4 · 미리보기 120s × 2).
- **전반** — preflight 11/11 · doctor 15/15 · `--rehearse` 10/10 이 **전부 green 인 채** 본 실행이 네 번 멈췄다.
  세 검사 중 **대상 sha 의 화면 걷기(seed)** 를 밟는 것이 없고, 러너↔제품 권한 모델·업로드 계약 정합을 보는 게이트도 없다
  (인계서 `HANDOFF-20260924-k3-k4-corpus.md` §5-6).
- **최종(2026-09-25)** — 28/28 등록 · 3/3 seed exit 0 · 재시드 `stage_verify` exit 1(미리보기 성립 25 · 미성립 1 · 판정불가 2) ·
  **계정 최종화 미실행**. 수선 시도 3회 소진(#16 규칙). 문제 **26건**(1~15 = 1~4차 · 16~25 = 5차 이후 · 26 = 1차 reset 사고 추기). 제품 후속 후보 5건은 §7(RCA §5 P1~P5).

| # | 증상 | 원인 한 줄 | 임시 조치(commit) | 막았어야 할 검사 | 개선 후보 |
|---|---|---|---|---|---|
| 1 | 1차 projects 「대상 연구실을 선택해 주세요.」 | 권한 개정이 운영자 주체의 생성 4종에 대상 연구실 헤더 요구 · 러너는 교수를 임시 운영자로 둔 채 걷는다 | 운영자 창을 accounts 로 좁힘 `0d6a27b6` | 러너↔권한 모델 계약 | 계약 게이트 · 국면별 주체 선언 |
| 2 | 2차 projects 「지목점 project-new-button 해소 실패」 · 화면은 로그인 | 운영자 해제가 열린 세션을 무효화 → 401 → 로그인 화면 | 해제 뒤 재로그인 `c4cdd5ea` | 대상 sha 로컬 리허설 | 해제·재로그인을 리허설 원시동작으로 |
| 3 | (L1) `--from seed` 재개가 accounts 에서 「계정 관리 화면(account-create)이 열리지 않았다」 | 앞 회차가 이미 해제 → 재개 회차의 교수는 비운영자 | seed 첫 login 앞 운영자 올림 `71a1d95c` | 재개 경로 리허설 | 재개 전제(자격·상태)를 preflight 로 |
| 4 | 3차 datasets seq 1 「단계 이동 실패: ② 메타데이터 입력」 | 업로드 필수 칸(분류·유형·관측 간격·Lv0 출처)이 러너·계획·정본 어디에도 없음 | 배관 `d03b744e`·`02f5ab1f` · 값 서명 `b210ce5c` | 계약(required) ↔ 계획 대조 | 계약 게이트 · 값 출처를 정본에 |
| 5 | (L2) 「기간 고르기가 열리지 않았다」 · 3차 disabled 단추 click 성공 기록 | 가려진·비활성 요소 click 이 rc 0 | 기간 단추만 초점＋Enter `02f5ab1f` | 없음 | click 뒤 사후조건 · 비활성 사유 판독 |
| 6 | (L1) 엉뚱한 요소 누름을 선택자로 캐시 · 로그인 화면을 빈 프로젝트 목록으로 읽음 | `spot()` rc 0 캐시 · `project_index()` fail-open | 없음 | 없음 | 캐시 전 사후조건 · 로그인 화면 판별 |
| 7 | 4차 격자 칸 180s 초과 4회 · grid-skip 지목점 실패 6회 · 6차 격자 칸 180s 초과 4회 재발 | 부재를 고정 상한으로 기다린다 · 건너뛰기 문구 사라짐 | 없음 | 없음 | 상태 판독형 대기 · 선택 단계 정리 |
| 8 | 재개 상태·실행 자리가 앞 회차 체크아웃에 묶임 · `result.json` 이 회차를 섞음 | `SEED_WORK_DIR` 기본값이 `$REPO_ROOT` · run-dir 누적 | `COLAB_SEED_WORK_DIR` 손 지정 | 없음 | 체크아웃 밖 상태 자리 · 회차별 결과 |
| 9 | 회차 기록 뼈대가 실행 체크아웃을 dirty 로 만든다 | `stage_report` 가 `$REPO_ROOT/dev-package/sessions/` 에 미추적 파일 | 손으로 정리(이후 7회 · #24) | 없음(preflight:git 은 **다음** 실행에서 증상만) | 기록은 run-dir 에만 |
| 10 | 앞 회차 seed.log 소실 · 진행 로그가 줄 중간에서 끊겨 지연 | 단계 시작마다 로그 비움 · `redact` 의 `sed` 블록 버퍼 | 러너 자체 로그로 대조 | 없음 | 추가 쓰기 · 줄 버퍼 |
| 11 | 접속 env 감싸개가 둘 · 내용이 다름 | `with-dev-env.sh` 는 `COLAB_DEV_SECRETS_DIR` 미해제 · 대상 ref 없음 | 추적 제외 `wu2-env.sh` | 없음 | 도구가 env 를 스스로 검증 |
| 12 | 파일럿은 사람이 SQL 을 낸다 · 중단 수단이 없다 · 판정 기준이 포맷을 모른다 | 러너 기능 아님 · `reseed.sh` 에 trap 없음 · 첫 절차의 SQL 이 소유자 롤 | 인계서에서 BYPASSRLS 롤로 정정 | 없음 | 파일럿을 러너 국면으로 |
| 13 | L1 이 datasets 를 「참조자료가 없어」 면제 → 업로드 칸 결함이 dev 3차에서 처음 드러남 | 레인에 참조자료 경로(`03 Reference-Data`)가 주어지지 않음 | L2 에서 실물로 재측 | 없음 | 로컬 리허설의 입력 선언 |
| 14 | 재시드 창 동안 사람이 dev 계정을 못 쓴다 | 최종화 검사가 시드 직후 계정 상태를 전제한다 | 계정 변경 연기 | 없음 | 사람 계정과 시드 계정 분리 · 재시드 재개 전 계정 설계 재검토 |
| 15 | 4·5차 seq 18 「기준 격자 판정 표시가 나타나지 않았다」(846s) | 업로드 전체 파일 렌더가 서버 `RENDER_TIMEOUT`(138~147s) → 「위치 확인」이 끝내 안 선다 · 배너는 기본 문구 고정 | 격자 대기 2단 분리 `d3f7a222` | 대상 행 실물 리허설(L2 미답 24행) | 실패 표식 동시 판독 · 배너 문구 모순(§7) |
| 16 | 5·7차가 앞 회차와 같은 자리·같은 문면으로 멈춤 | 결정적 실패를 원인 확인 없이 **변경 없이 재시도** | 사용자 규칙 — 무변경 재시도 금지 · 수선 재시도 최대 3회 · 전략 서면 | 없음 | 무변경 재개 거절 · 「바꾼 것」 기록 필수 |
| 17 | 6·7차 러너 verify seq 15(GRIB) 「미리보기 terminal/display 시간 초과」(120s) · 뒤 4행 미측정 | 이미 고른 파일 재선택 → 설명 비움 → 보기 영구 비활성 · 비활성 click rc 0 | 재선택 금지 · 정착 대기 `f938198c`·`7ba6cdaf` | 없음 | 누름 사후조건 · 재선택은 러너 전용(§7 제외) |
| 18 | 1/3 러너 5행 `client_no_request` · 재시드 verify 26행 「클릭 미반영」 | 좌표 click 이 화면 밖 버튼(y≈1630~2424 · 창 577)을 스크롤 없이 누름 | 적중 검사 ＋ 초점·Enter(JS click 폴백) `008ad5ab`·`74612bca` | 없음 | 모든 누름을 초점·Enter 로 · 대역 페이지를 dev 창 크기로 |
| 19 | 1/3 재시드 verify seq 13·14 「미지원 상태 미확인」 ＋ 판정불가 행에 「기대와 달리 미리보기 성립」 | 미지원 표시는 보기를 누른 뒤에만 선다 · 누르지 않고 기다림 · 판정불가를 성립으로 셈 | 미지원 행도 누름 · 오표기 제거 `aa75b0e9` | 없음 | 기대 표식의 발생 조건을 계획에 |
| 20 | 2/3 seq 25(HDF4) `server_no_terminal` — 화면은 done · 「총 2.4초」 | 측정이 창과 교차하는 이미지만 셈 · 지도가 접힌 선 아래 | 슬롯 스크롤 ＋ 슬롯 안 이미지 계수 `c24c03d4` | 없음(앞 결함이 가림) | 측정 기준을 슬롯으로 · 행별 계수 |
| 21 | 3/3 seq 13·14(GeoPackage) 보기 비활성 45s → 판정불가 ＋ 파생 「가공 단계 불일치」「계수 판정불가」 | 비활성 원인 **미확인** · 판정불가 갈래가 읽은 계수를 `?` 로 적음 → **원인 확정**: ⑴ `describe()` 415 미매핑 ＋ ⑵ describe catch 가 「그릴 수 없음」으로 보내지 않음 — 두 곳 모두 수정 필요(RCA §2-5) | 없음(3회 소진) | 없음 | 비활성 사유 판독 · 계수 보존 · 제품(§7) |
| 22 | 3/3 seq 16(ERA5 변환 결과) 「예상 밖 미리보기 미성립」 | 누름 뒤 1.9s 에 slot failed · 사유 **미확인** → **원인 확정**: 경도 0–360 격자 bbox 거절(RCA §2-6) | 없음 | 없음 | 서버 `failure` 코드 판독 |
| 23 | 「3패스 전부 미성립」 3행(19·21·25)이 그려지고 「3패스 중 1회」 2행(15·17)이 매번 그려짐 | 기대값 출처(DR-3) 측정이 같은 누름 결함을 탔을 가능성(미검증) → **대부분 확정 · bin 행 미확정**: DR-3 자동 러너(고정 6초) 측정의 PV-2 시기 임시값이 `660854bb` ＋ `cf07186d` 뒤 낡음(RCA §2-7) | 없음 | 없음 | 기대값 재측정·서명 |
| 24 | 회차 기록 뼈대 손 이동 7회 · 와일드카드 이동이 추적 기록 7건까지 옮김 | #9 미수선 · 손 정리가 추적/미추적을 구분 못함 | 되돌려 놓음 | 없음 | #9 수선 · 손 정리 대상 한정 |
| 25 | 수선마다 가지 하나 — 9가지 선형 사슬 | 레인마다 새 가지 · 모을 가지 없음 | 최종 가지 `corpus-reseed-final` | 없음 | 회차 단위 가지 하나에 누적 |
| 26 | 1차 reset 이 계수 35·585·6·22 를 세고 13s 뒤 DROP · S3 1,778 키 삭제 — 비어 있지 않은 dev 를 지움 | reset 이 DROP 직전 계수를 보고도 진행(fail-open) · 전제 「DB 0」은 앱 롤 경계 없는 거짓 0 | 없음 — dev 재시드 동결(2026-09-25) | DROP 직전 계수 ↔ 기대(0) 대조 · BYPASSRLS 계수 | 계수 ≠ 0 이면 정지 · 계수는 BYPASSRLS/API · S3 계획의 DB 참조 대조 |

---

## 1. 멈춘 자리 (본 실행을 세운 것)

### #1 대상 연구실 헤더 — 1차 `projects`
- **증상** 「x 실패: 프로젝트 생성 실패(precipitation): 대상 연구실을 선택해 주세요.」(08:36:04Z). 덤프
  `.work/fail/project-precipitation-submit.txt` 에 빈 「대상 연구실」 combobox.
- **근거** `services/core-api/src/colab_core/app/target_scope.py:8`(`_CREATIONS` 4종) · `:31-34`(비운영자는 통과) · `:42-43`(거절) ·
  `frontend/src/components/project/ProjectFormModal.tsx:86` · prelude ④ 올림 `dev-package/tools/dev-reseed/stages.sh:486`.
- **원인** `bafae4a7`(2026-09-17 KST)이 운영자 주체에만 헤더를 요구한다. DR-4(`d5cd6ca9`, 09-14)에는 없던 규칙이고,
  러너는 교수를 임시 운영자로 둔 채 화면을 걸었다. `_CONTEXT` 8종(`getPreviewRender` 등, `:9-10`)도 같은 요구다.
- **임시 조치** ㈏ 운영자 창을 `accounts` 로 좁힘 — `0d6a27b6`(해제 `stages.sh:662`).
- **왜 못 잡았나** preflight 11 은 환경(git·aws·qemu·자원·계획 생성), doctor 15 는 배포 건강만 본다. 리허설 ⑻ 은 러너 `--phase report`
  (읽기 전용), ⑼ 는 첫 화면 제목만 읽는다. `dev-reseed-selftest` 는 dev 무접촉이라 러너 화면 경로가 없다.
- **개선 후보** ⓐ 제품 권한 규칙(`_CREATIONS`·`_CONTEXT`)과 러너 국면의 주체를 대조하는 게이트 ⓑ 국면별 주체(운영자/교수)를 계획에 선언.

### #2 운영자 해제 = 세션 무효화 — 2차 `projects`
- **증상** 「x 실패: 지목점 project-new-button 해소 실패. 마지막 사유 = ('text', '새 프로젝트') -> Element not found.」(09:14Z).
  URL 은 `/projects`, 화면은 「로그인」(`.work/fail/projects-fail.txt`).
- **근거** 발급 주장 `kernel/session_token.py:60-62` · 매 요청 재조회 `kernel/login_sessions.py:193-194` · 불일치 거절 `:226-228` ·
  API 해제 경로의 세션 폐기 `kernel/db_credentials.py:260-265` → `/me-v2` 401 → `frontend/src/api/client.ts:64-66` `clearIfCurrent` →
  `frontend/src/auth/store.ts:108-116`(suspended) · `:78-81`(`getToken()` null) → `frontend/src/auth/AuthGate.tsx:128-130` 로그인 화면.
- **원인** 자격을 어떤 길로 내리든 열린 세션은 닫힌다(제품 설계). ㈏ 옵션표가 이 결합을 몰랐다(보고 §12-1).
- **임시 조치** 해제 뒤 `--phase login` — `c4cdd5ea`(`stages.sh:672`). 당시 「재개 1회」 규칙으로 실물 미검증 → L1 A-2 에서 `POST /projects` 201×4 확인.
- **왜 못 잡았나** #1 과 같다. 로컬에서 대상 sha 로 seed 국면을 한 번도 돌리지 않았다.
- **개선 후보** ⓐ 운영자 올림/해제·재로그인을 리허설 원시동작으로(일회용 스택) ⓑ 해제 뒤 세션 유효성을 러너가 먼저 확인.

### #3 재개 회차의 운영자 창 부재 — L1 에서 발견(막지 않았으면 3차 멈춤)
- **증상** B-1 red 「x 실패: 계정 관리 화면(account-create)이 열리지 않았다 — 관리자 권한 계정으로 로그인했는지 확인한다.」
  (`local-verify-2026-09-24.txt` §B-1).
- **근거** `runner.py:1811-1814` 가 항목별 「이미 생성됨」 판정(`:1819-1822`)보다 **먼저** 계정 관리 화면을 기다린다.
- **원인** 앞 회차 ③ 이 이미 해제했으므로 `--from seed` 재개의 교수는 평범한 교수다.
- **임시 조치** `stage_seed` 첫 login 앞 `operator_grant` — `71a1d95c`(`stages.sh:651`). 보고 `8f90f7d7`.
- **왜 못 잡았나** preflight 는 재개 전제(누가 어떤 자격인가·state 의 어느 국면까지 왔나)를 보지 않는다.
- **개선 후보** ⓐ 재개 시 dev 의 자격 상태와 `state.json` 을 대조하는 preflight 항목 ⓑ 이미 끝난 국면은 화면을 열기 전에 건너뛰기.

### #4 업로드 필수 칸 — 3차 `datasets` seq 1
- **증상** 「x seq 1 실패: 단계 이동 실패: ② 메타데이터 입력 (기대 [data-testid="reg-name"])」(12:47:28Z). 덤프
  `…/agent-a9c33a67acf039b61/dev-package/tools/dev-seed/.work/fail/step-reg-step-2.txt` — 「분류 필수」「유형 필수」 `직접 선택해 주세요`,
  「② 메타데이터 입력」 `[disabled]`.
- **근거** `frontend/src/components/upload/RegisterArea.tsx:1164`(`classifyBlocked`) · `:1299` · 제출 검사
  `frontend/src/components/upload/UploadModal.tsx:1145`(#78) · 서버 `routes/ingestion.py:610`@`ea21d8c2aa54`
  (`_validate_create_required_metadata`) · 계약 `contracts/seams/fe-core.yaml:4871`(`required: [… category, dataType, observationInterval]`).
- **원인** `6705675d`(09-17 · 분류·유형 기본값 제거) · `e171c5c2`(09-16 · 관측 간격·Lv0 출처 필수). 값이 러너·`upload-plan.json`·
  정본 md 4건·`canonical-metadata.json`·k4 dev 스냅샷 **어디에도 없었다** → 28행 제안표(`upload-classify-proposal-2026-09-24.md`).
- **임시 조치** 배관 `d03b744e`(서명 전 계획 생성 거절) · `02f5ab1f`(러너 `select_classify`·`fill_interval`·`fill_source`,
  `runner.py:1165`·`:1175`·`:1185`) · 보고 `2d24bf0c` · 사용자 서명 `b210ce5c`(값 `dev-package/tools/dev-seed/upload-classify.json`). 소요 12:47Z→13:37Z.
- **왜 못 잡았나** preflight ⑽ 은 정본 md → 28/18 만 봤다. 계약의 `required` 와 계획 행을 대조하는 자리가 없다.
- **개선 후보** ⓐ `fe-core.yaml` 의 `DatasetCreate.required` ↔ 계획 행 대조 게이트(제품 병합 시점에 red) ⓑ 필수 값의 출처를 정본 md 로 올리기
  ⓒ 제품 필수 칸 개정 PR 의 완료 조건에 러너 갱신 포함.

## 2. 러너 판정의 약한 자리 (멈추거나 시간을 버리게 한 것)

### #5 누른 척 click — rc 0 인데 동작 없음
- **증상** L2 R2 「기간 고르기가 열리지 않았다」 — `reg-period-open` 중심의 `elementFromPoint` = `DIV.modal-h`(`upload-classify-local-2026-09-24.txt`).
  3차 러너 로그 `.work/logs/run-20260924-214536.log` — 비활성 「② 메타데이터 입력」 을 누르고 「지목점 reg-step-2 <- role:button」 성공 기록 → 「대기 시간 초과(15s)」 ×4.
- **근거** `runner.py:603-607`(rc 0 이면 성립) · `goto_step` `:1239-1250`(`reg-next` 비활성이면 탭 단추 폴백 후 15s 대기 4회) ·
  `activate` 머리말 `:1198-1205`(2026-09-13 「+ 추가」 같은 계열 선례).
- **원인** agent-browser `click` 은 가려지거나 비활성인 요소에도 0 을 돌려준다. 러너는 사후조건 없이 성공으로 센다.
- **임시 조치** 기간 단추 여닫기만 `activate`(초점＋Enter, `:1113`·`:1118`) — `02f5ab1f`. `spot()` 22 자리의 기본 `click` 은 그대로(보고 §32 ⒃).
- **왜 못 잡았나** 러너 픽스처는 계획 매핑만 보고 화면을 걷지 않는다.
- **개선 후보** ⓐ 모든 click 에 사후조건(다음 표식 출현) ⓑ `reg-next` 비활성이면 막는 칸을 읽어 즉시 실패(60s 대기 제거).

### #6 fail-open 둘 — 선택자 캐시 · 프로젝트 목록
- **증상** L1 — `vite` 개발 서버에서 `spot()` 문구 후보가 `<style>` 안 「새 프로젝트」를 누르고 성립으로 캐시(dev 번들에서는 미재현).
  `project_index()` 는 로그인 화면을 빈 목록으로 읽는다(2차에서는 뒤의 지목점 실패가 막았다).
- **근거** `runner.py:603-607`(캐시 저장) · `runner.py:865-893`(`project-list-error` 없으면 행 0 을 「없음」으로 반환).
- **원인** 성공 판정이 「명령 rc」와 「오류 표식 부재」 뿐이다.
- **임시 조치** 없음(보고 §25 ⑾⑿).
- **개선 후보** ⓐ 캐시 전 사후조건 ⓑ 목록 판독 전 로그인 화면(`login-submit`) 판별 — `stage_verify` 는 이미 이 판별을 한다(`stages.sh:889-900`).

### #7 고정 대기로 버린 시간 — 4차
- **증상** seq 6·7·15·17 「! 대기 시간 초과(180s) 격자 칸」→「격자 칸 미출현 — 화면이 좌표 보유로 판정. 격자 부착 생략」.
  seq 8~11·13·14 「! 지목점 grid-skip 해소 실패(선택 단계라 계속): ('text', '건너뛰기 — 나중에 올릴게요')」.
- **근거** `runner.py:1271-1279`(부재를 180s 상한으로 기다림) · `:1262-1268`(건너뛰기 후보) · 4차 러너 로그
  `.work/logs/run-20260924-223757.log`. seq 17 은 282.7s 중 180s 가 이 대기였다.
- **원인** 「격자 칸이 안 붙는다」가 정상 갈래인데 그 판정을 시간 초과로만 낸다. 건너뛰기 문구는 현 화면에 없다.
- **임시 조치** 없음. 비치명이라 실행은 이어졌다(합계 720s).
- **추기(6차)** seq 21·22·25·26 「! 대기 시간 초과(180s) 격자 칸」 재발 — 합계 720s. `d3f7a222` 의 2단 대기는 격자 칸이
  **붙은 뒤**의 판정 대기라 이 자리(`runner.py:1295`@`c24c03d4`)를 바꾸지 않았다.
- **개선 후보** ⓐ 화면의 격자 판정 상태(`data-grid-state` 등)를 읽어 즉시 분기 ⓑ 계획에 행별 기대(격자 부착/좌표 보유)를 싣기 ⓒ 사라진 선택 단계 정리.

## 3. 운영의 함정 (실행을 느리게 하거나 기록을 흐린 것)

### #8 상태·실행 자리가 앞 회차 체크아웃에 묶인다
- **증상** 재개 상태(`state.json` 의 계정 국면 기록)가 1차를 낸 `agent-a9c33a67acf039b61` 의
  `dev-package/tools/dev-seed/.work` 에만 있다. 3·4차는 다른 체크아웃에서 냈다 → `COLAB_SEED_WORK_DIR` 를 손으로 줘야 했다(어드바이저 발견).
- **근거** `dev-package/tools/dev-reseed/reseed.sh:247`(기본값 `$REPO_ROOT/dev-package/tools/dev-seed/.work`). run-dir 도 같은 워크트리
  `…/dev-package/reports/dev-reseed-runs/wu2-20260924T0834Z`. 4차 `result.json` 실측 — `startedAt 08:33:26Z`(1차) · `durationSec 2544`
  (1·4차 단계 합) · `report` 조각 `12:47:28Z`(3차 — `stage_report` 가 자기 조각보다 먼저 결과를 만든다) · `blocked` 에 09:12Z `preflight:git` ·
  `doctorSummary` 는 08:34Z 값.
- **원인** 상태·결과의 자리가 「체크아웃」과 「run-dir 누적」에 매여 있다(보고 §18 ⑺⑻).
- **개선 후보** ⓐ 상태를 체크아웃 밖(대상 dev·회차 식별자 키)에 ⓑ 회차마다 결과를 따로, 누적은 색인으로 ⓒ 재개 시 상태 자리 불일치를 preflight 가 거절.

### #9 회차 기록 뼈대가 실행 체크아웃을 dirty 로 만든다
- **증상** 3차를 낸 `agent-a604d198b8b428287` 에 `?? dev-package/sessions/DR-4-run-20260924T124453Z.md`(21:47 KST 생성 · 이후 정리됨).
  4차를 낸 `agent-ad12aacdc6d835b49` 에도 `DR-4-run-20260924T133731Z.md` 가 생겼다(23:18:59 KST). 그 체크아웃에서 다음 실행은 preflight:git 에 걸린다.
- **근거** `stages.sh:1212-1215`(바꾸는 단계가 돌면 `$REPO_ROOT/dev-package/sessions/` 에 쓴다) · `preflight.sh:43-45`(작업 트리 청결).
- **정정** 2차 시도 1 의 「작업 트리가 깨끗하지 않다 — 4 건」(`blocked.jsonl`)은 보고 §14 가 「커밋 후 재시도」로 적었다 —
  회차 기록 탓이 아니라 수선 파일 미커밋으로 읽힌다(§19 의 네 파일).
- **추기** 4차 이후 7회 모두 재발 · 손 정리 사고 1회 → #24. 쓰는 자리는 `c24c03d4` 에서 `stages.sh:1352-1355`.
- **개선 후보** ⓐ 기록은 run-dir 에만, 레포 반영은 사람이 ⓑ preflight 가 자기 도구의 산출물만 있는 dirty 를 구분해 안내.

### #10 로그가 덮이고 늦게 보인다
- **증상** 실행 자리 `logs/seed.log` 에 4차만 남았다(1~3차 소실 — 3차 datasets 기록은 러너 자체 로그 `.work/logs/run-20260924-214536.log` 에만 있다).
  4차 도중 `seed.log` 가 22:58:17 KST 에 「· 등록 확정(reg-done) — 자동 재시도 」 에서 줄 중간으로 끊긴 채 20분 멈춰 보였다. 같은 시각 러너 로그는 seq 18 까지 진행.
- **근거** `dev-package/tools/dev-reseed/lib.sh:133`(`: > "$STAGE_LOG"`) · `lib.sh:18-22`(`redact` = `sed -E`, `-u` 없음) · `lib.sh:80`
  (`"$@" 2>&1 | redact | tee -a`). 러너는 줄마다 flush 한다(`runner.py:147-153`) — 지연은 `sed` 블록 버퍼다.
- **정정** 오케스트레이터가 본 「등록 확정(reg-done) — 자동 재시도」 1회는 모든 건에 찍히는 「… 자동 재시도 없음」(`runner.py:1606`)이 버퍼 경계에서 잘린 것이다. 재시도는 없었다.
- **개선 후보** ⓐ 단계 로그를 회차별 파일로(덮지 않기) ⓑ 줄 버퍼(`sed -u` 등) ⓒ 진행 판독의 정본을 `state.json` 으로 명시.

### #11 접속 env 감싸개가 흩어져 있다
- **증상·근거** 레포 밖 dev 접속 env 감싸개(`with-dev-env.sh`)는 `COLAB_DEV_SECRETS_DIR` 를 export 하는 설정을 싣고 **unset 하지 않으며**
  `COLAB_RESEED_TARGET_REF` 도 안 싣는다. 실제 실행은 추적 제외 `dev-package/reports/dev-reseed-runs/wu2-env.sh`(unset ＋ 대상 ref)로 했다.
  규칙은 `.agents/skills/dev-reseed/SKILL.md:101-106` 에만 있고, 도구는 그 이름을 읽지 않는다고만 적혀 있다(`reseed.sh:26-30`).
- **개선 후보** ⓐ 레포 안 감싸개 하나 ⓑ `reseed.sh` 가 금지 env 를 보면 거절(78) ⓒ 대상 ref 를 인자로 필수화.

### #12 파일럿이 러너 기능이 아니다
- **증상** 「첫 업로드 뒤 자동 메타 non-null 확인」은 사람이 `state.json` 을 보다가 SQL 을 내는 절차다(`wu2-prereq-2026-09-24.md` §7).
  첫 절차 §7-2 는 소유자 롤 접속 정보로 내라고 적었다 — FORCE RLS 로 **거짓 0**(보고 §6 · 인계서 §4-(b)에서 BYPASSRLS 로 정정).
  중단하려면 `reseed.sh` 에 trap 이 없고 `run()` 이 파이프라(`lib.sh:80`) bash 만 죽이면 러너가 계속 올린다 → 프로세스 그룹 kill →
  `result.json`·회차 기록이 안 남는다(`reseed.sh:337-343` 의 report 가 돌지 못함).
- 판정 기준 「첫 3건 전부 null 이면 중단」(인계서 §4-(b))은 포맷을 모른다(seq 1 bin · seq 2 nc · seq 3 npy). 4차 실측으로 seq 1(Binary)도
  crs·grid·period_start 가 찼다 — 격자 파일 지정 때문으로 보인다. 도구 안에 autometa 를 보는 자리는 없다(`stages.sh`·`runner.py` 에 해당 판독 0).
- **개선 후보** ⓐ `datasets` 국면에 N 건 뒤 읽기 전용 autometa 판독(BYPASSRLS)과 포맷·격자 지정별 기대표 ⓑ 판정 미달이면 러너가 스스로 멈추고 결과를 남김
  ⓒ `reseed.sh` 에 신호 trap(자식 정리 ＋ report).

### #13 로컬 검증 레인에 참조자료가 주어지지 않았다
- **증상** L1 이 `datasets`·`verify`·`report` 를 「참조자료가 없어」 명시 면제(보고 §21 · `local-verify-2026-09-24.txt` 의 `SKIP(local exemption)` 3줄).
  참조자료는 레포 밖 `03 Reference-Data`(4.4 GB)에 있었다. L2 는 그것으로 R1 을 dev 3차와 같은 문면으로 재현했다.
- **원인** 레인 지시에 경로가 없었다. L1 에서 datasets 를 돌렸다면 #4 는 3차 전에 나왔을 것이다.
- **개선 후보** ⓐ 로컬 리허설 명령이 `COLAB_REF_ROOT` 를 필수 입력으로(없으면 78) ⓑ 면제는 건수·사유와 함께 **다음 dev 실행의 go 조건**에서 빼기.

### #14 재시드 창 동안 사람의 dev 계정이 묶인다
- **증상** 사람이 쓰는 dev 계정의 변경을 재시드 창 동안 미뤘다.
- **근거** 최종화 단계 `stages.sh:741-752`(`account_finalize`).
- **개선 후보** ⓐ 사람 계정과 시드 전용 계정 분리 ⓑ 최종화 검사를 「시드가 만든 직후」로 한정하고 이후 사람 변경은 허용 ⓒ 창의 시작·끝을 사람에게 알림
  ⓓ 계정 최종화 미실행 — 재시드 재개 전 계정 설계 재검토 필요.

---

## 4. 4회차 이후 — 5~7회차 · 수선 시도 3회 (최종 · 2026-09-25 추기)

| 회차 | UTC | 도구(가지 · sha) | 결과 | 멈춘 자리 |
|---|---|---|---|---|
| 4 | 13:37:31–14:18:59 | `corpus-upload-classify` `b210ce5c` | seed exit 1 · 17/28 등록 | seq 18 격자 판정 846s(#15) |
| 5 | 14:20:48–14:36:58 | 같음(무변경) | seed exit 1 · 같은 문면 | 같음(#15·#16) |
| 6 | 14:37:13–15:15:22 | `corpus-grid-wait` `d3f7a222` | **28/28 등록** · seed exit 1 | 러너 verify seq 15 120s(#17) |
| 7 | 15:16:31–15:19:45 | 같음(무변경) | seed exit 1 · 같은 문면 | 같음(#16·#17) |
| 1/3 | 15:44:39–16:00:57 | `corpus-verify-preview` `f938198c` ＋ `-2` `7ba6cdaf` | seed exit 0 · verify exit 1 | 러너 5행 요청 0 · 재시드 verify 28행 판정불가(#18·#19) |
| 2/3 | 16:18:59–16:22:42 | `-3` `008ad5ab`·`74612bca` ＋ `-4` `aa75b0e9` | seed exit 1 | seq 25 `server_no_terminal`(#20) |
| 3/3 | 16:30:59–16:37:12 | `-5` `c24c03d4` | seed exit 0 · verify exit 1 · **최종화 미실행** | 13·14 판정불가 · 16 미성립(#21·#22) |

- 4차 — 체크아웃 `agent-ad12aacdc6d835b49` · run-dir 1~3차와 같음 · `COLAB_SEED_WORK_DIR` 명시. preflight 11/11 → 올림 `INSERT 0 1` →
  login → accounts 4건 「이미 생성됨」 → 해제 `DELETE 1` → 재로그인 → projects 4건 「이미 완료」 → datasets 13:37:57Z.
- **파일럿 통과**(오케스트레이터 실측) — seq 1 Binary · seq 2 NetCDF 모두 `d3_dataset_autometa` 행 있음 · crs·grid·period_start non-null
  (BYPASSRLS 읽기 전용 SQL). ⇒ 운영자 해제 뒤 **교수 세션으로 업로드·등록이 선다**는 첫 실측.
- 4차 진행 — seq 1~17 등록(done 15 · `registered_no_preview` 2 = seq 13·14 「형식 인식 실패」). seq 16 「격자 경계 위생 실패 — 지도형 미생성」(비치명).
- 1/3~3/3 은 #16 규칙(수선 뒤 재시도 최대 3회 · 매 시도 전 전략 서면)으로 돌았다. 한 수선이 앞 결함을 걷어내면 그 뒤 결함이 드러나는
  순서였다(#17 → #18 → #20).
- 3/3 재시드 `stage_verify` — 28행을 **실제로 판정한 dev 첫 순회**(1/3 순회는 28행 전부 판정불가). 판정 성립 25 · 미성립 1 · 판정불가 2 →
  대조 「미리보기 판정불가 13,14 · 예상 밖 미리보기 미성립 16 · 계수 판정불가 13,14 · 가공 단계 판정불가 13,14 · 가공 단계 불일치 13,14」 →
  `stages.sh:1141` 에서 종료 → `account_finalize`(`:1144`) 미실행. 증거 `preview-judgment.tsv` · `logs/verify.log` · `session-records/`.

### #15 업로드 전체 파일 렌더 실패 = 격자 판정 무한 대기 — 4·5차 `datasets` seq 18
- **증상** 「! 대기 시간 초과(846s) 격자 판정」→「x seq 18 실패: 기준 격자 판정 표시가 나타나지 않았다.」(4차 14:18:59Z · 5차 14:36:58Z).
  seq 18 = GK-2A LST 변환 결과 · npy 143 파일 463,338,304 B ＋ 기준 격자 `lat2d.npy`·`lon2d.npy` 6,480,256 B(`plan-manifest.yaml:283-297`).
  덤프 `.work/fail/18-failed.txt` — 「그리는 데 너무 오래 걸려요. 조각 하나나 좁은 기간으로 다시 해 보세요.」 · 「올린 파일 145개」.
- **근거**(`ea21d8c2aa54`) 렌더 마감 초과 `services/viz-render/src/colab_viz/domains/d7_visualization/jobs.py:553-558` · 문구 `failures.py:49` ·
  성공 결과만 `onResult` `frontend/src/components/upload/PreviewPanel.tsx:270-273` · 수렴 문구는 `onPreviewResult` 안에서만 갱신
  `UploadModal.tsx:558-572` → 배너가 기본 문구 「전체 파일 기준으로 미리보기를 다시 확인해요.」에 고정 `UploadModal.tsx:1491-1495`(:1493) ·
  격자 「위치 확인」(`up-grid-accept`)은 렌더 결과가 서야 `gridFlow.ts:264` · `GridUploadBlock.tsx:156-161`.
- **원인** 서버 렌더 `RENDER_TIMEOUT` 실패(dev 로그 실측 · 정정 RCA §2-3 — seq 18 격자 부착 재업로드 147,334 ms(4차) · 137,794 ms(5차) · 139,478 ms 는 seq 17).
  화면은 오류(`up-preview-error`)를 띄우지만 실패를 수렴 배너·격자 판정에 전하지 않는다.
  러너는 성공 표식만 846s 기다렸다(`runner.py:1286-1311`@`b210ce5c`). seq 17(nc 143 파일 · 약 50 MB)도 시간 초과 — 파일 수 × 파일별 좌표 처리가 시간을 지배(RCA §2-3).
  5차는 같은 입력이라 결정적으로 재현.
- **별도 증상** `describeTarget` 503 — `services/core-api/src/colab_core/app/relay.py:28` `RELAY_TIMEOUT_SECONDS = 10` <
  viz-render `target-descriptions` — 503 7건(13:58~14:40Z) · viz describe 11.8~21.7s · 파일 수 143~145 묶음. core-api 가 ≈10,03x ms 에 503 을 낸 뒤 viz-render 는 200.
  DR-3(2026-09-13)이 같은 결합을 이미 실측했다(`stages.sh:682-684` · `PV-2`). 격자 후보는 `status.ready` 로만 서므로(`UploadModal.tsx:613-624`)
  격자 대기와는 독립.
- **임시 조치** 격자 대기 2단 — 1단 판정 표시 또는 서버 격자 수용(`up-grid-expected-bounds`) → 2단 렌더 판정 ≤ 300s
  (`GRID_RENDER_WAIT_S` `runner.py:93`). 렌더 성립을 요구하지 않는 행은 실패·미도착이면 `registered_no_preview` 로 등록
  (`runner.py:1317-1344` · `:1684-1689`) — `d3f7a222`. 6차 seq 18 「전체 파일 렌더 실패 — 격자는 서버 수용, 위치 확인 없이 등록」
  (수용 → 실패 판독 139s · 행 334.8s).
- **왜 못 잡았나** L2 가 밟은 4행 밖(보고 §32 ⒀). 러너가 실패 표식(`up-preview-error`)을 읽지 않았다(#7 과 같은 계열).
- **개선 후보** ⓐ 성공·실패 표식을 한 대기에서 판독 ⓑ 좌표·격자가 있는 다중 파일 행의 렌더 기대를 계획에 ⓒ 제품 후속(§7 P3·P4·P5).

### #16 변경 없는 재시도 — 5·7차
- **증상** 5차(14:20:48Z)는 4차와 같은 `b210ce5c` 로 재개 → seq 18 에서 같은 846s · 같은 문면(seed 967s).
  7차(15:16:31Z)는 6차와 같은 `d3f7a222` → 러너 verify seq 15 에서 같은 120s · 같은 문면(seed 189s).
- **원인** 결정적 실패를 원인 확인 없이 우발로 보고 다시 냈다 — 오케스트레이터의 절차 실수. 두 회차 dev 점유 ≈ 19분.
- **임시 조치** 사용자 규칙(2026-09-25) — 무변경 재시도 금지 · 수선 뒤 재시도 최대 3회 · 매 시도 전 전략(원인 · 바꾼 것 · 기대 결과)을 서면으로.
- **왜 못 잡았나** 도구가 재시도의 무변경을 보지 않는다 — 같은 도구 sha·같은 run-dir 재개가 그대로 돌았다.
- **개선 후보** ⓐ 재개 시 직전 실패 회차와 도구 sha·입력이 같으면 거절(78) ⓑ 회차 기록에 「바꾼 것」 칸 필수 ⓒ 시도 횟수를 도구가 셈.

### #17 같은 파일 재선택 → 보기 영구 비활성 — 6·7차 러너 `verify`
- **증상** 「데이터셋 계수 화면 28 / 계획 28 · 저장 기간 28 / 28 · 계보 간선 18 / 18」 뒤 seq 15(GRIB)
  「! 대기 시간 초과(120s) 미리보기 terminal/display」→「x 실패: 미리보기 terminal/display 시간 초과 — 후속 요청을 중단한다」
  (6차 15:15:21Z · 7차 15:19:44Z). 뒤 4행(17·19·21·25) 미측정.
- **근거**(`ea21d8c2aa54`) `frontend/src/components/datasetpreview/DatasetPreviewSection.tsx:347-351`(파일을 고르면 설명을 비움) ·
  `:169-183`(describe 는 `selectedFileId` 가 바뀔 때만) · `:355`(`!description` 이면 보기 disabled).
- **원인** 러너가 이미 선택된 파일을 다시 골랐다 → 설명이 비고 다시 오지 않음 → 보기 영구 비활성.
  agent-browser 0.27.0 `click` 은 비활성 버튼에도 성공(#5 계열) → POST `/api/v1/previews` 0건인 채 120s.
- **임시 조치** `f938198c`(러너) — 재선택 금지 · 정착(파일 일치 · 보기 활성 · slot idle 1s 연속, `runner.py:2023-2027`) 뒤에만 누름 ·
  시간 초과 증거(요청·콘솔) · 행별 판정표 · `client_no_request` 는 「관측 · 제품 결함 추적」으로 비차단(사용자 2026-09-25 · `runner.py:2202-2205`).
  `7ba6cdaf`(재시드 verify 같은 규칙 · `stages.sh:686-693` · `:770`).
- **왜 못 잡았나** 러너 픽스처는 화면을 걷지 않는다.
- **개선 후보** ⓐ 모든 누름에 사후조건(slot 이 idle 을 떠남) ⓑ 재선택은 러너 전용 — 제품 후보에서 제외(§7 · RCA §5).

### #18 좌표 click 이 화면 밖 버튼을 누른다 — 시도 1/3
- **증상** 러너 5행(15·17·19·21·25) 모두 `client_no_request` — 「보기 활성 · POST /api/v1/previews 0건 · slot idle」(15:47~15:56Z).
  비차단이라 seed exit 0. 재시드 verify 는 26행 「클릭 미반영(5000ms 안에 slot 이 idle 을 떠나지 않았다 · 받은 값 [idle])」 ＋ 13·14 →
  28행 전부 판정불가 · verify exit 1(`blocked.jsonl` · `session-records/DR-4-run-20260924T154439Z.md`).
- **근거** 적중 검사 — 보기 단추 중심 y ≈ 1630~2424 · 창 1280×577(`preview-judgment.tsv` 비고). 머리말 `runner.py:2085-2091` · `stages.sh:701-707`.
- **원인** agent-browser 0.27.0 좌표 click 은 화면 밖 요소를 스크롤 없이 누르고 rc 0. 로컬 대역 페이지에서는 스크롤 뒤 고정 층이 버튼 중심을
  덮는 경로도 실측했다(오케스트레이터 보고의 「올리다 만 것이 있어요」 층). dev 에서는 2/3·3/3 의 스크롤 뒤 적중이 전 행 「= 보기 단추」 —
  **덮임은 dev 에서 관측되지 않았다**.
- **임시 조치** 누르기 전 적중 검사(스크롤 전·뒤 중심 `elementFromPoint`) ＋ 초점·Enter · 5s 안에 idle 을 안 떠나면 JS click 한 번
  (`runner.py:2164-2197` · `stages.sh:739` · `:755`) — `008ad5ab`·`74612bca`. 2/3 러너 15·17·19·21 그려짐.
- **왜 못 잡았나** 1/3 전 로컬 재현(jsdom · 대역 페이지)은 재선택 결함만 봤다(`f938198c` 본문). 창 크기·접힌 선은 재현하지 않았다.
- **개선 후보** ⓐ 러너 전체 누름을 초점·Enter 원시동작으로(#5 ⓐ 와 합침) ⓑ 대역 페이지를 dev 창 크기로.

### #19 미지원 행 확인 흐름 — 재시드 verify seq 13·14
- **증상** 1/3 재시드 verify 13·14 「승인된 미지원 표시를 확인하지 못했다」 ＋ 대조 「기대와 달리 미리보기 성립 seq 13,14」(판정불가 행을 성립으로 셈).
- **근거**(`ea21d8c2aa54`) 미지원 표시(`dt-preview-unsupported`)는 보기 → 「그릴 수 없음」 응답 뒤에만 선다 `DatasetPreviewSection.tsx:362-366`.
  조각이 전부 비렌더면 열자마자 선다(`:155-156`).
- **원인** 재시드 verify 가 미지원 행에서 누르지 않고 표시만 기다렸다. 대조가 판정불가를 성립 쪽에 넣었다.
- **임시 조치** 미지원 행도 정착 → 적중 → 초점·Enter 뒤 표시 대기(`stages.sh:929-960`) · 「기대와 달리 성립」은 실제 성립 행에만
  (`stages.sh:1097-1100`) — `aa75b0e9`. 로컬에서 발견해 2/3 전에 넣었다.
- **왜 못 잡았나** 계획 기대 「미성립(포맷 미지원 · 판정 표에 이름으로)」(`plan-manifest.yaml:215`·`:228`)에 표시의 발생 조건이 없다.
- **개선 후보** 기대 표식과 그 발생 조건(누름 뒤 · 열자마자)을 계획에 함께.

### #20 측정이 창과 교차하는 이미지만 셌다 — 시도 2/3
- **증상** 러너 seq 25(HDF4) 「server_no_terminal · POST /api/v1/previews 1건(202) … slot done」→「미리보기 hdf4(seq 25) = 미확인 · 차단」 →
  seed exit 1(16:22:42Z). viz-render 2.0s 성공 · 화면 「총 2.4초」(오케스트레이터 실측).
- **근거** 종전 측정 `runner.py:1944-1957`@`aa75b0e9` — `box.top < window.innerHeight` 필터. 보기 단추를 y≈288 로 스크롤한 채
  지도는 범례 아래(접힌 선 밖)에서 시작 → imageCount 0.
- **원인** 「그려졌다」를 창 교차로 판정했다.
- **임시 조치** slot done 이면 슬롯을 화면으로 스크롤 · 슬롯 **안** 이미지를 폭·높이 > 0 ＋ decode(`complete ∧ naturalWidth > 0`)로 셈 ·
  행별 images·decoded 칸(`runner.py:1944-1972`) — `c24c03d4`. 3/3 러너 5/5 그려짐(「총 2.3~3.5초」) · 「전건 통과」 · seed exit 0.
- **왜 못 잡았나** 1/3 까지는 누름이 반영되지 않아 측정 단계에 닿은 행이 없었다 — 앞 결함이 가렸다.
- **개선 후보** 측정 기준을 슬롯으로 고정하고 행별 계수를 판정표에 남기기(반영됨).

### #21 GeoPackage 보기 비활성 → 판정불가 — 3/3 잔여
- **증상** seq 13 SPI-4weeks · 14 SPEI-4weeks 「보기 전 정착 미확인(45000ms · 파일 [01M39TZBY0WHSNA8YRCERKA7WX] · 보기 활성 [false] ·
  slot [idle]) · 보기를 누르지 않았다」 → 「미지원 표시 [0] · slot [idle] · 보기 활성 [false] · preview-unavailable [0] · 로그인 [0] · 상세 [1] ·
  누름 없음」 → 판정불가(`logs/verify.log:788`·`:797`·`:1063`·`:1072`). 파생 「가공 단계 불일치」「가공 단계 판정불가」「계수 판정불가」 13,14.
- **근거** 보기 disabled 식 `DatasetPreviewSection.tsx:355`(`!selectedFileId || !description || palettes.length === 0`) · 오류 표시 `:340`(`role="alert"`).
  전부 비렌더 갈래(`:155-156`)도 아니다(파일 선택값 있음 · 미지원 표시 0). 파생 판정 — 판정불가 갈래가 이미 읽은 가공 단계·「미지정」·usage
  (`stages.sh:966-968`)를 버리고 `?` 로 적는다(`:981`) → `count-verdict.json` 이 계수·가공 단계 판정불가로 센다(`:821-826`).
- **원인** 보기 비활성의 직접 원인(describe 오류 · 설명 미도착 · 팔레트 0) **미확인** — `role="alert"` 를 판독하지 않았다.
  4차 등록 때 두 행은 「형식 인식 실패」(형식 미탐지 2 · §8). 선행 등재 `FMT-GPKG`(`03-HANDOFF.md:13`).
- **원인 확정**(RCA §2-5) — describe 가 415 로 옳게 거절하는데 ⑴ FE `describe()` 에 415 → 「그릴 수 없음」 매핑이 없고(`datasetPreviewSource.ts:77-86`@`ea21d8c2aa54` · `cf07186d` 도입) ⑵ describe catch(`DatasetPreviewSection.tsx:179-181`)가 「그릴 수 없음」으로 보내지 않고 `loadError` 만 세운다 — 두 곳 모두 수정 필요(⑴ 만 고치면 여전히 alert · 로컬 재현). 설명이 안 오고 보기가 영구 비활성 — 제품 결함. 파생 3건은 판정불가 갈래의 `?`.
- **임시 조치** 없음(수선 3회 소진).
- **개선 후보** ⓐ 비활성 사유(오류 문구 · describe 응답 · 팔레트 수) 판독 ⓑ 판정불가 행도 읽은 계수를 남겨 파생 판정 제거 ⓒ 제품(§7 P1).

### #22 seq 16 예상 밖 미리보기 미성립 — 3/3 잔여
- **증상** seq 16 ERA5 변환 결과 — 적중 「= 보기 단추」 · 초점·Enter → slot drawing → 1,909 ms 뒤 failed · `dt-preview-total` 없음
  (`logs/verify.log:1126-1148`) · 「미리보기 최종 상태가 failed다」 → 대조 「예상 밖 미리보기 미성립 seq 16」.
- **근거** 계획 기대 「미판정 대상」(`plan-manifest.yaml:250-256`) · 「예상 밖」 = 미성립 − 포맷 미지원 기대 행(`stages.sh:1096`).
- **원인** **미확인** — 서버 `failure` 코드를 판독하지 않았다. 4차 등록 때 「격자 경계 위생 실패 — 지도형 미생성」과의 관계도 미확인.
- **원인 확정**(RCA §2-6) — 짝 격자 `lon2d.npy` 가 경도 0–359.75 라 viz-render warp 전 1차 검사(`preview.py:261` → `check_bbox_4326` `:100-101`@`ea21d8c2aa54`)가 `MAP_BOUNDS_IMPLAUSIBLE` 로 거절(436 ms) — 제품 결함 · 4차 「격자 경계 위생 실패」와 같은 원인. 「미판정 대상」 행을 판정한 것은 계획-러너 의미 불일치.
- **임시 조치** 없음.
- **개선 후보** ⓐ 실패 행의 `failure` 코드를 판정표에 ⓑ 격자 위생 실패 행의 미리보기 기대를 계획에 ⓒ 제품(§7 P2).

### #23 계획 기대값 표류 — 「3패스」 기대
- **증상** 「**렌더 미성립(3패스 전부)**」 3행(seq 19 bin · 21 tif · 25 hdf4)이 그려졌고, 「렌더 성립(3패스 중 1회)」 2행(15 grib · 17 nc)은
  누름이 반영된 뒤 매번 그려졌다(2/3 러너 4행 · 3/3 러너 5/5 · 3/3 재시드 verify 성립).
- **근거** 기대값 `plan-manifest.yaml:241`·`:274`·`:305`·`:336`·`:398`. 출처 DR-3(2026-09-13) 「미리보기 렌더 성립 2/5(grib·nc · 미렌더 bin·tif·hdf4)」
  (`03-HANDOFF.md:359`).
- **원인** 추정 — DR-3 의 「flaky」 측정이 #17·#18 과 같은 누름 결함을 탔다. **미검증**.
- **원인 대부분 확정**(RCA §2-7 · bin 행 미확정) — 출처는 수동 관측이 아니라 DR-3 agent-browser 자동 러너(고정 6초 대기)의 3패스 측정이다. PV-2 서버 한계 아래 임시값이 `660854bb`(09-15) ＋ `cf07186d`(09-16 · 측정 단위를 전체 데이터셋 자동 렌더 → 선택 파일 1건으로 바꿈) 뒤 낡았고 재측정 트리거가 없었다 — stale-product-changed. 이번 러너 결함(09-24~25)은 09-13 측정에 끼어들 수 없다. grib·nc·tif·hdf4 는 503 문구가 뒷받침 · bin 행은 러너 고정 대기 개입 여지(미확정).
- **임시 조치** 없음. 러너는 기대와 달리 그려진 행을 통과로 셌다.
- **개선 후보** ⓐ 기대값을 재측정해 사용자 서명으로 갱신 ⓑ 기대값 옆에 측정 방법·sha.

### #24 회차 기록 손 정리 — 7회 · 추적 기록 휩쓸림
- **증상** 4차~3/3 의 7회 모두 실행 체크아웃에 `?? dev-package/sessions/DR-4-run-*.md` 가 생겨 손으로 run-dir `session-records/` 로 옮겼다
  (`T133731Z`~`T163059Z` 7건 · 3차 `T124453Z` 포함 8건 보관). 한 번은 오케스트레이터의 와일드카드 이동이 **추적 중인** 기록 7건
  (`DR-4-run-20260914T*` 5 · `20260924T083322Z` · `T091342Z`)까지 옮겨 되돌렸다.
- **근거** `stages.sh:1352-1355`(`MUTATED` 이면 `$REPO_ROOT/dev-package/sessions/`) · #9.
- **원인** #9 미수선. 손 정리의 대상 선택이 추적·미추적을 가리지 않는다 — 손 정리 자체가 위험원.
- **임시 조치** 되돌려 놓음.
- **개선 후보** ⓐ #9 ⓐ(기록은 run-dir 에만) ⓑ 수선 전까지 손 정리는 `git status --porcelain` 의 `??` 줄만 대상으로.

### #25 수선마다 가지 하나 — 9가지 선형 사슬
- **증상** `k3-resume` `4b162d54` → `corpus-local-verify` → `corpus-upload-classify` → `corpus-grid-wait` → `corpus-verify-preview` →
  `-2` → `-3` → `-4` → `-5`(`c24c03d4`). 가지마다 앞 가지 위에 1~4 커밋. 곁가지 `corpus-reseed-issues`(이 문서 1판).
- **원인** 레인마다 새 가지를 냈고 모을 가지가 없었다.
- **임시 조치** 최종 가지 `corpus-reseed-final`(= `-5` ＋ 이 문서).
- **개선 후보** 회차 단위 가지 하나에 수선 커밋을 누적 · 레인 가지는 합친 뒤 정리.

### #26 reset 이 DROP 직전 계수를 보고도 진행(fail-open) — 1차 `reset`(2026-09-25 발견)
- **증상** `logs/reset.log` 08:33:26Z 「계수(연구실 경계를 건 상태에서 센 값) … {'d3_dataset': 35, 'd3_file': 585, 'd6_project': 6, 'd4_lineage_edge': 22} · S3 uploads/ 1028 · previews/ 750」
  → 08:33:28–38Z 앱 4 단위 정지 → 08:33:39Z 두 체인 스키마 DROP·재생성 → `s3` 단계 08:34:12–17Z 1,778 키 삭제(실패 0).
  지운 것 = 07:40:53Z BYPASSRLS 백업의 4,189행(9-16 시드 28 ＋ 팀원 QA 계정 3 의 QA 자료) — 상세 RCA §0′.
- **근거** `stages.sh:186-191`(`stage_reset` ① 계수 뒤 `test -s …/count-before.json` 만) · `services/core-api/ops/reset_dev_environment.py:279-307`@`ea21d8c2aa54`(`_phase_count` 는 보고만 · 0 반환) ·
  `:310-355`(`_phase_schema` 의 거부 조건 = 스키마 집합 불일치뿐) · `:357-380`(S3 계획 = 접두사 전수 · DB 참조 대조 없음 · DROP 뒤에 돈다).
- **원인** 도구가 자기 계수를 기대값과 비교하지 않는다(fail-open). 전제 「dev 플랫폼 DB 0」(`wu0-precheck-2026-09-24.md` §2-1)은 앱 롤 `colab_app` 으로 연구실 경계 없이 센 거짓 0 이다
  (`.agents/rules/deploy.md` 10번 증보 〈363〉 재발). 게이트 ③ advisor 는 그 전제를 재측정하지 않고 go 했고, dev 상시 승인(11번 개정 〈398〉)은 비어 있음을 조건으로 두지 않는다.
  레인은 `nohup` 뒤 `tail -30`·좁은 grep 만 봐서 계수 줄을 보지 못했다. 같은 진행이 9-15(`a7d9a6d8`)·9-16(`d81e4387`)에도 있었다 — 세 번째.
- **임시 조치** 없음. 결정(2026-09-25) — dev 재시드는 reset 게이트를 고칠 때까지 동결 · QA 자료 복원하지 않음 · 백업 로컬 보존하지 않음.
- **왜 못 잡았나** preflight 11 · doctor 15 · 리허설 10 어느 것도 데이터 행을 BYPASSRLS 로 세지 않는다(리허설 ⑸ = S3 키 수 · ⑹ = `select 1`). 승인 기록의 게이트 넷은 대상 식별만 본다.
  selftest 에 비영 계수 픽스처가 없다. 07:40Z 백업 크기(219,290 B)를 빈 상태 기준치(20,983 B)와 비교하는 자리가 없다.
- **개선 후보** ⓐ `stage_reset` 이 DROP 직전 계수를 선언된 기대(빈 dev = 0)와 비교해 다르면 멈추고 사람 확인 ⓑ 계수는 BYPASSRLS 롤(`colab_backup`) 또는 API 로만 · `d1_account`·무소속 행 포함
  ⓒ S3 계획을 DROP 전에 DB 참조 키와 대조(#59) ⓓ preflight 에 최신 BYPASSRLS 백업의 크기·행 수 대 빈 상태 기준치 ⓔ 보고서가 `count-before` 값을 인용 ⓕ 관련 #49 ⑶(계수 0).

## 5. 개선 방향 후보 (판단은 intent 에서)

전제 — dev-reseed 의 산출물은 **검증용 데이터**(28건·계보 18·autometa)다. 화면 회귀 시험은 부산물이다.
autometa 는 업로드 분석 사건으로만 들어온다(`routes/ingestion.py:1004`@`b210ce5c` `apply_autometa` · intent Q1).

| 안 | 무엇 | 얻는 것 | 치르는 것 |
|---|---|---|---|
| A 화면 걷기 유지 ＋ 대상 sha 로컬 end-to-end 리허설 | 일회용 스택(대상 sha 이미지·번들·참조자료 실물)에서 seed 전 국면을 먼저 돈다 | #1~#5·#13·#15 계열을 dev 전에 잡는다. 러너 구조는 그대로 | 리허설에 수십 분·4.4 GB. 화면 개정마다 러너 수선은 계속 |
| B 제품 공개 API 로 업로드·등록 | 화면이 부르는 같은 API(`/uploads`→전송→분석→`/datasets`)를 러너가 직접 | DOM·click 판정(#5~#7) 소거. 계약(`fe-core.yaml`)이 변경을 알려준다 | 업로드 전송·대상 연구실 헤더·필수 칸을 러너가 재현. `load-seed.py` 직적재와 달리 분석 사건을 타는지 실측 필요 |
| C 혼합 | 대량은 B, 대표 표본 몇 건만 화면 걷기 | 속도와 화면 확인을 나눠 가진다 | 두 경로 유지 비용 |
| D 러너↔제품 계약 게이트 | `DatasetCreate.required`·`_CREATIONS`/`_CONTEXT`·러너가 쓰는 testid 를 계획·러너와 대조, 제품 PR 에서 red | 개정 시점(#1·#4)에 잡는다. A·B·C 어느 쪽과도 겹쳐 쓸 수 있다 | 대조 규칙 유지. 화면 동작(#2·#5)은 못 본다 |
| E 실행 자리·기록 분리 | 상태·run-dir·회차 기록을 체크아웃 밖·회차별로 | #8~#10 소거, 재개가 체크아웃과 무관 | 기존 `--from seed` 자리 규약 이전 |
| F 러너 판정 강화 | click 사후조건 · 상태 판독형 대기 · 파일럿 국면 · 신호 trap | #5~#7·#12 · 추기 #15·#17~#22 | 러너 코드량. 화면 구조에 더 묶인다(A 와 같은 비용) |

## 6. 관련 별건

- dev DB 가 2026-09-15 04:17~07:27Z 사이 비워진 사건 — ⚠ 정정: 재시드 `a7d9a6d8` 의 reset `DROP SCHEMA`(05:59Z · 06:05Z)다(인계서 §3-3 정정 · RCA §0′-4 · #26).
- dev 야간 백업 크론이 9-13 이후 죽어 있다 — EC2 `/etc/cron.d/colab-dev` 가 `infra/dev/install-cron.sh:49` 정본보다 옛 판(`wu2-prereq` §2-3).
- 백업 행수 급감 경보 권고(미착수) · 크론 드리프트 검사 없음(인계서 §5-5).
- `variables` 축은 재시드로 안 채워진다(0/28) — 후속 PATCH `d3_catalog.replace_variables`(`services/core-api/src/colab_core/domains/d3_catalog.py:827`).
- `--rehearse` 가 배포 0건인데 `--release-plan`·`HEAD = 후보 sha` 를 요구(`stages.sh:1053-1056` · `wu2-prereq` §3-3).
- `ship.sh` 가 `/opt/colab-repo` 를 갱신하지 않는데 재시드 도구는 거기를 본다(`wu2-prereq` 후속 ⑶).
- 3차가 dev 에 남긴 미등록 업로드(만료 24h · 보고 §32 ⒁). 4차 덤프의 「올리다 만 것이 있어요」 9줄과의 관계는 미확인.
- `stages.sh:667` 주석의 줄 인용(`session_token.py:59-61` · `login_sessions.py:192-206`)이 실제 자리(`:60-62` · `:193-194`·`:226-228`)와 어긋난다.

## 7. 제품 후속 후보 (미수선 · GitHub 이슈 등재 여부는 사용자)

file:line 은 `ea21d8c2aa54`. 정정 2026-09-25 — RCA §5 P1~P5 에 맞췄다(주장 검증 C1~C6 반영). 상세는 RCA §5.

| # | 결함 | 근거 | 관측 |
|---|---|---|---|
| P1 | GeoPackage 등 미지원 본체 상세 미리보기 막다른 길 — 미지원 문구(「이 형식은 아직 지도로 못 그려요.」)가 오류 틀(alert)로 뜨고 무익한 「다시 불러오기」가 붙는다 · 보기 영구 비활성. 필요 원인 둘(`describe()` 415 미매핑 ＋ describe catch) · 업로드 화면도 같다 | `datasetPreviewSource.ts:77-86` · `DatasetPreviewSection.tsx:179-181`·`:340`·`:355`·`:362-366` · 선행 `FMT-GPKG` | #21 |
| P2 | 경도 0–360 격자 미리보기 거절(`MAP_BOUNDS_IMPLAUSIBLE`) · 실패 시 썸네일·값 미리보기 소실 · 업로드 문구 「한반도 밖」 오안내 | `preview.py:261` → `check_bbox_4326` `:100-101` · `jobs.py:637`·`:668-673` · `gridFlow.ts:81-84` | #22 |
| P3 | 업로드 전체 파일 렌더 실패 뒤 배너 문구가 오류와 모순 — 미리보기 칸은 오류(`up-preview-error`)와 재시도 안내 ＋ 「미리보기 그리기」를 띄우는데 수렴 배너는 기본 문구에 고정 · 격자 「위치 확인」 불가. 조용한 정체는 아니다 | `UploadModal.tsx:558-572`·`:1493` · `PreviewPanel.tsx:268`·`:270-273`·`:580-584` · `gridFlow.ts:264` | #15 |
| P4 | relay 10s < viz describe 11.8~21.7s → 503 7건(13:58~14:40Z · 파일 143~145 묶음) | `services/core-api/src/colab_core/app/relay.py:28` · 선행 `PV-2` | #15 |
| P5 | viz-render `RENDER_TIMEOUT`(120s 하드코딩) — 원인은 바이트가 아니라 좌표·격자가 있는 약 143개 다중 파일(파일 수 × 파일별 좌표 처리). 637 MB(12 파일)는 시간 초과가 아니라 413 | `kernel/config.py:72` · `jobs.py:553-558` · `failures.py:49` | #15 |

- 제외 — 같은 파일 재선택이 설명을 비움(`DatasetPreviewSection.tsx:347-351`·`:169-183`·`:355`): 러너 전용(Chromium 키보드·팝업 경로 재현 · 팝업 항목 마우스 클릭·타 브라우저 미실측) · 파일이 2개 이상이면 다른 파일로 복구 · RCA §5 제외(#17).
- 제외 — 상세 화면 보기 단추가 접힌 선 아래(중심 y≈1630~2424 · 창 1280×577): 사용성 항목 · #125 와 겹친다 · RCA §5 제외(#18).

## 8. dev 현재 상태 (2026-09-25 · 오케스트레이터 읽기 전용 실측)

- 데이터셋 28 · 프로젝트 4 · 계보 간선 18(run-dir `counts.json` 과 일치) · 저장 기간 28/28 · 모델 입력 설명 2/2.
- autometa 행 28/28 · period_start 28 · grid 26 · crs 24.
- 형식 — NumPy 11 · GeoTIFF 7 · NetCDF 3 · Binary 2 · HDF4 2 · GRIB 1 · 미탐지 2(seq 13·14 GeoPackage).
- 상세 화면 미리보기 — 성립 25 · 미성립 1(16) · 판정불가 2(13·14)(`preview-judgment.tsv`). seq 18 은 상세 화면에서 성립(대표 파일 보기 3.0s) —
  업로드 전체 파일 렌더 실패(#15)와 별개.
- 계정 — 계정 최종화 미실행 — 재시드 재개 전 계정 설계 재검토 필요(#14).
- 재시드 — reset 게이트를 고칠 때까지 동결(2026-09-25 · #26 · RCA §0′-6).

## 9. 개선 항목 (intent 입력 · 판단하지 않는다)

1. **누름·측정의 사후조건** — #5 · #6 · #17 · #18 · #19 · #20. 누름 원시동작(초점·Enter) 통일 · 적중 검사 상시 · 캐시 전 사후조건 ·
   측정 기준을 슬롯으로.
2. **상태 판독형 대기 · 실패 표식 판독** — #7 · #15 · #21 · #22. 고정 상한 대신 성공·실패 표식 동시 판독 · 비활성 사유 · 서버 `failure` 코드.
3. **러너↔제품 계약 · 대상 sha 리허설** — #1 · #2 · #3 · #4 · #13. 계약 게이트 · 일회용 스택 end-to-end · 참조자료 필수 입력 ·
   리허설 창 크기를 dev 와 같게.
4. **계획 기대값의 출처·갱신** — #12 · #19 · #23. 기대 표식과 발생 조건 · 재측정·서명 · 측정 방법·sha 동반 · 포맷별 autometa 기대표.
5. **판정 결과의 보존** — #8 · #10 · #21. 판정불가 행도 읽은 값 보존 · 회차별 결과 · 로그 누적·줄 버퍼.
6. **실행 자리·기록 분리** — #8 · #9 · #11 · #24. 상태·기록은 체크아웃 밖·run-dir 에만 · env 감싸개 단일화 · 손 정리 제거.
7. **재시도·중단 규율** — #12 · #16. 무변경 재개 거절 · 시도 상한과 전략 기록을 도구가 강제 · 신호 trap · 판별 시험의 사전 중단 기준.
8. **가지·계정 운영** — #14 · #25. 회차 단위 가지 · 사람 계정과 시드 계정 분리.
9. **제품 결함 추적** — §7 P1~P5. 도구 개선과 별개 트랙 · 등재 여부는 사용자.
10. **reset fail-closed** — #26. DROP 직전 계수 ↔ 기대 대조 · BYPASSRLS/API 계수 · S3 계획의 DB 참조 대조 · 「비었다」 판정 경로 단일화.
