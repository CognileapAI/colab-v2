# WU2 — dev 재시드 실행 · **중단(seed 단계)**

메타 — intent `dev-package/intent/2026-09-24-corpus-expansion-dev-reseed.md` · 레인 lane-worker ·
기준 커밋 `0a794f5b` · 가지 `corpus-wu2-reseed` · 실행 2026-09-24T08:33:22~08:36:04Z ·
task `121276dea04b448e862c3f74bfdfcb39` · 실행 자리 `dev-package/reports/dev-reseed-runs/wu2-20260924T0834Z/`(추적 제외).

**한 줄** — `reset`~`prelude` 일곱 단계는 **전부 ok** 로 지났고, `seed` 가 **첫 프로젝트 생성에서
`대상 연구실을 선택해 주세요.` 로 멈췄다.** 원인은 자동 메타 경로가 아니라 **제품 권한 개정(`bafae4a7`)과
dev-seed 러너의 어긋남**이다. 데이터셋은 **0건 등재** 되었고 **업로드 0 B** — 3.64 GB 를 쌓기 전에 멈췄다.

⚠ **파일럿(첫 3건 자동 메타 확인)은 도달하지 못했다.** `seed` 가 `datasets` 국면에 들어가기 전
`projects` 국면에서 죽었다. 따라서 **자동 메타가 서는지 여부는 이 회차가 판정하지 않는다.**

---

## 1. 낸 명령 (패킷 §6 대조 후 그대로)

```bash
COLAB_RESEED_TARGET_REF=ea21d8c2aa54 \
bash dev-package/tools/dev-reseed/reseed.sh --from reset \
     --run-dir dev-package/reports/dev-reseed-runs/wu2-20260924T0834Z
```

- 접속값은 `~/.config/colab-platform/dev-operator.env` 에서 싣고 **`COLAB_DEV_SECRETS_DIR` 는 unset** 했다
  (`SKILL.md:101-106` · 감싸개 `dev-package/reports/dev-reseed-runs/wu2-env.sh` · 추적 제외).
- `--release-plan` 미지정(deploy 단계 없음). 배포 **0건** — 대상 ref = 실행 중 sha.

## 2. 진입 직전 재확인 (게이트 ③ 조건 재측정 · 08:33:07~08:33:11Z)

| 항목 | 값 | 판정 |
|---|---|---|
| preflight | **통과 11 · 미달 0** | 충족 |
| EC2 디스크 | **2,926,391,296 B**(2.73 GiB) | ≥ 2 GiB 충족 |
| 백업 신선도 | 07:40:53Z 산출물로부터 **52분** | 24h 이내 — 재백업 불요 |
| 대상 ref | `ea21d8c2aa54` ∈ `origin/develop` · 작업 트리 깨끗 | 충족 |

## 3. 단계 시간표 (실측)

| 단계 | 시작(UTC) | 종료 | exit | 소요 | 귀결 |
|---|---|---|---|---:|---|
| `preflight` | 08:33:22 | 08:33:26 | 0 | 4s | 11/11 |
| `reset` | 08:33:26 | 08:33:40 | 0 | 14s | 승인기록 기록 → 앱 4단위 정지(①′) → `DROP SCHEMA public, account_admin CASCADE`(platform) · `DROP SCHEMA public CASCADE`(ai) → S3 계획 적용 |
| `bootstrap` | 08:33:40 | 08:33:52 | 0 | 12s | 두 체인 `alembic upgrade` |
| `up` | 08:33:52 | 08:34:10 | 0 | 18s | 4단위 healthy · 전건 `storageMode/sourceMode = s3` · **doctor 15/15** |
| `s3` | 08:34:10 | 08:34:17 | 0 | 7s | 접두사 `uploads/`·`previews/` 만 |
| `prelude` | 08:34:17 | 08:34:22 | 0 | 5s | 연구실·교수·로그인 자격·**서비스 운영자 등록**(④) |
| `seed` | 08:34:22 | **08:36:04** | **1** | 102s | login ok · accounts 4/4 ok · **projects 0/4 실패** |
| `verify` | — | — | — | — | 미실행(skipped) |
| `report` | 08:36:04 | 08:36:04 | 0 | 0s | `result.json` 기록 |

- **총 소요 = 2분 42초**(162s). `outcome=failed` · `failedStage=seed` · `blocked 0` · `recovery 0`.
- `recovery` 가 비어 있다 = `reset` 이 정지 뒤 실패하지 않았으므로 **자동 재기동이 걸릴 일이 없었다.**
  앱은 `up` 이 정상 기동했고 지금도 healthy 다.

## 4. 멈춘 자리 — 축자

`dev-package/reports/dev-reseed-runs/wu2-20260924T0834Z/logs/seed.log` 마지막 세 줄 —

```
[17:36:01]   ! 대기 시간 초과(60s) 모달 닫힘
[17:36:04]   x 실패: 프로젝트 생성 실패(precipitation): 대상 연구실을 선택해 주세요.
2026-09-24T08:36:04Z ── 단계 seed 종료 code=1 102s
```

덤프 `dev-package/tools/dev-seed/.work/fail/project-precipitation-submit.txt` 의 모달 스냅샷에
러너가 **채우지 않은 칸**이 그대로 찍혀 있다 —

```
- combobox "대상 연구실" [expanded=false, ref=e39]: 연구실을 선택해 주세요
  - option "연구실을 선택해 주세요" [selected, ref=e44]
  - option "고려대학교 수문학연구실" [ref=e45]
```

## 5. 원인 — 제품 권한 개정과 러너의 어긋남 (실측 대조)

- 거절문의 출처 = **서버**다. `services/core-api/src/colab_core/app/target_scope.py:43`
  축자 `raise errors.bad_request("대상 연구실을 선택해 주세요.")`.
- 그 검사가 걸리는 조건 = **주체가 운영자(시스템 관리자)이고** 조작이 아래 넷 중 하나인데
  `X-CoLAB-Target-Lab` 헤더가 없을 때다. `target_scope.py:8` 축자 —
  `_CREATIONS = {"createProject", "createUpload", "initiateUploadTransfer", "createDataset"}`.
- **왜 이번에 걸렸나** — `prelude` ④(`stages.sh:485`)가 교수 계정을
  `provision-service-operator.sql` 로 **서비스 운영자로 등록**한다. 그 상태로 러너가 화면을 걷는다.
  화면 쪽 분기도 같은 조건이다 — `frontend/src/components/project/ProjectFormModal.tsx:86` 축자
  `if (!editing && account?.canManageServiceAccounts && !targetLabId) { setError('대상 연구실을 선택해 주세요.'); return; }`.
- **언제 갈렸나** — `target_scope.py` 는 커밋 `bafae4a7`(「시스템 관리자 전체 권한과 교수 관리자 연구실 범위 분리」)가 낳았다.
  `git cat-file -e` 실측 — **DR-4 가 28/28 을 세운 sha `d5cd6ca9` 에는 그 파일이 없고**,
  오늘 dev 가 도는 `ea21d8c2aa54` 에는 있다. ⇒ DR-4 이후 제품이 바뀌었고 **dev-seed 러너가 따라가지 않았다.**
- 러너의 프로젝트 생성부(`dev-package/tools/dev-seed/runner.py:880-887`)가 채우는 칸은
  **이름·설명·유형 셋뿐**이고 연구실 칸이 없다. 같은 러너의 계정 생성부는 이미
  `select_by_label(css, …, "연구실")`(`runner.py:1646`)을 쓴다 — **연구실을 고르는 수단은 있으나 프로젝트 경로에 안 붙어 있다.**

### 5-1 ⛔ 이 결함은 프로젝트 한 칸으로 끝나지 않는다

`_CREATIONS` 의 넷이 전부 같은 헤더를 요구한다. 프론트가 그 헤더를 **호출 자리마다 따로** 싣는다(전역 전환기가 아니다) —
`frontend/src/components/upload/uploadSource.ts:144`(`POST /uploads`) · 같은 파일 `:178`(`POST /datasets`) ·
`frontend/src/components/project/projectSource.ts` · `transferSource.ts`.
⇒ 러너를 고치려면 **프로젝트 모달 · 업로드 · 데이터셋 등록 세 자리**에서 각각 연구실을 골라야 한다.
「한 줄 추가」로 끝나는 수선이 아니다. **그래서 이 레인은 고치지 않고 멈춘다.**

### 5-2 어느 검사에 걸리나

- 제품 쪽은 **걸린다** — `services/core-api/tests/test_operator_designation.py:154` 가
  `"대상 연구실 미선택 거부"` 를 축자로 시험한다. 즉 서버 동작은 의도된 것이고 결함이 아니다.
- **dev-seed 러너의 어긋남은 어느 게이트에도 걸리지 않는다.**
  `gates/run.sh dev-reseed-selftest` 는 dev 무접촉이고 `reseed.sh` 의 요약줄·복귀·리허설만 잰다 —
  `runner.py` 가 **현재 제품 화면과 맞는지**를 보는 검사는 없다. 리허설 원시동작 10 에도 그 항목이 없다
  (⑻ `runner_phase_report` 는 계획 보고만 읽는다). **후속 항목 ⑴.**

## 6. 재시드 뒤 dev 실물 계수 (읽기 전용 · BYPASSRLS `colab_backup` 롤)

⚠ **소유자 롤로 세면 안 된다** — `platform-owner-db.url` 로 센 첫 시도는 `d1_account` 를 **0** 으로 돌려줬다.
FORCE RLS 아래라 소유자에게 행이 가려진다(`infra/dev/backup.sh` 머리말과 같은 사유).
아래 표는 **`backup-platform-db.url`·`backup-ai-db.url`** 로 다시 센 값이다.

| 대상 | 값 | 기대(완주 시) | 읽기 |
|---|---:|---:|---|
| `d3_dataset` | **0** | 28 | seed 가 datasets 국면에 못 들어갔다 |
| `d6_project` | **0** | 4 | 첫 건에서 멈췄다 |
| `d4_lineage_edge` | **0** | 18 | 미도달 |
| `d3_dataset_autometa` | **0** | (측정 대상) | **미판정** — 이 회차는 자동 메타를 재지 못했다 |
| `d5_upload` | **0** | — | **업로드 0 B** · S3 에 쌓은 것 없음 |
| `d1_account` | **5** | 5 | 교수 1 ＋ 무소속 운영자 4 — **계정은 전건 섰다** |
| `d1_lab` | **1** | 1 | `00000000000000000000HYMETS` 고려대학교 수문학연구실 |
| `alembic_version_platform` | **`0033_admin_body_access`** | 같음 | ㉮ 트리 갱신분과 일치 |

계정 5건(실측) — `cjun@korea.ac.kr`(전창현) · `joddanddan@gmail.com` · `samtae0610@gmail.com` ·
`ttlhi10@gmail.com`(Ted) · `wlsghd90@gmail.com`. **비밀번호는 어디에도 적지 않는다.**

### 6-1 AI 사전 — **예측 손실 0 이 실측으로 확정됐다**

| 표 | WU0 재시드 전 | **재시드 후 실측** | 차 |
|---|---:|---:|---:|
| `d9_concept` | 49 | **49** | 0 |
| `d9_concept_edge` | 19 | **19** | 0 |
| `d9_method_term` | 13 | **13** | 0 |
| `d9_place_alias` | 4 | **4** | 0 |
| `d9_topic_synonym` | 18 | **18** | 0 |
| **계** | **103** | **103** | **0** |

- `alembic_version_ai` = **`0007_merge_vocab_and_category`** — WU2 선행 패킷 §4 의 예측과 같다.
  `k3-resume` 의 `0008` 은 대상 ref 에 없으므로 서지 않는다(**잃은 것이 아니라 생기지 않은 것**).
- ⇒ 「마이그레이션이 사전을 되세운다」는 문장이 **이번에 처음 실제 DROP 을 거쳐 확인됐다.**

## 7. 이상·복구·컨테이너

- **복구 동작 0건** — `result.json` 의 `recovery` 가 빈 배열. `reset` 이 성공했으므로 자동 재기동 경로는 타지 않았다.
- **앱 4단위는 지금 healthy** 다 — `up` 단계가 기동했고 `deploy_doctor` 가 **`항목 15 — ✓ 15 · ✗ 0 · ─ 0`**(한 번의 실행).
  ⇒ **dev 는 지금 「빈 채로 정상 가동」** 이다. 사람이 올리러 들어갈 일이 없다.
- `previewJudgment[]` = **0건**(verify 미실행). 「판정불가」 0 은 **통과를 뜻하지 않는다** — 재지 않았다.
- `counts` = `{}`(verify 미실행).
- **DR-4 가 남긴 도구 결함 2건**(`project_index()` 앵커 의존 · 미리보기 순회기 대기 조건)은
  **이번에 발현하지 않았다** — 둘 다 그 자리에 닿기 전에 멈췄다. 여전히 미수선이다.

## 8. 되돌릴 수 있나 / 지금 무엇을 잃었나

- `reset` 은 **이미 돌았고 되돌릴 수 없다.** 다만 **잃은 자료는 없다** —
  intent 가 적은 대로 재시드 전 dev 플랫폼은 이미 **데이터셋 0 · 계정 0** 이었고,
  AI 사전 103행은 §6-1 대로 **전건 복원**됐다.
- 07:40:53Z 백업 2건(`_ops/backups/dev/2026-09-24T074053Z-*.sql.gz`)은 그대로 있다.
- ⇒ **이 중단의 실질 손실 = 0.** 얻은 것은 「러너가 현재 제품과 맞지 않는다」는 **실측 사실**이다.

## 9. 다음 걸음 — WU3 은 지금 열 수 없다

- **WU3(스냅샷 재포획)의 입력은 dev 의 28건 실물이다. 지금 0건이므로 WU3 은 시작 조건 미충족이다.**
  `recapture_snapshot.py` 의 이름 충돌 검사(㉰)는 **0건일 때 78 로 떨어지는 것이 정상 동작**이다.
- 먼저 닫아야 할 것 = **러너를 `ea21d8c2aa54` 의 권한 모델에 맞추는 일**(아래 둘 중 하나). **이 레인은 판단만 적고 고르지 않는다.**

| 안 | 내용 | 크기 | 위험 |
|---|---|---|---|
| **㈎ 러너에 연구실 선택을 붙인다** | 프로젝트 모달·업로드·데이터셋 등록 **세 자리**에 `select_by_label(…, "대상 연구실")` 추가 | 중 — 화면 세 곳 · 지목점 3개 신설 | 화면 구조에 다시 묶인다. 같은 개정이 또 오면 또 깨진다 |
| **㈏ `accounts` 국면이 끝나면 임시 운영자를 내린다** | `prelude` ④ 로 준 운영자 자격을 `projects` 시작 전에 해제 → `canManageServiceAccounts=false` → 헤더 요구가 사라지고 DR-4 와 같은 단일 연구실 경로로 돈다 | 소 — 순서 변경 | ⚠ **미검증 가설.** 교수 권한만으로 28건 등재·계보 연결이 전부 되는지 확인해야 한다. `stages.sh:606` 이 이미 「검증 후 임시 운영자 해제」를 하므로 **해제 시점을 앞당기는 것**에 가깝다 |

- 어느 쪽이든 **고친 뒤에는 `--from seed` 로 같은 실행 자리에서 잇는다**(DR-4 §9 · 러너가 완료분을 건너뛴다).
  `reset`~`prelude` 를 다시 돌릴 이유가 없다 — **재시드를 처음부터 다시 낼 필요는 없다.**
- ⛔ **이번 회차에서 `--from seed` 재개를 시도하지 않았다.** 거절은 서버가 내는 **결정적** 판정이라
  고치지 않은 재개는 같은 자리에서 똑같이 죽는다. 「같은 단계 2회 실패」를 만들지 않으려고 멈췄다.

### 9-1 WU3 이 실제로 실을 것 (조건 충족 시 그대로 쓰라고 남긴다)

데이터셋 1건당 — `id` · `name` · `processing_level`(Q3 · 이름의 「(Lv.n)」 이 아니라 **열로**) ·
`lab_id` · `project` 연결 · `autometa` 5축(`format`·`crs`·`grid`·`period_start/end`·`variables`) ·
`variable_rows`(`d3_dataset_variable` 행 표 — 배열 사본이 아니다) · `files`(본체와 `grid_files` 역할 구분) ·
`parents`(`parent_dataset_id` · `role` `주입력`/`보조입력` · `method` 유무).
출력 규율 = `id` 오름차순 · 파일·부모 고정 순서 · `indent=2` · `ensure_ascii=False` · **쓰기 SQL 0줄**.

## 10. 후속 항목

| # | 무엇 | 어느 검사에 걸리나 |
|---|---|---|
| ⑴ | dev-seed 러너가 현재 제품 화면·권한 모델과 맞는지 보는 검사가 없다. `bafae4a7` 이후 프로젝트·업로드·데이터셋 생성이 전부 `X-CoLAB-Target-Lab` 을 요구하게 됐는데 러너는 모른다 | **어디에도 없다.** `dev-reseed-selftest` 는 dev 무접촉이고 러너 화면 경로를 밟지 않는다 |
| ⑵ | (선행 패킷 ⑴ 승계) EC2 `/etc/cron.d/colab-dev` 가 옛 판이라 야간 백업이 죽어 있다 | `deploy_doctor` ⑭ 가 증상만 24h 뒤에 |
| ⑶ | (선행 패킷 ⑵ 승계) `--rehearse` 가 배포 0건인데 `--release-plan` 과 HEAD 고정을 요구한다 | 없다 |
| ⑷ | (선행 패킷 ⑶ 승계) `ship.sh` 가 `/opt/colab-repo` 를 갱신하지 않는데 재시드 도구는 거기를 본다 | `deploy_doctor` ⑥ 이 증상만 |
| ⑸ | 소유자 롤로 계수를 세면 FORCE RLS 때문에 **0 으로 보인다**(§6). 검증 SQL 이 롤을 틀리면 조용히 거짓 0 을 낸다 | 없다 — 이번엔 사람이 알아챘다 |

## 11. 파일

| 자리 | 무엇 |
|---|---|
| `dev-package/reports/corpus-expansion/wu2-reseed-2026-09-24.md` | 이 문서 |
| `dev-package/reports/dev-reseed-runs/wu2-20260924T0834Z/` | 실행 자리 — `result.json` · `logs/`(추적 제외) |
| `dev-package/reports/dev-reseed-runs/wu2-20260924T0834Z/logs/seed.log` | 멈춘 자리 축자 |
| `dev-package/tools/dev-seed/.work/fail/project-precipitation-submit.{png,txt}` | 모달 덤프 — 빈 「대상 연구실」 칸 (추적 제외) |
| `dev-package/sessions/DR-4-run-20260924T083322Z.md` | 러너가 낸 회차 기록 뼈대(바꾸는 단계가 실제로 돌았다) |
| `_ops/backups/dev/2026-09-24T074053Z-*.sql.gz`(S3) | 되돌림 재료 — 그대로 있다 |

---

# 재개 (㈏) — 2026-09-24T09:12~09:14Z · **중단(`projects` · 다른 자리)**

메타 — 기준 커밋 `54c3e981` · 가지 `corpus-wu2-resume` · task `68fb46bb1ea642d391a61e1c8674e92d` ·
실행 자리는 중단 회차와 **같다**(`…/wu2-20260924T0834Z/`) · 러너 상태(`.work`)도 그대로 이었다.

**한 줄** — ㈏ 의 전제는 **실측으로 맞다**(「대상 연구실을 선택해 주세요.」는 사라졌다).
그러나 자격을 내리면 **열린 세션이 반드시 닫힌다**는 제품 설계를 옵션표가 몰랐고,
`projects` 가 프로젝트 목록 대신 **로그인 화면**을 봤다. 데이터셋 **0건 등재 · 업로드 0 B**.

## 12. Step 1 — ㈏ 는 유효하다 (코드 실측)

- 헤더 요구는 **운영자에게만** 선다 — `services/core-api/src/colab_core/app/target_scope.py:29-34`
  (`if not subject.operator: … return`) · 요구 자체는 `:42-43`. 교수는 요구가 없을 뿐 아니라
  헤더를 실으면 `:33` 이 거절한다(`대상 연구실 선택은 시스템 관리자만 할 수 있어요.`).
- 화면도 같은 **한 조건**으로 갈린다 — 선택기 노출·제출 차단·헤더 적재가 전부
  `account.canManageServiceAccounts` 다: `frontend/src/components/project/ProjectFormModal.tsx:86·98·118` ·
  `frontend/src/components/common/TargetLabSelect.tsx:4-24` · `frontend/src/components/upload/UploadModal.tsx:176`.
- 교수 역할은 네 스위치가 전부 켜진다 — `services/core-api/src/colab_core/domains/d2_access.py:132-133`.
  `createProject` 는 `프로젝트 생성` 스위치만 본다(`app/routes/project.py:134-138`).
- 운영자 자격이 **실제로 필요한 국면은 `accounts` 하나**다(무소속 운영자 4명 생성).
- DR-4 대조 — `git show d5cd6ca9:dev-package/tools/dev-reseed/stages.sh` 의 `:506` 에 prelude ④ 가
  **이미 있었다.** 즉 DR-4 의 28/28 도 **교수 계정이 임시 운영자인 채로** 만든 것이고,
  달라진 것은 `target_scope.py` 가 생겼다는 사실 하나다.

### 12-1 옵션표가 적지 않았던 결합 셋

| # | 무엇 | 근거 |
|---|---|---|
| ⑴ | 제품 API `set_operator` 로 내리면 **세션을 끊는다** | `kernel/db_credentials.py:260-264` |
| ⑵ | 그런데 **행만 빼도 세션은 닫힌다** — 토큰이 발급 시점의 운영자 여부를 주장으로 싣고, 세션 확인이 `service_operator` 를 매 요청 재조회해 **어긋나면 거절**한다 | `kernel/session_token.py:59-61` · `kernel/login_sessions.py:192-206` |
| ⑶ | `accounts.py` 최종화는 교수 비밀번호를 초기값으로 되돌릴 **자격의 표식**으로 운영자 행을 읽는다. 일찍 내리면 「final professor credential drift; refusing another reset」 | `dev-package/tools/dev-reseed/accounts.py:57-62` |

⇒ ㈏ 는 「순서 변경」이 아니라 **해제 ＋ 재로그인 ＋ 최종화 직전 복원** 셋이다.

## 13. 변경

- `dev-package/tools/dev-reseed/stages.sh`
  - `operator_grant()`·`operator_revoke()` 신설. prelude ④ 는 `operator_grant "prelude:operator"` 호출로 바뀌었다(문장은 한 벌).
  - `stage_seed` 순서 = login → accounts ×4 → check-created → **③ 해제** → **④ 재로그인** → projects → datasets → verify → report.
  - `account_finalize()` 머리에 `operator_grant "verify:operator-grant"`(멱등 · `ON CONFLICT DO NOTHING`). 최종화가 마지막에 다시 내리므로 **끝 상태는 종전과 같다.**
  - 해제 SQL 은 대상 ref 의 트리에 없으므로 base64 로 실어 `psql -1 -f -` 에 넣는다. 한 트랜잭션에 판정 둘 — 행이 남으면 거절 · **운영자가 한 명도 남지 않으면 거절**(제품의 「마지막 관리자는 해제할 수 없다」와 같은 불변식).
- 검사 — `dev-package/tools/dev-reseed/tests/operator-window.sh` 신설, `gates/tools/dev-reseed-selftest.sh` 의 픽스처가 7 → **8**.
  `preflight-red.sh:368` 은 argv 만 보는 자리라 `ssh_script` 도 대역으로 가렸다.
- 해제 SQL 실측(일회용 postgres · dev 무접촉) — ⓐ 마지막 한 명이면 **거절 ＋ 롤백**(행 1 그대로) ⓑ 운영자 4명 남으면 `DELETE 1` ⓒ 재실행은 `DELETE 0`(멱등).

## 14. 재개 실행 — 두 시도

| 시도 | 시각(UTC) | 어디까지 | 귀결 |
|---|---|---|---|
| 1 | 09:12:47~09:12:50 | preflight | **미달 1 = `git`(작업 트리 4건)** — `seed` 미진입. 게이트가 fail-closed 로 막은 것이라 재시드 시도로 세지 않는다. 커밋 후 재시도 |
| 2 | 09:13:42~09:14:04 | preflight **11/11** → seed 18초 | login 건너뜀 · accounts 4건 「이미 생성됨」 · check-created ok · **③ 해제 `DELETE 1`** · **`projects` 실패** |

멈춘 자리 축자 — `logs/seed.log` ·
`x 실패: 지목점 project-new-button 해소 실패. 마지막 사유 = ('text', '새 프로젝트') -> Element not found.`

덤프 `dev-package/tools/dev-seed/.work/fail/projects-fail.txt` 의 스냅샷이 원인을 그대로 보인다 —
URL 은 `/projects` 인데 화면은 **로그인**이다:

```
- heading "로그인" [level=1, ref=e3]
- textbox "이메일" [ref=e4]
- textbox "비밀번호" [ref=e5]
- button "들어가기" [disabled, ref=e6]
```

- ⇒ **「대상 연구실을 선택해 주세요.」는 한 번도 나오지 않았다.** ㈏ 의 전제는 맞았다.
- 막은 것은 §12-1 ⑵ 다. 이 회차가 그 사실을 **실측으로** 얻었다.
- 수선(해제 뒤 `--phase login` 한 줄)은 코드에 넣고 픽스처로 못 박았으나
  ⛔ **실물로 검증하지 않았다** — 지시의 「재개는 1회」를 지켜 세 번째 실행을 내지 않았다.

## 15. 파일럿 — **미도달**

`projects` 에서 멈춰 데이터셋을 한 건도 올리지 않았다. 자동 메타가 서는지는 **이 회차도 판정하지 않는다.**

## 16. 재개 뒤 dev 실물 계수 (읽기 전용 · BYPASSRLS `backup-platform-db.url`)

| 대상 | 값 | 기대(완주 시) |
|---|---:|---:|
| `d3_dataset` | **0** | 28 |
| `d6_project` | **0** | 4 |
| `d4_lineage_edge` | **0** | 18 |
| `d3_dataset_autometa` | **0**(행 0 · 축별 전부 0) | 측정 대상 |
| `d5_upload` | **0** | — |

- dev 는 **비어 있고 정상 가동** 상태 그대로다. 사람이 올리러 들어갈 일이 없다.
- 교수 계정은 지금 **평범한 교수**다(임시 운영자 해제됨 · 무소속 운영자 4명은 유지).
  다음 `--from seed` 는 해제가 멱등(`DELETE 0`)이라 그대로 잇는다.
- ⚠ 회차 기록의 `deploy_doctor` 15/15 는 **08:34Z `up` 단계의 값**이 그대로 실린 것이다.
  **이 회차는 doctor 를 다시 재지 않았다.**
- 소요 — 시도 1 = 3초 · 시도 2 = 22초(preflight 4 · seed 18). 배포 0건 · 복구 동작 0건.

## 17. 게이트

`COLAB_GATE_REPORT_DIR=dev-package/reports/corpus-expansion/wu2-resume COLAB_REF_ROOT=<참조자료> bash gates/run.sh task`
— **green 2 · red(판정) 0 · red(준비) 0**.
`dev-reseed-selftest — 픽스처 8 · 통과 8 · 결함 0 · 판정 못 함 0` ·
`seed-plan-drift green — datasets 28 edges 18 data_bytes 3641736593`.

## 18. 후속 항목 (승계분에 더함)

| # | 무엇 | 어느 검사에 걸리나 |
|---|---|---|
| ⑹ | 해제 뒤 재로그인이 **실물로 검증되지 않았다.** 픽스처는 순서만 보고, 세션이 실제로 다시 서는지는 dev 를 한 번 더 돌아야 안다 | `operator-window` 는 순서만 판정한다 |
| ⑺ | `blocked.jsonl` 이 실행 자리에 **누적**된다 — 재개 회차의 회차 기록이 앞 시도의 차단(`preflight:git`)을 자기 것으로 싣는다 | 없다 |
| ⑻ | 재개 회차의 단계표가 앞 회차의 `reset`~`prelude` 와 이번 `preflight`·`seed` 를 **한 표에 섞는다**(시각 구분 없음) | 없다 |

## 19. 파일 (재개분)

| 자리 | 무엇 |
|---|---|
| `dev-package/tools/dev-reseed/stages.sh` | `operator_grant`·`operator_revoke`·`stage_seed` ③④·`account_finalize` |
| `dev-package/tools/dev-reseed/tests/operator-window.sh` | 국면 순서·해제 SQL 픽스처(신설) |
| `gates/tools/dev-reseed-selftest.sh` | 픽스처 목록 ⓖ 추가 |
| `dev-package/tools/dev-reseed/tests/preflight-red.sh` | argv 검사 자리의 `ssh_script` 대역 |
| `dev-package/sessions/DR-4-run-20260924T091342Z.md` | 러너가 낸 재개 회차 기록 |
| `dev-package/reports/dev-reseed-runs/wu2-resume-*.sh`·`wu2-resume-console.log` | 재개 감싸개·파일럿 SQL·콘솔 기록(추적 제외) |
| `…/wu2-20260924T0834Z/logs/seed.log` · `.work/fail/projects-fail.{png,txt}` | 멈춘 자리 축자·화면 덤프(추적 제외) |
