# stage 2 개발 착수 준비도 실측 (2026-08-30)

> 조사만 한 회차다 — 코드·계약·대장·범위 **무접촉**. 커밋 0건.
> 계수 기준 = `dev-package/work-items.yaml`(항목 상태의 유일한 원본) · 게이트 = `bash gates/run.sh all` 1회 실행 실측.
> 총 항목 **96** — `stage1` 43 · **`stage2` 33** · `after_stage2` 15 · `out_of_scope` 5.

---

## 1. stage 2 항목 전량 (33건)

- 판정 기준 — `status`·`depends_on` 은 대장 축자, `완료조건`은 `completion_def` 가 「완료 정의 미작성」이 아니면 **있음**.
- 선행 미충족(`미충족`)은 `depends_on` 중 `status != done` 인 것만 적었다.

| id | 제목 | status | depends_on | 미충족 | 완료조건 |
|---|---|---|---|---|---|
| `IS4` | terraform state 보관 | partial | `IS2` | — | 있음 |
| `I3` | 배포 자동화 | partial | `I2` | — | 있음 |
| `I4` | 운영 준비 (추적·알람·복구 리허설) | open | `I3` | `I3` | 있음 |
| `PV-1` | 미리보기 뒷단 (헤더 파싱·좌표계 통일·지도용 영상 변환) | open | `D5` | `D5` | 있음 |
| `X-1` | 슈퍼유저 `POSTGRES_PASSWORD` 회전 | done | `X-4`,`R-1`,`#22` | `R-1` | 있음 |
| `X-2` | 계약 동결 해제 프로토콜 명문화 | done | — | — | 있음 |
| `X-3` | 프로젝트↔데이터셋 실연결 | done | — | — | **없음** |
| `X-4` | `_FILE` 배선 재기동 실전 검증 | done | `R-1` | `R-1` | 있음 |
| `X-5` | ㉰-4 소급 전수확인 | done | — | — | 있음 |
| `X-6` | 별칭 재부착 실패 삼킴 | done | — | — | 있음 |
| `R-1` | 복원 절차 | partial | — | — | 있음 |
| `V-1` | 팔레트 선택 재렌더 | open | `P3` | `P3` | **없음** |
| `V-2` | 값 조회 | open | `P3` | `P3` | **없음** |
| `F-1` | S-08 화면 (미등록 미리보기 화면) | in_progress | — | — | 있음 |
| `F-2` | 카탈로그 키워드 입력 제거 | open | — | — | 있음 |
| `J-1` | 편의 기능 묶음 (유효 9건) | open | — | — | **없음**(범위만) |
| `Y-1` | 자동 무효화 | open | `P3`,`D5` | `P3`,`D5` | 있음 |
| `CT-1` | 카탈로그 표 스펙 세목 | open | `P1`,`P6` | `P6` | 있음 |
| `P3` | 계보 그래프·2D 시각화 3종·`createScreenshot` | open | `P2`,`D5` | `D5` | 있음 |
| `P6` | 승인 처리 | open | `P1`,`P2`,`P3`,`P5` | `P3`,`P5` | 있음 |
| `P7` | 연구실 대시보드 | open | `P6` | `P6` | 있음 |
| `P8` | E-01 적용 지점 표 | open | `P7` | `P7` | 있음 |
| `PA-G` | 구글 IdP 어댑터 | open | `PA` | — | 있음 |
| `S3` | 실데이터 4종 E2E | open | `S2`,`P3` | `P3` | 있음 |
| `S3-e2e-4종` | 실데이터 4종 E2E 완주 | open | `S3` | `S3` | 있음 |
| `#22` | D2·D8 고아 행 삭제 | done | `R-1` | `R-1` | 있음 |
| `전범위백업` | 전범위 백업 (platform ＋ ai ＋ 볼륨) | done | `R-1` | `R-1` | 있음 |
| `2단-A` | A 동결해제 프로토콜 | done | — | — | **없음** |
| `2단-E` | E main 병합 | done | — | — | **없음** |
| `2단-계보간선-원자료→월평균` | 계보 간선 확정 | done | — | — | 있음 |
| `2단-격자전용-워커제외` | 격자 전용 업로드 워커 제외 | done | — | — | 있음 |
| `2단-격자전용-실패3건-처분` | 실패 3건 처분 | **deferred** | — | — | 있음 |
| `W-1` | 항목 상태 대장·검사기 | done | — | — | 있음 |

- **잔여(닫히지 않은 것) = 19건** — `done` 13 ＋ `deferred` 1 을 뺀 값.
- ⚠ `done` 인데 선행이 `partial`(`R-1`)인 항목 4건(`X-1`·`X-4`·`#22`·`전범위백업`) — 대장의 실물이고 고치지 않았다.

### 1-b. stage 2 밖으로 옮겨진 것 (별도 계수)

- **`after_stage2`(＝ stage 3) 15건** — `R2` · `C1` · `C2` · `C3` · `C4` · `C3·C4` · `G1b` · `DP-1` · **`T-1`**(형태소 검색 설정 · 2026-08-30 이동) · **`P4`**(검색 화면) · **`K3`**(계보 제안) · **`K4`**(자연어 검색) · `K5` · `LV-3` · `LV-4`.
- **`out_of_scope` 5건** — `I0`(prod 보류) · `I1` · `I5` · `G6` · `VZ-1`.

---

## 2. 착수 가능 vs 차단 (잔여 19건)

### 착수 가능 — **7건**

- `IS4`(partial) — terraform state 보관. 선행 `IS2` done.
- `I3`(partial) — 배포 자동화. 잔여 = 실배포 red 로그 부재 · 스크립트 롤백 경로 0건.
- `R-1`(partial) — 복원 절차. 잔여 = `:i2` 태그 이동 이력 기구 부재.
- `F-1`(in_progress) — S-08 화면. 별도 워크트리에서 구현 중.
- `F-2`(open) — 카탈로그 키워드 입력 제거. 선행 `[]`.
- `J-1`(open) — 편의 기능 묶음 9건. 선행 `[]`.
- `PA-G`(open) — 구글 IdP 어댑터. 선행 `PA` done.

### 차단 — **12건** (사유 유형 3종)

- **유형 ①: stage 1 미완 항목이 선행 — 5건**
  - `PV-1` ← `D5`(partial) · `P3` ← `D5`(partial) · `Y-1` ← `P3`+`D5` · `P6` ← `P3`+`P5`(partial) · (`S3` ← `P3`).
  - ⚠ **차단의 뿌리는 stage 2 내부가 아니라 `D5`·`P5` 두 stage 1 항목이다.**
- **유형 ②: stage 2 내부 사슬 — 6건**
  - `I4` ← `I3` · `V-1`/`V-2` ← `P3` · `CT-1` ← `P6` · `P7` ← `P6` · `P8` ← `P7` · `S3-e2e-4종` ← `S3`.
- **유형 ③: 판정 대기가 실질 차단 — 위 항목에 겹침**
  - `CT-1`(정본 침묵 3건) · `F-2`(대체 문구 판정) · `P6`(`Verified` 갈림) — §4 참조.

---

## 3. 게이트 실측 (`bash gates/run.sh all` · 2026-08-30)

- **계 = green 29 / red(판정) 0 / red(준비) 4** · 실행기 종료코드 1.
- **판정 red 는 0건이다** — 지금 red 인 넷은 전부 **입력이 선언되지 않아 검사 자체를 못 돈 것**(`exit 78`)이고, 이전 회차 기록과 **동일**하다.
- ⚠ `--` 넷 다 「검사 대상이 한 건도 판정되지 않았다」 — 못 돈 것은 통과가 아니다(`CLAUDE.md §4`).

| 게이트 | exit | 원인 유형 | 무엇을 선언·설정하면 풀리는가 |
|---|---|---|---|
| `schema-diff` | 78 | 입력미선언 | `COLAB_APPLIED_DB_URL_PLATFORM`·`COLAB_APPLIED_DB_URL_AI` 를 **둘 다** 지정. 체인마다 DB 를 세우고 `db/<체인>/versions` 를 alembic `upgrade head` 한 뒤 그 URL 을 넘긴다. 구 변수 `COLAB_APPLIED_DB_URL` 만 있으면 여전히 red |
| `autometa-loss` | 78 | 입력미선언 | `COLAB_APPLIED_DB_URL_PLATFORM` 1개. ＋ **대상 0건도 red** 이므로 워커 `COLAB_WORKER_STAGE2` 선언 상태에서 업로드 1건의 사건 발행이 있어야 판정으로 넘어간다. 면제는 `gates/config/autometa-loss.toml` 의 `datasets` 에 **이름으로** (현재 `[]` = 면제 없음) |
| `preview-tile-slot` | 78 | 입력미선언 | `COLAB_APPLIED_DB_URL_PLATFORM` ＋ **`COLAB_PREVIEW_TILE_DIR`**(실재하는 디렉터리여야 한다 — 첫 결손에서 멈추므로 DB 를 채운 뒤 이 값이 다음 red 로 드러난다). ＋ 워커 `COLAB_WORKER_STAGE2=on`·`COLAB_WORKER_PREVIEW_DIR`. 면제는 `gates/config/preview-tile-slot.toml` 의 `files` 에 **파일 이름으로** (현재 `[]`) |
| `e2e-format-coverage` | 78 | 환경대기(원천 미마운트) | `COLAB_REFERENCE_DATA` 가 원천 데이터 디렉터리를 가리키게 한다. ＋ `services/viz-render` 시험에 **포맷 표식이 붙은 케이스**가 포맷별 최소 1건 — 필수 목록 정본 = `gates/config/e2e-format-coverage.toml` `formats = [GeoTIFF, NetCDF, Binary, HDF4, NumPy]` **다섯**. 표식 케이스 0건도 red |

- ⭑ **넷 중 셋이 같은 열쇠 하나(적용 DB URL)로 열린다** — 적용 DB 를 게이트 실행 환경에 세우는 것이 stage 2 착수 준비의 단일 최대 레버다.
- ⭑ **`e2e-format-coverage` 는 `S3` 를 닫지 않는다** — 계보 확정과 staging 배포 green 은 여기서 재지 않는다.
- 참고 — `work-item-consistency` **green**(불일치 0) · `stage2-markers` **green** · selftest 13종 전부 green.

---

## 4. 판정 대기 (pending judgment) 큐

- stage 2 에 직접 걸리는 것 **5건**, stage 3 으로 넘어간 뒤에도 남는 것 2건.

| 항목 | Ted 가 판정할 것 |
|---|---|
| `CT-1` ㈎ | 승인 처리 도착 전 `Verified` 조건 열의 **정직한 상태 문구** — 값을 채울 기능이 이번 릴리즈에 없어 참이 영영 안 되는 자리를 무엇으로 표시할 것인가 |
| `CT-1` ㈏ | 카탈로그 행 `Verified` 칸에 **어떤 글자**를 쓸 것인가 (정본이 계보 열 네 표기는 못 박고 이 칸은 침묵) |
| `F-2` | 검색 화면 없는 릴리즈에서 **카탈로그 하단 상호 안내와 0건 안내를 무엇으로 대체**하는가 (한 줄 판정) |
| `P6` | `Verified` 갈림 — E-06 적용 지점 표 7행 중 **stage 2 몫이 어디까지인가** |
| `F-1` | S-08 화면의 **권한·노출 규칙** — 정본에 이 화면의 컨트롤 행을 세우거나 화면 전용 규칙을 판정 |
| `P4`(stage 3) | 「우선 정렬」의 소유가 `P6` 인데 `P6` 이 `P4` 를 선행으로 건다 — **투입 시점** |
| `T-1`(stage 3) | 완료 정의 잔여가 ⓓ 뿐인지, 그렇다면 **`K4`·`P4` 오라클을 어디에 거는가** |

- ⚠ 이 중 `CT-1`·`F-2`·`P6` 셋은 **판정 없이는 완료 정의가 확정되지 않는다** — 구현을 시작해도 닫을 기준이 없다.

---

## 5. 완료조건 정의가 없는 항목

- **잔여 항목 중 3건** — 착수 전에 정의가 필요하다.
  - `V-1` 팔레트 선택 재렌더 — `completion_def: 완료 정의 미작성`.
  - `V-2` 값 조회 — 동일.
  - `J-1` 편의 기능 묶음 — **범위(유효 9건)만 확정**돼 있고 **닫는 기준(9건 각각의 통과 조건)이 미기재**. 착수 가능 7건에 들어 있으나 **완료 판정을 못 한다.**
- 참고(이미 `done` 이라 착수 전제는 아니나 기록) — `X-3` · `2단-A` · `2단-E` 도 완료 정의 미작성 상태로 닫혀 있다.

---

## 6. 권고 착수 순서 (임계경로 기준)

- 임계경로 = `D5` → `P3` → `P6` → `P7` → `P8` (**5홉**) · 곁가지로 `P3` → `S3` → `S3-e2e-4종`(4홉) · `P6` → `CT-1`(4홉).
- **경로 위 항목을 못 여는 이유가 stage 2 밖(`D5`·`P5`)에 있다** — 그래서 상위 순서는 「해제」가 앞에 온다.

1. **`D5` 잔여 닫기 (stage 1) — 최우선.** 잔여 12건 중 **5건이 이 항목 하나에 걸려 있다**(`PV-1`·`P3`·`Y-1`·`S3`·간접 `P6`). 사유 = 저장 배치 회귀 시험 0건 · `/healthz` `implemented:false`. ⚠ 산문(`§4`)은 둘 다 ✅ 해소로 적어 대장과 어긋나 있으므로 **실물 재측정이 먼저**다.
2. **게이트 적용 DB 배선.** `COLAB_APPLIED_DB_URL_PLATFORM`·`_AI` ＋ `COLAB_PREVIEW_TILE_DIR` 선언으로 red(준비) 4 중 **3건**을 판정 가능 상태로 바꾼다. 의존 0 · 코드 무접촉 · 나머지 전 작업의 판정 기반.
3. **`P3`**(계보 그래프·2D 시각화 3종·`createScreenshot`). `D5` 해제 직후 열리는 **임계경로 2번째 홉**이고, 그 아래 `V-1`·`V-2`·`Y-1`·`S3`·`P6` 다섯이 매달려 있다.
4. **`P5` 잔여 닫기 (stage 1) — `P3` 과 병행.** `P6` 의 남은 미충족 선행이다. 사유 = 프로젝트 op 넷 501 · 모달 F-03~F-05 부재. `P3` 과 독립이라 동시에 갈 수 있다.
5. **`P6`**(승인 처리). `P3`+`P5` 가 닫히면 열리고, `P7` → `P8` 과 `CT-1` 을 함께 푼다. ⚠ **착수 전 §4 의 `P6`·`CT-1` 판정 3건이 먼저 필요하다.**

- 임계경로 밖 병행 후보(의존 0 · 언제든) — `F-2`(판정 1건 선행) · `PA-G` · `I3`→`I4` 사슬 · `R-1`.
- ⛔ **`J-1` 은 순서에 넣지 않았다** — 착수는 가능하나 닫는 기준이 없어 **부분 완료로 남을 위험**이 가장 크다(`CLAUDE.md §5` 「WU 는 부분 완료로 닫지 않는다」).

---

## 7. 한 줄 결론

- **stage 2 는 「게이트가 막아서」 못 여는 것이 아니다** — 판정 red 0건이고, red 넷은 전부 입력 선언 문제다.
- **막고 있는 것은 셋** — ⑴ stage 1 잔여 `D5`·`P5`, ⑵ 판정 대기 5건, ⑶ 완료 정의 미작성 3건.
- 셋 다 코드가 아니라 **선언·판정·정의**의 자리다.
