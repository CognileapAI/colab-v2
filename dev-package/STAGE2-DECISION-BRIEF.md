# Stage 2 판정 브리프 — 10건 (사람이 고른다 · 작성자는 고르지 않는다)

- 시점 = 2026-08-29 · 상위 문서 = `dev-package/STAGE2-ROADMAP.md §6`
- 정본 = `dev-package/work-items.yaml` · 결정 로그 = `dev-package/PLAN-SoT.md §9`
- 각 항목 = 무엇인가 / 왜 판단이 필요한가 / 선택지 / 권고 / 미루는 비용 / 가역성
- ⭑ **⑩ 은 이번 조사에서 「이미 끝난 판정」으로 실측됐다** — 아래 ⑩ 참조

---

## ① `F-1` 구간 조절 select 처분

**무엇인가**
- `F-1` = 「연구실에 등록하지 않고 파일만 미리 보는 화면」(S-08). 라우트 = `frontend/src/routes/UnregisteredPreviewPage.tsx`
- 그 화면 안에 **표현을 바꾸는 컨트롤이 하나 살아 있다** — 「구간 수」 드롭다운(값 3~9 · 기본 6). 지도 색을 몇 칸으로 나눌지 고르는 것
- 실물 = `frontend/src/components/preview/PreviewControls.tsx:15-16`(`CLASS_COUNTS = [3..9]` · `DEFAULT_CLASS_COUNT = 6`) · 렌더 자리 `:33-48`
- 소비자 = `UnregisteredPreviewPage.tsx:25,131` **한 곳뿐**(전수 grep · 다른 화면 0건)
- 반대편 = 완료 정의 ⑵ (`work-items.yaml:785` · Ted 판정 `PLAN-SoT §9 〈183〉`) 축자 「**표현 변경 없음 — 보기만**(팔레트 변경·구간 조절 없음)」

**왜 판단이 필요한가**
- 완료 정의는 「구간 조절 없음 · 정본 무근거」라 적었는데, 코드 주석은 **정본 근거를 댄다** — `PreviewControls.tsx:14` 축자 「정본이 준 값 그대로: 3~9 단계, 기본 6 (`Policy_데이터셋_상세 §5 시각화 구간 수`)」
- 즉 「정본에 근거가 없다」는 전제와 「정본 §5 가 값을 줬다」는 실물이 정면 충돌. **둘 중 하나는 사실이 아니다**
- 팔레트 쪽은 충돌이 없다 — 이미 「고르지 않는다」로 비워 둠(`PreviewControls.tsx:24-32`)

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ 코드를 걷는다 — select 제거, 구간 수 고정 6 | 완료 정의 무개정 · `F-1` 즉시 닫기 경로 | 정본 `Policy §5` 가 준 값을 화면에서 뺀다 — S-08 밖(등록된 데이터셋 미리보기)에서 다시 넣을 때 중복 작업 |
| ⓑ 완료 정의를 고친다 — 「구간 수만 허용 · 팔레트는 범위 밖」 | 정본 §5 와 정합 · 코드 무변경 | Ted 판정 `〈183〉` 개정 회차 1건 · 「보기만」의 경계가 흐려진다 |
| ⓒ 팔레트까지 연다 | 컨트롤 2종 완비 | **범위 확대** — `V-1`(서버측 재렌더)·`J-1`(팔레트 UI) 소유를 침범 |

**권고 — ⓐ 코드를 걷는다.**
- 근거 = 소비자가 1곳이라 되돌리는 비용이 가장 싸고(`import` 1줄 · JSX 1블록), 「보기만」이라는 화면 성격이 흐려지지 않는다

**판단 미룰 때의 비용**
- `F-1` 이 `in_progress` 로 고정 — 완료 정의 ⑵ 와 실물이 어긋난 채로는 닫기 시험을 쓸 수 없다
- `F-2`(문구 정합)와 같은 프런트 레인이라 후속 착수가 함께 지연

**되돌릴 수 있는가** — **가역.** 코드 제거·복원 둘 다 파일 2개 · 마이그레이션 0 · 데이터 0

---

## ② `K3` 합격선 수치

**무엇인가**
- `K3` = 「업로드할 때 AI 가 부모 데이터를 추천해 주는 서비스」(계보 제안 · `services/ai-service`)
- 완료 정의(`work-items.yaml:979`) 축자 = 「**평가셋 대비 제안 품질** ＋ D4 쓰기 경로 부재 음성 테스트 green」
- 뒤쪽(음성 테스트)은 기계가 판정할 수 있다 — 「AI 가 계보를 직접 쓰지 못한다」는 `CLAUDE.md §3-2` 불변 규칙
- 앞쪽(제안 품질)에 **수치가 없다.** 몇 건 중 몇 건을 맞히면 통과인지 어디에도 없다

**왜 판단이 필요한가**
- 「평가셋」이 가리키는 실물 = `S2b` 검색 평가셋 18건인데 그것은 **검색용**이지 계보 제안용이 아니다 → 계보용 오라클은 **존재하지 않는다**
- 선행 3건(`K2` 시드 · `core-ai.yaml:64-90` `suggestLineage` 선언 · 업로드 모달)은 전건 실물 완료 — **구현은 지금 착수 가능하고 막힌 것은 닫기 기준뿐이다**
- 모수도 작다 — staging 계보 간선 **6건** · 데이터셋 **12건**(`sessions/X22-ORPHAN-DELETE.md §5`). 재현율 퍼센트는 통계적 의미가 얕다

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ 수치 합격선을 새로 고정 (예: 시드 계보 6건 중 상위 3 안에 정답 포함 ≥ 5) | 기계 판정 가능 · 회귀 검출 | 모수 6 에서 뽑은 수치라 **근거가 얇다** · 데이터 늘면 다시 정함 |
| ⓑ 오라클을 「사람 검수 통과」로 바꾼다 | 즉시 착수·즉시 판정 가능 | 회귀 검출 불가 · 다음 사람이 같은 판정을 반복 |
| ⓒ 이번 회차는 **음성 테스트 green ＋ 정직한 빈 상태 성립**만으로 닫고, 품질 계량은 별건으로 뺀다 | `K3`·`K5` 사슬이 즉시 열린다 · `CLAUDE.md §3` AI 응답 규격은 그대로 강제 | 「품질」이 판정 없이 남는다 — 별건 항목을 반드시 세워야 함 |

**권고 — ⓒ.**
- 근거 = 모수 6 에서 만든 퍼센트는 오라클 행세를 할 뿐이고, 이 레포가 금지한 「green-by-skip」의 다른 얼굴이다. 계량은 데이터가 늘어난 회차에 별건으로 연다

**판단 미룰 때의 비용**
- `K3` 착수는 가능하나 **닫을 수 없다** → `K5`(제안 원장)가 무기한 대기
- 사슬 E 전체가 정지

**되돌릴 수 있는가** — **가역.** 완료 정의 문구 개정 1건. 코드·데이터 영향 0

---

## ③ `R-1` 상태 확정

**무엇인가**
- `R-1` = 「백업에서 되살리는 절차」(복원 절차 · 완료 정의 12행 · 초안 `sessions/R1-RESTORE-DRAFT.md`)
- 대장 상태 = `conflict`. 산문 셋이 서로 다르게 적는다 — `03-HANDOFF §1 T-X`(🟧 표기 안에 ⛔ 글리프 혼용) · `WORK-UNITS §10.2`(⛔) · 같은 문서 `§10.3`(진행 중)
- 완료 정의 12행은 전건 ✅ 인데 **꼬리 넷**이 남아 있었다 — ⑴ 크론 첫 회차 로그 ⑵ `POST /searches` 1회 ⑶ `preflight` 성질 판정 ⑷ `:i2` 태그 이력

**왜 판단이 필요한가**
- ⑴ 은 닫혔다 — `LAST-SUCCESS.txt` 마지막 줄 `2026-08-29T03:30:41+0900 backup-full.sh OK` · 볼륨 산출물 `vol-uploads`·`vol-previews` 실재(`work-items.yaml:735` `deadline.fired: true`)
- ⑵⑶⑷ 는 **아직 안 쟀다.** 재는 데 드는 비용은 각각 명령 1~2회
- 실측이 갈라짐을 없애지는 않는다 — 산문 셋을 누가 하나로 맞추라는 결정이 별도로 필요

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ 잔여 셋을 재고 나서 상태를 하나로 확정 | 근거 있는 단일 상태 · `W-1` red 1건 해소 | 측정 회차 1건 소요 |
| ⓑ 지금 `partial` 로 내리고 산문 셋만 정렬, 잔여 셋은 꼬리로 명기 | 즉시 갈라짐 해소 · `I4`·`#22`·`전범위백업` 대기 해제 | 「부분 완료로 닫지 않는다」(`CLAUDE.md §5`)와의 거리 — `partial` 은 닫기가 아니므로 위배는 아니다 |
| ⓒ `done` 선언 | 최대 속도 | ⑵⑶⑷ 미측정 상태의 완료 선언 = 이 레포 대표 실패형 |

**권고 — ⓐ, 단 산문 정렬은 즉시.**
- 근거 = 측정 비용이 명령 수회로 작고, 이 항목이 `I4`·`#22`·`전범위백업`·`W-1` 넷을 동시에 잠그고 있다

**판단 미룰 때의 비용**
- `work-item-consistency` 게이트 red 유지 → **어떤 Stage 2 항목도 완료 판정을 못 받는 구조**(`STAGE2-ROADMAP §2`)
- 사슬 C 전체 정지

**되돌릴 수 있는가** — **가역**(상태·산문). 다만 ⓒ 로 잘못 닫으면 **실질 비가역** — 닫힌 항목은 다시 열리지 않는 것이 이 레포 관행

---

## ④ `S2`·`S2b` 재판정

**무엇인가**
- `S2` = 「초기 데이터를 staging 에 실제로 넣는 일」. `S2b` = 「데이터셋 설명 문구 ＋ 검색 평가셋 만들기」
- `S2` 는 계수 갈림(파일 122/123/129/139 · 계보 5/6 · 데이터셋 11/12)이 **2026-08-28 `〈184〉` 로 해소**됐다 — 정본 파일 수 **123**(기준 격자 제외), `129` 는 저장·백업 맥락 병기
- 그런데 `status: conflict` 는 유지 — 「계수 밖 잔여가 있는지」가 `[미확인]`(`work-items.yaml:1069`)
- `S2b` 는 `03-HANDOFF §1 T-S` 가 ✅(Ted 승인 · 설명 15건 승인/11건 적재 · 평가셋 18건), `WORK-UNITS §8.5` 가 🟧(A층 재판정 = 통과 8 · 실패 8 · 층 밖 2)로 **정반대**

**왜 판단이 필요한가**
- `S2b` 실패 8건 재측정은 `T-1` ⓓ 로 이관됐는데, `T-1` 의 ⓐⓒ 는 `〈182〉` 로 **하지 않기로 결정**됐다(형태소 분석기가 이미지에 없음). 즉 실패 8건을 고칠 수단이 사라졌는데 오라클만 남아 있다
- `S2b` 는 `WORK-UNITS §11` 체크리스트에 **행 자체가 없다**
- 이 두 건이 `work-item-consistency` red 3건 중 둘

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ 평가셋 A층을 1회 재측정하고 그 결과로 판정 | 근거 있는 확정 | `T-1` ⓐ 가 사라진 뒤라 **수치가 개선될 이유가 없다** — 통과 8/실패 8 이 그대로 나올 공산 |
| ⓑ `S2b` 를 「설명 문구·평가셋 산출 완료」로 닫고, **평가셋 대비 품질 판정은 `K4`·`P4` 로 이관**한다 | red 2건 즉시 해소 · `S3` 대기 해제 | 오라클 이전을 문서에 명시하지 않으면 갈라짐이 재발 |
| ⓒ 현상 유지 | — | 게이트 red 고착 = ③ 과 같은 구조적 정지 |

**권고 — ⓑ, 그리고 `S2` 는 `§8.5` 세 행 ＋ `§1.5` 조건 8 을 실물과 1:1 대조한 뒤 `done` 또는 `partial` 로 내린다.**
- 근거 = `S2b` 의 산출물(설명·평가셋)은 실재하고, 남은 것은 산출물이 아니라 **그 평가셋으로 재는 검색 품질**이다. 둘은 다른 항목의 일이다

**판단 미룰 때의 비용** — ③ 과 동일(게이트 red · 완료 판정 전면 정지) ＋ `S3` 착수 불가

**되돌릴 수 있는가** — **가역.** 상태·오라클 귀속 문구 개정

---

## ⑤ 고아 행 id 목록 고정

**무엇인가**
- 「고아 행」 = 가리키는 데이터셋이 이미 사라졌는데 활동 기록만 남은 줄
- 실측 완료(`sessions/X22-ORPHAN-DELETE.md` · 2026-08-28 · 읽기 전용) — `d8_activity` 9행 중 **고아 6건**, id 전건 확정(§4.3)
  - 1·2 = 자리표시 시드 잔재(`...DSA1`/`DSB1`) · 3~6 = 파이프라인이 만든 뒤 대상 데이터셋이 사라진 것
- D2 3종(`d2_dataset_access`·`d2_verified`·`d2_dataset_access_grant`) = **테이블이 통째로 비어 고아 0건** → `#22` 의 「D2 고아」는 지금 존재하지 않는다
- 범위 밖 발견 = `d8_download` 에 같은 성질 고아 **2건**(§4.4)

**왜 판단이 필요한가**
- ⛔ **삭제가 구조적으로 막혀 있다** — `d8_activity_append_only` 트리거(`BEFORE DELETE OR UPDATE`)가 `deny_update_delete()` 를 호출해 무조건 예외를 던진다(`db/platform/schema.sql:62-68`). 축자 「append-only 기록이다 — 수정·삭제 경로를 만들지 않는다 (`PLAN-SoT 9-28`)」
- 즉 id 를 못 박아도 그대로는 실패한다. **「무엇을 지울까」보다 「지우는 것이 맞는가」가 먼저다**

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ 트리거를 한시 비활성 후 6건 삭제, 트리거 복구 | `#22` 종결 | **append-only 결정(`§9-㉘`)을 스스로 깬다** · 「한 번 껐다」가 선례가 된다 · `#28`(경위 증거 보존)과 충돌 |
| ⓑ 삭제를 철회하고 고아를 남긴다 — `#22` 를 「측정·문서화로 종결」로 닫는다 | 코드·DB 무접촉 · append-only 유지 | 「고아 0」이라는 사후 실측 기준을 쓸 수 없다 → 그 문언을 고쳐야 함 |
| ⓒ 6건 ＋ `d8_download` 2건 = 8건 일괄 삭제 | 시드 잔재 전부 정리 | ⓐ 의 리스크 ＋ 범위 확대(문안에 없던 테이블) |

**권고 — ⓑ 철회.**
- 근거 = 삭제 대상 6건 중 4건이 「경위 증거」에 해당할 개연이 있고(§4.2), append-only 는 이 레포가 명시 결정으로 세운 성질이다. **9행짜리 정리를 위해 그 성질을 끄는 것은 값이 맞지 않는다**

**판단 미룰 때의 비용**
- `#22` 가 `R-1` 뒤에 매달린 채 사슬 C 꼬리에 남는다. 다만 다른 항목을 막지는 않는다 — **10건 중 가장 덜 급하다**

**되돌릴 수 있는가** — **ⓑ 가역 / ⓐ·ⓒ 비가역.** 삭제된 활동 기록은 복원 경로가 없다(append-only 이므로 재삽입도 이력이 아니다)

---

## ⑥ `V-1`·`V-2`·`J-1`·`전범위백업` 완료 정의 작성

**무엇인가** — 네 항목 모두 대장에 `completion_def: 완료 정의 미작성`
- `V-1`(`work-items.yaml:752`) = 「팔레트를 고르면 서버가 그림을 다시 그린다」. 범위 = **서버측 재렌더 경로까지**, 고르는 UI 는 `J-1` 소유
- `V-2`(`:765`) = 「지도에서 한 점을 찍으면 그 값이 뜬다」
- `J-1`(`:804`) = 편의 기능 묶음 **9건**(계수는 `〈187〉`-㉰ 로 확정 · 목록 `sessions/S1-PLAN-REFOUND.md §J` · `J-1`~`J-6`·`J-8`~`J-10`)
- `전범위백업`(`:1105`) = platform ＋ ai ＋ 볼륨을 다 뜨는 백업. 기구 ✅ · 유인 GREEN ✅ · 크론 첫 회차 GREEN(2026-08-29 03:30)

**왜 판단이 필요한가**
- 완료 정의가 없으면 **닫는 기준이 없다** = 영원히 안 닫히거나 아무렇게나 닫힌다
- 넷의 성숙도가 다르다 — `전범위백업` 은 실물이 이미 다 서 있어 정의만 받아 적으면 되고, `V-1`·`V-2` 는 선행 `P3` 이 미착수라 지금 쓰면 추측이 섞인다

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ 넷을 지금 한 번에 작성 | 대장의 정의 공백 0 | `V-1`·`V-2` 는 `P3` 산출물 형태를 모른 채 쓰게 된다 → 재개정 확실 |
| ⓑ 성숙한 것부터 — `전범위백업` 즉시 · `J-1` 은 §J 9건 목록을 그대로 정의로 · `V-1`·`V-2` 는 `P3` 착수 회차에 | 추측 0 · 회차당 부담 작음 | 정의 공백이 두 건 남는다 |
| ⓒ 전부 `P3` 뒤로 | 회차 절약 | `전범위백업` 이 근거 없이 계속 `partial` |

**권고 — ⓑ.**
- 근거 = `전범위백업` 은 합격선이 이미 코드에 있다 — `infra/staging/backup/config.example.env:61` 축자 `COLAB_VOLBACKUP_VOLUMES="uploads previews"` · 볼륨별 `MIN_FILES_uploads=67`·`MIN_FILES_previews=19`. **재는 것이 이미 있는데 정의만 없다**

**판단 미룰 때의 비용**
- `전범위백업` 이 `R-1` 종결 뒤에도 못 닫힌다 → 사슬 C 가 꼬리를 남긴 채 끝난다
- `J-1` 은 Stage 2 마지막 배치라 정의 부재가 **완주 선언 자체를 막는다**

**되돌릴 수 있는가** — **가역.** 문서 작성·개정

---

## ⑦ `unknown` 5건 단계 배정

**무엇인가** — 대장에 `stage: unknown` 인 5건. 「몇 단에 속하는가」가 안 정해진 것

| ID | 무엇 | 상태 | 실질 위치 |
|---|---|---|---|
| `R1` | 스캐폴드 ＋ CI 골격 | done | 이미 끝났고 단계 표기만 빈다 |
| `IS4` | terraform state 보관 | partial | 미달 2건 = 마지막 `apply` 미실행 · 맨몸 호스트 미실험. **맨몸 리허설은 `R-1` 과 재료가 겹쳐 `R-1` 뒤가 싸다** |
| `G1b` | E-04·E-02 목업·화면 최신성 | open | 선행 `P2`·`P4`. `§10.2`·`§10.2-b`·`§10.3` 어디에도 행이 없어 **순서표에 자리가 없다** |
| `P4` | 검색 히어로(S-01)·검색 결과(S-06) | open | ⭑ **`P6` 의 선행 — Stage 2 완주의 실질 경로 안에 있다.** 화면은 이미 서서 staging 실결과를 낸다 |
| `LV-4` | 계보 삭제 시 부모 이름만 상수로 남기기 | open | 2026-08-29 신설(`〈202〉`) · 저장 자리 설계가 `[미확인]` |

**왜 판단이 필요한가**
- **계수와 실질 경로가 갈린다** — 34건 계수에는 안 들어가는데 `P4` 없이는 `P6`→`P7`→`P8` 이 못 간다
- `P4` 는 「stage 1 에 지어지고 stage 2 에 판정된다」라 스키마 값 하나에 안 담긴다(`work-items.yaml:891`)

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ 다섯을 개별 배정 (`P4`·`IS4`=stage2 · `G1b`·`LV-4`=after_stage2 · `R1`=stage1) | 순서표·계수 일치 · 사슬 B 착수 순서 확정 | 배정 근거를 항목마다 적어야 함 |
| ⓑ `P4` 하나만 배정하고 나머지는 유지 | 최소 개입으로 사슬 B 를 연다 | `unknown` 4건이 계수 밖에 남아 완주 선언이 흐려진다 |
| ⓒ 스키마에 「두 단 걸침」 값을 신설 | `P4` 성질을 정확히 표현 | 검사기·대장 스키마 개정 파급 |

**권고 — ⓐ, 단 `P4` 는 「stage 2 에서 판정」을 배정 근거로 명기.**
- 근거 = `P4` 의 오라클(평가셋)이 ④ 의 판정에 걸려 있으므로 판정 시점이 곧 stage 2 다. `IS4` 는 `R-1` 과 재료를 공유하므로 같은 단에 두는 것이 실측 비용이 싸다

**판단 미룰 때의 비용** — 사슬 B(`P3`＋`P4`＋`P5`→`P6`→`P7`→`P8`) 착수 순서가 안 서서 화면·승인 라인 전체가 대기

**되돌릴 수 있는가** — **가역.** 대장 필드 값

---

## ⑧ `PV-1` 순서표 자리 · 산출물 창구 · `layout.json` 슬롯 재사용

**무엇인가**
- `PV-1` = 「미리보기 뒷단」 — 파일 헤더를 읽고 · 좌표계를 맞추고 · 지도용 영상으로 바꾸는 일(`services/pipeline-worker`)
- 착수 가능 상태(선행 `D5` stage 1 파트 완료). 실작업 3건 = ⑴ `worker.py:147` `stage1=True` 하드코딩 해제 ⑵ autometa 소비자 신설(`crs`·`grid` 를 채우는 코드가 `core-api` 에 0건) ⑶ 저장 슬롯 판정
- ⭑ **⑶ 은 이번 조사에서 이미 끝난 것으로 실측됐다** — `contracts/storage/layout.json` 에 `previewsRoot` ＋ 「미리보기 산출물」 종류가 **이미 신설·집행**돼 있다(`PLAN-SoT §9 〈200〉` = **9차 동결 해제** · 소비자 `viz-render/domains/d7_visualization/preview.py:_write` 실재 · 생성물 3종 재생성 완료)

**왜 판단이 필요한가** — 남은 판정은 셋이고, 셋 다 성격이 다르다
- ㉮ **순서표 자리** — `WORK-UNITS §10.3` 몇 단인지가 `[미확인]`(`〈189〉`-㉷-⑵). 자리가 없으면 병렬 레인 배정이 안 된다
- ㉯ **수신 창구의 형태** — 「사건(이벤트) 경유」는 `〈190〉` 로 확정됐고 기각 둘(직접 API 호출·주기 폴링)도 기록됐다. **남은 것은 창구의 모양뿐**(새 이벤트 종류를 세우는가, 기존 7종 소비 경로에 얹는가)
- ㉰ **COG 산출물이 `previews` 볼륨에 놓이는가** — `〈191〉`-㉶-⑵ 승계 `[미확인]`. 지금 변환 산출은 `workdir` 임시 자리에만 떨어져 그리는 서버가 찾을 방법이 없다

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ 신설된 「미리보기 산출물」 슬롯을 **그대로 재사용**하고, 창구는 기존 이벤트 7종 소비 경로에 얹는다 | 계약 동결 해제 **0건** · 착수 즉시 | 렌더 산출물과 COG 산출물이 한 종류에 섞인다 — 나중에 갈라야 하면 파일 이동 발생 |
| ⓑ COG 전용 종류를 하나 더 세운다 | 두 산출물이 구분됨 | **10차 동결 해제 ＋ Ted 승인 필수**(등급 ㉯) · 회차 1건 소요 |
| ⓒ 새 이벤트 종류를 신설한다 | 창구가 명시적 | 이벤트 계약 개정 ＋ `event-breaking` 게이트 파급 |

**권고 — ⓐ 재사용.**
- 근거 = `〈191〉`-㉳ 축자 「경로를 쓰기 시작한 뒤엔 파일 이동이 딸린다」의 반대편 — 지금은 옮길 실물이 0건이고, 섞였을 때 가르는 비용도 아직 0건이다. **가장 늦게까지 싸게 갈릴 수 있는 갈래**
- 순서표 자리는 **1단**(사슬 A 의 머리이자 `P3`·`V-1`·`V-2`·`Y-1`·`S3` 전부의 선행)

**판단 미룰 때의 비용**
- 착수 가능 1순위 항목이 서지 않는다 → 사슬 A 전체(`P3`·`V-1`·`V-2`·`Y-1`·`S3`) 정지. **10건 중 가장 넓게 막는 자리**

**되돌릴 수 있는가** — **ⓐ 가역**(구현 되돌림 · 계약 무접촉) / **ⓑ·ⓒ 준비가역**(계약 되돌림 ＋ 생성물 재생성 · 마이그레이션 0)

---

## ⑨ `I4` 가 `R-1` 을 흡수하는가

**무엇인가**
- `I4` = 「운영 준비」 — 추적·알람·복구 리허설(`work-items.yaml:358` · 완료 정의 축자 「복원 리허설 성공 (`§4`) · 정본 = `WORK-UNITS §10.2-b`」)
- `R-1` = 「복원 절차」 — 완료 정의 12행
- 둘 다 「복원을 한 번 돌려 본다」를 재료로 쓴다. **한쪽이 다른 쪽을 흡수하는지 두 문서 모두 미기재**(`work-items.yaml:366` note)

**왜 판단이 필요한가**
- 흡수 관계를 안 정하면 **같은 리허설을 두 번 돌리거나, 둘 다 상대가 했겠거니 하고 0번 돌린다**
- 후자가 이 레포 대표 실패형(green-by-skip)과 같은 모양
- 참고 = `I4 ── P6` 은 WU 의존이 아님이 `〈168〉`-㉵ 로 이미 판정됐다 → `I4` 는 사슬 B 를 막지 않는다

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ `I4` 가 `R-1` 을 흡수 — `R-1` 을 `I4` 의 한 행으로 내린다 | 항목 1건 감소 | `R-1` 이 `X-1`·`X-4` 재기동의 전제조건(`〈158〉`-㉯)이라 **`I3` 뒤로 밀리면 재기동이 함께 밀린다** |
| ⓑ 분리 유지 — `R-1`=절차·기구 증명 / `I4`=운영 리허설(추적·알람 포함) · `I4` 완료 정의에 「`R-1` 종결을 전제로 하고 복원 리허설을 재실행하지 않는다」 1행 추가 | 중복 0 · 누락 0 · `R-1` 의 선행 성질 보존 | 완료 정의 1행 개정 |
| ⓒ 현상 유지 | — | 중복 실행 또는 0회 실행 |

**권고 — ⓑ 분리 유지 ＋ 1행 명기.**
- 근거 = `R-1` 은 `I3` 무의존(`entry_conditions: 없음`)인데 `I4` 는 `I3` 선행이다. 흡수하면 무의존 항목이 `I3` 대기로 끌려 들어간다

**판단 미룰 때의 비용** — `I4` 범위가 안 서서 `I3` 종결 직후 착수가 안 된다. 사슬 C 꼬리 지연

**되돌릴 수 있는가** — **가역.** 완료 정의 문구

---

## ⑩ `X-5` 잔여 — 생산자 0 인 `422` 선언 2건 철회 여부

**무엇인가**
- `422` = 「형식은 맞는데 처리할 수 없다」는 응답 코드. 계약(`contracts/seams/*.yaml`)이 그 응답을 내겠다고 **선언만 하고 실제로 내는 코드가 없던** 자리 2건
  - `core-viz.yaml#createRender`(그리기 요청) · `core-ai.yaml#searchDatasets`(데이터셋 검색)

**왜 판단이 필요한가 — ⭑ 필요하지 않다. 이미 판정·집행이 끝났다**
- **실측(2026-08-29 · 본 조사)** — `contracts/` 전수 grep 결과 남은 `422` 는 **두 자리뿐**이고 둘 다 대상이 아니다
  - `contracts/.spectral.yaml:60` = 린트 규칙 자체
  - `contracts/seams/core-ai.yaml:92` = `suggestLineage` 의 `422` — **의도적 존치**(구현 예정 `K3` · 소비자 `core-api/app/relay.py:282` 실재 · `〈180〉` 범위 밖 명기)
- 즉 `core-viz.yaml#createRender`·`core-ai.yaml#searchDatasets` 의 `422` 는 **파일에 없다** — 2026-08-28 Ted 승인으로 철회·집행 완료(`PLAN-SoT §9 〈180〉` · 회차 번호는 `〈199〉` 가 **8차**로 사후 발급)
- 대장도 이미 반영 — `work-items.yaml:698` `X-5` 는 `status: done` · 축자 「열린 판정 0건」
- 게이트 출력(2026-08-28) = `contract-lint` green · `contract-breaking` green · `seam-consistency` green · `generated-up-to-date` green
- ⚠ **따라서 `STAGE2-ROADMAP §6-⑩` 은 낡은 행이다** — 로드맵이 참조한 `sessions/X5-DECISION-BRIEF.md` 는 판정 **이전**의 선택지 제시분이고, 문서 자신이 머리에서 「판정은 이미 끝났다」고 밝힌다

**남아 있던 것 하나 — 이번에 닫혔다**
- `〈199〉`-㉲-⑧-ⓑ 가 열어 둔 `[미확인]` = 「프런트·`fe-core.yaml` 이 `422` 를 여전히 선언하는지 출력 근거 없음」
- **실측 = `grep -rn "422" frontend/src contracts/seams/fe-core.yaml` → 0건.** 미도달 추정이 출력으로 확인됐다

**선택지**

| 갈래 | 결과 | 리스크 |
|---|---|---|
| ⓐ `STAGE2-ROADMAP §6` 에서 ⑩ 행을 **판정 완료로 정정**하고 위 grep 0건을 `〈199〉` 의 `[미확인]` 해소 근거로 등재 | 판단 대기 10건 → 9건 · 이력 정합 | 없음(문서 정정) |
| ⓑ 그대로 둔다 | — | 다음 사람이 **이미 끝난 판정을 다시 연다** — `〈98〉`→`〈151〉` 이 증명한 두 회차 낭비의 재생산 |

**권고 — ⓐ.**
- 근거 = 판정할 것이 없는 자리를 판정 대기로 인쇄하면, 낡은 감사 문서가 결정을 재등장시킨다(`X-5` note 의 `PermissionSwitchChange` 사례가 정확히 그것)

**판단 미룰 때의 비용** — Ted 판정 대기 목록이 실제보다 부풀어 우선순위가 왜곡

**되돌릴 수 있는가** — **가역**(문서 정정). 원 철회 자체도 축자 「두 줄 복원이면 원복」(`〈180〉`)

---

## 부록 — 판정 순서 권고

| 순 | 항목 | 이유 |
|---|---|---|
| 1 | ⑩ | 판정할 것이 없다 — 목록에서 빼는 데 비용 0 |
| 2 | ③ ④ | `work-item-consistency` red 3건을 함께 지운다 → **완료 판정 전면 정지가 풀린다** |
| 3 | ⑧ | 가장 넓게 막는 자리(사슬 A 전부) |
| 4 | ① ② ⑦ | 착수 가능 항목·사슬 B 를 연다 |
| 5 | ⑥ ⑨ | 닫기 기준·범위 정리 |
| 6 | ⑤ | 다른 항목을 막지 않는다 |
