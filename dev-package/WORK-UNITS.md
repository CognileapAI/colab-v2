# WORK-UNITS — 작업 단위와 의존 그래프

> **일정(주차)으로 표현하지 않는다.** AI가 개발하므로 사람 기준의 소요 시간 산정은 의미가 없고, 지배 변수는 시간이 아니라 **의존과 게이트**다.
> 진행은 "몇 주차"가 아니라 **"어떤 작업 단위가 닫혔는가"**로 센다.

---

## 1. 작업 단위(WU)의 정의

**한 에이전트 세션이 계약 동결 → 구현 → 게이트 green까지 통째로 닫을 수 있는 최소 단위.**

WU 하나는 반드시 아래 넷을 가진다. 넷이 다 없으면 WU가 아니라 할 일 목록이다.

| 항목 | 뜻 |
|---|---|
| **진입조건** | 이것이 없으면 시작해도 헛일이 되는 선행 산출물 |
| **산출물** | 파일·커밋·결정 기록 중 무엇이 남는가 |
| **완료 판정(오라클)** | 사람 의견이 아니라 **기계가 green/red를 말하는 것**. 없으면 WU를 더 쪼갠다 |
| **병렬성** | 어떤 WU와 동시에 돌 수 있는가 |

WU가 한 컨텍스트에 안 들어가면 쪼갠다. 쪼갤 수 없으면 그건 도메인 경계가 잘못 그어진 것이다(→ `DOMAINS.md`).

---

## 1.5 완료 정의 — "v2가 끝났다"는 것

**v2는 전체가 끝나야 한다.** 일부 화면만 나온 상태, 로컬에서만 도는 상태, AI 두 지점이 빠진 상태는 전부 미완이다.

> **⭑ ⟨개정 2026-08-24 — `〈70〉`⟩ v2 는 두 단(stage)으로 나뉘어 지어진다. 이 문장은 stage 2 를 향해 그대로 유효하다.**
> **stage 1** = 「데이터 업로드 · 데이터셋 인포 · AI 검색(온톨로지 기반) · 연구실-프로젝트-데이터셋 · 계보 설정
> **· 미리보기 3층과 기준 격자 파일 수용**(`〈74〉`·`〈75〉`)」. 완료 정의는 `sessions/S1-PLAN-REFOUND.md §F`.
> **stage 2** = 가공(헤더 파싱·좌표계 변환·COG) · 타일 서빙·확대 · `createScreenshot` · 팔레트 선택 재렌더 · S-08 · 값 조회 ·
> 자동 무효화 · P3·P6·P7·P8·S3·K3·K5 · `〈78〉` 의 편의 기능 묶음.
> ⚠ **stage 1 green 은 v2 완료가 아니다.** 그 오해를 막는 것이 이 문단이 존재하는 이유다.

### 끝났다고 말할 수 있는 조건 (전부 충족)

| # | 조건 | 판정 |
|---|---|---|
| 1 | **에픽 8개 전부** — E-00 ~ E-07. 스토리 45개 전부 | 스토리별 수용 기준 통과 |
| 2 | **화면 전부** — S-01 연구실 · S-02/S-02b 프로젝트 · S-03 카탈로그 · S-04 업로드 모달 · S-05 데이터셋 상세 3섹션 · S-06 검색 결과 · S-08 미등록 미리보기 · `연구실 설정 > 구성원·권한` | 목업 대비 검증<br> **⭑ ⟨stage 1 개정 2026-08-24 — `〈70〉`·`〈74〉`⟩ stage 1 화면 목록에서 `S-08 미등록 미리보기` 는 빠진다 — S-08 은 stage 2 다.** ⚠ **미리보기 자체는 stage 1 안이다**(`〈74〉`) — **S-04 업로드 모달 안의 패널과 S-05 데이터셋 상세**가 그 자리이고, **S-08 이라는 별도 화면만** 빠진다. 되살리면 `handoff.ts` 의 등록 진입 경로까지 함께 돌아온다 |
| 3 | **도메인 10개 전부** — D1~D8 기록 + D9 온톨로지/KG + D10 AI 서비스 | 도메인별 게이트 green |
| 4 | **AI 두 지점 동작** — 계보 제안(확신도 3단계·근거·사람 확인 게이트), 자연어 검색(관련도·근거 한 줄·범위 고지) | 평가셋 통과 |
| 5 | **경계 강제 장치 전부 동작** — 계약 게이트 · import 경계 · **D10→D4 쓰기 부재** · RLS 커버리지 · 스키마 diff | 게이트 self-test 전부 fail-closed 증명 |
| 6 | **staging 배포 완료** — 5개 배포 단위 전부. 롤백 경로 · 복구 리허설 동작 | **staging 헬스 green** |
| 6-prod | ⏸ **AWS prod 배포** — `㊻`(2026-08-23 Ted)로 **v2 완주 조건에서 빠졌다.** ① staging 에서 v2 전체가 충분히 동작하고 ② Ted 가 **출시를 결정**했을 때 연다. 예산 알람은 이때 따라온다 | (보류) |
| 7 | **실데이터 통과** — **지원 포맷 4종**(**NetCDF · Binary · HDF4 · GeoTIFF** — `〈51〉`) 실파일이 업로드→파싱→COG→시각화→계보 확정까지 완주. ⚠ **숫자가 아니라 목록으로 읽어라** — PoC 의 4포맷(GRIB·NetCDF·Binary·HDF5)과 **수만 같고 구성이 다르다** | E2E 실측<br> **⭑ ⟨stage 1 개정 2026-08-24 — `〈70〉`·`〈74〉`·`〈77〉`⟩ stage 1 판 = 「업로드 → 감지 → 등록 → **미리보기** → 계보 확정」.** **파싱·좌표계 변환·COG 는 stage 2 완료 조건으로 이관한다.** ⭑ **지원 포맷 목록에 `NumPy` 가 더해진다** — **`NetCDF` · `Binary` · `HDF4` · `GeoTIFF` · `NumPy`**(`〈77〉`). ⚠ **여기서도 숫자가 아니라 목록으로 읽어라.** **「미리보기」의 뜻** = ①썸네일 · ②비지도형 상세 · ③지도형 상세(bbox 사이드카 + `.pgw`). **`Binary`(HSR)·`NumPy` 는 사람이 격자를 올려야 ③이 성립하고, 안 올리면 `격자 없음 — 값만 표시` 다**(`〈75〉`) |
| 8 | **초기 데이터가 staging 에 들어가 있고 화면에서 보인다** — 원천 = `03 Reference-Data`(3.5 GB · 2,122 파일). 빈 카탈로그는 완료가 아니다 | **S2 완료 판정** (T-S 트랙 · `§8.5`) |

> **★ 2026-08-26 마감 회차 — 완료 조건 전항 판정 (stage 1 기준. `PLAN-SoT §9 〈116〉~〈128〉`).**
> **미충족을 충족으로 반올림하지 않는다.**
>
> | # | stage 1 판정 | 근거 |
> |---|---|---|
> | 1 | ⬜ **판정 보류** | stage 1 은 에픽 전항이 아니라 `〈70〉` 이 좁힌 범위다. 스토리 단위 대조를 이 회차에서 하지 않았다 — `[미확인]` |
> | 2 | 🟧 **부분 미충족** | 데이터셋 인포의 `summary` 결손 2건(`03-HANDOFF §4 #36` · 판정 `〈127〉` **미실행**) · 자동 메타 칸 공백(`#35` · 판정 `〈123〉` **선행 확인 미실시**) |
> | 3 | ⬜ **판정 보류** | 도메인별 게이트는 green 25/26 이나 D9·D10 을 포함한 stage 1 도메인 대조를 이 회차에서 하지 않았다 — `[미확인]` |
> | 4 | ⛔ **미충족** | 평가셋 A층 **통과 8 · 실패 8 · 층 밖 2**(`〈117〉-㉰`). 「평가셋 통과」가 성립하지 않는다 |
> | 5 | ✅ **충족** | 게이트 26종 개별 실행 · green 25 · red 1(`schema-diff` — 적용 DB 미지정, **환경 조건이며 검사 축소가 아니다**) · selftest 8종 전부 green |
> | 6 | 🟧 **부분 미충족** | 컨테이너 8/8 · 헬스 6/6 이나 **`〈116〉`·`〈119〉` 미배포** — 최신 커밋 기준으로 staging 이 뒤에 있다 |
> | 7 | 🟧 **부분 충족** | 미리보기 3층이 포맷 4종 6 데이터셋 전건 산출(`〈115〉-㉮`). **계보 확정까지의 완주는 stage 2 범위**(`〈70〉-㉮`) |
> | 8 | 🟧 **부분 충족** | 데이터셋 12 · 파일(본체 기준) 123 · 계보 6 이 staging 에 있고 화면에서 보인다. **설명 2건 결손이 남는다**(조건 2 와 같은 사유) |
>
> **해소 5** — 기준 격자 수용(`〈74〉`) · 격자 축 감지(`#31`·`#32`) · 미리보기 3층 실측 · S2 완료 조건 3항 · 픽스처 A·B 부분 철거.
> **잔여** — 평가셋 실패 8 · 설명 결손 2 · **판정 확정 미실행 7건** · **미배포 2건** · **신규 정본 후보 13건 미대조**(`#37`).


### 범위 밖 (정본이 이미 뺀 것)

아래는 **미완이 아니다.** 260818 정본이 1차 범위에서 명시적으로 제외했다.

- **P1 계정과 연구실 소속** (A-01~A-06) — 에픽 자체 보류
- **백로그 B-01~B-09** — 데이터·사이트 추천 / 플로팅 검색 / 활용 역할 태그 / 계보 그래프 한계선 / 연구실 밖 검색 / 보조입력 부모 표기 등
- **대화 입구 · 의도 라우터 · 협업 프로젝트** — v3 영역

> 이 경계가 흔들리면 "전체 완료"가 정의되지 않는다. 범위를 늘리려면 정본을 먼저 고치고, 그 다음에 WU를 추가한다.

### 유예 금지 규칙

- WU는 **부분 완료로 닫지 않는다.** 오라클이 green이 아니면 `🟧 부분(사유)` 로 남긴다
- 차단이 생기면 **차단 해제 자체를 WU로 만든다.** "나중에"는 상태가 아니다
- 각 WU의 완료 판정에 **staging 배포 green이 포함된다**(I2 이후)

## 2. 트랙 여덟 개

```
T-R  저장소   레포 결정 · 스캐폴드 · CI 골격     ─┐
T-C  정리     v1 이관 · PoC/v1 지식 추출        ─┤
T-G  게이트   사람 결정 · 기획 패키지 재빌드     ─┼─ 서로 병렬, 지금 착수 가능
T-I  인프라   AWS 계정 · IaC · 배포 경로        ─┤
T-S  초기데이터 03 Reference-Data 인벤토리(S1) ─┘
                        │
                        ▼
T-D  도메인   경계 확정 · 계약 동결 · 게이트 구축
                        │
        ┌───────────────┴───────────────┐
        ▼                               ▼
T-P  플랫폼   D1~D8 구현              T-K  지식·AI   D9·D10 구현
      (에픽 0~7단계)                       (온톨로지 → KG → 두 지점)
        └───────────────┬───────────────┘
                        ▼
              T-I 계속 (모든 WU가 배포까지 포함)
```

- **T-P와 T-K는 계약 동결 후 완전 병렬.** 만나는 지점은 두 곳뿐 — E-04 계보 제안, E-02 검색.
- **T-I는 처음부터 끝까지 흐른다.** 배포는 마지막 단계가 아니라 P0 직후부터 모든 WU의 완료 조건이다.
- **감사 트랙이 없다.** 코드 기반 결정이 "신규 구축"으로 닫혀(PLAN-SoT §9-①) Launch 코드 감사가 불필요해졌다. 그 자리를 **지식 추출(C3·C4)**이 대신한다.

---

## 2.5 T-R · 저장소 트랙

설계 근거는 `REPO.md`. 시작점은 `CognileapAI/colab-dev-package`이고, 구성은 v2 기준으로 다시 짠다.

| WU | 내용 | 진입조건 | 완료 판정 |
|---|---|---|---|
| **R0** | 레포 결정 — `colab-dev-package` 승격 vs `colab-v2` 신설. **모노레포 권고**(`REPO.md §2`) | 없음 | 결정 로그 기록 |
| **R1** | 스캐폴드 — 디렉터리 구조 · CODEOWNERS · `main` 보호 · CI 실행 환경 · 변경 경로 필터 골격 · dev-package 문서 이관 | R0 | 빈 CI 워크플로가 1회 완주 |
| **R2** | v1 레포 **5종** archive (읽기 전용 전환) — `colab-dev-package` 포함 | C4 | archive 표시 완료 |

> **R1이 D2·D3의 그릇이다.** 계약과 게이트가 들어갈 자리가 없으면 계약 동결을 시작할 수 없다.

## 3. T-C · 정리 트랙

| WU | 내용 | 진입조건 | 완료 판정(오라클) |
|---|---|---|---|
| **C1** | 5 repo 푸시 상태 확인 — `git status` clean + `git log origin/main..HEAD` 비어 있음 + 열린 draft PR 5건 브랜치 원격 존재 | 없음 | 5개 전부 확인됨 (하나라도 로컬 전용이면 **이동 차단**) |
| **C2** | v1 이관 — PoC·Launch를 `20 CoLAB-v1/{00-poc,10-launch}` 로. 부피/임시파일 제거 → 이동 → 경로 참조 갱신 → FROZEN 배너 | C1 | 두 폴더가 `20 CoLAB-v1` 아래에 있고 원래 자리는 비어 있으며, **이동 후 `scripts/run_gates.sh` 5게이트 + pytest 가 green 으로 재현**된다 *(2026-08-23: 완료 판정이 다른 C2 행이 둘이었다. 약한 쪽을 버리지 않고 합쳤다 — 옮겼다는 사실보다 옮기고도 돌아간다는 사실이 필요하다)* |
| **C3** | **PoC 지식 추출** — `HARVEST.md`. 4포맷 디코딩 절차와 함정 · 포맷 감지 판별 규칙 · COG/타일 파라미터 · 실패 목록 | C1 | 모든 항목이 실제 파일·라인으로 도달 가능하고, **v2에서 다시 구현할 수 있을 만큼 서술**돼 있다 |
| **C4** | **v1 방법론 추출** — `METHOD.md`. 계약 게이트 6종의 구성 방식 · 멀티테넌시 3중 방어 · 세션 인계 형식 · 실패 교훈 | C1 | WU-D3이 이 문서만 읽고 게이트를 재구축할 수 있다 |

C2·C3·C4는 서로 병렬. C1만 공통 선행.

> **코드를 계승하지 않으므로 감사(재현성·계약 정합)는 하지 않는다.** 대신 C3·C4가 지식을 뽑아낸다. 추출이 끝나면 v1 폴더는 읽기 전용 참조가 된다.

## 4. T-I · 인프라 트랙 (**staging 배포가 목표** · prod 는 `㊻` 로 보류)

**배포는 마지막 단계가 아니다.** P0 직후 빈 껍데기를 한 번 올리고, 이후 모든 WU가 배포까지 포함한다.

| WU | 내용 | 진입조건 | 완료 판정 |
|---|---|---|---|
| **I0** ⏸ | 계정·결제·조직 구조 — AWS 계정/결제, 리전 확정, 예산 알람 | **출시 결정**(`㊻`) | 계정에서 리소스 생성 가능 + 예산 알람 동작 |
| **IS1** | **staging 호스트 구성** — 철거된 PoC 자리(WSL + Cloudflare Tunnel)에 v2 오리진을 세운다. 터널은 살아 있고 토큰 사본도 있으므로 **DNS·터널을 새로 만들지 않는다**. 참조 = `DEPLOY-CURRENT.md` · `reference/poc-deploy/` | 없음 (**I0 불필요** — `PLAN-SoT §9-㉓`) | `colab-hydro.com` 이 v2 오리진으로 200 응답 + 되돌리기 1회 성공 |
| **IS2** | **터널 라우팅을 레포로 끌어온다** — 현재 ingress 정본이 Cloudflare 대시보드에만 있어 레포에 재현본이 없다. IaC 로 선언하고 대시보드를 그 산출로 만든다 | IS1 | 레포에서 라우팅을 재적용해 동일 상태 재현 |
| **IS4** | **terraform state 보관** — 지금 state 가 WSL 호스트 로컬에만 있다. 레포에는 못 넣는다(비밀 포함). 호스트가 사라지면 재import 로만 복구된다. 원격 백엔드(S3+DynamoDB 등)로 옮기거나, 최소한 **복구 절차를 재현 가능한 형태로** 남긴다 | IS2 · (원격 백엔드면 I0) | state 를 잃은 상태에서 문서만 보고 복구 성공 |
| **IS3** | **staging 백업 체계** — PoC 백업 크론을 비활성화한 자리를 대체한다. **복원 리허설까지 포함한다** — 8주간 빈 백업이 성공으로 기록된 전례가 있다(`DEPLOY-CURRENT.md` 백업 사건) | IS1 | 백업 1회 + **복원 1회 성공** + 빈 백업 fixture 로 fail-closed 증명 |
| **I1** | 토폴로지 확정 + IaC 저작 — VPC·DB·오케스트레이션·스토리지·CDN·시크릿. `plan`까지 로컬 검증 | I0 | `plan` 무오류 + 비용 추정치 기록 |
| **I2** | **Walking skeleton 배포** — 5개 배포 단위(core-api·pipeline-worker·viz-render·ai-service·frontend)를 **빈 상태로** staging에 올린다. 헬스체크 + 롤백 경로 증명 | **IS1 또는 I1**, P0 | staging에서 5개 전부 헬스 green, 배포·롤백 각 1회 성공 | **⭑ 2026-08-25 — 헬스 green 뒤에 제품이 잠겨 있었다.** 다섯 자리(viz env 0건 · relay · `COLAB_AI_DB_URL` · 주체 표 · named volume 소유권)를 `W7`(`c5a2fbf`)이 이었다. **교훈을 완료 정의에 박는다 — 「헬스 200」은 배포 판정이 아니다. 단위별 본문과 실제 op 한 건이 돌아야 배포다.**
| **I3** | 배포 자동화 — 커밋 → 빌드 → staging 자동 배포. prod는 승인 게이트 | I2 | 파이프라인 1회 완주 |
| **I4** | 운영 준비 — 분산 추적 · 로그 · 알람 · 시점 복구 리허설 1회 · 데이터 레지던시 기록 | I3 | 복원 리허설 성공 |
| **I5** ⏸ | **prod 전환** | I4 + P7 + **출시 결정**(`㊻`) | prod 헬스 green + 롤백 경로 증명 |

> **I2가 이 트랙의 분수령이다.** 여기를 지나면 "로컬에선 되는데" 라는 문장이 사라진다. 이후 각 WU의 완료 판정에 `staging 배포 green`이 붙는다.

## 5. T-G · 게이트 트랙

사람이 결정하거나, 문서를 다시 만들어야 닫히는 것들. **코드와 무관하므로 지금 전부 병렬로 연다.**

| WU | 게이트 | 무엇을 막는가 | 완료 판정 |
|---|---|---|---|
| **G1** | **패키지 신선도 게이트** — 재빌드가 아니었다. 임베드 md ↔ 원본 md 를 content-hash 로 대조하는 검사기 + `planning-freshness` 게이트 | **WU-P0** | 게이트 green(15/15 일치) + selftest 가 변조·미마운트 둘 다 red 로 fail-closed 증명 |
| **G1b** | E-04 · E-02 **목업·화면** 최신성 — 문서 임베드는 G1 게이트가 이미 지킨다 | P2 · P4 | 목업이 최신 PRD·Policy 와 어긋나지 않음 |
| **G2** | 자연어 검색 엔진 결정 — 관련도 값의 형태 + 근거 한 줄 생성 방식 | **WU-P4 · K4** | 결정 로그 기록. **리드타임 최장 — 최우선 착수** **✅ 해소** — `㊶` Postgres 단독. **⟨개정 2026-08-24 — `〈81〉`⟩ stage 1 에서 `pgvector` 는 빠진다**(`postgres:16-alpine` 이 제공하지 않는다 — 실측). **stage 1 = tsvector + LLM 질의 해석** |
| **G3** | E-01 권한 — **원칙(선행) / 화면별 적용 표(후행) 분리** | P0 품질 · **G7**(정본이 지목한 "권한표 원본" `IA_사이트맵.md` 7장이 G7 전에는 존재하지 않는다 — `PLAN-SoT §9-㉛`) | 원칙 문서 확정 |
| **G4** | 미배치 화면 3건 단계 배정 — `연구실 설정 > 구성원·권한`(권한 값의 원천 → P0~P1), S-08 미등록 미리보기(P2 편입 권장), 연구실 정보 읽기 모달 | P0 · P1 | 순서표 갱신<br> **⭑ ⟨stage 1 개정 2026-08-24⟩ S-08 은 「P2 편입 권장」이 아니라 stage 2 배정이다**(`〈70〉-㉰` · `〈74〉-㉳`) |
| **G5** | DataModel v1.8 ↔ 기존 스키마 대조 — 파일 1:N, 다중 부모 계보, 프로젝트 N:N, 계보 상태 4값, 레코드 시점 3종 | **WU-P0** | diff 표 작성, 소급 항목 0 |
| ~~G6~~ | 인프라 결제 → **T-I 트랙의 I0으로 승격.** AWS 배포가 완료 조건이 되면서 게이트가 아니라 임계경로가 됐다 | — | → I0 |
| **G7** | **(G3 의 선행 — `PLAN-SoT §9-㉛`)** ver2 기획 SSOT 통합 — 260818 폴더에 없는 인덱스 5종(README·에픽_목록·IA_사이트맵·백로그·사용자_핵심_시나리오) 합치고 `에픽_목록.md`를 260818 기준으로 갱신 | 전 단계 참조 정확도 | 정본 폴더 하나로 완결 |
| **G8** | **온톨로지 범위 합의** — D9가 무엇을 아는가. 변수·좌표계·유역/행정 위계·관측소·가공 방식 어휘 중 1차 범위 | **WU-K1** | 어휘 목록 확정 (수문학 도메인 소유자 승인) |
| **G9** | **`db-boundary` 게이트** — 배포 단위가 **허용된 DB 체인 밖에 접속을 선언**하는 것을 막는다. 2026-08-25 에 `ai-service` 가 `db/platform` 에 직접 붙었는데 **모든 게이트가 green 이었다** — 횡단이 import 가 아니라 **DB 접속**이었고 `import-boundary` 는 import 만 본다 | `CLAUDE.md §3-1`·`§3-3` 이 실제로 강제되는가 | **✅ 닫힘 (2026-08-25 · `c499123` · `〈92〉`)** — 매니페스트 `gates/config/db-boundaries.toml` 이 **유일한 정의처**(코드에서 역산하지 않는다) · selftest **18** 케이스가 소스·Dockerfile·compose 세 자리의 red 를 증명 · **게이트 21 → 23** |

## 6. T-D · 도메인·계약 트랙

| WU | 내용 | 진입조건 | 완료 판정 |
|---|---|---|---|
| **D1** | 도메인 경계 확정 — `DOMAINS.md` 리뷰·승인 | G5 초안 | 10 도메인 소유자 배정 완료 |
| **D2** | 계약 동결 — seam OpenAPI(FE↔core, core↔viz, core↔ai) + async 봉투(core↔pipeline) + 공통 JSON-Schema(ULID 등) | D1 ~~A3~~ *(`A3` 는 존재하지 않는 WU 였다 — A 트랙 자체가 없다. v1 잔재 유령 참조, 2026-08-22 삭제)* | spectral + oasdiff 게이트 green |
| **D2b** | **이벤트 계약 게이트** — `contracts/events/**` 는 지금 **어떤 게이트도 보지 않는다.** `contract-lint`(spectral)는 `contracts/seams/**` 만 훑고, `contract-breaking`(oasdiff)은 OpenAPI 전용이라 이벤트의 파괴적 변경을 아무도 못 잡는다. 필요한 것 = `event-lint`(ajv 로 스키마 유효성 + 인스턴스 fixture) · `$defs` 단위 diff 규칙 · red fixture selftest | D2 | 게이트 self-test green + 이벤트 계약 변경이 실제로 red 를 냄 |
| **D3b** | **RLS 실효 증명 — 음성·양성 테스트** | D3 · **P0 스키마** | ① 허용자 아님·만료됨 두 경우 본체 조회가 **DB 층에서 0행** ② 잠긴 데이터셋의 **메타는 반드시 조회됨**(`P-13` 회귀 방지) ③ cross-tenant 조회 0행. 셋 다 red fixture 로 fail-closed 증명 |
| **D3** | 경계 강제 장치 — import-linter 계약, banned-import(geo), **D10→D4 쓰기 금지 음성 테스트**, RLS 커버리지 게이트, 스키마 diff, alembic single-head | D2 | 게이트 self-test 전부 fail-closed 증명 |
| **D5** | **인제스트·파이프라인 도메인 구현** (`pipeline-worker` 런타임 · `DOMAINS §D5`) — ⑴ **포맷 감지는 magic-byte 로 한다.** 확장자를 신뢰하지 않는다(`DR-3`) — `.hdf` 는 **HDF4**(`0e031301`)로 판정된다(`㊿-F-2`) ⑵ **지원 포맷 = `NetCDF` · `Binary` · `HDF4` · `GeoTIFF`**(`〈51〉` — **숫자가 아니라 목록으로 적는다.** PoC 의 4포맷과 수만 같고 구성이 다르다) ⑶ **헤더 파싱 = 자동 추출**(`DATAMODEL §4.1`) — 포맷·변수·기간·좌표계·격자·용량을 파일에서 읽는다. PoC 의 archive-first 구조는 **계승하지 않는다**(`DR-14`) ⑷ **좌표계 변환** — 격자가 파일 안에 없는 포맷은 **`04.Lat_Lon_info` 기준 격자 파일**을 읽는다(`〈57〉`·`〈58〉`, 0~2건). ⚠ **실측상 그 포맷은 `Binary`(HSR) 하나다** — 헤더의 투영 파라미터 자리가 전부 0 이라 재현 불가(오차 5.9 km). **HDF4·GeoTIFF·NetCDF 는 파일 내부 정보로 격자가 계산되므로 기준 격자를 강제하지 않는다**(`DATA-REFERENCE §1.1` · 정본 `DATA-PIPELINE-MEASUREMENT.md`). **못 읽으면 `[미상]` 이고 실패다 — 지어내지 않는다**(`DR-9`). ⚠ **D5 는 격자 파일의 읽기·변환만 담당한다** — 후주입 경로 자체(계약·화면)는 `D2c`·`P2` 소관이다 ⑸ **COG 변환** — 오버뷰 리샘플링을 **범주형/연속형으로 분기**한다(`DR-12`). **입력 tif ↔ 산출 COG 를 층에서 가른다**: *이미 COG 인 tif*(타일+오버뷰)와 *일반 GeoTIFF*(타일만/스트립)를 구분하고, **사람이 올린 tif 가 우리 산출물로 기록되지 않는다**(`DR-2`. PoC 에 선례 없음 — 처음부터 세운다) ⑹ **outbox/워커** — 전체 파일 메모리 적재 금지, 윈도우/스트리밍(`DR-11` — 원천에 1.3 GB 단일 묶음이 있다) ⑺ **드리프트 정리** — `services/pipeline-worker/README.md` 의 「4포맷」 표를 `〈51〉` 목록으로 고친다(`DR-1`, 소유 = 이 WU) | **없음 — 지금 착수 가능**(`03-HANDOFF §0`). 참고 입력 = `SEED-DATA.md`(S1 ✅ · 실번들 픽스처) · `HARVEST.md`(C3, **있으면 쓰고 없어도 진행한다 — 차단 아님**) | **넷 다 충족해야 닫힌다.** ① **감지·파싱 자기 증명이 red fixture 로 fail-closed** — 확장자만 바꾼 파일이 magic-byte 로 바로잡히고, **좌표를 못 찾은 파일이 「성공」을 반환하지 않는다**(합성 격자 0건 · `DR-9` 음성 시험) ② **이미-COG 판별 3부류 red→green** — *이미 COG* 6건 · *타일만* 16건 · *스트립* 40건이 각각 올바르게 갈린다(`SEED-DATA` 전수 측정분) ③ **게이트 green** — 도메인 경계 게이트(banned-import: `core-api` 에 rasterio 0 · D5 런타임 분리)가 통과 ④ **실데이터 E2E** — 지원 4종 실파일이 **각 최소 1건** 감지→파싱→좌표→COG 까지 완주하고 **실패 파일은 목록으로 남는다**(조용히 건너뛰지 않는다). ⚠ **화면·계약 왕복은 이 WU 의 완료 조건이 아니다** — 그건 `P2`·`S3` 이 판정한다<br> **⭑ ⟨stage 1 개정 2026-08-24 — `〈74〉〈75〉〈77〉〈79〉`⟩ D5 의 stage 1 범위를 다시 긋는다.** **stage 1 = ⑴ 감지 · ⑵ 지원 포맷(＋`NumPy`) · ⑷ 중 「사람이 올린 기준 격자 파일의 수용·축 판별·형상 대조」 · ⑹ outbox/워커 · ⑺ 드리프트 정리.** **stage 2 = ⑶ 헤더 파싱 · ⑷ 중 좌표계 변환 · ⑸ COG 변환.** ⭑ **그리는 일은 D5 가 아니라 D7 소유다** — 미리보기 3층은 `viz-render`(D7)가 만들고, **D5 는 격자 파일을 받아 축을 확정하는 데까지**다(`DOMAINS.md` 소유 분리). ⚠ **격자 원장 행은 축 확정 뒤에 워커가 만든다** — `0004` 의 CHECK 때문에 축 없는 격자 행은 애초에 만들어지지 않는다(`〈79〉`). **`0004` 무수정 · 마이그레이션 0건.** ⚠ **자동 짝짓기(서버 로컬 탐색)를 하지 않는다** — 사용자가 그 업로드에 직접 올린 격자만 쓴다(`〈75〉-㉴`). **축 판별은 파일명 단독으로 하지 않는다**(`〈75〉-㉲`) | **⭑ 2026-08-25 — staging 배포됨**(`c5a2fbf`·`974cf9f`, 시험 118/0). **🟧 유지** — ⓐ 저장 배치 회귀 시험 0건(`§4` #26) ⓑ `/healthz` 본문이 실이미지인데 `implemented:false`(`§4` #23).

> ✅ **`DR-13` 해소 — 제3안 (2026-08-23 사용자 승인).** 원래 선택지는 ⓐ「전부 표기 착오」 ⓑ「다섯 줄이 빠졌다」 둘이었는데 **둘 다 아니다.** 도메인마다 사정이 갈린다 — **소유자가 있고 워크로드가 분리되며 화면과 독립으로 진행 가능한 D5 만 정식 WU 로 승격**하고(위 `D5` 행), **나머지는 흡수 관계를 표기로 바로잡는다**: **`D6`(Project) → `P5` 에 흡수** · **`D7`(Visualization) → `P3` 에 흡수** · **`D4`(Lineage) → `P2`(쓰기 발생) 와 `P3`(그래프 조회) 에 흡수** · **`D8`(Insight) → `P7` 에 흡수**. **판단 근거 = D5 만 「화면 없이도 닫히는가」에 예**(고 CPU 워커 런타임이 따로 서고, 실파일만 있으면 진행된다)**이고 나머지는 아니오다** — 아니오인 것을 WU 로 세우면 P 와 동어반복인 빈 껍데기가 하나 더 생긴다. **효과 = `§7` 의 P2 진입조건이 실재하는 WU 를 가리키게 되고, 「D5 가 닫혔는가」를 판정할 오라클이 생긴다**(`sessions/P2.md §7 STOP-4` 닫힘). ⚠ **흡수는 면제가 아니다** — 흡수된 도메인의 책임은 해당 P 의 완료 정의 안에서 판정된다. `DOMAINS.md` 의 D4·D6·D7·D8 은 **경계 정의로서 그대로 유효하다.** *(`03-HANDOFF §5.5 DR-13`)*

> **D3이 이 프로젝트의 보험이다.** v1에서 실제로 터진 버그 — ID 타입 드리프트, FE-BE 타입 수동 동기화, 스코프 누락 노출 — 는 전부 "관례로 지키기로 했던 것"이었다.

## 7. T-P · 플랫폼 트랙 (에픽 0~7단계)

| WU | 단계 | 내용 | 주 도메인 | 진입조건 |
|---|:--:|---|---|---|
| **P0** | 0 | 공통 기반 — 스키마 정본 · 공통 셸 · GNB(첫 탭 `연구실`) · 권한 게이트 틀 · 비워 둘 자리 3곳(Verified 배지·잠금 표시·할 일 함) | D1 D2 (+D3 · D4 D6 은 **스키마 정본만** — WU 아님) | **G1 · G5 · D3** |
| **P1** | 1 | 카탈로그(S-03) + 데이터셋 상세 헤더·기본 정보(S-05 상단) + **`연구실 설정 > 구성원·권한`**(권한 값의 원천, E-01 · WU-G4) — *읽을 화면을 먼저 세우고, **권한 값의 원천 하나만 예외로 함께 연다*** | D3 D2 D1 | P0 |
| **P2** | 2 | 업로드 전체화면 모달(S-04) · S-08 미등록 미리보기 · 계보 확정 — *여기서 계보 데이터가 처음 생긴다* · **＋ 미리보기 최소 렌더 경로**(`〈63〉-㉮` 로 P3 에서 넘어왔다 — `createRender`·`getRender`·`getRenderTile`·`listPalettes` + core-api 중계 `createPreviewRender`·`getPreviewRender`. **미리보기는 서버가 그린다**고 정본이 화면 규칙으로 못박았는데(`Policy_업로드와_계보_확정 §8`) 그리는 쪽이 P3 에 있어 **P2 화면이 요구하는 것을 아무도 못 만드는 자리**가 생겼다) | **D5**(WU · `§6`) · D4 (P2 에 흡수 — 계보 쓰기) · **D7 일부**(미리보기 렌더 경로만. 나머지는 P3) | P1, G4 · (AI 지점은 K3)<br> **⭑ ⟨stage 1 개정 2026-08-24 — `〈70〉`·`〈74〉`⟩ P2 는 stage 1 · 축소다.** **나가는 것** = S-08 화면 전체 · 타일 서빙·서명 · 팔레트 선택 재렌더 · `createScreenshot` · COG 변환 · 값 조회. **남는 것** = S-04 ①② · 업로드 3 op · 계보 3 op + Lv 파생 · `d5_*` 원장 · outbox/워커/reaper · 감지 · 계보 확정 모달 ③. **더해지는 것** = **미리보기 3층 · 미리보기 중계 2 op · 격자 3 op · 격자 요청 블록 · 축 판별 사다리 · 좌표 정합 확인 화면 · 뒤집기 · `.npy` 지원.** 집행 계획 = `sessions/S1-PLAN-REFOUND.md` | **⭑ 2026-08-25 — 완료 정의에 둘이 붙는다.** ⓐ **저장 배치를 단언하는 시험**(접수 ↔ 워커가 같은 자리를 본다는 것을 한 자리에서 못 박는 회귀 시험). 2026-08-25 에 두 파일의 규칙이 갈라졌는데 시험 0건이라 **배포에서만 났다**(`03-HANDOFF §4` #26). ⓑ **`addDatasetFile` 202 의 저장 자리** — 계약은 열렸으나 축 미정 격자를 둘 원장 행이 없다. **스키마 판정(Ted) 전에는 P2 를 닫지 않는다**(`§4` #21).
| **P3** | 3 | 계보 그래프 · **데이터셋 상세의 2D 시각화 3종**(격자·경계·점) · `createScreenshot` · 렌더 표현 확장 | D4 · D7 (둘 다 P3 에 흡수 — WU 아님. **단 D7 의 미리보기 렌더 경로는 P2 로 빠져나갔다**) | P2<br> **⭑ ⟨stage 1 개정 2026-08-24 — `〈74〉`⟩ P3 은 stage 2 전체다.** 「D7 의 미리보기 렌더 경로는 P2 로 빠져나갔다」는 문장은 **여전히 유효하다** — 미리보기가 stage 1 로 되돌아왔기 때문이다(`〈74〉-㉯`). P3 에 남는 것은 **계보 그래프 · 확대/타일 뷰 · `createScreenshot` · 렌더 표현 확장**이다 |
| **P4** | 4 | 검색 히어로(S-01) · 검색 결과(S-06) | D3 | P1, **G2**, K4 | **⭑ 2026-08-25 — 화면은 섰고**(`15847b3`) **staging 에서 실결과를 낸다**(`강우` 3건 · `degraded:false`). **닫지 않는 이유** = 완료 판정의 오라클인 **검색 평가셋이 아직 초안**이다(`S2b`). `SEARCH_UNAVAILABLE` — **폴백 없음**이 계약이다(`〈87〉`).
| **P5** | 5 | 프로젝트 목록·상세(S-02/S-02b) → 완료 후 E-03 섹션3 채움 | D6 (P5 에 흡수 — WU 아님) | P1 | **⭑ 2026-08-25 — 화면 셋 산출 완료**(`〈84〉` · 501 표 24 → 21). **🟧 로 남긴다** — P6(승인 표시)과 `§1.5` 완료 조건이 아직 이 화면들을 안 지난다.
| **P6** | 6 | 승인 처리 — Verified 배지·우선 정렬·접근 요청 모달·잠금 표시를 앞 화면 7곳에 얹기 | D2 | P1~P5 |
| **P7** | 7 | 연구실 대시보드 — 데이터 맵(계보 상태별·주제별) · 할 일 함 · 최근 활동 · **연구실 정보 읽기 모달**(`우리 연구실` 라벨 · 읽기는 전 구성원, 편집 버튼만 권한자 · WU-G4) | D8 (P7 에 흡수 — WU 아님) · D1 | P6 |
| **P8** | 후 | E-01 화면별 적용 지점 표 → 정책·패키지 재생성 | D2 | P7 |
| **PA-G** | 후 | **구글 IdP 어댑터 — 정본이 적은 유일 로그인 수단**(`〈108〉-㉮`). `PA` 의 비밀번호 어댑터를 **대체**한다. ⚠ **비밀번호 어댑터는 임시 수단이다** — `〈107〉`·`〈108〉` 로 어드민 계정＋비밀번호가 섰으나 정본은 구글을 유일 수단으로 적었다 | D1 | **⭑ 진입조건 = stage 1 완료** (`PLAN-SoT §9 〈122〉` · Ted 2026-08-26 ⑺). 자격 발급을 stage 1 판정의 앞에 두지 않는다 — 발급 대기가 stage 1 을 막으면 안 된다.<br>**⭑⭑ 교체 기한 = stage 1 완료 판정과 동시에 이 WU 를 연다.** `〈122〉-㉯` 가 요구한 기재다 — **기한이 문서에 없으면 임시 수단이 정착한다.** 비밀번호 어댑터는 이 WU 가 닫히는 시점에 **제거**한다(공존시키지 않는다). ⚠ **이 줄이 그 기한의 정본이다** — 다른 곳에 적지 않는다(두 곳에 적으면 갈라진다) |
| **PA** | — | **로그인 · 세션** — 로그인 화면 · 세션 발급·검증 · 로그아웃 · FE 요청 첨부. **범위 확대이며 정본 개정 위에 선다**(`PLAN-SoT §9 〈107〉` · Ted 2026-08-26 지시). ⚠ **stage 1 완료 판정과 분리된 별도 WU 다** — 이 WU 의 상태가 stage 1 의 완료 여부를 바꾸지 않는다. **회원가입·초대·프로필·승계는 이 WU 가 아니다** — P1 에 그대로 남는다(`〈107〉-㉵`) | D1 | P0 (스코프 커널·주체 표 · 실동작 `GET /me`) |

> **PA 완료 정의** — ① 로그인 성공·실패·미인증 401·만료·로그아웃 후 401 이 **실동작 시험으로 green** ② **cross-tenant 음성** — A 주체 세션으로 B 연구실 자원 조회가 **404** ③ **기존 주체 표 병존** — 심어 둔 토큰 경로가 그대로 통한다(`〈107〉-㉱`) ④ **마이그레이션 0**(`〈107〉-㉯`) ⑤ 계약 게이트·`generated-up-to-date`·`seam-consistency` green ⑥ FE 는 미인증 시 로그인 화면을 보이고 **가짜 진행을 그리지 않는다** ⑦ 인증 세부가 **교체 지점 한 곳**에만 있다(아래 소유 디렉터리).
> **소유 디렉터리** — `services/core-api/src/colab_core/kernel/authn.py`(교체 지점) · `.../kernel/session_token.py` · `.../app/routes/session.py` · `frontend/src/auth/`.
> **진입조건 미충족 시 구현하지 않는다** — `CLAUDE.md §4`.
> **이 WU 에 staging 배포는 포함되지 않는다** — 별도 회차다.


> ✅ **`〈63〉-㉮` 반영 (2026-08-23 Ted · `PLAN-SoT §9`) — D7 의 절단선을 P2 와 P3 사이에 다시 그었다.**
> **P3 에서 P2 로 빠져나간 것**(넷 + 중계 둘) — `createRender` · `getRender` · `getRenderTile` · `listPalettes`, 그리고 core-api 중계 `createPreviewRender` · `getPreviewRender`. **함께 닫힌 미결** = `NB-7`(격자 파일 지도 렌더) · `listPalettes` FE 중계 부재(`D2c` C1 열린 항목 ①).
> **P3 에 그대로 남은 것** — 계보 그래프 · **데이터셋 상세의 2D 렌더 3종**(격자·경계·점) · `createScreenshot` · 표현 종류 확장 · 렌더 성능·상한 조정.
> **왜 옮겼는가** — 범위 확대가 아니라 **잘못 뭉친 배정의 정정**이다. 정본이 S-04·S-08 화면 규칙으로 **서버 렌더**를 요구하는데 D7 이 통째로 P3 에 있었고, **P3 은 P2 를 진입조건으로 삼는다.** 그대로 두면 P2 는 미리보기 없이 닫히거나(`CLAUDE.md §5` 「부분 완료로 닫지 않는다」 위반) P3 이 자기 진입조건을 스스로 막는다.
> ⚠ **P3 은 이만큼 작아졌다** — P3 을 착수할 때 **여기 적힌 「빠져나간 것」을 다시 자기 범위로 세지 않는다.** 반대로 **advisor 는 이 흡수에 반대하고 별도 WU 절단을 권고했다**(「렌더 4포맷+타일+팔레트는 사실상 P3 의 심장이다」). 그 우려는 `sessions/P2-EXEC.md §8` 에 위험 1번으로 살아 있고, **`P2-viz` 가 예상보다 커지면 범위를 줄이지 말고 멈추고 절단을 재론한다.**

> **진입조건에 쓰인 `D#` 두 종류를 갈라 읽는다.** **`§6` 에 행이 있는 작업 단위**(D1·D2·D2b·D3·D3b·**D5** — 닫힘 판정 오라클이 있다)와, **도메인 경계 참조**(D4·D6·D7·D8 — `DOMAINS.md`. 해당 P 에 흡수돼 있고 별도로 닫히지 않는다). `DR-13` 해소(2026-08-23)로 이 구분이 명시됐다.

병렬 가능: **P4와 P5는 서로 독립**(둘 다 P1만 필요). P3은 P2 이후. P6은 합류 지점.

**각 P의 완료 판정 (I2 이후 공통)** — 넷 다 충족해야 닫힌다.

1. 해당 단계 스토리의 수용 기준 통과
2. 도메인 게이트 green (계약·경계·스키마·RLS)
3. **staging 배포 green** — 로컬 green은 완료가 아니다
4. 목업 대비 화면 검증

## 8. T-K · 지식·AI 트랙

D9·D10. **계약(D2)만 동결되면 T-P와 완전히 병렬로 진행된다.**

| WU | 내용 | 진입조건 | 완료 판정 |
|---|---|---|---|
| **K1** | 온톨로지 스키마 — 개념 유형·관계 유형·동의어 구조. 저장소는 플랫폼과 **분리된 스키마** | G8 | 스키마 정의 + 예시 인스턴스 왕복 |
| **K2** | 온톨로지 시드 적재 — 변수·좌표계·유역/행정 위계·관측소·가공 방식 어휘 | K1, G8 | 시드 커버리지 체크 (핵심 어휘 미커버 0) |
| **K3** | **계보 제안 서비스** — 업로드된 파일의 메타 + KG 조회 → 가공 전 데이터 후보 + 가공 방식 제안. **확신도 3단계(확실/애매/모름), 근거 동반, 못 찾으면 정직한 빈 상태** | K2, D2 | 평가셋 대비 제안 품질 + **D4 쓰기 경로 부재 음성 테스트 green** |
| **K4** | **자연어 검색 서비스** — 질의 해석 → 관련도 값 + 근거 한 줄. 뒤진 범위를 먼저 밝히고, 잠긴 것도 노출 | K2, **G2**, D2 | 평가셋 대비 재현율/근거 정확도 **⭑ ⟨개정 2026-08-24⟩ 2층으로 갈렸다**(`〈71〉-㉱`) — **`K4-a`** tsvector 층(`G8b` 무의존, `W4`) ∥ **`K4-b`** 그래프 증강(`G8b` Q1 대기, `W5`). **임베딩·pgvector 는 stage 1 밖**(`〈81〉`). 우선순위 = 그래프가 임베딩보다 먼저(`〈82〉`). ⚠ **완료 정의의 「평가셋 대비」는 평가셋이 0건이라 아직 판정 불가**(`03-HANDOFF §4` 블로커 #13) | **⭑ 2026-08-25 — 서비스가 섰다**: `K4-a` 실행기가 **core-api 로** 옮겨졌고(`〈87〉`), `K4-b` 가 그래프로 넓힌다 — **D9 가 넓히고 core-api 가 찾는다**(`〈90〉`), 한국어는 **접두 + `pg_trgm`**(`〈89〉`). **완료 판정은 그대로다** — 평가셋 대비 재현율. **평가셋이 초안인 동안은 닫히지 않는다.**
| **K5** | 제안 원장 — 제안·확인·수정·거절 이력. 모델 교체 시 회귀 판정의 근거 | K3 | 원장 왕복 테스트 |

**KG가 두 지점 모두의 토대다.** K3·K4를 각각 따로 만들면 의미 체계가 두 벌 생긴다 — 그래서 K1·K2가 선행이다.

## 8.5 T-S · 초기 데이터 트랙 (`03 Reference-Data`)

**v2 셋팅이 끝났다는 것은 빈 화면이 뜬다는 뜻이 아니다.** 사람이 들어가서 **볼 것이 있어야** 끝이다.
`DEPLOY-CURRENT §6` 이 "PoC 데이터는 전량 폐기하고 v2 초기 데이터는 Ted 가 별도 제공"으로 못 박았고, **그 별도 제공물이 이제 실재한다** — `F:\00_Project\00 CoLAB\03 Reference-Data`.

그동안 이 원천은 어느 WU 에도 없었다. 완료 정의 7번(`§1.5`)이 "실데이터 E2E 완주"를 요구하면서 **그 실파일이 어디서 오는지는 비어 있었다.** 그 자리를 이 트랙이 채운다.

### 원천 실측 (2026-08-23)

총 **3.5 GB · 2,122 파일**.

| 경로 | 내용 | 규모 |
|---|---|---|
| `01.level-data/01.precipitation` | 강우·강수 | 40 파일 · 103 MB |
| `01.level-data/02.vegetation` | 식생·NDVI | 80 파일 · 347 MB |
| `01.level-data/03.drought-…` | 가뭄 | 5 파일 · 49 MB |
| `02.File-format/file_format_1_grib` | GRIB | 39 파일 · 49 MB — ⚠ **원파일 0건**(파생 `.npy`·`.png` 뿐) |
| `02.File-format/file_format_2_nc` | NetCDF | 157 파일 · 59 MB |
| `02.File-format/file_format_3_bin` | Binary | 47 파일 · 689 MB |
| `02.File-format/file_format_4_tif` | GeoTIFF | 27 파일 · 885 MB — ⚠ **tif 6건 전부 이미 COG** |
| `02.File-format/file_format_5_HDF5` | HDF5 | 86 파일 · 1.3 GB — ⚠ **실체는 HDF4**(매직 `0e031301`) |
| `3_KWRA_conference-…` | 학회 산출물(대부분 `.npy`·`.png`) | 1,639 파일 · 116 MB |

> ✅ **`tif` 는 입력 포맷이다** (`㊿`). **`GRIB` 는 범위에서 뺐다** (`〈51〉`) — 원천에 원파일이 0건이다.
> **D5 지원 포맷 = 4종: `NetCDF` · `Binary` · `HDF4` · `GeoTIFF`.** ⚠ PoC 의 4포맷과 **수만 같고 구성이 다르다**(겹치는 것은 NetCDF·Binary 둘뿐이고 HDF 는 버전이 다르다).
> 단, GeoTIFF 는 v2 에서 **파이프라인의 출력**(COG)이기도 하다. 그래서 **입력 tif 와 산출 COG 를 층에서 갈라 두어야 한다** — 포맷 감지가 *이미 COG 인 tif*(overview·타일 구조 있음)와 *일반 GeoTIFF* 를 구분해야 하고, 계보에서 **사람이 올린 tif 를 우리가 만든 COG 로 착각하면 계보가 거짓이 된다**. S1·D5 가 이 구분을 명시한다.
> ✅ **`.npy` 1,622 개는 넣지 않는다** (`㊿`). 학회 폴더의 중간 산출물이고 포맷 목록에 없다. **제외이지 삭제가 아니다** — 원천에는 그대로 둔다.
>
> ⚠ **PoC 가 실증한 것은 여전히 4포맷이다.** C3(`HARVEST.md`)·`PLAN-SoT §7` 의 「4포맷」은 **과거 사실**이라 고치지 않는다. **GeoTIFF 만 PoC 선례 없이 v2 에서 새로 여는 경로**이고, 그만큼 D5 에서 함정 목록이 비어 있다.

### 작업 단위

| WU | 내용 | 진입조건 | 완료 판정(오라클) |
|---|---|---|---|
| **S1** | **원천 인벤토리와 적재 범위 확정** — 위 실측을 파일 단위로 내리고, 각 묶음을 ① 초기 데이터로 넣는다 ② 시험용으로만 쓴다 ③ 넣지 않는다 로 분류. **`tif`·`.npy` 는 `㊿` 로 이미 닫혔다**(5번째 포맷으로 받는다 · `.npy` 제외) — S1 은 그 결정을 **파일 단위 배정으로 집행**하고, **입력 tif ↔ 산출 COG 구분 규칙**을 기재한다. 데이터셋 경계(무엇이 데이터셋 하나이고 무엇이 그 안의 조각인가)·주제 4값 매핑·연구실 귀속을 기재 | 없음 (**지금 착수 가능**) | 인벤토리 문서 `dev-package/SEED-DATA.md` 가 존재하고, **2,122 파일 전부가 세 분류 중 하나에 배정**돼 미분류 0. **`.npy` 1,622 건이 「제외」로 배정**돼 있고, **`tif` 62 건이 「입력 포맷」으로 배정**되며 그중 *이미 COG 인 것*이 따로 표시됨 |
| ↳ S1 실적 | **✅ 닫힘 (2026-08-23)** — `SEED-DATA.md` 작성. 배정 **① 넣는다 292 · ② 시험용 148 · ③ 제외 1,682 = 2,122, 미분류 0** *(`〈57〉` 정정 — `04.Lat_Lon_info` 의 좌표 기준 격자 `.npy` 14건을 ③→① 로 옮겼다. 확장자로 싸잡은 제외 규칙이 **원자료의 일부**를 삼켰던 것이고, 이 파일들은 `DR-9` 가 「지어냈다」고 지적한 좌표의 실물이다)*. 데이터셋 15건·계보 3갈래·온톨로지 대조 완료. **마지막 빈칸이던 연구실 귀속이 `〈52〉` 로 확정** — **고려대학교 수문학연구실(전창현 교수)**. 시드 15건 전부의 `lab_id` 가 여기를 가리킨다 | — | — |
| ↳ S2 실적 | **⛔ 미실행 (2026-08-26 야간 레인)** — **적재는 한 건도 하지 않았다.** 막은 것 둘 = `03-HANDOFF §4` **#29**(`subjects.json` 읽기가 하네스에 거부돼 staging API 인증 불가 — **우회하지 않았다**) · **#28**(staging 에 `d3_dataset` **0**, 백업 8종 전부 0, 고아 D2·D6·D8 행만 잔존 — 2단 관측치 고정의 전제가 깨졌다). **실행분 = 절차 1단뿐** — 백업 두 체인 GREEN · 복원 리허설 GREEN(다이제스트 일치, 소요 0 초). **산출 = 적재 도구 `infra/staging/load-seed.py` ＋ 시험 16건**(표준 라이브러리 · mock HTTP 서버 · staging 무의존, red→green + 변이 3종 전건 검출로 fail-closed 증명). 도구는 공개 API op **4건**(`listDatasets`·`createUpload`·`createDataset`·`attachUploadGridFiles`)만 부르고 **DB 드라이버를 import 하지 않는다**(`㊾-③` · `§14.4` R-2). **완료 조건 3항 = 전항 `[미확인]`** — ②멱등은 **mock 서버에서만** 성립했고 staging 실측이 아니다, ③은 **적재 전 상태로만** 통과다. **매니페스트 미작성** — 기준 격자 16건에 적재 제외분(`D-14`)·범위 밖(GRIB) 격자가 섞여 데이터셋 귀속을 지어내야 하고 그것은 `㊴-②` 저촉이다 | — | — |
| **S2** | 🟧 **열린 채로 둔다 — 2026-08-26 마감 회차 갱신.** 파일 수 판정이 본체 기준으로 바뀌어(`〈119〉`) **응답 계수가 129 → 123** 이 된다(저장 열은 총수 129 유지). **⚠ 미배포** — staging 은 아직 이전 값을 낸다. 설명 결손 2건은 `〈127〉` 실행 대기. **부분 완료로 닫지 않는다.** ／ 이전 🟧 **2026-08-26 집행 — 적재는 실행됐고 부분 미충족으로 남는다** (`PLAN-SoT §9 〈109〉`). 실측 = **데이터셋 11 · 파일 122 · 계보 간선 5**. 완료 조건 **① 충족**(카탈로그 11건 표시 · 상세 열림) · **② 조건부 충족**(재실행 11건 전건 건너뜀 · 쓰기 호출 0 · 계수 불변 — ⚠ **격자 항목을 뺀 보조 측정**이다. 본 매니페스트로는 ⛔ #31 이 도구를 중단시킨다) · **③ 미실행**(적재 후 IS3 복원 리허설). **파일 계수가 계획치 139 에 11 미달이고 반올림하지 않는다** — 격자 6 미부착(⛔ #31) ＋ 배정처 없음 5. **절차 5단(픽스처 철거) 미실행** — 비가역이라 별도 회차. 신규 산출 = `infra/staging/provision-lab.sh`·`.sql`·`manifest-s2.json` |
| **S2** | **초기 데이터 적재** — S1 이 "넣는다"로 분류한 것을 staging 에 올린다. **화면이 쓰는 경로 그대로 간다**(업로드 모달 S-04 → 파싱 → COG → 계보 확정). DB 직접 INSERT 로 넣지 않는다 | **S1 · P2**(업로드·계보) · I2 | ① staging 카탈로그(S-03)에서 적재분이 **보이고** 상세(S-05)가 열린다 ② **재실행 멱등** — 두 번 돌려도 데이터셋 수가 늘지 않는다 ③ **IS3 백업·복원 리허설을 적재 후 상태로 1회 재통과**<br> **⭑ ⟨stage 1 개정 2026-08-24⟩ stage 1 판 경로 = 「S-04 → 감지 → 등록 → 미리보기 → 계보 확정」.** **파싱 → COG 는 stage 2 다**(`〈70〉-㉯` · `〈74〉`) | **⭑ 2026-08-26 계수 갱신 (`〈104〉`)** — 적재 대상 = **데이터셋 11**(`D-01`~`D-09`·`D-13`·`D-15`) · **파일 139** · **계보 간선 5**. 완료 판정의 계수 대조값이 이 셋이다(`sessions/S2-EXEC-PLAN.md §5.3`·`§7.1`). 종전 값 15·292·5 중 앞의 둘이 내려갔다 — 주제 확정 곤란 4건(`D-10`·`D-11`·`D-12`·`D-14` · 파일 153)이 **적재 제외 · 원천 보존 · stage 2 재판정**으로 판정됐다. 배정 대조식 = **139 + 153(`①-b` 재판정 대기) + 148 + 1,682 = 2,122**, 미분류 0 유지. **⚠ 2026-08-25 — 진입조건이 하나 늘었다.** staging 이 지금 **시험 픽스처로 시드돼 있다**(A/B 두 연구실). 실적재 전에 **픽스처 철거 시점을 판정**해야 한다 — 걷어내면 현재의 검색·미리보기 관측치가 전부 무효가 된다(`03-HANDOFF §4` #22). 적재 경로는 그대로 **화면만**(`㊾-③`).
| **S2b** | 🟧 **열린 채로 둔다 — 2026-08-26 마감 회차 갱신.** 기대값을 12건 기준으로 개정했고(`PLAN-SoT §9 〈117〉` · `Q-06` 6→7 · `Q-09` 5→6, **대상 집합 변경분 한정**) A층 재판정 = **통과 8 · 실패 8 · 층 밖 2**. **부분 완료로 닫지 않는다** — 완료 정의가 「평가셋 대비 판정」인데 실패 8 이 남는다. 종속 = `〈127〉` `updateDataset`(설명 결손 2건) · `〈89〉` 매칭 규칙(`Q-01` 하나, `〈118〉`). ／ 이전 🟧 **2026-08-26 평가셋 첫 실측 — 통과 7 · 실패 11 · `[미확인]` 0** (`03-HANDOFF §4` ⛔ **#33**). **기대값을 한 줄도 고치지 않았다**(`S2b-SEARCH-EVALSET §3-2`). ★ 회귀 게이트 5 중 `Q-01`·`Q-04` 실패. 통과 = `Q-02`·`Q-03`·`Q-10`·`Q-17`·`Q-18`·`Q-19`·`Q-20`. **`§0` 의 `[미확인]` 18칸이 이 실측으로 채워졌다** |
| **S2b** | **설명 문구·검색 평가셋** — 데이터셋 설명 **15건 작성 / 11건 적재** + 검색 평가셋 **18건** *(2026-08-26 `〈104〉` 갱신 — 종전 「설명 15 + 평가셋 20」)*. **작성 방식 = Claude 초안 → Ted 줄 단위 승인** *(2026-08-25 Ted)*. **검색 레인이 자기 숙제를 자기가 채점하지 않는다** — 기대값은 정본·판정에서만 도출하고, 검색을 돌려 보고 거꾸로 적지 않는다. 설명은 **원천 문서에서 옮겨 쓰고, 근거가 없으면 비운다**(`㊴-②` — 지어낸 설명은 색인되고 검색된다) | S1 · `〈72〉` | **✅ Ted 승인 완료 (2026-08-26 · `〈104〉`)** — 설명 15건 일괄 승인(원천 근거 11 · 실측 도출 4). **그중 4건은 적재 제외**(`D-10`·`D-11`·`D-12`·`D-14`) — 설명은 문서에 보존하고 stage 2 재판정 시 재사용한다. **평가셋은 대상 소멸 3건 삭제(`Q-15`·`Q-16`·`Q-16b`) · 기대값 수정 3건 · 축 이동 1건으로 18건**, 축 구성 = A 4 · B 3 · C 5 · D 4 · E 2. **기대값은 정본·판정에서 재도출했고 검색 실행 후 역산하지 않았다.** ⚠ **잔여 결손 1건** — 적재 11건 중 `topic IS NULL` 이 0건이라 `〈72〉` 의 「주제 빈 상태 표현」 오라클이 시드에 없다(`〈105〉` · 열린 것 `⑳-b`) |
| **S3** | **실데이터 E2E 완주** — `§1.5` 완료 조건 7. **지원 4종**(NetCDF·Binary·HDF4·GeoTIFF, `〈51〉`) 실파일이 업로드→파싱→COG→시각화→계보 확정까지 끊김 없이 간다 | S2 · P3 *(**GRIB 차단 해제** — `〈51〉` 로 범위에서 뺐다)* | **4종 각각 최소 1건**이 **시각화 화면에 그려지고** 계보가 확정 상태로 남는다. **GeoTIFF 를 가장 먼저 돌린다** — *근거 정정(2026-08-23): 「PoC 선례가 없어서」가 아니다. 디코드 선례는 있다(`viz-service/app/decoders/geotiff.py`). **없는 것은 인제스트·COG 경로와 이미-COG 판별**이고, 원천의 tif 6 건이 **이미 COG** 라 `DR-2` 가 **이 포맷에서만 발현된다.** 결론은 살고 근거가 바뀌었다.* 실패 파일은 목록으로 남긴다(조용히 건너뛰지 않는다)<br> **⭑ ⟨stage 1 개정 2026-08-24⟩ S3 전체가 stage 2 로 이관된다** — 파싱·COG 완주가 stage 1 완료 조건이 아니기 때문이다. ⚠ **다만 지원 포맷 목록은 `〈77〉` 로 `NetCDF · Binary · HDF4 · GeoTIFF · NumPy` 가 된다** — S3 이 열릴 때 그 목록으로 판정한다 |

> **왜 DB 직접 적재를 금지하는가.** 직접 INSERT 로 채우면 화면은 채워지지만 **파싱·좌표계·COG·계보가 한 번도 안 돌아본 채로 green 이 된다** — v1 에서 "로컬 green" 을 완료로 착각한 것과 같은 종류의 거짓 완료다. 초기 데이터를 넣는 행위 자체가 **파이프라인의 첫 실사용 시험**이어야 한다.
>
> **왜 S1 을 지금 여는가.** S1 은 코드가 아니라 분류다. P2 를 기다릴 이유가 없고, 오히려 **S1 의 데이터셋 경계 결정이 P2 업로드 화면의 실입력**이 된다.

---

## 9. 의존 그래프 (전체)

```
   착수 가능 (전부 병렬)
   ─────────────────────
   R0 ── R1 ──────────────────────┐
                                  │
   C1 ──┬── C2                    │
        ├── C3  (PoC 지식)        │
        └── C4  (v1 방법론) ──────┼───────┐
                                  │       │
   I0 ── I1 ──────────────────────┤       │
                                  │       │
   G1 G2 G3 G4 G5 G7 G8 ──────────┤       │
                                  ▼       ▼
                            D1 ── D2 ──── D3
                                           │
              ┌────────────────────────────┴────────────────┐
              ▼                                             ▼
   P0 ──┬─ P1 ─┬─ P2 ─ P3 ─┐              K1 ─ K2 ─┬─ K3 ─ K5
    ▲   │      ├─ P4 ──────┤                        └─ K4
 G1,G5,D3│     └─ P5 ──────┤                            ▲
         │                 ▼                           G2
         └── I2 ── I3 ── I4 ── P6 ─ P7 ─ P8 ── I5(prod)
             (walking skeleton, P0 직후)

   S1 ────────────────── S2 ── S3     (S1 은 지금 착수 가능 · S2 는 P2 필요 · S3 은 P3 필요)

   P2 ← K3 소비 (계보 제안)      P4 ← K4 소비 (검색)
   S2 ← P2 필요 (업로드·계보 경로로만 적재)   S3 ← P3 필요 (시각화)
   I2 이후: 모든 WU의 완료 판정에 staging 배포 green 포함
```

## 10. 지금 착수 가능한 WU

진입조건이 이미 충족된 것들. **전부 서로 병렬이다.**

| WU | 왜 지금인가 |
|---|---|
| **D2c** 계약 개정 (`〈54〉` 이행) | **다음 WU.** `DR-7` 이행 — 이것이 닫혀야 `P2.md STOP-1` 이 닫힌다. **선행 = `DR-13` 해소**(`03-HANDOFF §5.5`). 지시서 `sessions/D2c.md` |
| **D5** 포맷 처리 준비 | `DR-1`~`DR-3` 소유. 포맷 5종→4종+GeoTIFF(`㊿`·`〈51〉`). 입력 = `DATA-PROCESSING-HARVEST.md` |
| **I3** 배포 자동화 | 지시서 초안 `sessions/I3.md` 있음 — 실행 전 `[정본 무근거]` 7건에 **Ted 답 대기** |
| ~~**`generated-up-to-date` 게이트**~~ | **✅ 해소** — 구현됐고 green(2026-08-24 실측). **게이트는 이제 23 종이고 2026-08-25 기준 전부 green**(`db-boundary` 신설 · `〈92〉`) |
| **IS4 잔여** | 마지막 apply(실 터널) + 맨몸 호스트 조건 리허설 |
| ~~**R0** 레포 결정~~ | **✅ 닫힘** — `colab-v2` 신규 모노레포 |
| **I0** AWS 계정·결제·리전 | ⏸ **보류** (`㊻`) — 임계경로에서 빠졌다. staging 은 IS1 이 대신하고, 실제로 I0 가 막고 있던 트랙은 없었다 |
| ~~**G2** 검색 엔진 결정~~ | **✅ 해소** (`㊶`) — Postgres 단독. P4·K4 차단 해제 |
| ~~**G8** 온톨로지 범위 합의~~ | **✅ 닫힘** (`㊸`) — `ONTOLOGY-SCOPE.md` 승인 완료 |
| **C1** 5 repo 푸시 확인 | 이관의 차단 조건. 확인 자체는 짧다 — 단, T-C 는 지금 열지 않는다(`03-HANDOFF` 헤더) |
| ~~**G7** 기획 SSOT 통합~~ | **✅ 닫힘** — 정본 폴더 하나로 완결 |
| ~~**G1** E-00 패키지 재빌드~~ | **✅ 닫힘** — 재빌드 불요, 최신성 게이트 `planning-freshness` 산출 |
| **C3 · C4** 지식·방법론 추출 | D5(포맷 처리)의 입력 — T-C 라 지금 열지 않는다. D3 은 이미 ✅ |
| ~~**S1** 초기 데이터 인벤토리~~ | **✅ 닫힘** (2026-08-23) — `SEED-DATA.md`. 배정 2,122 미분류 0 · 연구실 귀속 확정(`〈52〉`) |

## 11. 완주 체크리스트

`1.5 완료 정의`의 조건을 WU로 환원한 것. **⏸ 를 뺀 전부가 ✅ 여야 v2가 끝난 것이다.**
**⏸ = 아직 열지 않기로 결정한 것**(대기가 아니다). ⬜ 와 구분한다 — ⬜ 는 착수 후보에 오르고 ⏸ 는 오르지 않는다.

```
저장소  R0 ✅  R1 ✅  R2 ✅
에픽    P0 ✅  P1 ✅  P2 ✅  P3 ✅  P4 ✅  P5 ✅  P6 ✅  P7 ✅  P8 ✅
지식AI  K1 ✅  K2 ✅  K3 ✅  K4 ✅  K5 ✅
경계    D1 ✅  D2 ✅  D2b ✅  D3 ✅  D3b ✅
인프라  I2 ✅  I3 ✅  I4 ✅          ← v2 완주에 필요한 것
        I0 ⏸  I1 ⏸  I5 ⏸          ← prod. `㊻` 로 보류 — 출시 결정 시 연다
staging IS1 ✅  IS2 ✅  IS3 ✅  IS4 ✅
정리    C1 ✅  C2 ✅  C3 ✅  C4 ✅
게이트  G1 ✅  G1b ✅  G2 ✅  G3 ✅  G4 ✅  G5 ✅  G7 ✅  G8 ✅
초기데이터 S1 ✅  S2 ✅  S3 ✅      ← 03 Reference-Data 적재 · 화면에서 보임
실데이터 **4종**(NetCDF·Binary·HDF4·GeoTIFF) E2E 완주 ✅  (= S3 · `〈51〉`)
```

---

## 12. 진행 표기 규칙

`03-HANDOFF.md` 에는 **WU 단위로만** 적는다. "며칠 걸린다", "몇 주차"는 쓰지 않는다.

```
✅ 닫힘   🟦 진행  ⬜ 대기  🟧 부분(사유 필수)  ⛔ 차단(무엇이 막는지 명시)
```
