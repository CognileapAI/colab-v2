# 03 · HANDOFF — 진행 상태 추적기 (단일 진실원)

> **이 문서가 "지금 어디까지 왔는가"의 진실원.** 매 세션 끝에 갱신한다(규약 `01-CLAUDE-DRIVER.md §C`).
> 새 세션은 여기부터 읽는다. 코드·인프라와 모순되면 실제 상태를 점검해 **이 문서를 먼저 바로잡는다.**

**최종 갱신** 2026-08-26 (**★ 워커가 연구실 하나에 묶여 있던 것을 풀었다 — 건별 스코프. `#31`·`#32` 해소.** Ted 판정 ㈑ 실행 — 워커는 대상 연구실을 원장에서 읽고 **연구실마다 제 트랜잭션 · 제 스코프**로 돈다. **한 번에 하나의 연구실 스코프만 갖는다.** 기각 3건(㈎ 연구실 C 고정 · ㈏ 연구실별 워커 · ㈐ 제한 제거)의 사유는 `PLAN-SoT §9 〈110〉-㉮`. ⚠ **판정문 문언과 한 곳이 갈린다** — 「이벤트 표만 여는 시스템 권한」은 **BYPASSRLS(전 표 면제) 또는 마이그레이션**을 요구해 둘 다 막혔고, 대신 **이미 RLS 면제인 `d1_lab`** 에서 명단만 읽는다. **신규 롤 0 · 신규 면제 0 · 마이그레이션 0 · 계약 개정 0.** **Ted 확인 항목**(`〈110〉-㉯`). **이벤트 봉투는 `labId` 를 이미 필수로 싣는다** — 계약 개정 불요. **배포 = `pipeline-worker` 단독 교체 1회 · 실패·롤백 0건** · 롤백 수단 = `colab-v2/pipeline-worker:i2-pre32` 태그(⚠ `rollback.sh` 는 이 변경의 수단이 아니다). **감지 실행 = 아웃박스 14/14 발행**(직전 미발행 12 → **0**). 처리 대상은 1건뿐 — 나머지 11건은 이미 `registered_at` 이 찍혀 집합 밖이다. **#31 해소 실측** — 축 판별이 돌아 `carries_lat`/`carries_lon` 이 서고 `attachUploadGridFiles` 가 **400 → 201**. 이어 남은 4건도 붙어 **기준 격자 파일 6/6 · `d3_file` 128**. **적재 도구 결함 1건 시정** — 400 은 서버 결함이 아니라 **경주**였다(축 판별 전 부착). `_await_grid_axes` 추가 · 도구 시험 **17/0**(기다림을 빼면 2 red). **적재 계수 산술 확정 — 6 의 차이는 표기 오류였다.** **139 = 122 ＋ 10(격자 16→6) ＋ 1(미배정 tif) ＋ 6(#31)**. 매니페스트 확정치 **128** 에 이번 회차로 도달했다. **`GK2A_NDVI_mean_202305.tif` 귀속 = 기존 11건 어디에도 속하지 않는다** — 1280×1280 float64 EPSG:4326 ≈100 m · 2023-05 월평균 · 경계는 `D-03`~`D-06` 과 같고 **격자는 10 배 촘촘**하다. **별도 데이터셋 후보 · Ted 판정 사안.** **평가셋 재실행 = 17/18 실행 · 통과 7 · 실패 10 · 미실행 1(`Q-19` — `ai-service` 정지가 거부됐다).** **기대값 무수정.** `Q-08` 이 실패 → **통과**로 바뀌었다. ★ **회귀 게이트 `Q-01`·`Q-04` 는 여전히 실패** — **되돌림 미실행, 판정만 올린다.** **원인 판정 = 실패 10건 전부 ⓑ 그래프·매칭 확장 경계 · ⓐ 감지 종속 0건**(`search_vector` 는 이름 A·주제 B·설명 C 만 훑는다). **⚠ 새 사실 — 검색이 결정적이지 않다**(같은 질의 연속 2회에 `Q-13` 0→3건 · `Q-11` 3→1건). **평가셋을 회귀 게이트로 쓰려면 이것이 먼저다 · Ted 판정 사안.** **⚠ 자동 메타 5칸은 여전히 비어 있다** — `#32` 해소가 되돌리지 않는다(넷은 stage 2 범위 · 포맷 한 칸은 등록이 감지보다 먼저라 **소급 경로가 없다**). 새 ⛔ **#35**. 시험 = **워커 110/0**(직전 106 · 신규 6건은 red 확인 후 green) · **적재 도구 17/0**. 게이트 **개별 26종 · green 25 · red 1**(`schema-diff` — 종전과 같은 환경 조건, **신규 red 0**). 등재 = `PLAN-SoT §9 〈110〉`. **스키마 변경 0 · 마이그레이션 0 · 계약 개정 0 · 동결 해제 0회**)
／ 이전 **최종 갱신** 2026-08-26 (**★ 로그인이 staging 에 서고 실데이터 적재가 실행됐다 — 절차 5단(픽스처 철거)은 미실행이다.** **연구실 C 신설** = `infra/staging/provision-lab.sh`＋`.sql`(신규) — `d1_lab`·`d1_lab_profile`·`d1_account`·`d2_member_role` 각 1행, `d2_permission_switch` 0(교수는 판정 P-5). **`colab` 계정 생성** = 연구실 C · 역할 `연구원` · 스위치 4행(위임 둘 꺼짐). **배포** = `deploy.sh` 1회 · 컨테이너 **8/8** · 헬스 **6/6 본문 대조** · postgres recreate 로 **로그 상한(50m×5) 적용**(Ted ②). **로그인 실동작 5항 green** — 정 비밀번호 201 · 오 비밀번호 401 · 미존재 계정 **401 동일 본문**(존재 여부 미노출) · 로그아웃 204 후 401 · cross-tenant 음성 404 3건. FE 화면 로그인 → 데이터셋 목록 표시 확인. **비밀번호 회전 실행** — 구 비밀번호 **401 음성 확인**. 값은 홈의 `0600` 파일에만 있고 레포·대화·보고 어디에도 없다. **적재 실측 = 데이터셋 11 · 파일 122 · 계보 간선 5(전건 확정).** 회차 ①(`D-01`·`D-02` 11파일)은 **S-04 화면**으로, 나머지 9건은 도구로 적재했다. **⚠ 계획치 139 에 11 미달** — 격자 6 미부착 ＋ 매니페스트 단계 배정 불가 5. **반올림하지 않는다**(`§4` **#31**·**#32**). **A·B 관측치 불변 = 21/21 일치**(DB 17칸 ＋ **API 4칸 신규 취득**) — 경계 격리 실증 성립. **격자 사본 차이 측정(Ted ③) = 차이 0** — `D-07`/`D-15` 사본은 **바이트 완전 일치**(2881×2305 f32, 26,562,948 B). 통일 판단 불요. **평가셋 18건 첫 실측 = 통과 7 · 실패 11 · `[미확인]` 0.** ★ 회귀 게이트 5 중 **Q-01·Q-04 실패**. **기대값을 고치지 않았다**(`§4` **#33**). **새 ⛔ 3건** — #31 격자 부착 차단 · #32 pipeline-worker 가 연구실 A 에만 붙어 감지 미수행 · #33 평가셋 11건 불일치. 게이트 **개별 16종 · green 15 · red 1**(`schema-diff` — 종전과 같은 실행 환경 조건, **신규 red 0**). 등재 = `PLAN-SoT §9 〈109〉`. **절차 5단 미실행 · 스키마 변경 0 · 계약 개정 0 · 동결 해제 0회**)
／ 이전 **최종 갱신** 2026-08-26 (**★ 로그인·세션이 선다 — 범위 확대이며 정본 개정 위에 있다.** 정본은 로그인 화면(A-01~A-06)을 **P1 보류**로 뺐고(`IA_사이트맵.md:7` · P-17), Ted 지시가 개정을 인가했다 — `PLAN-SoT §9 〈107〉`·`〈108〉`. **인증 수단은 교체 가능한 경계 뒤**(`kernel/authn.py::build` 한 곳) · **마이그레이션 0**(무상태 서명 세션) · **계약 5차 동결 해제**(신설 2 op, 기존 50 op 무변경 · op 50→52 · 501 표 23 유지) · **기존 주체 표 병존**. 어드민 수단 = 계정＋비밀번호(**scrypt 해시만** · 회전 스크립트 동봉 · 시도 제한 5/15분). ⚠ **정본은 비밀번호 로그인을 명시적으로 뺐다**(`PRD_계정과_연구실_소속 §5.2`) — 어긋남을 `〈108〉-㉮` 에 판정 사안으로 남겼다. 실측 = core-api **407/0** · frontend **274/0** · `tsc` 0 · 게이트 **15종 green**. **staging 무접촉 · `colab` 계정 미생성**(스크립트만). 산출 = `sessions/WU-AUTH-DESIGN.md`)
／ 이전 **최종 갱신** 2026-08-26 (**★ S2 멱등 설계 확정 — 문서 전용 레인.** **멱등의 귀속처 = 적재 도구**(Ted 판정 ㈑). **제품 계층 무변경 · 계약 개정 0 · 마이그레이션 0 · 완료 조건 ② 무개정.** 기각 = ㈏ 제품 계층 멱등(사용자 정상 동작 차단) · ㈐ 완료 조건 개정(위험 미해소). 조회 기준 = `listDatasets` 전수 순회 ＋ `DatasetRow.name` **완전 일치**(이름 11건 전수 대조 = 중복 0). `searchDatasets` 는 **부분 일치·AI 의존**으로 조회 수단에서 기각. 부분 적재 상태는 **2종뿐**(`createDataset` 원자성) — 재개는 **건너뜀 또는 이어붙임**, **삭제 경로 부재**(`deleteDataset`·`getDatasetDeletionImpact` 501). 적재 도구 = **스크립트 1건 · 공개 API 4건만 · DB 직접 INSERT 0** → `㊾-③` 정합, 단 **FE 코드는 지나지 않아** 회차 ①(`D-01`·`D-02`)은 S-04 화면으로 손수 적재한다. 등재 = `PLAN-SoT §9 〈106〉`. 산출 = `sessions/S2-EXEC-PLAN.md §5.4`·`§14`. **구현은 범위 밖 — 판정 로직은 실측된 적이 없다.** **코드·계약·마이그레이션 무수정 · staging 무접촉**)
／ 이전 **최종 갱신** 2026-08-26 (**★ S2 야간 레인 — 적재 도구는 섰고, 적재는 실행되지 않았다.** **⛔ 새 블로커 둘이 절차를 막았다**(`§4` **#28**·**#29**). **#28 = staging 에 데이터셋이 0 건이다** — `d3_dataset`·`d3_file`·`d4_lineage_edge`·`d5_upload` 전부 **0**(`postgres` 슈퍼유저 RLS 우회 실측)이고, 그것을 참조하는 `d6_project_dataset` **3** · `d2_dataset_access` 2 · `d2_verified` 2 · `d8_download` 2 만 **고아**로 남았다. **보관 백업 8종 전부 `d3_dataset`=0 — 복원으로 되살릴 수 없다.** 이로써 **인계 진술 2건이 실물과 어긋난다**(`#22` 「A/B 두 연구실과 그에 딸린 데이터셋」 · 아래 이전 헤더의 「검색 `강우` 3건 · `degraded:false`」). **원인은 [미확인] — DB 를 만지지 않았다**(고아 행을 지우면 경위 증거가 사라진다). **#29 = `subjects.json` 읽기가 하네스 분류기에 거부돼 staging API 인증이 불가능하다** — **우회하지 않았고**, 그래서 **절차 2·3·4단(관측치 고정 · 연구실 C 신설 · 적재 · 검증 · 평가셋 18건)이 전부 미실행**이다. **실행한 것 = 1단뿐** — 백업 두 체인 GREEN(platform 23 테이블/32행 · ai 6 테이블/91행) · 산출물 검사 GREEN · **복원 리허설 GREEN**(테이블 수·행 수·내용 다이제스트 일치, 소요 **0 초** — 실데이터 규모가 아니라 `U-4` 는 여전히 미실측). **만든 것 = 적재 도구 `infra/staging/load-seed.py` ＋ 시험 `load-seed-test.py` 16건**(표준 라이브러리만 · mock HTTP 서버 · staging 무의존). **red 확인 후 green**, 그리고 **변이 3종**(멱등 판정 무력화 · 순회 첫 페이지만 · 순서 짝 역전)이 **전부 잡히는 것으로 오라클이 fail-closed 임을 증명**했다(각각 7·1·1 건 실패). 도구는 **공개 API op 4건만** 부르고 **DB 드라이버를 import 하지 않는다**(`㊾-③` 위반을 코드로 불가능하게). **정정 1건 — 501 표 산문이 「계약 49 중 실동작 26」이었으나 실측은 「50 중 27」이다**(고유 `operationId` 50 · 표 23). **차 23 만 우연히 맞아 시험이 red 를 안 냈다 — 같은 자리 네 번째 오기.** 표 자체는 **23 으로 옳다.** **⚠ 적재 매니페스트는 만들지 않았다** — 기준 격자 16건 중 일부가 **적재 제외분(`D-14` HLS)과 범위 밖(GRIB)** 의 격자라 데이터셋 귀속을 **지어내야만** 완성되고, 그것은 `㊴-②` 저촉이다(**열린 것**). **시험·게이트 무회귀 — core-api 361/0 · frontend 260/0 · `tsc` 0 · 501 표 23.** **⭑ Ted 판정 도착분 처리 (2026-08-26)** — ①**평가셋 불일치 2건 확정**(ⓐ 「한국수자원학회」 기대값 **0건 확정 · 항목 삭제 아님** ＋ **적재 시 `source_label` 부여 금지** · ⓑ `pg_trgm` 임계 **0.3 유지**, 한계를 기대값에 반영) — `S2b-SEARCH-EVALSET §4` 에 등재. **독립 확증** = SRC-A·SRC-I 원문 어디에도 「한국수자원학회」·「KWRA」 문자열이 **0건**이다. ③**PDF 2건 개봉 승인분 실행** — **둘 다 열었다.** **SRC-J 가 HSR 규격을 주었다** — 1024 B 헤더 구조(`RDR_CMP_HEAD` 64 ＋ `RDR_CMP_STN_LIST` 20 × 48) · `ptype` 5=HSR · **투영 LCC · 기준위경도 N38.0°/E126.0° · 기준격자점 1121, 1681** · 결측 −20000/−25000/−30000 · `반사도(dBz)=값/100` · 비압축 **39,845,254 B**. **교차 검증 = 1024 ＋ 3 × 2 × 2305 × 2881 = 39,845,254 정확히 일치** → **규격은 3 블록이고 실물 12 건은 1 블록치**임이 확정됐다(`DATA-PIPELINE-MEASUREMENT §3-③` 의 그 자리). SRC-I 는 `SRC-A` 와 **유사도 0.904** 로 대조돼 닫혔다. ⚠ **처방이 틀렸었다 — `poppler-utils` 로는 SRC-J 가 안 열린다**(본문이 쪽마다 이미지 1장, 텍스트는 표제 3줄뿐). ②**브라우저 자동화 적재 = 실행 못 했다 → 새 ⛔ `#30`.** **미실행 지시 1건 = ② 회차 ① 화면 적재.** **staging 무접촉 · DB 무수정 · 계약·스키마·마이그레이션 무수정 · 동결 해제 0회**)
／ 이전 **최종 갱신** 2026-08-26 (**★ S2b 승인 · 적재 제외 판정 반영 — 문서 전용 레인.** 설명 **15건 일괄 승인**(원천 근거 11 · 실측 도출 4) · **주제 확정 곤란 4건**(`D-10`·`D-11`·`D-12`·`D-14`)**을 적재 제외 · 원천 보존 · stage 2 재판정**으로 판정. **시드 계수 재산정 = 데이터셋 15 → 11 · 파일 292 → 139 · 계보 간선 5 → 5**(차감 0 — 간선 다섯은 전부 `D-01`·`D-02`→`D-03`~`D-06` 사이). 배정 대조식 재작성 = **139 + 153(신규 분류 `①-b` stage 2 재판정 대기) + 148 + 1,682 = 2,122**, 미분류 0 유지. 검색 평가셋 **20 → 18건**(삭제 3 · 기대값 수정 3 · 축 이동 1 · 신규 0). 등재 = `PLAN-SoT §9 〈104〉`(계수·제외) · `〈105〉`(`〈72〉` 관계 = **비상충**, 다만 적재 11건 중 `topic IS NULL` 이 0건이라 「주제 빈 상태 표현」 증명 수단이 시드에서 사라졌다 — 열린 것 `⑳-b`). **코드·계약·마이그레이션 무수정 · staging 무접촉 · 게이트 `planning-freshness` green**)
／ 이전 **최종 갱신** 2026-08-25 (**★ 규약 기계화 · 문서 드리프트 정정 레인.** `stage2` 마커 규약을 기계가 강제한다 — 마커 **28건 부여**(pipeline-worker, `-m "stage2 and not e2e"` 실행 대상 **17건**) · 게이트 `stage2-markers` ＋ `stage2-markers-selftest` 신설 · CI `dormant-tests` 잡 추가. **수집 0건·skipped·failed 는 전부 red** — 「안 돌리면 휴면은 부식」(`〈71〉-㉰`)이 이제 기계다. `e04-flow.json` 을 stage 1 흐름으로 재작성(15단계 이월 1 → **18단계 이월 4**). `§4` 블로커 전수 대조 — **해소 4건 정정**. 새 판정 = `§9 〈94〉`~`〈103〉` **10건**. 게이트 **24 green / 1 red**(`schema-diff` = DB 미지정, 설계상 red) + 신설 2 · 시험 회귀 0.**)
／ 이전 **최종 갱신** 2026-08-25 (**★ stage 1 물결 W4~W7 완주 — staging 이 제품으로 선다.** 게이트 **23/23 green**(`db-boundary` 신설 · `schema-diff` 는 적용된 staging DB 대조로 green) · 시험 **core-api 332/0 · frontend 248/0 · ai-service 68/0 · viz-render 99/0 · pipeline-worker 118/0 · `tsc --noEmit` 0** · **501 표 = 23** · staging 컨테이너 **8** · 헬스 **6** · 호스트 노출 `127.0.0.1` only(**`0.0.0.0` 0건**) · 체인 platform `0006` / ai `0005` · 롤백 왕복 증명(530 없음, 본문 대조). **staging 에서 검색이 실결과를 낸다**(`강우` 3건 · `degraded:false`)이고 **미리보기 ①②가 공개 주소에서 200**. 새 판정 = `PLAN-SoT §9 〈83〉~〈93〉`. **다만 ⛔ 둘이 열려 있다** — 지도형 미리보기(격자 전달 규약 불일치)·`addDatasetFile` 202(집행은 400). `§4` #20·#21)
／ **최종 갱신** 2026-08-24 (**★ `W4` 레인 `S1-preview` 닫힘** — `〈74〉` 미리보기 3층이 실데이터 7건으로 선다. ①썸네일 128 px WEBP · ②비지도형 1024 px PNG(블록평균) · ③지도형 EPSG:3857 PNG + 사이드카 JSON + `.pgw`. **파일 내부 격자 계산을 열었다** — hdf 2.8e-14° · nc 1.26e-5° 로 동봉 격자와 대조 확인, 이제 격자 파일이 필요한 것은 **HSR 하나**다. **공통 색 범위 잠정/확정 2단계 + 단계 토큰 캐시 키** · **축 판별 사다리**(값 범위가 파일명을 이긴다) · **`.npy` 독립 포맷**(결측 NaN 만). 시험 viz-render **42p/6f → 90 passed** · pipeline-worker **100 → 104 passed**(9 errors = DB 부재, 변동 없음) · **게이트 회귀 0건**. 새 판정 = `§9 〈83〉`(다섯, 그중 ㉮ 는 계약 개정 사안) · 새 블로커 = `§4` #19. **세부는 `§9 〈83〉` 에만 있다 — 여기는 링크뿐이다.**) ／ 이전 최종 갱신 2026-08-24 (**★ 체크포인트 `C1` 통과 — `W3` 세 레인이 닫히고 stage 1 의 절반이 독립 가치로 섰다.** `C1` 통과 조건 다섯(`sessions/S1-PLAN.md §5.0`) 판정 — **① 전 게이트 = 21 green / 1 red.** 유일한 red 는 `schema-diff`(**적용 DB 가 지정되지 않았다** — DB 부재로 **설계상 red**, `CLAUDE.md §4`). **기준선은 2 red 였고**(`sessions/S1-W0-baseline.md`) `generated-up-to-date` 가 이 물결에서 **green 으로 회복**했다. **회귀 0건.** **② 501 = 24 유지 green** — `services/core-api/tests/test_not_implemented.py:72` 의 `len(OPERATIONS) == 24`. ⚠ **계획이 한때 예측한 28·29 가 아니다** — 격자 3 op·미리보기 2 op 은 **애초에 501 표에 없었고**, `searchDatasets` 는 **계약을 여는 그 회차에 구현**돼 표에 행을 더하지 않았다(안 그랬으면 25 였다). **③ 휴면 절제 완료 + `stage2` 마커 시험 CI 유지** — `W2`(`1995e0f`). 시험 총계가 움직이지 않은 것이 `〈71〉-㉰` 이 지켜졌다는 증거다. **⭑ 정정 — `stage2` 마커 실물은 0 건이다.** `〈74〉` 가 미리보기를 되들이면서 마커 대상(viz 42 · FE 미리보기 23)이 **범위로 돌아왔고**, 그래서 계획이 적었던 **`[미측정]` 값 「약 46+23+20」은 무효다.** 조용히 지우지 않고 여기 적는다. **④ 계약 동결 해제 1회 통과**(`〈80〉` 묶음 8항 + 소비자, `613427b`) — **두 번째 해제 0건.** **⑤ 커밋** = 이 커밋. **시험 실측(기준선 → 현재)** — core-api **224 → 246 passed / 0 failed** · pipeline-worker **72 → 83 passed**(17 failed / 9 errors 는 **변동 없음** — `COLAB_REFERENCE_DATA`·`COLAB_PIPELINE_DB_URL` 부재에 대한 **fail-closed**) · frontend **147p+1f → 166 passed / 0 failed**. **물결 커밋 7** — `1995e0f`(W2 절제) · `f887d91`(검색 인프라) · `613427b`(동결 해제 8항 + 소비자) · `9f0fb10`(검색 경로 분리) · `1f56968`(워커 배선) · `72206b9`(FE 결함) · `da6c0f1`(계보 모달). **새 판정 둘** = `PLAN-SoT §9 〈81〉`(stage 1 검색은 pgvector·임베딩 없이 tsvector + LLM 질의 해석 — 이미지에 pgvector 가 없다) · **`〈82〉`**(KG 우선순위 = 그래프 기반 온톨로지 검색). **값과 근거는 `PLAN-SoT §9` 에만 있다 — 여기는 링크뿐**(`CLAUDE.md §6-3`). **새 블로커 셋 = `§4` #16·#17·#18.** **초안 `sessions/G8b-DRAFT.md` 를 이 커밋으로 레포에 넣는다.** **코드·계약·DB·프런트엔드는 한 줄도 안 만졌다 — 문서 전용 회차다.**)
／ 이전 갱신 2026-08-24 (**⭑ `W2` 레인 `S1-detach` 완료.** 범위가 대폭 축소된 뒤(`〈74〉〈75〉`) 남은 두 가지만 실행: ① `frontend/src/app/routes.tsx` 에서 S-08(미등록 미리보기) 라우트 2줄 + import 제거 ② `services/pipeline-worker/src/colab_pipeline/d5/` 의 `parse.py`·`hsr.py`·`cog.py`·`tiff_probe.py`·`renderable.py`·`pipeline.py` 6파일에 stage2 대기 표기(docstring, 파일·시험 유지 — `〈71〉-㉰`). **`grid.py`·`axis.py` 는 만지지 않았다**(`〈75〉` 로 부활) — `〈76〉` 근거는 `S1-PLAN.md` §5.2 행 7 참조. `compose.i2.yml`·`viz-render`·`components/preview/` 는 이 레인 소관에서 빠졌으므로 **손대지 않았다.** FE 시험 148건(147 pass·1 known-fail, 베이스라인과 동일 — 델타 0), 게이트 12 green·2 red(베이스라인과 동일 — `schema-diff` DB부재·`generated-up-to-date` 는 `bb941c3` 기인, 둘 다 회귀 아님). **`W2` 진입조건 ③(`stage2` 마커 시험 실물 재계수)은 이 레인 범위 밖으로 판단해 손대지 않았다 — 미착수로 남긴다.** `S1-ontology-canon`(Ted/지정자) 레인도 미착수다. **`W2` 는 아직 안 닫혔다.**)
／ 이전 갱신 2026-08-24 (**⭑ `W1` 완료 — stage 1 정본 개정 단독 물결.** `S1-PLAN-REFOUND` 의 `§H` 개정문이 정본에 등재됐다: **`PLAN-SoT §9 〈74〉`**(미리보기 3층 재적재 · `viz-render` 배포 복귀 · 501 = 24 유지) **`〈75〉`**(HSR 격자가 지도형의 전제 · 격자 수용이 stage 1) **`〈76〉`**(`§1.4` 분류 규칙 전면 교체 — 복원 가능성·유한성·좌표 출처) **`〈77〉`**(`.npy` 를 지원 포맷에 추가 · 결측은 `NaN` 만) **`〈78〉`**(편의 기능은 `§J` 에 모으고 지금 안 만든다) **`〈79〉`**(격자 파일의 파이프라인 정체성 = 요청이 `kind` 선언 · 원장 행은 축 확정 뒤 · **마이그레이션 0건 · 계약 개정 0건**) **`〈80〉`**(계약 동결 **1회 해제 승인** · 묶음 8항). **값과 근거는 전부 `PLAN-SoT §9` 에 있다 — 여기에는 링크만 둔다**(`CLAUDE.md §6-3`). **고친 문서** = `PLAN-SoT.md` · `WORK-UNITS.md` · `SEED-DATA.md` · `gates/README.md` · `CLAUDE.md` · `sessions/S1-PLAN.md`(부분 갈음 표기) · `sessions/S1-PLAN-REFOUND.md`(반영 완료 표기) · 이 문서. **코드·계약·DB·프런트엔드는 한 줄도 안 만졌다.**
／ 이전 갱신 2026-08-24 (**⚠ stage 1 재정의 — `§9 〈70〉` Ted.** P2 를 실행하던 도중 범위가 다시 잘렸다: **가공(미리보기가 이것)·가시화·데이터 확인·검증류가 전부 빠지고** 「데이터 업로드 · 데이터셋 인포 · AI 검색(온톨로지 기반) · 연구실-프로젝트-데이터셋 · 계보 설정」으로 좁힌다. **새 계획 = `sessions/S1-PLAN.md`**(advisor 게이트 ① `approve-with-changes` 반영본, 열린 질문 0). 판정 기록 `〈70〉〈71〉〈72〉`. **P2 산출 여섯 커밋은 지우지 않았다 — 살린다 13 · 휴면 8 로 배정**)
**현재 단계** **stage 1 · staging 에 실데이터가 서 있고 감지가 돈다 — `#31`·`#32` 해소. 남은 ⛔ 는 `§4` **#30**·**#33**·**#35** 셋.** 적재 계수 = **데이터셋 11 · 파일 128(매니페스트 확정치 도달) · 계보 5**. Ted 판정 사안 4건 — ① 워커 권한 편차 확인(`〈110〉-㉯`) ② 평가셋 회귀 게이트 `Q-01`·`Q-04` 되돌림 여부 ③ 검색 비결정성 ④ `GK2A_NDVI_mean_202305.tif` 신규 데이터셋 여부.
／ 이전 **현재 단계** **stage 1 ＋ WU `PA`(로그인·세션) 구현 완료 — staging 배포·`colab` 계정 생성은 별도 회차.** Ted 판정 대기 **8건**(`sessions/WU-AUTH-DESIGN.md §5`).
／ 이전 **현재 단계** **stage 1 · S2b 닫힘 · S2 멱등 설계 확정(`〈106〉`) — 남은 것은 `§4` 의 ⛔ 둘 · 적재 도구 구현 · S2 실적재(데이터셋 11 · 파일 139).**
／ 이전 **현재 단계** **stage 1 · S2 적재 도구는 섰고 적재는 미실행 — 막는 것은 `§4` **#28**·**#29** 둘이고 **사람 판정이 있어야 열린다**.** 종전 ⛔ **#20**(지도형 미리보기)·**#21**(`addDatasetFile` 202)도 그대로 열려 있다. **S2 완료 조건 3항 판정 = 전항 `[미확인]`** — ①카탈로그 표시 · ②재실행 멱등 · ③백업 복원 재통과 중 **어느 것도 staging 에서 재지 못했다**(②는 mock 서버에서만 성립 · ③은 **적재 전 상태로만** 통과). **반올림하지 않는다.**
／ 이전 **현재 단계** **stage 1 · S2b 닫힘(승인 완료) — 남은 것은 `§4` 의 ⛔ 둘과 S2 실적재(데이터셋 11 · 파일 139).**
／ 이전 **현재 단계** **stage 1 · `W7` 까지 완주 · staging 배포 green — 남은 것은 `§4` 의 ⛔ 둘과 S2 실적재.** 계획 정본은 `sessions/S1-PLAN-REFOUND.md`. 물결 = W1 → W2 → W3 → **C1** → W4/W5 → 4차 해제 → K4-b → 게이트 → **W7(배포)** → 보안.
／ **현재 단계** **stage 1 · `W4` 진행 중 — 레인 `S1-preview` 완료(미리보기 3층이 실데이터로 선다), `S1-search-a`·`S1-ontology-graph` 는 다른 레인 소유.**
／ 이전 단계 **stage 1 · ★`C1` 통과 — 다음은 `W4`(병렬 3).** 계획 정본은 **`sessions/S1-PLAN-REFOUND.md`** 이고 `sessions/S1-PLAN.md` 는 **부분 갈음**됐다(어긋나면 REFOUND 가 이긴다). 물결 = W1 → W2(2) → W3(3) → ★C1 → **W4(3)** → W5(3) → W6(2) → W7, 동시 레인 최대 3. **`W2` 가 남긴 `[미확인]` 둘 중 ②(`stage2` 마커 재분류)는 이 회차에 실물로 닫혔다 — 마커 0건이고, 대상 집합은 `〈74〉` 로 범위에 복귀했다.** **⚠ `W4` 세 레인이 같은 조건에서 시작하지 않는다** — `S1-preview` 와 `S1-search-a` 는 **지금 착수 가능**하고, **`S1-ontology-graph` 는 `G8b` Q1 에 Ted 가 답하기 전에는 착수 금지**다(아래 진입조건 ⑤).
／ 이전 단계 **stage 1 · `W2` 진행 중 — 레인 `S1-detach` 완료 · 레인 `S1-ontology-canon`(Ted/지정자) 미착수 · 진입조건 ③(`stage2` 마커 재계수) 미착수.** 계획 정본은 **`sessions/S1-PLAN-REFOUND.md`** 이고 `sessions/S1-PLAN.md` 는 **부분 갈음**됐다(어긋나면 REFOUND 가 이긴다). 물결 = **W1 → W2(2) → W3(3) → ★C1 → W4(3) → W5(3) → W6(2) → W7**, 동시 레인 최대 3. **`W1` 이 남긴 `[미확인]` 둘** — ① `§2.1` 배정 계수(살린다 18 · 휴면 4 는 **행의 이동에서 유도한 값**이지 실물 대조가 아니다) ② `stage2` 마커 시험의 「부활 / 여전히 휴면」 재분류 세 수(**`[미측정]`** — 지어내지 않는다). **둘 다 `W2` 가 실물로 센다.**
／ 이전 단계 **P2 는 실행 중 범위 재정의로 멈췄다 — 부분 완료로 닫지 않고 `S1-PLAN` 으로 재편한다.** 이번 세션이 실제로 만든 것: **`0004`**(격자 축 두 불리언 + `d5_*` 업로드 원장 + PRECOUNT 가드, 드리프트 시험 4케이스) · **파이프라인**(outbox·워커·릴레이·reaper · 이벤트 7종 실발행 · 축 판별 실물 16건 전건 · tiff 3부류, 시험 98건) · **core-api**(12 op · 계보 오염 방지 음성 8종 RED→GREEN 개별 관측 · `NB-A` 동일성 단언, 시험 224건) · **viz-render**(렌더 4 op · 4포맷 실렌더 · 타일 서명 — **휴면 대상**) · **FE 둘**(preview 완료 · upload 미완, 둘 다 휴면/재배정). **정본 개정 실적 = `〈67〉`**(업로드 수명 규칙 신설 + `DR-8` 재개정 7자리 — `planning-freshness` 15/15 green). **501 = 24 → stage1 에서 29**(실측)
**다음 WU** → **사람 판정 넷이 먼저다 — 코드 레인이 아니다.** ① **#29 staging 주체 접근**(운영자 직접 실행 / 접근 허용 / 절차서 개정) — 이것이 열리기 전에는 S2 2·3·4단을 **아무도 못 돈다** ② **#28 A·B 재시드 여부** — 안 하면 4-3 「A·B 불변」이 **0→0 자명 대조**가 되어 경계 격리 실증이 성립하지 않는다 ③ **#28 고아 D2·D6·D8 행 처리**(삭제 / 존치 / 재시드 복구) ④ **기준 격자 16건의 데이터셋 귀속** — 적재 제외분·범위 밖 격자가 섞여 있어 **매니페스트를 지어내지 않고는 못 만든다**(`㊴-②`). 그다음 **S2 3·4단**, 그리고 종전대로 ⛔ **#20**·**#21**.
／ 이전 **다음 WU** → **⛔ 둘을 WU 로 세우는 것이 먼저다.** ① **`WU-V1` 격자 전달 규약 일치**(`§4` #20 — 지도형 미리보기가 staging 에서 안 뜬다) ② **`addDatasetFile` 202 의 저장 자리 — 스키마 판정 필요(Ted)**(`§4` #21). 그다음 **S2**(초기 데이터 화면 경로 적재) · `S2b` 초안 2건의 **Ted 줄 단위 승인**.
／ **다음 WU** → **`W4` 잔여 — `S1-search-a`(K4-a) ∥ `S1-ontology-graph`(K1b·K2b). ⭑ 레인 `S1-preview` 는 닫혔다**(아래 「최종 갱신」·`§4` #19). ／ 이전 다음 WU → **`W4`(병렬 3) — `S1-preview`(viz-render ①②③ + 사이드카 + 축 사다리) ∥ `S1-search-a`(K4-a) ∥ `S1-ontology-graph`(K1b·K2b — **대기 중, `G8b` Q1 미답**).** 그리고 **`G8b` Q1 은 Ted 가 답할 것**이다 — 코드 레인이 아니다.
／ 이전 다음 WU → **`W2` 잔여 — `S1-ontology-canon`(Ted/지정자, `G8b`) · 진입조건 ③(`stage2` 마커 시험 실물 재계수, 소유 미배정).**
**다음 세션 진입조건 — 착수 전에 확인할 것 여섯** ① **호스트를 껐다 켰다면 `RESTART.md` 를 먼저** — `-f compose.i2.yml` 을 빼면 조용히 I2 이전으로 롤백된다(루트 헬스는 그래도 200). ② **게이트 23/23 green** — `schema-diff` 는 **적용된 staging DB** 를 봐야 판정이 성립한다. `db-boundary` 는 매니페스트(`gates/config/db-boundaries.toml`)가 정본이다(`§9 〈92〉`). ③ **staging 컨테이너 8 · 헬스 6 · 호스트 노출 `127.0.0.1` only** — `0.0.0.0` 이 하나라도 있으면 멈추고 보고한다. ④ **주체 표(`subjects.json`)에 레포 픽스처 토큰이 없다** — 있으면 그 자체가 사고다(`§9 〈93〉`). 값은 어디에도 적지 않는다. ⑤ **⛔ 둘이 아직 열려 있다**(`§4` #20 지도형 미리보기 · #21 `addDatasetFile` 202) — **「나중에」로 두지 않는다.** 착수하려면 WU 로 세운 뒤 시작한다. ⑥ **staging 은 시험 픽스처로 시드돼 있다**(A/B 두 연구실). **실데이터가 아니다** — 화면에 보이는 것을 실적재 근거로 인용하지 않는다(`§4` #22).
／ 이전 진입조건(`W4`) **`W4` 진입조건 — 다음 세션이 착수 전에 확인할 것 여섯** ① **`C1` 커밋이 있다**(이 커밋) — `C1` 통과 조건 다섯이 위 「최종 갱신」에 값으로 적혀 있다. ② **게이트가 21 green / 1 red 이고 그 1 이 `schema-diff` 하나뿐이다** — **다른 red 가 하나라도 늘었으면 그것은 회귀이고, `W4` 를 시작하기 전에 그것부터 본다.** `schema-diff` 는 **DB 부재로 설계상 red 이므로 끄거나 우회하지 않는다**(`CLAUDE.md §4`). ③ **`len(OPERATIONS) == 24` 가 여전히 green** — `W4` 도 이 수를 바꾸지 않는다(`〈74〉-㉱`). 바꿔야 할 것 같으면 **멈추고 보고한다.** ④ **`tsc --noEmit` red 3건이 아직 그대로인지 확인한다**(`§4` #16). **`W4` 가 늘리지 않는지가 판정이고, 고치는 것은 `W5`(`S1-fe-preview`) 소관이다.** ⑤ **⭑ `S1-ontology-graph` 는 `G8b` Q1 이 닫히기 전에는 착수 금지다** — 초안은 `sessions/G8b-DRAFT.md` 에 있고 **Q1(관계 값의 출처 채널 ㈎/㈏/㈐)에 Ted 의 답이 실재해야 한다.** 근거 = `㊴-②`·`DOMAINS §7` 규칙 4 — **`[정본 무근거]` 위에 스키마를 짜지 않는다.** ⚠ **없으면 빈 표를 만들고 끝난다**(`G8b-DRAFT §0-3`). **`S1-search-a` 와 `S1-preview` 는 `G8b` 에 안 묶인다 — 둘은 바로 간다**(`〈71〉-㉱` · `S1-PLAN §5.3`). ⑥ **`S1-preview` 는 `HSR 정본 격자 판정`을 기다리지 않는다**(`〈75〉-㉵` — 사용자가 격자를 직접 올린다). **대신 약 612 m 불일치를 stage 1 이 막지 않는다는 한계가 남는다**(`R-4`).
／ 이전 진입조건(`W2`) **`W2` 진입조건 — 다음 세션이 확인할 것 다섯** ① **`W1` 커밋이 있다**(이 커밋) ② **`S1-detach` 는 이제 `S-08` 라우트 2줄 + `d5` 6파일 휴면 표기만 한다** — **`compose.i2.yml` 의 viz 블록 제거 · `PreviewPanel` 절제 · `axis.py`/`grid.py` 휴면 표기는 하지 않는다**(`〈74〉-㉰`·`〈75〉-㉮` 로 전부 취소됐다) ③ **`stage2` 마커 시험을 실물로 재계수해 「부활 / 여전히 휴면」으로 가른다** — 기준은 `〈76〉` 규칙을 **그 시험이 단언하는 대상**에 적용하는 것이고, **red 를 없애려고 마커를 붙이는 것은 은폐다** ④ **`generated-up-to-date` green** ⑤ **`S1-ontology-canon` 은 코드 레인이 아니다** — 산출자가 Ted(또는 지정자)이고, 그 산출이 없으면 `W4` 의 `S1-ontology-graph` **만** 대기한다.
✅ **`W3` 이 지고 갈 것으로 적어 둔 넷 — 전부 닫혔다.** ⓐ **워커 배선**(`〈73〉`) = `1f56968` ⓑ **`COLAB_CORE_VIZ_BASE_URL` 배선** = 같은 커밋(compose 정합까지 — **실배포 확인은 `[미확인]`, `§4` #18**) ⓒ **계약 동결 해제 묶음 8항 1회 집행**(`〈80〉`) = `613427b`, **두 번째 해제 0건** ⓓ **`UploadModal.tsx:230` 제품 결함**(「보기만 할게요」가 등록을 불렀다) = `72206b9`.
／ 이전 다음 WU → **S1**(stage 1). **계획 = `sessions/S1-PLAN.md`** — 물결 7 · 레인 11(동시 최대 3) · **`W3` 뒤 체크포인트 `C1`**(온톨로지가 늦어도 절반이 독립 가치로 닫힌다). **착수 전건은 전부 닫혔다** — Ted 판정 `〈70〉〈72〉` 완료, 메인 판정 `〈71〉` 완료, 열린 질문 0. **`W1` 은 정본 개정 단독**(9파일 24자리 — stage 개념 신설 + `㊸` 되돌림). **⚠ `㊸` 되돌림은 수문학 도메인 소유자 시간을 임계경로로 되돌린다** — 그래서 `K4` 를 `K4-a`(tsvector+사전3종, 그래프 무의존) ∥ `K4-b`(그래프 증강)로 갈라 **그래프 지연이 검색을 못 막게** 했다
／ **Ted 비토 대기 (기본은 그대로 간다)**: `㉝` 스토리 6개 상한 폐기(정본 내용 변경) · `㉟` 표기 규약 · `㊱` 잠김 200·경계 404·권한 403 · `㊲` 멱등 키 둘 · `㊳` 기술 스택 · **`㊸-④-3` D9 시드 배치(④-4 함의로 확정 — 명시 답변 아님)**. **PLAN 이탈 1건**(`§5`)
／ ✅ **seam 결정 났다 — `〈54〉` 정본 = 이벤트 seam.** `fe-core` 를 거기 맞춘다. **P2·S2·P5 의 차단 해제 조건이 「계약 변경 이행」으로 확정**됐다(`§5.5 DR-7` — 결정됨·이행 대기)
／ **Ted 가 줄 것**: **IS2 apply 용 Cloudflare API 토큰**(`Account → Cloudflare Tunnel : Edit`) · 터널 ID 히스토리 처리(`§4-8`). **OpenAI API 키는 투입 완료 — K3·K4 차단 해제**
／ **⏸ 보류 — 열지 않기로 한 것**: **AWS 계정 (I0·I1·I5, prod 전용)**. `㊻`(2026-08-23 Ted) — **v2 완주 판정은 `staging 배포 green` 까지로 내렸다.** prod 는 ① staging 에서 v2 전체가 충분히 동작하고 ② Ted 가 **출시를 결정**했을 때 연다. **더 이상 블로커가 아니다 — 대기가 아니라 결정이다.**
／ Claude 착수 가능: **D2c**(선행 `DR-13` ✅ — 즉시 착수 가능) · **D5**(포맷 5종→4종 `〈51〉`, `DR-1`~`DR-3` 소유) · **I3** 배포 자동화(초안 `sessions/I3.md` — 실행 전 `[정본 무근거]` 7건에 Ted 답이 필요) · `generated-up-to-date` 게이트 · **IS4 잔여**(마지막 apply · 맨몸 호스트 조건)
／ **S1 초기 데이터 인벤토리 — 새로 열렸다.** 원천이 실재한다(`03 Reference-Data`, 3.5 GB · 2,122 파일). **빈 카탈로그는 v2 완료가 아니다**(완료 조건 8번, `㊾`). 코드가 아니라 분류라 아무것도 안 막히고, 여기서 나오는 **데이터셋 경계 결정이 P2 업로드 화면의 실입력**이 된다
／ **T-C(C1~C4)는 v1 정리 트랙이므로 지금 열지 않는다** — C3는 P2·D5, C4는 D3에 가서야 입력이 된다

---

## 1. 진행도

표기 — ✅ 닫힘 · 🟦 진행 · ⬜ 대기 · 🟧 부분(사유 필수) · ⛔ 차단(무엇이 막는지)

### T-R 저장소
| WU | 상태 | 비고 |
|---|---|---|
| R0 레포 결정 | ✅ | **`colab-v2` 신규 모노레포** (Ted, 2026-08-22). `colab-dev-package`는 v1 자산으로 archive |
| R1 스캐폴드 + CI 골격 | ✅ | 원격 `CognileapAI/colab-v2`(public) push 완료 · `main` 보호(force-push·삭제 차단, 리뷰 필수 없음) · CI 1회 완주 — 게이트 잡(`contract-gates` 등) 전부 "미구현 — red"로 **설계대로** 실패. 가시성 결정 = `PLAN-SoT §9-⑯` |
| R2 v1 레포 5종 archive | ⬜ | C4 후 (`colab-dev-package` 포함) |

### T-S stage 1 물결 — W4~W7 레인
| 레인 | 상태 | 비고 |
|---|---|---|
| `S1-preview` (viz-render ①②③ + 사이드카 + 축 사다리 + `.npy`) | ✅ | **코드 커밋 5** — `ef09064`(블록평균·viridis LUT) · `fc93055`(잠정/확정 색 범위 + 단계 토큰 캐시 키) · `a3fe5cf`(3층 산출기 + 사이드카 + `.pgw`) · `5c1a3a6`(렌더 작업 배선 · 이미지 갈래 · 파일 내부 격자) · `1ede28b`(`.npy` 독립 포맷 + 축 사다리). **시험 viz-render 42p/6f → 90 passed / 0 failed**(원천 마운트 시) · pipeline-worker **100 → 104 passed**(9 errors = DB 부재, 변동 없음). **게이트 회귀 0건.** ⚠ **남은 것 하나 = `§4` #19**(계약 `RenderResult.bounds` 필수라 ②비지도형을 「완료」로 낼 자리가 없다 — `§9 〈83〉-㉮`, Ted 판정 필요) |
| `S1-search` (K4-a — 검색 실행기 → core-api) | ✅ | `c1753a1`·`d11a5d7`·`35e9360`·`fdf8470` 실행기 이관 · `81f3020`·`10d9495` 3차 해제 + `SEARCH_UNAVAILABLE`(**폴백 없음** — 죽으면 「동작하지 않음」이 드러난다, `〈87〉`) · `9b33321` 한국어 접두 + `pg_trgm`(`〈89〉`) · `19d25ad` 실측 기록. **staging 에서 실결과** — `강우` 3건 · `degraded:false` |
| `S1-ontology-graph` (K1b·K2b) | ✅ | `57deb8e`·`61bcfa9`·`57c760b` — 노드 49 · 엣지 19, 등급 ⑥ **8**(`〈86〉`). 승인/적재가 갈리던 한 줄은 `〈91〉` 로 닫혔다(승인도 8). `bc229c0` `K4-b` 그래프 증강 — **D9 가 넓히고 core-api 가 찾는다**(`〈90〉`) |
| `S1-project` (P5 화면 셋) | ✅ | `e4e552a`·`2a0c489`·`c3fcc64` — 501 표 24 → 21(`〈84〉`). **WU-P5 자체는 아직 안 닫는다**(아래 T-P) |
| 계약 해제 2·3·4차 | ✅ | `6428e3a` `bounds`(`〈85〉`) · `81f3020`/`10d9495`(`〈87〉`) · `2ed6ef7`·`4783f4a`·`cdd8c40`·`0f7926f` **전수 스윕 16건 → 묶음 11항 승인**(`〈88〉`). **501 표 = 23**(실측) |
| `S1-preview-ui` (P4·미리보기 화면) | 🟧 | `a747c67`·`875aa2a` 미리보기 UI · `15847b3` P4 검색 화면. **①②는 공개 주소에서 200 · ③지도형은 안 뜬다**(`§4` #20) |
| 게이트 `db-boundary` | ✅ | `c499123` — 매니페스트가 정본(`〈92〉`). selftest 18. **게이트 21 → 23** |
| `W7` staging 배포 | ✅ | `974cf9f` 저장 키 = 배치 · `c5a2fbf` 배선 다섯 자리(viz env 0건 · relay · `COLAB_AI_DB_URL` · 주체 표 · named volume 소유권) + nginx `client_max_body_size`. **컨테이너 8 · 헬스 6 · 롤백 왕복 증명(530 없음, 본문 대조)** |
| 보안 | ✅ | `30c2f0a` — 주체 표는 **비밀**, `0600` + uid 정렬(`〈93〉`). 픽스처 토큰의 staging 반입 금지 |
| `S2b` | ✅ | **2026-08-26 Ted 승인 완료.** `S2b-DATASET-DESCRIPTIONS.md` — 설명 **15건 승인 / 11건 적재**(제외 4건의 설명은 보존, stage 2 재판정 시 재사용) · `S2b-SEARCH-EVALSET.md` — **18건**(축 = A 4 · B 3 · C 5 · D 4 · E 2). 계수·제외 근거 = `PLAN-SoT §9 〈104〉`, `〈72〉` 관계 판정 = `〈105〉`. ⚠ **잔여 결손 1건** — 적재 11건 중 `topic IS NULL` 0건(열린 것 `⑳-b`) |

### T-C 정리
| WU | 상태 | 비고 |
|---|---|---|
| C1 5 repo 푸시 확인 | ⬜ | 이관의 차단 조건 |
| C2 v1 이관 (`20 CoLAB-v1`) | ⬜ | C1 후 |
| C3 PoC 지식 추출 `HARVEST.md` | ⬜ | 4포맷·좌표계·COG·실패 목록 (**PoC 가 실증한 4다 — v2 D5 는 GeoTIFF 를 더해 5**, `㊿`) |
| C4 v1 방법론 추출 `METHOD.md` | ⬜ | 계약 게이트 6종 구성 방식 |

### T-I 인프라 (**staging 배포가 완료 조건** — prod 는 `§9-㊻` 로 보류)

> **staging = WSL+Cloudflare Tunnel 호스트 재사용 · prod = AWS** *(2026-08-22 Ted, `PLAN-SoT §9-㉓`)*.
> **PoC 스택은 2026-08-22 철거됐다** — 컨테이너·볼륨·데이터 전부. `colab-hydro.com` 은 현재 **530**(오리진 없음).
> **Cloudflare 터널·DNS 는 살아 있다.** 즉 staging 은 새로 만드는 게 아니라 **빈 터널에 오리진을 붙이는 일**이다.
> 기록과 컷오버 준비물은 `DEPLOY-CURRENT.md`. **AWS에는 아무것도 없다**(`~/.aws` 미설정).

| WU | 상태 | 비고 |
|---|---|---|
| I0 계정·결제·리전·예산 알람 | ⏸ | **보류 — 출시 결정 대기** (`㊻`). staging 은 IS1 이 대신한다. ⬜ 가 아닌 이유 = ⬜ 는 "곧 할 것"으로 읽혀 매 세션 착수 후보에 오른다 |
| **IS1 staging 호스트 구성** | ✅ | 기존 터널에 v2 오리진 재부착. 산출 `infra/staging/` — nginx + cloudflared **둘뿐**(데이터 저장소 없음), 호스트 노출은 `127.0.0.1:3000` 만(PoC 의 `0.0.0.0` 반복 회피). **롤백 왕복 증명 200→530→200.** 현재 `colab-hydro.com` = v2 staging 자리표시(중립 안내 + `/healthz`). 토큰은 레포 밖 홈 `0600` 파일. **커넥터 로그가 원격 ingress 정본을 노출했다 — `www.colab-hydro.com → http://nginx:80`** (그래서 서비스명을 `nginx` 로 고정) |
| **IS2 터널 라우팅 IaC화** | ✅ | **완료 판정 증명됨 — `terraform plan` = `No changes.`** 레포 선언이 실제 상태와 일치한다. 즉 **대시보드가 정본이 아니라 레포가 정본이고 대시보드가 그 산출**이다. 산출 `infra/staging/tunnel/`(versions·variables·tunnel.tf·tfvars.example·.gitignore·README). ingress 선언 = `www→nginx:80` + catch-all 404 **둘뿐이고 ssh 규칙 없음**(`§9-㉜`) — 없는 이유를 파일 주석에 남겼다. 비밀은 전부 변수(default 없음)이고 `.gitignore` 가 `*.tfvars`·상태파일 차단(`git check-ignore` 확인). 실행 기록 — `init → import → plan → apply`(`0 add, 1 change, 0 destroy`, destroy/replace 0건) → `www/healthz` **200 무중단** → 커넥터가 `version=7` 로 새 ingress 수신. **블로커 #7 은 세 겹이었다**(ingress · Access 앱 · DNS CNAME) — 셋 다 제거, `ssh.colab-hydro.com` 은 이제 **DNS 미해석**. terraform 은 `~/.local/bin` 에 사용자 수준 설치(sudo·apt 저장소 불필요, 파일 삭제로 원복). **한계 = state 가 이 호스트 로컬에만 있다**(레포에 커밋하지 않는다 — 비밀 포함). 호스트가 사라지면 재import 로 복구해야 한다 → **IS4 로 세웠다**. 터널은 **원격 관리형**(토큰만 · ingress 를 Cloudflare 가 push, 커넥터 로그 `version=6` 으로 확증). 3안 비교 후 **Terraform `cloudflare_zero_trust_tunnel_cloudflared_config`** 채택 — 무중단이고 `terraform plan` 이 "레포에서 재적용해 동일 상태 재현"을 그대로 만족시킨다. `.tf` 초안·apply/verify/rollback 시퀀스 = `sessions/IS2.md`(아직 `infra/` 에 배치하지 않았다). 블로커 #7 은 `§9-㉜` 로 **삭제 확정** — IS2 선언에서 빼면 apply 시 사라진다 |
| IS3 staging 백업 체계 | ✅ | **2026-08-23 닫힘 — 실 staging 대상.** 백업 1회(두 체인) · **복원 1회**(일회용 인스턴스, 테이블·행 수·내용 md5 대조 일치, K2 시드 22행 별도 확인) · **fail-closed fixture 11건 전부 red**. F2 가 핵심 — **빈 gzip 20B 는 `gzip -t` 를 통과한다**(PoC 가드가 통과시킨 그 파일). I2 가 넘긴 설정으로는 platform 만 덮여 부분 성공이 났고, 프로파일별 합격선으로 고친 뒤 **F9 로 박았다**. 스케줄 설치. **정직한 한계 = 실패가 「가서 봐야 보이는」 자리에만 남는다** — push 알림 채널이 없어 침묵 창이 0 이 된 것이 아니라 8주 → 최대 1주로 잘렸다. 산출 `sessions/IS3.md` |
| **IS4 terraform state 보관** | 🟧 | **2026-08-23 — 절차는 섰고 리허설도 돌렸다.** scratch 에 `.tf` 만 복사해 빈 state 로 `init → import → plan`(실자격증명). 원본 state md5 동일 · 실디렉터리 `plan` = `No changes.` · staging 내내 200. 산출 `sessions/IS4.md` · `infra/staging/tunnel/README §5-1`. **미달 2건 = ① 마지막 `apply` 미실행**(실 터널을 건드린다) **② 맨몸 호스트 조건**(terraform 미설치·자격증명 파일 없음) 미실험. 원격 백엔드(S3+DynamoDB)는 **I0 대기** |
| I1 토폴로지 + IaC (`plan`까지) | ⏸ | I0 후 — **보류** (`㊻`) |
| **I2 walking skeleton 배포** | ✅ | **2026-08-23 — 분수령 통과.** 5개 단위 전부 헬스 green(`/healthz/{core-api,frontend,pipeline-worker,viz-render,ai-service}`), 컨테이너 7/7 healthy, 호스트 노출 `127.0.0.1` 하나뿐 **`0.0.0.0` 0건**. **롤백 증명에서 방법을 고쳤다 — 상태 코드만으로는 판정할 수 없다**(자리표시가 모든 경로에 200 을 준다). 본문까지 대조했다: `<!doctype html>` ↔ `{"unit":"core-api"}` · `/api/v1/me` 200↔401. 배포→롤백→재배포 4구간 **530 없이 무중단**. postgres 2체인 분리 유지(`colab_platform` 20표 · `colab_ai` 시드 22행). 앱 롤 staging 실측 `rolsuper=f·rolbypassrls=f·소유 0` — P0 숫자와 동일. 터널 선언 무수정(`plan` = No changes). 산출 `sessions/I2.md` | **+ 2026-08-25 — 배선이 실제로 이어졌다**(`c5a2fbf`). I2 당시 green 이던 헬스 6종 뒤에 **제품이 잠겨 있었다** — viz env 0건 · relay 미구성 · `COLAB_AI_DB_URL` 부재 · 주체 표 공백 · named volume 소유권. 헬스 골격 판정으로서 ✅ 는 유지하고, 그 위의 배선은 `W7` 이 닫았다.
| I3 배포 자동화 | ⬜ | |
| I4 운영 준비 (추적·알람·복구 리허설) | ⬜ | |
| I5 prod 전환 | ⏸ | I4 + P7 — **보류** (`㊻`). v2 개발의 종점은 여기가 아니라 **staging 전체 green** 이다 |

### T-G 게이트
| WU | 상태 | 비고 |
|---|---|---|
| G1 패키지 신선도 게이트 | ✅ | **재빌드는 필요 없었다** — 15개 임베드 블록이 8개 에픽 전부에서 원본과 일치(sha256). 낡은 것은 *내용*이 아니라 "미반영"이라 적어둔 **서술**이었다. 산출 = `tools/check-package-freshness.py` · `planning-freshness` 게이트 · `PACKAGE-FRESHNESS.md` · `planning/README §3` 정정. **오라클은 content-hash 전용** — 파일명(`260817`)과 mtime이 둘 다 거짓말을 한다. selftest가 변조 fixture + 미마운트 둘 다 red 로 fail-closed 증명. 남은 것 = 정본 `E-00 README §4` 의 낡은 서술(Ted 판단, `sessions/G1.md` R3) |
| G1b E-04·E-02 패키지 재빌드 | ⬜ | **문서 임베드는 이미 일치**(G1 게이트가 매일 지킨다). 남은 것은 **목업·화면** 최신성뿐 — `planning-freshness` 는 이걸 판정하지 않는다 |
| G2 자연어 검색 엔진 | ✅ | **해소 (2026-08-23, `§9-㊶`)** — Postgres 단독(tsvector+pgvector). P4·K4 차단 해제 |
| G3 E-01 권한 원칙/적용표 분리 | ✅ | **원칙 문서 초안 완료** — 산출 `PERMISSION-PRINCIPLES.md`(P-1~P-34). 정본이 열어둔 9건 중 **4건을 이번 세션에 닫았다**(`§9-㉖㉗㉘㉙` — 잠금 시행 위치 · 접근 상태 기본값 · 감사 기록 · 재위임 금지). **G7 통합 후 `IA_사이트맵.md 7장`(권한표 원본) 대조 완료 — E-01 §2 와 동일, 새로 얹을 것 없음.** 잔여 미결 4건(①②⑤⑥)은 P8·P1 전까지 유예이며 원칙 문서에 목록으로 남아 있다 |
| G4 미배치 화면 3건 단계 배정 | ✅ | **순서표 갱신 완료.** ① `연구실 설정 > 구성원·권한` → **P1**(P1 단계 취지 문구도 함께 개정 — 권한 값의 원천 하나만 예외로 쓰기 화면을 연다) ② S-08 → **이미 P2 에 배정돼 있었다**(미배치는 실제로 3건이 아니라 2건) ③ 연구실 정보 읽기 모달 → **P7 편입**(E-07 안의 컴포넌트라 별도 단계로 뺄 이유가 없다). 조사 = `sessions/G4.md` |
| G5 DataModel v1.8 ↔ 스키마 대조 | ✅ | 산출 `DATAMODEL-BASELINE.md` — 기준표(누락 0) · v1 diff · 소급 위험 11건. **v1 저장 형태 계승 0**(계보·프로젝트 테이블 자체가 없음). 결정 `PLAN-SoT §9-⑰⑱⑲⑳` · 신규 열림 `㉑`(D4→D3 Port) |
| G7 기획 SSOT 통합 | ✅ | **정본 폴더 하나로 완결.** 인덱스 6파일을 정본 루트로 통합하고 260818 기준으로 갱신. **깨진 상대링크 38건 → 1건**(남은 1건은 P1 README → 없는 package html, P1 은 1차 범위 밖이라 미수정). 재검증 = 내가 직접 링크 스윕(222개 중 1개) · 스토리 실측(E-01 7 · E-07 8 · 합 45) · 게이트 green. **정본 내용 변경 1건 = "에픽당 스토리 6개 상한" 폐기(`§9-㉝`, Ted 비토 가능 · 백업 `99 temp/정본-백업-20260822`)**. 남은 `[미확인]` = `연구실 설정` 화면(S-07)의 소유 에픽이 어느 문서에도 없다 |
| G8 온톨로지 범위 합의 | ✅ | **승인 완료 (2026-08-23, `§9-㊸`)** — ④-1 (다) 하이브리드 · ④-2 (가) 문서 정본(목업은 예시) · ④-3 (가) D9 시드(④-4 함의, 비토 가능) · ④-4 **시드 테이블 3개**. 산출 `ONTOLOGY-SCOPE.md`(값 기재 완료). **K1 진입조건 충족** **⭑ 2026-08-24 — 후속 `G8b`(2차 범위) 초안이 레포에 들어왔다: `sessions/G8b-DRAFT.md`.** 판정 자료이지 판정이 아니다. **Q1(관계 값의 출처 채널)이 열린 채 Ted 를 기다린다** — `§9 〈82〉-㉱` · `§4` 블로커 #12 |

### T-D 도메인·계약
| WU | 상태 | 비고 |
|---|---|---|
| D1 도메인 경계 확정 | ✅ | **배정표 확정 — `DOMAINS.md §7` 신설**(10개 전부 Ted · 협의 필수 5곳은 **정본 대조** · 딸린 규칙 4개). 근거 `§9-㉚`·**`§9-㊴`**. **2026-08-23 닫힘** — 별도의 수문학 소유자를 두지 않고 **10개 전부 Ted**, 대신 **D9 의 판단 근거를 사람의 암묵지에서 `기획 정본` 으로 옮겼다.** 정본에 없으면 `[정본 무근거]` 로 남기지 만들지 않는다. `DOMAINS §3-③` 이 경고한 "아무도 검증하지 않는" 상태를 막는 방어가 이것이다 — **출처가 문서면 검증이 가능하다** |
| D2 계약 동결 | ✅ | **완료 판정(spectral + oasdiff green) 충족.** 산출 = `contracts/schemas/common.json`(정의 21종 · 값 집합 enum 9종을 DataModel v1.8 에서 전수 대조) · seam 3종 `fe-core`(op 34) `core-ai`(op 2) `core-viz`(op 5) · async 봉투 `contracts/events/`(이벤트 7종) · 게이트 `contract-lint`(spectral 6.16.3 핀) `contract-breaking`(oasdiff 도커 다이제스트 고정) · `contract-selftest` **15 케이스 green**. **진입조건 이탈 1건 — 2026-08-23 해소.** 착수 당시 `D1` 이 열려 있었으나 계약이 그 값을 소비하지 않아 전제를 밝히고 진행했고, `§9-㊴` 로 D1 이 닫히며 전제가 사실로 확인됐다(`§5`). 결정 ㉟㊱㊲ |
| **D3b RLS 실효 증명** | ✅ | **2026-08-23 닫힘** — 게이트 `rls-effect` 로 승격. ①본체 음성(허용자 아님·만료 → 0행) ②메타 양성(잠긴 것의 메타는 조회됨, `P-13`) ③cross-tenant(18표 전수 0행). **red fixture 18/18**, 그중 4방향이 **잘못된 롤이면 red**(superuser·BYPASSRLS·소유자 이전·없는 롤) — 소유자 롤로 도는 게이트는 green 을 보고하면서 아무것도 증명하지 않는다. 부수로 `db-selftest` 결합 결함을 고쳤다(기준 케이스가 레포 정본 allow-list 를 읽어, K1 의 정당한 `d9_*` 추가가 「낡은 면제」로 걸렸다 — **게이트가 옳고 selftest 배선이 틀렸다.** 검사와 3줄을 둘 다 남기고 픽스처를 격리). ~~신설~~ `rls-coverage` 는 정책이 *걸렸는지* 만 본다. 정책이 *실제로 막는지* 는 음성·양성 테스트만 증명한다 — ① 허용자 아님·만료됨 → 본체 조회 **DB 층 0행** ② 잠긴 데이터셋의 **메타는 조회됨**(`P-13` 회귀 방지) ③ cross-tenant 0행. **진입조건 = P0 스키마** |
| **D2b 이벤트 계약 게이트** | ✅ | **2026-08-23 닫힘** — `event-lint`(ajv 8.17.1 핀 · 스키마 컴파일 + 유효 5 통과/무효 8 거부) · `event-breaking`($defs 단위 diff, **ERR 14규칙 / WARN 3규칙을 표로 명문화** — 파괴 규칙이 암묵인 게이트는 게이트가 아니다) · `event-selftest` **33 케이스**(그중 5건은 green 을 주장한다 — enum 추가·선택 필드 추가·제약 완화. 「파괴가 아닌 것」의 절반도 증명해야 한다). **실제 파괴가 red 를 냄을 fixture 가 아니라 진짜 파일로 증명**했다(`envelope.json` 의 enum 값 제거 + required 추가 → E06·E04, 되돌린 뒤 `contracts/` 바이트 동일). 산출 `sessions/D2b.md`. ~~신설~~ `contracts/events/**` 를 보는 게이트가 없다 — `contract-lint` 는 `seams/**` 만, `contract-breaking`(oasdiff)은 OpenAPI 전용. **이벤트 계약의 파괴적 변경을 아무도 못 잡는다.** 필요 = `event-lint`(ajv) + `$defs` 단위 diff + red fixture selftest |
| D3 경계 강제 장치 | ✅ | **2026-08-23 닫힘** — 잔여였던 RLS 실효 증명이 D3b 로 완결됐다. 이하 원 기록: | **게이트 6종 배선·증명 완료. 남은 것은 RLS 실효 증명 하나**(→ **D3b**, P0 스키마가 있어야 쓸 수 있다). `import-boundary`(import-linter 2.13 · 계약 7개) · `banned-import`(ast · 배포 단위별 deny) · **`ai-no-lineage-write`**(음성 · red 조건 12개를 계약/코드/체인 3단으로) 배선 완료, **`boundary-selftest` 30 케이스 전부 의도대로 green**. 세 게이트 자체는 **red 이고 그게 정상** — `services/`·`db/` 에 코드가 없다("AI 가 계보에 쓰지 않는다"와 "AI 가 아직 없다"는 다른 사실이라 0건을 green 으로 세지 않았다). **부수 산출 = 모듈 경로 관례 확정**(`sessions/D3-boundary.md`) — P0 가 이걸 따른다. **DB 게이트 3종도 완료** — `migration-single-head`(alembic 미사용, `ast` 로 `down_revision` 그래프 직접 파싱 → DB 접속 없이 판정) · `schema-diff`(선언 `schema.sql` 을 일회용 postgres 에 적용해 `pg_dump` 대조. **DB 없음·도커 없음도 red** — 여기서 skip 하면 그게 정확히 v1 의 실패다) · `rls-coverage`(`pg_class`/`pg_policy` 조회 ↔ `gates/config/rls-allowlist.toml`). **`db-selftest` 38 케이스 green**(도커 없이 도는 24 + e2e 14). **한계 = RLS 정책의 *내용* 은 안 본다** — `USING(true)` 가짜 정책이 통과한다. ㉖ 이 요구한 실효 증명은 D3b 다. **C4 입력은 없어도 진행됐다** |

| **D5 인제스트·파이프라인 (포맷 처리)** | 🟧 | **2026-08-23 — WU 완료조건 ①~④ 전부 로컬 green · staging 배포만 남음(이 레인은 배포 권한이 없다).** 산출 = `services/pipeline-worker` D5 모듈 6종(감지·TIFF 판정·HSR·기준격자·파싱·COG·파이프라인) + 테스트 32건(단위 26 + 실데이터 E2E 6) 전부 green, **red 선행 확인**. ① 확장자 스왑이 매직으로 바로잡히고(`DR-3`) **좌표 못 찾은 파일은 FAILURE + `[미상]`, 소스에 `linspace` 0건**(`DR-9` 음성) ② 이미-COG 3부류 red→green — 합성 픽스처 + **실물 62건 전수 재판정이 SEED-DATA 실측(6/16/40)과 정확히 일치** ③ `banned-import`·`import-boundary`·`boundary-selftest` green(core-api rasterio 0 · pipeline-worker 만 geo 허용) ④ 실파일 4포맷 각 ≥1건 감지→파싱→좌표→COG 완주(NetCDF GK2A · Binary HSR 2881×2305 · HDF4 MOD15A2H h27v05 · GeoTIFF HLS 이미-COG + KWRA 스트립→COG 변환). `DR-1`(README 표 정정) ✅ · `DR-2`(입력↔산출 층 분리 + 음성시험) ✅ · `DR-3` ✅ · `DR-9` ✅ · `DR-11`(스트리밍/청크) ✅ · `DR-12`(오버뷰 범주/연속 분기) ✅ · `DR-14`(자동 추출) ✅ · `DR-15`(계보 레코드에 가공 방식 필수) ✅. **HSR 헤더 오프셋을 실파일로 실측 확정**(TIME_SS@3·nx@20·ny@22·num_data@32 — 원천 배포본은 헤더가 3블록을 선언하며 1블록만 담는다: mismatch 로 기록). HDF4 는 pip 환경 제약으로 GDAL 드라이버 대신 **pyhdf** 채택(휠 설치 확인). outbox/워커·reaper(⑹ 잔여)와 staging 배포는 후속. 실행 기록 `sessions/D5.md`<br> **⭑ ⟨stage 1 개정 2026-08-24⟩ D5 의 stage 1 범위가 다시 그어졌다** — **stage 1 = 감지 · 지원 포맷(＋`NumPy`) · 사람이 올린 기준 격자 파일의 수용·축 판별 · outbox/워커** / **stage 2 = 헤더 파싱 · 좌표계 변환 · COG.** 근거·값은 `PLAN-SoT §9 〈74〉〈75〉〈77〉〈79〉` | **+ 2026-08-25 — staging 배포됐다**(`c5a2fbf` 워커 배선 · `974cf9f` 저장 키). 시험 118/0. **여전히 🟧** — 저장 배치 회귀 시험 0건(`§4` #26) · `/healthz` 가 실이미지인데 `implemented:false`(`§4` #23).

### T-P 플랫폼 (에픽 0~7)
| WU | 단계 | 상태 | 진입조건 |
|---|:--:|---|---|
| P0 공통 기반 | 0 | ✅ | **2026-08-23 닫힘.** 완료 판정 8행 중 1~7 충족(8 은 조문 자체가 「I2 이후」). 스키마 20표 · core-api **34 라우트**(실질의 5 · 501 29, code 두 종으로 「저장처 없음」과 「로직 미착수」를 구분) · **경계 증명 6종**(cross-tenant 음성 4 + `body_access` 실효 2, red 9/9) · FE 셸. 산출 `sessions/P0-schema.md`·`P0-core-api.md`·`P0-rls-proof.md`·`P0-frontend.md` |
| P1 카탈로그 + 상세 헤더 | 1 | ✅ | **닫힘 — 네 레인 전부 + staging 배포 green** (2026-08-23). 작업지시서 `sessions/P1.md`. ✅ **P1-db** `d3_dataset.file_count` 메타 열 + 문장 트리거(증분) + 백필 + 드리프트 시험 3종 — `㊼`·`㊽` ✅ **P1-fe-catalog** S-03 8열·헤더 조건·잠긴 행(`조각 N` 포함)·0건, 시험 24 ✅ **P1-fe-members** `구성원·권한` 4스위치·재위임 금지(서버 배열 준수)·확인 모달 1회=요청 1회, 시험 13 ✅ **P1-api** 4 op 구현 — **501 표 29 → 25**. cross-domain 전부 Port 경유, 계약 무수정. 재위임 금지를 서버가 강제(행별 `editablePermissions` + 403). 산출 `sessions/P1-api-report.md` ✅ **P1-fe-detail** S-05 상단(되돌아가기·헤더 4줄·기본 정보 9칸·잠긴 상세·묘비 404), 시험 22. 화면 배치는 집 관습인 `src/routes/` 를 따랐다(지시서의 `src/pages/` 는 비어 있다). **목업(기본 정보 4칸) ↔ 정본 §5(v1.9, 9칸) 충돌 → 정본을 따랐다**(`㊸-④-2`). 산출 `sessions/P1-fe-detail-report.md` ／ **검증**(두 레인이 게이트를 동시에 돌린 구간이 있어 최종 트리에서 재확정) — core-api pytest **144** · frontend vitest **73** + typecheck clean · 게이트 **12종 GREEN** · `generated-up-to-date` 만 RED(미구현, 설계대로) · selftest **exit 0** · `rls-effect` 본체 음성 · 메타 양성(`P-13`) · cross-tenant 전수 0행 · `schema-diff` 두 체인 각각 선언=적용 ／ **staging 배포 green** (커밋 `06eafa9`) — 컨테이너 8/8 healthy · **헬스 6종 전부 200** · `alembic_version_platform`=`0002_p1_file_count` · `_ai`=`0003_k2_ontology_seed` · K2 시드 22행 · **본문 대조 실시**(브라우저 렌더 — 카탈로그 8열 6건, 잠긴 행이 🔒+`조각 3` 유지, 상세 기본 정보 9칸, 잠긴 상세는 기본 정보 통째로 비어 있음). 배포 전 백업 1회 GREEN(platform·ai) ／ ⚠ **P6 가 되짚어야 한다** — `accessRequestPending`·`canApproveVerification` 이 **항상 `false`** 다. 지어낸 값이 아니라 **저장처가 P0 스키마에 없다**(P6 몫). 화면이 조용히 거짓을 말하는 자리다 ／ ⚠ **되돌리려면 `rollback.sh` 가 아니라 수동 `:prev` 태그다** — 배포 전 5개 앱 이미지를 `colab-v2/*:prev` 로 박아 뒀다(`docker images` 에 살아 있음). `rollback.sh` 는 직전 릴리스가 아니라 **I2 이전 자리표시로 간다**(`DR-5`·`DR-1`) ／ **정직한 한계** — 엣지에서 토큰 없이 보면 **구현 op 과 미구현 op 이 둘 다 401 이라 구별되지 않는다**(인증이 501 핸들러보다 먼저 걸린다). 501 축소는 로컬 144 시험과 검증된 트리로 증명된 것이지 staging 에서 독립 증명된 것이 아니다. 화면 렌더도 **픽스처 기준**이다(비로그인) |
| P2 업로드·계보 확정·S-08 | 2 | 🟧 **실행 중 범위 재정의로 멈춤 — `S1` 로 재편** | **부분 완료로 닫지 않는다**(`CLAUDE.md §5`). 2026-08-24 Ted 가 stage 1 을 다시 잡아(`§9 〈70〉`) 미리보기·가시화·검증이 범위 밖으로 나갔다. **만든 것은 지우지 않았다** — 커밋 `585e27b`·`ee79064`·`90b9793`·`f4933e4`·`4fce105`·`c39702f`·`a868d64`. 배정 = **살린다 13 · 휴면 8**(`sessions/S1-PLAN.md §2`). **살아남는 것** = `0004`·`d5_*` 원장·outbox/워커/릴레이/reaper·`upload.accepted`·포맷 감지·업로드 6 op·계보 3 op·음성 시험 8종. **휴면** = `viz-render` 전체·미리보기 중계 2 op·격자 3 op·S-08 FE·헤더 파싱/좌표/COG. **⚠ 휴면은 시험을 끄는 것이 아니다** — `pytest -m stage2` 가 CI 에서 계속 green 이어야 한다(`〈71〉-㉰`). 미완 2건은 `S1-PLAN` 이 재배정했다: 격자 후주입 400(격자가 범위 밖이 되어 소멸) · 501 표 문서 갱신(29 로)<br> **⭑ ⟨재개정 2026-08-24 — `〈74〉`~`〈80〉`⟩ 전제 하나가 정정되어 배정이 다시 바뀌었다.** **미리보기 3층과 기준 격자 파일이 stage 1 안으로 돌아왔다** — 따라서 **휴면에서 살아나는 것** = `viz-render` 전체(배포 복귀) · 미리보기 중계 2 op · 격자 3 op · S-04 미리보기 패널 · `axis.py`·`grid.py`. **여전히 휴면** = S-08 FE · 타일 서명 · 헤더 파싱/좌표/COG · `d5` 6파일. **501 = 24 로 변동 없다**(29 는 무효). **배정 계수는 살린다 13 → 18 · 휴면 8 → 4 로 이동하나 실물 계수는 `[미확인]`.** 값과 근거 = `PLAN-SoT §9 〈74〉〈75〉〈76〉〈77〉〈78〉〈79〉〈80〉` · 집행 계획 = `sessions/S1-PLAN-REFOUND.md` **⭑ 2026-08-24 `C1`** — 재편된 `S1` 이 `W3` 까지 왔고 체크포인트가 통과했다. P2 가 남긴 것 중 **업로드 워커 배선**(`1f56968`) · **계보 확정 FE 완주**(`da6c0f1`) · **「보기만 할게요」 결함**(`72206b9`)이 닫혔다. **미리보기·격자는 `〈74〉〈75〉` 로 stage 1 에 복귀해 `W4`(`S1-preview`)·`W5`(`S1-fe-preview`) 가 진다.** **P2 행 자체는 여전히 🟧 다 — 닫는 것은 `S1` 완주(`W7`)이지 이 체크포인트가 아니다** | **+ 2026-08-25 — 업로드가 staging 에서 실제로 돈다**(`974cf9f` 저장 키 = 배치 · nginx 8g). **그래도 닫지 않는다** — `addDatasetFile` 202 가 계약만 열리고 집행은 400 이고(`§4` #21), 저장 배치를 단언하는 시험이 **0건**이었다(`§4` #26).
| P3 계보 그래프 · 2D 시각화 | 3 | ⬜ | P2 |
| P4 검색 히어로 · 결과 | 4 | ⬜ | P1 · **G2** · K4 **⭑ 2026-08-24 — 검색 인프라가 먼저 섰다**(`f887d91` D3 tsvector 생성 열 3 + GIN 3 · `9f0fb10` 진입점 `/dataset-searches`). **`searchDatasets` 는 계약을 여는 회차에 구현돼 501 표에 행을 더하지 않았다.** 소비 화면은 `W6`(`S1-fe-search`) | **+ 2026-08-25 — 화면이 섰다**(`15847b3`) 그리고 **staging 에서 실결과**(`강우` 3건 · `degraded:false`). **⬜ 를 유지하는 이유** = `K4` 완료 판정이 **평가셋 대비 재현율**인데 평가셋이 아직 **초안**이다(`S2b`, Ted 승인 대기).
| P5 프로젝트 | 5 | 🟧 **레인 `S1-project` 산출 완료 · WU 는 아직 안 닫는다** | P1 ／ **⭑ 2026-08-24 `W5` 레인 `S1-project`** — S-02 목록·S-02b 상세와 **`linkProjectDataset` 이 열렸다**(`S1-PLAN §4.2` P5 행이 「여기서 열린다」고 지목한 op). **501 표 24 → 21** — 뺀 셋 = `listProjects`·`getProject`·`linkProjectDataset`, 자리마다 실동작 시험(`tests/test_project_screens.py`). 값과 근거 = `§9 〈84〉`. **D6→D3 은 테이블로 건너지 않았다** — `d6_project.py` 는 자기 표의 식별자·의미 문장만 내고 조립은 `routes/project.py` 가 한다(`routes/catalog.py` 의 반대 방향과 같은 무늬 · 새 Port 없음). 실측 = core-api **246 → 260 passed** · frontend **166 → 198 passed** · viz-render **90** · pipeline-worker **104**(9 errors = DB 부재, 변동 없음) · 게이트 **21 green / 1 red**(`schema-diff`, 설계상) · `tsc` red **3 유지**(preview 2 · upload 1 — 이 레인 것이 아니다). 커밋 `e4e552a`·`2a0c489`<br> ⚠ **닫지 않는 이유 둘** — ① 프로젝트 op **넷이 아직 501**(`updateProject`·`deleteProject`·`setProjectStatus`·`unlinkProjectDataset`) 이고 **P1 배정**이라 이 레인이 범위를 늘려 가져오지 않았다(`CLAUDE.md §5`). 화면은 그 자리를 `data-slot` 으로만 둔다 ② 새 프로젝트 모달·정보 수정 모달·닫기 확인 모달(F-03~F-05)이 아직 없다 | **+ 2026-08-25 — 변동 없음.** `〈84〉` 산출 그대로. P6(승인)·`§1.5` 완료 조건이 남아 닫지 않는다.
| P6 승인 처리 | 6 | ⬜ | P1~P5 |
| P7 연구실 대시보드 | 7 | ⬜ | P6 |
| P8 E-01 적용 지점 표 | 후 | ⬜ | P7 |

### T-K 지식·AI
| WU | 상태 | 비고 |
|---|---|---|
| K1 온톨로지 스키마 | ✅ | **2026-08-23 닫힘.** 시드 테이블 3개 — `d9_method_term`·`d9_topic_synonym`·`d9_place_alias`, 전부 `db/ai` 체인(`0002_k1_ontology`). 그래프 구조 없음(`㊸-④-4`). **RLS 를 걸지 않았다** — 세 표에 `lab_id` 가 없고, 공유 어휘를 경계로 감싸면 `current_lab_id()` NULL 비교로 전 연구실에서 안 보이게 되어 사전이 죽는다. 면제가 소극적 선택이 아니라 옳은 값이며, 뒤집히는 조건을 allowlist 주석에 남겼다. 게이트 4종 green · **왕복 증명이 음성을 포함한다** — 목업의 `유출·수문` 이 CHECK 위반으로 튕긴다(`㊸-④-2` 「목업은 예시」가 DB 층에서 강제된 실물). 산출 `sessions/K1.md` |
| K2 온톨로지 시드 적재 | ✅ | **2026-08-23 닫힘.** **적재 22행**(어휘 13 · 주제 5 · 지명 4) / **커버리지 기준 21항목 미커버 0**. 둘이 다른 수인 이유 — 기준의 주제는 4값이고 적재는 5행이다(항등행 4 + 정본이 준 유일한 동의어 `강우데이터` 1) — 21건 전부 정본에서 직접 grep 해 인용 위치를 기재했다. **중요한 것은 넣은 것이 아니라 넣지 않은 것이다** — 정본이 준 주제 동의어는 `강우데이터` **하나뿐**이었고 나머지를 만들지 않았다. `크롭` 은 §2.6 표의 의역이라(정본 grep 0건) 원문 `유역 경계로 잘라냄` 만 적재. `[정본 무근거]` 5건은 `K2.md §5`. 커버리지 체크 red 실증 5방향 — 그중 **기준 파일을 13→12 로 줄이면 red**(검사 대상을 줄여 green 을 얻는 경로를 스스로 막는다). 시드는 마이그레이션 체인 안(`0003`), `ON CONFLICT DO UPDATE` 멱등 실측. 산출 `sessions/K2.md` |
| K3 계보 제안 서비스 | ⬜ | **D4 쓰기 경로 부재 음성 테스트 필수** · 모델 = `㊷` GPT(luna/terra) — **luna 품질 실측 후 승격 판단** |
| K4 자연어 검색 서비스 | ⬜ | ~~G2 차단~~ **해제**(`㊶`) · 모델 = `㊷` **⭑ ⟨개정 2026-08-24 — `§9 〈81〉`⟩ `pgvector` 와 임베딩 파이프라인은 stage 1 밖이다** — 이미지가 pgvector 를 제공하지 않는다(실측). **stage 1 = tsvector 매칭·순위 + LLM 질의 해석**(`〈72〉-㉮`). 2층 분리 유지(`〈71〉-㉱`) — **`K4-a` 는 `G8b` 에 안 묶여 `W4` 에서 바로 간다**, `K4-b`(그래프 증강)는 `G8b` Q1 대기. 우선순위 = **`〈82〉`** | **+ 2026-08-25 — 서비스는 섰다**(`K4-a` 실행기 core-api 이관 · `K4-b` 그래프 증강 · 한국어 접두 + `pg_trgm`). **닫지 않는다** — 완료 판정의 오라클(평가셋)이 초안 상태다.
| K5 제안 원장 | ⬜ | |

**T-S 초기 데이터** — 원천 `03 Reference-Data`(3.5 GB · 2,122 파일). 상세 = `WORK-UNITS §8.5` · 결정 `㊾`

| WU | 상태 | 비고 |
|---|:--:|---|
| **S1** 원천 인벤토리·적재 범위 확정 | **✅** | **지금 착수 가능**(코드 아님, 분류다). 산출 `SEED-DATA.md` — 2,122 파일 전부를 넣는다/시험용/제외 로 배정, 미분류 0. **미결 둘은 `㊿` 로 닫혔다** — `tif` 27 건 = **5번째 입력 포맷**(D5 +1) · `.npy` 1,622 건 **제외**. S1 이 이를 집행했다 — **산출 `SEED-DATA.md`**. 배정 **① 292 · ② 148 · ③ 1,682 = 2,122, 미분류 0**(`〈57〉` 정정 — `04.Lat_Lon_info` 좌표 기준 격자 14건을 ③→① 로 옮겼다) · 데이터셋 15건 · 계보 3갈래 · 온톨로지 D9 22행 대조. **범위를 바꾸는 발견 4건**(§0): **F-1 GRIB 원파일 0건** · **F-2 `.hdf` 는 HDF4** · **F-3 tif 6/6 이미 COG** · **F-4 bin 명세는 코드에만 있고 크기 불일치**. **2026-08-23 닫힘** — 마지막 빈칸이던 **연구실 귀속이 `〈52〉` 로 확정**됐다: **고려대학교 수문학연구실(전창현 교수)**. 원천의 가뭄 메타데이터 카드가 생산자를 `차호영·고려대학교` 로 적어 둔 것과 일치한다. **v2 의 첫 실제 연구실**이라 시드 15건 전부의 `lab_id` 가 여기를 가리키고 **RLS 가 처음으로 진짜 자료를 지킨다** |
| **PA** 로그인 · 세션 | ✅ | **2026-08-26 — 구현 완료.** 정본 개정 = `PLAN-SoT §9 〈107〉`·`〈108〉`(Ted 지시 인가). **교체 지점 = `services/core-api/src/colab_core/kernel/authn.py::build` 한 곳** — 요청 경계·라우터·도메인·화면 어느 것도 수단을 모른다. **마이그레이션 0**(무상태 HMAC 서명 세션 · 세션 표 없음). **계약 5차 동결 해제** — 신설 `createSession`·`endSession` 둘뿐이고 기존 50 op 무변경. **기존 주체 표 병존**(대체 아님). 어드민 = 계정＋비밀번호, **scrypt 해시만 저장**(`kernel/password.py`·`kernel/credentials.py`), 회전 = `ops/set-password.py`, 시도 제한 5회/900초 → 429.<br>⚠ **결손 둘을 감추지 않는다** — ① 무상태라 **만료 전 조기 회수 불가**(`〈107〉-㉳`) ② 시도 제한은 **프로세스 안에서만** 센다(`〈108〉-㉰`).<br>⚠ **정본 충돌 1건** — 정본은 비밀번호 로그인을 명시적으로 뺐다(`PRD_계정과_연구실_소속 §5.2`). `〈108〉-㉮` 판정 대기.<br>**이 회차가 하지 않은 것** — staging 배포 · `colab` 계정 실제 생성(스크립트만) · 비밀번호 회전 | 실측 core-api **407/0** · frontend **274/0** · `tsc` 0 · 게이트 15종 green (`schema-diff`·selftest 5종은 **안 돌렸다**) |
| **S2** 초기 데이터 staging 적재 | ⬜ | **⭑ 2026-08-26 — 멱등 설계 확정(`〈106〉`).** 귀속처 = **적재 도구**(제품 무변경). 조회 = `listDatasets` 전수 순회 ＋ 이름 완전 일치. 완료 조건 ② 판정 = **진행표 없이 전량 재실행 → 쓰기 호출 0건 · 11/139/5 불변**. 절차서 = `sessions/S2-EXEC-PLAN.md §5.4`(설계)·`§14`(도구 명세). **진입조건 = 적재 도구 구현**(명세만 서 있다) ＋ `§4` ⛔ 둘.<br> **P2 필요**(업로드·계보). **DB 직접 INSERT 금지 — 화면 경로(S-04→파싱→COG→계보)로만.** 완료 = 카탈로그에서 보이고 상세가 열리고 재실행 멱등 + IS3 복원 리허설 재통과<br> **⭑ ⟨stage 1 개정 2026-08-24⟩ 화면 경로 = S-04 → 감지 → 등록 → 미리보기 → 계보 확정.** 파싱·COG 는 stage 2 다(`〈70〉-㉯`·`〈74〉`) **⭑ 2026-08-24 — 업로드 경로가 실제로 켜졌다**(`〈73〉` 워커 배선, `1f56968`). ⚠ **다만 staging 실배포는 `[미확인]`**(`§4` 블로커 #18) — 적재를 시작하기 전에 **띄워서 확인하는 것이 먼저다** | **⚠ 2026-08-25 — staging 은 지금 시험 픽스처로 시드돼 있다**(A/B 두 연구실). **실데이터가 아니다.** 적재는 화면 경로로만(`㊾-③`) — 픽스처 철거 시점은 판정 대기(`§4` #22).
| **S3** 실데이터 **4종** E2E | ⬜ | **P3 필요**(시각화). **GRIB 차단 해제** — `〈51〉` 로 범위에서 뺐다(수급하지 않는다). 지원 = **NetCDF·Binary·HDF4·GeoTIFF**. ⚠ **PoC 4포맷과 수만 같고 구성이 다르다.** 완료 정의 7번 그 자체. **GeoTIFF 를 가장 먼저 돌린다** — PoC 선례가 없는 유일한 경로다(`㊿`). 실패 파일은 목록으로 남긴다 |

## 2. 현재 상태 스냅샷

- **개발환경**: WSL. 레포는 기존 작업트리를 그대로 승계(`core.fileMode false` 유지) · `gh` 인증 완료. **레포 밖 자산(기획 정본)은 승계되지 않는다** — 2026-08-22 정본을 작업공간 형제 폴더로 들여와 이 위험을 줄였다(`PLAN-SoT §9-㉕`).
- **레포**: `CognileapAI/colab-v2` (신규 모노레포, **public** — PLAN-SoT §9-⑯). **원격 push 완료**, `origin/main` = 스캐폴드 커밋(`620238c`). `main` 보호 ON: force-push·삭제 차단, 리뷰 필수 없음(1인 CODEOWNERS 셀프-락아웃 회피), enforce_admins off. CI 1회 완주 — 게이트 잡 전부 "미구현 — red". 스캐폴드 = `contracts/` `services/{core-api,pipeline-worker,viz-render,ai-service}` `frontend/` `db/{platform,ai}/` `gates/` `eval/` `infra/` `dev-package/` `planning/` + CODEOWNERS + CI 골격.
- **게이트**: **23종 전부 구현 · 전부 green (2026-08-25).** ⟨옛 기록 — 13종 중 12종 구현⟩ `planning-freshness` `contract-lint` `contract-breaking` `event-lint` `event-breaking` `import-boundary` `banned-import` `ai-no-lineage-write` `migration-single-head` `rls-coverage` `rls-effect` `schema-diff`. 미구현은 `generated-up-to-date` 하나이고 **설계대로 red** 다. 자기 증명 **139 케이스**(contract 15 · event 33 · boundary 30 · db 43 · rls-effect 18). **⭑ 2026-08-24 `C1` 실측 — 전 게이트 실행 = 21 green / 1 red.** 그 1 은 `schema-diff`(**적용 DB 가 지정되지 않았다** — DB 부재로 **설계상 red**, `CLAUDE.md §4`). 기준선 2 red 에서 `generated-up-to-date` 가 green 으로 회복했다. **회귀 0건.** ⚠ **`tsc` 게이트는 존재하지 않는다** — 그래서 전 게이트 green 이 타입 무결성을 뜻하지 않는다(`§4` 블로커 #16). **★ 2026-08-25 실측 — 게이트 23 · 전부 green.** `db-boundary` 신설(`c499123` · `〈92〉`)로 **21 → 23**(게이트 + selftest). `schema-diff` 는 **적용된 staging DB** 대조로 green 이 됐다(DB 를 지정하지 않으면 여전히 설계상 red). 자기 증명 **179 케이스**(contract 15 · event 33 · boundary 30 · **db-boundary 18** · db 43 · rls-effect 18 · seam-consistency 13 · generated 9). ⚠ **`tsc` 게이트는 여전히 존재하지 않는다** — 별도로 쟀다(`tsc --noEmit` 0).
- **코드**: **있다.** `services/core-api`(FastAPI, 34 라우트 · pytest 106) · `frontend`(React 앱 셸) · `pipeline-worker`·`viz-render`·`ai-service`(빈 도메인 자리 + 헬스). `db/platform` 20표 · `db/ai` 3표+시드 22행. **저장 형태 기준은 `DATAMODEL-BASELINE.md`가 정본** — 코드가 이 표와 어긋나면 코드가 틀린 것이다. **⭑ 2026-08-24 `C1` 시험 실측(기준선 → 현재)** — core-api **224 → 246 passed / 0 failed** · pipeline-worker **72 → 83 passed**(17 failed / 9 errors 는 **변동 없음** — `COLAB_REFERENCE_DATA`·`COLAB_PIPELINE_DB_URL` 부재에 대한 **fail-closed**) · frontend **147p+1f → 166 passed / 0 failed**. **501 표 = 24 유지**(`services/core-api/tests/test_not_implemented.py:72`). **★ 2026-08-25 실측 — core-api 332/0 · frontend 248/0 · ai-service 68/0 · viz-render 99/0 · pipeline-worker 118/0 · `tsc --noEmit` 0.** **501 표 = 23**(24 → 21 → 23 — `〈84〉` 가 셋 줄이고 `〈88〉` 4차 해제가 새 op 을 열며 늘었다). `db/ai` 에 그래프 두 표(`d9_concept`·`d9_concept_edge`) 추가 — 노드 49 · 엣지 19.
- **staging**: **살아 있다.** `www.colab-hydro.com` 에 5개 배포 단위 + postgres(2체인). 컨테이너 `colab_v2_staging_*` 8개. 앱 롤은 비소유자·NOBYPASSRLS. 백업은 일 03:30 · 주간 재검사. **★ 2026-08-25 — 헬스만이 아니라 제품이 선다.** 배선 다섯 자리를 이었고(`c5a2fbf`), 검색이 실결과를 낸다(`강우` 3건 · `degraded:false`), 미리보기 ①②가 공개 주소에서 200. 체인 platform `0006` · ai `0005`. 호스트 노출 `127.0.0.1` only(**`0.0.0.0` 0건**). 롤백 왕복 증명(530 없음 · 본문 대조). **⚠ 시드는 시험 픽스처다**(A/B 두 연구실) — 실데이터가 아니다. **주체 표는 비밀**(`〈93〉`).
- **AWS 리소스**: 없음. I1에서 IaC로 생성.
- **기획 정본**: `Co-Lab_ver2_1차마일스톤_목업패키지_260818_이태헌` — **작업공간 형제 폴더 `40 COLAB-기획/`**(2026-08-22 이전, `PLAN-SoT §9-㉕`). E-00 DataModel **v1.8**·Policy **v1.4**, E-01 Policy **v1.3** 확인 · DataModel `§8 미결 = 없다`(1차 저장 형태 전부 확정). 인덱스 5종은 260808판에만 있음 → G7.
- **전달 패키지**: 8종 중 **6종이 낡음**. E-00이 가장 크게 밀림(v1.7·v1.8 두 판).
- **참조 자산**: `20 CoLAB-v1` (이관 전 — 현재 `00 CoLAB-PoC`·`10 CoLAB-Launch`). 읽기 전용.
- **도메인**: `DOMAINS.md` 10개 정의 완료. **소유자 배정 완료**(`§7` — 10개 전부 Ted, D9 판단 근거는 기획 정본. `§9-㊴`).

## 3. 결정 로그

확정·열림 전체는 `PLAN-SoT.md §9`. 새 결정이 나면 **거기 적고 여기서 링크**한다(두 곳에 적으면 갈라진다).

최근 확정 — ① 신규 구축 ~~② AWS 배포까지~~(**㊻ 로 개정 — staging 까지**) ③ 정본 260818 ④ AI 도메인 분리 ⑤ WU 단위 ⑥ Assistant BC 미계승 ⑦ `colab-v2` 모노레포 ⑯ 레포 public *(전부 2026-08-22 Ted)* · **⑰ G5 대조 상대 = v1 스키마 ⑱ v1 저장 형태 계승 0 ⑲ 값 집합은 DB가 강제 ⑳ 파생값 2종 미저장** *(2026-08-22 G5)*. **㉑ D4→D3 읽기 Port 시그니처 확정 ㉒ "도메인 소유자" = 변경 승인권자 ㉓ staging=터널 호스트·prod=AWS ㉔ PoC 철거·데이터 폐기** *(2026-08-22 Ted)*. **㊵ 승격 게이트(staging 확인 → Ted 승인 → AWS) ㊶ 검색 = Postgres 단독 ㊷ 모델 = GPT 전면 통일(luna/terra, API 키 · Claude 어댑터 휴면) ㊸ 온톨로지 §④ 4항목 확정 — G8 승인, K1 진입조건 충족** *(2026-08-23 Ted)*. **㊹ 미구현 엔드포인트 = 501 + code 두 종(404 아님, 계약 무수정) ㊺ staging postgres 2 DB · 앱 롤 비소유자·NOBYPASSRLS** *(2026-08-23 Claude 판단 · 비토 가능)*. **㊻ AWS 보류 — v2 완주 판정을 `staging 배포 green` 까지로 내리고 prod 는 출시 결정 뒤에 연다** *(2026-08-23 Ted)*. **㊼ `fileCount` 는 메타다 — 조각 수를 `d3_dataset` 메타 층으로 올린다**(잠긴 행에도 `조각 N`) *(2026-08-23 Ted)*. **㊽ 마이그레이션 백필은 `FORCE RLS` 를 한 트랜잭션 동안만 내린다 — 런타임 무변경, 게이트가 복구를 강제** *(2026-08-23 Claude 판단 · 비토 가능)*. **㊿ `tif` 입력 포맷 채택 · `.npy` 제외** · **〈51〉 GRIB 을 범위에서 뺀다 — 지원 = NetCDF·Binary·HDF4·GeoTIFF 4종**(⚠ PoC 4포맷과 **수만 같고 구성이 다르다**) · **〈52〉 초기 데이터 귀속 = 고려대학교 수문학연구실(전창현 교수) — v2 의 첫 실제 연구실** *(전부 2026-08-23 Ted)* — **미결 부채 2건은 D5 로 넘겼다**(아래 §5.5). *표기: 원문자가 `㊿`(50)에서 끝나 51 부터는 `〈51〉` 로 적는다.* **㊾ v2 초기 데이터 원천 = `03 Reference-Data` · 적재는 화면 경로로만 · T-S 트랙 신설 · 완료 조건 8번(「초기 데이터가 화면에서 보인다」) 추가** *(2026-08-23 Ted 요청 · Claude 정리, 비토 가능)*. 열린 결정 **⑧⑨⑩ 해소**(⑪~⑮ 그대로 · **⑯ 즉시 해소** — `㊿`). **㉖(RLS 실효 증명) 은 D3b 로 닫혔다.** 새로 열린 것 — 없음 **★ 2026-08-25 추가 — `〈83〉`~`〈93〉`.** 그중 이번 세션이 새로 적은 것은 **`〈91〉` `F-8` = ㈎ 빼둔다**(승인 9 → 8) · **`〈92〉` `db-boundary` 게이트 — 매니페스트가 정본** · **`〈93〉` staging 주체 표는 비밀 · `0600` + 소유권 정렬**. **값과 근거는 `PLAN-SoT §9` 에만 있다**(여기는 링크만 — `CLAUDE.md §6-3`).

### 3.5 결정 이행 현황 — 결정과 실물은 다른 것이다

**결정 로그는 「무엇을 정했나」만 말한다. 「그게 실제로 걸렸나」는 안 말한다.** 그 둘이 갈라진 채 아무도 안 보는 자리가 `DR-8` 이었다(`〈51〉`·`F-2` 가 정본에 안 닿음). 같은 일이 반대 방향으로도 일어난다 — **이행은 끝났는데 문서가 모르는 것.** 여기서 둘 다 센다.

| 결정 | 이행 | 실물 |
|---|:--:|---|
| `〈61〉` 계약 동결 전 옳음 판정 | 🟧 **㉠㉡ 이행 · ㉢ 미실시** | **㉠·㉡ 기계 검사 구현·green**(D2c-C2 · `gates/tools/seam_consistency.py` — ㉠ 신설 50건 전수 통과 · ㉡ 15단계 완주 끊김 0 · selftest 13케이스). **㉢ 미실시 — 동결하지 않았다**(제출 패키지 `sessions/D2c-ted-approval.md`, ㉠·㉡ green 은 ㉢ 을 대신하지 않는다). ⚠ ㉡ fixture 는 `E04-step-op-map.DRAFT` 승계본 — **Ted 검토 전(DRAFT)** 이라 표가 틀리면 ㉡ 은 틀린 흐름을 완주로 판정한다 |
| `〈55〉` 주제 4값 DB 강제 | ✅ **걸렸다** | `db/platform/versions/0003_p1_topic_check.py` · `schema.sql:247` = `CHECK (topic IS NULL OR topic IN (4값))` — **nullable 유지까지 명세대로** · **staging 적용 확인**(`alembic_version_platform = 0003_p1_topic_check`) · 픽스처의 `유출·수문` 제거까지 한 묶음 |
| `〈58〉` 기준 격자 2건 + 축 열 | ⬜ **미이행** | `(dataset_id, grid_axis)` 제약은 **아직 안 썼다 — `0004` 예정.** 현재 DB 는 여전히 `d3_file_one_reference_grid_per_dataset` 로 **1건 강제** |
| `〈58〉`·`〈59〉` 격자 **추가·교체·삭제** | 🟧 **계약 경로 신설됨 (D2c-C1)** | **계약 층은 이행됐다** — `addDatasetFile`·`replaceDatasetGridFile`·`deleteDatasetGridFile` 신설(추가만이 아니라 **교체·삭제까지** — 「절반 이행」 함정 회피). **단 DB `0004`(`(dataset_id, grid_axis)` 제약)는 여전히 P2-db 몫**이고, 라우트는 501 표 등재만(D2c-api 대기). *(구 기록)* ~~⛔ 막힘 — 계약에 파일 조작 경로가 없다(`DR-7`). `D2c` 선행.~~ ⚠ **추가만 열면 결정의 절반 이행이다** — `〈59〉` 는 「격자는 **조정하는** 대상」이라 **교체·삭제까지가 정상 동작**이다(잘못 붙인 격자를 바로잡는 것 포함). 판정은 `업로드·편집` 스위치(`⑳`) |
| `〈60〉` `좌표계·격자 변경` 활동 기록 | ⬜ **미이행** | **스키마 변경은 불필요**(`d8_activity` 가 이미 받는다). 남은 것은 **쓰는 코드**뿐 — P2 |
| `〈51〉`·`㊿-F-2` 포맷 범위 | ✅ **걸렸다 (2026-08-23 재개정으로)** | ⚠ *(구 기록 — 「직접 수정 6건 + 정오표 2건」의 ✅ 는 **틀렸다**)* **정정 2026-08-23 — 이 ✅ 는 사실이 아니었다.** 「Ted 승인 2026-08-23」 이 달려 있었지만 **그 승인은 틀린 파일 목록 위에 서 있었다.** 실제로 고친 4건은 `01 CoLAB-Plan/reference/v2_1차마일스톤_기획_260808/` 과 `01 CoLAB-Plan/planning-base/` 아래에 있었고, 그 두 폴더는 `planning/README.md §2` 가 **「이제 참조하지 않는다 — 정본은 위 한 폴더뿐이다」**로 명시적으로 배제한 옛 사본이다. **260818 정본은 한 글자도 닿지 않았다.** `planning-freshness` 는 이것을 못 잡는다 — 그 게이트는 260818 정본 안의 임베드 ↔ 원본만 보고 다른 폴더는 시야에 없다. **재개정 완료 2026-08-23 (WU-P2 · NB-2 개정과 한 회차).** 260818 정본에서 `grib`·`HDF5` 를 쓰던 **7자리를 전부** `NetCDF · Binary · HDF4 · GeoTIFF` 기준으로 고쳤다 — E-04 Policy 3(§5 파일 · §9 그릴 수 없는 형식 · §10 파일 파서) · E-04 PRD 1(§7 파일 파서) · E-03 Policy 2(§8 그릴 수 없는 형식 · §8 지도 표현의 격자 예시 `(nc·grib·tif)`→`(tif·nc)`) · E-00 DataModel 1(§4.1 자동으로 읽은 정보). **지도 표현의 격자 예시까지 포함한 것은 Ted 판정이다** — 예시라도 정본에서 `grib` 을 본 사람은 그것을 지원 포맷으로 읽는다. 개정 전 정본에 `HDF4` 는 **0회** 등장했고 개정 후 등장한다. 패키지 임베드 3파일(E-00·E-03·E-04)을 원본과 재동기했고 `planning-freshness` 는 **본 체크아웃에서 green**(15개 블록 전부 MATCH). 검사기·정규화 규칙에는 손대지 않았다. 적용 기록 = `dev-package/sessions/P2-NB-2-canon-amendment-applied.md` 계약 무변경 — `core-pipeline.json` 의 인용은 D2c 개정 시 정본 기준으로 정리된다 |
| `〈56〉`·`〈57〉`·`〈59〉`·`⑳` | — | **문서 확정만 필요한 것들.** 별도 이행 없음 |
| `〈62〉` Ted 본인 확인 · 규칙 5 소급 면제 · 울트라리뷰 예약 | ✅ **문서 확정으로 이행 완료 (2026-08-23)** | 승인 꼬리표 13개 파일 정리(v2 5 · 기획 정본 8) · 소급 PR 없음(`㉯`) · staging 실측·전수 검수는 기능군 완료 시 울트라리뷰(`㉰`) — `sessions/D2c.md` 마감 표·`D2c-ted-approval.md` 갱신 |

> **다음 사람이 여기만 보고도 「무엇이 실제로 걸려 있나」를 알아야 한다.** 결정 로그로 되돌아가 하나씩 확인하게 만들면 안 본다.

---

## 4. 블로커 (사람이 풀어야 할 것)

> **★ 2026-08-25 (2차) 전수 대조 — 블로커 27건을 실물로 다시 쟀다. 해소 4건 정정**(#16 `tsc` 0 · #19 `〈85〉` · #23 헬스 본문 · #26 저장 배치 정본). **#19 는 `〈85〉` 로 이미 해소됐는데 ⛔ 표기가 존치돼 있었다 — 드리프트다.** 존속 = #8·#12·#13·#14·#17·#18·#20·#21·#22·#24·#25·#27. **새 판정 = `§9 〈94〉`~`〈103〉`**(격자 후주입 설계 · 3안 미채택 · 동기안 ㈑ 철회 · `〈70〉-㉴` 비저촉 · 「본체 1건 이상」 자리 이동 · `202` 철회 보류 · staging 권한 부여 불필요 · `libexpat1` 회귀 방지 · 저장 배치 정본 · 판단기준 5문항 미등재). **세부는 `§9` 에만 있다 — 여기는 링크뿐이다.**
／ **★ 2026-08-25 갱신 — 사람이 풀어야 할 것이 다시 하나 생겼다.** **#21(`addDatasetFile` 202 의 저장 자리 = 스키마 판정)은 Ted 의 판정이 있어야 움직인다.** 그리고 **⛔ #20**(지도형 미리보기 · 격자 전달 규약)은 사람 결정이 아니라 **WU 로 세워야 할 일**이다. 아래 #20~#27 이 이번 세션이 연 항목이다.
／ 이전 **2026-08-23 갱신 — 진행을 막는 사람 결정이 남아 있지 않다.** AWS 계정(#1)은 `§9-㊻` 으로 **보류가 결정됐고**, OpenAI API 키(#9)는 **투입됐다.** **전 트랙이 Claude 착수 가능이다.**

| # | 블로커 | 막는 것 |
|---|---|---|
| 1 | ~~**AWS 계정·결제**~~ | **성격 변경 · 블로커 아님** (2026-08-23, `§9-㊻`) — Ted 가 **보류**를 결정했다. 완주 판정이 `staging 배포 green` 까지로 내려갔으므로 I0·I1·I5 는 임계경로에서 빠진다. prod 는 **출시를 결정할 때** 연다. 실제로 이 항목이 막고 있던 P·K·D 트랙은 **하나도 없었다** |
| 2 | ~~**자연어 검색 엔진 결정**~~ | **해소** (2026-08-23, `§9-㊶`) — **Postgres 단독**(tsvector + pgvector). 별도 엔진을 두지 않는다. 경계가 RLS 로 공짜로 따라오고, 정렬 최종 결정권이 어차피 core 에 있다. **P4·K4 차단 해제** |
| 3 | ~~**온톨로지 범위 (수문학 소유자 시간)**~~ | **성격 변경** (2026-08-23, `§9-㊴`) — 더 이상 **사람의 시간을 기다리는 블로커가 아니다.** 범위·어휘를 **기획 정본에서 길어 올리는 작업**이고 승인은 Ted 가 한다. **G8·K 트랙이 Claude 착수 가능**이 됐다. 남은 위험은 하나 — 정본이 값을 주지 않는 곳에서 **만들어 채우는 것**. 그런 자리는 `[정본 무근거]` 로 남긴다 |
| 7 | ~~**`ssh.colab-hydro.com` ingress 가 오리진 없이 살아 있다**~~ | **해소 · 실행 완료** (2026-08-23, `§9-㉜`). 진단이 얕았다 — 잔재는 **세 겹**(ingress · Access 앱 · DNS CNAME)이었고 셋 다 제거했다. **엣지에 만든 것은 라우팅·인증·이름 세 층에 흩어진다** |
| 8 | **Cloudflare 터널 ID 가 public 레포 히스토리에 남아 있다** — 커밋 `2c06cc3` 의 `sessions/IS2.md`. 다음 커밋에서 값은 빠졌으나 히스토리는 그대로다. **자격증명은 아니다**(터널 조작에는 API 토큰이나 터널 토큰이 필요하고 둘 다 유출되지 않았다). 심각도 낮음이나 이 프로젝트 규약은 ID 도 레포 밖에 둔다. 선택지 = ① 그대로 둔다(권장 — `main` 은 force-push 차단) ② 히스토리 재작성 + 보호 일시 해제 ③ 터널 재생성(`TUNNEL_TOKEN` 무효화 = staging 재구성, 과하다) | 없음 (판단만) |
| 10 | ~~**`STOP-7` 미리보기 서버 렌더 부재**~~ | **해소 (2026-08-23 · `§9 〈63〉-㉮` Ted)** — **P2 가 렌더를 안고 간다.** `P2-viz` 레인을 신설해 미리보기 최소 렌더 경로(`createRender`·`getRender`·`getRenderTile`·`listPalettes` + FE 중계 2 op)를 P2 안에서 만든다. **범위 확대가 아니라 잘못 뭉친 배정의 정정** — `WORK-UNITS.md:168` 이 D7 을 통째로 P3 에 넣었는데 P3 이 P2 를 진입조건으로 삼아 **정본이 P2 화면에 요구하는 것을 아무도 못 만드는 자리**가 있었다. `NB-7`·`listPalettes` FE 중계 부재도 함께 닫혔다. **⚠ advisor 는 별도 WU 절단을 권고했고 Ted 가 흡수를 골랐다** — 「P2 가 한 WU 로 감당 못 할 수 있다」는 우려는 `sessions/P2-EXEC.md §8` 위험 1번으로 살아 있다. **데이터셋 상세의 2D 렌더 3종은 P3 그대로** |
| **11** | ✅ **해소 (2026-08-23 · `§9 〈66〉` Ted)** — `0004` 의 축은 **`carries_lat`/`carries_lon` 두 불리언 + 축별 부분 유니크 인덱스 2개**로 세운다. HSR 정본 격자는 **`rdr_500m_latlon.nc`**. 근거는 코드 실사용 조사(`sessions/P2-W0-R1-code-usage.md`)와 격자 재실측(`sessions/P2-W0-HSR-grid-measurement.md`). **W1 착수 해제.** *(이하 원문)* ⛔ **`R-1` — 격자 파일 한 장이 두 축을 다 담는다. `0004` 가 그것을 표현하지 못한다** *(2026-08-23 `W0-1-ⓐ` 실측으로 발견 · advisor 게이트 ② 가 Ted 판정감으로 판정)* — 실물 16건 중 **2건이 한 파일 안에 `lat` 과 `lon` 을 모두** 담는다(12.5%). 그중 하나(`3_bin/04.Lat_Lon_info/rdr_500m_latlon.nc`)는 **바이너리(HSR) 본체의 동봉 격자**라 「파일이 자기 격자를 갖고 있다」로 넘길 수 없다. 그런데 `〈58〉-㉠`·`P2.md §2-22` 가 세운 `0004` 는 `grid_axis` **단일값** + `CHECK(격자 파일 → NOT NULL)` + `UNIQUE(dataset_id, grid_axis)` 다 — **축을 못 갈라서가 아니라 둘 다 갈려서 적재가 막힌다.** ⚠ **`〈58〉` 확정문 자체가 「위도·경도 한 쌍이 실물이다」를 전제로 섰고 실측이 그 전제를 깼다** — 정본 전제의 개정 여부라 관례 판정으로 못 닫는다. **선택지 = ㈏ `grid_axis` 에 제3값 허용**(⚠ 값 집합을 P2 가 지어내는 것이라 `㊴-②` 저촉) **㈑ `grid_axis` 를 「이 파일이 담는 축」으로 재정의**(단일·복수 허용)하고 유일성을 「축당 최대 1파일」로 재구성. **함께 올린다 — HSR 격자가 두 벌이다**: `rdr_500m_latlon.nc` 의 위경도가 `Lat_HSR.npy`/`Lon_HSR.npy` 와 **0.004~0.007° 어긋난다.** 어느 쪽이 정본 격자인지 **지금 아무 문서도 안 다룬다** | ⛔ **W1(`P2-db` `0004`)를 막는다 → P2 전 물결.** `0004` 는 기존 인덱스를 걷는 되돌리기 어려운 마이그레이션이고 R-1 이 도전하는 것이 정확히 그 제약의 모양이라, **틀린 전제로 굳히면 되돌리는 비용이 크다.** Ted 판정 1건이면 열린다 |
| **12** | 🟧 **온톨로지 그래프 = 수문학 도메인 소유자 시간이 임계경로로 돌아왔다** *(2026-08-24 · `§9 〈70〉-㉳`)* — Ted 가 stage 1 의 「온톨로지 기반」을 **관계·그래프까지**로 정했다. 이것은 **`㊸` 를 되돌리는 결정**이다(`㊸` 는 온톨로지를 **사전 3종**으로 줄였고 `K2` 가 그대로 22행을 적재했다). 그래프의 어휘·관계는 **정본이 주지 않으므로 지어낼 수 없다**(`㊴-②`) — 사람이 정해야 한다. ⚠ **블로커 #3 이 「사람 시간 대기」에서 「정본에서 길어 올리는 작업」으로 성격이 바뀌었던 것이, 이 결정으로 일부 되돌아왔다.** **완화는 걸어 뒀다** — `〈71〉-㉱` 로 `K4` 를 `K4-a`(tsvector + 이미 적재된 사전 3종, **그래프 무의존**) ∥ `K4-b`(그래프 증강)로 갈라 **그래프가 늦어도 검색이 뜬다.** 그리고 `S1-PLAN` 의 **체크포인트 `C1`**(W3 뒤)에서 절반이 독립 가치로 닫힌다.<br> **⭑ 2026-08-24 `C1` — 이 블로커가 이제 한 질문으로 좁혀졌다.** 초안 `sessions/G8b-DRAFT.md` 가 실측으로 확정했다: 기획 정본에 「온톨로지」 **1건**(`백로그.md B-04`, 1차 범위 밖) · 「지식 그래프」 **0건** → `ONTOLOGY-SCOPE §③` 10항목이 정본에서 얻는 값 **0건**. **그래프는 추출이 아니라 저작이다.** 그래서 막는 것은 「소유자 시간」 일반이 아니라 **`G8b` Q1**(관계 값의 출처 채널 ㈎/㈏/㈐)이고, 그것이 `K1b`·`W4 S1-ontology-graph` 의 **진입 스위치**다. 우선순위 근거 = **`§9 〈82〉`**(그래프가 임베딩보다 먼저) · 우회로 부재 = **`〈81〉`** | 🟧 **`W4` 의 `S1-ontology-graph` 와 `K4-b`·`K2b` 를 막는다.** **`S1-preview`·`S1-search-a` 는 안 막는다**(그렇게 되지 않도록 갈랐다) |
| **13** | 🟧 **검색 평가셋이 0건이고 작성 소유자가 없었다** *(2026-08-24 · `S1-PLAN` 완료 정의 7)* — `eval/` 에 README 하나뿐이고, `K3`·`K4` 의 완료 정의가 **「평가셋 대비」**라 현재 판정 자체가 불가능하다. 이대로 두면 **검색 레인이 자기 채점표를 자기가 쓴다.** **확정된 완화** = 최소 **15~20 질의**를 **원천 문서에서 유래**시켜 **검색 레인이 아닌 소유자**(`S2b` 또는 메인)가 작성하고, **없으면 `W5` 완료 판정 금지**. 초안은 `W6` 진입조건 | 🟧 **`W5`(검색) 완료 판정만 막는다.** 착수는 안 막는다 |
| **14** | 🟧 **`planning-freshness` 는 워크트리 안에서 green 이 될 수 없다** *(2026-08-24 · `S1-W0-baseline §5.1`·`§7`)* — 게이트가 정본 패키지를 **레포 루트 기준 상대 경로**로 찾는데 워크트리에서는 그 경로가 실재하지 않는다. **정본이 낡았다는 신호가 아니라 정본을 아예 못 읽은 것이고, 게이트는 그 경우 skip 하지 않고 red 를 낸다 — fail-closed 는 정상 동작이다.** ⚠ **`W1`(정본 개정)의 유일한 기계적 수호가 이 게이트다.** **`W1` 개정 커밋을 병합하기 전에 메인 체크아웃에서 한 번 돌려 확인한다 — 게이트를 고치거나 우회하지 않는다.** 함께 red 인 `schema-diff` 는 **DB 부재**이고 `W1` 과 무관하다.<br> **⭑ 2026-08-24 `C1` 실측 — 이번 회차의 전 게이트 실행에서 `planning-freshness` 는 green 이었다**(21 green / 1 red, 그 1 은 `schema-diff` 하나). **다만 「워크트리에서 못 찾는다」는 성질 자체가 사라졌다는 증거는 아니다** — 정본 폴더가 보이는 환경에서 잰 값이다. **그래서 이 줄을 지우지 않는다.** 다음 회차에서 이 게이트가 red 로 돌면 **코드 결함이 아니라 여기부터 본다** |
| **15** | ✅ **`generated-up-to-date` 가 red 다 — 기준선(`S1-W0-baseline §5`)의 green 에서 뒤집혔고, 원인은 `W1` 이 아니다** *(2026-08-24 · `W1` 실측)* — `frontend/src/generated/fe-core.ts` 가 계약보다 낡았다. **드리프트의 실체** = 커밋 `bb941c3`(데이터 파이프라인 실측 정본 신설)이 `contracts/seams/core-viz.yaml:302` 의 `withoutReferenceGrid` 설명에 **HSR 5.9 km 주석 5줄**을 더했는데 **생성물을 재생성하지 않았다.** **`W1` 이 원인이 아니라는 근거** — `git diff HEAD -- contracts frontend` 가 **빈 결과**이고, `W1` 이 만진 것은 `dev-package/` · `gates/README.md` · `CLAUDE.md` 뿐이다(문서만). **조치 = 생성물을 재생성해 커밋한다. 손으로 고치지 않는다**(`CLAUDE.md §3-7`). **소유 = `W3`(`S1-fe` 또는 `S1-api` — `frontend/src/generated/` 를 만지는 레인).** ⚠ **`C1` 통과 조건 1(전 게이트 green)이 이것을 다시 만난다** **⭑ ✅ 해소 (2026-08-24 · `C1`)** — `W3` 이 생성물을 재생성해 커밋했다. **게이트 green 으로 회복**했고, 그래서 `C1` 의 게이트 red 는 2 → 1 로 줄었다 |
| **16** | ✅ **해소 (2026-08-25 · `a747c67`)** — 세 파일이 `RenderResult` `oneOf` 두 갈래를 옳게 읽도록 고쳐졌다. **실측 = `frontend/node_modules/.bin/tsc --noEmit` 출력 0줄**(2026-08-25 재확인). ⚠ **아래 「진짜 문제는 게이트 구멍」은 존속한다 — `tsc` 게이트는 여전히 없다.** *(이하 원문)* ⛔ **`tsc --noEmit` 이 red 다 — 3건** *(2026-08-24 · `C1` 실측)* — `frontend/src/components/preview/PreviewPanels.tsx:148` · `frontend/src/components/preview/usePreviewRender.ts:88` · `frontend/src/components/upload/PreviewPanel.tsx:197`. **⭑ `W3` 회귀가 아니다** — 세 파일 모두 이 물결 **이전**(`a868d64`)이 마지막 손댐이다. **⚠ 진짜 문제는 red 3건이 아니라 게이트 구멍이다 — `tsc` 게이트가 존재하지 않는다.** 그래서 기준선(`sessions/S1-W0-baseline.md`)도 이것을 못 잡았고, **전 게이트 green 이어도 타입은 깨져 있을 수 있다**는 뜻이 된다. `vitest` 는 타입을 안 본다. **소유 = `W5`(`S1-fe-preview`)** — 세 파일이 전부 그 레인의 소유 디렉터리다. **`W4` 는 이 수를 늘리지만 않으면 된다**(진입조건 ④). |
| **17** | 🟧 **pgvector 가 `postgres:16-alpine` 에 없다 — 그래서 stage 1 이 임베딩을 안 쓴다** *(2026-08-24 · `C1` 실측 → `§9 〈81〉` Ted)* — 두 방법으로 쟀다: `pg_available_extensions` 에 `vector` **0 행**(같은 조회에서 `pg_trgm`·`unaccent` 는 행을 낸다) · 컨테이너 확장 디렉터리에 `vector*` 파일 **0 건**. **이것은 이제 막는 것이 아니라 결정이다** — `〈81〉` 이 **선택지 ㈎**(LLM 은 질의 해석, 매칭·순위는 tsvector)를 채택해 임베딩 파이프라인을 stage 1 밖으로 냈다. ⛔ 가 아닌 이유 = **아무것도 기다리지 않는다.** **⚠ 함께 남는 미결 = 한국어 `ts_config` 가 `'simple'` 이고 여전히 `[정본 무근거]` 다** — 이미지의 29개 설정에 한국어가 없다. 결과: 「강수량」이 **한 토큰**이라 「강수」로는 안 잡히고 **`강수:*` 접두 질의로만** 잡힌다. **값을 바꾸면 생성 열 전 행이 재생성된다.** **소유 = Ted 판정**(`〈81〉-㉲`) — 레인이 임의로 정하지 않는다. |
| **18** | 🟧 **staging 실배포가 `[미확인]` 이다 — compose 정합까지만 확인했다** *(2026-08-24 · `C1`)* — `W3` 의 워커 배선(`1f56968`)과 **컨테이너 8 · 헬스 6** 형태는 **`compose.i2.yml` 선언의 정합성**으로만 확인됐고, **실제로 배포해 띄운 것이 아니다.** ⚠ **이 프로젝트는 이 함정을 이미 한 번 밟았다** — `〈73〉` 이 「이벤트 7종 실발행」 보고가 **시험 안에서의 실발행**이었음을 드러냈다. **여기서 같은 문장을 반복하지 않으려고 `[미확인]` 로 적는다.** **판정 자리 = `W7`**(staging 배포 green 이 완료 조건, `CLAUDE.md §0` — 체크포인트는 배포가 아니다). 그 전에 누가 띄워 보는 것은 자유지만 **띄우기 전까지는 「된다」고 적지 않는다.** |
| **19** | ✅ **해소 (2026-08-24 · `§9 〈85〉` Ted 판정 ㈎ · 커밋 `6428e3a`)** — `bounds` 는 **지도형 갈래에만** 필수가 됐다. **실물 대조 = `contracts/seams/core-viz.yaml:431`(「`legend` 만 무조건 필수」) · `:441-443` `dependentRequired: {sidecarUrl|worldFileUrl|tileUrlTemplate: [bounds]}`.** ②비지도형은 「완료」로 나가고 `failure.details` 우회는 코드에서 사라졌다. **⛔ 표기가 `〈85〉` 뒤에도 존치돼 있었다 — 드리프트였고 여기서 정정한다.** *(이하 원문)* ⛔ **계약 `RenderResult` 가 ②비지도형을 낼 수 없다 — `bounds` 가 필수다** *(2026-08-24 · `W4` 레인 `S1-preview` 실측 → `§9 〈83〉-㉮`)* — `〈74〉-㉮`·`§5.5` 는 「격자가 없어도 ①②는 보여준다 · 그것은 실패가 아니라 보류다」인데, `〈80〉` 이 연 `oneOf` 는 **두 갈래 모두**에 `bounds`(WGS84)를 요구한다. 좌표 없는 ②에는 그 값이 없다. **집행은 경계를 지어내지 않았다** — 격자 없는 렌더는 `실패(REFERENCE_GRID_MISSING)` 로 두고 ①②를 실제로 구운 뒤 URL·배지·`colorRangeStage` 를 `failure.details` 로 말한다. **막는 것 = `W5` 의 「값만 표시」 화면이 계약 표면으로는 「실패」를 받는다.** **판정 필요 — 이것이 「두 번째 동결 해제」를 부를 유일한 항이다**(`R-8`). |
| **20** | ⛔ **지도형 미리보기(③)가 staging 에서 안 뜬다 — 격자 전달 규약이 두 자리에서 갈렸다** *(2026-08-25 · `W7` 실측)* — core-api 는 접수한 바이트를 `{root}/uploads/{targetId}/{fileId}` 로 **평평히** 놓는데(`974cf9f`), viz-render 의 `FilesystemSourcePort` 는 기준 격자를 **`{root}/{targetId}/grid/`** 아래에서 찾는다(`services/viz-render/src/colab_viz/ports/source.py` · `GRID_DIRNAME`). **그 디렉터리를 만드는 코드가 어디에도 없다** → `grid_dir` 이 늘 비어 `find_reference_grid` 가 격자를 못 찾고 ③이 서지 않는다. ①②는 격자를 요구하지 않아 200 이라 **상태코드로는 안 보인다**(`〈83〉`·`〈85〉` 와 같은 무늬). **막는 것** = `〈75〉`(HSR 은 격자를 올려야 지도형이 켜진다)의 집행 · P3 · S3. **⛔ 처리 — `CLAUDE.md §5` 대로 「나중에」로 두지 않는다. 다음 세션이 WU 로 세운 뒤 착수한다**(가칭 `WU-V1` 격자 전달 규약 일치). 규약을 **어느 쪽으로** 맞출지는 설계 판정이므로 고르고 나서 `PLAN-SoT §9` 에 값으로 적는다 |
| **21** | ⛔ **`addDatasetFile` 202 는 계약만 열렸고 집행은 400 이다 — 축 미정 격자를 둘 자리가 없다** *(2026-08-25 · `〈88〉` 4차 해제 뒤 실측)* — 계약은 202 를 낼 수 있게 열렸는데, 원장에 **축이 정해지지 않은 격자 파일 행을 만들 자리가 없다**: `d3_file` 의 CHECK 와 업로드 이벤트의 `upload_id` **NOT NULL** 이 둘 다 「축이 이미 정해졌다」를 전제한다(`〈79〉` 는 「축 확정 뒤 워커가 원장 행을 만든다」로 적었고, 그 사이 구간에 자리가 없다). **막는 것** = P2 닫기 · `〈75〉` 격자 정문 · S2 화면 경로 적재. **🔴 사람이 풀어야 한다 — 스키마 판정이 필요하다(Ted).** 선택지는 ⓐ 축 미정 상태를 원장이 표현하게 스키마를 고친다 ⓑ 접수를 뒤로 미뤄 축 확정 후에만 행을 만든다 ⓒ 202 를 계약에서 도로 닫는다. **어느 것도 코드로 우회하지 않는다** |
| **22** | ⚠ **⭑ 2026-08-26 실측 정정 — 「그에 딸린 데이터셋」은 지금 staging 에 없다.** 아래 원문은 **데이터셋이 있다는 전제로 쓰였고 그 전제가 실물과 어긋난다**(`#28`). 연구실 A/B 와 **주체**는 그대로 있으나 **`d3_dataset` = 0** 이다. **「걷어내는 순간 검색·미리보기의 지금 관측치가 전부 무효가 된다」는 우려는 이미 실현돼 있다** — 걷어낼 데이터셋이 남아 있지 않다. 판정 필요 항목은 「언제 걷어낼 것인가」가 아니라 **「D1·D2·D6·D8 잔존분을 어떻게 할 것인가」**로 바뀐다(`#28`). *(이하 원문 — 무수정 보존)* ⚠ **staging 이 시험 픽스처로 시드돼 있다 — 실데이터가 아니다** *(2026-08-25)* — 연구실 A/B 두 개와 그에 딸린 데이터셋·주체가 **시험용 픽스처**다. 화면에 보이는 것을 **실적재의 근거로 인용하지 않는다**(`DATA-REFERENCE §0` — 측정 안 한 것을 측정된 것처럼 인용하기). **S2 실적재는 화면 경로로만 간다**(`㊾-③` · DB 직접 INSERT 금지). **판정 필요** = 픽스처를 **언제 걷어낼 것인가** — 걷어내는 순간 검색·미리보기의 지금 관측치가 전부 무효가 되므로, S2 착수와 같은 회차에 정한다 |
| **23** | ✅ **해소 (2026-08-25 · `6592fcc`)** — **실물 = `services/pipeline-worker/src/colab_pipeline/app/health.py:29` `{"unit": UNIT, "status": "alive", "implemented": True}`.** 그 본문을 단언하는 시험이 함께 붙었다(red 확인 후 green). **헬스 본문을 워커 판정에 다시 쓸 수 있다.** *(이하 원문)* ⚠ **`pipeline-worker` 의 `/healthz` 가 실이미지인데 `implemented:false` 를 낸다** *(2026-08-25)* — 워커는 실제로 배선됐고 도는데(`c5a2fbf`) 헬스 본문이 아직 스캐폴드 시절 값을 낸다. **상태코드가 아니라 본문으로 판정하라**는 이 레포의 규칙(`RESTART §③`)을 **이 한 줄이 흐린다** — 다음 사람이 본문을 보고 「워커는 아직 없다」로 읽는다. 고치는 것은 한 줄이지만, 고치기 전까지는 **헬스 본문을 워커 판정에 쓰지 않는다** |
| **24** | ⚠ **백업 도구 `verify-artifact.sh` 에 설정 재source 버그가 있다** *(2026-08-25)* — 검증 중 설정 파일을 다시 읽으면서 앞서 잡은 값을 덮어쓴다. **이번에는 우회해서 검증을 마쳤고, 코드는 고치지 않았다.** IS3(백업 체계)가 ✅ 인 채로 남아 있으므로 **다음에 복원 리허설을 돌리는 사람이 같은 자리에서 막힌다** |
| **25** | ⚠ **정본 대 실자료 불일치 2건** *(2026-08-25 · `S2b` 초안 작성 중 발견)* — ⓐ **「한국수자원학회」가 어느 데이터셋에도 `source_label` 로 배정돼 있지 않다** — 정본이 원천으로 드는데 실자료에 0건이다(`〈56〉` 이 사람 이름을 그 열에서 뺀 뒤 재배정이 안 된 자리로 보인다 — **[미확인]**, 원인은 안 쟀다). ⓑ **`pg_trgm` 임계 0.3 을 이름 **전체**에 걸어 짧은 오타가 안 걸린다** — 긴 이름일수록 한 글자 오타의 유사도 손실이 작아 임계를 넘고, 짧은 이름은 못 넘는다(`〈89〉` 의 보조 팔이 의도만큼 안 잡힌다). 둘 다 **평가셋 승인 전에 판정해야 기대값이 흔들리지 않는다** |
| **26** | ✅ **해소 (2026-08-25 · `6929aee` · 판정 `§9 〈102〉`)** — 배치의 **단일 정의처**가 섰다: `contracts/storage/layout.json` ＋ `contracts/codegen/manifest.toml` 등기 3건(`storage-layout-core`·`-pipeline`·`-viz`)이 세 단위에 같은 바이트를 준다(등기부 1 → 4건). 배치를 단언하는 시험 **11건**(red 확인 후 green)과 `generated-up-to-date` 게이트가 회귀를 막는다. *(이하 원문)* ⚠ **저장 배치를 단언하는 시험이 0건이었다 — 그래서 아무도 못 봤다** *(2026-08-25 · `974cf9f`)* — core-api 와 pipeline-worker 가 **같은 저장 규칙을 두 파일에 각각 적어 두고 실제로 갈라졌는데**, 양쪽 시험이 **서로 다른 가짜 저장소**를 써서 둘 다 green 이었다. 배포에서만 났고 실파일 E2E 로 드러났다. **회귀 방지가 필요하다** — 저장 키·경로를 **한 자리에서 단언하는 시험**(또는 배치 규칙의 단일 정의처)이 없으면 같은 실패가 다시 난다. 지금은 고쳐졌을 뿐 **막혀 있지 않다** |
| **27** | ⚠ **`:prev` 태그 실수 1건 — 직전 core-api 이미지 태그를 잃었다** *(2026-08-25)* — `:i2` 로 재빌드하면서 그 태그가 가리키던 **직전 core-api 이미지**가 이름을 잃었다. **`:prev` 기준선과 `rollback.sh` 는 무사하고 롤백 왕복도 증명됐다**(530 없음 · 본문 대조) — 즉 롤백 능력은 살아 있다. 다만 **`:i2` 를 「직전 판」으로 믿으면 안 된다** |
| **28** | ⛔ **staging 에 데이터셋이 0 건이고, 그것을 참조하는 D2·D6·D8 행만 고아로 남아 있다 — 인계 진술 2건이 실물과 어긋난다** *(2026-08-26 · S2 야간 레인 실측)* — **실측**(`postgres` 슈퍼유저 · `rolbypassrls=t` 로 RLS 우회) = `d3_dataset` **0** · `d3_dataset_description` **0** · `d3_file` **0** · `d4_lineage_edge` **0** · `d5_upload` **0**. 그런데 **`d6_project_dataset` 3** · `d2_dataset_access` 2 · `d2_verified` 2 · `d8_download` 2 · `d8_activity` 6 은 남아 있고, `d6_project_dataset` 3행이 가리키는 `dataset_id` 는 `…DSA2`·`…DSB1`(**`seed.sql` 값**) ＋ 실 ULID 1건이다 — **전부 고아 참조**다. 불변규칙 1(cross-domain FK 금지)이 참조 무결성을 강제하지 않아 남았다. **보관 백업 8종(08-23~08-26) 전부 `d3_dataset`=0** — **복원으로 A·B 데이터셋을 되살릴 수 없다.** 그런데 `d8_activity` 는 08-24 19:36 「데이터셋 등록」 2건, 08-25 13:01~14:20 「좌표계·격자 변경」 4건을 기록한다 → **`seed.sql` 이 한때 전량 적용됐고 이후 D3/D4/D5 행이 DB 수준에서 지워졌다**(이전 세션들의 시험 데이터 정리로 **추정** — 원인은 **[미확인]**, 재지 않았다). **어긋나는 인계 진술 2건** = ⓐ `#22`「A/B 두 연구실과 그에 딸린 데이터셋」 ⓑ 헤더 「staging 에서 검색이 실결과를 낸다(`강우` 3건 · `degraded:false`)」 — **후자는 지금 재현 불가다**(`#29` 로 실측도 못 했다 — **[미확인]**). **⚠ 이 레인은 DB 를 만지지 않았다** — 고아 행을 지우면 삭제 경위의 증거가 함께 사라진다. **사람이 정할 것 3건** = ① A·B 를 **재시드할 것인가**(안 하면 `S2-EXEC-PLAN` 2단 관측치 고정과 4-3 「A·B 불변」이 **0→0 자명 대조**가 되어 경계 격리 실증력이 사라진다) ② **고아 D2·D6·D8 행 처리**(삭제 / 존치 / 재시드로 복구 — 방향이 서로 반대다) ③ 5단 픽스처 철거가 **이미 부분 수행된 상태**임을 절차서에 반영할 것인가 | ⛔ **`S2-EXEC-PLAN` 2단(관측치 고정)의 전제를 깬다 → 4-3 경계 격리 실증.** 3단 적재 자체는 안 막는다 |
| **29** | ⛔ **`subjects.json` 을 읽을 수 없어 staging API 인증이 불가능하다 — S2 절차 2·3·4단이 실행되지 않았다** *(2026-08-26 · S2 야간 레인)* — 하네스 분류기가 자격증명 파일 읽기를 **거부**했다. **우회하지 않았다.** 그 파일 없이는 주체 토큰을 얻을 수 없고, 따라서 **2단 관측치 취득 · 3단 적재 · 4단 검증·평가셋 18건이 전부 실행 불가**다. ⚠ **절차서 안에 긴장이 있다** — `S2-EXEC-PLAN §12 U-2` 는 「`〈93〉` 이 비밀로 규정. **값을 읽지도 적지도 않는다**」인데, 같은 문서 `§8.6` 관측치 표는 취득 수단으로 `listDatasets`·`searchDatasets` 를 지정한다. **비밀 규정과 API 취득 지시가 같은 문서 안에서 어긋난다** — 절차서 작성자만 해소할 수 있다. 3단의 `subjects.json` 갱신은 성격이 다르나(신규 토큰 **발급·기입**) **같은 비밀 파일에 대한 쓰기**이고, `〈93〉-㉰`(제자리 덮어쓰기 · `0600` · uid 10001) 는 되돌리기가 까다로운 조작이라 **사람 승인 없이 야간에 하지 않았다.** **사람이 정할 것** = 이 레인에 staging 주체 접근을 **어떻게 줄 것인가**(운영자가 직접 실행 / 접근 허용 / 절차서 개정) | ⛔ **S2 절차 2·3·4단 전부.** 적재 도구 구현·시험은 안 막는다(staging 무의존) |
| **30** | ⛔ **프런트엔드에 인증 수단이 없다 — 배포된 화면은 staging API 에서 401 만 받는다** *(2026-08-26 · S2 야간 레인 실측)* — **실물** = `frontend/src/api/client.ts` 가 `createClient({ baseUrl: '/api/v1' })` 뿐이고 **`Authorization` 헤더를 싣는 코드가 없다.** `frontend/src` 전체에 `Authorization`·`Bearer`·`localStorage`·`login` **0 건** · `Dockerfile`·`vite.config.ts` 에 토큰 주입 **0 건** · `compose.i2.yml` 의 frontend 서비스에 토큰 env **0 건**. nginx 는 클라이언트의 헤더를 **전달만** 한다(`nginx.i2.conf:60` `proxy_set_header Authorization $http_authorization`). **공개 주소 실측** = `GET /api/v1/datasets` → **401** `{"code":"UNAUTHORIZED"}` · `GET /api/v1/me` → **401**. **귀결 = 배포된 화면으로는 카탈로그·검색·업로드 무엇도 뜨지 않는다.** ⚠ **이것이 인계 진술 둘의 측정 경로를 다시 묻게 한다** — 「staging 에서 검색이 실결과를 낸다(`강우` 3건)」·「미리보기 ①②가 공개 주소에서 200」은 **화면이 아니라 토큰을 실은 직접 API 호출로 잰 값**이었을 것이다(**[미확인]** — 당시 측정 방법을 재지 않았다). **막는 것** = **S2 회차 ① 의 「S-04 화면으로 손수 적재」**(`S2-EXEC-PLAN §14.3`) 와 **브라우저 자동화 적재**(2026-08-26 Ted 지시 2) — **둘 다 화면에 토큰을 넣을 자리가 없어 성립하지 않는다.** 자동화 도구가 헤더를 강제 주입하면(`extraHTTPHeaders`) FE 코드는 지나지만 **그것은 「사람이 화면에서 하는 일」의 재현이 아니고**, 그마저 토큰이 있어야 한다(`#29`). **사람이 정할 것** = ⓐ FE 인증을 stage 1 범위로 들일 것인가(**범위 확대** — `CLAUDE.md §5`) ⓑ `§14.3` 의 결손 보전 요구를 **다른 수단으로 바꿀 것인가** ⓒ 결손으로 남기고 기재할 것인가 | ⛔ **`§14.3` 회차 ① 화면 적재 · Ted 지시 2 브라우저 자동화.** 도구 경로 적재는 안 막는다(토큰만 있으면 된다) |
| **31** | ✅ **해소 (2026-08-26)** — 원인은 둘이었다. ⓐ **`#32`** 때문에 축 판별이 돌지 않아 `carries_lat`/`carries_lon` 이 전부 false 였다 ⓑ **적재 도구가 축 판별을 기다리지 않았다** — 접수와 축 판별 사이는 비동기 seam 인데 `createUpload` 직후 `attachUploadGridFiles` 를 불렀다. **400 은 서버 결함이 아니라 경주였다.** 조치 = 워커 건별 스코프(`〈110〉-㉮`) ＋ `load-seed.py` 의 `_await_grid_axes`(상한 180초). 실측 = 그 부착이 **400 → 201**, 이어 남은 4건도 붙어 **기준 격자 파일 6/6 · `d3_file` 128 · 매니페스트 확정치 도달** | — |
| **32** | ✅ **해소 (2026-08-26 · Ted 판정 ㈑)** — 워커는 대상 연구실을 원장에서 읽고 **연구실마다 제 트랜잭션 · 제 스코프**로 돈다. `COLAB_WORKER_LAB_ID` 는 필수 → **선택(좁히는 값)** 이고 `compose.i2.yml` 에서 고정 값을 걷었다. **신규 롤 0 · 신규 면제 0 · 마이그레이션 0 · 계약 개정 0** — 명단의 출처는 이미 RLS 면제인 `d1_lab` 하나다. 실측 = **아웃박스 14/14 발행**(직전 미발행 12 → 0) · 워커 시험 **110/0**. 판정문 문언과의 편차 1건은 `PLAN-SoT §9 〈110〉-㉯` 에 적었고 **Ted 확인 항목**이다 | — |
| **33** | ⛔ **재실행해도 남는다 — 통과 7 · 실패 10 · 미실행 1** *(2026-08-26 2차 실측)* — `#31`·`#32` 해소 뒤 17/18 을 다시 돌렸다. **기대값 무수정.** 통과 = `Q-02`·`Q-03`·**`Q-08`**·`Q-10`·`Q-17`·`Q-18`·`Q-20`(`Q-08` 이 7건 → 정확히 3건으로 바뀌어 실패 → 통과). `Q-19` **미실행** — `ai-service` 정지가 이 회차 실행 환경에서 거부됐다. **직전 통과를 이번 통과로 세지 않는다.** ★ **회귀 게이트 `Q-01`·`Q-04` 여전히 실패.** **되돌림 미실행 — 판정만 올린다.** **원인 판정 = 실패 10건 전부 ⓑ 그래프·매칭 확장 경계이고 ⓐ 감지 미실행 종속은 0건이다** — `0005` 의 `search_vector` 가 훑는 것은 이름 A·주제 B·설명 C 뿐이라 자동 메타는 애초에 색인 대상이 아니다. **⚠ 그리고 이 평가셋은 결정적이지 않은 경로를 재고 있다** — 같은 질의 연속 2회에 `Q-13` **0 → 3건**, `Q-11` **3 → 1건**(`Q-01`·`Q-08` 은 안정). **회귀 게이트로 쓰려면 결정성이 먼저다 — Ted 판정 사안** | `〈89〉`·`〈86〉` 유효성 판정 · stage 1 검색 완료 정의 |
| **34** | ⚠ **`colab_app` DB 비밀번호가 컨테이너 환경변수 조회로 평문 노출됐다** *(2026-08-26)* — `docker inspect` 의 환경변수 목록에 접속 문자열이 통째로 들어 있고, 그 값이 이 회차의 작업 기록에 남았다. **회전 대상이다**(`〈93〉`). 회전 = `~/.colab-v2-staging.env` 의 `COLAB_APP_PASSWORD` 교체 → `db-bootstrap.sh app-grants` → core-api 재기동 | 비밀 취급 규약 |
| **35** | ⛔ **자동 메타 5칸이 여전히 비어 있다 — 감지가 돈 뒤에도 채워지지 않는다** *(2026-08-26 실측)* — `#32` 해소로 감지·릴레이는 돌지만 상세 화면의 `basicInfo` 는 `variables`·`crs`·`period`·`grid`·`format` **전부 null** 이다(`hasReferenceGridFile` 만 `#31` 해소로 true 가 됐다). 원인 둘 — ⓐ **넷(구성·좌표계·기간·격자)은 stage 2 범위**다(`〈73〉`) ⓑ **한 칸(포맷)은 등록이 감지보다 먼저 일어나 대상에서 빠졌다** — `pending_uploads` 가 `registered_at IS NULL` 을 요구하고 **소급 경로가 없다.** **소급 수단을 지어내지 않았다** — 등록된 업로드를 되살릴지, 등록 시점에 감지를 요구할지가 판정 사안이다. ⚠ **곁가지 하나** — **격자만 든 업로드가 `upload.failed`(`형식 인식 실패`)를 낸다.** 본체가 0건이라 stage 1 이 「본체 전건이 알려진 매직바이트가 아니다」로 끝내기 때문이다. **부착 자체는 201 로 성공한다** — 원장에 남는 실패 표시만 사실과 어긋난다. `〈79〉-⑶`(격자는 감지를 태우지 않는다)·`-⑷`(ready 판정은 축으로 정한다)와 어긋나는 자리다 | stage 1 「자동 메타」 완료 정의 · 미리보기 3층의 실데이터 확인 |
| 4 | ~~CI 실행 환경 결제~~ | **해소** (2026-08-22) — public 전환으로 GitHub Actions 무료 러너 사용. R1에서 CI 1회 완주 확인 |
| 5 | ~~전달 패키지 재빌드 (기획 소유자)~~ | **해소** (2026-08-22) — 패키지 내용은 이미 `DataModel v1.8`·`Policy v1.4` 와 일치했다(sha256 대조). 재빌드 대상이 없다. 남은 것은 그 사실을 기계가 지키게 하는 것(G1) |
| 6 | ~~기획 정본 마운트~~ | **소멸** (2026-08-22) — 정본을 작업공간 안 `40 COLAB-기획/` 로 옮겨 **마운트 의존 자체가 없어졌다**(`PLAN-SoT §9-㉕`). 외부 드라이브 스트리밍 사본은 읽기가 간헐 실패해 게이트에 오탐 red 를 냈다. 참조 고리를 전부 끊었다 |

| 9 | ~~**OpenAI API 키**~~ (`㊷`) | **해소** (2026-08-23) — Ted 가 `~/.colab-v2-staging.env` 에 투입. `ai-service` 컨테이너까지 통로 연결 완료(`compose.i2.yml`, 커밋 `6737b73`). **K3·K4 차단 해제.** 소비 시작은 K3. |

## 4.5 다음 세션 진입조건 (WSL — 작업 시작 전 확인)

> **호스트를 껐다 켠 뒤라면 `RESTART.md` 를 먼저 읽고 그 절차를 마친 뒤 이 표로 온다.**

| # | 확인 | 명령 | 실패하면 |
|---|---|---|---|
| 1 | **기획 정본이 읽히는가** | `./gates/run.sh planning-freshness` 가 **green** | 폴더 목록만 보는 확인은 이 조건을 못 잡는다 — **목록은 되고 읽기가 실패**하는 상태가 실제로 있었다. 게이트는 15개 문서를 실제로 **읽어서** 해시를 낸다. red 면 정본 폴더(`planning/README §1`)가 제자리에 있는지 먼저 본다 |
| 2 | 레포가 원격과 같은가 | `git status -sb` 가 clean + ahead/behind 0 | `git pull` |
| 3 | gh 인증 | `gh auth status` | `gh auth login` (환경별 키링 — Windows 인증은 WSL로 안 넘어온다) |
| 4 | **staging 이 살아 있는가** | `docker ps` 에 `colab_v2_staging_*` **8개**(nginx·cloudflared·pg·core_api·frontend·pipeline_worker·viz_render·ai_service) · `curl -I https://www.colab-hydro.com/healthz` 200 · 5개 단위 `/healthz/<unit>` 각각 200 | WSL 재시작 시 컨테이너가 안 뜬다(데몬이 자동 기동하지 않는다). 되살리는 명령은 **`-f compose.i2.yml` 을 반드시 붙인다** — `docker compose -f infra/staging/compose.i2.yml --env-file ~/.colab-v2-staging.env up -d`. **`-f` 를 빼면 `compose.yml`(자리표시 오리진 2개)이 떠서 I2 가 조용히 롤백된다.** 컨테이너는 `restart=unless-stopped` 이고 `pgdata` 는 홈의 바인드 마운트라 **데이터는 재부팅을 넘긴다**. **토큰은 홈의 `.colab-v2-staging.env`(0600) — 레포에 없다** |

> **1번은 이 프로젝트의 반복 함정이었다.** 레포는 환경을 옮겨도 따라오지만 **레포 밖 자산(기획 정본)은 따라오지 않는다.**
> 2026-08-22 정본을 **작업공간 형제 폴더 `40 COLAB-기획/`** 로 들여와 외부 드라이브 의존을 끊었다(`PLAN-SoT §9-㉕`).
> 확인 방법도 바꿨다 — **폴더가 보이는가**가 아니라 **문서가 실제로 읽히는가**를 본다. 앞의 것은 통과하고 뒤의 것만 실패하는 상태가 실재했다.

## 5.5 알려진 드리프트 — 소유자가 정해진 미결

**아무도 소유하지 않은 채 남는 것을 막으려고 여기 적는다.** 각 줄은 소유 WU 가 있고, 그 WU 의 작업지시서가 이 줄을 입력으로 받는다.

| # | 드리프트 | 소유 WU | 왜 지금 안 고치는가 |
|---|---|---|---|
| **DR-16** | **`STOP-6` 이벤트 계약 산문이 결정과 어긋난다** — `contracts/events/core-pipeline.json:39` `UploadAcceptedPayload.files` 설명이 아직 **「기준 격자 파일 0~1건」**이다. `〈58〉` 이 **0~2건**으로 닫았고 `contracts/schemas/common.json#FileKind` 는 **이미 고쳐져 있다**(축은 `grid_axis` 가 가른다까지 적혀 있다) | **다음 계약 개정 창** | **구조 제약이 아니라 산문 1줄**이다(`minItems: 1` 뿐, `maxItems` 없음). 계약은 `〈61〉-㉢` 로 **동결**됐고 P2 는 계약을 고치지 않는다. 옳은 값이 `common.json` 에 이미 있으므로 **동결 해제 판정 비용을 지금 쓰지 않는다** — 대신 어긋난 문장이 남아 있다는 사실을 여기 남긴다 |
| **DR-18** | **계약 산문이 아직 없는 DB 열 이름을 앞질러 못박았다** — `contracts/schemas/common.json:79` `FileKind.description` 이 「위도·경도 한 쌍이 실물이고(**축은 `grid_axis` 가 가른다**), 등록 뒤 후주입도 정상 동작이다」라고 적었는데, **오늘 `db/platform/schema.sql` 의 `d3_file` 에는 그런 열이 없고**(`§5.5 DR-17` 채택 권고가 `grid_axis text` 단일 열 대신 `carries_lat`·`carries_lon` 두 불리언 안을 제시한 상태다 — `sessions/P2-W0-R1-code-usage.md §3.4`·`§4.1`), 실제 구현이 두 불리언 안으로 가면 **DB 열 이름이 `grid_axis` 가 아니게 된다.** 계약 산문과 DB 스키마가 서로 다른 이름을 말하는 드리프트다 | **다음 계약 개정 창** | **고치는 방향은 산문을 DB 열 이름에 맞추는 것이지, 그 반대(DB 열 이름을 `grid_axis` 로 구부리는 것)가 아니다.** DB 설계는 `0004`(또는 그 대안)가 코드 증거(`§2.1`·`§2.5`·`§3` — 코드가 축을 파일의 종류로 묻지 않는다)에 따라 결정할 몫이고, 계약 산문은 사람이 미리 지어낸 이름일 뿐이다. 계약은 `〈61〉-㉢` 로 동결됐고 P2 는 계약을 고치지 않으므로, 여기서는 어긋난 문장이 있다는 사실만 등재하고 실제 열 이름이 확정된 뒤 다음 계약 개정 창에서 산문을 그 이름에 맞춘다 |
| **DR-17** | **격자 축을 계약이 못 싣는다** — `createUpload` 는 `files[]`+`fileKinds[]`, `addDatasetFile` 은 `file`+`kind` 만 받고 `FileRef`·`UploadFileRef`·`DatasetFile` 전부 4값이라 **어디에도 `gridAxis` 가 없다.** 그런데 `0004` 는 `CHECK (kind='기준 격자 파일' → grid_axis IS NOT NULL)` 를 요구한다 | **P2** (`P2-EXEC` W0-1) | 권고는 **서버가 파일에서 축을 판별**(`DR-14` 「파일에서 자동 추출, 사람이 타이핑하지 않는다」와 정합 — 계약 무수정). ⚠ 그대로 채택하면 막다른 길이 생긴다 — **경도 0~90° 는 위도 범위 [-90,90] 안에 통째로 들어간다.** 채택 조건 넷(실물 16건 전건 실측 · 쌍 정합 1차 신호 · 실패는 그 파일만 막고 등록은 막지 않음 · 수동 지정 폴백 부재를 다음 개정 후보로) 은 `P2-EXEC §2` |
| **DR-1** | `services/pipeline-worker/README.md` 의 **「4포맷」 표가 아직 4다.** `㊿` 로 D5 지원 포맷은 **5**(GeoTIFF 추가) | **D5 착수 레인** | 그 파일은 P1 어느 레인의 소유도 아니다. P1 이 끼워 넣으면 **P1 커밋이 D5 범위 변경을 업고 가서** 「4→5 는 언제 누가 왜」가 P1 이력에서 나오는 이상한 모양이 된다. *(2026-08-23 두 세션 합의)* |
| **DR-2** | **`tif` 는 입력이자 출력이다** — 감지가 *이미 COG 인 tif* 와 *일반 GeoTIFF* 를 구분하지 못하면 **사람이 올린 파일이 우리 산출물로 계보에 기록된다.** 표시 오류가 아니라 **계보 오염**이라 조용히 틀리고 되돌릴 수 없다 | **D5** (구분 규칙 + **음성 시험**) · **정본**(「타일만 있는 tif」의 판정) · **S1** (원천의 tif 27 건 중 이미 COG 인 것을 표시) | 지금 코드로 막을 자리가 P1 이 아니다. `㊿` 근거란·`WORK-UNITS §8.5`·`SEED-DATA.md` 셋에 남겨 뒀다 **⟨2026-08-23 전수 측정으로 성격이 바뀌었다⟩** 원천 tif 62 건을 IFD 파싱으로 전수 판정 — **COG 는 포맷견본 6 건뿐이고 KWRA 51 건은 0 건**이다. 그래서 **긴급성은 내려가고**(첫 적재에서 안 터진다) **판별 규칙의 요구는 올라간다** — **타일링만 보고 COG 라 하면 16 건**(KWRA Input 11 + vegetation 5)**을 우리 산출물로 오인한다.** 실파일이 그 함정에 앉아 있다. `DR-3`(확장자를 믿는다)과 같은 모양 — **한 신호만 보면 그럴듯하게 틀린다** **⟨2026-08-23 PoC 조사 — 물려받을 선례가 없다⟩** PoC 는 **이미-COG 탐지를 하지 않는다**(`cog_validate`·`is_cog` grep 0건). 그뿐 아니라 **`.tif` 프로세서가 레지스트리에 없어 archive-only 로 빠진다** — **PoC 는 GeoTIFF 를 입력으로 처리한 적이 없다.** ⚠ 그런데 **문서와 주석은 처리한다고 주장한다**(`policy-e014.md:453` 「이미 COG, 등록만」 · `format_detector.py:39-41`·`upload_service.py:33` 「worker 가 COG 로 승격」) — **구현이 없다.** `DR-4` 와 같은 계열이 PoC 에서도 나온 것이다. **D5 는 이 규칙을 처음부터 세운다** |

| **DR-3** | **확장자가 거짓말을 한다** — 원천 `01.level-data/*/Lv.0/*.nc` 가 매직바이트상 **HDF5 컨테이너**다(NetCDF4 이므로 기술적으로는 정상). **포맷 감지가 확장자를 믿으면 틀린 로더를 고른다** | **D5** (감지는 매직바이트 기준) · **S1** (원천 실측표에 확장자↔실제 포맷 불일치 표기) | 2026-08-23 원천 조사에서 실측. DR-2 와 같은 계열 — **감지가 틀리면 조용히 틀린다** |

| **DR-4** | **`deploy.sh` 는 커밋이 아니라 워킹트리를 굽는다 — 그리고 주석이 그 반대를 말한다.** `infra/staging/deploy.sh:14` 가 `dc --profile migrate build` 로 **빌드 컨텍스트**를 굽는데, 바로 위 **13행** 주석은 *「배포되는 것이 **커밋의 산출**이 되도록 이미지 안에서 빌드한다」* 고 적혀 있다. **읽는 사람은 SHA 재현성이 보장된다고 믿는다.** 실제로는 배포 순간의 파일시스템이 올라가므로 **「staging 에 무엇이 떠 있는가」를 SHA 로 특정할 수 없고**, 트리를 두 세션이 쥔 상태에서는 **검증하지 않은 변경이 섞여 올라갈 수 있다** | **I3** (배포 자동화 — SHA 고정 빌드 + 주석 정정) | 2026-08-23 이 세션이 `deploy.sh`·`compose.i2.yml` 을 직접 읽어 확인. 당장은 **사람이 거는 락**(세션 간 코드 동결 합의)으로 막고 있다 — 관례이지 기계가 아니다 |
| **DR-5** | **직전 릴리스 이미지가 배포 순간 이름을 잃는다.** `compose.i2.yml` 의 앱 이미지 5종이 전부 **`:i2` 고정 태그**(`colab-v2/{core-api,pipeline-worker,viz-render,ai-service,frontend}:i2`)라, 재빌드하면 같은 태그가 새 이미지로 옮겨 붙고 **직전 이미지는 dangling 이 되어 사라진다.** 즉 **되돌릴 이미지가 남지 않는다** | **I3** | 2026-08-23 확인. **`rollback.sh` 를 고치는 것보다 태그 보존(`docker tag … :prev`)이 먼저다** — 스크립트가 옳아도 돌아갈 이미지가 없으면 소용없다. *(colab-v2-dev 세션 advisor 검토에서 제기, 이 세션이 compose 파일로 확인)* |

| **DR-6** | **`deploy.sh` 의 `exit 0` 은 green 의 증거가 아니다.** 앱 5종에 **헬스 대기가 없다** — `infra/staging/deploy.sh` 는 `dc up -d --remove-orphans`(34행) 뒤에 `dc ps`(37행)만 찍고 끝난다. 헬스 대기 루프는 **postgres 하나에만** 있다(18~21행). 그래서 컨테이너가 `health: starting` 인 채로 스크립트가 **성공을 반환한다**(2026-08-23 P1 배포에서 실측). ⚠ **16행 주석이 「앱보다 postgres 가 먼저 healthy 여야 한다」고 적혀 있다** — 헬스가 판정에 필요하다는 것을 **알면서 postgres 에만 걸고 앱에는 안 걸었다** | **I3** (배포 자동화 — 앱 5종 헬스 게이트를 판정에 넣는다) | **fail-open 이다.** 모른다고 말해야 할 자리에서 성공이라고 말한다. P1 배포가 green 이 된 것은 **사람이 따로 기다렸다 6종을 확인해서**이지 스크립트가 판정한 것이 아니다. `DR-4` 와 같은 계열 — 배포 도구가 자기 능력을 실제보다 크게 말한다 |

| **DR-7** ✅ **이행 완료 (2026-08-23 — D2c C1~C2)** — `fe-core` 개정 34→45 op(위임 산문 정정 + 업로드·데이터셋·연결·미리보기 중계 신설) + `seam-consistency` 게이트(G-e·G-b + 〈61〉-㉠㉡) green — **개정 전 red 실측 → 개정 후 green** 으로 검증. 동결(㉢)·501 라우트(D2c-api)는 대기. *(이하 원문)* | **오배정된 위임 — `fe-core` 가 존재하지 않는 seam 에 일을 넘겼다.** *(2026-08-23 감사로 진단 확정. 잠정이던 「두 seam 이 갈렸다」는 틀은 **틀렸다** — 이벤트·viz 는 **근거와 함께 같은 세계**를 그렸고 **`fe-core` 의 부재만 근거가 없다**.)* `fe-core.yaml:13-16` 의 **「이 seam 에 없는 것과 그 이유」** 절이 업로드·파일파싱·**프로젝트 연결 생성**을 **「이벤트/업로드 seam」에 명시적으로 위임**하는데, 그 seam 이 `contracts/README.md` seam 목록(4종)에 **없다.** `core-ai`·`core-viz` 로 위임한 둘도 **core-api 전용 내부 표면이라 FE 가 못 부른다.** **위임 문장은 있는데 위임처가 없다 — 설계된 경계가 아니라 끊어진 참조다.** `contracts/seams/fe-core.yaml` 은 **34 op 중 업로드 진입점도 `createDataset` 도 없이** 데이터셋이 **이미 존재하는 세계**만 그린다. 그런데 `contracts/events/core-pipeline.json` 은 `upload.accepted` 를 **「core-api 가 내는 유일한 이벤트이며 파이프라인의 입구」**로 선언하고 `source` 를 **`core-api` 상수**로 못 박았다 — **core-api 가 반드시 내야 하는 이벤트인데 그것을 촉발할 HTTP 입구가 없다.** 게이트 19 종을 확인했다: `contract-lint`·`contract-breaking` 은 **`contracts/seams` 만**, `event-lint`·`event-breaking` 은 **`contracts/events` 만** 본다. **둘 사이를 보는 게이트가 하나도 없었다** — 감사가 정합 게이트 후보 5종을 냈고, 그중 **`G-e`(산문 위임 참조가 실재하는지 검증) 하나만으로 `fe-core.yaml:16` 이 red** 다. **기계가 잡을 수 있는 결함이었다.** ⚠ **증거가 셋이다** — ① 업로드 입구 부재 ② `source: core-api` 상수 ③ **`link`/`unlink` 비대칭**: `/projects/{projectId}/datasets/{datasetId}` 에 **`unlinkProjectDataset` 은 있는데 `linkProjectDataset` 이 없다**(`grep` 0건). **끊는 길은 있는데 잇는 길이 없다.** 게다가 같은 계약이 `createProject` 는 준다 — **프로젝트는 만들 수 있는데 데이터셋은 만들 수도 이을 수도 없다.** 두 엔티티가 같은 계약 안에서 다른 대우를 받는다 | **D2 · D2b** (seam 간 정합 게이트) · **정본**(어느 seam 이 정본인가) | **⛔ P2 · S2 를 막고, P5 도 잠재적으로 막는다** — `㊾-③` 이 적재를 업로드 화면 경로로만 가도록 못 박았으므로 **S2 는 P2 를 기다리는 게 아니라 이 결정을 기다린다.** **P5(프로젝트)는 아직 안 열려서 안 터졌을 뿐이다** — ③ 의 `link` 가 없으면 프로젝트에 데이터셋을 **붙일 수 없다**. ⚠ **다른 여섯과 성격이 다르다** — `DR-2`~`DR-6` 은 **검사가 틀린 신호를 봤다.** 이건 **아무도 안 본 자리**다. 게이트를 통과해서가 아니라 **게이트가 없어서** 살아남았다. 그래서 「op 을 하나 더한다」로 끝나지 않을 수 있다 — **`〈54〉` 로 정본 = 이벤트 seam 이 확정됐다** — `fe-core` 를 이벤트 seam 에 맞춘다(반대 방향이 아니다). **남은 것은 이행이다.** 그리고 **고칠 범위가 업로드보다 넓다** — ③ 은 업로드와 무관한 동작이고, `link` 는 `DATAMODEL` 상 **N:N 이며 연결마다 활용 의미 문장이 붙으므로** 본문 있는 op 이다. **「op 하나 더한다」로 끝나지 않는다.** *(colab-v2-dev 세션 P2 착수 중 발견, 이 세션이 계약·게이트로 확인)* |

| **DR-8** ✅ **해소 (2026-08-23 재개정) — 첫 번째 ✅ 는 무효였다.** ~~해소 (2026-08-23) — 정본 갱신 완료(260808 md 4건·planning-base 2건 직접 수정 + 스냅샷 2건 정오표, 목록 표기). 승인: Ted 승인 2026-08-23 (본인 확인)~~ **정정 2026-08-23 — 이 ✅ 는 사실이 아니었다.** 「Ted 승인 2026-08-23」 이 달려 있었지만 **그 승인은 틀린 파일 목록 위에 서 있었다.** 실제로 고친 4건은 `01 CoLAB-Plan/reference/v2_1차마일스톤_기획_260808/` 과 `01 CoLAB-Plan/planning-base/` 아래에 있었고, 그 두 폴더는 `planning/README.md §2` 가 **「이제 참조하지 않는다 — 정본은 위 한 폴더뿐이다」**로 명시적으로 배제한 옛 사본이다. **260818 정본은 한 글자도 닿지 않았다.** `planning-freshness` 는 이것을 못 잡는다 — 그 게이트는 260818 정본 안의 임베드 ↔ 원본만 보고 다른 폴더는 시야에 없다. **재개정 완료 2026-08-23 (WU-P2 · NB-2 개정과 한 회차).** 260818 정본에서 `grib`·`HDF5` 를 쓰던 **7자리를 전부** `NetCDF · Binary · HDF4 · GeoTIFF` 기준으로 고쳤다 — E-04 Policy 3(§5 파일 · §9 그릴 수 없는 형식 · §10 파일 파서) · E-04 PRD 1(§7 파일 파서) · E-03 Policy 2(§8 그릴 수 없는 형식 · §8 지도 표현의 격자 예시 `(nc·grib·tif)`→`(tif·nc)`) · E-00 DataModel 1(§4.1 자동으로 읽은 정보). **지도 표현의 격자 예시까지 포함한 것은 Ted 판정이다** — 예시라도 정본에서 `grib` 을 본 사람은 그것을 지원 포맷으로 읽는다. 개정 전 정본에 `HDF4` 는 **0회** 등장했고 개정 후 등장한다. 패키지 임베드 3파일(E-00·E-03·E-04)을 원본과 재동기했고 `planning-freshness` 는 **본 체크아웃에서 green**(15개 블록 전부 MATCH). 검사기·정규화 규칙에는 손대지 않았다. 적용 기록 = `dev-package/sessions/P2-NB-2-canon-amendment-applied.md` 계약 무변경 — 처방대로 정본을 고쳤고 계약은 따라온다. *(이하 원문)* **`〈51〉`·`㊿-F-2` 가 기획 정본에 도달하지 않았다.** 지원 포맷을 **NetCDF·Binary·HDF4·GeoTIFF** 로 정하고 `.hdf` 가 HDF4 임을 실측했는데, **고친 것은 `dev-package/` 문서뿐**이다. 기획 정본(`Policy §9`)은 여전히 **GRIB·HDF5** 를 말하고, `contracts/events/core-pipeline.json:54,59` 가 **그 정본을 충실히 인용**하고 있다 — **계약이 틀린 게 아니라 정본이 낡았다** | **기획**(정본 갱신) · 반영 확인은 **D2** | **처방을 틀리기 쉬운 자리다** — 계약에서 `grib`·`HDF5` 를 지우면 **정본과 어긋난 계약**이 된다. 고칠 것은 정본이고 계약은 따라온다. ⚠ **`planning-freshness` 게이트는 이걸 못 잡는다** — 그 게이트는 **임베드 ↔ 원본**의 일치만 보지, **결정 ↔ 정본**의 일치는 아무도 안 본다. `DR-7` 과 같은 부류(**아무도 안 본 자리**)다 |

| **DR-9** ⚠**최상위** | **PoC 는 좌표를 못 찾으면 임의 정규격자를 지어내고 「성공」을 반환한다 — 4곳.** 그리고 **HSR 의 위경도는 현재 dummy `linspace`(합성값)이며 실제 좌표가 아니다.** 즉 **지도 위에 그려지는데 그 위치가 거짓일 수 있고, 아무 신호도 안 난다** | **D5** (좌표 없으면 **실패로 처리** — 지어내지 않는다) | **이 프로젝트가 이번 라운드에 모은 「에러 없이 그럴듯한 값」의 결정판이다.** 앞의 것들은 **값이 틀렸고**(잠긴 행 0 · COG 오인 · 확장자 · exit 0), 이건 **없는 값을 만들어 낸다.** 그리고 결과가 **지도라서 그럴듯해 보인다** — 사람이 눈으로 봐도 안 걸린다. **⟨2026-08-23 — 지어낼 이유가 없었다⟩ 실제 좌표가 원천에 있다.** `03 Reference-Data` 의 **포맷 5종 폴더 전부에 `04.Lat_Lon_info`** 가 있고 형상이 대응 격자와 정확히 일치한다 — HSR `(2881,2305)` 는 **PoC 자신의 상수 `HSR_NY×HSR_NX` 와 같다**(`SEED-DATA §3.5`, `〈57〉`). **PoC 는 옆에 있는 진짜 좌표를 안 쓰고 합성했다.** **v2 는 이 경로를 계승하지 않는다**: 좌표계·격자는 `DATAMODEL §4.1` 상 **「파일에서 자동으로 읽는 값」**이고, 못 읽으면 **`[미상]` 이지 추정이 아니다.** `㊴-②`(정본에 없으면 만들지 않는다)의 데이터판 |
| **DR-10** | **PoC 에 디코딩 구현이 두 벌 있다** — `backend/app/services/processors/`(COG 출력, 구버전)와 `viz-service/app/decoders/`(PNG 렌더, E018 신버전). **같은 포맷에 다른 상수·다른 전략**이고 **GeoTIFF 디코더는 viz-service 에만 있다** | **C3** (어느 쪽을 정본으로 뽑는지 명시) · **D5** | **C3 가 「PoC 지식을 뽑는다」고만 적혀 있으면 두 벌 중 어느 것인지가 안 정해진다** — 뽑는 사람이 그때그때 고르면 상수가 섞인다. `HARVEST.md` 는 **포맷별로 어느 구현에서 뽑았는지를 밝혀야** 한다 |

| **DR-11** | **PoC 는 파일을 통째로 메모리에 올린다** — `src.read(1)` · `ds[var].values` · `f.read()`. **50 GB 대응이 없다.** 그런데 v2 는 `services/pipeline-worker/README.md` 에서 **「전체 파일 메모리 적재 금지 — 50GB급이 들어온다, 윈도우/스트리밍 처리」**를 규칙으로 세웠다 | **D5** | **계승하면 안 되는 것을 명시하는 자리다.** C3 가 「PoC 지식을 뽑는다」로만 적혀 있으면 **이 구조까지 따라 뽑힐 수 있다.** PoC 가 다룬 파일이 작아서 안 터졌을 뿐이고, `03 Reference-Data` 에 이미 **1.3 GB 단일 묶음**이 있다 |
| **DR-12** | **오버뷰 리샘플링이 `nearest` 단일이다** — 범주형/연속형 분기가 없다(rio-cogeo 기본값에 위임, `overview_resampling="nearest"`). **`토지피복·LULC` 같은 범주형에는 맞지만 `NDVI`·강수 같은 연속형에는 `average`/`bilinear` 가 맞다** | **D5·D7** | 축소 뷰에서 **연속값이 튄다** — 조용히 틀리는 종류이고 **줌아웃했을 때만 보인다.** PoC 의 COG 파라미터는 대부분 **미지정으로 rio-cogeo 기본값에 위임**돼 있어(blocksize·predictor·overview·BIGTIFF 전부), **v2 가 고를 자유가 있다는 뜻이기도 하다** |

| ~~**DR-13**~~ ✅ **해소 (2026-08-23)** | **정의되지 않은 WU 를 진입조건으로 걸고 있었다.** `WORK-UNITS §6`(T-D)이 정의한 작업 단위는 **D1 · D2 · D2b · D3 · D3b 다섯**뿐인데 `§7` 의 P2 가 `D5`, P3 이 `D7`, P5 가 `D6` 을 진입조건으로 썼다(같은 모양이던 **P0 의 `D4 D6`·P7 의 `D8`** 도 함께 정정) | — (해소됨) | **제3안으로 닫혔다 — ⓐ 도 ⓑ 도 아니다** *(사용자 승인 2026-08-23)*. **`D5` 만 정식 WU 로 승격**(`WORK-UNITS §6` 행 신설 — magic-byte 감지·헤더 파싱·좌표계 변환·COG·outbox/워커, 지원 `NetCDF·Binary·HDF4·GeoTIFF`, 입력 tif ↔ 산출 COG 구분)하고 **나머지는 흡수 표기로 정정** — **D6→P5 · D7→P3 · D4→P2·P3 · D8→P7**, `P0` 의 `D4 D6` 은 **스키마 정본 참조**임을 명시. **가른 기준 = 「화면 없이도 닫히는가」** — D5 만 예다. **⚠ D5 완료 조건은 의도적으로 계약 op 를 참조하지 않는다** — `D2c` 가 만들 것에 완료 판정을 걸면 `STOP-1` 과 `STOP-4` 가 다시 묶인다. 게이트 green + 실데이터 E2E 로 닫는다. **효과 = `sessions/P2.md §7 STOP-4` 닫힘 · P2 는 이제 `STOP-1` 하나만 막는다 · `D2c` 의 선행 조건 충족** |

| **DR-14** ⚠**전제 불성립** | **v2 의 「자동으로 읽은 정보」에 PoC 선례가 없다 — 반대 선례가 있다.** `DATAMODEL §4.1` 은 포맷·변수·기간·좌표계·격자·용량을 **「파일에서 자동 — 사람이 타이핑하지 않는다」**로 못 박았다. **PoC 는 등재 시점에 파일을 한 번도 열지 않는다**(archive-first): 포맷 = **확장자 문자열** · 기간·좌표계·격자·bbox = **사람이 타이핑** · 용량 = **클라이언트 신고값 합산**. 파일을 여는 것은 **선택적 후속 워커**이고, 그 워커가 뽑은 시간축은 `time_start/end` 와 **영영 연결되지 않는다**(워커가 그 열을 안 건드린다). ⚠ **Binary 프로세서는 파일을 열지도 않고 변수·좌표를 하드코딩 반환한다** | **D5** | **C3 가 「PoC 지식을 뽑는다」로만 적혀 있으면 여기서 잘못 뽑힌다** — 뽑을 것은 **디코딩 방법**이고, **인제스트 구조는 v2 가 정반대로 간다.** v2 는 「업로드 = 파싱」인데 PoC 는 **등재와 파싱이 분리**돼 있었다. `㊾-③`(적재는 화면 경로로) 이 성립하려면 **D5 가 이 자동 추출을 실제로 구현**해야 하고, 그건 물려받는 게 아니라 **새로 만드는 것**이다 |
| **DR-15** | **PoC 계보에는 「가공 방식」이 없다.** 구조는 있다 — `dataset_lineage`(parent/child/relation_type/link_type/description · unique·index · **순환 감지 · 레벨 제약 · 권한 마스킹**까지 구현). **그러나 처리 방법·파라미터 필드가 전무**하고 `relation_type` 은 **항상 `derived`**, `description` 은 **항상 NULL** 이다. 그리고 **워커는 계보를 만들지 않는다** — 사람이 폼에서 고른 것만 기록된다 | **D5 · P2** | **v2 는 `가공 방식을 관계에 부착`한다**(`㊸` 이전부터 `DATAMODEL §4.2`). **그 부분만 선례가 없다.** 반대로 **순환 감지·레벨 제약·권한 마스킹은 물려받을 만하다** — PoC 가 이미 풀어 둔 문제다. **`SEED-DATA §4.1` 의 KWRA 묶음이 정확히 이 빈자리를 시험한다**(부모 1 → 자식 4, 관계마다 가공 방식이 다름) |
| **DR-16** | **`core-pipeline.json:39` 의 「기준 격자 0~1건」 산문이 `〈58〉`(0~2건·grid_axis) 과 어긋난다.** `common.json:79` 는 D2c 에서 0~2 로 갱신됐으나(additive·contract-breaking green), 이벤트 계약은 동결(`D2c §2-1`)이라 산문을 남겼다 — **저장소 안에서 두 계약이 서로 다른 규칙을 말한다.** core-pipeline 을 먼저 읽는 독자는 낡은 규칙을 얻는다 | **이벤트 개정 권한 있는 회차**(차기 이벤트 개정 워크리스트) | 기계 게이트는 산문 드리프트를 못 잡는다(lint 는 양쪽 다 green — advisor 실측). `DR-8` 과 같은 부류의 자리라 ID 로 추적한다 *(2026-08-23 advisor 수용 검토 지적)* |

> **PoC 유래 「4포맷」은 드리프트가 아니다.** C3(`HARVEST.md`)·`PLAN-SoT §7` 의 4는 **PoC 가 실증한 과거 사실**이라 보존한다. 올리는 것은 **v2 의 D5 지원 포맷·완료 조건 7·S3** 뿐이다.

---

## 5. PLAN 이탈 기록

| # | 이탈 | 왜 | 되돌리는 법 |
|---|---|---|---|
| **1** | **D1 이 열린 채로 D2·D3 을 진행했다** (2026-08-22 밤) | `WORK-UNITS §6` 이 D2 의 진입조건을 `D1, A3` 로 적었는데 둘 다 문제가 있었다. **(a) `A3` 는 존재하지 않는 WU 다** — 트랙 목록에 A 트랙 자체가 없다(v1 잔재로 보이는 유령 참조). **(b) D1 의 잔여는 `HYD` 실명 한 칸뿐**이고, 그 값은 *누가 승인하는가*의 문제이지 계약의 내용이 아니다 — OpenAPI·JSON-Schema 어디도 그것을 소비하지 않는다. Ted 가 "밤새 개발할 수 있는 것은 개발하자"고 한 상황에서, 이름 하나를 기다리며 밤을 비우는 것이 더 큰 손실이라고 판단했다 **해소 (2026-08-23)** — `§9-㊴` 로 D1 이 닫혔고, 전제("경계·배정 구조는 확정, 이름 한 칸만 미기재")가 **사실로 확인됐다.** 배정은 바뀌었지만(2인 → Ted 1인 + 정본 대조) 계약은 한 줄도 손대지 않았다 — **계약이 배정과 독립이라는 판단 자체가 검증된 것이다.** 되돌릴 것 없음 |

> **`A3` 유령 참조는 별도로 정리해야 한다.** 진입조건에 없는 WU 가 적혀 있으면 다음 세션이 그것을 기다린다.

---

## 갱신 방법 (세션 종료 시)

1. §1에서 해당 WU 상태 갱신 (✅/🟧+사유/⛔+원인)
2. §2 스냅샷 갱신
3. 상단 **최종 갱신 / 현재 단계 / 다음 WU** 갱신
4. 새 결정은 `PLAN-SoT §9`에 기록하고 §3에서 링크
5. 새 블로커·이탈은 §4·§5에
6. **미완 항목과 다음 WU의 진입조건을 명시** — 다음 세션이 §1을 선행조건으로 확인한다
