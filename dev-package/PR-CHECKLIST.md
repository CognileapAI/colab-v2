# PR 전 체크리스트 — 열려 있는 판정 · `[미확인]` · 정본↔대장 어긋남

> **이 문서가 무엇인가.** Ted 축자 = 「**PR 가기 전 체크리스트들임**」.
> 여기 모인 것은 **지금 판정으로 막는 것이 아니라, PR 전에 반드시 털고 가는 목록**이다.
> 등재 = `PLAN-SoT §9 〈257〉`(Ted RULING ㉛) · 최초 작성 2026-08-31 · 워크트리 `lane-checklist`.
>
> ⭑ **상태의 원본은 `dev-package/work-items.yaml` 이고 이 문서는 「무엇이 열려 있는가」의 목록이다.**
> 상태를 바꿀 때는 대장을 먼저 고친다(`CLAUDE.md §6`). **이 문서로 항목을 닫지 않는다.**
>
> **읽는 법 — 각 건에 넷을 붙였다.**
> ㈎ 무엇이 걸려 있는가(쉬운 말로) ㈏ 누가 무엇을 정해야 하는가 ㈐ 안 정하면 무엇이 막히는가(항목 id) ㈑ 어디에 근거가 있는가(문서·행)
>
> **급한 순으로 세웠다** — 많이 막는 것, **정본 어긋남**이 위다.

---

## §1. 정본 ↔ 대장 어긋남 — **가장 위** (9묶음)

> **왜 가장 위인가.** 이것은 「아직 안 정한 것」이 아니라 **「정했는데 문서가 딴말을 하는 것」**이다.
> 읽는 사람이 **틀린 것을 옳다고 믿는다.** 이 세션에서 「타일 서빙」·`CT-1` 의 `P6` 가 그 사례였고,
> 훑어 보니 **같은 무늬가 훨씬 넓게 퍼져 있었다.**
>
> ⭑ **⟨2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`⟩ D-1~D-9 아홉 묶음을 전건 실측하고 고쳤다.**
> **각 건의 조치는 아래 제목 줄에 붙였고, 종전 등재문은 지우지 않았다.**
> ／ 종전 머리말 ↓
> ⚠ ~~**이 회차는 이 어긋남을 고치지 않았다**~~ — 대부분 다른 레인·다른 항목의 소유이고 범위를 늘리지 않는다(`CLAUDE.md §5`).
> **찾아서 이름과 근거 행으로 적는 것까지가 이 문서의 몫**이다.

### D-1 ✅ **해소 2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`** — 산문 4종을 대장에 맞췄다(`CLAUDE.md` stage 행·AI 두 지점 행 · `03-HANDOFF` `P4`·`K3`·`K4`·`K5` 행 · `WORK-UNITS §7`·`§8`·`§8.5`·`§10.2`(`T-1`)·`§11`). **원문 무삭제 · 종전 문면은 취소선으로 보존.** ⭑ 새로 찾은 것 = **`T-1` 도 stage 3 인데 `WORK-UNITS §10.2`·`§11` 이 stage 2 로 세고 있었다.**

／ 종전 등재문 ↓

#### D-1 ⭑⭑ AI 3항목 ＋ `P4` 의 stage 3 이동이 산문에 **한 줄도** 반영되지 않았다

- ㈎ 2026-08-30 Ted 판정으로 `K3`·`K4`·`K5`·`P4` 가 **stage 3**(`after_stage2`)으로 갔다. **대장만 바뀌고 산문은 전부 옛 자리에 있다.** 산문 4종에 **「stage 3」 문자열이 0건**이다.
- ㈏ **다음 문서 회차가 산문 4종을 대장에 맞춘다.** 새 판정이 아니라 반영이다.
- ㈐ **stage 2 마감 범위가 문서마다 다르게 읽힌다.** 막는 것 = `K3`·`K4`·`K5`·`P4`, 그리고 stage 2 완주 계수 전체.
- ㈑ 대장 `work-items.yaml` `K3`·`K4`·`K5`·`P4` 의 `stage: after_stage2` ↔ 낡은 자리 = `03-HANDOFF.md:247`(`P4`)·`:263`(`K3`)·`:265`(`K5`) · `WORK-UNITS.md:30`·`:33`·`:220`·`:640` · `CLAUDE.md:15`·`:19`

### D-2 ✅ **해소 2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`** — **역할을 갈라 적었다.** `CLAUDE.md §1` 1번 = 「진행 상황 요약의 출발점」 ＋ 「**항목 상태·`stage`·완료 정의의 원본은 대장이고 이 문서는 반영본이다**(`§6`)」 한 줄. `§6` 은 손대지 않았다(나중 규율이 이긴다). 종전 ~~여기가 진실원~~ 은 취소선으로 보존.

／ 종전 등재문 ↓

#### D-2 ⭑⭑ 「진실원이 어디인가」가 `CLAUDE.md` **안에서** 갈려 있다

- ㈎ `CLAUDE.md §1`(`:28`)은 「`03-HANDOFF.md` — **여기가 진실원**」이라 하고, 같은 파일 `§6`(`:119-120`)은 「상태의 **유일한 원본은 `work-items.yaml`**, `03-HANDOFF §1` 은 그 반영본」이라 한다. **정면으로 충돌한다.**
- ㈏ **`CLAUDE.md §1` 의 문장을 「진행 순서의 출발점」으로 좁히거나, 「상태의 진실원은 대장」을 명시해야 한다.** 소유 = CLAUDE.md 유지 회차.
- ㈐ 막는 것 = **모든 항목**. 매 세션 자동으로 읽히는 파일이라 파급이 가장 크고, 이 어긋남이 §1 의 D-1 같은 드리프트를 계속 만든다.
- ㈑ `CLAUDE.md:28` ↔ `CLAUDE.md:119-120` · 같은 취지 낡은 문장 `WORK-UNITS.md:590`·`:599` · 판정 `PLAN-SoT §9 〈176〉`(2026-08-28) — **`CLAUDE.md:28`·`WORK-UNITS` 두 줄이 08-27 판이라 더 낡았다**
- ⚠ `03-HANDOFF.md` 머리말이 이미 경고했다 — 「이 구분을 적지 않으면 **「진실원」이라는 말 자체가 다음 갈라짐이 된다**」

### D-3 ✅ **해소 2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`** — 산문 4곳을 대장 문면으로 교체했다 — `K3` = **수치 합격선 없음**(`〈211〉`) · `K4` = **「미판정 0건 ＋ 판정 후 전건 통과 ＋ 사유 기록」**(`〈217〉`) · **평가셋 18건 실재 · `S2b` `done`**. `P4` 의 「닫지 않는 이유」 두 곳도 함께 고쳤다.

／ 종전 등재문 ↓

#### D-3 ⭑ `K3`·`K4` 의 완료 정의가 정본에 **폐기된 문면 그대로** 남아 있다

- ㈎ 대장은 두 문면을 **취소선으로 폐기**했는데(`〈211〉`·`〈208〉`·`〈217〉`), 산문은 그 폐기된 문장을 **축자로 다시 인쇄**하고 있다. 특히 「**평가셋이 초안이다**」는 대장이 「평가셋 18건이 `eval/s2b-alayer/` 에 **실재**한다」로 뒤집은 문장이다.
- ㈏ **다음 문서 회차가 산문을 대장 문면으로 교체한다.**
- ㈐ 막는 것 = `K3`·`K4`·`P4`. 「평가셋이 초안이라 못 닫는다」가 **성립하지 않는 차단 사유**로 계속 인용된다(`S2b` 는 이미 `done`).
- ㈑ 대장 `K3`(「수치 합격선은 이 항목이 정하지 않는다」)·`K4`(「합격선은 퍼센트가 아니다 — 미판정 0건 ＋ 판정 후 전건 통과」) ↔ `WORK-UNITS.md:260`·`:261`·`:444`·`:220` · `03-HANDOFF.md:247` · `CLAUDE.md:15`

### D-4 ✅ **해소 2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`** — **남아 있던 2곳**(`03-HANDOFF` `S3` 행 · `WORK-UNITS §8.5` `S3` 행)을 5종으로 맞췄다. ⚠ **고치지 않은 자리 셋을 이름으로 적는다** — `WORK-UNITS §1.5` 조건 7·판정 표(정의 원문 · `〈248〉` 이 「고치지 않는다」로 못 박았다) · `§11` 마지막 줄(이미 `〈248〉` 주석이 붙어 있다) · `§11` 2026-08-27 정정 이력문(과거 표를 인용한 이력). **`§7` `D5` 행의 「4종」은 다른 값이다 — 건드리지 않았다.**

／ 종전 등재문 ↓

#### D-4 `S3` — 「4종」과 「5종」이 같은 레포 안에 함께 있다

- ㈎ `〈248〉`(2026-08-31)이 **5종**(＋NumPy)으로 판정했고 대장·게이트 설정(`gates/config/e2e-format-coverage.toml`)은 다섯인데, **산문 5곳이 아직 「4종」**이다. `WORK-UNITS §11` 코드블록 한 줄만 고쳐졌다.
- ㈏ **다음 문서 회차가 산문 5곳을 5종으로 맞춘다.** 값 판정은 이미 끝났다.
- ㈐ 막는 것 = `S3`. 완주 조건의 계수가 갈린다.
- ㈑ 대장 `S3` name/`completion_def` · `PLAN-SoT §9 〈248〉` ↔ `03-HANDOFF.md:109`·`:276` · `WORK-UNITS.md:48`·`:290`·`:307`
- ⚠ 부수 — `gates/config/e2e-format-coverage.toml` 주석이 아직 「어긋난 채로 등재돼 있다」고 미해소를 선언한다. **값은 맞고 주석만 낡았다.**

### D-5 ✅ **해소 2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`** — `WORK-UNITS §7` `P4`·`P6` 행, `§8.5` `P6` 행, `03-HANDOFF` `P4` 행에 대장 값을 적었다 — `P6` = `[P1,P2,P3,P5]` · `P4` = `[P1,G2,T-1]`.

／ 종전 등재문 ↓

#### D-5 `depends_on` 절단 2건이 산문에 미반영

- ㈎ `P6` 에서 `P4` 를, `P4` 에서 `K4` 를 뺐다(2026-08-30). 산문은 아직 옛 의존을 그린다.
- ㈏ **다음 문서 회차가 반영한다.**
- ㈐ 막는 것 = `P6`·`P4`. **착수 가능 시점을 실제보다 늦게 읽게 한다.**
- ㈑ 대장 `P6` `depends_on: [P1, P2, P3, P5]` · `P4` `depends_on: [P1, G2, T-1]` ↔ `03-HANDOFF.md:247`·`:249` · `WORK-UNITS.md:220`·`:222`·`:441`

### D-6 ✅ **해소 2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`** — `03-HANDOFF` `LV-2` 행과 `WORK-UNITS §11` 의 「`LV-2` 는 미정」 한 줄을 `stage1`(`〈203〉`)로 맞췄다. `CLAUDE.md` 도 함께. **`WORK-UNITS §11` 의 `〈203〉` 주석은 이미 있었다 — 그 아래 다른 줄이 낡아 있었다.**

／ 종전 등재문 ↓

#### D-6 `LV-2` stage 가 `CLAUDE.md` 와 `03-HANDOFF §1` 에만 `[미확인]` 로 남아 있다

- ㈎ `〈203〉`(2026-08-29)이 `stage1` 로 **확정**했다. `03-HANDOFF.md:51` 은 반영했는데 **같은 파일 `:266` 은 아직 `[미확인]`** 이다(한 파일 안에서 갈린다).
- ㈏ **문서 회차가 두 자리를 지운다.** 판정은 끝났다.
- ㈐ 막는 것 = `LV-2` 착수 판단.
- ㈑ 대장 `LV-2` `stage: stage1` · `PLAN-SoT §9 〈203〉` ↔ `CLAUDE.md:15` · `03-HANDOFF.md:266`

### D-7 ✅ **해소 2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`** — `03-HANDOFF` `LV-4` 행을 `after_stage2`(`〈212〉`)로 맞췄다. **저장 자리·`〈196〉` 경고 문구는 여전히 `[미확인]` 이라 그대로 뒀다** — 이 둘은 미판정이지 드리프트가 아니다. ⚠ `sessions/LINEAGE-LEVEL.md` 는 회차 기록물이라 고치지 않았다.

／ 종전 등재문 ↓

#### D-7 `LV-4` stage 가 `[미확인]` 로 남아 있다

- ㈎ `〈212〉` 가 `after_stage2` 로 배정했는데 산문은 `[미확인]` 이다.
- ㈏ 문서 회차가 반영한다. ㈐ 막는 것 = `LV-4`. ㈑ 대장 `LV-4` `stage: after_stage2` ↔ `03-HANDOFF.md:255` · `sessions/LINEAGE-LEVEL.md:383`

### D-8 ✅ **해소 2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`** — `CLAUDE.md` stage 2 정의에서 「타일·확대」를 **「확대」**로 바꾸고 `〈240〉` 개정 표시를 얹었다(정본 축자 POL-021 인용 · 종전 ~~타일·확대~~ 보존).

／ 종전 등재문 ↓

#### D-8 「타일 서빙」이 `CLAUDE.md` 에만 **요구처럼** 남아 있다

- ㈎ `〈240〉`(2026-08-31)이 「타일 서빙은 **요구가 아니라 선택 갈래**다 · 기본은 「한 장」 · **정본에는 없던 문구**」로 판정했다. `CLAUDE.md:19` 의 stage 2 정의는 아직 「타일」을 요구 목록에 넣고 있다.
- ㈏ **CLAUDE.md 유지 회차가 그 낱말을 갈래 표기로 바꾼다.**
- ㈐ 막는 것 = `P3` 계열의 범위 읽기. **매 세션 자동 로드라 파급이 크다.**
- ㈑ `WORK-UNITS.md:410-426`(`〈240〉` · 정본 축자 POL-021 「타일 서버도 바탕 지도도 쓰지 않는다」) ↔ `CLAUDE.md:19`

### D-9 ✅ **해소 2026-09-01 · 워크트리 `lane-drift` · `PLAN-SoT §9 〈268〉`** — `WORK-UNITS §11` 산문 한 줄을 대장 실측으로 다시 셌다(기준·시점 명기). ⭑ **보고보다 넓었다 — `P2`·`P3`·`P5` 에 더해 `S2` 도 `done` 인데 잔여로 세고 있었고**, 같은 절의 「✅ 로 닫힌 것」 목록이 `P2`·`P3`·`P5`·`S2`·`S2b`·`PV-1` 여섯을 빠뜨리고 있었다. 둘 다 고쳤다.

／ 종전 등재문 ↓

#### D-9 완주 계수 산문 한 줄이 닫힌 항목을 아직 세고 있다

- ㈎ `WORK-UNITS.md:640` 이 `P2`·`P3`·`P5` 를 잔여로 센다. **셋 다 `done` 이다.** 바로 위 코드블록(`:624-626`)은 맞다 — **게이트가 보는 자리만 고쳐졌다.**
- ㈏ 문서 회차가 산문 한 줄을 맞춘다. ㈐ 막는 것 = 완주 계수(`§11`). ㈑ 대장 `P2`·`P3`·`P5` `status: done` ↔ `WORK-UNITS.md:640`

### ⚠ D-0 — **왜 게이트가 green 인데 이만큼 갈렸는가** (이 절 전체의 원인)

⭑ **⟨일부 해소 2026-09-01 · `PLAN-SoT §9 〈268〉`⟩ `work-item-consistency` 에 ㈕ 를 더했다** — 대장의
`stage: after_stage2` 집합 ↔ `CLAUDE.md` 의 표지 블록 대조. **`CLAUDE.md` 가 대조 대상이 된 것은
이번이 처음이다**(0 → 1). fail-closed = 표지 부재·미폐쇄·파일 부재 전부 red. 변이 증명 =
`work-item-selftest` **14 → 17 케이스**(㈕ red 둘 ＋ 지침 문서 부재 red 하나 · 기호까지 대조).
⚠ **남은 사각은 그대로다** — `completion_def`·`depends_on`·비고 열·`WORK-UNITS §7`·`§8`·`§10.2`
산문은 여전히 대조 대상 밖이고, `stage1`·`stage2` 는 대조하지 않는다(사본을 만들지 않기 위해).
**아래 표는 지우지 않는다.**

`work-item-consistency` 가 **보는 것**은 일곱뿐이다 — 대장 스키마 · `WORK-UNITS §11` **코드블록** · `03-HANDOFF §1` 표의 **상태 열** · `deferred` 재등장 · `deadline.fired` · `conflict` 잔존 · 결정번호 중복(`〈252〉`).

**보지 않는 것 = 위 D-1~D-9 가 사는 자리 전부다.**

| 안 보는 축 | 여기서 난 어긋남 |
|---|---|
| `stage` 를 산문과 대조하지 않는다 | D-1 · D-6 · D-7 |
| `completion_def` 를 산문과 대조하지 않는다 | D-3 |
| `depends_on`·`entry_conditions`·`owner` 대조 없음 | D-5 |
| **비고 열**(스크립트가 「산문이라 건드리지 않는다」고 명시) | D-3 · D-5 |
| `WORK-UNITS §7`·`§8`·`§8.5`·`§10.2`·`§1.5` 는 대조 대상 밖 | D-1 · D-3 · D-4 |
| **`CLAUDE.md` 는 아예 대조 대상이 아니다** | D-1 · D-2 · D-6 · D-8 |
| 이름·범위 낱말(포맷 수·「타일 서빙」) | D-4 · D-8 |

---

## §2. 열린 블로커 — `03-HANDOFF §4` (**열린 것 11건** · 닫힌 `#55` 1행은 이력으로 남긴다 = 표 12행)

> ⭑ **닫힌 근거를 남긴다** — `#52`(`PA-G` 개시 조건) · `#53`(`LV-1`·`LV-2` 등급)은 **2026-08-31 Ted RULING ㉜·㉝ 으로 닫혀 이 목록에서 빠졌다**(`PLAN-SoT §9 〈258〉`). 왜 빠졌는지는 `§7` 에 적었다.
>
> ⭑ **⟨2026-09-01 증보 3건 — `#58` 은 야간 회차 추가⟩ `#56`(등록 전환 시 산출물 자리 이동의 소유 · `〈264〉`-㉳) · `#57`(`preview-tile-slot` 이 경계 롤에 속는다 · `〈262〉`-㉳). 9건 → **11건**.

| # | ㈎ 걸린 것 | ㈏ 누가 무엇을 | ㈐ 막는 것 | ㈑ 근거 |
|---|---|---|---|---|
| **#57** | **`preview-tile-slot` 이 경계 롤에 속는다** — `d5_pipeline_event` 를 롤 확인 없이 세므로 경계 롤이면 발행 0 → 「발행＞0 인데 쓸 수 있는 타일 0」 판정이 **통째로 건너뛰어지고 green 이 난다**(green-by-skip). 배선이 스키마 전용 데이터베이스라 발행이 구조적으로도 0 인 결함이 겹쳐 있다 | **Ted** — 롤 판정을 붙일지, 배선을 먼저 바꿀지, 둘을 묶을지 | 미리보기 타일 자리의 **검사 신뢰도 전체** · `A-1` 착수 회차의 인용 | `03-HANDOFF §4 #57` · `PLAN-SoT §9 〈262〉`-㉳ |
| **#58** | **`NumPy` 헤더 파싱이 멈추고 메시지가 「지원 목록 밖」이라 거짓말한다** — `SUPPORTED_FORMATS`·`RENDERABLE_FORMATS` **둘 다에 있는데** 처리가 없다(선언 여섯 · 처리 넷) | **Ted 판정** — 처리를 여섯으로 채울지, 선언을 넷으로 줄일지(범위 결정) | **`S3` 는 막지 않는다**(화면 경로는 `D7`). 막는 것 = `NumPy` 를 정본 지원 목록에 남길지의 확정 · 소유는 닫힌 `D5` 위의 신규 결함 | `PLAN-SoT §9 〈267〉`-㉲ · `sessions/S3-E2E-20260901.md` |
| **#56** | **등록 전환 때 미리보기 산출물의 자리를 누가 옮기는가** — 접수분 바이트는 `_relocate` 가 `uploads/{uploadId}/`→`uploads/{datasetId}/` 로 옮기는데 굽는 시점의 대상은 `uploadId` 이고 등록 뒤 그리는 쪽은 `datasetId` 를 본다. **등록 경로에 재생성 트리거가 없고** core-api 는 미리보기 볼륨을 마운트하지 않는다 ⟹ 그대로 배선하면 **에러 없이 빈 지도** | **Ted** — ⓐ core-api 에 볼륨(경계를 넘는다) ⓑ 등록 사건을 D5·D7 이 받는다(이벤트 계약 넷째 종류 = **동결 해제 한 번 더**) ⓒ 재생성 트리거 신설 | **`A-1` 의 배선 절단(1-③)** — 이 판정 뒤에 연다 | `03-HANDOFF §4 #56` · `PLAN-SoT §9 〈264〉`-㉳ |
| ~~**#55**~~ ✅ **닫혔다 (2026-08-31 · Ted RULING ㉗ · `PLAN-SoT §9 〈253〉` · 12차 동결 해제)** — 이벤트 계약을 확장해 발신·수신을 같은 회차에 배선했다. ⭑ **⟨정정 2026-09-01⟩ `03-HANDOFF §4` 는 이미 ✅ 였는데 이 표만 열린 채였다.** `Y-1` 을 지금 막는 것은 이 항이 아니라 **대장 완료 정의 ⓕ `staging 배포 green`** 이다. ／ 이하 원문 — ~~`D5` → `D7` 자동 무효화 **트리거를 보내는 쪽**이 없다. 받는 자리(`TriggerPort`)는 섰다. 우회로 둘이 다 막혔다 — D7 이 D5 표를 직접 읽는 것은 **불변규칙 1** 위반이고, 이벤트 계약은 `core-api ↔ pipeline-worker` 전용이라 종류 추가가 **계약 개정**이다 ~~ | ~~**Ted** — 계약 개정으로 갈지, 다른 경로를 세울지 | **`Y-1`** (닫는 마지막 조각) | `03-HANDOFF §4 #55` · `PLAN-SoT §9 〈247〉` · 대장 `Y-1` note |
| **#54** | `V-1`·`V-2`·`J-1` 의 완료 조건이 **「초안」** 이다 | **Ted 확정** | `V-1`·`V-2`·`J-1` **셋 다 착수 가능이 아니다** | `PLAN-SoT §9 〈250〉`·`〈251〉` · 대장 `V-1`·`V-2`·`J-1` `completion_def_draft` |
| **#39** | `work-item-consistency` 가 **CI 에 배선돼 있지 않다** — `work-item-selftest` 만 돈다. **대장↔산문 갈라짐을 CI 가 안 잡는다.** 지금 그대로 배선하면 CI 가 항상 실패한다 | **Ted** — 배선 자리와 **설계상 red 의 CI 실패 정책** | **`§1` 어긋남 9묶음 전부** — 이것이 없어서 위가 쌓였다 | `03-HANDOFF §4 #39` · `PLAN-SoT §9 〈178〉-㉵`-⑴ |
| **#41** | 게이트가 CI 에서 처음 돌자 실패 셋이 드러났다 — `generated-up-to-date`(frontend 의존성 미설치) · `seam-consistency-selftest` 4건(얕은 클론에서 기준선을 못 잡는 것으로 **보인다 · `[미확인]`**) · `db-boundary-selftest` 2건 | **실측이 선행** — 원인을 재고 나서 고친다. 추정으로 고치지 않는다 | CI 신뢰도 전체 · `G1b` | `03-HANDOFF §4 #41` |
| **#42** | **문서만 바꾼 커밋의 CI success 는 빈 성공**이다 — 경로 필터가 `dev-package/**` 를 어느 잡에도 걸지 않아 잡 5개가 전부 `skipped` 인데 success 가 찍힌다 | **Ted** — 문서 계열을 경로 필터에 걸 것인가, 걸지 않기로 못 박을 것인가 | CI green 의 인용 가능성 전체 | `03-HANDOFF §4 #42` |
| **#43** | **스크립트 롤백 경로가 없다** — green `deploy` 행이 하나뿐이라 `rollback.sh` 가 `die` 하고, `:prev` 는 원장에 없는 수동 빌드본이라 후보로 보이지도 않는다 | **I3 레인** — 롤백 대상 선택 규칙 | `I3` · 사고 시 되돌림 | `03-HANDOFF §4 #43` |
| **#44** | staging 실물의 **`403` 을 잴 시험 데이터가 없다.** 결함이 아니라 데이터 부재다 — 거부를 유발할 주체가 staging 에 없다 | **Ted 판정** — 스위치가 꺼진 `연구원` 토큰 1건을 주체 등기부에 등재할 것인가(**자격증명 파일 쓰기**라 사람 자리) | **`X-5` 의 마지막 `[미확인]` 1건** | `03-HANDOFF §4 #44` · `sessions/X5-403-VERIFY.md` |
| **#40** | 실행 비트 결함이 `gates/` 밖에 **52개** 남아 있다(`100644`). 로컬은 DrvFs 라 안 드러나고 **리눅스 경로에서 호출되는 순간 죽는다** | **인프라 레인** — 일괄 정정 | CI·리눅스 실행 경로 전반 | `03-HANDOFF §4 #40` |
| **#22** | D2·D8 고아 행 삭제 — **판정 완료 · 실행 대기.** ⚠ 삭제 대상을 **조건문이 아니라 id 목록으로 못 박은 뒤** 실행한다 | 실행 회차 | 데이터 정합 | `03-HANDOFF §4 #22` · `PLAN-SoT §9 〈167〉-㉰`·`〈173〉` |

---

## §3. Ted 판정 대기 — `03-HANDOFF §4.5` 진입조건 (7건)

| ㈎ 걸린 것 | ㈏ 정할 것 | ㈐ 막는 것 | ㈑ 근거 |
|---|---|---|---|
| 자연어 검색 1세대 평가셋의 **기대값 개정 6건**을 적용할 것인가 | Ted | **`K4`** — 적용하면 미충족 8 → 2 | `03-HANDOFF §4.5` · `PLAN-SoT §9 〈220〉-㉱` |
| **Verified 우선 정렬**을 언제 투입할 것인가 — 실행 경로에 `verified` 0건이라 `P6` 확정 전엔 판정 불가 | Ted | **`P4`** | `03-HANDOFF §4.5` |
| 「전 종 green」 **합격선을 어디서 재는가** ＋ 자동 트리거 점화 시점 | Ted | **`I3`** | `03-HANDOFF §4.5` · `PLAN-SoT §9 〈226〉-㉳` |
| **확대·타일을 `P3` 범위에 넣을 것인가** — 정본에 규정 0건 | Ted | **`P3`** 계열 · `§1 D-8` 과 같은 뿌리 | `03-HANDOFF §4.5` |
| **제안을 한 번도 만들지 않은 채** 「제안 서비스」를 닫는 것이 판정의 뜻인가 | Ted | **`K3`** | `03-HANDOFF §4.5` |
| 검색 평가셋 — **결합 규칙**(`〈89〉` 미판정) · 유사도 임계 · 설명 본문 갱신 | Ted | `K4` · 평가셋 | `03-HANDOFF §4.5` · `PLAN-SoT §9 〈89〉` |
| **기존 12개 데이터셋을 미리보기 뒷단에 소급 반영할 것인가** — 면제 선언이 비어 있다 | Ted | `PV-1` 후속 | `03-HANDOFF §4.5` |

---

## §4. 대장의 「완료 정의 미작성」 (29 항목)

> ㈎ **닫는 기준이 없다.** 기준이 없으면 「끝났다」를 말할 수 없고, **부분 완료로 조용히 닫히는 자리**가 된다.
> ㈏ **각 항목에 도달한 회차가 기준을 쓴다.** 지금 몰아서 쓰지 않는다 — 근거 없이 쓰면 지어내는 것이 된다.
> ㈑ 근거 = `dev-package/work-items.yaml` 각 항목의 `completion_def`.

| 묶음 | 항목 | ㈐ 막는 것 |
|---|---|---|
| **착수 임박** ⭑ | `V-1` · `V-2` · `J-1` · `P3` | 셋은 `#54` 로 **Ted 확정 대기**(`§2`). `P3` 는 `done` 인데 `completion_def` 가 비었다 — **닫은 근거가 문서에 없다** |
| 이관·정리 | `C1` · `C2` · `C3` · `C4` · `C3·C4` | `T-C` 트랙 · 완주 계수에 든다 |
| 초기 레인 | `R0` · `R1` · `R2` · `SC-1`~`SC-5` · `계약해제-2-3-4차` | 대부분 이미 지나간 자리 — **기준 없이 닫혔거나 기준이 산문에만 있다** |
| 게이트·보안 | `gate-db-boundary` · `W7` · `보안` · `G9` · `generated-up-to-date` | 게이트 계열 |
| 그 밖 | `D2c` · `DP-1` · `X-3` · `F-1` · `2단-A` · `2단-E` | — |

⚠ **`X-5` 는 이 목록 밖이지만 남은 `[미확인]` 1건이 `§2 #44` 다.**

---

## §5. 대장의 `[미확인]` · `[미측정]` 값 자리 (23 항목)

> ㈏ **전부 「재면 되는 것」과 「Ted 가 정할 것」으로 갈린다.** 갈래를 아래에 적었다.
> ㈑ 근거 = `dev-package/work-items.yaml` 각 항목.

| 항목 | ㈎ 무엇이 비어 있는가 | ㈏ 갈래 | ㈐ 막는 것 |
|---|---|---|---|
| **`R-1`** | ⑵ 를 **복원된 스택에서** 잰 회차 0건 · ⑴ 을 회차마다 다시 재는 기구가 게이트 밖 · `RUNBOOK §5` ⑤·③-보가 손검사 | **재면 된다** | `R-1` (`§7` 참조) |
| **`I4`** | 분산 추적 · 로그 · 알람 · 데이터 레지던시 **실물 0건** | 구현 | `I4` |
| **`I3`** | 자동 트리거 완주(크론 미설치) · 롤백 왕복 미검증 · 직전 릴리스 확인 미검증 | 재면 된다 ＋ `§2 #43` | `I3` · `I4`(선행) |
| **`X-5`** | 계약 감사 잔여 2건 · staging `403` 실측 1건 | **Ted**(`§2 #44`) | `X-5` |
| **`X-6`** | 유사 형제 패턴 탐색 범위 · `I3` 상태 판정 | Ted | `X-6` |
| **`T-1`** | 검색 평가셋 A층 실패 8건의 거취 = 「판정 범위 밖」 | Ted | `T-1` |
| **`V-1`** | 합격선이 될 실측 없음 · **옛 키 파일 회수 장치** | Ted(`#54`) | `V-1` |
| **`V-2`** | 새 눈금 관련 값 | Ted(`#54`) | `V-2` |
| **`J-1`** | **5자리 — `§6` 에 따로 펼쳤다** | Ted(`#54`) | `J-1` |
| **`W-1`** | 계약 동결 해제 등급 · `contract-breaking` ERR 건수 | ⭑ **`LV-1` 몫은 2026-08-31 실측으로 닫혔다**(`§7`) | — |
| **`LV-1`** | ~~등급~~ ⭑ **㉯ 로 확정**(`§7`) · 쌓인 값 재계수는 **착수 시점에** 한다(설계 그대로) | — | — |
| **`LV-2`** | ~~동결 해제 등급~~ ⭑ **「해당 없음」으로 닫힘**(`§7`) · **stage 표기 낡음은 `§1 D-6`** | — | — |
| **`LV-3`** · **`LV-4`** | 저장 자리 설계 · 삭제 경고 문구·조건 · stage 표기(`§1 D-7`) | Ted | `LV-3`·`LV-4` |
| **`VZ-1`** | 계약 산문 정정 미실시 | 문서 | — (범위 밖) |
| **`D5`** | 유실 감지 구현 방식 · 착수 순서 · 일부 `owner` **미정** | 구현 회차 | `D5` 잔여 |
| **`G7`** | **연구실 설정 화면(S-07)의 소유 에픽이 어느 문서에도 없다** | **Ted** — 정본 확인 자리 | `G7` |
| **`PV-1`** · **`P2`** · **`P3`** · **`P6`** · **`S2`** · **`CT-1`** · **`F-1`** · **`F-2`** | 대부분 **이미 닫힌 항목의 이력 문구**에 `[미확인]` 이 남은 것 | 문서 정리 | — |

---

## §6. `J-1` 편의 기능 묶음 — 값이 비어 있는 **5자리**

> ㈎ 9건의 닫는 기준 초안 중 **다섯 자리가 비어 있다.** 근거가 없어 **숫자를 지어내지 않고 비워 뒀다**(`〈251〉`).
> ㈏ **이 다섯은 `J-1` 착수 회차로 미룬다.** 여기 박아 두는 것은 **「무엇을 정해야 하는지」**다.
> ㈐ 막는 것 = **`J-1` 전체.** 9건은 함께 서고 하나씩 꺼내지 않는다(`§J` 규칙 ③).
> ㈑ 근거 = `work-items.yaml` `J-1` `completion_def_draft` · `PLAN-SoT §9 〈251〉` · `03-HANDOFF §4 #54`

| 자리 | ㈎ 정해야 하는 것 | ㈏ 왜 지금 못 정하는가 |
|---|---|---|
| `J-1`⑷ **연구실 밖 가져오기** | 다른 연구실의 격자를 가져오는 것을 **허용할 것인가** | **권한 판정**이라 Ted 몫. 연구실 경계는 자동 주입되는 불변규칙(`CLAUDE.md §3-5`)이라 임의로 뚫지 않는다 |
| `J-2`⑷ **기본 격자를 정할 수 있는 역할** | 연구실 기본 격자를 **누가** 지정하는가 | 역할 배정은 정본(E-01 권한)의 자리다 |
| `J-3`⑶ **경고 문구에 실을 거리 수** | 격자 불일치 경고에 **몇 m** 를 적을 것인가 | **612 m 는 HSR `.npy`↔`.nc` 실측**이고 **임의의 두 격자에 일반화되지 않는다.** 지어내지 않는다 |
| `J-8`⑴ **남은 시간 예측의 합격선** | 「약 n초」를 **무엇을 근거로** 내보내는가 | **근거가 될 실서버 값이 `[미측정]`**(`P-5`)이다. 표본 없이 내보내는 것은 **정직성 위반**이라 red 로 삼는다. **선행 = 단계별 실측 표본이 서는 것** |
| `J-9`⑶ **이득 합격선** | 첫 파일 즉시 렌더가 **얼마나 빨라져야 넣는가** | 지배 구간이 업로드(tif 12:1 · `§8.4`)라 렌더를 당겨도 체감 이득이 얼마인지 재지 않았다 |

---

## §7. 이번 회차가 닫은 것 · 이번 회차가 남긴 것 (2026-08-31 `lane-checklist`)

### 닫혔다 — **왜 이 목록에서 빠졌는지의 근거**

| 무엇 | 닫힌 근거 |
|---|---|
| `03-HANDOFF §4 #52` **`PA-G` 개시 조건** | **`〈258〉`(RULING ㉜)** — 「`stage1`·`stage2` 미완 0건」으로 확정. 규칙 넷과 실측 **16**(`PA-G` 자기 제외)까지 대장 `entry_conditions` 에 있다 |
| `03-HANDOFF §4 #53` **`LV-1` 등급** | **`〈258〉`(RULING ㉝)** — **검사를 실제로 돌려** ERR 0·WARN 1 을 재고 **등급 ㉯** 로 확정. 추정 아님. ⚠ **승인은 착수 회차의 Ted 몫**(12차) |
| `03-HANDOFF §4 #53` **`LV-2` 등급** | **`〈258〉`(RULING ㉝)** — **「해당 없음」.** 열 계약이 없다(op·`reason` 갈래가 이미 있고 완료 정의에 계약 표면 항이 0건) |
| **stage 2 완주 계수의 정의** | **`〈258〉`(RULING ㉞)** — stage 1 미완 3건(`SC-5`·`LV-1`·`LV-2`)을 계수에 넣는다. 정의·수치는 `WORK-UNITS §11` 머리말 |
| `R-1` **미확정 4행의 통과 기준** | **`〈255〉`(RULING ㉙)** — 넷 다 확정. 약점 둘(주 1회 감시 · 배포 경로 한정)도 기준과 함께 적었다 |
| `I4` **시점 복구의 거취** | **`〈256〉`(RULING ㉚)** — 이번 범위 밖 · **`§9-㊻` prod 개통 관문 (다)** 로 이동 |

### 남았다 — 닫지 못한 조건

| 항목 | ㈎ 남은 것 | ㈏ 누가 | ㈐ |
|---|---|---|---|
| **`R-1`** | ⓐ ⑵(`POST /searches`)를 **복원된 스택에서** 잰 회차 0건 — 최신값은 살아 있는 스택 측정(`〈207〉`) ⓑ ⑴ 을 **회차마다 다시 재는 기구가 게이트 밖**(사람이 `LAST-SUCCESS.txt` 를 본다) ⓒ `RUNBOOK §5` ⑤(권한·RLS 양성·음성)·③-보가 **손검사** — `verify-restored.sh` 스스로 「검증 미완 — 손검사 2건」을 찍는다 | 다음 복원 리허설 회차 | `R-1` 을 닫는다 |
| **`I4`** | 분산 추적 · 로그 · 알람 · 데이터 레지던시 기록 — **넷 다 실물 0건** ＋ 진입조건 `I3` 이 🟧 | 인프라 | `I4` |
| **`LV-2`** | 완료 정의 ⓐ 의 마운트 자동 조회가 `LineageStep.tsx:91` 에 **그대로 있다** · ⓑ~ⓔ 구현·시험 0건 | 프런트 | `LV-2` |
| **`R-1` ⑴·⑷ 의 자동 강제** | 두 행 다 **게이트가 0건**이다 — ⑴ 은 주 1회 감시, ⑷ 는 배포 경로 안에서만 강제된다 | 게이트 회차 | 회귀 감지 |
| **완주 계수의 자동 측정** | 셈은 `status`·`stage` 두 필드로 뽑히지만 **`gates/run.sh` 가 이 계수를 재지 않는다** | 게이트 회차 | `PA-G` 개시 판정 |
| **앞 회차 계수(19/18)와의 차** | 정의 차인지 시점 차인지 **이 회차가 재지 않았다** — 앞 값의 셈 근거가 남아 있지 않아 **`[미확인]`** 으로 둔다 | — | — |

---

## §8. 알려진 드리프트 — `03-HANDOFF §5.5` (소유 WU 가 정해진 미결 · 15건)

> ㈏ **전부 소유 WU 가 있다.** 여기서 새로 정할 것은 없고, **그 WU 가 열릴 때 함께 닫는다.**
> ㈑ 근거 = `03-HANDOFF §5.5`(행 번호 병기).

| ID | ㈎ 걸린 것 | ㈐ 소유 | 행 |
|---|---|---|---|
| **DR-9** ⭑ | HSR 위경도가 **합성값(dummy linspace)** 이던 PoC 관행 — 실좌표 있음이 밝혀졌으나 **v2 정책(자동추출 실패 시 `[미상]` · 지어내지 않음)의 명문화**가 남았다 | `D5` | `:480` |
| **DR-14** ⭑ | 「파일에서 자동 추출」에 **PoC 선례가 없다**(PoC 는 사람이 타이핑) — 전제 불성립 | `D5` | `:488` |
| **DR-2** | tif 가 입출력 겸용인데 **「이미 COG 인 tif」 판별 로직이 없다** → 계보 오염 | `D5` · 정본 · `S1` | `:467` |
| **DR-3** | 확장자와 실제 포맷이 다른 원천이 있다(`.nc` 가 실은 HDF5) — **매직바이트 감지** 필요 | `D5` | `:469` |
| **DR-4** | `deploy.sh` 주석은 「커밋 기준 빌드」인데 **실제는 워킹트리를 빌드**한다 | `I3` | `:471` |
| **DR-5** | `:i2` 고정 태그라 재빌드 시 **직전 이미지가 dangling 으로 사라진다** | `I3` | `:472` |
| **DR-6** | `deploy.sh` 가 **앱 5종 헬스 대기 없이 `exit 0`**(postgres 만 대기) — fail-open 배포 판정 | `I3` | `:474` |
| **DR-12** | 오버뷰 리샘플링이 `nearest` 단일 — 연속형은 `average/bilinear` 가 필요하다(줌아웃 시 값 왜곡) | `D5`·`D7` | `:484` |
| **DR-11** | PoC 는 파일 전체를 메모리에 올린다(50GB 미대응) — **계승 금지 명문화** | `D5` | `:483` |
| **DR-10** | PoC 디코딩 구현이 **2벌**인데 정본 미지정 | `C3`·`D5` | `:481` |
| **DR-15** | PoC 계보에 **「가공 방식」 필드가 없다**(`relation_type` 항상 `derived`) | `D5`·`P2` | `:489` |
| **DR-17** | 계약에 `gridAxis` 실을 자리가 없는데 `0004` 는 축 CHECK 를 요구한다 | `P2`(W0-1) | `:465` |
| **DR-16** | 이벤트 계약 산문이 「기준 격자 0~1건」인데 확정치는 **0~2건**(`〈58〉`) | 다음 계약 개정 창 | `:463` |
| **DR-18** | 계약 산문이 아직 없는 DB 열 이름(`grid_axis`)을 앞질러 명시한다 | DB 열 확정 뒤 | `:464` |
| **DR-1** | pipeline-worker README 가 「4포맷」인데 실제는 5포맷(GeoTIFF 누락) — **`§1 D-4` 와 같은 뿌리** | `D5` | `:466` |

---

## §9. 이 문서가 낡지 않게 하는 법 — **제안만 한다 (구현 0건)**

> Ted 지시 = 「레포의 기존 기제 안에 있으면 제안하라. **구현은 하지 말고 제안만.**」
> 아래 셋은 **새 기제를 만들지 않는다** — 이미 있는 것의 **범위를 넓히는 쪽**이다.

**㉮ `work-item-consistency` 에 `stage` 대조 검사를 더한다** — 가장 값이 크다.
`§1 D-0` 표가 보여 주듯 **어긋남 9묶음 중 넷(D-1·D-6·D-7 ＋ D-4 의 절반)이 `stage`·범위 낱말 축**이고, 그 축은 지금 **아무도 안 본다**.
`〈252〉` 가 **결정번호 중복 검사**를 같은 게이트에 더한 선례가 그대로 본이 된다. **대조 대상에 `CLAUDE.md` 를 넣는 것이 핵심**이다 — D-1·D-2·D-6·D-8 이 전부 거기 산다.

**㉯ 이 문서의 각 건에 「소유 항목 id」를 필수로 달고, 그 항목이 `done` 이 될 때 행이 남아 있으면 red 를 낸다.**
이 문서는 이미 ㈐ 칸에 항목 id 를 달고 있으므로 **형식은 갖춰져 있다.** 게이트가 그 짝을 재면 **「항목은 닫혔는데 체크리스트가 남는」 자리**가 사라진다.
이것이 없으면 이 문서 자체가 다음 드리프트 면이 된다 — `§1 D-0` 이 말하는 그 무늬다.

**㉰ `§2 #39` 를 먼저 푼다** — `work-item-consistency` 가 **CI 에 배선돼 있지 않다.**
㉮·㉯ 로 검사를 아무리 늘려도 **로컬에서만 도는 게이트는 병합을 막지 못한다.** 실제로 `§1` 의 어긋남이 **게이트 green 상태에서 쌓였다.**
⚠ 다만 `#39` 자체가 **사람 판정 대기**다(설계상 red 를 CI 에서 어떻게 다룰 것인가) — **그 판정이 이 세 제안의 실질적 선행**이다.

> ⭑ **⟨증보 2026-08-31 · 3레인 병합 회차⟩ 아래 둘은 이 회차의 오케스트레이션 사고 실측이다.** 위 셋과 같은 규율 — **제안만 한다. 구현 0건 · Ted 판정 사안.**

**㉱ 결정 번호를 레인에 **배정하고도**, 회차 도중 추가된 판정에는 번호를 안 줬다 — 그래서 중복이 났다.**
실측 = `lane-checklist`(`R-1` 4행 기준)와 `lane-v2def`(`V-1` 삭제 원칙)가 **둘 다 `〈255〉`** 를 집었다. 두 레인 모두 「Ted RULING ㉙」을 근거로 적었는데, **그 ㉙ 가 회차 도중 두 레인에 각각 구두로 중계되면서 번호가 함께 가지 않았다.** 착수 시 배정한 번호는 지켜졌고, **배정 밖에서 생긴 판정**이 구멍이었다. `〈252〉` 의 중복 검사는 이것을 **막지는 못했고 병합 시점에 잡아 줬다** — 설계대로다.
**재발 방지책(제안)** — **번호는 레인이 아니라 판정에 붙인다**: 회차 도중 나온 판정도 중계하는 그 자리에서 다음 빈 번호를 **먼저 발급하고**, 번호 없는 판정 문면은 레인에 내리지 않는다.

**㉲ 한 레인이 **미커밋 마이그레이션을 공유 적용 DB 에 적용**해, 형제 레인들의 `schema-diff` 를 red 로 만들었다.**
실측 = `lane-trigger` 의 `0009_preview_stale_event_types` 가 커밋되기 전에 **공유 일회용 적용 DB 에 먼저 올라갔다.** 적용 DB 는 체인마다 **하나**를 레인들이 함께 쓰므로, 그 순간 형제 레인의 선언(`db/platform/schema.sql`)과 적용이 갈렸다 — **형제 쪽 잘못이 아닌데 형제 쪽이 red 를 맞았다.** 게이트는 정확히 제 일을 했다(드리프트를 봤다).
**재발 방지책(제안)** — **적용 DB 를 레인마다 따로 짓거나**, 공유를 유지한다면 **`upgrade head` 는 병합 뒤 `main` 에서만** 돌린다 — 레인 안에서는 미커밋 리비전을 공유 DB 에 올리지 않는다.


---

**갱신 규약** — 이 문서의 건을 닫을 때는 **대장을 먼저 고치고** 여기서 지운다. 지울 때 **닫힌 근거(결정 번호 · 실측)를 `§7` 형식으로 남긴다** — 왜 빠졌는지가 추적되지 않으면 이 문서도 `§1` 의 무늬가 된다.
