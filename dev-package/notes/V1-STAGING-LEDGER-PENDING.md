# `§9` 등재 초안 — `V-1` staging 실물 증명 ⭑ **등재 완료 → `PLAN-SoT §9 〈322〉`** (2026-09-05)

> ⭑ **번호가 배정됐다 — 종전 머리글 ~~(번호 미배정)~~ 는 낡았고 지우지 않는다.**
> 등재 자리 = `dev-package/PLAN-SoT.md §9 〈322〉`(코드 집행 판은 `〈321〉`). 아래 `〈N〉` 자리표시는 그 번호로 읽는다.
> ⭑ **오케스트레이터가 이어서 할 일 ⑴~⑶ 은 이 회차에 집행됐다** — `V-1` `status` `open` → `done` · `03-HANDOFF` 갱신 완료.
> **⑷ 의 별건 둘** = ㉮ 회수 로그가 안 보이는 자리 → **`BF-12` 신설**(`〈324〉`) · ㉯ 구판 벌의 처분 → **`TL-2` 신설**(`〈323〉`).

> ⚠ **파일 이름이 지시와 다르다 — 감추지 않는다.** 지시는 `notes/V1-LEDGER-PENDING.md` 였으나 **그 이름은 이미 쓰이고 있다**
> (`V-1` **코드 집행** 등재 초안 · 워크트리 `lane-v1` 작성 · 아직 번호를 못 받았다). 덮으면 미등재 초안 하나가 사라진다.
> ⟹ **이 시트를 `V1-STAGING-LEDGER-PENDING.md` 로 뗐다.** 오케스트레이터가 병합 때 **두 초안을 함께** 본다
> (`V1-LEDGER-PENDING.md` = 코드 판 · 이 파일 = staging 실물 판 · `V1-FLAKE-LEDGER-PENDING.md` = `〈317〉` 판).

> ⛔ **번호를 예약하지 않는다.** `〈N〉` 은 병합 직전 `origin/main` 의 최대 번호 ＋1 로 오케스트레이터가 재실측해 박는다.
> ⛔ **이 레인은 `PLAN-SoT.md`·`work-items.yaml`·`03-HANDOFF.md` 를 만지지 않았다.** 아래는 초안이고 정본이 아니다.

---

| 번호 | 제목 | 본문 |
|---|---|---|
| 〈N〉 | **`V-1` 을 닫는다 — `§6(vii)㉡` 팔레트 재렌더를 staging 실물에서 쟀고 `〈317〉` 의 실물 판도 함께 섰다** | **집행 ＋ 실측 (2026-09-05 14:54~15:00 KST · 전문 `sessions/V-1-STAGING-20260905.md` · 배포 0 · 인가된 staging 쓰기 1건).** **㉮ 무엇이 비어 있었나** — `〈318〉`(창 5b 단계 B)이 `§6(vii)㉡` 을 **경계 때문에** 집행하지 않았다(그 레인의 인가된 쓰기가 `§6(iv)` 선기재 목록뿐이었다). 그래서 `V-1` 은 ⑹ 배포 green 만 서고 **실물 관측이 비었고**, `〈317〉` 의 staging 실물 증명도 함께 비어 있었다. Ted 가 ㉡ 1건에 명시 쓰기 승인을 냈고 이 회차가 **배포 없이** 그 자리만 쟀다. **㉯ 집행 — 쓰기 하나.** 공개 엣지 `POST /api/v1/previews` **1회** · 대상 **GK-2A `01M0YWDXN7CA13KB7MQC9PGEW5`**(정본이 데이터셋 이름을 대지 않아 지시가 정한 값) · `style {palette: 다색-무지개, classCount: 6}` — **화면이 만드는 요청과 같은 모양이고 바꾼 것은 팔레트 한 칸뿐**이다(`DatasetPreviewSection.tsx` 가 `{datasetId, palette, classCount}` 로 걸고 기본 구간 수가 6). `renderId 01M1R298204P32QTE2DM00ZS5R` · 202 → **완료**. **㉰ ⭑ `〈317〉` 이 staging 실물에서 섰다 — 계측이다(추정 아님).** 20 ms 폴링 ＋ 5 ms 자리 감시로 잰 시간축: `14:57:23.685` POST 202 → `23.755~24.168` **새 3키 7파일 생성** → `24.090` 폴링 = **`그리는 중`·`stage 범례 만드는 중`·옛 벌 7 생존** → `24.168~24.173` **옛 3키 7파일 소멸** → `24.306` 폴링 = **`완료`·옛 벌 생존 0**. ⟹ ⑴ **새 것이 선 뒤에 옛 것이 지워졌다**(마지막 신규 `24.168408` ＜ 첫 소멸 `24.173629` · ⑷-b) ⑵ **「완료」는 회수가 끝난 뒤에 발행됐다**(소멸 `24.173659` ＜ 완료 `24.306060`) ⟹ 시험 `test_화면이_완료를_본_그_순간_옛_벌은_이미_지워져_있다` 의 **실물 판**이다. ⑶ 회수 구간의 응답(`그리는 중` ＋ `stage` ＋ `result` 없음)은 **계약 안**이다(`〈317〉`-㉴ 와 같은 판정). **㉱ 완료 정의 ⑴ 이 문자 대조로 섰다.** 전(`단색-파랑`) ↔ 후(`다색-무지개`) — `classes[].min/max` **6쌍 전건 동일** · `bounds` **4값 전건 동일**(`west 126.676219 · south 36.077845 · east 127.956574 · north 37.3582`) · `variable` **`band1` 동일** · 갈린 것은 **`color` 6칸뿐**. 격자도 같다(지도형 **821 × 1024**). **㉲ 자리 계수 — 델타 0.** 자리 파일 **306 → 306**(새 벌 7 − 지운 벌 7) · 사이드카 **122 → 122** · GK-2A 사이드카 **10벌 무변**(새 3벌이 옛 3벌 자리를 정확히 대체) · **고아 0 · 중복 벌 0** · 사이드카 없는 산출물 **16 무변** · **`tile-` 키 2 → 2 무접촉**(⑷-c 음성 조건). 컨테이너 **8/8 healthy · `RestartCount` 0 유지**. **㉳ 로그 — 회수 성공은 로그를 남기지 않는 설계다.** `viz_render` 에는 접수·조회 3줄뿐이고 `jobs.py` 는 **실패했을 때만** `log.warning("팔레트 재렌더의 옛 벌 회수가 실패했다 …")` 을 찍는다. ⛔ **「로그에 회수 줄이 있다」로 적지 않는다 — 경고 0건이 「예외 없이 끝났다」이고 집행 증거는 자리 대조다.** **㉴ 판정 = `V-1` 완료 정의 ⑴~⑹ 전건 충족 ⟹ `status` `open` → `done`.** ⑴ 색만 바뀜(㉱) · ⑵ 목록 밖 거절 — **조항이 못 박은 합격선이 「음성 시험 1건」**이고 `test_palette_rerender.py:84 test_목록_밖의_팔레트는_거절한다`(`"없는-팔레트"` → 400)가 실재·green 이다 · ⑶ 화면 도달 산출물이 새 키(옛 키는 자리에 없다) · ⑷⑷-b⑷-c(㉰㉲) · ⑸ 타일 갈래 = **「무효화할 것이 없음이 유지된다」**(`layers null` · 서버에 구워 두는 조각 0벌) · ⑹ **배포 green** = `〈318〉` 의 `e37808cace69`(통과 15 · SKIP 0 · red 0). ⑺ 은 범위 선언이라 판정 대상이 아니고 위반 0. **㉵ `[미확인]` 둘 — 완료를 막지 않는다.** ⓐ **화면에 팔레트 갱신이 보이는가** — 팔레트 선택 UI 가 **없다**(`J-6` 미착수 · ⑺ 이 명시로 범위 밖으로 뺀 자리). 화면은 언제나 `listPalettes` 첫 항목으로 걸어 `다색-무지개` 를 띄울 방법이 없다. ⭑ **간접 근거는 섰다** — 재렌더 전 화면 DOM 범례 6줄이 렌더 응답 `classes` 6쌍과 **글자 대조로 일치**했다(화면은 받은 값을 그대로 그린다) · 변수명 `band1` 전후 동일. ⓑ **`TL-1` 관측 전용 루프의 첫 한 바퀴 = 「못 봤다」**(㉶). **㉶ ⛔ `TL-1` — 98분 경과인데도 로그 0건이다.** 기동 `13:22:00` · 확인 `15:00` = **경과 ≈98분** · 주기 기본 **3600초** ⟹ 한 바퀴가 돌았어야 하는데 `reclaim`·`회수`·`liveness` 문자열 **0건**이다. 등급별 계수 **「못 봤다」**(0 으로 적지 않는다). ⭑ **삭제는 자리 기준 0건** — 이 창이 인가받아 회수한 7파일 말고 사라진 파일 **0개** · `tile-` 무변 ⟹ `§6(vi)` no-go 미발동. **후보 둘 · 가르지 못했다 `[미확인]`** — ㉮ **로그 설정 귀책이 유력**(`services/viz-render` 전수에 `basicConfig`·`dictConfig`·`setLevel` **0건** ⟹ 루트 로거에 처리기가 없어 `trigger_loop.py:118` 의 `log.info` 가 어디에도 안 찍힌다) · ㉯ 루프 미기동(약한 후보 — `compose.i2.yml:335` 가 `COLAB_VIZ_TRIGGER_SPOOL` 을 주고 있어 `main.py` 의 기동 조건이 선다). **푸는 법** = 로그 준위·처리기를 세우는 코드 한 자리, 또는 회수 요약 준위를 올리는 판정. **⟹ `〈313〉` 의 「켜도 지금 지울 것이 없다」는 이 회차에서도 검증되지 않았다.** **㉷ 이 회차가 만진 것.** staging 쓰기 **1건**(`POST /api/v1/previews` · 승인됨) · **배포 0 · 원장 append 0 · 컨테이너 수명 조작 0 · 파괴 플래그 0 · DDL 0 · SQL 0**(DB 무개방) · **롤백 0 · 병합 0 · 코드 0줄 수정**. 자리에서 지워진 파일 = **인가된 재렌더가 회수한 7개뿐**. `work-items.yaml`·`PLAN-SoT.md`·`03-HANDOFF.md` **무접촉** · `main` **무접촉** · `a2_pg`·`ai_pg` **무접촉**. 토큰·접속 문자열 **미출력**(파일 경로로만 읽어 헤더·`localStorage` 로만 건넸고 확인 뒤 잔여 0). 게이트 `work-item-consistency` **green**. |

---

## 오케스트레이터가 이어서 할 일 (이 레인이 하지 않은 것)

- ⑴ `〈N〉` 번호 배정 — `origin/main` 최대 ＋1 로 **병합 직전 재실측**.
- ⑵ `work-items.yaml` `V-1` — `status: open` → **`done`** ＋ `evidence` 를 이 회차 값으로 갱신(현 `evidence` 는 「안 선 것 = staging 실물 증명 `[미확인]`」이라고 적고 있다).
- ⑶ `03-HANDOFF.md` — `V-1` 관련 열린 자리 정리(있다면).
- ⑷ **별건으로 남는 것 둘** — ㉮ `TL-1` 회수 로그가 `docker logs` 에 안 보이는 자리(관측 전용의 관측이 안 되면 관측 전용이 아니다) ㉯ `RC-1` 이 지는 GK-2A 구판 7벌(2026-09-02·09-03 생성 · 이 창의 회수 사정거리 밖).
