# QA 개선 전략 — 2026-09-03 (advisor · 읽기 전용 검증)

- 입력 = `notes/QA-SCREENS-20260903.md`(30행 · 범위 밖 3 별도) · 스크린샷 `qa-screens/01~12` · `main` HEAD `f843165` · 릴리스 `77813d391f94`
- 검증 방법 = 결함 30행 전건을 코드·계약·대장 `PLAN-SoT §9` 와 대조. staging 접촉 0 · 파일 수정 0(이 문서 1건 제외)
- ⚠ QA 표 계수 정정 — 표는 **30행**인데 §1 계수는 25. 유형별 실계수 = 결함 13 · 어긋남 10 · 미완 4 · 개선 3

---

## 1. 상위 5건 코드 대조

| # | 관찰 | 실물 위치 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | 연구실 정보 탭 백지 | `routes/LabSettingsPage.tsx:44` `<div data-panel="연구실 정보" data-fills-in="WU-G4" />` | **(a) 진짜 결함(미완)** | G4 는 「읽기 모달 → P7」만 배정(`work-items.yaml:438~`). 읽기는 `dashboard/LabInfoModal.tsx` 로 홈에 있음. **편집 화면은 어느 WU 도 안 맡음** — 모달의 `연구실 정보 편집` 버튼(`LabInfoModal.tsx:62`)이 `/lab-settings` 로 보내 백지에 닿는다. 계약 `updateLab`(`fe-core.yaml:175`) 은 서 있음 |
| 2·3 | 활용·접근 섹션 부재 · `#sec-usage` 죽은 앵커 | `routes/DatasetDetailPage.tsx:2` 「활용 프로젝트 섹션은 WU-P5 가 이어서 채운다」 · `LineageSection.tsx:279` `href="#sec-usage"` | **(a) 진짜 결함(미완)** | `WORK-UNITS §7` P5 행 「완료 후 E-03 섹션3 채움」. `〈230〉` 이 P5 를 「잔여 op·모달 0건」으로 닫으면서 **op 이 아닌 섹션3 이 계수에서 빠졌다**. ST-1 다운로드는 **카탈로그 행**(`CatalogTable.tsx:220` · 릴리스에 이미 포함)이고 상세는 다른 표면 — 배포 대기 아님 |
| 5 | 기본 정보 6칸 `—` | `detail/BasicInfoGrid.tsx` · 서버 `d3_catalog.py:165` | **(d) 데이터 상태** + 확인 1건 | 변수·좌표계·기간 = `〈280〉`-㉯ 실측 13행 중 1·1·0 — 사람 입력 칸이 `〈280〉` 에서 처음 열렸고 기존 13행은 그 전 등록. 포맷·격자 = 사건 경유 autometa(`〈190〉`) — 13행에 autometa 행이 있는지 **SELECT 1회 미실측**. 원천 표기 = 사람 값. 코드 결함 0 |
| 7·8 | Verified 취소선 | `CatalogTable.tsx:187` `verified--pending` · `catalog.css` | **(c) 정본 갱신 필요** | `〈282〉`-㉮ Ted 판정(2026-09-02) 축자. 정본 `Policy_프로젝트 §8` 이 뒤처짐. 단 #8 의 프로젝트 상세 표 `—`(`ProjectDatasetTable.tsx:57`)는 `〈282〉` 가 안 다룸 — 같은 규칙 확장은 권고로 가능 |
| 6 | 없는 id → 「지워졌어요」 | `detail/detailSource.ts:19` `404 → DatasetGone` · 서버 `catalog.py:635` 「둘을 같은 404 로 낸다」 | **(a) 진짜 결함(문구)** | 서버는 P-9·P-10 으로 묘비·경계 밖·미존재를 일부러 한 404 로 냄. 화면이 그 404 를 전부 묘비 문구로 번역 — 정본 §9 문구는 묘비 전용. 중립 문구 1줄로 고친다 |

추가 검증 — #25 업로드 모달 520px = **CSS 누수**: `members/members.css:105` `.modal{max-width:520px}` 가 전역이고 `upload.css` `.modal.modal-takeover{width:100%}` 를 덮음. 연구실 설정 방문 뒤 업로드를 열면 재현(스크린샷 08 배경이 S-07). #24 = React `onWheel` 은 passive 라 `useZoomPan.ts:135` `preventDefault` 가 무효 — 네이티브 리스너 `{passive:false}` 로. #10 = **검수 오류 부분**: 관련도 막대·원천·소유는 표시됨(스크린샷 09 · `SearchHitCard.tsx` relbar) — 미표시는 Verified·요약·기간 셋뿐. #11·#15 = `〈224〉` 가 이미 「Verified 우선 정렬 실행 경로 0건」으로 등재(`catalog.py:240` ts_rank_cd 단독) · 서버 `verified` 파라미터는 있음(`catalog.py:450`) → 토글은 화면만. #17 = **검수 오류**: `Policy_홈_대시보드 §5 126·169행` 「두 묶음 둘 다 보여주고 계보 상태별이 위」 — 탭 아님, 목업이 정책보다 낡음. #18 = 정본 내부 충돌: `Policy_데이터셋_상세 78행` 「기본 정보 → 계보 → 미리보기 → 활용」 vs `IA_사이트맵` 「미리보기 → 계보 → 활용」 — 구현은 Policy 를 따름.

## 2. 재분류 (30행)

| 분류 | 건수 | 항목 |
|---|---|---|
| 배포 대기(main 에 이미 있음) | **0** | `77813d3..HEAD` 프런트 변경은 `LineageStep.tsx`(〈288〉)뿐 — 30행 어느 것도 안 닿음 |
| 진짜 결함(코드 수정) | **17** | 1 · 2 · 3 · 6 · 8 · 9(고정 4목록) · 11 · 12 · 13 · 14 · 15 · 20 · 22 · 23 · 24 · 25 · 26 |
| 진짜 결함 · 판정 선행 | **2** | 4(파일 목록 — op 없음, 계약) · 19(시각화 컨트롤 — 기존 물음 `SC-5`·`V-1` 에 종속) |
| 정본 갱신 필요 | **4** | 7(〈282〉) · 16(검색 = 낱말 그대로 · `〈72〉`·`〈81〉`·`〈112〉` 판정 반영) · 18(정본 내부 충돌) · 30(엿보기·행 다운로드 등재) |
| 데이터 상태 | **3** | 5 · 21(summary 열에 처리 이력 전문이 들어 있음 — 시드 값) · 29(기간 값 null → 줄 생략) |
| 검수 오류 | **2** | 10(부분 — 3항만 실제 미표시) · 17 |
| 정본 침묵 · Ted 판정 | **2** | 27(404 문구) · 28(모바일) |
| 범위 밖 | 3 | QA 판정 그대로 |

## 3. 체계 원인과 구조적 처방

| 원인 | 실증 | 가장 싼 처방 |
|---|---|---|
| **A. 완료 판정 ④「목업 대비 화면 검증」에 오라클이 없다** — WU 가 op 계수로 닫힘 | P5 `〈230〉` 「잔여 op·모달 0건」→ 섹션3 누락 · G4 가 편집 화면 미배정 | `WORK-UNITS §7` ④ 를 「정본 §8 화면 동작 표의 행별 ✓ 표」 제출로 바꾼다. QA 가 만든 `spec-expectations.md` 가 그 양식이다 — 화면 하나 닫을 때 그 화면 행 전건 표기 |
| **B. 정본이 판정을 뒤따르지 않는다** | 〈282〉 취소선 · 검색 성격 · 섹션 순서 충돌 | `PLAN-SoT §9` 항목에 「정본 델타」열 1개 신설 — 화면 표기를 바꾼 판정은 정본 파일·행 번호를 적어야 닫힌다. 미반영 목록을 `notes/SPEC-DELTA-PENDING.md` 하나로 |
| **C. 프런트 동작 게이트 부재** | `gates/run.sh` 는 `frontend-typecheck` 만. `frontend/test` 25파일이 게이트 밖 | `run.sh` 에 `frontend-test`(`vitest run`) 1행 추가 — 비용 최소. CSS·휠·범례류는 jsdom 밖 → CT-1 ⓒ(Playwright) 물음과 합쳐 「화면 스모크 1본」으로 |
| **D. 전역 CSS 이름 충돌** | `members.css .modal` 이 업로드 모달을 520px 로 | 컴포넌트 css 접두 규칙 + 게이트 grep 1줄(`^\.(modal|btn|chip) \{` 금지) |
| **E. 빈 값 표기 규칙 분산** | `—` / 줄 생략 / 구분자 잔존 / 404 전부 묘비 | `detail/format.ts` 의 `orEmpty` 를 프로젝트·계보에도 쓰게 통일 · 404 문구 중립 |
| **F. staging 데이터 빈곤이 검수를 막는다** | 계정 1 · 연구실 1 · 잠김 0 · 사람값 13행 중 1 | §5 픽스처 — 단 **로컬 스택**에서 |

## 4. 전략 목록 (Ted 없이 되는 것부터)

| 순 | 무엇을 | 왜 지금 | 형태 | 선행 | 되돌리는 비용 | 판정 |
|---|---|---|---|---|---|---|
| 1 | 게이트 `frontend-test` 추가(`vitest run`) | 시험 25파일이 이미 있는데 안 돈다 | 대장만(게이트 1행) | 없음 | 행 삭제 | 불요 |
| 2 | 레인 **Q-A 상세·검색 소수리** — #20 범례 자릿수 · #22 라벨 · #23 구분자 · #24 휠 · #25 CSS 누수 · #13·#14 0건 문구 · #12 문체 · #29 `—` · #8 프로젝트 표 취소선 | 전건 1~10줄 수정, 정본 축자 있음 | 레인 1개 | 1 | git revert | 불요 (#8 은 advisor 권고: 〈282〉 규칙 확장) |
| 3 | 레인 **Q-B 연구실 정보 탭** — 읽기 본문 + 편집 폼(`updateLab`) | 홈 모달 편집 버튼이 백지로 보냄 · op 있음 | 레인 1개 | 없음 | revert | 불요(정본 `Policy_홈_대시보드 165행` 축자) |
| 4 | 레인 **Q-C 검색 미완** — Verified 우선 정렬(서버 정렬만) · 정렬 근거 줄 · `Verified만 보기` 토글(서버 파라미터 있음) · 카드 Verified·요약·기간 | `〈224〉` 가 열어 둔 정본 위반 | 레인 1개 | 1 | revert | 불요 |
| 5 | 정본 갱신 묶음 — 〈282〉 취소선 · 엿보기·행 다운로드 · 검색 「낱말 그대로」(stage 1) · 데이터 맵 두 묶음 | 판정은 났고 문서만 낡음 | 정본 갱신 | 없음 | 문서 | 불요 (#18 순서 충돌만 Ted) |
| 6 | 레인 **Q-D 상세 섹션3** — 활용 프로젝트 카드 · 다운로드 버튼(기존 `/download`) · 잠김 시 접근 요청(`LockedNotice` 재사용) · `#sec-usage` 앵커 · #6 404 중립 문구 · #9 주제 고정 4목록 | 「사용 불가」 4건 중 3건이 여기 | 레인 1개 | 2 | revert | 데이터셋→프로젝트 목록 op 유무 확인 후: 있으면 불요, 없으면 Ted(계약) |
| 7 | 데이터 정비 — 13행 변수·좌표계·기간·원천 · summary 한 줄화 · autometa 유무 SELECT 1회 | 상세 6칸 `—` 는 코드가 아니라 값 | 창(staging 쓰기) | 없음 | 백업본 복원 | **Ted**(staging 쓰기) |
| 8 | Ted 판정 묶음(1회) — #27 404 문구 · #26 GNB `▾` 둘 처분 · #18 섹션 순서 · #16 「AI 검색」 표기 시점 · #4 파일 목록 op · #28 모바일 후일 | 정본 침묵·충돌 6건, 한 번에 | 판정 | 없음 | — | **Ted** |
| 9 | `WORK-UNITS §7` ④ 오라클 개정 + `PLAN-SoT §9` 정본 델타 열 | 원인 A·B 재발 방지 | 대장만 | 없음 | 문서 | advisor 권고 가능 |
| 10 | QA 픽스처(§5) | 미도달 4영역 | 창 또는 로컬 | 9 | — | 로컬이면 불요 |

충돌 — 2·6 은 `catalog/*`·`catalog.test.tsx` 를 건드림 → **CT-1 종결 뒤** 착수. 4·6 산출은 배포 후보 → 창 E→G→F 에 끼우지 말고 **F 다음 창**에 묶음. V-2(값 조회)와 파일 충돌 0.

## 5. 미도달 영역의 도달 방법

| 영역 | 방법 | 비용 | 판정 |
|---|---|---|---|
| 잠긴 데이터셋 · 접근 요청 · 거절 | 접근 정책 1행 + 대기 요청 1건 시드 | SQL 2~3행 | staging 이면 Ted · 로컬이면 불요 |
| 역할별 홈 4종 · 권한 숨김 | 연구실 2 · 계정 4(교수·기본·위임·빈 연구실) — `subjects` 1행에 추가 | 시드 스크립트 1개 · PA 비밀번호 발급 | 동상 |
| 업로드 3단계 · AI 제안 · 등록 게이트 · S-08 | 쓰기라 staging 에선 영구 불가 | — | **로컬 스택**(compose + 시드)에서 Playwright 스크린샷 12장 자동 채취 → 게이트 산출물로 |
| 권고 | 검수는 **로컬 픽스처 스택**에서, staging 은 배포 확인만 | 초기 1레인(스크립트·시드) | 불요 |

## 6. 깨끗한 것

- #20·#22·#23·#24·#25·#13·#14·#12·#26(핸들러 없는 버튼) — 지적이 맞고 뉘앙스 없음. 그대로 고친다
- 프로젝트 목록·상세 「통과」 판정, 할 일 함 3그룹 일치 — 코드와 어긋남 0
