# `PLAN-SoT §9` 등재문 초안 — 단독 창 5b 단계 B ⟹ **등재 완료 → `§9 〈318〉`**

> ✅ **⟨등재 완료 2026-09-05⟩ 번호가 박혔다 — `PLAN-SoT §9 〈318〉`.** 병합 직전 `origin/main`(`48744f2`)의
> `§9` 최대 번호가 **`〈317〉`** 임을 grep 으로 재실측해 **＋1 = 〈318〉** 로 발급했다. 등재 행은 `〈317〉` 바로 뒤에
> 한 줄 3칸 표 행으로 변환해 붙였고, **원값·시각·`[미확인]` 항목·「재굽기로는 구판이 안 덮인다」 판정은 그대로 옮겼다.**
> ⛔ **이 초안은 지우지 않는다** — 아래가 변환 전 원문이고, 전문 근거는 `sessions/WINDOW-20260905-A5b.md §10` 이다.
>
> ／ 이전 표기 ~~⛔ **번호 `〈N〉` 은 비워 둔다.** 예약하지 않고 **병합 직전 `origin/main` 의 최대 번호 ＋1 로 재실측**해 박는다.
> 이 파일은 레인 산출물이고, 등재는 오케스트레이터 자리다. **이 레인은 `PLAN-SoT.md` 를 만지지 않았다.**~~

---

〈N〉 **staging 단독 창 5b 단계 B — 배포 `e37808cace69` GREEN · 첫 접촉 4건 채집 · 구판 재굽기 3건 · 「재굽기로는 구판이 안 덮인다」 실측** (2026-09-05 13:19~13:52 KST · `sessions/WINDOW-20260905-A5b.md §10` · 대상 목록 `sessions/WINDOW-20260905-REBAKE-TARGETS.md` · 원값 `sessions/WINDOW-20260905-FIRSTTOUCH.tsv`)

**⑴ 배포 — GREEN.** Ted go/no-go ① GO(후보 `e37808c`)를 받아 원장 승인 행을 손으로 append(`13:20:36+0900`)한 뒤 `deploy.sh --target staging`(다른 플래그 0)을 1회 돌렸다. 착수 `13:21:32` → 판정 `13:22:17` = **45초**. 배포 판정 **GREEN(통과 15 · SKIP 0)** · 체인 판정 GREEN(2 · SKIP 0) · 별칭 `:i2` **6/6** 이미지 ID 대조 · digest 이력 6종 append · **red 0 · SKIP 0 · 롤백 미발동**. 두 체인은 `A5b §1.1` 예측대로 **움직이지 않았다**(platform `0011` · ai `0005`). ⚠ 45초는 **착수~판정 구간이고 5xx 지속 시간이 아니다** — 재는 계측이 이 경로에 없다(`〈312〉` 와 같은 `[미확인]`).

**⑵ 첫 접촉 채집 — `〈304〉` 후보 ⓓ 가 두 회차 연속 재현 실패.** 8 healthy 확인(`13:22:12`) **23 ms 뒤** 값 조회 4건을 데이터셋당 1회씩 쐈다(`deploy.sh` 종료를 기다리지 않았다). 관측 최대 **410 ms**(GK-2A) — `〈304〉` 의 1,164 / 2,111 ms 스파이크는 **이번에도 나오지 않았다.** `〈312〉`(327 ms)에 이어 **두 번째 표본**이고, ⛔ **후보 ⓓ(재기동 첫 접촉)로는 그 스파이크가 설명되지 않는다**가 두 회차로 굳었다. **원인은 `[미확인]` 그대로**다. `Server-Timing` 은 4건 전부 공개 엣지를 통과했다. ⭑ 새로 드러난 것 — 첫 접촉의 서버 안 비용이 `〈312〉` 의 **2배 이상**이다(GK-2A `vizReadPoint` 79.1 → **202.4 ms** · `coreRelay` 97.8 → **218.5 ms**). 그 차이가 어디서 왔는지는 이 창이 가르지 않았다 — **`[미확인]`**.

**⑶ 게이트 전수 50/0/0.** `gates/run.sh all -j 1` 선언 50 · **green 50 · red(판정) 0 · red(준비) 0** · 재실행 0회(red 가 없어 조건이 안 섰다). `service-tests-viz-render` 는 `A5b §2.1` 의 단독 3회에 1회를 더해 **합 9회 연속 green**. ⛔ **「간헐이 사라졌다」로 적지 않는다 — 「9회 표본에서 안 났다」가 실측이다.**

**⑷ ⛔ `〈315〉`-㉯ 의 판정이 반증됐다 — 재굽기는 구판을 덮지 못한다.** `artifact-ownership` 을 재굽기 전후로 돌렸다. 전 = 대상 **127** · 살아 있다 **108** · 접수분 0 · 고아 0 · **판정 불가 19** · 자리 파일 285. 후 = 대상 **136** · 살아 있다 **117** · 접수분 0 · 고아 0 · **판정 불가 19** · 자리 파일 **306**. **판정 불가가 한 벌도 내려가지 않았다.** 스냅숏 대조 실측 — 전후의 판정 불가 19개 캐시 키가 **집합으로 동일**하고(사라진 키 0 · 새 키 0), **사라진 벌 0개 · 새 벌 9개**다. 원인은 실측으로 잡힌다 — **재굽기가 새 캐시 키에 판 2 벌을 세울 뿐 옛 키의 구판 벌을 건드리지 않는다.** ⟹ `〈315〉`-㉯ 의 「`tolerate` 기한 연장이 아니라 재굽고 재계수한다」와 게이트 선언(`artifact-ownership.toml [legacy] reason`)의 「캐시 키가 안 바뀌었으므로 다음 재굽기에 그대로 덮인다」가 **둘 다 이 실측과 어긋난다.** ⛔ **구판을 없애려면 재굽기가 아니라 회수(`invalidation`)여야 한다 — 그 판정은 Ted 자리로 연다.**

**⑸ 재굽기 3건 — 대상은 창에서 실측해 먼저 적고 커밋한 뒤 집행했다.** `〈312〉` 의 「구판 19벌」은 자리 파일 236 시점 계수라 쓰지 않았다. 스냅숏 285줄에서 판정 불가 **19벌**을 뽑아 **㉮ 사이드카 판 없음 5 / ㉯ 사이드카 부재 14** 로 가르고, ㉮ 5건의 `source` ULID 를 platform DB 에 **읽기 전용**(`BEGIN READ ONLY`)으로 대조해 **데이터셋 3건**을 얻었다. 목록을 파일로 고정·커밋(`aa04792`)한 뒤 `POST /api/v1/previews` 3건(팔레트 `단색-파랑`)을 집행했고 **3건 전부 `완료` · 실패 0 · 격자 거절 0** 이다. ⭑ **HSR Binary 1건이 실패하지 않았다** — 짝 격자가 동봉돼 `동봉 격자 적용` 으로 섰다(`core-viz.yaml` 의 `DR-9` 실패 갈래에 들어가지 않았다). ⛔ **19벌 중 3벌만 사정거리 안이었다** — ㉯ 14벌은 **사이드카가 없어 스냅숏의 `source` 칸이 비어 있고**(A5b §6(iv) 가 「스냅숏 쪽에서 읽어야 한다」고 적은 경로가 실측에서 서지 않았다), ㉮ 중 2건은 **원천이 `d3_file`·`d5_upload_file` 어디에도 없다**. 남은 **16벌은 대상이 서지 않는다** — `[미확인]`.

**⑹ TL-1 관측 전용 — 삭제 0건이되, 루프는 「못 봤다」.** 도는 컨테이너의 `COLAB_VIZ_TILE_RECLAIM_*` 3키는 **전부 빈 값**(길이 0)이라 `config.py` 기준 **꺼짐**이다. 회수 바퀴 기록은 로그에 **0건** — 기동 `13:22:00`, 최종 확인 `13:52:04`, 경과 **30분**, 주기 **3600초**라 **한 바퀴가 아직 안 돌았다.** ⛔ **「0건 셌다」로 적지 않는다 — 「못 봤다」다.** 다만 **자리 기준 삭제는 0건이 실측**됐다(스냅숏 대조 사라진 벌 0 · 자리 파일 285 → 306 으로 증가만). §6(vi) 의 no-go 조건은 걸리지 않았다. `〈313〉` 의 「켜도 지금 지울 것이 없다」는 **검증되지 않았다** — `[미확인]`.

**⑺ 현물 — `〈314〉` 가 고친 자리가 화면에 섰다.** 재굽기 **뒤에** 열었고 **온디맨드 굽기 0벌**(자리 파일 306 불변 · 렌더 요청 0건)이다 — `〈312〉` §3.1 의 순서 오류가 재발하지 않았다. ⭑ **`〈312〉` 에서 자리째 빠졌던 GK-2A 의 격자 캡션이 선다**(`821 × 1024` · 산출 격자 · `〈315〉`-㉮ 가 정한 형태). 3 데이터셋 전부 이름·파일명·격자 3요소 충족. 값 조회 3건 `200 ＋ available:true ＋ 값` · DEM 대조군 `false` · 검색 `verified` 걸름 동작(0행 ↔ 대조군 3행) · `/lab` 200 · 없는 자원 **404**(중립 문면). 앱 귀책 콘솔 오류 0건. ⚠ **U-Net 범례 변수명은 여전히 원천 ULID** — `〈312〉` 와 같은 상태이고 이 배포가 고친 자리가 아니다.

**⑻ 완료 판정 — `BF-4` 는 닫을 수 있고 `V-1` 은 아직이다.** `BF-4` 의 마지막 조건(⑶ 화면 실측)이 **GK-2A 포함 3 데이터셋에서 섰다** ⟹ `in_progress` → `done` 의 근거가 실측으로 확보됐다. `V-1` 은 ⑹ staging 배포 green 이 섰으나 ⛔ **§6(vii)㉡(팔레트 재렌더 실물 관측)이 집행되지 않아 닫지 않는다.** 그 미집행은 판단이 아니라 **경계**다 — 이 레인의 인가된 쓰기는 §6(iv) 의 선기재 목록에 대한 `POST /api/v1/previews` 하나뿐이었고, ㉡ 은 **그 목록 밖의 추가 쓰기**라 **좁은 쪽을 따라 집행하지 않고 보고했다.** ⟹ **`〈317〉` 의 staging 실물 증명도 함께 비어 있다**(시험은 green · 실물 판은 `[미확인]`). **㉡ 에 대한 명시 승인 1건이면 다음 창이 배포 없이 잰다** — ㉡ 은 배포당 한 번뿐인 창이 아니다.

**⑼ 이 회차가 만진 것.** staging 쓰기 **둘**(`deploy.sh` 1회 · `POST /api/v1/previews` 3건 · 둘 다 승인됨) · 원장 append 2행(`approve`·`deploy`). **컨테이너 수명 조작 0**(`deploy.sh` 안의 교체 제외) · **파괴 플래그 0** · **DDL 0** · **SQL 쓰기 0**(platform DB 는 읽기 전용 조회만) · **삭제 0** · **롤백 0** · **코드 0줄 수정** · `work-items.yaml`·`PLAN-SoT.md`·`03-HANDOFF.md` **무접촉**. 자격증명·토큰 **미출력**.
