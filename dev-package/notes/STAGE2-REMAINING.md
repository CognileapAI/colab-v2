# stage 2 잔여 — 대장 기준 착수 가능성 실측

**작성 2026-09-02 · 기준 `main` `740b8c6` · 오라클 = `dev-package/work-items.yaml` ＋ `dev-package/PLAN-SoT.md §9`(〈271〉까지).**
산문(`03-HANDOFF.md`·`WORK-UNITS.md`)과 갈리면 대장이 이긴다. 갈린 자리는 §3 에 전건 적는다.
세는 기준 = `stage: stage2` 인 대장 항목만. `after_stage2`·`out_of_scope`·`stage1` 은 불산입.

---

## 1. 착수 가능 (여섯 관문 전건 통과)

| 순 | 항목 | 실의존 계수 | 한 줄 근거 |
|---|---|---|---|
| 1 | **A-1** (산출물 소유 구조) | 1/1 done | `work-items.yaml:626` `depends_on: [PV-1]` · `PV-1` = `done`(`:작업항목 PV-1`, `stage2`). 진입조건 둘째 줄이 걸던 블로커 `#56` 은 **소멸**(`03-HANDOFF.md:409` 행 #56 · `PLAN-SoT.md:621` 〈270〉-㉰). 완료 정의 8항 확정(`work-items.yaml:627-657` · 등재 〈263〉). **후행 3건(`V-1`·`V-2`·`J-1`)을 푸는 유일한 자리** — 셋의 `depends_on` 에 `A-1` 이 박혀 있다(`:799`·`:851`·`:934`) |
| 2 | **S3** (실데이터 5종 E2E) | 2/2 done | `work-items.yaml:1242` `depends_on: [S2, P3]` — `S2` done(stage1) · `P3` done(stage2). 완료 정의 확정(`:1243` · 〈248〉). 넷 중 셋 실측 충족(`:1244` · 〈267〉), 남은 것 = 「시각화 화면에 그려진다」 포맷별 실측 1건(`:1246`) |
| 3 | **CT-1** (카탈로그 표 스펙 세목) | 1/1 done | `work-items.yaml:1019` `depends_on: [P1]` — 〈244〉 가 `[P1, P6]` → `[P1]` 로 줄였다(`:1023`). `P1` done. 완료 정의 = 정본 축자 이관, 확정본(`:1020`). 기준선 실측도 이미 있다(`:1021`) |
| 4 | **R-1** (복원 절차) | 0 | `work-items.yaml:771` `depends_on: []`. `status: partial`. 완료 정의 12행 ＋ 미확정 4행의 통과 기준이 〈255〉 로 확정(`:773`). 남은 것 = 크론 무인 회차 **연속 3회 GREEN** 등 측정 중심 |
| 5 | **IS4** (terraform state 보관) | 1/1 done | `work-items.yaml:310` `depends_on: [IS2]` — `IS2` done. 완료 정의 확정(`:311`). 미달 2건 = 마지막 `apply` 미실행 · 맨몸 호스트 조건 미실험(`:314`). ⚠ **`R-1` 이 닫혀도 이 둘은 남는다**(〈249〉 실측 · `:314`) — R-1 대기 사유가 아니다 |

**착수 시 충돌 경고**
- **A-1** — 갈래 B 가 쓸 선례가 `gates/tools/preview-tile-slot.sh`(`work-items.yaml:659` note)인데, **`lane-57` 이 바로 그 게이트를 고친다**(`03-HANDOFF.md` #57 · 〈271〉-㉮ = ⓒ 둘 다). 같은 파일을 두 레인이 만진다.
- **S3** — 남은 조건이 포맷 판정이고, **`#58`(파서 처리 4→6 확장 · 〈271〉-㉯)이 같은 선언(`SUPPORTED_FORMATS`·`RENDERABLE_FORMATS`)을 만진다.** `#58` 집행 회차와 겹치면 `e2e-format-coverage` 정본이 흔들린다.

---

## 2. 착수 불가 (실격 사유 명시)

| 항목 | stage | 실격 사유 |
|---|---|---|
| **T-1** | **`after_stage2`** | **stage 2 항목이 아니다** — `work-items.yaml:783` T-1 `stage: after_stage2`(2026-08-30 이동 · 〈268〉 이 산문까지 정정). ⓐ 는 〈182〉 로 `deferred`. 종전 조사가 「미차단 병목」으로 지목한 자리이고 **틀렸다** |
| **P7** | stage2 | 의존(`P6`)은 done 이라 형식상 열려 있으나 **`lane-p7` 이 이미 착수 중**(브랜치 실재). 중복 착수 금지 |
| **P8** | stage2 | `depends_on: [P7]`(`:1110`) · `P7` = `open`. 의존 미충족 |
| **I3** | stage2 | `partial` · 열림/부분 6행 중 ⑷ 롤백 왕복 · ⑸ 직전 릴리스 확인이 **열린 블로커 `#43`(스크립트 롤백 경로 없음)** 에 걸린다(`work-items.yaml:355` note ＋ `03-HANDOFF.md` #43). 완주 불가 |
| **I4** | stage2 | `depends_on: [I3]` · `I3` = `partial`. 더해 남은 조건 넷(분산 추적·로그·알람·레지던시) **레포 실물 0건**(〈249〉-㉮ 전수 grep · `:359` note) |
| **V-1** | stage2 | **완료 정의가 초안이다** — `completion_def: 완료 정의 미작성`(`:800`), 초안은 별도 필드 `completion_def_draft`(`:801`). 블로커 `#54` 대상. 더해 `depends_on: [P3, A-1]` 중 `A-1` = `open` |
| **V-2** | stage2 | 완료 정의는 **확정본이다**(`:852` · 〈254〉 · `#54` 표기가 낡았다) — 그러나 `depends_on: [P3, A-1]`(`:851`) 중 **`A-1` = `open`**. 의존 미충족 |
| **J-1** | stage2 | **완료 정의 초안**(`completion_def_draft` `:935` · `[미확인]` 5자리) · `#54` 대상. 더해 `depends_on: [A-1]`(`:934`) 미충족 ＋ 진입조건 = 「stage 2 마지막」(`:933` · 〈157〉-㉱) |
| **PA-G** | stage2 | 진입조건 = 「대장에서 `stage1`·`stage2` 미완 0건」(`:1213` · 〈258〉 · `#52` 로 확정). 지금 미완 13건 |
| **2단-격자전용-실패3건-처분** | stage2 | `status: deferred` |
| **LV-1 · LV-2** | **`stage1`** | stage 2 항목이 아니다(둘 다 `stage: stage1`). 단 `PA-G` 개시 계수에는 산입된다 |
| **P4 · K3 · K4 · G1b · R2 · C1~C4 · DP-1 · LV-3 · LV-4** | `after_stage2` | stage 2 범위 밖 |

---

## 3. 산문 드리프트 — 문서 레인 수정 목록

1. `03-HANDOFF.md:117` — 「다음 WU → `A-1` … **착수 가능이 아니다.** 진입조건 = 블로커 `#56` 판정」. **낡았다.** 같은 문서 `§4` #56 행이 「소멸(2026-09-02 · 〈270〉-㉰)」이라 **자기 문서 안에서 모순**이다. 대장 `A-1` note 도 「그 멈춤이 풀렸다」로 적는다. → 「착수 가능(갈래 B)」로 고칠 것.
2. `03-HANDOFF.md §4 #54` — 「`V-1`·`V-2`·`J-1` … **대장의 `completion_def` 는 셋 다 `완료 정의 미작성` 그대로**」. **틀렸다** — `V-2` 는 `work-items.yaml:852` 에서 〈254〉 확정본이다. `#54` 의 대상은 **`V-1`·`J-1` 둘**로 줄여 적을 것.
3. `03-HANDOFF.md §1 T-P` CT-1 행 — 진입조건을 「P1(✅) · **P6**」로 적는다. 대장은 〈244〉 로 `depends_on: [P1]` 이다(`work-items.yaml:1019`·`:1023`). `P6` 를 지울 것(P6 가 done 이라 결과는 같으나 근거가 갈린다).
4. `03-HANDOFF.md §1 T-X` `R-1` 행 — 상태 칸 `🟧` 와 본문 `⛔` 글리프 병기, 그리고 `WORK-UNITS §10.2`(⛔) ↔ `§10.3`(진행 표기)이 서로 다르다(대장 `work-items.yaml:777` note 가 이 갈라짐을 기록). 대장 = `partial` 로 세 자리를 통일할 것.
5. `WORK-UNITS.md:515-517` — 착수 순서표 「3단 `T-1`」이 남아 있다. `T-1` 은 `after_stage2`(같은 문서 `:400`·`:669` 는 이미 정정) — **같은 문서 안에서 두 값이 공존**한다. 순서표에서 내릴 것.
6. `WORK-UNITS.md:438` — 「위 표의 일곱(`R-1`·`T-1`·`V-1`·`V-2`·`F-1`·`J-1`·`X-5`)은 그대로 유효」. `T-1` stage 이동 · `F-1`·`X-5` = `done` 반영 안 됨. 목록을 대장 상태로 다시 뽑을 것.
7. `WORK-UNITS.md §10` 착수-후보 표 — `R0`·`G1`·`generated-up-to-date` 가 **취소선 행으로 잔존**(대장 `:26`·note 다수). 닫힌 항목은 후보 표에서 내릴 것.
8. `03-HANDOFF.md §4 #57`·`#58` — 「판정됨 · 미집행」 표기는 정확하나, **어느 WU 를 막는지 적혀 있지 않다.** `#57` → `A-1` 갈래 B(같은 게이트 파일) · `#58` → `S3` 잔여 1행. 막는 대상을 표에 적을 것.
9. `work-items.yaml` 자체의 `#54` 잔재 — `V-2` `completion_def_draft`(`:877`) 가 「판정 대기 = `03-HANDOFF §4 #54`」로 끝난다. 확정됐으므로 그 줄에 해소 표시를 얹을 것(원문 삭제 금지).

---

## 4. stage 2 잔여 계수

- **stage 2 항목 총 33건** — `done 19` · `partial 3`(`I3`·`IS4`·`R-1`) · `open 10` · `deferred 1`(`2단-격자전용-실패3건-처분`).
- **미완 = 14건**(`done` 아닌 것 전부). `deferred` 제외 시 **13건** — 이것이 `PA-G` 개시 셈법(〈258〉 규칙 ①②④)의 stage 2 몫이다.
- `PA-G` 자신을 빼면 stage 2 미완 **12건**. ⚠ **stage 1 에도 미완 2건**(`LV-1`·`LV-2` · 둘 다 `open`)과 `SC-5`(`partial`)가 있어 `PA-G` 개시 계수는 stage 2 만으로 닫히지 않는다. 대장 전수 재계수는 `〈258〉` 회차 값(`17`/`16`)과 갈릴 수 있고 그 차이는 `[미확인]` 로 남아 있다(`work-items.yaml:1220`).
