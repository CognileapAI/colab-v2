# P8 등재문 **초안** — 번호 미발급

> 이 레인은 `PLAN-SoT.md §9` 에 쓰지 않고 번호를 발급하지 않는다(최신 `〈278〉`). **번호와 등재는 오케스트레이터가 직렬로 한다.**
> 회차 = 2026-09-02 · 워크트리 `.claude/worktrees/lane-p8` · 브랜치 `lane-p8` (off `main` @ `ac3c8fc`) · 산출 `dev-package/sessions/P8-E01-APPLY-POINTS-DRAFT.md`

---

## 등재문 초안 (번호 `〈 〉`)

| 〈 〉 | **`P8`(E-01 화면별 적용 지점 표) 값을 실물에서 재고 초안으로 세웠다 — 정본은 고치지 않았다. 판정 2건을 올린다** | **실측 ＋ 초안 (2026-09-02 · Claude · 워크트리 `lane-p8`).** **㉮ 진입 재대조** — `P8` = `open` · `stage2` · `depends_on: [P7]` · `P7` = `done`(`〈273〉`) · `completion_def` = 「각 P 의 완료 판정」 4항(`WORK-UNITS.md:245-250`) **확정본**(`completion_def_draft` 없음) · `§9` 에 `P8` 을 이연·재범위·재배정한 결정 **0건**. 실격 사유 **0건**. **㉯ 실측 = 축 A 적용 지점 8곳**(`PermissionGate` 전수) **＋ 서버 재판정 10자리 ＋ 할 일 함 3그룹.** 정본 `가. 원칙`(§2·§4·§6·§6.1)·`PERMISSION-PRINCIPLES.md` 와 **불일치 0건**, `[미확인]` **1건**(검색 결과 S-06 — 화면이 `P4`=`after_stage2` 라 실물 없음). **㉰ 정본을 고치지 않았다 — 파일 수정 0건.** 선례 `〈232〉`·`〈233〉`·`〈245〉`·`〈254〉` 는 기획 정본 개정을 전부 「Ted 판정 ＋ 집행 ＋ 번호」로 처리했고, 정본이 예고한 것은 **표가 생긴다는 사실**이지 **표에 무엇이 적히는가**가 아니다. **㉱ 재발 방지 시험 신설** = `frontend/test/e01-apply-points.test.ts` — 초안 §1.1 과 `PermissionGate` 실물을 집합으로 대조. red 실증 2방향(행 삭제 · 한 행이 스위치 둘). **㉲ `P8` 은 열린 채로 둔다** — `status` 무수정 · `completion_def` 무수정 |

---

## 판정 대기 2건 (Ted)

### ⑴ 기획 정본 개정 승인 — `Policy_역할과_권한` v1.3 → v1.4

- **배경** — 정본 `# 나. 화면별 적용 지점 표` 절이 비어 있고 축자 「⏸ E-07 완료 후 작성한다」로 자기 시점을 예고했다. E-07 = `P7` = `done`.
- **현재 상태** — 값은 재어 초안으로 섰다(불일치 0 · `[미확인]` 1). 정본 파일 수정 **0건**.
- **선택지** — ⓐ 초안대로 정본에 싣고 v1.4 로 올린다 ⓑ 값을 고쳐 싣는다 ⓒ 싣지 않는다.
- **고르면 달라지는 것** — ⓐⓑ 를 고르면 **패키지 `package/역할과_권한_260817.html` 재생성이 같은 회차에 붙는다.** md 본문이 `<script type="text/markdown" id="md-policy">` 로 패키지 안에 박혀 있어 md 만 고치면 `planning-freshness` 가 red 다(현재 15/15 MATCH · E-01 sha8 `533c6d6a`). `dev-package/PACKAGE-FRESHNESS.md` 스냅샷의 E-01 행도 함께 갱신한다.
- **권고** — ⓐ. **되돌리는 비용** = 정본이 git 밖이라 편집 전에 사본을 뜬다(선례 `_backup_260830_ZOOM/` 류). 사본이 있으면 되돌림은 파일 2개 복사다.

### ⑵ `P8` 완료 정의의 부적합 — 4항 중 ③④ 에 대상이 없다

- **배경** — `P8` 의 완료 정의는 「각 P 의 완료 판정」 4항이다: ① 스토리 수용 기준 ② 도메인 게이트 green ③ **staging 배포 green** ④ **목업 대비 화면 검증**.
- **현재 상태** — E-01 은 「전용 화면 없음 · 목업 없음 · 패키지는 문서 전용」(`README_E-01` 머리말)이라 **④ 는 대상이 0건**이고, `P8` 산출이 문서·패키지뿐이라 **③ 은 판정할 배포물이 없다**.
- ⚠ **이것은 블로커 `#54` 가 아니다.** `#54` 는 「완료 정의가 초안 상태」이고 `P8` 의 것은 **확정본이되 문서 전용 WU 에 안 맞는** 것이다 — 다른 유형이다.
- **선택지** — ⓐ ③④ 를 `P8` 에 한해 면제하고 완료 정의를 ①②＋「정본·패키지 해시 일치(`planning-freshness` green)」로 재적는다 ⓑ 4항을 그대로 두고 ③ 을 다음 배포 회차에 묶는다 ⓒ 무판정 유지.
- **고르면 달라지는 것** — ⓐ 면 `P8` 은 정본 개정 회차에서 바로 닫힌다. ⓑ 면 `P8` 은 배포 회차까지 열려 있다.
- **권고** — ⓐ. 근거 = `③` 의 취지(「로컬 green 은 완료가 아니다」)가 배포물이 없는 WU 에서는 오라클이 아니라 통과 불가 조건이 된다. **되돌리는 비용** = 대장 한 줄.

---

## 이번 회차가 재지 **않은** 것 (다음 회차 진입조건)

- 검색 결과 S-06 의 축 A 적용 지점 — `P4` 착수 회차가 실측한다.
- 축 B(데이터 잠김)의 화면별 노출 범위 — 값의 정본은 E-06 적용 지점 표다. 이 회차는 손대지 않았다.
- staging 배포 green — 이 레인은 배포 금지.
