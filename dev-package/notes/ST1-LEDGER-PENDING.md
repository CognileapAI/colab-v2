# ST-1 — 대장 반영 대기분 (레인 `lane-st1` · 2026-09-02)

이 레인은 **결정 번호를 발급하지 않는다.** 아래 등재문은 오케스트레이터가 번호를 붙여
`PLAN-SoT.md §9` 에 싣는다(다음 번호 후보 = 〈279〉 · 최신은 〈278〉).

전제 = **Ted 판정 2026-09-02 「파일 저장처는 지금 볼륨을 그대로 쓴다」** ＋ 그 판정에 붙은 조건
「내려받기 경로를 **이음매 하나** 뒤에 두어 나중에 저장처를 갈아 끼울 수 있게 한다」.

---

## 1. `ST-1` 완료 정의 — 이 회차가 처음 기재한다

종전 값 = `완료 정의 미작성`(저장처 형태 미판정 · `〈278〉`-㉯). **그 사유가 판정으로 풀렸다.**
아래 여섯은 전부 **무엇을 재고 무엇이면 통과인가**를 적었다.

- **⑴ 바이트를 어디서 읽는가** — 원본은 **접수 볼륨**(`compose.i2.yml` named volume `uploads`)에서
  읽는다. 자리는 `contracts/storage/layout.json` 의 배치 그대로이고 **키는 원장이 들고 있다**
  (`d3_file.storage_key`). ⟹ **통과 기준** = 볼륨에 놓인 실제 바이트가 `downloadDataset` 응답으로
  그대로 돌아오는 시험 1건 이상이 green. **새 저장 배치 0건 · 마이그레이션 0건.**
- **⑵ 경계·접근 판정을 무엇으로 하는가** — **기존 기계를 그대로 쓴다. 병렬 기계 0건.**
  ㈎ 연구실 경계 = RLS(`d3_dataset` 행이 안 보이면 404) ㈏ 잠금 = `d2_access.DatasetAccessAdapter`
  의 `body_accessible`(거짓이면 403) ㈐ 조각 질의 = `d3_file` 의 `body_access` 정책이 한 겹 더.
  ⟹ **통과 기준** = `listDatasetFiles` 와 **같은 두 줄**을 쓴다(코드 대조) ＋ 그 두 줄을 지우면
  시험이 red 가 된다는 것을 실제로 확인.
- **⑶ 잠긴 행·경계 밖 행은 어떻게 되는가** — 잠김 = **403**(존재는 인정한다 · `P-13`·`P-34`) ·
  다른 연구실 = **404**(존재를 알리지 않는다 · `P-9`·`P-10`) · 토큰 없음 = **401**.
  **거절에는 다운로드 이력을 쌓지 않는다.** ⟹ **통과 기준** = 세 음성이 각각 시험 1건 이상 green
  이고, 잠금 음성은 이력 행 증가 0건까지 센다.
- **⑷ 갈아 끼우는 이음매가 어디인가** — **`services/core-api/src/colab_core/kernel/file_store.py`
  한 파일.** 라우트는 `FileStore` 말고 아무것도 모른다(루트도 · 경로도 · 볼륨이라는 사실도).
  ⟹ **통과 기준** = 내려받기 라우트에 `pathlib`·저장 루트·`open()` 이 **0회** 등장하고, 저장처를
  객체 저장소로 바꿀 때 고칠 파일이 그 하나임을 코드로 보일 수 있을 것.
- **⑸ 이력을 남기는가** — `d8_download` 에 **302 응답 시점에** 한 줄(`DataModel §6.2` ·
  계약 산문 축자). **받은 횟수는 어느 화면에도 내리지 않는다.** ⟹ **통과 기준** = 성공 1회에
  행 +1 · 거절 1회에 행 +0 을 시험이 센다.
- **⑹ 무엇이 이 항목을 닫는가** — ㈎ core-api 시험 green(양성 ＋ 음성 셋 ＋ 결손 1) ㈏ 화면에서
  누른 다운로드가 실제로 저장 경로를 타는 frontend 시험 green ㈐ `gates/run.sh all` red(판정) 0
  ㈑ 501 표에서 `downloadDataset` 이 빠지고 `test_route_table.py` 오라클이 green.
  ⛔ **staging 배포 green 은 이 항목이 재지 않는다** — 이번 회차 배포 0건이고, 그 칸은
  stage 2 완료 정의가 따로 잰다.

**`[미확인]` 2건 — 지어내지 않았다.**

- **㈎ 묶음에 기준 격자 파일을 넣는가.** 정본은 「조각 묶음이면 묶어서 한 번에」라고만 적고
  (`Policy_데이터셋_상세 §2`·`:69`), 그 「조각」이 본체만인지 격자까지인지 **어느 조항도 말하지
  않는다.** 이번 구현은 **본체만** 넣었다(같은 정본 `§5` 의 `파일` 칸이 본체 수이고 격자는
  `hasReferenceGridFile` 로 따로 말하기 때문). **푸는 법** = 정본에 한 줄이 생기거나 Ted 가 판정한다.
- **㈏ 큰 파일의 전달 방식.** 지금은 조각이 여럿이면 **메모리에서 zip 을 만들어** 내린다.
  합계 용량의 상한·스트리밍 요건을 정본도 대장도 적은 적이 없고, staging 실측 최대 묶음 용량도
  이 회차에서 재지 않았다. **푸는 법** = 합격선(상한 용량·응답 시간)을 정하고 그 값으로 잰다.

---

## 2. 등재문 초안 (번호 미발급)

> **| 〈?〉 | `ST-1` 파일 저장처를 볼륨으로 확정해 내려받기 경로를 열고, `CT-1` 의 남은 한 칸을 닫는다 |**
> **집행 ＋ 실측 (2026-09-02 Ted 판정 ＋ Claude · 워크트리 `lane-st1`).**
> **㉮ Ted 판정 = 「파일 저장처는 지금 볼륨을 그대로 쓴다」.** 객체 저장소는 인프라·비용·운영
> 경계를 한꺼번에 끌고 들어오고 이 단계에서 정당화되지 않는다. **조건 = 내려받기 경로를 이음매
> 하나 뒤에 둔다.** ⟹ `ST-1` 진입조건(저장처 형태 판정)이 풀렸고 `03-HANDOFF §4 #63` 이 해소된다.
> **㉯ `ST-1` 완료 정의를 처음 기재했다** — 여섯 조항(바이트 출처 · 접근 판정 재사용 · 음성 3종 ·
> 이음매 자리 · 이력 · 닫는 증거) ＋ **`[미확인]` 2건**(묶음의 격자 포함 여부 · 큰 파일 전달 방식).
> **㉰ 집행 — `downloadDataset` 501 을 실동작으로 갈았다.** 501 표 **5 → 4** 이고
> **`NOT_IMPLEMENTED_NO_STORE` 는 0건이 됐다.** 그 사유(「저장처가 P0 스키마에 없다」)는 **반만
> 참이었다** — 이력 표 `d8_download` 는 P0 이 세웠고 바이트는 접수 볼륨 위에 이미 있었다.
> **마이그레이션 0건**(head `0011` 그대로) · **계약 개정 0건**(계약은 처음부터 302 ＋ `Location`
> 을 들고 있었다) · **동결 해제 0건.** 이음매 = `kernel/file_store.py` 하나.
> **㉱ 접근 판정은 신설 0건이다** — `listDatasetFiles` 와 같은 두 줄(`dataset_exists` → 404 ·
> `body_accessible` → 403)을 쓰고, 그 아래 `body_access` RLS 정책이 한 겹 더 막는다. 그 두 줄을
> 지우면 시험이 실제로 red 가 되는 것을 확인했다(잠금 음성 2건 red).
> **㉲ 화면 — 카탈로그 행의 다운로드가 실제로 열리게 됐다.** 종전 `<a href>` 는 브라우저가 만드는
> 요청이라 **세션 토큰이 안 붙어 401** 이었다(구조적 결함 · 실측). 인증 첨부 자리는 여전히
> `api/client.ts` 한 곳이다.
> **㉳ `CT-1` 의 남은 한 칸을 닫는다** — 「권한 있는 행의 다운로드가 실제로 열리는가」에 대해
> **양성**(바이트가 그대로 돌아온다 · 화면이 저장 경로를 탄다)과 **음성**(잠김 403 · 다른 연구실
> 404 · 표식을 손으로 붙여도 403)을 둘 다 실측했다. **`[미확인]` ㈎㈏㈐(정본이 침묵하는 자리 셋)는
> 그대로 남는다** — 그 셋은 다운로드와 무관한 자리다.
> **㉴ 실측 (2026-09-02)** — 게이트 `all -j 1` **green 37 / red(판정) 0 / red(준비) 0** ·
> core-api **519 → 524 pass** · frontend **424 → 427 pass** · `tsc --noEmit` **신규 오류 0**
> (기존 4건은 `test/e01-apply-points.test.ts` 로 `main` 과 동일) · **staging 무접촉 · push 0 · 배포 0.**

---

## 3. `work-items.yaml` 반영 (이 레인이 직접 고쳤다)

- `ST-1` — `entry_conditions` 해소 표시 · `completion_def` 를 위 여섯으로 기재 · `evidence` 실측 ·
  **`status` 는 `open` 그대로 뒀다** — 완료 정의 여섯은 전부 실측으로 찼으나, 이 항목은 `03-HANDOFF §1` T-P 표·`WORK-UNITS §11` 체크리스트에 **행이 없다**(신설 회차가 완료 정의 부재를 이유로 안 넣었다). 닫으려면 그 두 자리에 행을 세우는 것이 함께 가야 하고, 그것은 **오케스트레이터 판정**이다.
- `CT-1` — 남은 한 칸(**권한 있는 행의 다운로드**)을 양성·음성 둘 다로 닫고 근거를 `evidence` 에 적었다. **`status` 는 `open` 그대로다** — 완료 정의 안의 `[미확인]` ㈎(승인 처리 도착 전 `Verified` 열 문구)가 **제품 판정 대기**라고 명시돼 있고 그 판정은 Ted 몫이다. **부분 충족으로 닫지 않는다.**

## 4. 이 레인이 하지 않은 것

- `PLAN-SoT.md §9` 무접촉 · 결정 번호 발급 0건.
- 계약(`contracts/**`) 무접촉 · 마이그레이션 0건 · staging 무접촉 · push·병합·배포 0건.
