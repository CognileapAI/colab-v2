# V-1 · 팔레트 선택 재렌더 — 등재문 초안 (번호 미발급)

작성 2026-09-05 · 워크트리 `lane-v1`(브랜치 `lane-v1` · `main` `3f31bc0` 기점) · **레인은 `PLAN-SoT §9` 에 직접 쓰지 않는다.**
번호 발급·등재는 오케스트레이터 몫이다 — `origin/main` 최대 +1 로 **병합 직전에 재실측**한다(`〈316〉` 는 자리표시다).
⛔ **계약 개정 0 · 마이그레이션 0(head `0011` 무변) · staging 쓰기 0 · 배포 0 · push 0.**

---

## 등재문 (§9 한 항목 · 표 한 행으로 옮겨 붙일 것)

| 〈316〉 | **`V-1` 집행 — 팔레트 재렌더의 「호출 한 자리」를 놓았다. 지우는 것은 색만 바뀐 그 벌 하나다** | **집행 ＋ 실측 (2026-09-05 · 워크트리 `lane-v1`)** … 아래 ㉮~㉳ |

**㉮ 무엇이 없었나 — 기구가 아니라 호출 한 자리였다.** `〈250〉`·`〈259〉` 의 실측이 그대로 확인됐다.
지우는 기구(`invalidation.plan`·`apply`)는 `Y-1` 병합으로 이미 서 있고, `apply` 를 부르는 곳이
**`_run_and_plan` 의 자동 경로 하나뿐**이라 사람이 팔레트를 바꿔 다시 그리는 경로에서는
범위가 `job.invalidation` 에 실리기만 하고 **집행되지 않았다.** 이 회차가 더한 것이 그 호출이다.
**red 가 그것을 짚었다** — 신설 시험 8건 중 **첫 판에서 red 는 정확히 1건**(⑷ 「팔레트 재렌더가
옛 벌을 하나도 안 지웠다」 · `assert () == invalidation_removed`)이고 ⑴⑵⑶⑸ 과 음성 넷은 **처음부터 green** 이었다.
⟹ **없던 것이 조항 넷이 아니라 조항 하나였음을 시험이 먼저 말했다.**

**㉯ 값(파일 4 · 시험 1) — 계약·마이그레이션·정본 무접촉.**
- `d7_visualization/cache.py` — payload 를 `_payload()` 한 곳으로 모으고 **`render_variant_key()`** 신설(팔레트만 뺀 서명).
  **키 규칙을 두 곳으로 늘리지 않는다** — 같은 payload 를 쓰고 빼는 열쇠 하나만 다르다. `render_cache_key` 의 출력은 **불변**(기존 시험 17건 green).
- `d7_visualization/preview.py` — `Artifact.variant_key` 필드 ＋ 세 층(썸네일·비지도형·지도형)이 자기 변이 키를 실어 나른다.
  **사이드카 문서·자리 이름은 한 글자도 안 바뀐다** — 변이 키는 디스크에 안 적힌다.
- `d7_visualization/invalidation.py` — `StaleCandidate.variant_key` ＋ **`supersede_plan()`** 신설.
  `reclaim_plan` 과 **같은 모양**이다 — 계산기를 새로 만들지 않고 `plan()` 을 부르며 회수 대상이 **아닌** 키를 `keep_keys` 로 넘긴다.
  ⟹ `tile-` 은 `kept` · 미리보기 루트 밖은 `OutOfScope` · **지우는 문은 여전히 `apply()` 하나**가 공짜로 따라온다.
- `d7_visualization/jobs.py` — `_run_and_plan` 의 완료 경로에 **수동 갈래 한 줄**(`elif job.status == STATUS_DONE`) ＋ `_supersede_for()`.
  **`plan.stale` 전체를 집행하지 않는다** — 그 자리의 범위는 그 대상의 「방금 구운 것 아닌 전부」라 그대로 지우면 **다른 변수·다른 시각·다른 원본의 그림까지** 사라진다.
- 시험 `services/viz-render/tests/test_palette_rerender.py` **8건 신설** — 조항 ⑴⑵⑶⑷⑷-b⑷-c⑸ 과 경계 음성.

**㉰ 회수 대상을 무엇으로 골랐나 — 「변이 키가 같고 캐시 키가 다른 벌」 하나뿐이다.**
변이 키가 같다 = 원본 다이제스트·긴 변·다운샘플·결측값·선택 변수·시각·색범위(값＋단계 토큰)·격자 다이제스트·좌표계가 **전부 같다**.
그 위에서 캐시 키가 갈렸다 = **팔레트가 갈렸다**(빠진 입력이 그것 하나다). ⟹ 완료 정의 ⑴ 의 「같은 데이터·같은 색범위·같은 구간 수에서 **색만 바뀐 그림**」이 곧 회수 조건이다.
**원본이 바뀐 재렌더는 변이 키부터 갈리므로 들어오지 않는다** — `Y-1` 의 「사람이 부른 렌더는 앞의 산출물을 지우지 않는다」가 그 자리에서 그대로 서고, 그 시험도 green 이다.
⚠ **구간 수(`classCount`)는 캐시 키에 없다** — 재던 자리다: 그림은 256단 LUT 로 칠해져 구간 수와 무관하고 구간 수는 **범례에만** 든다(`legend_from_range`). 키에 없는 것이 정확하다. **고치지 않았다.**

**㉱ 실측 — 자리에 쌓이는 벌이 팔레트당 3벌씩 줄어든다.** 같은 대상 하나에 팔레트 3종을 차례로 그린 뒤 자리를 센 값(로컬 · 실 GeoTIFF):

| | 벌(키) | 파일 |
|---|---:|---:|
| `main` `3f31bc0` | **9** | **21** |
| 이 레인 | **3** | **7** |

⟹ 팔레트를 한 번 더 바꿀 때마다 **벌 3 · 파일 7** 이 덜 쌓인다(썸네일 `.webp`＋`.json` · 비지도형 `.png`＋`.json` · 지도형 `.png`＋`.json`＋`.pgw`).
⛔ **이미 쌓인 재고는 이 회차가 회수하지 않는다** — `RC-1`(done `〈309〉`)의 자리이고 `_produced_for` 는 여전히 **이 프로세스가 구운 것만** 센다.

**㉲ 소유·타일 생존 판독에 미치는 영향 — 등급은 안 바뀌고 모집단만 준다.**
- `ownership.grade()` 의 입력은 **사이드카 `sources` ＋ 원장**이고 **팔레트는 그 입력이 아니다**(덫 ①). 살아남은 벌의 등급은 `main` 의 같은 벌과 **글자 그대로 같다**.
- 바뀌는 것은 **세는 대상의 수**다 — 위 표대로 팔레트 재렌더 1회마다 자리의 벌이 3 줄어든다. `artifact-ownership` 의 **고아 계수는 0 그대로**다(고아 판정에 팔레트가 안 들어간다).
- `tile_liveness` **무영향** — `tile-` 키는 `plan()` 이 언제나 `kept` 로 가르고(⑷-c 음성 시험 1건이 그것을 잠근다), 판독기는 D5 재료로 키를 다시 지어 판정한다(`〈294〉` 승격분 · `TL-1`). **두 모듈의 의미(semantics)는 한 줄도 안 고쳤다.**

**㉳ 게이트** — `./gates/run.sh all -j 1`(직렬 · 1회) **계 green 50 / red(판정) 0 / red(준비) 0** — `main` `3f31bc0` 기준선과 같다.
서비스 시험 실행 건수 = core-api **626** · ai-service **130** · **viz-render 303**(기준선 295 → 신설 8) · pipeline-worker **231** · frontend **595**(38 파일).
`artifact-ownership` green — **대상 106 벌**(살아 있다 87 · 접수분 0 · **고아 0** · 판정 불가 19) · **지도 타일 2** · 삭제 0건. ⟹ **`main` 과 같은 수다** — 그 게이트가 읽는 자리에서 팔레트 재렌더가 아직 일어나지 않았으므로 당연하다. 수가 줄기 시작하는 것은 **배포 뒤 첫 팔레트 변경부터**다.
`render-latency` green — 눈금(`〈233〉` p95 10 s · 상한 60 s) 무변경 · 이 회차가 새 눈금을 세우지 않았다.

**㉴ 조항별 상태 · 이번에 세지 않은 축** —
- ⑴ ⭕ 시험 — 범례 `classes[].min/max` 와 `bounds` 동일 · 색만 다름
- ⑵ ⭕ 시험(음성 1건) — 목록 밖 400 · `listPalettes` 가 준 3종 전건 202
- ⑶ ⭕ 시험 — 응답이 가리키는 키 ⊆ 새 키 · 옛 키와 교집합 0
- ⑷·⑷-b·⑷-c ⭕ 시험(운영 기본 실행기 `thread` 로 잼) — 새 `.png`＋`.json`＋`.pgw` 가 서고 옛 벌 삭제 · 실패한 재렌더는 삭제 0 · 원본·기준 격자 바이트 동일 · `tile-` 존치
- ⑸ ⭕ 시험(음성) — 자리에 구워 둔 조각 **0건 유지** · 새 `renderId` 의 `tileUrlTemplate` 로 갈아탐 · 타일 픽셀은 팔레트 따라 바뀜
- ⑹ ⛔ **`[미확인]` — staging 배포를 안 했다.** 이 조항은 **배포 회차가 닫는다**(`CLAUDE.md §0`). ⟹ **V-1 은 이 회차로 닫히지 않는다.**
- ⑺ ⭕ 범위 밖 준수 — 고르는 UI(`J-6`) 무접촉. 이 레인에 프런트엔드 변경 0.
- ⓐ **값 배열 재사용** — `〈289〉`-㉰ⓐ 로 **요구에서 제외**됐다. 재지 않았고 코드도 안 넣었다. 성능 눈금은 `〈233〉`(p95 2.081 s ≪ 10 s) 그대로.
- ⓑ **옛 키 파일 재고 청소** — `RC-1` 이다. 이 회차가 재고를 세지 않았다.
- ⓒ 병합·번호 발급 — 레인 밖이다.
