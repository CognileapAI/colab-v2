# stage 2 완수 경로 — 실측 지도 (2026-09-05)

**기준** `main` `3f31bc0` · 오라클 = `dev-package/work-items.yaml`.
**읽기 전용 조사** — 이 파일 외 어떤 파일도 고치지 않았고 게이트·staging 을 건드리지 않았다.
값이 산문(`03-HANDOFF.md`·`WORK-UNITS.md`)과 갈리면 대장이 이긴다.

---

## 0. 계수 재실측 — 기계로 다시 뽑았다

`status != done` 을 `stage` 별로 센 값(대장 두 필드만 사용).

| stage | 미완 | 항목 (`work-items.yaml` 행) |
|---|---|---|
| `stage1` | **6** | `BF-4` in_progress `:2003` · `BF-7` open `:2079` · `BF-8` `:2097` · `BF-9` `:2114` · `BF-10` `:2132` · `BF-11` `:2151` |
| `stage2` | **9** (deferred 1 제외 시 **8**) | `IS4` partial `:304` · `I3` partial `:345` · `I4` open `:358` · `V-1` open `:793` · `TL-1` in_progress `:944` · `J-1` open `:996` · `PA-G` open `:1312` · ~~`2단-격자전용-실패3건-처분` deferred `:1455`~~ · `CR-2` open `:1876` |
| `after_stage2` | 15 | 지시문의 「stage3 15」는 이 값이다 — `stage: after_stage2` 15건. `PA-G` 계수에 **불산입**(규칙 ④) |
| `out_of_scope` | 5 | 불산입 |

⟹ 지시문의 6 / 8 / 15 는 **전건 일치**한다.

**의존 실측 — 14건 중 `depends_on` 미충족은 `I4` 하나뿐이다.**
`A-1`(`:620`) · `V-2`(`:861`) · `P3`(`:1143`) · `CR-1`(`:1838`) · `BF-1`(`:1921`) · `BF-5`(`:2031`) · `SC-5`(`:120`) · `IS2`(`:278`) · `I2`(`:332`) · `PA`(`:1299`) 전부 `done`.
`I4.depends_on: [I3]`(`:364`)이고 `I3` = `partial`(`:347`) ⟹ 유일한 WU-대-WU 차단.

⚠ **`LV-1`·`LV-2` 는 이제 `done` 이다**(`:1487`·`:1541`). `PA-G.note`(`:1321`)의 2026-09-02 실측 「stage1 미완 3건 = `LV-1`·`LV-2`·`SC-5`」는 **낡았다** — 지금 stage1 미완 6건은 전부 `BF-*` 다.

---

## 1. 항목별 — 잔여 조건 축자 · 막는 것 · 집행 형태 · 의존 · 회차 환산 · 소유

### 1-1. `IS4` terraform state 보관 (`:304` · partial)

**완료 정의 축자**(`:311`) — 「state 를 잃은 상태에서 문서만 보고 복구 성공 (`§4` T-I 표)」

| 조항 | 판정 | 근거 |
|---|---|---|
| ① 마지막 `apply` 미실행 | ✅ **미달 아니다** | `note:314` 축자 「**Ted 판정 = `terraform plan` 의 `No changes.` 를 ① 의 오라클로 받는다.**」 · 재측정 2026-09-03 `No changes. Your infrastructure matches the configuration.`(`evidence:312` · `apply` **0회**) |
| ② 맨몸 호스트 조건 미실험 | ⛔ **미달** | `note:314` 축자 「**미달은 ② 맨몸 호스트 조건 미실험 하나다.**」 |

- **무엇이 막는가** — **재료 부재**. `R-1` 산출물 규약이 `platform-*`·`ai-*`·`vol-*` 뿐이라 **terraform state 가 백업에 없다**(`note:314` · 런북 `notes/STAGING-WINDOW-RUNBOOK.md §11 ⛔4`). `R-1` 이 `done` 이 돼도(`:765`) ② 는 남는다.
- **집행 형태** — **창 필요 ＋ 앞선 Ted 판정 1**. 런북 §8 단계 H 는 파괴 등급 `read-only ~ additive` 이나 **그 창의 산출은 「② 가 남는다」의 재확인일 뿐**이다(§8 축자). ② 를 실제로 닫으려면 state 를 **정말 잃은 상태**를 만들어야 하고, 실디렉터리에서 하면 파괴적이다.
- **파괴 단계** — 실디렉터리 state 삭제·재`init`(destructive). 사본 디렉터리 리허설이면 read-only.
- **의존** — `depends_on: [IS2]`(`:310`) 충족. 진입조건 「원격 백엔드면 I0」는 I0 `deferred` 라 로컬 백엔드 전제.
- **회차 환산** — **Ted 판정 1 ＋ 레인 1**(사본 리허설이면 창 불요). 판정이 「실디렉터리」면 **단독 창 1**.
- **소유** — **배포개선 회차**. 인프라 소유(`owner: 인프라` `:308`)이고 창 재료가 I3·I4 와 같다.

### 1-2. `I3` 배포 자동화 (`:345` · partial)

**완료 정의 축자**(`:352`) — 「파이프라인 1회 완주 (`WORK-UNITS §4`) · 15행 정본 = `sessions/I3.md §6` · 상태 정본 = `PLAN-SoT §9 〈185〉`」

`note:355` 의 **재계수 2026-09-03 축자** = 「완료 정의 15행 = **닫힘 11 · 열림 1 · 부분 3**」

| 잔여 | 축자 | 판정 |
|---|---|---|
| ⑴ 자동 트리거 완주 | 「**여전히 열림 1 = ⑴ 자동 트리거 완주** — 사람이 `deploy.sh` 를 불러야 한다」 | ⛔ 열림. 크론 셋째 줄은 **백업 연속성 검사**이지 배포 트리거가 아니다(`note:355`) |
| 2-b 실배포 red 로그 | 「원장 red 행 `5a71779510f8` 은 **이미지 빌드 단계**에서 죽은 것이라 여전히 **헬스 판정 미도달**이다 · 판정 red 의 실회차 로그는 아직 0건」 | 🟧 부분 |
| ⑽ 게이트 | 「`all -j 1` **green 42 · red(판정) 0 · red(준비) 1**(`rls-effect-selftest`) ＋ 그 1건 단독 재실행 green ⟹ **계수 기준을 밝히면 43 green**」 · 「**한 번의 `all` 실행에서 43/43 이 나온 것은 아니다**」 | 🟧 부분 — 계수 기준 판정 자리 |
| ⑾ 전후 200→200 | 「이 회차는 롤백 **직전** `/healthz` 를 안 찍었다」 | 🟧 부분 — 다음 창의 **측정 규율**로 닫힌다 |
| ⑷ 롤백 왕복 · ⑸ 직전 릴리스 확인 | 「**닫힘으로 옮긴 둘**」 | ✅ 2026-09-03 `〈301〉` 집행(`evidence:353` ㉮㉯) |

- **무엇이 막는가** — **staging 행위(파괴적) ＋ Ted 판정 2**. ⑴ 은 크론 설치(mutating), 2-b 는 **의도적 red 유발**, ⑽ 은 계수 기준 판정.
- **집행 형태** — **창 1(staging 독점)**. 파괴 단계 = 배포(마이그레이션 forward-only · 런북 §2 「**destructive**」) · 롤백(§6 「**destructive**」).
- **의존** — `depends_on: [I2]` done. 선행 블로커 `#43` 은 `〈301〉` 로 소멸(`note:355` 「낡았다」).
- **회차 환산** — **창 1** = A(짝 백업 additive) ＋ B(배포 destructive) ＋ 후속(⑾ 전·후 `/healthz` 채집 · ⑴ 크론 · ⑽ 계수 기준 기재). **2-b 만 판정에 따라 창 밖으로 빠질 수 있다.**
- **소유** — **배포개선 회차**. 이 세션이 손댈 파일 표면이 0건이다.

### 1-3. `I4` 운영 준비 (`:358` · open)

**완료 정의 축자**(`:365`) — 「복원 리허설 성공 (`§4`) … **이번 범위 = 「회차 단위(하루 1회 03:30) 복원 리허설 1회」**이고 `R-1` 의 결과를 받아 쓴다 … **이번 범위의 남은 조건 = 분산 추적 · 로그 · 알람 · 데이터 레지던시 기록 넷(실물 0건) ＋ 진입조건 `I3`**」

| 조항 | 판정 | 근거 |
|---|---|---|
| 회차 단위 복원 리허설 1회 | ✅ 재료 있음 | `R-1` = `done`(`:765`). `〈215〉` 축자 「`R-1` 의 리허설 **결과를 받아 쓰고** 다시 돌리지 않는다」(`:365`) |
| 시점 복구(WAL) | ⭕ **범위 밖** | 「`〈256〉` 로 prod 개통 관문 (다) 로 옮겼다」(`note:368`) |
| ⓐ 분산 추적 ⓑ 로그 ⓒ 알람 ⓓ 레지던시 기록 | ⛔ **실물 0건** | `note:368` 축자 「`otel\|opentelemetry\|jaeger\|trace_id\|alertmanager\|prometheus\|residency\|레지던시` 전수 grep … 히트는 **요구를 적은 산문 두 줄뿐**(`infra/README.md:33`·`:37`)이고 **구현·설정·시험 0건**」 |
| 진입조건 `I3` | ⛔ 미충족 | `I3` = `partial` |

- **무엇이 막는가** — **선행 항목(`I3`) ＋ 재료 부재(신축)**. 이것은 「완성」이 아니라 **신축**이다 — 넷 중 셋이 레포에 한 줄도 없다.
- **집행 형태** — **레인 2 ＋ 창 1**. 레인은 계장(추적·로그) · 알람＋레지던시 문서. 창은 배포 ＋ 추적·알람이 붙은 운영 리허설.
- **의존** — `depends_on: [I3]`(`:364`). 추가로 `I4.note` 증보 2026-09-04(`〈311〉`)가 **엣지·터널 262 ms 구간을 이 항목의 추적이 볼 수 있어야 한다**고 적는다 — 계장 범위가 nginx 안쪽으로 끝나면 그 요구를 못 만족한다.
- **회차 환산** — **레인 2 ＋ 창 1**. `I3` 창 뒤. 「완료 항목 재개」가 아니라 **0 → 1 신축**이므로 최소 계수도 레인 2 아래로 못 내려간다.
- **소유** — **배포개선 회차**. 파일 표면이 `infra/**`·compose 이고 이 세션·버그 회차와 0 중첩.

### 1-4. `V-1` 팔레트 선택 재렌더 (`:793` · open)

**완료 정의 축자**(`:800`, ⑴~⑺ 확정본 · `〈289〉`-㉰) — 요지만 옮긴다(전문은 `:800`·`:801`).
⑴ 「색만 바뀐 그림」 · `classes[].min/max`·`bounds` 불변 ⑵ 「고를 수 있는 것은 `listPalettes` 가 준 것뿐 — 목록 밖 이름은 거절(음성 시험 1건)」 ⑶ 「재렌더 뒤 화면에 도달한 산출물의 키가 새 키임을 시험이 대조」 ⑷ 「새 `.png`(＋`.json`·`.pgw`)가 새 키로 서고 화면이 그 키를 가리키며, **옛 산출물은 지운다**」 ⑷-b 「**새 것이 선 뒤에 옛 것을 지운다**」 ⑷-c 「지우는 대상은 렌더 산출물뿐 … `tile-` 은 `kept`」 ⑸ 켠 갈래 = 「**무효화할 것이 없음이 유지된다**」 ⑹ 「**staging 배포 green**(`CLAUDE.md §0`)」 ⑺ 「범위 밖 = 고르는 UI 는 `J-6` 소유」

| 조항 | 판정 |
|---|---|
| ⑴⑵⑶⑸⑺ | ⛔ 미착수 — `evidence: null`(`:844`) |
| ⑷⑷-b⑷-c 의 **기구** | ✅ 이미 있다 — 「`Y-1` 병합(`main` `9edf00b`)으로 **지우는 기구가 이미 서 있다**(`invalidation.plan`·`apply`). **없는 것은 기구가 아니라 호출 한 자리다**」(`:801`) |
| ⑹ 배포 green | ⛔ |
| ⓐ 값 배열 재사용 | ⭕ **요구에서 제외** — 「팔레트는 **이미 `render_cache_key` 의 입력**」 ＋ 「실측 `〈233〉` **p95 2.081 s ≪ 10 s**」(`:800`) |
| ⓑ 옛 키 파일 청소 | ⭕ **`RC-1` 로 분리** — `RC-1` = `done`(`:848`) |

- **무엇이 막는가** — **코드 미작성**뿐. 판정·계약·마이그레이션 0. `completion_def:800` 축자 「⟹ **이 확정으로 `V-1` 은 착수 가능이 된다**」.
- **집행 형태** — **레인 1 ＋ 배포 창 편승**(⑹).
- **의존** — `depends_on: [P3, A-1]`(`:799`) — **둘 다 `done`**. 미충족 0.
- **회차 환산** — **레인 1**(호출 한 자리 ＋ 음성 시험 2건 ＋ 키 대조 시험) ＋ 창 지분 0(다른 배포에 편승).
- **소유** — **이 세션이 닫아야**. 파일 표면이 `invalidation.apply` 호출부이고 **`TL-1` ⑷ 의 「`invalidation.apply()` 와의 결선」과 같은 자리**다(`:952` 축자). 두 회차로 가르면 같은 함수의 호출자를 둘이 만진다.

### 1-5. `TL-1` 지도 타일 생존 판독기 (`:944` · in_progress)

**완료 정의 축자**(`:951`) — ⑴「살아 있는 데이터셋 파일 또는 보류 업로드가 **하나라도** 가리키면 산다」 ⑵「입력을 다시 계산한다」 ⑶「네 등급을 그대로 쓴다 … **`판정 불가` 로 떨어지지 않아야 한다**」 ⑷「**`ownership.scan()`·`grade()` 는 고치지 않는다** … `invalidation.apply()` 와의 결선은 이 항목이 판정한다」 ⑸「3 주체 공유 표본을 픽스처로 고정」 ⑹「**미작성으로 남긴 것 = ⑹ 회수 집행 주체·주기**(누가 언제 부르는가) … **지어내지 않았다** `[미확인]`」

| 조항 | 판정 | 근거(`evidence:952`) |
|---|---|---|
| ⑴⑵⑶⑸ | ✅ **충족** | 축자 「완료 정의 ⑴⑵⑶⑸ 충족」 · 첫 참값 「`지도 타일 생존 — 자리의 타일 2벌 · 살아 있다 2 · … 판정 불가 0 · 계산 불가 0`」 · 시험 16 케이스 RED→green |
| ⑷ | 🟧 **반쪽** | 「`ownership.py` 는 한 글자도 고치지 않았다」 ✅ / 「⛔ 그러나 **`invalidation.apply()` 와의 결선은 판정하지 않았다** — 회수 경로를 넓히는 일은 **Ted 판정 자리**」 |
| ⑹ | ⛔ `[미확인]` | 「⑹ **회수 집행 주체·주기 `[미확인]` 그대로**」 |

- **무엇이 막는가** — **Ted 판정 2건**(⑷ 결선 · ⑹ 주체·주기). 코드는 병합돼 있다(`3f31bc0`).
- **집행 형태** — **판정만 ＋ 판정이 「집행」이면 레인 1**. staging 접촉 0 · 계약 0 · 마이그레이션 0(`entry_conditions:949` 축자).
- **의존** — `depends_on: [A-1, V-2]` 둘 다 `done`. 미충족 0.
- **회차 환산** — **판정 1회차 ＋ 레인 0~1**. `〈296〉`-㉵ 의 **주기 배경 루프**가 이미 서 있으므로(`03-HANDOFF §4 #60` 「집행 호출자를 주기 배경 루프로 세웠다」) 「거기 얹는다」로 판정하면 레인이 작다.
- **소유** — **이 세션이 닫아야**. 증거를 이 세션이 만들었고, ⑷ 결선이 `V-1` 과 **같은 함수**를 만진다.
- ⚠ **게이트를 세우지 않은 이유가 기록돼 있다** — 「지금 세우면 **매 회차 red(준비)** 다 — 접수분 루트가 **바인드가 아닌 도커 볼륨**이라 게이트 사용자가 못 읽는다」(`:952`). 이것은 결손이 아니라 **명시된 설계 판단**이다.

### 1-6. `J-1` 편의 기능 묶음 (`:996` · open)

**완료 정의**(`:1063`, 9건 확정본 · `〈289〉`-㉱) — `J-1`~`J-6`·`J-8`~`J-10`. 묶음 전체 축자 = 「ⓐ **9건이 함께 선다**(하나씩 꺼내지 않는다 · `§J` 규칙 ③) ⓑ **staging 배포 green**」(`:1003` draft 말미).

| 조항 | 판정 |
|---|---|
| 9건 통과 조건 확정 | ✅ 「`[미확인]` **5자리**가 이번에 전부 닫혔다」(`:1063`) — `J-1`⑷ 불허(RLS `db/platform/schema.sql:775`·`:786`) · `J-2`⑷ 교수(`schema.sql:131`) · `J-3`⑶ 고정값 없음 · `J-8`⑴ 표본 없으면 안 그린다 · `J-9`⑶ 수치 없는 문면 |
| 9건 실물 | ⛔ 전건 미착수 — `evidence: null`(`:1064`) |
| `J-2` 마이그레이션 | ⛔ 「**마이그레이션 `0012` 가 `J-2` 몫으로 비어 있다**(체인 head `0011_lv1_drop_level_user_set`)」(`:1063`) |
| `J-8` 선행 | ⛔ 「선행 조건 = 단계별 실측 표본이 서는 것(**`P-5` 는 여전히 `[미측정]`**)」(`:1003`) |
| ⓑ 배포 green | ⛔ |

- **무엇이 막는가** — **코드 미작성 ＋ 마이그레이션 필요(`0012`) ＋ 진입조건 해석**. 진입조건 축자 = 「**stage 2 마지막**(`〈157〉-㉱`)」(`:1001`).
- **집행 형태** — **레인 3~4 ＋ 배포 창 편승**. 서버(`J-1`·`J-3`·`J-4`) · 마이그레이션(`J-2` `0012`) · 프론트(`J-5`·`J-6`·`J-9`·`J-10`).
- **의존** — `depends_on: [A-1]`(`:1002`) `done`. 형식 의존은 충족이고, **막는 것은 진입조건 문면 하나**다.
- **회차 환산** — **레인 3~4**. 「9건이 함께 선다」가 조항이므로 **부분 착수로 계수를 줄일 수 없다** — 이 묶음이 stage 2 잔여 중 가장 크다.
- **소유** — **이 세션이 닫아야**(또는 이 세션이 여는 별도 묶음 회차). 근거 = ⓐ 「9건이 함께」라 회차를 갈라 나눌 수 없고 ⓑ `J-6` 팔레트 UI 가 `V-1` 서버 경로와 짝(`⑺` 축자 「범위 밖 = 고르는 UI 는 `J-6` 소유」)이며 ⓒ `J-5`·`J-9` 가 **`BF-4`·`BF-10` 과 같은 미리보기 표면**이라 **버그개선 회차 병합 뒤**여야 한다.

### 1-7. `CR-2` 코드리뷰 회차 후속 (`:1876` · open)

**완료 정의 축자**(`:1885`~`:1898`) — 「⟹ **닫는 기준 = 다섯이 각각 판정을 받고, 판정이 「집행」이면 그 집행이 실측으로 끝난 것.**」

| 조항 | 축자 | 판정 | 막는 것 |
|---|---|---|---|
| ⑴ 계약 델타 | 「`fe-core.yaml` … 410/413/415/422 선언 ＋ `core-viz.yaml` 경계 헤더 ＋ `getRender` 400 ＋ `ScreenshotRequest.layers maxItems 8` … **동결 해제 회차로 묶을지 판정한다**」 | ⛔ | **Ted 판정 ＋ 계약 개정** |
| ⑵ 로그인 제한 버킷 | 「`X-Forwarded-For` 마지막 홉으로 확정할지 … 배포측 후속 = nginx 가 `X-Real-IP` 를 세운다」 | ⛔ | **Ted 판정 ＋ 배포측 집행** |
| ⑶ 재굽기 ＋ 소유 재실측 | 「캐시 키에 시각·격자 digest 가 들어가 기존 산출물이 전량 미스가 된다」 · `note:1909` 「**`BF-2` 배포 즉시 ③지도형 층 키가 다시 갈려 배포 뒤 재굽기 1회 더 ＋ 소유 계수(103/84/19) 재실측이 필요**」 | ⛔ | **staging 배포 선행**(`entry_conditions:1883` 축자) |
| ⑷ 정책 넷 | 「`artifact-ownership.toml tolerate=true` 기한 · compose 비밀값 `_FILE` 전환 · `expires_at` 없는 등록 데이터셋 렌더 수명 · 발행 재시도 DLQ」 | ⛔ | **Ted 판정 ×4** |
| ⑸ CI 인프라 red 둘 | 「`schema-gates` 잡에 일회용 DB 기동·마이그레이션·URL export 스텝 · `planning-gates` 의 정본 폴더 체크아웃. **둘 다 main 사전존재**」 | ⛔ | **인프라 결정** — `note:1905` 「⑸ 는 판정이 아니라 인프라 결정이다 … **게이트를 약화시켜 green 을 만들지 않는다**」 |

- **무엇이 막는가** — **Ted 판정 ×7 ＋ 계약 개정 ＋ staging 배포 ＋ CI 인프라 레인**. 다섯 조항이 서로 다른 회차 성질이다.
- **집행 형태** — **판정 묶음 1 ＋ 레인 2**(계약 동결 해제 · CI 인프라) **＋ 배포 창 편승**(⑶).
- **의존** — `depends_on: [CR-1]`(`:1884`) `done`. ⑶ 만 「**staging 배포 선행**」.
- **회차 환산** — **판정 1 ＋ 레인 2 ＋ 창 지분 0**(⑶ 은 `I3` 창에 편승).
- **소유** — **갈린다.** ⑴⑷ = **이 세션이 판정 목록으로 올린다**(코드리뷰 회차 산물이고 그 회차는 끝났다) · ⑵⑶⑸ = **배포개선 회차가 닫는 것이 자연스럽다**(nginx·재굽기·CI 잡이 전부 그 표면).

### 1-8. `PA-G` 구글 IdP 어댑터 (`:1312` · open)

**진입조건 축자**(`:1317` · `〈258〉` Ted RULING ㉜) — 「**개시 조건 = 대장에서 `stage1`·`stage2` 항목 중 미완이 0건이 되는 시점.** **세는 규칙 넷** — ① **미완 = `status` 가 `done` 이 아닌 것 전부**(`partial` 포함) ② **`deferred` 는 제외** ③ **`PA-G` 자기 자신은 반드시 제외** ④ **`after_stage2`·`out_of_scope` 는 불산입**」

**⟹ 지시문의 읽기는 옳다.** 규칙 넷을 오늘 값에 그대로 적용 = stage1 **6** ＋ stage2 **9** − deferred **1** − 자기 자신 **1** = **13**. 이 13 이 0 이 될 때 열린다.
`note:1321` 축자 「**`stage 2` 를 다 닫아도 열리지 않는다**」 — 그 문장의 **논리는 여전히 유효**하나 **예시는 낡았다**: 당시 근거였던 `LV-1`·`LV-2`·`SC-5` 는 전부 `done` 이고, 오늘 남은 stage1 6건은 **`BF-4`·`BF-7`~`BF-11`** 이다.

**완료 정의 축자**(`:1319`) — 「ⓐ 어댑터가 `kernel/authn.py` 한 파일에만 ⓑ 비밀번호 발급 경로 제거(공존시키지 않는다) ⓒ `credentials.json`·`ops/set-password.py` 폐기 ⓓ admin 계정 처리 + 음성 시험 ⓔ PA ①②③⑤⑥ 재통과 ⓕ staging 배포 green」 — 전건 미착수(`evidence: null` `:1320`).

- **무엇이 막는가** — **선행 항목 13건 전부**. 자기 코드는 하나도 막혀 있지 않다.
- **집행 형태** — **레인 1~2 ＋ 배포 창 1**(ⓕ). ⓑⓒ 는 **파괴적이지 않으나 되돌리기 어려운 제거**다 — 「공존시키지 않는다」가 조항이라 부분 착수가 금지된다.
- **의존** — `depends_on: [PA]`(`:1318`) `done`. 실질 의존은 진입조건 13건.
- **회차 환산** — **레인 1~2 ＋ 창 1**. 체인의 마지막.
- **소유** — **어느 쪽이든** — 실질적으로 **마지막 창을 쥔 회차**가 닫는다.
- ⚠ **자동 계수는 없다** — `entry_conditions:1317` 축자 「`gates/run.sh` 에 이 계수를 재는 게이트는 **0건**이다. 자동이 되려면 `work-item-consistency` 에 계수 검사가 붙어야 한다」. **즉 개시 판정은 지금도 사람이 센다.**

---

### 1-9 ~ 1-14. stage 1 잔여 6건 (`PA-G` 개시 계수에 산입된다)

#### `BF-4` (`:2003` · in_progress · frontend D7)

**완료 정의 축자**(`:2010`) — 「⑴ `DatasetPreviewSection` 머리에 `detail.name` ＋ `fileName` ⑵ `PreviewPanels` 범례 첫 줄에 `legend.variable` ⑶ **캡션 「격자 `<grid>` · W × H」** = `basicInfo.grid` ＋ ③지도형 사이드카 `mapGeometry` 의 `width×height` ⑷ 새 API·계약 0.」

| 조항 | 판정 | 근거 |
|---|---|---|
| ⑴ | ✅ | staging 실측 「머리 `미리보기 — <데이터셋 이름>` **3건 전부** · 파일명 3건 전부」(`note:2021`) |
| ⑵ | ✅ | 「범례에 `변수` 칸 실재」 — 「**완료 정의 ⑵ 는 자리 기준으로 충족**」 |
| ⑶ | 🟧 **코드는 고쳤고 화면은 미검증** | 2026-09-04 실측 「캡션이 `격자 1280x1280` 까지만 나오고 **「· W × H」가 3건 전부 빠진다**」 → 2026-09-05 원인·수정 「고친 것 = 사이드카를 `sidecarUrl` 유무로 묻는다(갈래 무관)」 · `frontend-test` **595/595**. **커밋 `e0212bc`·`fd272b9` 로 `main` 에 들어 있다** |
| ⑷ | ✅ | 「계약·op·서비스 코드 **무변경**(프론트 1파일 ＋ 시험 1파일)」 |

- **무엇이 막는가** — **staging 배포 행위 하나**(비파괴 · 배포 자체는 destructive 등급이나 이 변경은 프론트 1파일). `note:2021` 축자 「**프론트 변경이라 다음 배포 전에는 사용자 눈에 변화가 없다**(`CLAUDE.md §0`). **배포 창이 실화면에서 캡션을 재고 전환한다.**」
- **＋ Ted 판정 1건이 잠재적으로 남는다** — 축자 「사이드카 `width`·`height` 는 ③지도형 **산출 격자(3857)** 이고 3 데이터셋 전부 **821 × 1024** 로 같다 … **Ted Q2 의 「원본 픽셀 크기」로 읽으면 세 자료가 같은 수를 말한다** — **완료 정의를 고칠지는 Ted 가 연다**(원천 배열 크기는 `DatasetBasicInfo` 새 칸 = **계약 개정**이다)」.
- **집행 형태** — **창 편승(배포) ＋ 화면 실측 1회**. 판정이 「원본 픽셀」이면 **＋ 계약 개정 레인 1 ＋ 동결 해제 행 1**.
- **회차 환산** — **레인 0 ＋ 창 지분 0**(다른 배포에 얹는다). 판정이 뒤집히면 **레인 1 ＋ 창 1 추가**.
- **소유** — **배포개선 회차가 닫는 것이 자연스럽다** — 남은 것이 배포·화면 실측뿐이다.

#### `BF-7` 스크린샷 객체 URL 즉시 revoke (`:2079` · open · frontend D7)

**완료 정의 축자**(`:2087`) — 「⑴ 실패 시험 red — 다운로드 앵커가 클릭된 뒤 revoke 가 같은 tick 에 일어나지 않음을 단언 ⑵ 회수를 `setTimeout`/`load` 뒤로 미루고 앵커를 문서에 붙였다 뗀다 ⑶ **크롬에서 다운로드가 완주함을 실측(브라우저 1회)** ⑷ 스크린샷은 서버가 그린다 — **서버 경로 무접촉**.」 — 전건 미충족(`evidence: null` `:2089`).

- **막는 것** — **코드 미작성**. 원인이 코드에 실재한다(`entry_conditions:2082` 축자 「`ScreenshotButton.tsx:72-77` · `a.click()` 직후 같은 tick 에 `URL.revokeObjectURL(url)`」). 두 번째 진입조건 「통합 브랜치 `bugfix/20260903-ui-12` 병합 뒤 착수」는 **이미 충족**(`BF-5`·`BF-6` = `done`, 그 브랜치 병합분이 `main` 에 있다).
- **형태·환산** — **레인 1 ＋ 브라우저 실측 1회 ＋ 배포 창 편승**(⑶ 은 배포된 화면이어야 뜻이 있다).
- **소유** — **버그개선 회차**.

#### `BF-8` `LabPage`(S-01) 뿌리 규칙 부재 (`:2097` · open · frontend D8)

**완료 정의 축자**(`:2105`) — 「⑴ 실패 시험 red — S-01 뿌리의 계산값 `padding` > 0 · 최대폭(카탈로그·상세와 같은 값) ⑵ `dashboard.css` 또는 뿌리 클래스에 여백·최대폭 ⑶ 히어로의 자기 여백과 이중이 되지 않는다 ⑷ `vite.config.ts` `test.css.include` 에 해당 CSS.」 — 전건 미충족.

- **막는 것** — **코드 미작성**. `depends_on: [BF-5]`(`:2104`) `done` ⟹ 진입조건 「`BF-5` 병합 뒤 그 관례를 그대로 따른다」 **충족**.
- **형태·환산** — **레인 1**(CSS ＋ 규칙 원문 시험 · `BF-5` 가 만든 선례 그대로) ＋ 배포 창 편승.
- **소유** — **버그개선 회차**. `BF-5` 와 같은 무늬·같은 파일군.

#### `BF-9` core-api 계보 원천→루트 edge 부재 (`:2114` · open · core-api D4)

**완료 정의 축자**(`:2122`) — 「⑴ 원천 노드가 있을 때 원천 → 루트 edge 를 응답에 싣는다(`method` 는 null) ⑵ 프론트 `BF-1` 의 「라벨 없는 화살표」 수용 코드가 edge 유무와 무관하게 같은 그림을 낸다 ⑶ 시험 — core-api 응답 edge 수 · 프론트 픽스처 「원천+루트+자식」 화살표 2 유지 ⑷ **계약 개정이 없으면 0, 있으면 동결 해제 행 1**.」

- **막는 것** — **코드 미작성 ＋ 계약 `[미확인]`**. 진입조건 축자(`:2118`) 「계약 `fe-core.yaml` `LineageEdge`(`:3257`) 가 원천을 끝점으로 허용하는지는 `[미확인]`」 · 「**계약 변경이 필요하면 멈추고 보고 — 동결 해제 회차로 묶는다**(`X2-FREEZE-PROTOCOL`)」.
- **형태·환산** — **레인 1**(계약 무변경이면) / **레인 1 ＋ 동결 해제 회차 편승**(계약 개정이면). `depends_on: [BF-1]` `done`.
- **소유** — **버그개선 회차**. 다만 계약 개정이 뜨면 **`CR-2` ⑴ 의 동결 해제 회차와 한 묶음**으로 가는 것이 싸다.
- ⚠ 「화면이 이미 수용하므로 **막는 것 0건**」(`note:2125`) — 사용자에게는 무증상이다.

#### `BF-10` 바탕지도 대안 — 격자선·눈금 (`:2132` · open · frontend D7)

**완료 정의 축자**(`:2140`) — 「⑴ 위경도 자료(EPSG:4326)의 미리보기에 **경위도 격자선·눈금**(사이드카 bbox 4값에서 계산 · 외부 자원 0)을 켜고 끌 수 있다 ⑵ 기본값 off ⑶ **화면과 저장 PNG 의 불일치 판정을 먼저 받는다** ⑷ 시험 — bbox 픽스처에서 눈금 위치 계산값.」

- **막는 것** — **Ted 판정 2건**. 진입조건 축자(`:2135`) 「**착수는 Ted 가 회차를 연다**」 · 「⚠ 스크린샷은 서버가 그린다 — 화면 오버레이만 더하면 저장 PNG 에는 없다. **그 불일치를 받아들일지가 완료 정의에 앞선 판정이다**」.
- **형태·환산** — **판정 2 ＋ 레인 1**. 판정 전에는 레인을 세울 수 없다.
- **소유** — **버그개선 회차** — 단 **판정이 먼저 떨어져야 그 회차가 담을 수 있다**.
- ⚠ **계수 자체가 판정 대상이다** — `note:2143` 축자 「결함이 아니라 **선택 갈래**라 `stage1` 계수에 들어가는 것이 옳은지는 Ted 가 회차를 열 때 다시 본다(`〈310〉`-㉵)」. **이 한 건을 `after_stage2` 로 옮기면 `PA-G` 개시 계수가 13 → 12 로 준다.**

#### `BF-11` `project.css` 미정의 토큰 (`:2151` · open · frontend shell)

**완료 정의 축자**(`:2159`) — 「⑴ 실패 시험 red — `project.css` 가 `tokens.css` 에 없는 `--*` 이름을 참조하면 실패(정적 대조) ⑵ 다섯 이름을 `--color-border`·`--color-text-muted` 계열 정본 토큰으로 치환 · 폴백 리터럴 제거 ⑶ backrow/backlink 규칙을 `detail.css`·`project.css` 두 벌에서 **공용 한 벌로** ⑷ 화면 회귀 0.」

- **막는 것** — **코드 미작성**. `depends_on: [BF-5]` `done` ⟹ 진입조건 충족. 「`--fg` 는 지시에 든 이름이고 origin/main 실측은 0건이다 — 레인 C 가 더한 자리가 있으면 **병합 뒤 재실측한다**」(`note:2162`) ⟹ **레인 착수 첫 단계가 재실측**이다.
- **형태·환산** — **레인 1** ＋ 배포 창 편승. **`BF-8` 과 같은 CSS 표면이라 한 레인에 합칠 수 있다.**
- **소유** — **버그개선 회차**.

---

## 2. 임계 경로 — `PA-G` 로 끝나는 사슬

```
                                 ┌── BF-7 (레인)      ─┐
                                 ├── BF-8 ＋ BF-11 (레인, CSS 합침) ─┤
  [Ted 판정: BF-10 회차 개시 ＋ PNG 불일치] ── BF-10 (레인) ─┤
                                 ├── BF-9 (레인 · 계약 [미확인]) ─┤
                                 │                            ├─▶ 배포 창 W2 ──┐
  [Ted 판정: TL-1 ⑷결선·⑹주기] ── TL-1 close ─────────────┤                │
  V-1 (레인, 의존 0) ───────────────────────────────────┤                │
  J-1 (레인 3~4 · 마이그레이션 0012) ─────────────────────┘                │
                                                                            │
  I3 잔여 ── 배포 창 W1 (destructive) ──┬── I3 close                        │
       [Ted 판정: 2-b red 오라클 · ⑽ 계수기준 · ⑴ 크론]                     │
                                        ├── BF-4 화면 실측 → close          │
                                        ├── CR-2 ⑶ 재굽기·소유 재실측       │
                                        └── I4 진입조건 해제                 │
                                                  │                          │
  I4 계장 레인 ×2 (추적·로그·알람·레지던시 = 실물 0건, 신축) ──┬── I4 운영 리허설 ──┤
  IS4 ② 맨몸 호스트 [Ted 판정: 오라클] ── 레인/창 ────────────┘                │
  CR-2 ⑴⑵⑷⑸ [Ted 판정 ×7] ── 레인 2(계약 해제 · CI 인프라) ─────────────────┤
                                                                            ▼
                                              미완 13 → 0  ⟹  PA-G 개시
                                              PA-G 레인 1~2 ＋ 배포 창 W3 (ⓕ)
```

**각 고리를 막는 것**

| 고리 | 막는 것 | 유형 |
|---|---|---|
| `BF-7`·`BF-8`·`BF-11` | 없음 — 의존 전건 `done` | 코드 미작성 |
| `BF-9` | `LineageEdge` 원천 끝점 허용 여부 `[미확인]` | 코드 ＋ 계약 |
| `BF-10` | **Ted 판정 2**(회차 개시 · PNG 불일치 수용) | Ted 판정 |
| `BF-4` | staging 배포 1회 | 배포 행위 |
| `V-1` | 없음 — `P3`·`A-1` 둘 다 `done` | 코드 미작성 |
| `TL-1` | **Ted 판정 2**(`invalidation.apply()` 결선 · 회수 주체·주기) | Ted 판정 |
| `J-1` | 진입조건 「stage 2 마지막」 ＋ 마이그레이션 `0012` | 계약·마이그레이션 |
| `I3` | 크론 미설치 · 2-b red 오라클 · ⑽ 계수 기준 · ⑾ 배포 직전 미측정 | staging 창(파괴적) ＋ 판정 |
| `IS4` | terraform state 가 `R-1` 백업 규약 밖 | 재료 부재 ＋ 판정 |
| `I4` | `I3` 선행 ＋ 넷 중 셋 **실물 0건** | 선행 ＋ 신축 |
| `CR-2` | 판정 5조항 ×7 ＋ 배포 선행(⑶) | Ted 판정 ＋ 인프라 |
| `PA-G` | 위 13건 전부 | 선행 |

**임계 경로의 실질 길이** = `I3`(창 W1) → `I4` 계장 신축(레인 2) → `I4` 운영 리허설(창 W2) → `PA-G`(창 W3).
**`I4` 가 최장 고리다** — 유일하게 「완성」이 아니라 **0 → 1 신축**이고, 유일한 WU-대-WU 차단(`I3`)을 뒤에 달고 있다.

---

## 3. 최단 완수안 — 창 **3개**

전제 = 코드 레인은 병렬, 배포 창은 직렬(staging 독점).

### 창 W1 — 배포·계측 창 (배포개선 회차 소유)
| 순 | 내용 | 파괴 등급 |
|---|---|---|
| A | 짝 백업(`backup-full.sh`) | additive |
| B | `main`(`3f31bc0` 이후) 배포 · **배포 직전 `/healthz` 를 먼저 찍는다**(`I3` ⑾) | **destructive** |
| C | 크론 배포 트리거 설치(`install-schedule.sh`) — `I3` ⑴ | mutating |
| D | `BF-4` ⑶ 캡션 화면 실측 3 데이터셋 | read-only |
| E | `CR-2` ⑶ 재굽기 ＋ 소유 네 등급 재실측 | mutating |
| F | `I3` ⑽ 계수 기준 기재 · 2-b 판정 반영 | 기록 |
**닫히는 것** = `I3` · `BF-4`(판정이 「산출 격자 수용」이면) · `CR-2` ⑶. **여는 것** = `I4` 진입조건.

### 병렬 레인군 (창 밖 · W1 과 동시)
- **이 세션** — `V-1`(레인 1) · `TL-1` 결선(판정 후 레인 0~1) · `J-1`(레인 3~4)
- **버그개선 회차** — `BF-7` · `BF-8＋BF-11`(CSS 합침) · `BF-9` · `BF-10`(판정 후)
- **배포개선 회차** — `I4` 계장 레인 2 · `IS4` ② · `CR-2` ⑸ CI 인프라 레인
- **동결 해제 회차 1** — `CR-2` ⑴ ＋ `BF-9` 계약분(뜨면) 한 묶음

### 창 W2 — 통합 배포 ＋ 운영 리허설 창
`BF-7`~`BF-11` · `V-1` · `J-1` · `TL-1` 결선분 병합 후 1회 배포 → **`I4` 운영 리허설**(추적·알람이 붙은 상태) ＋ **`IS4` ② 맨몸 조건 판정**.
**닫히는 것** = `BF-7`~`BF-11` · `V-1` · `J-1` · `TL-1` · `I4` · `IS4` · `CR-2` 잔여.
⟹ 이 시점에 **미완 13 → 0**, `PA-G` 개시.

### 창 W3 — `PA-G` 창
`PA-G` ⓐ~ⓔ 레인 1~2 → ⓕ staging 배포 green.

**⟹ 최소 창 3개.** 창을 더 줄일 수 없는 이유 셋 — ⓐ `I4` 는 `I3` 이 닫힌 **뒤에만** 진입한다 ⓑ `PA-G` 는 나머지 13이 `done` 인 **뒤에만** 진입한다 ⓒ 창은 staging 독점이라 겹칠 수 없다.

### 이 세션이 반드시 지는 것 / 넘겨도 되는 것

| 이 세션이 닫아야 | 근거 |
|---|---|
| `TL-1` | 증거를 이 세션이 만들었고(`lane-tl1` · `3f31bc0`), ⑷ 결선이 `V-1` 과 같은 함수를 만진다 |
| `V-1` | `invalidation.apply` 호출부가 `TL-1` ⑷ 와 **같은 자리** — 두 회차로 가르면 충돌 |
| `J-1` | 「9건이 함께 선다」라 갈라 나눌 수 없고, `J-6` 이 `V-1` 의 짝이다 |
| `CR-2` ⑴⑷ 판정 상정 | 코드리뷰 회차 산물인데 그 회차가 끝났다 — 판정 목록으로 올릴 자리가 여기뿐 |

| 넘겨도 사슬이 끊기지 않는 것 | 받는 회차 |
|---|---|
| `BF-7`·`BF-8`·`BF-9`·`BF-10`·`BF-11` | **버그개선 회차** — 파일 표면이 프론트 CSS/컴포넌트로 이 세션과 0 중첩 |
| `BF-4`·`I3`·`IS4`·`I4`·`CR-2` ⑵⑶⑸ | **배포개선 회차** — 전부 창·`infra/**`·CI 표면 |
| `PA-G` | 마지막 창을 쥔 회차 |

**⚠ 순서 제약 하나** — `J-1` 의 `J-5`·`J-9` 와 `J-6` 이 **`BF-4`·`BF-10` 과 같은 미리보기 표면**이다. `J-1` 레인은 **버그개선 회차 병합 뒤**에 서야 충돌이 없다.

---

## 4. Ted 판정 목록 — 상품성 질문으로

| # | 상품 질문 | 가장 싼 선택지 | 무엇이 풀리나 |
|---|---|---|---|
| 1 | ~~**`TL-1`** 타일 고아를 **누가 언제** 치우게 할까? 사람이 부를 때만? 아니면 이미 도는 청소 루프가 함께?~~ **⭐ 판정됨 (2026-09-05 · `lane-tl1b` 회수 · 별도 번호로 등재 — 이 세션이 매긴 번호가 아니다)** | **이미 서 있는 주기 배경 루프에 얹는다**(`〈296〉`-㉵ · `#60` 로 이미 세운 집행 호출자) — 새 op·계약 0 | `TL-1` ⑹ · ⑷ 결선 → `TL-1` close |
| 2 | ~~**`BF-4`** 상세 화면의 「크기」는 **그린 그림의 크기**인가 **원본 자료의 크기**인가? 지금 세 자료가 전부 `821 × 1024` 로 같게 나온다~~ **⭐ 판정됨 (2026-09-05 · Ted · 등재 `PLAN-SoT §9 〈315〉`-㉮) — 산출 격자를 그대로 수용, 계약 개정 0** | **완료 정의 ⑶ 축자대로 산출 격자를 수용** — 계약 개정 0, 배포 즉시 close | `BF-4` close. 「원본 픽셀」이면 `DatasetBasicInfo` 새 칸 = **계약 개정 ＋ 동결 해제 행 1 ＋ 레인 1** |
| 3 | ~~**판정 불가 19벌** — 누구 것인지 못 가리는 산출물 19벌을 지울까, 두고 볼까?~~ **⭐ 판정됨 (2026-09-05 · Ted · 등재 `PLAN-SoT §9 〈315〉`-㉯) — `tolerate` 연장이 아니라 재굽고 재계수. 대상 목록 `[미확인]` — 다음 창에서 실측** | **`tolerate=true` 기한을 명시로 연장하고 다음 재굽기 뒤 재계수** — 삭제 0 | `CR-2` ⑶⑷ · `〈309〉`-㉶⑴ 진입조건 |
| 4 | **캐시 키 원인 귀속** — 배포마다 그림 키가 갈려 옛 그림이 쌓인다. 게이트에 「stale」 등급을 새로 만들까, 재굽기 뒤 회수 1회로 처리할까? | **`RC-1` 선례대로 재굽기 뒤 회수 1회** — 게이트 개정 0 | `CR-2` ⑶ · `TL-1` 경계(`〈312〉` stale 3벌은 타일이 아니다) |
| 5 | **`I3` ⑴** staging 배포를 **크론이 스스로** 부르게 할까, 사람이 계속 부를까? | **`install-schedule.sh` 로 크론 1회 설치**(창 W1 단계 C · 되돌림 비파괴) | `I3` ⑴ |
| 6 | **`I3` 2-b** 「배포가 실패하는 모습」을 **일부러 만들어** 기록할까, 진짜 실패를 기다릴까? | **`〈185〉` 변이 시험 선례를 오라클로 수용** — 실회차 red 를 기다리지 않는다 | `I3` 2-b |
| 7 | **`I3` ⑽** 게이트 「43/43」을 **한 번에 나온 값**으로만 셀까, **준비 실패 재실행분**을 포함해 셀까? | **계수 기준을 명시로 기재**(red(준비) ≠ red(판정)) — 게이트 개정 0 | `I3` ⑽ |
| 8 | **`IS4` ②** terraform state 를 정말 잃은 상태를 **실디렉터리**에서 만들까, **사본**에서 만들까? | **사본 디렉터리 리허설을 오라클로 받는다**(① 이 `plan No changes.` 로 대체된 것과 같은 선) — 창 불요 | `IS4` close |
| 9 | **`I4`** 「운영 준비」를 **어디까지** 지을까? 추적·로그·알람·레지던시 넷의 실물이 지금 **0건**이다 | **최소 계장으로 완료 정의를 못 박는다** — 구조화 로그 1 ＋ 알람 1종 ＋ 레지던시 문서 1장, 분산 추적은 엣지·터널 구간(`〈311〉` 262 ms)까지 보이는 최소본 | `I4` — 이 판정 없이는 레인 범위가 안 정해진다 |
| 10 | ~~**`BF-10`** 격자선 옵션을 **이번에 만들까**? 그리고 **화면에는 보이는데 저장 PNG 에는 없는** 불일치를 받아들일까?~~ **⭐ 판정됨 (2026-09-05 · Ted · 등재 `PLAN-SoT §9 〈315〉`-㉰) — `after_stage2` 이관 보류, `stage` 필드 무변경, 버그개선 회차가 닫는다** | **회차를 열지 않고 `after_stage2` 로 옮긴다** — `〈310〉`-㉵ 가 이미 「계수에 드는 것이 옳은지 다시 본다」로 열어 두었다 | **`PA-G` 개시 계수 13 → 12** · 버그 회차 레인 1 절약 |
| 11 | **`BF-9`** 계보 그래프에서 「원천 → 루트」 화살표를 뒷단이 실어 줄까? 계약 `LineageEdge` 가 원천을 끝점으로 허용하는지가 `[미확인]` | **계약이 이미 허용하면 레인 1로 끝낸다** · 허용 안 하면 **`CR-2` ⑴ 동결 해제 회차에 합친다** | `BF-9` · 동결 해제 회차 1회로 통합 |
| 12 | **`CR-2` ⑴** 계약이 코드를 못 따라간 자리 여럿(410/413/415/422 · 경계 헤더 · `getRender` 400 · `maxItems 8`)을 **한 번에** 풀까, 흩을까? | **한 동결 해제 회차로 묶는다** — 해제 1회 | `CR-2` ⑴ ＋ `BF-9` 계약분 ＋ `BF-4` 판정 2 가 「원본 픽셀」로 갈 때의 새 칸 |
| 13 | **`CR-2` ⑵** 로그인 제한을 **누구 기준**으로 셀까? 지금은 프록시 뒤라 전원이 한 통에 들어갈 수 있다 | **nginx 가 `X-Real-IP` 를 세우고 마지막 홉으로 확정** | `CR-2` ⑵ · 배포측 후속 |
| 14 | **`CR-2` ⑷** 넷 — `tolerate=true` 기한 · compose 비밀값 `_FILE` 전환 · `expires_at` 없는 등록 데이터셋의 렌더 수명 · 발행 재시도 DLQ | 넷을 **한 판정 묶음**으로 받는다(#3 과 같은 자리에서) | `CR-2` ⑷ |
| 15 | **`CR-2` ⑸** CI 가 못 도는 잡 둘을 **준비 단계를 더해** 살릴까? | **잡에 없는 준비 단계를 더하는 쪽으로만**(`note:1905` 축자 「**게이트를 약화시켜 green 을 만들지 않는다**」) | `CR-2` ⑸ · CI 신뢰 회복 |
| 16 | **`J-1`** 진입조건 「stage 2 마지막」은 **다른 stage 2 가 전부 닫힌 뒤**라는 뜻인가, **마지막 배치에 넣는다**는 뜻인가? | **「마지막 배치」로 읽어 W2 에 넣는다** — 엄격히 읽으면 창이 하나 더 는다 | `J-1` 착수 시점 · **창 3개 안이 성립하는 전제** |
| 17 | **`BF-6`** 데이터셋 설명문 여러 줄 표시가 정본 `Policy_데이터셋_상세 §8` 「한 줄 요약」 위반인가? (`BF-6` 은 `done` 이나 위반이면 `git revert 02738e4`) | **여러 줄을 정본 개정으로 수용** — revert 0 | `BF-6` 이 다시 열려 계수가 13 → 14 가 되는 위험 제거 |

⭑ **⟨2026-09-05 갱신 · 워크트리 `lane-dec`⟩ 판정됨 4건(#1·#2·#3·#10) — 잔여 open 17 → 13.** #1(`TL-1` 회수 주체·주기)은 `lane-tl1b` 가 별도 번호로 집행 중이라 이 세션이 매긴 번호가 아니다. #2·#3·#10 은 Ted 2026-09-05 판정 · 등재 `PLAN-SoT §9 〈315〉`. 나머지 13건(#4~#9·#11~#17 중 판정됨 4건 제외)은 그대로 열려 있다.

---

## 5. `[미확인]`

1. **`〈313〉`(`TL-1`)·`BF-4` 등재문이 `PLAN-SoT §9` 에 없다.** 실측 = `dev-package/PLAN-SoT.md` 의 결정 행은 `〈312〉`(`:662`)가 마지막이고 `| 〈313〉` 행은 **0건**. 초안은 `notes/TL1-LEDGER-PENDING.md`·`notes/BF4-LEDGER-PENDING.md` 에 있으나 원장에 안 실렸다. **이것이 게이트 red 를 내는지는 재지 않았다**(게이트 미실행 지시).
2. **`I4` 넷의 「최소본」이 무엇인지 정본에 없다.** `infra/README.md:33`·`:37` 이 요구만 적고 합격선이 없다 — 판정 #9 가 없으면 레인 범위를 못 정한다.
3. **`BF-9` 계약 `LineageEdge`(`fe-core.yaml:3257`)가 원천을 끝점으로 허용하는지** — 대장이 `[미확인]` 으로 적고(`:2118`) 이 조사도 계약 파일을 열지 않았다(읽기 전용 범위를 대장으로 한정).
4. **창 소요 시간** — 런북 `§12` 가 A·D·E·F 를 전부 `[미확인]` 으로 적는다. **창 3개**는 **횟수**이지 기간이 아니다.
5. **`TL-1` 모집단** — 「검증된 것은 규칙이지 「타일이 많은 자리에서도 0」이 아니다 `[미확인]`」(`:952`). 타일 2벌뿐인 자리에서 잰 값이다.
6. **`J-1` 레인 3~4 는 이 세션의 추정이다.** 9건의 파일 표면을 실측하지 않았다 — 대장의 조항 수와 소유(서버/마이그레이션/프론트)로만 갈랐다.
7. **`BF-7`~`BF-11` 이 `staging 배포 green` 을 완료 조건으로 지는지** — 이 다섯의 `completion_def` 에는 그 문장이 **없다**. `BF-4`·`BF-5`·`BF-6` 은 `note` 에서 advisor 게이트 ③ 로 그 규율을 받았다(`:2021`·`:2041` 무늬). **다섯에 같은 규율이 적용되는지는 판정 자리다** — 적용되면 창 W2 가 필수, 아니면 병합만으로 닫힌다.
