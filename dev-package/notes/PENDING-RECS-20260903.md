# PENDING-RECS — 미결 판정 9건 권고 (advisor · 읽기 전용 · 2026-09-03)

> 입력 = `notes/QA-STRATEGY-20260903.md §4` 순 8 · `notes/SPEC-DELTA-PENDING.md §2·§3` · `PLAN-SoT §9 〈280〉-㉹·〈284〉-㉳·〈286〉·〈288〉-㉴-⑹·〈295〉` · `reference/IMAGE-DIGESTS.md §3/§5`. `main @ 196584c`. 코드·계약·staging 무접촉 · 파일 수정 = 이 문서 1건.
> 표기 — **레인** = 코드 수정 레인 · **대장** = 문서/게이트만 · **Ted** = 계약 해제·마이그레이션·staging 파괴·정본 반전.

## 0. 집행 권한 요약

| 구분 | 항목 |
|---|---|
| 오케스트레이터 권한으로 집행(권고대로 집행) | **1**(문서 1행) · **2**(정본 §9 재사용 · 계약 0) · **3-#27**(정본 신설 문구) · **3-#26**(대장 등재) · **3-#16**(정본 이미 갱신 · 시점 규칙 1행) · **3-#4**(레인 · op 실재) · **3-#28**(대장 등재) · **5**(대장 1행) · **6**(게이트 1행 · 읽기 전용) · **8**(문구 1줄 · 〈194〉 축자) · **9**(경계 유지 · 문서) |
| Ted 로 올린다 | **4**(계약 개정 = 16차 해제) · **7**(배포 스크립트가 레포 파일을 고치는 규약 — 「워킹트리 변경 0건」 전제 반전) |

## 1. #18 상세 섹션 순서 — Policy 가 정본

- 권고 = **⑴ Policy v2.8 정본 · `IA_사이트맵.md` 4장 106~118행을 Policy 순서로 고친다. 구현 무변경.**
- 결정 사실 = Policy `§4` 105행이 「2026-08-17 에 계보를 미리보기보다 앞으로 옮겼다」를 **의도와 날짜로 적었다**. IA 는 개정 전 문면이고 옮긴 이력이 없다. 이력이 있는 쪽이 이긴다. `SPEC-DELTA-PENDING §2-ⓐ`.
- 되돌리는 비용 = 문서 1행(⑵ 는 화면 재배치 ＋ `#sec-*` 앵커 재확인).
- 집행 = **대장**(정본 갱신 레인 · ⓑ IA 자연어 서술 7행과 같은 회차에 — `§2-ⓑ` 진입조건이 이것이다).

## 2. 미리보기 없는 데이터셋의 엿보기 — 숨기지 않고 §9 문구로 채운다

- 실물 = `CatalogTable.tsx:204~209` 엿보기 = `props.onOpen(row.datasetId)` = **상세 이동**. 정본 `Policy_데이터_찾기 §8` 164행 「상세로 들어가지 않고 확인하거나 받는다」와 어긋난다 — **이것이 먼저 등재할 델타**다.
- 권고 = ⓐ 지금: 엿보기 = 상세 이동인 동안 이 물음은 **상세 §9 가 이미 답한다**(369행 「이 포맷은 아직 미리보기를 지원하지 않아요. 파일을 내려받아 확인해 주세요.」). 결정 불요. ⓑ 엿보기 표면(팝오버)을 만드는 회차: **버튼을 숨기지 않는다** — 열면 상세 미리보기 패널을 재사용하고 실패 5종(v2.0 이력)과 §9 문구를 그대로 낸다.
- 결정 사실 = 숨기려면 행에 「미리보기 가능」 값이 필요한데 `DatasetRow` 13칸에 없다(`fe-core.yaml:2723`) → 숨김 = 계약 개정. §9 문구 재사용 = 계약 0.
- 되돌리는 비용 = 문구 1줄. 집행 = **대장**(정본 §8 「엿보기 = 미리보기 팝오버 · 없으면 §9 문구」 1행 ＋ 실물 델타 등재).

## 3. QA 판정 묶음

| # | 정본 침묵 지점 | 실물 | 권고 | 근거 · 비용 · 집행 |
|---|---|---|---|---|
| **#27** 404 | 404·권한없음 화면 규격 0건 | `routes/NotFoundPage.tsx:3` `<div data-screen="not-found"/>` 빈 div (일부러 비움) | 정본 `Policy_공통_기반` 에 「없는 주소」 행 신설 — 중립 문구 「이 주소에는 화면이 없어요.」＋ `연구실로 돌아가기`. 상세 §9 「지워졌어요」 문구는 쓰지 않는다(묘비 전용 · QA-STRATEGY #6) | 문구 1행 · revert · **대장→레인 Q-A** |
| **#26** GNB `▾` | 전환기·아바타 메뉴 내용 0건 | `shell/Gnb.tsx:58` 「전환 목록·동작은 P0 범위 밖」 | `▾` 를 **뗀다**(메뉴 없음을 표기로 약속하지 않는다) · 연구실 전환은 계정 1·연구실 1 인 지금 동작 자체가 없다 → 백로그 등재. 아바타 = `로그아웃` 1항이면 메뉴가 아니라 버튼 | 글자 1개 · revert · **레인 Q-A** ＋ 백로그 1행 |
| **#16** AI 검색 표기 시점 | 「언제 다시 다나」 | `Policy_홈 v1.6 §8` 두 행 병존(릴리스별) | 시점 = **날짜가 아니라 계약 값**: 응답에 `SearchResponse.interpretation`(3차 `〈87〉`) 이 실리는 릴리스부터 배지·placeholder 복귀. `〈136〉` ㈎ 「UI 만 닫는다」와 일치 | 정본 1행 · **대장** · IA 7행은 #18 회차에 |
| **#4** 파일 목록 | — (침묵 아님) | `fe-core.yaml:800~835` **`listDatasetFiles` 실재**(GET · 403 P-34 · `보기` 눌렀을 때만). 화면이 안 부른다 | **화면 결함**. 상세 `파일` 칸에 `보기` → 목록(자체 스크롤 금지 · 기준 격자 유무 「없으면 없다고」) — Policy 상세 122행 축자 | 계약 0 · revert · **레인 Q-D** 에 합류 |
| **#28** 모바일 | 반응형 규격 「침묵」이라는 QA 판정이 틀렸다 | 목업 `데이터_찾기_260817.html:138~168` 「GNB 반응형 사다리」 — 740px 에서 전환기·업로드 라벨을 접되 **주 내비 라벨은 마지막까지 지킨다**, 560px 가로 스크롤 | 침묵이 아니라 **목업 미반영**. 사다리 CSS 이식 = 「개선」이 아니라 어긋남 | CSS 30행 · revert · **레인 Q-A** · 후일이면 `convenience-deferred` 묶음 |

## 4. 검색 계약 확장 (〈295〉 STOP ②③) — 16차 한 회차로 묶어 Ted

- 권고 = **`SearchResultRow` 에만 `summary`·`period`(nullable) 추가 ＋ `SearchQuery.verified?: boolean`** 을 **16차 하나**로. `DatasetRow` 는 건드리지 않는다.
- 결정 사실 = `SearchResultRow` 는 `allOf: [DatasetRow, {relevanceBar, rationale}]`(`fe-core.yaml:2849~2862`) 이라 **두 번째 객체에 칸을 더하는 것이 이미 규약**이다 — 다른 표면의 required 13칸 무변. `verified` 걸름은 라우트가 미지 필드를 400 으로 막아(`〈295〉`-㉱) 계약 없이는 못 연다.
- 왜 함께 = 둘 다 정렬 한 줄(`〈295〉`-㉯ `items.sort`) 과 같은 파일 `routes/catalog.py` · 같은 레인 · `X2 §5-㉰-6` 묶음 쪼개기 금지.
- 미룰 이유 = 지금 결과 카드에 값 없는 칸 2개뿐 · `stage 1` 검색이라 요약은 `summary` 열 시드 값(QA #21 처리 이력 전문)이 그대로 노출된다 → **데이터 정비(§4 순 7) 뒤가 순서**.
- 비용 = 가산(oasdiff ERR 0 · ㉯ 등급) · 되돌리기 = 17차. 집행 = **Ted**(동결 해제).

## 5. 회차표 7-A 표기 규약 (〈280〉-㉹) — 규약으로 세운다

- 권고 = `X2-FREEZE-PROTOCOL §1` 에 규칙 1행 신설: 「**사후 발견된 개방 회차는 직전 정수 번호 ＋ `-A`·`-B` 로 세우고 행에 `결번 · 사후 등재 · 발견 회차` 를 적는다. 정수 번호는 밀지 않는다.**」
- 결정 사실 = 이미 표에 `7-A` 행이 서 있고(`X2 §1`) 〈280〉-㉹ 가 「규약 신설이다 — 다음 결번은 Ted 가 정한다」로 남겼다. 선례가 하나 있는 규칙을 문서화 안 하면 다음 결번이 다른 모양을 낸다(8차 사후 기재 `〈199〉` 가 그 실례). 재발 방지 문장은 `〈200〉`-㉰ 커밋 메시지 규칙과 같은 자리.
- 비용 = 문서 1행. 집행 = **대장**(Ted 가 「다음 결번은 내가 정한다」를 남겼으나 이것은 표기 규약이지 해제가 아니다 — 권고대로 집행 후 보고).

## 6. `check-cron-streak.sh` 게이트 편입 (〈286〉 ①) — 결과 파일 판독 게이트

- 권고 = `gates/run.sh` `ALL_GATES` 에 **`backup-cron-streak`** 1행. 검사기는 스크립트를 다시 돌리지 않고 **크론 04:40 회차의 마지막 판정 줄**(`run-scheduled.sh` 가 `$LOG` 에 남긴 `verdict "크론 연속 GREEN"` 요약줄)을 읽는다.
- 판정 규칙(pg-slot 과 같은 fail-closed) — ⓐ 요약줄 없음 = RED ⓑ 요약줄 시각이 **36시간**보다 오래됨 = RED(일 1회 ＋ 12h 여유) ⓒ 요약줄이 RED = RED ⓓ 요약줄 GREEN 이고 새것 = GREEN. 「대상 0건」 통과 없음(`CLAUDE.md §4`). 요약줄에 창 값(`COLAB_CRON_WINDOW_MIN`)이 찍히므로 근사 한계도 게이트 출력에 남는다.
- 왜 재실행이 아닌가 = 게이트 호스트가 백업 보관처(`COLAB_BACKUP_STATE_DIR`)를 못 보면 재실행은 항상 RED 가 되고, 그때 「보관처 없음」과 「연속 깨짐」이 안 갈린다 — 결과 판독은 둘을 가른다. 보관처 접근 가능한 호스트에서만 도는 게이트는 `render-latency` 선례.
- 비용 = 행 삭제. 집행 = **대장**(게이트 ＋ selftest 1쌍 · staging 무접촉 · 읽기 전용).

## 7. digest 대장 자동 갱신 (〈284〉-㉳) — 대장을 배포가 쓰지 않는다, 검사기가 원장을 본다

- 권고 = **ⓒ `check-image-digests.sh` 가 자체 6종은 `§5` 원장의 「서빙 중 릴리스 태그 행」을 기대값으로 읽고, 외부 4종만 `§3` 을 읽는다.** `deploy.sh` 는 레포 파일을 고치지 않는다. `§3` 자체 6줄은 「원장을 가리킨다」 로 바꾸고 값을 지운다(종전 값은 취소선).
- 결정 사실 = ⑴ `deploy.sh:71` 은 워킹트리 변경 N건이면 **착수 거부** — 배포가 레포 파일을 쓰면 다음 배포가 자기 산출로 막힌다(㉳-ⓑ). ⑵ 2026-08-29 「덮지 않는다」의 이유 = **덮으면 대조 기준을 잃는다**였고, 〈284〉 가 그 전제를 「이력은 원장이 진다」로 닫았다 → 대장이 「지금」을 두 번째로 적는 것 자체가 두 곳 기재(`IMAGE-DIGESTS` 머리 「두 곳에 적으면 갈라진다」). ⑶ 원장은 릴리스 태그 열을 가지므로(`§5` 한 줄 = 시각·릴리스태그·별칭·이미지·digest) 「지금 서빙 중 태그」(`ledger_path` 마지막 green 행)로 기대값이 한 번에 나온다. ⑷ 외부 4종은 원장에 안 남으므로(`§3` ⚠ `alpine`) `§3` 이 계속 정본.
- 대안 기각 = ⓐ `deploy.sh` 가 §3 갱신 → ⑴ 과 충돌 ＋ 커밋 주체 부재(㉳-ⓒ). ⓑ 별도 `sync-digests.sh` → 사람이 안 부르면 안 도는 기구, 〈286〉① 과 같은 실패형.
- 비용 = 검사기 1파일 ＋ 대장 문서. 되돌리기 = revert. 집행 = **Ted**(대장 정본 지위 변경 `〈165〉-㉱`·2026-08-29 판정 반전 · 복원 preflight P8 판정 경로 변경).

## 8. LineageStep 안내 문구 (〈288〉-㉴-⑹) — 두 번째 문장 삭제

- 실물 = `LineageStep.tsx:487~488` 「가공 단계는 이어 붙인 앞선 데이터에서 자동으로 정해져요. 다르면 상세 화면에서 바꿀 수 있고, 바꾼 값은 계보를 고쳐도 그대로 남아요.」
- 권고 = 「**가공 단계는 이어 붙인 앞선 데이터에서 자동으로 정해져요. 다르면 상세 화면에서 앞선 데이터를 고치면 함께 바뀌어요.**」
- 결정 사실 = `〈194〉` 「사람이 고르는 것은 부모이고 레벨은 그 결과다 (예외 없음)」 · 13차 `〈276〉` 이 `processingLevel` 쓰기 경로를 걷었다 → 「바꾼 값이 남는다」는 존재하지 않는 컨트롤 안내(같은 파일 주석 480~484행이 금지한 실패형). 정본 문면이 따로 없으므로 〈194〉 축자에서 만든다.
- 비용 = 1줄 ＋ 시험 문자열. 집행 = **레인**(Q-A 에 합류 · 정본 델타 등재).

## 9. `tile-` 키와 `ownership.scan()` — 경계 유지

- 권고 = **V-2 뒤에도 `scan()` 을 tile 키로 넓히지 않는다.** 대신 후속 RC-1/A-1 완료 정의에 「tile 키 생존 규칙 = 살아 있는 데이터셋 파일 또는 보류 업로드가 **하나라도** 가리키면 산다」를 별도 판독기로 세운다.
- 결정 사실 = `ownership.py:49~51` 「D5 가 구운 것이라 판정 대상이 아니다(`〈247〉` 경계)」 · `ARTIFACT-OWNER-DESIGN:140` 타일 하나를 **3개 주체**(데이터셋 2 ＋ 미등록 업로드 1)가 공유 — `scan()` 의 4등급(살아 있다·접수분·고아·판정 불가)은 1:1 소유를 전제라 공유 내용 키에 「고아」가 정의되지 않는다. `plan()` 의 `kept` 규칙(:351·:568 음성 시험)이 이미 잠겨 있다.
- 비용 = 문서. 집행 = **대장**(완료 정의 1행).

## 10. 이 문서가 재지 않은 것

- 게이트 실행 0 · staging 조회 0 · V-2 정지 보고서 원문(`REMAINING-20260903.md` 의 해당 행)은 `tile-` 문맥만 확인 · #21 summary 시드 값의 실제 노출은 실측 안 함 · `run-scheduled.sh` 가 요약줄을 어느 파일에 어떤 모양으로 남기는지는 `install-schedule.sh:41` 리다이렉트만 확인(6번 판독기 구현 시 실측 필요).
