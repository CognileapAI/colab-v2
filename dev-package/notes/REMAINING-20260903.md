# 잔여 항목 전체 갱신 — 2026-09-03

**기준 `main` `8cc54fe` · 오라클 = `dev-package/work-items.yaml`(전수 97항목) ＋ `PLAN-SoT.md §9`(〈286〉까지).**
산문(`03-HANDOFF.md`·`WORK-UNITS.md`·`notes/STAGE2-REMAINING.md`·`notes/PARALLEL-LAUNCH-MAP.md`)과 갈리면 **대장이 이긴다**.
등재 = `PLAN-SoT §9 〈287〉`. 이 회차는 **문서·대장 조회 전용** — 코드·계약·DB·인프라·staging 무접촉 · 대장 상태 변경 **0건**.

---

## 0. 계수 — 규칙을 먼저 적는다

**세는 규칙(`〈258〉` 그대로 · 값만 다시 쟀다)**
① **미완 = `status` 가 `done` 이 아닌 것 전부**(`partial` 포함 · `CLAUDE.md §5` 「WU 는 부분 완료로 닫지 않는다」)
② **`deferred` 는 제외**(하지 않기로 한 것) ③ **`PA-G` 자기 자신은 제외**(자기를 기다리면 영영 열리지 않는다)
④ **`after_stage2`·`out_of_scope` 는 불산입** — 단 아래 표에는 전부 행으로 싣는다.

| 단 | 총 항목 | done | partial | open | deferred | **미완(규칙 ①)** |
|---|---|---|---|---|---|---|
| `stage1` | 43 | 40 | 1 | 2 | 0 | **3** |
| `stage2` | 34 | 24 | 2 | 7 | 1 | **10** → `deferred` 제외 **9** → `PA-G` 제외 **8** |
| `after_stage2`(＝ stage 3) | 15 | 0 | 0 | 15 | 0 | (불산입) 15 |
| `out_of_scope` | 5 | 1 | 0 | 0 | 4 | (불산입) 4 |
| **계** | **97** | **65** | **3** | **24** | **5** | **총 미완 = 3 ＋ 9 = 12**(`PA-G` 포함 · `deferred` 제외) |

**직전 계수와의 대조 — 어느 쪽이 틀린 것이 아니라 「그 사이에 세 항목이 닫혔다」**

| 시점 | stage2 미완(`deferred` 포함 / 제외 / `PA-G` 제외) | stage1 미완 | 총 미완 |
|---|---|---|---|
| `〈278〉`-㉮ (2026-09-02 · `ST-1` 신설 **전**) | 12 / 11 / 10 | 3 | 14 |
| `〈278〉`-㉮ (같은 결정 · `ST-1` 신설 **후**) | 13 / 12 / 11 | 3 | 15 |
| `〈281〉` 뒤 (`03-HANDOFF §4.5` 갱신문) | 11 / 10 / 9 | 3 | 13 |
| **이번 실측 (2026-09-03 · `8cc54fe`)** | **10 / 9 / 8** | **3** | **12** |

**차이의 전건 설명 — 규칙 변경 0건 · 재분류 0건. 닫힌 항목만큼 줄었다.**
`ST-1` `done`(`〈279〉`) · `P8` `done`(`〈281〉`) · **`R-1` `done`(`〈286〉`)**.
15 − 2(`ST-1`·`P8`) = 13 → 13 − 1(`R-1`) = **12**. `stage1` 은 한 건도 닫히지 않아 **3 그대로**다.
⚠ **자동 계수는 여전히 0건 `[미확인]`** — `gates/run.sh` 에 이 셈을 재는 게이트가 없다(`〈258〉` 이래 그대로).

---

## 1. 미완 전수 — 32행 (`done` 제외 전건)

분류 — **A** 판단 불요·즉시 착수 · **B** 판단 필요·권고로 진행 가능 · **C** Ted 판정 필수 · **D** 선행 대기 · **E** 보류·범위 밖.

### 1.1 `stage1` — 3건

| id | 기능명 | status | `depends_on`(실상태) | 완료 정의 | 무엇이 막는가 | 분류 |
|---|---|---|---|---|---|---|
| `LV-1` | 사람이 가공 단계를 직접 고르는 경로 제거 | `open` | `P2`=`done` | **확정** ⓐ~ⓔ | **막는 것 0건.** 완료 정의 ⓐ~ⓔ 전건 충족 실측(`〈276〉`) · 마이그레이션 `0011` 이 `main` 에 있고 릴리스 `77813d391f94` 로 배포됨. 남은 것은 **닫는 판정 1회 ＋ staging 열 부재 읽기 전용 확인** | **A** |
| `LV-2` | AI 계보 추천을 버튼으로 되살린다 | `open` | `P2`=`done` | **확정** ⓐ~ⓔ | **막는 것 0건.** `entry_conditions` 축자 「없음 — 지금 착수 가능하다」(`〈203〉`) · 계약 동결 해제 **해당 없음**(`〈258〉` RULING ㉝) · 미충족 실물 = `LineageStep.tsx:91` 자동 조회 존치(이번 회차 재확인) | **A** |
| `SC-5` | 화면 레인 — 미리보기 화면 (P4) | `partial` | 없음 | **미작성** | **완료 정의가 없다.** 닫는 기준이 0건이라 착수도 종료도 판정 불가. **`PA-G` 를 여는 마지막 자물쇠이기도 하다** — 이 자리를 지는 항목·결정 0건 | **C** |

### 1.2 `stage2` — 10건

| id | 기능명 | status | `depends_on`(실상태) | 완료 정의 | 무엇이 막는가 | 분류 |
|---|---|---|---|---|---|---|
| `CT-1` | 카탈로그 표 스펙 세목 (열·조건·정렬·잠긴 행·0건 문구) | `open` | `P1`=`done` · `ST-1`=`done` | **확정**(`〈279〉`·`〈282〉` 로 조항 전건 충족) | **잔여 1칸 = 「표 헤더가 스크롤 중 고정되는가」 시험 부재.** 제품 판정이 아니라 **도구 제약**(jsdom 이 stylesheet 를 싣지 않는다) | **B** |
| `V-1` | 팔레트 선택 재렌더 | `open` | `P3`=`done` · `A-1`=`done` | **미작성**(`completion_def_draft` 만) | `#54` — Ted 확정 전에는 착수 가능이 아니다(축자) | **C** |
| `V-2` | 값 조회 | `open` | `P3`=`done` · `A-1`=`done` | **확정**(`〈254〉`) | **착수 경로 미판정 둘** — op 신설(동결 해제 등급 ㉯) ＋ COG 조회 경로 3택(`〈278〉`-㉱) | **C** |
| `J-1` | 편의 기능 묶음 9건 | `open` | `A-1`=`done` | **미작성**(범위만 · 초안에 `[미확인]` 5자리) | `#54` ＋ `entry_conditions` 「stage 2 마지막」 | **C** |
| `S3` | 실데이터 5종 E2E | `open` | `S2`=`done` · `P3`=`done` | **확정**(`〈248〉`) | **잔여 1행 = 공개 엣지 포맷별 왕복.** 선행 = **staging 쓰기**(NumPy 본체 1건 적재) ＋ **엣지 자격증명 접근** — 둘 다 승인 사안 | **C** |
| `IS4` | terraform state 보관 | `partial` | `IS2`=`done` | **확정** | 미달 2 = 마지막 `apply` 미실행(**실 터널을 건드린다**) · 맨몸 호스트 조건 미실험 | **C** |
| `I3` | 배포 자동화 | `partial` | `I2`=`done` | **확정**(15행 · 열림/부분 6) | 잔여 = **롤백 왕복 리허설**(staging 파괴적) ＋ 직전 릴리스 확인 ＋ 자동 트리거 완주. ⭑ **`#43` 의 전제는 이번 회차에 소멸했다 — §3 참조** | **C** |
| `I4` | 운영 준비 (추적·알람·복구 리허설) | `open` | `I3`=**`partial`** | **확정**(＋`〈215〉`·`〈249〉` 증보) | **선행 미종결.** ＋ 요구 넷 중 셋(분산 추적·로그·알람·레지던시)의 레포 실물 **0건** | **D**(`I3`) |
| `PA-G` | 구글 IdP 어댑터 | `open` | `PA`=`done` | **확정** ⓐ~ⓕ | **개시 조건 = `stage1`·`stage2` 미완 0건**(`〈258〉`). 지금 **11**(자기 제외 후 8 ＋ stage1 3) | **D**(전건) |
| `2단-격자전용-실패3건-처분` | 격자 전용 업로드 실패 3건의 처분 | `deferred` | 없음 | — | **하지 않기로 한 것**(`〈145〉`-㉳ Ted 「그대로 둬」). ⬜ 가 아니다 | **E** |

### 1.3 `after_stage2`(＝ stage 3) — 15건 · 전부 **E**

단 이동은 전부 Ted 판정이다. **stage 2 완주 계수에 들지 않는다.**

| id | 기능명 | `depends_on`(실상태) | 완료 정의 | 이월 근거 |
|---|---|---|---|---|
| `T-1` | `ts_config` 한국어 재작성 → 범위 ⓑ(온톨로지 어휘·관계 확장) 하나 | 없음 | 확정(범위 축소본) | `〈274〉`(단 이동 `〈182〉`·2026-08-30 4회차) |
| `P4` | 검색 히어로(S-01) · 검색 결과(S-06) | `P1`·`G2`=`done` · `T-1`=`open` | 확정 | 2026-08-30 3회차 Ted — 「검색 화면은 자연어와 AI 를 지닌 채 통째로 stage 3」 |
| `K3` | 계보 제안 서비스 | `K2`·`D2`·`P2`=`done` | 확정(`〈211〉`) | `sessions/STAGE3-AI-SPLIT.md` — 「AI 는 전부 다음 단계」 |
| `K4` | 자연어 검색 서비스 | `K2`·`G2`·`D2`=`done` · `T-1`=`open` | 확정(`〈208〉`·`〈217〉`) | 같음 |
| `K5` | 제안 원장 | `K3`=`open` | 확정 | `K5 ⊇ K3` 엄격 직렬의 귀결 |
| `DP-1` | 데이터 프로세스(가공 → 새 데이터셋) | 없음 | **미작성** | `〈189〉`-㉰ — 「stage 2 다음 · 지금 만들지 않는다」 |
| `LV-3` | 「CoLAB에서 가공됨」 표식 | `DP-1`=`open` · `P3`=`done` | 미작성 | `DP-1` 이 서야 표식이 가리킬 것이 생긴다 |
| `LV-4` | 계보 관계 삭제 시 부모 이름 상수 보존 | `P2`=`done` | 미작성 | 저장 자리 설계 `[미확인]` |
| `G1b` | E-04·E-02 목업·화면 최신성 | `G1`·`P2`=`done` · `P4`=`open` | 확정 | `〈212〉` — 순서표에 자리가 없다는 사실 그대로 |
| `R2` | v1 레포 5종 archive | `C4`=`open` | 미작성 | `C4` 후 |
| `C1` | 5 repo 푸시 확인 | 없음 | 미작성 | T-C 는 stage 2 에서 열지 않는다 |
| `C2` | v1 이관 | `C1`=`open` | 미작성 | `C1` 후 |
| `C3` | PoC 지식 추출 `HARVEST.md` | `C1`=`open` | 미작성 | `C1` 후 · 비차단 선언 |
| `C4` | v1 방법론 추출 `METHOD.md` | `C1`=`open` | 미작성 | `C1` 후 · `R2` 의 선행 |
| `C3·C4` | 위 둘을 한 행으로 묶은 산문 자리 | `C1`·`C3`·`C4`=`open` | 미작성 | 묶음 행 자체를 지우지 않고 등재한 자리 |

### 1.4 `out_of_scope` — 4건 · 전부 **E**

| id | 기능명 | 보류 근거 |
|---|---|---|
| `I0` | AWS 계정·결제·리전·예산 알람 | `§9-㊻` — 출시 결정 전까지 보류(⏸ 이지 ⬜ 가 아니다) |
| `I1` | 토폴로지 ＋ IaC | `I0` 종속 · `〈187〉`-㉯ |
| `I5` | prod 전환 | `I4` ＋ `P7` ＋ 출시 결정 ＋ **시점 복구(WAL) 관문**(`〈256〉`) |
| `VZ-1` | 2D 시각화의 경계·점 표현 | 벡터 원천 **0건** 실측 — 원천 5종 전부 격자 |

---

## 2. 분류 집계와 물음

| 분류 | 건수 | 항목 |
|---|---|---|
| **A** 판단 불요·즉시 착수 | **2** | `LV-1` · `LV-2` |
| **B** 판단 필요·권고로 진행 | **1** | `CT-1` |
| **C** Ted 판정 필수 | **7** | `SC-5` · `V-1` · `V-2` · `J-1` · `S3` · `IS4` · `I3` |
| **D** 선행 대기 | **2** | `I4`(←`I3`) · `PA-G`(←미완 0건) |
| **E** 보류·범위 밖 | **20** | stage 3 **15** ＋ `out_of_scope` **4** ＋ `2단-격자전용-실패3건-처분` **1** |
| 계 | **32** | ＝ 대장 미완 전수 |

### B — 물음 하나 (어드바이저 권고로 집행 가능)

- **`CT-1`** — 「표 헤더가 스크롤 중 고정되는가」를 **어느 층에서 시험하는가.**
  후보 = ⓐ `catalog.css` 규칙 축자 단언(지금 방식의 연장 · 계산값 아님) ⓑ jsdom 에 stylesheet 를 실어 계산값을 재는 설정 추가 ⓒ 브라우저 층 시험 도구 도입(Playwright 등 · 레포에 선례 0건).
  **제품 판정이 아니라 도구 선택이고 트레이드오프가 명확하다** — 권고 하나를 받아 집행하고 「권고대로 집행」으로 보고한다.

### C — 물음 일곱 (문면 그대로)

1. **`SC-5`** — 미리보기 화면 레인의 **완료 정의를 무엇으로 쓰는가.** (`#54` 와 같은 무늬 — 증거로 안 풀린다. 이 자리를 지는 항목·결정 0건이고, **`PA-G` 는 이것이 서기 전에는 열리지 않는다**)
2. **`V-1`** — 초안 일곱을 확정으로 올리는가, 그리고 초안이 못 닫은 둘(팔레트만 바꿀 때의 값 재사용 · 옛 키 파일의 수명·자리 청소)을 이 항목이 지는가. (`#54`)
3. **`V-2`** — **값 조회 op 신설**(계약 동결 해제 등급 ㉯)을 승인하는가 ＋ 자리의 COG 를 **ⓐ 사이드카 / ⓑ 저장 규약 승격 / ⓒ Port 신설** 중 무엇으로 읽는가. (`〈278〉`-㉱)
4. **`J-1`** — 9건 초안의 **`[미확인]` 5자리**(`J-1`⑷ 연구실 밖 가져오기 · `J-2`⑷ 기준 격자 지정 역할 · `J-3`⑶ 경고 문구의 거리 수 · `J-8`⑴ 예측의 근거 · `J-9`⑶ 이득의 합격선)를 확정하는가. (`#54`)
5. **`S3`** — **staging 쓰기**(NumPy 본체 1건 적재)와 **엣지 왕복용 자격증명 접근**을 승인하는가.
6. **`IS4`** — **실 터널에 terraform `apply` 1회**를 승인하는가(맨몸 호스트 리허설 포함).
7. **`I3`** — **staging 롤백 왕복 리허설 1회**를 단독 창에서 도는 것을 승인하는가. ⭑ **`#43`(롤백 대상 0건)의 전제는 소멸했다 — §3-㉮.**

---

## 3. 이번 회차가 실측으로 걷은 드리프트

**㉮ ⛔ `03-HANDOFF §4 #43`·`§2 스냅샷` 의 「green `deploy` 행이 하나뿐이라 롤백 대상 0건」은 낡았다.**
실측(2026-09-03 · `~/colab-v2-releases/release-ledger.tsv` 읽기 전용) = `deploy` 행 **15** · 그중 **green 12** ⟹ 현재 서빙 태그(`77813d391f94`)를 뺀 **롤백 대상 11건**.
⟹ `rollback.sh` 가 `die` 하는 조건은 성립하지 않는다. **`I3` 의 잔여는 「대상이 없다」가 아니라 「왕복을 아직 안 돌았다」로 성격이 바뀐다.**

**㉯ `03-HANDOFF §2` 게이트 계수 「33 종」이 낡았다.** 실측 = `gates/run.sh:12` `ALL_GATES` **39 종**(`〈286〉` 회차 실측 39/0 과 일치).

**㉰ `03-HANDOFF §2` staging 서빙 태그 「`30b3e0a7b3f3`」이 낡았다.** 실측 = 원장 마지막 green `deploy` **`77813d391f94`**(2026-09-02 23:44).

**㉱ `notes/PARALLEL-LAUNCH-MAP.md` 는 `〈275〉` 기준이라 착수 가능 표 6행 중 3행이 이미 닫혔다**(`R-1`·`P8` · ＋`ST-1` 신설로 `CT-1` 실격 사유 소멸). §4 에서 갱신한다.

⚠ **대장은 한 줄도 고치지 않았다** — 위 넷은 전부 **산문 쪽**이 낡은 자리다(`〈268〉` 원칙: 대장이 원본, 산문이 반영본).
⚠ **`LV-1` 의 staging 열 부재는 이 회차가 재지 않았다 `[미확인]`** — 푸는 법 = 읽기 전용 카탈로그 조회로 `d3_dataset.processing_level_user_set` 부재를 1회 확인한다.

---

## 4. 충돌 지도 갱신 — A ＋ B 3레인

`notes/PARALLEL-LAUNCH-MAP.md §3` 의 논리를 그대로 잇고 **값만 다시 잰다.**

| | LV-1(닫기) | LV-2 | CT-1 |
|---|---|---|---|
| **LV-1(닫기)** | — | | |
| **LV-2** | | — | ▲ |
| **CT-1** | | ▲ | — |

- **`■` 0건.** 세 레인의 쓰기 파일 면이 겹치지 않는다.
  `LV-1`(닫기) = `work-items.yaml` ＋ 산문(코드 0) · `LV-2` = `frontend/src/components/lineage/LineageStep.tsx` ＋ 시험 · `CT-1` = `frontend/test/catalog.test.tsx`(＋ 도구 설정).
- **`▲` 하나** — `LV-2` ↔ `CT-1` 은 게이트 `frontend-typecheck`·`frontend-typecheck-selftest` 와 `frontend/` 시험 계수를 공유한다. **파일은 안 겹치고 게이트 실행만 직렬이면 된다.**
- **공유 정본 파일** — 세 레인 모두 `dev-package/work-items.yaml`·`03-HANDOFF.md`·`PLAN-SoT.md §9` 를 만진다. **결정 번호 발급과 §9 등재는 오케스트레이터가 직렬로** 한다(`〈252〉`).

**계약·마이그레이션·게이트 실측값**

| 축 | 값 (2026-09-03) |
|---|---|
| 계약 개정 | **A·B 3레인 전건 0건.** `LV-1` 은 이미 집행·병합됨 · `LV-2` 는 `〈258〉` RULING ㉝ 로 「해당 없음」 · `CT-1` 은 시험 층 |
| 마이그레이션 | **0건.** platform 체인 head = **`0011_lv1_drop_level_user_set`** ⟹ **다음 번호 `0012` 는 비어 있다**(먼저 집는 레인이 가진다 — 지금 후보는 `J-1`(`J-2` 연구실 기본 격자) 하나) |
| 게이트 | **39 종**(`ALL_GATES` 실측). 이번 회차 실행분 = `work-item-consistency` green · `planning-freshness` green · `work-item-selftest` green |
| pg 슬롯 | 호스트 전역 한도 4 · 레인 기본 병렬도 2 ⟹ **`./gates/run.sh all` 은 한 번에 1 레인**(`PARALLEL-LAUNCH-MAP §4` 그대로 · 재측정 안 함) |
| staging 스택 | **1 레인 배타** — 배포·복원·백업·롤백은 상호 배타 |

---

## 5. 권고 착수 순서

**A 먼저 — 무충돌 · 계수를 가장 많이 줄인다.**

1. **`LV-1` 닫기** — 코드 0건. 완료 정의 ⓐ~ⓔ 는 이미 충족이고(`〈276〉`) 병합·배포도 끝났다(`0011` ＋ 릴리스 `77813d391f94`). **읽기 전용 열 부재 확인 1회 ＋ 대장 `status: done` ＋ 산문 반영**으로 닫힌다. **가장 싸고 `stage1` 미완을 3 → 2 로 줄인다.**
2. **`LV-2`** — `stage1` 의 마지막 기능 항목. 계약 0 · 마이그레이션 0 · 프런트 한 파일 ＋ 중계 시험. **`stage1` 미완을 2 → 1 로 줄여 `SC-5` 하나만 남긴다** ⟹ `PA-G` 를 여는 데 남는 자물쇠가 **완료 정의 하나로 압축된다**(그것이 §2-C-1 의 물음이다).

**B 다음 — 권고를 받아 집행한다.**

3. **`CT-1`** — 어드바이저 권고로 시험 층을 고르고 그 자리에서 헤더 고정 시험 1건을 세운다. `LV-2` 와 파일은 안 겹치므로 병행 가능하되 **게이트는 직렬**.

**그 뒤 (C 가 풀리는 순서대로)** — `SC-5` 완료 정의 → `V-2` 착수 경로 → `V-1`·`J-1` 완료 정의 → `S3`·`IS4`·`I3`(전부 운영 접촉 · 단독 창) → `I4` → `PA-G`.

---

## 6. 이 문서가 재지 않은 것

- **`LV-1` 의 staging 실물 열 부재** `[미확인]` — 자격증명 접근이 이 회차 범위 밖.
- **`#43` 이 소멸했는지의 최종 판정** — 원장 행은 셌으나 `rollback.sh` 를 **돌리지는 않았다**(파괴적).
- **자동 계수 게이트** — 여전히 0건. `work-item-consistency` 에 계수 검사가 붙어야 자동이 된다(`〈258〉` 이래 그대로).
- **`I4`·`PA-G` 의 폭발 반경** `[미확인]` — 실물 0건이라 대장에서 파일 면이 나오지 않는다.
- **pg 슬롯·`render-latency` 경합 재측정** — `PARALLEL-LAUNCH-MAP §4` 값을 그대로 인용했다(이 회차 게이트 실행 3건은 전부 `-j 1`).
