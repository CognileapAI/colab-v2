# V-1 간헐 red 종결 — 「완료」는 회수 뒤에 발행한다 · 등재문 초안 (번호 미발급)

작성 2026-09-05 · 워크트리 `lane-v1fix`(브랜치 `lane-v1fix` · `main` `1ae5dc0` 기점) · **레인은 `PLAN-SoT §9` 에 직접 쓰지 않는다.**
번호 발급·등재는 오케스트레이터 몫이다 — `origin/main` 최대 +1 로 **병합 직전에 재실측**한다(`〈N〉` 는 자리표시다).
⛔ **계약 개정 0 · 마이그레이션 0(head `0011` 무변) · staging 접촉 0 · 배포 0 · push 0.**

---

## 등재문 (§9 한 항목 · 표 한 행으로 옮겨 붙일 것)

| 〈N〉 | **`V-1` 간헐 red 를 닫는다 — 「완료」가 회수보다 먼저 발행되고 있었다. 순서를 고쳤다(시험이 아니라)** | **판정 ＋ 집행 (2026-09-05 · 워크트리 `lane-v1fix` · advisor 권고 ⓐ 대로 집행)** … 아래 ㉮~㉳ |

**㉮ 무엇이 막았나.** 창 5(`WINDOW-20260905-A5.md`)가 `main` `1ae5dc0` 을 **`NO-GO`** 로 닫았고,
사유는 배포 전 검사기 하나 — `service-tests-viz-render` **4회 중 3회 red** · 언제나 같은 케이스
`test_팔레트_재렌더는_새_키를_세우고_옛_벌을_지운다` · 축자 `AssertionError: 팔레트 재렌더가 옛 벌을 하나도 안 지웠다`.
**시험 파일 단독은 6/6 green** 이었다 — 그래서 「환경 탓」으로 넘어갈 수 있는 모양이었다.

**㉯ 결정론 재현을 먼저 세웠다 — 운으로 고친 간헐은 고친 것이 아니다.**
레포 소스 무수정 하네스(`invalidation.apply` 를 0.3초 늦추는 pytest 플러그인)로
**5회 실행 · 5회 red · 언제나 같은 그 케이스** 를 얻었다. 고친 뒤 같은 명령이 **5/5 green** 이다.
⟹ 창을 벌리면 100% red, 순서를 고치면 100% green — **간헐이 아니라 순서였다**는 것을 재현이 말한다.

**㉰ 기구 — 계측이다(추정 아님).** 실제 전수 실패 1회에서 잰 타임스탬프(임시 프로브 · 되돌렸다):

```
51716.791048  DONE set    job=…FD0D               ← jobs.py:567  job.status = STATUS_DONE
51716.791082  REMEMBERED  job=…FD0D produced=14   ← jobs.py:684  색인은 이미 완전하다(두 벌 다)
51716.803988  TESTREAD    removed=0 status=완료   ← 화면이 「완료」를 보고 회수 결과를 읽는다
51716.806413  SUPERSEDE   produced=14 stale=7     ← jobs.py:701  회수 범위 계산
51716.807190  ASSIGNED    removed=7               ← job.invalidation_removed 가 그제야 적힌다
```

⟹ **「완료」는 `_run` 안(`jobs.py:567`)에서 발행되고, 회수는 `_run_and_plan`(`jobs.py:684~701`)에서
그 **뒤**에 돈다.** 실측 창 **≈15 ms** · 시험 폴링 주기 **20 ms** ⟹ 기대 red 율 ≈ 3/4 — **창 5 의 표본(4회 중 3회)과 일치한다.**
⚠ **회수 자체는 옳다** — `removed=7` 로 옛 벌은 실제로 지워진다. **늦을 뿐이다.**
⛔ **시험만의 문제가 아니다** — 상태를 폴링하는 진짜 클라이언트도 같은 자리를 본다.

**㉱ 다른 후보를 배제한 근거 — 창 5 가 지목한 방향은 **틀렸다**(감추지 않는다).**
- ⓧ **색인 등재 경합**(창 5 의 가설 「첫 벌이 색인에 들기 전에 둘째가 범위를 잰다」) — **배제.**
  `REMEMBERED … produced=14`(두 렌더의 산출물 전부)가 **화면의 읽기보다 먼저** 찍혔고,
  전수 red 판과 green 판을 대조하면 팔레트 쌍 5건 **전부 `produced=14 / stale=7` 로 동일**하다. 색인이 빈 적이 없다.
- ⓧ **시험 오염·공유 임시 자리·순서** — red 판과 green 판의 렌더 계열이 **동일**(대상 112 · 렌더 143).
  유일한 차이는 무관한 시험 안의 팔레트 집합 순회 순서(`PYTHONHASHSEED`)뿐이다.
- ⓧ **실행기 누수·모듈 캐시** — 같은 실행기 스레드 · 시험마다 캐시 키 전부 다름 · pytest 플러그인 0(무작위 순서 없음).

**㉲ 값(파일 2) — 계약·마이그레이션·정본 무접촉.**
- `d7_visualization/jobs.py` — ⑴ `_run` 이 `STATUS_DONE` 을 **안 세운다**. 대신 내부 신호 `RenderJob.render_succeeded`
  (계약 표면 밖 · `to_dict` 가 안 본다). ⑵ `_run_and_plan` 의 두 갈래가 `job.status == STATUS_DONE` 대신
  그 신호로 갈린다 — **`〈코드리뷰 #2〉`(집행이 한 번도 안 돌던 결함)의 재현을 막는 자리다.**
  ⑶ 「완료」는 `finally` 에서, **회수가 끝난 뒤 대입 하나로** 발행된다(원자적 · 잠금 불필요).
  `finally` 인 이유 = 범위 계산이 던져도 그린 렌더가 **영원히 「그리는 중」** 으로 남지 않게 한다.
  ⑷ 수동(팔레트) 갈래의 `apply` 를 `try/except` 로 감싼다 — **지우다 실패해도 렌더는 완료다**
  (`log.warning` 으로 남긴다 · 조용히 삼키지 않는다). ⚠ **자동 갈래(`plan.regenerate`)는 안 감쌌다** —
  거기서 던지는 것은 `Y-1` 의 규율이다(`regenerate` 가 그 예외로 트리거를 안 걷는다).
- 시험 `tests/test_palette_rerender.py` **2건 신설**(회귀 가드 · 아래 ㉳). **기존 단언은 한 글자도 안 약화했다.**

**㉳ 회귀 가드 — red-first 로 세웠다.**
- ⑴ `test_화면이_완료를_본_그_순간_옛_벌은_이미_지워져_있다` — 오라클은 작업 내부가 아니라 **화면이 보는 것**이다:
  HTTP 로 `완료` 를 본 그 순간 ⓐ 옛 파일이 자리에 없고 ⓑ 집행 결과가 이미 적혀 있다.
  ＋ 창이 열려 있는 동안의 계약 유효성도 함께 잠근다(`그리는 중` 하나뿐 · `stage` 는 없거나 enum 값 · `result` 없음).
  하네스(0.3초 지연)를 **시험 안 `monkeypatch`** 로 둔 이유 = **게이트가 돌아야** 한다(레포 밖 플러그인은 CI 가 못 본다).
- ⑵ `test_회수가_실패해도_렌더는_완료로_선다` — 음성 · ⑷-b 의 반대 절반. `inline` 실행기로 잰다
  (예외가 스레드 안에서 조용히 사라지지 않고 접수 표면까지 올라오는 자리).
- **red-first 실측** — `jobs.py` 를 `main` `1ae5dc0` 판으로 되돌리고 이 두 건만 돌린 결과 **3회 전부 red**:
  `AssertionError: 화면이 「완료」를 봤는데 옛 벌이 아직 자리에 있다` · `OSError: 자리를 지울 수 없다`.
  고친 판에서 **10/10 green**.

**㉴ 계약 판정 — 개정 0 · 위반 0.** `core-viz.yaml` `RenderJob.required = [renderId, status]` · `stage` 는 **선택 속성**이고
문면은 「`status` 가 `그리는 중` 일 때**만** 있다」(있을 수 있는 조건이지 필수가 아니다). 회수 구간(실측 ≈15 ms) 동안
화면이 보는 것은 `status: 그리는 중` ＋ `stage` 없음 — **계약 안이다.** `getRenderTile`(`renders.py:150`)·
`createScreenshot`(`screenshots.py:82`)도 `STATUS_DONE` 게이트라 **같은 순간에 함께 열린다** — 오히려 정합이 늘었다.

**㉵ `V-1` 완료 정의 — ⑷ 는 그대로 선다(좁아지지 않았다).**
⑷ 「옛 산출물은 지운다」가 **완료의 구성요소**이므로 「완료」 발행은 그 삭제 뒤여야 한다 — 이 회차가 문면과 코드를 일치시켰다.
⑷-b 「새 것이 선 뒤에 옛 것을 지운다」는 **파일 순서 제약**이고 `apply` 는 여전히 `_run` 성공 뒤에만 돈다 — 무변.
⑷-c 음성(원본·기준 격자·`tile-` 무접촉) 무변. ⑴⑵⑶⑸ 무변.

**㉶ 게이트** — 5회 연속 단독 실행 `service-tests-viz-render` **전부 green**(수집 326 · 실행 326 · failed 0 · errors 0).
기준선 324 → 신설 2.
`./gates/run.sh all -j 1`(직렬 · 1회) **계 green 50 / red(판정) 0 / red(준비) 0** — `main` `1ae5dc0` 기준선(`〈316〉` 실측)과 같은 수다.
서비스 시험 실행 건수 = core-api **626** · ai-service **130** · **viz-render 326**(기준선 324 → 신설 2) · pipeline-worker **231** · frontend **595**.
