# 세션 부팅 문서 감량 — 실측·집행·제안 (2026-09-05)

> **레인 = 문서 전용.** 코드·계약·DB·인프라 **무접촉** · staging **무접촉** · 배포 **0** · push 0(브랜치만).
> **한 줄도 지우지 않았다 — 옮겼다.** 옮긴 자리는 전부 `dev-package/archive/` 이고 원 줄 번호를 주석으로 달았다.
> 원장 등재는 이 레인이 하지 않는다(문서 정리 회차 · 새 판정 0).

---

## 1. 인벤토리 — 여섯 문서가 무엇이고 누가 읽는가

| 문서 | 무엇인가 | 기계가 읽는가 | 사람이 언제 읽는가 |
|---|---|---|---|
| `dev-package/03-HANDOFF.md` | 진행 상태 **반영본**(상태 원본은 대장) · 블로커 · 진입조건 | **읽는다** — `work-item-consistency` ㈐ | 매 세션 시작 |
| `dev-package/PLAN-SoT.md` | 설계 근거 + **`§9` 결정 원장** | **읽는다** — `work-item-consistency` ㈔ | 결정 번호로 집을 때만 |
| `dev-package/work-items.yaml` | **항목 상태의 유일한 원본**(대장) | **읽는다** — `work-item-consistency` ㈎ 전건 | 상태를 바꿀 때 |
| `dev-package/WORK-UNITS.md` | 완료 정의 · 의존 그래프 · `§10`~`§11` | **읽는다** — `work-item-consistency` ㈏㈑ | WU 착수 전 |
| `dev-package/RESTART.md` | 호스트 재기동 절차 · env 키 자리 | 안 읽는다 | 껐다 켠 뒤 |
| `CLAUDE.md`(레포 루트) | 상시 지침 — 규칙·범위·게이트·금지 | **읽는다** — `work-item-consistency` ㈕(stage 3 표지) ＋ **매 세션 자동 로드** | 항상 |

### 게이트가 정확히 무엇을 파싱하는가 (형식을 깨면 안 되는 자리)

`gates/tools/work_item_consistency.py` — 검사 8종. 문서 형식을 잡는 것은 다음 넷이다.

- **㈐ `03-HANDOFF §1`** — `section_lines(..., r"^1\.\s*진행도")` 로 `## 1. 진행도` 절을 자른 뒤 그 안의 **모든 markdown 표**를 읽는다.
  열은 **위치가 아니라 헤더명**으로 찾는다 — 식별자 열 = `WU`·`레인`·`항목` 중 하나, 상태 열 = `상태`.
  각 행에서 ⑴ 식별자 열의 **맨 앞** 토큰이 항목 id 여야 하고 ⑵ **상태 열 안의 표기(`✅🟦⬜🟧⛔⏸`)가 정확히 한 종류**여야 한다.
  ⚠ **「비고」 열은 대조 대상이 아니다** — 그래서 이번 회차가 비고 칸을 옮길 수 있었다.
  ⚠ **T-P 표는 상태 열이 산문인 행이 있다**(`P2`·`P5`) — 그 행은 손대지 않았다.
- **㈏ `WORK-UNITS §11`** 완주 체크리스트 **코드 블록 안**의 `<식별자> <표기>` 쌍.
- **㈑ `WORK-UNITS §10`** 착수 후보 표(첫 열이 `WU` 인 표만 대상).
- **㈔ `PLAN-SoT §9`** 표의 결정 번호 `〈n〉` 중복 검출. **`PLAN-SoT` 부재 · `§9` 절 부재 · 번호 행 0건은 전부 red.**
- **㈕ `CLAUDE.md`** 의 `<!-- work-items:after_stage2 -->` … `<!-- /work-items:after_stage2 -->` 괄호 ↔ 대장 `stage: after_stage2` 집합 대조.

`planning-freshness`(`dev-package/tools/check-package-freshness.py`)는 **03-HANDOFF 를 읽지 않는다** —
기획 정본 폴더의 패키지 HTML 임베드 md ↔ 원본 md 의 sha256 대조뿐이다. 이번 회차와 무관하고 green 이다.

### 구조 실측 (정리 직전 판)

- **`03-HANDOFF.md`** 726줄 · **622,019 B**. 머리말(1~175줄)이 **162,299자**로 문서의 45% —
  「이전 최종 갱신」 계열 문단이 **한 줄당 최대 21,058자**로 **약 100개** 쌓여 있었다
  (`최종 갱신` 스택 · `현재 단계` 스택 19줄 · `다음 WU` 스택 11줄 · `다음 세션 진입조건` 스택 20줄).
  `§1` 71,819자 · `§4` 72,263자(닫힌 블로커 `#1`~`#68` 대부분) · `§4.5` 26,374자(지난 회차 블록 누적) · `§5.5` 16,370자.
- **`PLAN-SoT.md`** 676줄 · **1,202,359 B**. **`§1`~`§8` 이 9,518자, `§9` 원장 하나가 673,960자(98.6%)** · 결정 번호 275개.
- **`work-items.yaml`** 119항목 · **525,923 B**. 필드별 누적 = **`note` 118,966자 · `evidence` 107,868자** · `completion_def` 40,653자.
  ⚠ **`evidence` 는 배열이 아니라 긴 산문 문자열**이다(96항목이 문자열 · 23항목이 `None`). 최대 `CT-1` 8,611자.
- **`WORK-UNITS.md`** 141,965 B · **`RESTART.md`** 39,314 B(459줄) · **`CLAUDE.md`** 11,470 B(146줄).

---

## 2. 집행 — 무엇을 어디로 옮겼는가

| 파일 | 전 | 후 | 줄인 것 |
|---|---:|---:|---|
| `dev-package/03-HANDOFF.md` | 622,019 B | **127,198 B** | **−79.6%** |
| `dev-package/RESTART.md` | 39,314 B | **27,511 B** | **−30.0%** |
| `CLAUDE.md` | 11,470 B | **11,470 B** | 무변(감사만 — `§4` 참조) |
| `dev-package/PLAN-SoT.md` | 1,202,359 B | 1,202,359 B | 손대지 않음(제안만 — `§5`) |
| `dev-package/work-items.yaml` | 525,923 B | 525,923 B | 손대지 않음(제안만 — `§5`) |
| `dev-package/WORK-UNITS.md` | 141,965 B | 141,965 B | 손대지 않음(제안만 — `§5`) |

신설 (전부 **원문 그대로 · 한 글자도 고치지 않았다** · 원 줄 번호를 HTML 주석으로 달았다):

| 새 파일 | 크기 | 무엇이 들었나 |
|---|---:|---|
| `dev-package/archive/HANDOFF-HISTORY-2026-08.md` | 340,389 B | 2026-08 회차 문단 ＋ 그때 닫힌 블로커·드리프트 행 (317줄) |
| `dev-package/archive/HANDOFF-HISTORY-2026-09.md` | 107,367 B | 2026-09 회차 문단 ＋ `§4.5` 지난 회차 블록 (67줄) |
| `dev-package/archive/HANDOFF-DETAIL-2026-09.md` | 70,846 B | `§1` 비고 칸 35 ＋ T-P 진입조건 칸 12 ＋ `§5.5` 긴 행 10 |
| `dev-package/archive/RESTART-NOTES.md` | 15,201 B | `RESTART.md` 의 긴 경위·실측 문단 107줄 |

### 03-HANDOFF 에서 남긴 것 / 옮긴 것

- **남겼다** — 신설 `§0 읽기 규칙`(8줄) · **가장 최근 「최종 갱신」 한 문단**(＋ 그 회차의 후속 두 줄: 단독 창 5→5b · `lane-q4` 미병합)
  · `현재 단계`·`다음 WU`·`다음 세션 진입조건` **각 맨 앞 한 줄** · `§1` 전 표(상태 표기 무변) · `§2` 스냅샷 · `§3`·`§3.5` · `§4` **열린** 블로커 8건
  (`#39`·`#40`·`#41`·`#42`·`#44`·`#61`·`#67`·`#69`) · `§4.5` 맨 위 블록 ＋ 확인 표 · `§5.5` 열린 드리프트 · `§5` · 갱신 방법.
- **옮겼다** — 「이전 최종 갱신」·「이전 현재 단계」·「이전 다음 WU」·「이전 진입조건」 문단 전건 ·
  `§4` 닫힌 블로커 행 61건 · `§4.5` 지난 회차 블록 · `§5.5` 닫힌 드리프트(`DR-7`·`DR-8`·`DR-13`·`DR-19`) ·
  `§1` 250자 넘는 비고 칸 · `§5.5` 500자 넘는 행.
- 절마다 **포인터 한 줄**을 남겼다(`§1`·`§4`·`§4.5`·`§5.5` 머리).

⚠ **목표 40 KB 는 달성하지 못했다 — 127 KB 다.** 남은 것의 성격이 이력이 아니라 **현황**이다:
`§1` 표 자체가 50,111자(그중 T-P 27,089자 — 상태 열이 산문인 행은 게이트가 읽으므로 못 건드린다) ·
`§2`~`§3.5` 16,781자(게이트 계수·staging 실측·결정 이행 현황 — 전부 「지금」이다) · `§5.5` 9,040자.
**40 KB 로 내리려면 `§1` 을 표에서 대장 조회로 바꾸거나 `§2`·`§3.5` 를 별도 문서로 빼는 결정이 필요하다** — 그건 판정 사항이라 이 레인이 하지 않았다.

### RESTART 에서 남긴 것 / 옮긴 것

- **남겼다** — `§1` 죽고 사는 표 · `§2` 복구 절차 ①②(태그 한 줄 포함)②-0②-1 · **env 키 표 전건**(시크릿 표 · 접속 URL 파일 표 · 시험용 변수 표 · 게이트 적용 DB 표 · `㉰`·`㉱` 표) · `③` 확인 · `④` DB 세우는 절차와 세 함정 · `㉲` 재부팅 세 가지 · `⑤` Remote Control · `§3`·`§4` 체크리스트.
- **옮겼다** — 릴리스 태그·`:prev`·별칭 경위(44~69줄) · cloudflared 헬스체크 경위(163~173) · `㉯` 미리보기 자리 옛 문면 전건(252~281) · 각 절의 「／ 실측 …」 서술.
- ⚠ **목표 15 KB 미달 — 27.5 KB 다.** 남은 것의 대부분이 **표·명령 블록**이고, 그것이 이 문서의 존재 이유다. 더 줄이려면 env 키 표를 빼야 하는데 그건 지시가 「남기라」고 한 것이다.

---

## 3. 게이트 결과 (이 워크트리 · 2026-09-05)

| 게이트 | 결과 | 비고 |
|---|---|---|
| `work-item-consistency` | **green** | 계수가 정리 전후로 **완전히 같다** — 대장 119 · ㈐ 진실원 대조 **93행** · ㈏ 62 · ㈑ 32 · ㈒ 6 · ㈓ 0 · ㈔ 275 · ㈕ 15 · 검사 대상 밖 8 · 불일치 **0**. 원본과의 대조는 `COLAB_WORK_ITEMS_HANDOFF=<정리 전 판>` 로 같은 게이트를 돌려 확인했다(출력 동일) |
| `work-item-selftest` | **green** | 픽스처 14 — 판정기가 여전히 red 를 낼 수 있다 |
| `planning-freshness` | **green** | 기획 정본 임베드 sha256 — 이번 회차와 무관 |
| `contract-lint` | **green** | — |
| `generated-up-to-date` | **red(준비)** | `frontend/node_modules/.bin/openapi-typescript: No such file` — **이 워크트리에 `node_modules` 가 없어서**다. 문서 변경과 무관(문서만 고친 회차이므로 생성물 입력이 바뀌지 않았다) |
| `stage2-markers` | **red(준비)** | `services/pipeline-worker` 에 venv 가 없다 — 같은 사유 |

⚠ **두 red 는 「준비 실패」이고 판정 red 가 아니다.** 이 레인은 그것을 green 으로 만들려고 검사 대상을 줄이지 않았다(`CLAUDE.md §4`).
본 체크아웃에서 `./gates/run.sh all -j 1` 을 도는 것이 판정의 자리다.

---

## 4. `CLAUDE.md` 감사 — **판정: 지금은 고치지 않는다**

11,470 B · 146줄. 그중 **두 줄이 4.6 KB**다.

- **L15 「AI 두 지점」 ≈ 2.5 KB** — 현재 위상(계보 제안 = 사람이 기본 · AI 는 버튼 · `K3`/`K4` 는 stage 3) **＋ 폐기된 260818 표기의 취소선 이력**이 한 셀에 섞여 있다.
- **L19 「stage」 ≈ 2.0 KB** — stage 1/2 정의 **＋ `〈189〉`·`〈240〉` 개정 경위 ＋ 종전 표기 취소선**. ⚠ **이 줄에 게이트 ㈕ 가 읽는 `<!-- work-items:after_stage2 -->` 괄호가 들어 있다 — 형식을 못 건드린다.**
- 나머지 `§1`~`§8` 은 전부 **규칙·범위·게이트·금지**다. 이력이 아니다. 옮길 것이 없다.

**옮길 수 있는 것 = 두 줄의 취소선(`／ 종전 ~~…~~`·`／ 이전 표기 ~~…~~`) 부분뿐이고, 그것만 걷으면 대략 8.5 KB 다.**
그런데 그 문장들은 **한 문장 안에서 현재 규칙과 이력이 접속사로 엮여 있어** 잘라내려면 **문장을 다시 써야 한다** —
이 레인의 원칙(「고치지 않고 옮긴다」)을 깨고, `AI 두 지점`·`stage` 정의는 의미가 어긋나면 곧바로 범위 판정이 흔들리는 자리다.
⟹ **`CLAUDE.md` 는 무변으로 둔다.** 8 KB 목표는 **Ted 가 두 줄의 재작성을 승인할 때** 별건으로 집행한다.

---

## 5. `PLAN-SoT.md` · `work-items.yaml` · `WORK-UNITS.md` — 제안(집행하지 않았다)

### ㈎ `PLAN-SoT §9` 원장을 백 단위로 쪼갠다

- 실물 = **`§9` 하나가 673,960자 · 결정 번호 275개**(`〈1〉`~`〈326〉` · 결번 있음). `§1`~`§8` 은 9,518자다.
- 안 = `dev-package/PLAN-SoT-ledger/001-100.md` · `101-200.md` · `201-300.md` · `301-.md` 로 **원문 그대로** 가르고,
  `PLAN-SoT.md §9` 는 **번호 → 파일 · 한 줄 제목**의 색인 표만 남긴다(275행 · 약 30 KB).
  ⟹ `PLAN-SoT.md` 가 **1.17 MB → 약 45 KB**.
- **고쳐야 하는 게이트 = `work-item-consistency` ㈔ 하나.** 지금은 `PLAN-SoT.md` 안에서 `§9` 절을 잘라 `〈n〉` 중복을 센다.
  색인 표만 남기면 **색인이 곧 번호의 전수**가 되므로 ㈔ 는 오히려 단순해진다 — 다만 「`§9` 절 부재는 red」 조문과
  `work-item-selftest` 의 `red-h-dupnum`·`red-i-nodecision` 픽스처 둘을 새 배치에 맞춰 고쳐야 한다.
- ⚠ **번호 발급 규칙(`병합 직전 origin/main 최대＋1`)이 색인 한 파일만 보면 되도록 바뀐다** — 이건 이득이다.

### ㈏ `work-items.yaml` 의 산문 필드를 대장 밖으로 뺀다

- 실물 = 525,923 B 중 **`note` 118,966자 ＋ `evidence` 107,868자 ＋ `completion_def` 40,653자**. `evidence` 는 **배열이 아니라 긴 산문 문자열**이다(지시문의 「마지막 3건만 남기기」가 그대로는 적용되지 않는다).
- 안 = 항목별 `dev-package/archive/work-items-evidence/<ID>.md` 로 **원문 그대로** 빼고,
  대장에는 `evidence: "→ archive/work-items-evidence/CT-1.md (2026-09-03 닫힘 · 〈289〉-㉮)"` **한 줄**만 둔다.
  ⟹ 대장이 **526 KB → 약 180 KB**(상태·stage·의존·완료 정의만 남는 기계용 파일로 돌아간다).
- **고쳐야 하는 게이트 = 없다.** `work_item_consistency.py:209` 는 `status: done` 인데 `evidence` 가 **비었는지**만 본다 — 포인터 한 줄로 충족된다. `note` 는 ㈓ 메시지에만 쓰인다.
- ⚠ 대신 **「대장을 열면 근거가 안 보인다」가 생긴다** — 그래서 포인터에 **날짜와 결정 번호**를 반드시 함께 적는 것을 규칙으로 못 박아야 한다.

### ㈐ `WORK-UNITS.md`(141,965 B)

- 게이트가 읽는 곳은 **`§10` 착수 후보 표**와 **`§11` 완주 체크리스트 코드 블록** 둘뿐이다.
- 안 = 완료 정의 산문을 `dev-package/work-units/<WU>.md` 로 가르고 `§10`·`§11` 만 남긴다. **게이트 변경 0.**

### 순서 제안

**㈏ → ㈐ → ㈎.** ㈏ 는 게이트를 안 고치고, ㈐ 도 안 고친다. ㈎ 만 게이트·픽스처를 건드리므로 **단독 레인**으로 마지막에 한다.
셋 다 **Ted 판정 사항**이다 — 이 레인은 값만 재고 멈춘다.
