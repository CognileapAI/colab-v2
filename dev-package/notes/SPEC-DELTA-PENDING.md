# SPEC-DELTA-PENDING — 정본 델타 목록

> **신설 2026-09-03** · 회차 `PLAN-SoT §9 〈293〉` · 워크트리 `lane-spec` · 브랜치 `lane-spec`
> 근거 = `notes/QA-STRATEGY-20260903.md §3-B` — 「판정이 났는데 정본이 뒤따르지 않는다」를 한 파일로 모은다.
> **⚠ 이 파일은 오라클이 아니다.** 값과 근거는 `PLAN-SoT §9` 에 있고 여기는 **미반영 목록**이다.
> 정본 위치 = `<작업공간>/40 COLAB-기획/…` · 이 회차 백업 = `40 COLAB-기획/_backup_260903_SPECSYNC/`

## 1. 이 회차가 반영한 것 (닫힌 델타)

| # | 정본 파일 | version | 절 | 무엇을 적었나 | 인용 판정 |
|---|---|---|---|---|---|
| QA #7 | `E-02/documents/Policy_데이터_찾기.md` | **1.9 → 2.0** | `§8` 표 `Verified 열` 행 신설 ＋ 「Verified 열의 미도착 표기」 근거 블록 · `§12` 이력 | 승인 처리 도착 전 행은 **글자 `Verified` 를 그대로 두되 취소선·회색·꺼진 조작 모양**(`aria-disabled`)으로 그린다. 칸을 비우지 않는다 — 비면 「승인이 아니다」와 「아직 안 왔다」가 갈리지 않는다 | `PLAN-SoT §9 〈282〉`-㉮ (Ted 2026-09-02) |
| QA #8 | `E-05/documents/Policy_프로젝트.md` | **1.6 → 1.7** | `§8` 머리 문단 · `§12` 이력 | 상세 표의 승인 미도착 표기를 `—` → **취소선·회색·비활성 `Verified`**. 카탈로그와 같은 표기. 종전 문면은 취소선으로 남겼다 | `〈282〉`-㉮ 확장 · 등재 `〈291〉` |
| 〈292〉 미결 | `E-07/documents/Policy_홈_대시보드.md` | **1.5 → 1.6** | `§5` `데이터 공개 범위 값 집합` 행 신설 | 값 집합 = **`열림` · `잠김` 2값**. 데이터셋 값이 연구실 기본값을 덮어쓰며 값 집합이 같아 대입 한 번이다 | `PLAN-SoT §9 확정 ㉗` (Ted 2026-08-22) |
| 〈292〉 미결 | `mockups/제품_260817.html` | (판 없음) | 연구실 정보 편집 모달 `데이터 공개 범위` select | 산문 3지선다(`연구실 안에서만 · 요청 시 승인 후 열람` / `같은 학과에 공개` / `목록만 공개`) → **`열림` · `잠김` 2옵션**. 폐기 사유를 주석으로 남겼다 | `확정 ㉗` · 계약 `LabDefaultVisibility → AccessState` |
| QA #16 | `E-07/documents/Policy_홈_대시보드.md` | **1.5 → 1.6** | `§8` `검색 히어로 — 자연어 검색이 그 릴리스에 없을 때` 행 신설 ＋ 근거 블록 · `§12` 이력 | 자연어 검색 UI 가 닫힌 릴리스에서는 **`✦ AI 검색` 배지를 달지 않고** 「이 검색은 데이터에 **적혀 있는 낱말 그대로** 찾아요」를 밝힌다. 히어로의 자리·입력 칸·검색 버튼은 그대로. **종전 행은 지우지 않았다** — 자연어 검색이 있는 릴리스의 규칙으로 유효하다 | `〈136〉`(UI 만 닫는다 · Ted 2026-08-27) · `〈148〉`-㉱(「있으면 거짓말이었다」) · `〈81〉`(stage 1 = tsvector) |
| 〈283〉-㉸-㈏ | `Co-Lab_ver2_업로드_계보_260825_이태헌.html` | (UI 스펙 62건 판) | **UI 스펙 19 「기간 입력」** `desc` ＋ 목업 폼 행 | 「한 칸 자유 입력 · 예시 `2025-06 ~ 2025-09`」 → **두 칸(시작·끝) ＋ 끝은 조건부**. 끝을 비우면 무기한·진행 중, 끝만 적고 시작이 비면 막는다. 종전 문면을 `desc` 안에 명시해 남겼다 | `〈283〉` (Ted 2026-09-02 · 계약 동결 해제 14차 · `DataPeriod.end` nullable) |
| QA #18 | `IA_사이트맵.md` | **1.2 → 1.3** | 4장 데이터셋 상세 · `변경 이력` | `[섹션1]` **계보** · `[섹션2]` **미리보기** 로 순서를 뒤집었다. 구현 변경 0건 | `PLAN-SoT §9 〈296〉`-㉮ · 결정 사실 = `Policy_데이터셋_상세` v2.8 `§4` 105행 「2026-08-17 에 계보를 미리보기보다 앞으로 옮겼다」 — **옮긴 이력을 날짜와 의도로 적은 쪽이 이긴다** |
| 엿보기(`_재검토_E02-E03` 20행 미결) | `E-02/documents/Policy_데이터_찾기.md` | **2.0 → 2.1** | `§8` `엿보기 — 미리보기가 없는 데이터셋` 행 신설 · `§12` 이력 | **버튼을 숨기지 않는다.** 그릴 수 없으면 상세 `§9` 문구 축자(「이 포맷은 아직 미리보기를 지원하지 않아요. 파일을 내려받아 확인해 주세요.」)를 낸다. 팝오버는 만들지 않았다 | `〈296〉`-㉯ · 결정 사실 = 숨기려면 행에 「미리보기 가능」 값이 필요한데 `DatasetRow` 13칸에 없다(`fe-core.yaml:2723`) → 숨김 = **계약 개정** · `§9` 재사용 = **계약 0** |
| QA #27 | `E-00/documents/Policy_공통_기반.md` | **1.4 → 1.6** | `§2.4` `없는 주소` 행 신설 · `§6` 이력 | 중립 문구 「이 주소에는 화면이 없어요.」 ＋ `연구실로 돌아가기`. **묘비 문구 금지를 명문화**했다 | `〈296〉`-㉰ · 결정 사실 = 404 규격이 정본에 **0건**이었고 구현이 그것을 알고 빈 div 로 비워 뒀다(`routes/NotFoundPage.tsx:3`). ⚠ frontmatter 가 1.5 개정을 못 따라와 **1.4 로 멈춰 있었다** — 이번에 함께 정정 |
| QA #16 잔여 | `E-07/documents/Policy_홈_대시보드.md` | **1.6 → 1.7** | `§8` 「`✦ AI 검색` 이 언제 돌아오는가」 행 신설 · `§12` 이력 | 시점 = **날짜가 아니라 계약 값**. 응답에 `SearchResponse.interpretation` 이 실리는 릴리스부터 배지·placeholder 복귀 | `〈296〉`-㉱ · 결정 사실 = `〈87〉`(해제 3차 · `interpretation` 신설) · `〈136〉` ㈎ 「UI 만 닫는다」 |

**패키지 재동기** — 개정한 세 md 의 `<script type="text/markdown" id="md-policy">` 블록을 각 `package/*.html` 에 다시 실었다(`〈281〉` 의 레인과 같은 방식). 실측 = `./gates/run.sh planning-freshness -j 1` **green · 15/15 MATCH**. `PACKAGE-FRESHNESS.md §1` 표는 이 실행 출력으로 15행 전건 재기재했다.

## 2. 멈춘 것 — Ted 판정 대기

### ⓐ QA #18 — 데이터셋 상세 섹션 순서: 정본 내부 충돌 (⛔ 이 회차는 풀지 않았다)

> ✅ **⟨판정 2026-09-03 · `PLAN-SoT §9 〈296〉`-㉮ (Ted 사전 위임 · advisor 권고대로 집행)⟩ 닫혔다 — ⑴ 채택.**
> **Policy 가 정본**이고 `IA_사이트맵.md` 4장을 Policy 순서로 고쳤다(v1.2 → 1.3 · 구현 무변경).
> **아래 원문은 지우지 않는다** — 왜 그렇게 정했는지가 여기 있다. 다만 「멈춘 것」이 아니므로 §2 의 제목 뜻은 이 항목에 더 이상 적용되지 않는다.
> **ⓑ 의 진입조건(「ⓐ 판정」)이 이로써 충족됐다** — `IA_사이트맵` 자연어 검색 서술 7행은 **여전히 미반영**이고 아래 ⓑ 에 그대로 남는다.

**두 정본이 서로 다른 순서를 적는다. 어느 쪽도 지우지 않고 그대로 옮겨 적는다.**

**갑 — `E-03/documents/Policy_데이터셋_상세.md` (v2.8)**

- `§3.1` (78행) 축자 — 「결과: **기본 정보 → 계보(원천·가공 방식·확인 이력) → 미리보기 → 활용 문장**까지 보고 내려받는다.」
- `§4` 판단 순서 표 (105행) 축자 — 「판단 순서 ｜ **뭔가 → 믿을 수 있나 → 어떻게 생겼나 → 어떻게 쓰였나** ｜ 섹션 배치 순서와 같다. **2026-08-17 에 계보를 미리보기보다 앞으로 옮겼다**」

**을 — `IA_사이트맵.md` 4장 (106~118행)**

- 「[헤더] "이게 뭔가" : 이름 · 기본정보 · Verified 배지」
- 「[섹션1] "**어떻게 생겼나**" : 미리보기 시각화」
- 「[섹션2] "**믿을 수 있나**" : 계보(족보) 그래프」
- 「[섹션3] "어떻게 쓰였나·가져가기" : 활용 프로젝트(논문) · 다운로드」

**실물** = 구현은 **갑(Policy)** 을 따른다 — 헤더 → 기본 정보 → 계보 → 미리보기(`QA-SCREENS-20260903.md` 18행 · 스크린샷 04).
**갈리는 지점** = `Policy` 는 2026-08-17 에 **일부러 계보를 앞으로 옮겼다**고 이력에 적었고, `IA_사이트맵` 은 그 개정 전 순서 그대로다.
**물음(Ted)** = **어느 쪽이 정본인가.** ⑴ Policy 를 정본으로 두고 `IA_사이트맵` 4장을 고친다(구현 무변경) ⑵ IA 를 정본으로 두고 Policy 와 구현을 되돌린다(화면 재배치 1건).
**되돌리는 비용** = ⑴ 문서 1행 · ⑵ 문서 2행 ＋ 화면 섹션 순서 변경 ＋ `#sec-*` 앵커 재확인.

### ⓑ QA #16 의 잔여 — `IA_사이트맵.md` 의 자연어 검색 서술

`IA_사이트맵.md` 20·65·68·91·125·145·159행이 자연어 검색을 **있는 것으로** 서술한다(`Policy_홈_대시보드` 는 이 회차에 릴리스별로 갈랐다).
**이 회차는 손대지 않았다** — 같은 파일 4장이 ⓐ 의 판정 대상이라, **한 번의 판정 뒤 한 회차에 고치는 편이 싸다.** 진입조건 = ⓐ 판정.

## 3. 멈추지 않았고 반영도 하지 않은 것 — 검수 오류 정정

### QA #30 「`엿보기` 버튼 — 정본에 없는 기능명」 = **검수 오류. 정본 갱신 불요.**

`E-02/documents/Policy_데이터_찾기.md` 가 **세 자리에서 축자로 적고 있다** (이 회차 개정 전 v1.9 기준 · 행 번호는 v2.0 판) —

- `§2` 업무 흐름 — 「행에 마우스를 올린다 ｜ 오른쪽 끝에 **빠른 작업(엿보기·다운로드)** 이 나타난다. 잠긴 행에는 나타나지 않는다」
- `§5` 데이터 규칙 — 「빠른 작업 ｜ **엿보기 · 다운로드**. 행에 마우스를 올렸을 때만 나타난다. 잠긴 행에는 두지 않는다」
- `§8` 화면 동작 — 「행 빠른 작업 ｜ … 오른쪽 끝에 **엿보기·다운로드**가 나타난다 …」

목업 `mockups/제품_260817.html` 에도 `title="엿보기"` 행 빠른 작업 버튼이 실재한다. **「정본 침묵」이 아니다.**

⚠ **다만 옆에 진짜 미결이 하나 있다** — `_재검토_E02-E03_260826.md` 20행 축자: 「**엿보기가 무엇을 띄우는지 정의가 없는데**, 지원 외 확장자 데이터셋은 띄울 그림 자체가 없다. **미리보기 없는 데이터셋의 엿보기**를 정해야 한다」. 이 회차는 이것을 정하지 않았다 — **Ted 판정 대상이고 QA #30 과는 다른 물음이다.**

## 3-b. 실물이 정본과 어긋난 자리 — 등재만 하고 고치지 않은 것 (`〈296〉` 회차 신설)

**여기 셋은 「정본이 판정을 못 따라온 것」이 아니라 「실물이 정본과 다른 것」이다.** §2 와 성질이 반대라 절을 나눈다.

### ⓒ 엿보기 = 상세 이동 — 정본 `§8` 164행과 어긋난다 (⛔ 이 회차는 고치지 않았다)

- **실물** = `frontend/src/components/catalog/CatalogTable.tsx` 엿보기 버튼의 `onClick` 이 `props.onOpen(row.datasetId)` — **상세로 이동한다.**
- **정본** = `Policy_데이터_찾기` `§8` `행 빠른 작업` 행 축자 「**상세로 들어가지 않고** 확인하거나 받는다」.
- **어긋남** = 엿보기는 상세로 들어간다. 즉 이 행의 목적 절반(「상세로 들어가지 않고」)이 실물에 없다.
- **이 회차가 한 것** = 「미리보기 없는 데이터셋을 어떻게 하나」(`_재검토_E02-E03` 20행 미결)에 답을 적었다(`§8` v2.1 · **숨기지 않는다**). **엿보기 표면(팝오버) 자체는 만들지 않았다** — 권고 ⓑ 는 이 회차 범위 밖이다.
- **지금 사용자가 겪는 것** = 엿보기 = 상세 이동인 동안 「미리보기 없음」의 답은 **상세 `§9` 가 이미 낸다**(369행). 그래서 **결정이 급하지 않다.**
- **닫히는 조건** = 엿보기 팝오버를 만드는 회차. 그때 상세 미리보기 패널을 재사용하고 실패 5종(v2.0 이력)과 `§9` 문구를 그대로 낸다.

### ⓓ 연구실 전환기 `▾` — 코드에서 뗐고, 정본 도식은 그대로다 (⛔ 도식을 고치지 않았다)

- **실물(개정 후)** = `frontend/src/shell/Gnb.tsx` 의 `<span className="cv">▾</span>` 를 **걷었다**(`〈296〉`-㉰). 사유 = 전환 목록·동작이 P0 범위 밖이라 **없는 메뉴를 표기로 약속하지 않는다** · 계정 1 · 연구실 1 인 지금 전환이라는 동작 자체가 없다.
- **정본** = `Policy_공통_기반` `§1` 30행 GNB 도식 축자 `[연구실 전환기 ▾]   연구실 · 프로젝트 · 데이터셋   ←여백→   [⬆️ 업로드]  [연구실 설정]  [아바타 ▾]`.
- **어긋남** = 도식은 `▾` 두 개(전환기·아바타)를 그리고 있고 실물에는 전환기 쪽이 없다. **도식은 한 글자도 고치지 않았다** — 전환기가 돌아오는 회차에 `▾` 도 같이 돌아오므로, 지금 도식을 고치면 그때 두 번 고친다.
- ⚠ **아바타 쪽 `▾` 는 손대지 않았다** — 아바타 메뉴가 `로그아웃` 1항뿐이면 그것은 메뉴가 아니라 버튼이라는 권고가 따로 있고, **이 회차는 그것을 집행하지 않았다**(아래 백로그).
- **닫히는 조건** = 연구실 전환 기능이 들어오는 회차(백로그) — 그때 실물이 도식으로 돌아온다.

### ⓔ 후속 열림 — 이 회차가 등재만 한 것

| 항목 | 내용 | 왜 지금 안 하나 |
|---|---|---|
| 연구실 전환 기능 | 전환 목록·전환 동작. `▾` 는 이때 함께 돌아온다 | 계정 1 · 연구실 1 인 지금 **전환할 대상이 없다.** 대상 0건 위에 UI 를 세우면 검수할 수 없다 |
| 아바타 메뉴 → 버튼 | `로그아웃` 1항이면 메뉴가 아니라 버튼이다 | `▾` 제거와 같은 성질이나 **정본 도식이 아바타 `▾` 를 명시**한다 — 전환기 쪽과 달리 정본 개정이 선행이다 |
| `tile-` 키 생존 판독기 | `ownership.scan()` 이 아닌 **별도 판독기**로 세운다. 규칙 = 「살아 있는 데이터셋 파일 또는 보류 업로드가 **하나라도** 가리키면 산다」 | `〈296〉`-㉶ 참조. `scan()` 확장은 **하지 않기로 판정**됐다 |

## 4. 이 회차가 재지 않은 것

- `./gates/run.sh all` **미실행** — 같은 호스트에서 다른 레인이 `all` 을 돌고 있었다(`CLAUDE.md §1-b` ⑸). 이 회차가 돌린 것은 `planning-freshness` · `work-item-consistency` 둘뿐이고 **나머지는 「이 회차가 재지 않았다」로 남는다.**
- 코드·계약·DB·마이그레이션·staging **무접촉** · 배포 0 · push 0 · 병합 0.
- 목업 HTML 은 `planning-freshness` 의 판정 대상이 **아니다**(문서 임베드만 본다 · 화면·목업 최신성은 `WU-G1b`). `제품_260817.html`·`Co-Lab_ver2_업로드_계보_260825_이태헌.html` 의 변경은 **어느 게이트도 재지 않는다.**
