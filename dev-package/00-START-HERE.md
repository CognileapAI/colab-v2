# 00 · START HERE — CoLAB v2 개발 패키지

> 이 폴더 하나로 v2 개발을 끊김 없이 이어간다.
> 목표는 **v2 전체 완성 + AWS 배포**. 코드는 v2 기준으로 새로 짓고, PoC·v1은 참조만 한다.

## 문서 지도

| 파일 | 무엇에 답하나 | 주 독자 |
|---|---|---|
| `00-START-HERE.md` | 이 인덱스 | Ted |
| **`DOMAINS.md`** | **무엇을 무엇과 섞지 않는가** — 도메인 10개, 분할 기준 2개, 경계 강제 규칙 | 전원 |
| **`WORK-UNITS.md`** | **무엇이 끝나야 끝인가** — 완료 정의, 작업 단위, 의존 그래프, 지금 착수 가능한 것 | 전원 |
| `PLAN-SoT.md` | 왜 이렇게 짓는가 — 아키텍처, 계약 거버넌스, 지식 레이어, AWS, 결정 로그 | Claude + Ted |
| `REPO.md` | 어디에 짓는가 — 저장소 구성(모노레포 권고), 기존 레포 처분 | Ted |
| `03-HANDOFF.md` | **지금 어디까지 왔는가** — 매 세션 갱신 | Ted + Claude |
| `01-CLAUDE-DRIVER.md` | 세션 구동 규약 + 킥오프 프롬프트 | Ted → Claude |
| `02-DEV-PLAYBOOK.md` | 전체 여정과 Ted가 내릴 결정 | Ted |
| `sessions/` | 단계별 자기완결 작업지시서 | Claude |

읽는 순서는 **DOMAINS → WORK-UNITS → PLAN-SoT**. 앞의 둘이 경계와 완료를 정하고, 세 번째가 그 이유를 담는다.

## 지금 어디인가

```
게이트·인프라·지식추출 (병렬 착수 가능)  →  계약 동결  →  P0 + walking skeleton 배포
                                                              →  P1~P7 ∥ K1~K5  →  prod
      ▲
    현재
```

착수 가능한 WU는 `WORK-UNITS.md §10`. 먼저 볼 것 셋 — **R0**(레포 구성, 판단은 짧다) · **I0**(AWS 계정·결제) · **G2**(자연어 검색 엔진).

## 원칙 여섯

1. **경계가 먼저다.** 도메인 경계는 협업(누가 소유하나)과 컨텍스트 윈도우(에이전트가 담을 수 있나) 두 기준으로만 긋는다.
2. **계약 동결 → TDD → 게이트 통과.** 순서를 바꾸지 않는다.
3. **테스트가 오라클이다.** 오라클 없는 WU는 쪼갠다.
4. **AI는 제안만 한다.** D10은 D4(계보)에 쓰기 경로가 없다 — 규칙이 아니라 구조로.
5. **배포가 완료다.** I2 이후 모든 WU의 완료 판정에 staging 배포 green이 붙는다.
6. **절대경로를 문서에 적지 않는다.** v1의 HANDOFF가 이 규칙이 없어 실제로 틀렸다.

## 3단계로 시작

1. `02-DEV-PLAYBOOK.md` 통독
2. `WORK-UNITS.md §10`의 착수 가능 WU 중 **I0 · G2**부터 연다 (리드타임 최장)
3. 개발 세션은 `01-CLAUDE-DRIVER.md`의 킥오프 프롬프트 → 이후 `03-HANDOFF.md`가 다음 WU를 가리킨다
