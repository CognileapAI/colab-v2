# 03-HANDOFF 이력 — 2026-09

> **`dev-package/03-HANDOFF.md` 에서 옮겨 온 이력이다** — 2026-09-05 문서 정리 회차.
> **한 글자도 고치지 않았다.** 원 문서의 줄 번호를 함께 적는다(정리 직전 판 기준 · 726줄).
> 자리 — 머리말 「이전 최종 갱신」 문단 · `§4` 닫힌 블로커 행 · `§4.5` 지난 회차 진입조건 블록 · `§5.5` 닫힌 드리프트 행.
> 되찾는 법 = 이 파일에서 `grep`. 새 세션은 여기를 열지 않는다.

---


<!-- 원 03-HANDOFF.md L15 -->
／ 이전 **최종 갱신** 2026-09-05 (**★ 이 회차 = 원장 등재 회차 — `V-1` 종결 ＋ `TL-2`·`BF-12` 신설. 등재 `PLAN-SoT §9 〈321〉`·`〈322〉`·`〈323〉`·`〈324〉`.** ⛔ **원장·산문 전용 — 코드·계약·DB·인프라·staging 무접촉 · 배포 0 · 병합 0.** **값·근거는 그 네 항목에 있다.** ⑴ **`〈321〉`** = `V-1` 코드 집행(워크트리 `lane-v1` · 기준 `main` `3f31bc0` · 초안 `notes/V1-LEDGER-PENDING.md`) — 없던 것은 기구가 아니라 **`apply` 를 부르는 호출 한 자리**였다 · 시험 8건 신설 · 게이트 green 50 / red 0 · 자리 **벌 9·파일 21 → 벌 3·파일 7**. ⑵ **`〈322〉`** = staging 실물 증명(근거 `sessions/V-1-STAGING-20260905.md`) — 자리 파일 **306 → 306(델타 0)** · 옛 3키 `50dc2d4d`·`ad863d60`·`f48dc45c` 가 **14:57:24.173** 에 소멸하고 **「완료」는 24.306 에 발행**(＝ `〈317〉` 의 staging 실물 판) · `color` 6칸만 갈렸다 · **완료 정의 8/8** ⟹ 대장 `V-1` **`open` → `done`**. ⑶ **`〈323〉`** = Ted 판정(구두 「1,2 권고대로」) — 재굽기로 안 덮이는 구판 **16벌**(사이드카 부재 14 ＋ 원천 원장 부재 2)의 처분을 **`TL-1` 기구의 소유 판정에 등급을 더해** 회수한다 ⟹ 항목 **`TL-2` 신설** · `〈315〉`-㉯ 대체(원문 보존). ⑷ **`〈324〉`** = **`BF-12` 신설** — 관측 전용의 관측이 안 된다(98분 무로그 · 원인은 `viz-render` 전수 로그 설정 0건). **`[미확인]` 하나** = 화면 팔레트 갱신 육안 확인(팔레트 선택 UI 부재 · `J-6` · `V-1` ⑺ 이 명시로 범위 밖에 뺀 자리 ⟹ 완료를 막지 않는다).**)

<!-- 원 03-HANDOFF.md L16 -->
／ 이전 **최종 갱신** 2026-09-05 (**★ 이 회차 = staging 단독 창 5b 단계 B — 배포 `e37808cace69` GREEN 으로 `BF-4` 를 닫았고, 「재굽기로는 구판이 안 덮인다」가 실측으로 서면서 `〈315〉`-㉯ 가 반증됐다. 등재 `PLAN-SoT §9 〈318〉` · 초안 `notes/A5B-LEDGER-PENDING.md` · 전문 `sessions/WINDOW-20260905-A5b.md §10`.** 워크트리 = 이 등재 레인(기준 `main` `48744f2`) · 창 자체는 `lane-window-a5b`. **값·근거는 그 등재 행과 `§10` 에만 적는다.** ⑴ **배포 GREEN** — Ted go/no-go ① GO → 원장 `approve` `13:20:36` → `deploy` `13:22:17` · 통과 15 · SKIP 0 · red 0 · 롤백 미발동 · 서빙 릴리스 **`e37808cace69`** · **8/8 healthy** · 체인 무변(platform `0011` · ai `0005`). ⑵ **첫 접촉 4건 전부 200** · 관측 최대 **410 ms** ⟹ `〈304〉` 스파이크(1,164 · 2,111 ms)가 **두 회차 연속 재현 실패** — 후보 ⓓ(재기동 첫 접촉)로는 설명되지 않고 **원인은 `[미확인]`** 그대로다. ⑶ ⛔ **`〈315〉`-㉯ 반증** — 재굽기 전후 소유 계수 `127/108/0/0/19` → `136/117/0/0/19` 이고 **판정 불가 19 의 캐시 키 집합이 전후 동일**(사라진 벌 0 · 새 벌 9). **재굽기는 새 키에 벌을 세울 뿐 옛 키의 구판을 안 덮는다** ⟹ **구판 회수는 재굽기가 아니라 `invalidation`** 이고 **남은 16벌의 처분은 새 Ted 판정**이다. ⑷ **재굽기 3 데이터셋 · 실패 0**(대상은 창에서 실측해 먼저 적고 커밋 — `aa04792`). ⑸ **`BF-4` `in_progress` → `done`** — GK-2A 포함 3 데이터셋에서 캡션 `821 × 1024` 가 섰다(Ted Q2 충족). ⑹ ⛔ **`V-1` 은 `open` 그대로** — `§6(vii)㉡`(팔레트 재렌더 실물 관측)이 **인가된 쓰기 목록 밖**이라 집행하지 않았다 ⟹ `〈317〉` 의 staging 실물 증명도 함께 **`[미확인]`**. ⑺ **`TL-1` 첫 관측 전용 배포** — 회수 3키 전부 빈 값(꺼짐) · 삭제 0건 실측 · ⛔ **루프는 「못 봤다」**(경과 30분 · 주기 3600초). ⑻ **게이트 50/0/0** · `service-tests-viz-render` **합 9회 연속 green**. **코드 0줄 · 계약 0 · 마이그레이션 0 · 삭제 0 · 롤백 0.**)

<!-- 원 03-HANDOFF.md L17 -->
／ 이전 **최종 갱신** 2026-09-05 (**★ 이 회차 = `V-1` 간헐 red 종결 — 「완료」가 회수보다 먼저 발행되고 있었다. 고친 것은 시험이 아니라 순서다. 등재 `PLAN-SoT §9 〈317〉` · 초안 `notes/V1-FLAKE-LEDGER-PENDING.md`.** 워크트리 `lane-v1fix` · 기준 `main` `1ae5dc0` · 커밋 `1eb247f`(수정) · `71ea681`(〈317〉 등재). **값·근거는 그 등재 행에만 적는다.** ⑴ **근본 원인** = 「완료」(`STATUS_DONE`)가 회수·청소보다 **먼저** 발행됐다 — `_run` 이 상태를 세우고 색인 등재·회수 범위 계산·`invalidation.apply()` 는 `_run_and_plan` 에서 그 뒤에 돌았다(실측 창 ≈15 ms · 시험 폴링 20 ms). ⑵ **수정** = 회수가 끝난 뒤 **대입 하나로** 「완료」를 발행한다(`finally` · 원자적 · 잠금 0). 계약 개정 0 · 마이그레이션 0 · 배포 0. ⑶ **회귀 가드 2건** 신설(red-first 실측) — 「화면이 완료를 본 그 순간 옛 벌은 이미 지워져 있다」 ＋ 「회수가 실패해도 렌더는 완료로 선다」. ⑷ **게이트** = `service-tests-viz-render` **5회 연속 단독 green** · 수집 **326**(기준선 324 → 신설 2) · failed 0 · errors 0. ⚠ **대장 `work-items.yaml` 의 `V-1.status` 는 그대로 `open` 이다** — 이 회차가 닫은 것은 간헐 red 이지 항목이 아니다.

<!-- 원 03-HANDOFF.md L19 -->
／ 이전 **최종 갱신** 2026-09-05 (**★ 이 회차 = `TL-1` 종결 — 판독기의 지목을 **도는 배경 루프**가 집행한다. 등재 `PLAN-SoT §9 〈316〉` · 초안 `notes/TL1-CLOSE-LEDGER-PENDING.md`.** 워크트리 `lane-tl1b` · 기준 `main` `6d20bd7`. **값·근거는 그 항목과 `TL-1.evidence` 에만 적는다.** ⑴ **Ted 판정** = 회수 주체·주기는 `#60` 이 세운 **주기 배경 루프**다(새 스레드·HTTP 표면·계약 개정 0). ⑵ **주체는 저장소 루트에서 얻는다** — 「D7 에는 원장이 없어 디렉터리가 곧 사실」 ＋ core-api 가 원장에서 사라진 파일의 바이트도 치운다 ＋ 그릴 때 쓰는 입력도 같은 자리 하나 ⟹ **판독 입력 = 렌더 입력.** ⑶ **가드 넷** = 관측 전용 기본 · 상한 20벌/바퀴 · fail-closed 3종 · 주기 3600초(＋ 지운 키마다 등급·나이 로그). ⑷ **계획기 셋째 신설 0 · 지우는 문은 `apply()` 하나** — `plan()` 의 타일 보존은 기본값 그대로이고 명시로만 여는 문이다. ⑸ 시험 **21건** 신설(RED 선실측) · `viz-render` **364** 전건 통과 · 전 게이트 **green 50 / red(판정) 0 / red(준비) 0**. ⚠⚠ **`TL-1` `done` 은 「기구가 섰다」이지 「실물이 지워졌다」가 아니다 — 실물 삭제 0건 · staging 무접촉 · 배포 0.**)

<!-- 원 03-HANDOFF.md L20 -->
／ 이전 **최종 갱신** 2026-09-05 (**★ 이 회차 = `TL-1` 지도 타일 생존 판독기를 세우고 자리를 처음으로 셌다. 등재 `PLAN-SoT §9 〈313〉` · 초안 `notes/TL1-LEDGER-PENDING.md`.** 워크트리 `lane-tl1` · 기준 `main` `ea6d4fa` · 근거 `sessions/TL-1-20260905.md`. **값·근거는 그 문서와 `TL-1.evidence` 에만 적는다.** ⑴ **「생존」 = 키 재계산** — 타일에는 소유 필드도 사이드카도 없어 「누구 것인가」가 아니라 **「지금의 주체가 낳는 이름인가」**를 묻는다(`〈294〉` 승격분 ＋ `map_tile_content_key`). ⑵ **제3의 상태를 세웠다** — 주체의 본체를 못 열면 「고아」가 아니라 **계산 불가**이고 **판정을 시작하지 않는다**(진입점 종료 코드 78 = red(준비)). ⑶ **첫 참값** — 주체 **125**(보류 업로드 **0** — 접수분 17건 전건 등록 전환) · 후보 키 157 · 자리의 타일 **2벌** ⟹ 축자 `살아 있다 2 · 접수분에만 닿는다 0 · 고아(못 닿는다) 0 · 판정 불가 0 · 계산 불가 0` · **가장 오래된 못 닿는 벌 = 없다.** ⚠ **이 0 은 「결손이 없었다」가 아니다** — 종전 값은 `[미확인]` 이었다. ⛔ **`〈312〉` 의 stale 3벌은 `tile-` 벌이 아니다**(③지도형 **렌더** 산출물 · `CR-2` ⑶ 소관). ⑷ **`ownership.py` 무변경 · 게이트 신설 0**(접수분 루트가 도커 볼륨이라 세우면 매 회차 red(준비) — 없는 자리를 지어내지 않는다). ⑸ **`TL-1` `open` → `in_progress`** — ⑷ 후단(`invalidation` 결선)과 ⑹(회수 주체·주기 `[미확인]`)이 남아 **닫지 않는다.** 계수 무변. **삭제 0건 · staging 쓰기 0 · 배포 0 · push 0 · 계약 0 · 마이그레이션 0.**) ／ 이전 **최종 갱신** 2026-09-04 (**★ 이 회차 = `VL-1` 값 조회 초기화 지연의 원인을 구간으로 갈랐고 `V-2` 가 닫혔다. 등재 `PLAN-SoT §9 〈311〉`.** 워크트리 `lane-vl1` · 기준 `main` `d86a1db` · 근거 `sessions/VL-1-20260904.md` · 표본 원값 `sessions/VL-1-SAMPLES-20260904.tsv`(400건) · advisor 자문 1회. **값·근거는 `〈311〉` 에만 적는다.** ⑴ **「냉시동」은 D7 안에 없었다** — 같은 `POST` 가 nginx 직접 **17 ms** · 공개 엣지 **279 ms**(TCP 46 ＋ TLS 44 ＋ Cloudflare·터널 왕복 189). 결정적 사실 = **가장 느렸던 표본(DEM)이 서버 일을 가장 적게 하는 경로**다. 후보 기각 셋 = 페이지 캐시 · 키 재계산 · 워커 첫 요청. ⑵ **성능 고침 0건 · 계측 1건**(`Server-Timing` 구간 · viz-render 4 ＋ core-api 3 · 저쪽 구간 무해석 통과 · **계약 몸통 무변경** · 시험 6건 red 확인 후 green · advisor 권고대로 집행). ⛔ 측정이 지목하지 않은 `sourceDigest` 캐시는 **넣지 않았다**. ⑶ **재측정 = 유휴 33분 이상 × 5 회차 · n 400 · 오류 0 · 표본 원값 전건 보존** — 첫 접촉 **데이터셋별** p95 **338 / 350 / 330 ms**(대조군 333) · 관측 최대 **350 ms**. ⭑ **⟨정정 2026-09-04 · 수용 검토⟩** ~~nginx 경로는 찬 상태와 따뜻한 상태가 같다(서버에 유휴 벌금 없음)~~ — `cold_ngx` 20 표본은 **엣지 첫 접촉 1~4 초 뒤**라 프로세스가 이미 따뜻하다. 서버 유휴 벌금은 엣지 찬/따뜻한 차이로만 묶어 **≤ 약 125 ms(상한)**. ⑷ **`V-2` ⑻ 충족** ⟹ `V-2` `in_progress` → `done` · `VL-1` `done` · `stage2` 미완 **10 → 8**. ⛔ **「고쳐서 통과」가 아니다** — 성능 고침 0건 · `〈304〉` 스파이크(1,164 · 2,111 ms)가 **첫 접촉 20 표본에서 재현되지 않았다** · 그 원인은 **`[미확인]`**. ⚠ **서버 단독 p95 도 여전히 `[미확인]`** — 계측은 섰으나 **배포 전이라 staging 응답에 헤더가 없다**. ⚠ 프로세스 재기동 직후 첫 요청 비용 `[미확인]`(재시작 0회) · 바깥 망 관점 `[미확인]`(측정은 호스트 발). 게이트 = 병합 ref 단회 `all -j 1` 축자 **`── 계 : green 44 / red(판정) 5 / red(준비) 1`** ⟹ 귀속·재측정 뒤 **green 49 / red(판정) 1**(`schema-diff` — 게이트용 적용 DB 컨테이너 부재 · 호스트 환경) · 그중 **이 레인 소유의 red 1건**(새 시험 허용오차 `1e-6` < 헤더 반올림 `.3f` ⟹ `2e-3` 으로 고쳐 green) · 시험 실측 viz **279** · core **626** · `work-item-consistency` green. **staging 쓰기 0 · 배포 0 · push 0 · 병합 0.**) ／ 이전 **최종 갱신** 2026-09-04 (**★ 앞 회차 = 버그 회차 — Ted 접수 UI 12건을 레인 5 로 수정하고 대장에 `BF-` 접두사를 세웠다 · 등재 `PLAN-SoT §9 〈310〉` · 기록 `sessions/BUG-20260903.md` · 보고 사본 `reports/bug-20260903/`.** 대장 신설 = `BF-1`~`BF-6` ✅(수정 완료 · 커밋 8) · `BF-7`~`BF-11` ⬜(등록만). **값·근거는 `〈310〉` 과 그 항목에 있다.** ⑴ **Ted 판정 5건(Q1~Q5)** — 확대 한계는 `maxScale` 규칙 유지 ＋ 처음부터 한계 안내 · 상세에 원본 격자 캡션 · 미리보기 이름표 ＋ 범례 변수명 · 바탕지도는 미착수(격자선 옵션만 후보 등록 `BF-10`) · 설명문 `／` 는 표시만 분할(**정본 §8 「한 줄 요약」 문구 확인은 대기**). ⑵ **③지도형 리샘플 정의 변경** — 「warp 후 격자 평균」 → 「축소 후 근사 최근접」 · 캐시 라벨 `warp+nearest` · 등재 한계 둘(`PREVIEW-IMPLEMENTATION §5.3`). ⑶ **버그 아님 둘** — Ted 10 배율 차이(컨테이너 폭) · Ted 11 정체불명(DEM 페이지 오독 → 이름표로 해소). ⑷ **레인별 실측** — 계보 vitest 575/575 · viz-render 유닛 269 → 275 · 확대·이름표 580/580 · 프로젝트 CSS 573/573 · 설명문 575/575 · `tsc` 전건 0 · **전 레인 RED 선실측**. ⑸ ⛔ **배포 0 · push 0 · 병합 0 · staging 무접촉** — 코드는 통합 브랜치 `bugfix/20260903-ui-12` 로 모이고 있고 이 회차는 원장만 고쳤다. ⑹ ⚠ **배포 순서** — `BF-2` 배포 → 그 뒤 재굽기 1회 더(`〈310〉`-㉲ · 지고 가는 항목 `CR-2` ⑶ — `RC-1` 은 `〈309〉` 로 done · 판정 불가 19 유지 · 그 처분은 `〈309〉`-㉶⑴). ⑺ **계수** — `stage1` 미완 **0 → 5**(`BF-7`~`BF-11` · `〈258〉` 규칙 ④) · `PA-G` 개시 조건 값 **10 → 15** · 항목 **106 → 117**. ⭑ ⟨재계수 2026-09-04 · advisor 게이트 ③ · `BF-1`~`BF-6` `done → in_progress`(완료 = staging 배포 green · `CLAUDE.md §0`)⟩ `stage1` 미완 **5 → 11** · `PA-G` 개시 조건 값 **15 → 21** · 총 미완 **16 → 22**(`PA-G` 포함 · `deferred` 제외) — 대장 117 항목 실측. ⑻ **다음** = 통합 브랜치 게이트 전수 → advisor 게이트 ③ → PR → 배포 → 재굽기. ⭑ ⟨병합 2026-09-04⟩ `origin/main` 의 `〈309〉`(`RC-1` 회수 1회 · `done`)와 번호가 충돌해 이 회차를 **`〈310〉`** 으로 재번호했다 — `RC-1.entry_conditions` 증보는 철회(done 항목은 건드리지 않는다) · 순서 캐비어트는 `CR-2` ⑶ · `BF-2.note` · `§4.5` ⑷ 로 이관. ⚠ **PR #3 CI `planning-gates` red 는 이 회차가 만든 것이 아니다** — `planning-freshness` 가 「정본 폴더가 없다」로 fail-closed(정본은 레포 밖 형제 폴더 `40 COLAB-기획` · CI 는 체크아웃하지 않는다) · `main` 자체의 최신 실행(`33782941452` · `〈309〉` 커밋)도 같은 사유로 red · 병합 트리 로컬 실측은 green(15 임베드 전건 일치) · 지고 가는 항목 `CR-2` ⑸.) ／ 이전 **최종 갱신** 2026-09-04 (**★ 앞 회차 = `RC-1` 회수 1회를 집행했다 — 고아 14 벌(28 파일)만 지웠고 서 있던 게이트 red 가 닫혔다. 등재 `PLAN-SoT §9 〈309〉`.** 워크트리 `lane-rc1` · 기준 `main` `a6d4f1d` · 근거 `sessions/RC-1-RECLAIM-20260904.md` · Ted 승인 2026-09-04 「전수 스냅숏 후 회수」 · advisor 게이트 ③ go. **값·근거는 `〈309〉` 에만 적는다.** ⑴ **되돌릴 길을 먼저 세웠다** — 짝 백업을 회수 **전에** 새로 떴다(직전 짝은 재굽기 전이라 지금 자리를 못 담는다): `platform-20260904T013647` ＋ **`vol-previews-20260904T013726`(255건 · 매니페스트 전건 일치)**. 지운 28 파일이 그 안에 있다. ⑵ **회수 전 전수 스냅숏 255 줄** = `sessions/RC-1-SNAPSHOT-20260904T0138.tsv`(게이트 5열에 `sidecarVersion`·`baked_for`·`reason` 을 더한 8열). ⑶ **키 집합이 두 경로에서 같았다** — `reclaim_plan()` 의 고아 14 키와 게이트가 이름으로 뱉은 14 키의 정렬 sha256 이 양쪽 **`b5d5f004f9ddd6d7`** · `diff` 0줄. `stale 28 ＋ kept 227 = 255` = 자리 실물. ⑷ **음성 조건 전건** — 판정 불가 19 · 살아 있다 84 · 지도 타일 2 가 **전건 `kept`**(누락 0 · 침범 0) · `OutOfScope` 0 · 부분 삭제 0 · SQL 쓰기 0 · 컨테이너 수명 조작 0 · 파괴 플래그 0 · hand-`rm` 0. ⑸ **계수 전 → 후** = 대상 `117→103` · 살아 있다 `84→84` · 접수분 `0→0` · **고아 `14→0`** · 판정 불가 `19→19` · 자리 파일 `255→227`. ⟹ **`artifact-ownership` 이 red(판정) → green** — `〈308〉` 이후 서 있던 **유일한 red** 였다. ⑸-b **전 게이트 `green 50 / red(판정) 0 / red(준비) 0`** — `〈308〉` 의 `49/1/0` 에서 **닫힌 red 1 · 새로 난 red 0** · 못 돈 게이트 0건. `work-item-consistency` green(워크트리 실행). ⑹ **지운 14 벌은 등록되지 않고 버려진 접수분 3건의 그림이다** — `source` fileId 셋이 `d3_file` **0행** · `d5_upload_file` **0행**, `baked_for.target_id` 3건이 `d3_dataset` **0행**. ⟹ 등록된 데이터셋의 화면은 하나도 바뀌지 않는다. ⑺ **현물 확인 전후 `diff` 0줄** — 렌더 3건 × 3층 **9층 전건 200**(바이트 수까지 동일) · 값 조회 3건 200 `available true`(`〈308〉` 의 값과 글자 그대로 같다). ⑻ **`RC-1` = `done`** — 완료 정의 4조 전건. ⚠ **⑶ 의 「판정 불가 49→19」는 `〈308〉` 이 충족한 것이고 본 회차는 「19 유지」를 확인했다** — 앞 회차 값을 이번 값으로 옮겨 적지 않았다. ⚠ **면제 선언은 비운 채다**(`[exempt] keys = []`) — **회수로 닫았지 선언으로 닫지 않았다.** ⚠ **재지 않은 것** = 판정 불가 19 벌의 처분 `[미확인]` · 캐시 키가 갈린 원인의 귀속 `[미확인]` · 회수한 용량 합계 미산출. **push 0 · `main` 병합 0 · 배포 0.**) ／ 이전 **최종 갱신** 2026-09-03 (**★ 이 회차 = 코드리뷰 개선 회차를 로컬 `main` 에 병합했다 — 병합 커밋 `a1ff6af`(Ted 명시 지시) · 등재 `PLAN-SoT §9 〈307〉`.** 대장 신설 = `CR-1` ✅ · `CR-2` ⬜. **값·근거는 그 항목과 `sessions/CODE-REVIEW-20260903{,-PLAN}.md` 에 있다.** ⑴ **네 갈래를 레인 6 ＋ 후속 2 로 집행** — 경계·검증이 엉뚱한 서비스에 있던 것 · 검사기가 판정하지 않던 것(green-by-skip) · 화면의 픽스처 폴백 · 커널 손사본(레인 G 는 미착수). ⑵ **병합 트리 실측** — core-api **624** · viz-render **269** · pipeline-worker **231** · ai-service **130** · vitest **36파일/570** · `tsc --noEmit` **0** · `./gates/run.sh all -j 1` **green 50 · red(판정) 0 · red(준비) 0** · **게이트 43 → 50** · **CI 에 실리는 서비스 pytest 케이스 69 → 1070**. ⑶ **GitHub Actions** success 10 / failure 2 — `schema-gates`(적용 DB URL 미선언) · `planning-gates`(정본 폴더 미체크아웃)이고 **둘 다 main 사전존재**다. ⑷ **501 계수 정정 23 → 4** — 오라클 = `OPERATIONS` ＋ `tests/test_not_implemented.py`. ⑸ **main 17차(`〈303〉`·`〈306〉`) 충돌 3파일 해소** — 상세 화면에서 한 응답은 한 자리만 세운다(404 중립 · 410 묘비 · 오류 재시도). ⑹ **다음 작업** = `CR-2` 판정 5건 · 그중 **배포 뒤 산출물 재굽기 ＋ 소유 네 등급 재실측**이 순서를 묶는다. ⑺ **진입조건** — `contracts/**` 무접촉 · 마이그레이션 0 · **staging 무접촉 · 배포 0 · push 0** ⟹ **배포 전에는 캐시 키 변경이 아무 효과가 없다**.)

<!-- 원 03-HANDOFF.md L21 -->
／ 이전 **최종 갱신** 2026-09-03 (**★ 이 회차 = `V-2` 값 조회 **마감 실측** · 등재 `PLAN-SoT §9 〈304〉`.** 워크트리 `lane-v2close`. **값·근거는 그 항목과 `sessions/V-2-CLOSE-20260903.md` 에 있다.** ⑴ **`staging 배포 green` 은 닫혔다** — `git merge-base --is-ancestor d570f2d d9e89a3a074ba859…` **종료 코드 0**. ⑵ **응답 눈금은 값을 얻었으나 `V-2` 를 닫지 않았다** — 따뜻한 상태 합산 **p95 261 ms · max 280 ms**(n 95 · 공개 엣지 경유 · 서버 단독 `[미확인]`)이지만 **유휴 후 첫 실행에서 데이터셋 둘의 p95 가 1 초를 넘었다**(1,164 · 2,111 ms · 상한 5 초는 미초과). ⑶ ⛔ **막는 것은 값이 아니라 문면이다** — 완료 정의 ⑻ 이 **데이터셋별/합산 · 따뜻한 상태/유휴 후 첫 접촉**을 안 적어 읽는 법에 따라 판정이 뒤집힌다 ⟹ **Ted 판정 대기 1건.** ⑷ **값 대조 3/3 일치** — 블록 평균이 아니다. ⑸ ⚠ **착수 전제가 거짓이었다** — 자리 산출물은 **14건 중 3건**뿐이고 `〈254〉`-⑥ 문면도 `#58` 이후 낡았다. ⑹ **코드·계약·DB 무접촉 · staging 쓰기 0 · 배포 0 · push 0.** ／ 이전 **최종 갱신** 2026-09-03 (**★ 단독 창 2 를 닫았다 — 단계 F 롤백 왕복 집행 · 단계 G 건너뜀(Ted 판정). 등재 `PLAN-SoT §9 〈301〉`.** 워크트리 `lane-window-f2` · 기록 `sessions/WINDOW-20260903-F2.md`. **값·근거는 거기 있다.** ⑴ **롤백 왕복 GREEN** — `72408173be50` → **`77813d391f94`**(오라클 = `서빙 태그 대조 GREEN`, 헬스가 아니다) → 되돌아오는 배포 **`d9e89a3a074b`**. 양 국면 `배포 판정: GREEN (통과 15건 · SKIP 0)` · 별칭 `:i2` 6/6 · 체인 무변경(`0011`/`0005`) · 8/8 healthy. ⟹ **`I3` 완료 정의 ⑷·⑸ 닫힘 · 블로커 `§4 #43` 해소.** ⛔ **`I3` 는 `partial` 유지** — 잔여 = ⑴ 자동 트리거 완주 ＋ 부분 3(2-b·⑽·⑾). ⑵ **데이터 무접촉을 본문으로 증명** — 단계 E 적재분(데이터셋 `01M1JM3JTQSSTKR9A2QQ67QN81`)이 **옛 릴리스 아래에서도 200**. 이 창엔 마이그레이션이 0건이라 롤백이 남긴 격차가 없다. ⑶ **단계 G 건너뜀** — `terraform plan` = `No changes.`(2026-09-03 재측정 · `apply` 0회)를 `IS4 ①` 오라클로 받는다. **`IS4 ①` 의 자기모순 note 를 원문 취소선으로 정정**했고 `IS4` 는 **`partial` 유지**(② 맨몸 호스트 조건 남음). `terraform` 은 호스트에 **설치돼 있다(v1.9.8)** — 런북 `§11 ⛔6` 이 낡았다. ⑷ **배포 후 점검** — 게이트 `all -j 1` green 42 · red(판정) **0** · red(준비) 1(`rls-effect-selftest`) → **단독 재실행 green** ⟹ 43 green. `artifact-ownership` **고아 0**(대상 90 · 삭제 0건). preflight P8 digest **10/10** 이 새 릴리스를 **자동 추종**(`〈297〉` 검증). ⚠ **중단 구간은 「착수~판정 GREEN」이지 5xx 지속 시간이 아니다** — 롤백 약 12초 · 배포 약 44초 · 실제 오류 구간은 **`[미확인]`**.) ／ 앞 회차 ↓

<!-- 원 03-HANDOFF.md L22 -->
／ 이전 **최종 갱신** 2026-09-03 (**★ 이 회차 = `V-2` 값 조회 착수 · 계약 동결 해제 15차 · 등재 `PLAN-SoT §9 〈294〉`.** 워크트리 `lane-v2c`. **값·근거는 그 항목에 있다.** ⑴ **Ted 판정 = 읽는 쪽이 자리 이름을 다시 계산한다** — 굽는 쪽이 동반 기록을 남기는 길(재굽기)과 Port 신설은 기각. ⑵ **저장 규약에 변환 설정 셋을 승격**(`layout.json`) — 종전에는 굽는 쪽에만 있어 **읽는 쪽이 자리를 영영 못 찾았다.** 승격 전후 **키 비트 동일**을 고정값 시험이 못 박는다. ⑶ **op 2 신설 — seam 마다 따로 센다**: `fe-core` **54 → 55** · `core-viz` **5 → 6**. ⚠ 합쳐 「54 → 56」으로 읽지 않는다(앞 회차 정지 보고가 남긴 혼동). `contract-breaking` **ERR 0 · WARN 0**(순수 가산) · 생성물 **4벌** 재생성 · **마이그레이션 0**. ⑷ **집행 없는 신설 금지를 지켰다** — 계약 둘 · core-api 중계(내려받기와 **같은 두 줄**의 접근 판정) · viz-render 조회(창 **1×1** · Port 0 · 재굽기 0) · 화면 소비자 · **시험 20건**을 한 회차에 세웠다. **red 를 먼저 확인했다**(블록 평균으로 바꾸면 값이 갈린다). ⑸ ⛔ **`V-2` 는 닫지 않았다** — `in_progress`. 완료 정의 여덟 중 **미충족 둘** = ⑻ 응답 눈금 실측 · **staging 배포 green**. **다음 회차 진입조건**이 그 둘이다. ⑹ **새 항목 `V-3`** — 지도 타일이 소유·회수 규율 밖에 있다(고치지 않았다 · 판정 자리). ⑺ **`〈290〉` 은 결번**이다 — 앞 회차가 계약을 열지 않아 회차가 성립하지 않았고 번호를 되쓰지 않는다. ⑻ **배포 0 · push 0 · staging DB 접촉 0 · 산출물 삭제 0.** ／ 앞 회차 ↓)

<!-- 원 03-HANDOFF.md L24 -->
／ 이전 **최종 갱신** 2026-09-02 (**★ 이 회차 = 등재 2건 `〈282〉`·`〈283〉` · 문서·대장 전용. 코드·계약·DB·인프라 무접촉 · 배포 0 · push 0.** **값·근거는 `PLAN-SoT §9` 그 두 항목에 있다.** ⑴ **`〈282〉`** = Ted 판정 셋의 집행 등재 — 승인 처리 도착 전 `Verified` 열 **취소선·회색·비활성**(`CT-1` `[미확인]` ㈎㈏ 닫힘) · 묶음 내려받기에 **기준 격자 파일 포함**(종류 판별은 스키마 `CHECK` 그대로) · **용량 상한 없음 ⟹ 스트리밍 zip**(생성기 ＋ 버퍼 상한 증명 · **RSS 미측정**) ⟹ `ST-1` `[미확인]` 2건 닫힘. ⛔ **`CT-1` 은 `open` 유지** — 잔여는 **헤더 고정 시험 1건**이고 막는 것은 제품 판정이 아니라 **프런트 시험 도구 제약**이다. ⑵ **`〈283〉`** = **기간의 끝을 조건부로 — `DataPeriod.end` 를 `[string, "null"]` 로 · 계약 동결 해제 14차**(회차표 `sessions/X2-FREEZE-PROTOCOL.md §1` 14행 = `〈283〉`). ⚠ **전제 정정 — additive 가 아니다**: 같은 스키마가 응답에도 서서 `contract-breaking` **ERR 7**(`response-property-became-nullable` × 7) · red → 커밋 후 green 전이(7차 `〈151〉` 과 같은 모양) · **등급 ㉯**. 소비자 **2건 실재**(상세·프로젝트 표기) 같은 회차 수정 · **마이그레이션 0**(DB 열이 처음부터 nullable — **DB 가 계약보다 먼저 열려 있었다**). **귀속 = `P2`**(등록 화면 입력 칸 · 대장 `P2.note`). **새 `[미확인]` 3건** = 열린 기간 표기 문면 · UI 스펙 19 문면(레포 밖) · 끝이 NULL 인 기존 행 건수. ／ 앞 회차 ↓)

<!-- 원 03-HANDOFF.md L28 -->
／ 이전 **최종 갱신** 2026-09-02 (**★ 등재 3건 `〈279〉`·`〈280〉`·`〈281〉` · 문서·대장 전용. 코드·계약·DB·인프라 무접촉 · 배포 0 · push 0.** **값·근거는 `PLAN-SoT §9` 그 세 항목에 있다.** ⑴ **`〈279〉`** = 파일 저장처를 **접수 볼륨으로 확정** ＋ 내려받기 집행(501 표 **5 → 4**) ⟹ **`ST-1` = 닫힘**(`§1 T-P` 에 행이 처음 섰다) · **`CT-1` 은 열어 둔다**(`Verified` 열 문구가 제품 판정 대기) · `§4 #63` 해소. ⑵ **`〈280〉`** = 생성 요청 세 필드를 **서버가 받게 했다**(`_ALLOWED_CREATE_FIELDS` **7 → 10**) ＋ 등록 화면 세 칸 개방 · **계약 개정 0 · 동결 해제 0회** · 부수 수정 = `PATCH` 에 `period` 를 실으면 **500** 이던 것(`fff3034` 이래 · 시험 0건) · **판정 대기 = 기간의 입력 형태** · **동결 해제 회차표 결번을 `7-A` 차로 사후 등재**(정수 번호 무변경) · `§4 #62` 해소. ⑶ **`〈281〉`** = **완료 기준에 배제 규칙 신설**(문서뿐인 항목은 ③④ **대상 0건**) ＋ **E-01 기획 정본 v1.3 → v1.4** ＋ 패키지 재동기(`planning-freshness` **15/15 MATCH**) ⟹ **`P8` = 닫힘**. ／ 앞 회차 ↓)

<!-- 원 03-HANDOFF.md L30 -->
**⟨앞 회차⟩** 2026-09-02 (**★ 밀려 있던 등재를 원장에 실었다 — `〈276〉`·`〈277〉`·`〈278〉`. 코드·계약·DB·배포 무접촉 · 대장과 산문만 고쳤다.** **값·근거는 `PLAN-SoT §9` 그 세 항목에 있다.** ⑴ **`〈276〉` = `LV-1` 집행 ＋ 계약 동결 해제 13차** — 쓰기 바디(**생성·수정 둘 다**)에서 `processingLevel` 을 뺐다. 응답 필드·목록 질의 조건은 존치. 마이그레이션 `0011_lv1_drop_level_user_set`. `contract-breaking` **ERR 0 · WARN 2**(1 → 2 로 는 유일한 사유가 **생성 요청 확대**) · 프런트 소스 변경 **0건** · 사람이 값을 넣은 행 재계수 **0건**(2026-09-02). **Ted 판정 = `〈194〉` 의 「예외 없음」이 완료 정의의 좁은 문면보다 위다** ⟹ 완료 정의 ⓐ 개정(원문 보존). ⛔ **`LV-1` 은 닫지 않았다** — **staging 배포 0건**이라 실물에는 열이 그대로다(**다음 회차 진입조건**). 13차 기록은 `sessions/X2-FREEZE-PROTOCOL.md §1`. ⑵ **`〈277〉` = `#58` 해소 ＋ `S3` 재판정** — 파서 **4 → 6**(`NumPy`·`GRIB`) · **선언 무변경** · 거짓 문면(「지원 목록 밖」) 정정(실제는 **구현 결함**) · **선언↔구현 4자리 양방향 대조 시험 신설**(분기 재발 방지). `e2e-format-coverage.toml` **값 무변경** — 그 정본의 상대는 `RENDERABLE_FORMATS`(5)이지 `SUPPORTED_FORMATS`(6)가 아니다(근거를 기재로 남겼다). ⛔ **`S3` = `open` 유지.** 잔여는 포맷 조건이 아니라 **공개 엣지의 포맷별 왕복 실측**이고, 막는 것은 **staging 데이터 구성**이다 — 2026-09-02 실측 = tif 본체 53(데이터셋 8) · nc 41(2) · gz 22(2) · hdf 8(1) · **npy 6건 전부 기준 격자 파일 · 본체 0건**(계 데이터셋 13 · 파일 130). ⟹ **선행은 배포가 아니라 적재**(staging 쓰기) ＋ **엣지 왕복용 자격증명 접근** — **둘 다 Ted 승인 대기**. ⛔ **`〈275〉`-㉱-⑻ 정정** — 「`#58` 이 막는 것은 `S3` 잔여 1행」은 **틀렸다**(화면 미리보기는 D7 이 내고 그 경로는 NumPy 를 이미 읽는다). 원문은 지우지 않고 개정 표시를 얹었다. ⑶ **`〈278〉` = 계수 정정 ＋ 항목 신설 ＋ 이월** — ⛔ **`〈275〉`-㉮ 의 계수가 2건 과다였다**(같은 결정이 `A-1`·`P7` 을 닫았다고 적으면서 계수에서 빼지 않았다). **실측(2026-09-02 · 대장 전수)** = `stage2` 미완 **12**(`deferred` 포함) → `deferred` 제외 **11** → `PA-G` 제외 **10** · `stage1` 미완 **3** · **총 미완 14**. ⚠ **`ST-1` 신설 뒤에는 13 / 12 / 11 · 총 15 다 — 범위가 늘면 계수도 는다.** ⭑ **신규 항목 `ST-1`(파일 저장처) 신설** — `open` · `stage2` · `depends_on: [P2]` · **`completion_def` = 「완료 정의 미작성」**(저장처 형태가 미판정이라 지어내지 않았다). `CT-1.depends_on` = `[P1]` → **`[P1, ST-1]`**. ⭑ **`V-2` 다음 회차 이월** — op 신설(등급 ㉯)과 자리 COG 조회 경로(사이드카 / 저장 규약 승격 / Port 중 택일)가 **미판정**이다. `completion_def` 는 `〈254〉` 확정본 그대로. ⭑ **`R-1` 현황** — 크론 무인 연속 GREEN **5회**(8/29~9/2 · 끊긴 날 0) · **3회 창은 2026-08-31 완료** · 재측정기 신설 ＋ fail-closed 픽스처 **30건** · 잔여 = 복원된 스택 검색 1회 ＋ 운영 안내서 손검사 2건 자동화. **복원 실행은 Ted 승인됨(단독 창 조건)** · `partial` 유지. ⑷ **블로커 2건 신설** — `#62`(`DatasetCreate` 의 `variables`·`crs`·`period` 계약↔런타임 드리프트 · 400) · `#63`(파일 저장처 형태 판정). **둘 다 고치지 않았고 소유·처분 미정이다.**)

<!-- 원 03-HANDOFF.md L31 -->
／ 이전 **최종 갱신** 2026-09-02 (**★ 두 레인을 병합하고 `e935db2` 를 staging 에 배포했다 — 배포 GREEN. `A-1` 과 `P7` 이 닫혔고 `P8` 이 열렸다.** 등재 `PLAN-SoT §9 〈273〉`·`〈274〉`·`〈275〉` — **값·근거는 거기 있다**. ⑴ **병합 둘** — `lane-ct1`(원장·산문 전용 · 충돌 **0건**) → `lane-p7`(충돌 **1건** = 「최종 갱신」 머리줄 하나). **어느 쪽도 버리지 않았다** — `A-1` 갈래 B 문단을 머리에 두고 `P7` 문단을 「／ 같은 날 …」로 이어 붙였다. 원장 4묶음 전건 생존 확인 = `#57` 해소 · `A-1` ⑴~⑺ ＋ `〈272〉` · `CT-1` `evidence` · `P7`. ⑵ ⛔ **병합이 결함 하나를 드러냈다 — 병합 결함은 아니다.** frontend 시험이 회차마다 **422/421** 을 오갔고, `lane-p7` 워크트리 단독에서도 재현(3회 중 2회) · 병합 전 기준선 399 는 3회 안정 ⟹ **`P7` 결함**. 실물 = `DataMapCard.tsx:17` 이 `map.byLineageState` 를 무방비로 읽어 **렌더 예외가 App 트리를 통째로 죽였다**. 계약이 그 필드를 `required` 로 못박으므로(`fe-core.yaml:3350`) **규격을 지키는 응답에서는 이 수정이 아무것도 바꾸지 않는다.** 고친 근거는 `P7` 자신의 규칙 — `useDashboard.ts` 축자 「한 덩어리로 묶으면 하나가 죽을 때 홈이 통째로 죽는다」. 수정 후 **422 전건 green 5회 연속**. ⑶ **배포 전 실측** — 게이트 `all` **green 37 / red(판정) 0 / red(준비) 0** · core-api **519** · frontend **422** · viz-render **232** · pipeline-worker **222** · ai-service **145** · `tsc` 무오류. ⑷ **배포 GREEN · 릴리스 `e935db203b07`**(워킹트리 변경 0건 · 별칭 `:i2` 6/6 재부착). `verify-deploy.sh` **GREEN 통과 15건 · SKIP 0**. ⑸ **배포 후 게이트 재실행** — **green 37 / red(판정) 0 / red(준비) 0**(배포 전과 같다). **소유 네 등급(배포 후)** = 대상 **49** · 살아 있다 0 · 접수분 0 · **고아 0** · **판정 불가 49** · 지도 타일 1 · 스냅숏 80 줄 · 삭제 0건 — **배포 전과 같다**. ⚠ **바뀌지 않은 것이 정상이다** — 판정 불가의 원인은 사이드카 구판이고, 구판을 판 2 로 바꾸는 것은 **재굽기**인데 이 배포는 이미지만 갈고 산출물에 손대지 않았다. ⑹ **`A-1` = ✅** — 완료 정의 ⑻ 두 조각이 다 섰다. ⑴~⑺ 은 `〈272〉` 실측 승계(그 뒤 관련 변경 0건). ⛔ 「판정 불가 49」는 미충족이 아니다 — ⑸ 축자 「회수 대상은 **고아 등급뿐**」이고 고아는 0 이다. ⑺ **`P7` = ✅** — 3항 `staging 배포 green` 이 섰다. **배포 green 만으로 단언하지 않았다**: 배포 스택에서 D8 세 op 이 **401**(501 아님)을 내는 것을 확인했다. ⚠ 주체 토큰 양성 왕복은 **`[미확인]`**. ⟹ **`P8` 이 열렸다**(`status` 는 `open` — 열린 것과 착수한 것은 다르다). ⑻ **원장 정리** — `T-1` stage 3 이월 확정 ＋ 범위 ⓑ 하나로(`〈274〉` Ted) · `#54` 대상 셋 → **둘**(`V-1`·`J-1`) · `PA-G` 개시 조건 **미충족**(stage 1 에 `LV-1`·`LV-2`·`SC-5` 가 남는다) · `CT-1` `open` 유지 ＋ `downloadDataset` 소유 **판정 대기** · 산문 드리프트 9묶음. ⑼ **push** `origin/main` — force-push 0 · 다른 브랜치 push 0.)

<!-- 원 03-HANDOFF.md L33 -->
／ 이전 **최종 갱신** 2026-09-02 (**★ `A-1` 갈래 B 를 집행했다 — 소유 대조가 게이트로 섰고, 회수 문은 늘리지 않았다. 완료 정의 여덟 중 일곱이 서고 ⑻ 만 남는다.** 워크트리 `lane-a1b` · 등재 `PLAN-SoT §9 〈272〉` — **값·근거는 거기 있다**. ⑴ **게이트 `artifact-ownership` 신설**(＋ selftest) — 사이드카 `sources`(fileId) → `d3_file.dataset_id` 대조로 **네 등급**을 내고 **회수 전 전수 스냅숏**을 남긴다. 이음은 `d5_upload_file.id = d3_file.id`(`NB-A`) · 선례 `autometa-loss.sh:14`. **Port 0 · 계약 0 · 마이그레이션 0 · 동결 해제 0 · 배포 0**(갈래 B 예측대로). ⑵ **판정 규칙의 정본은 한 자리** — `d7_visualization/ownership.py` 이고 게이트가 그 파일을 **경로로 그대로 실어** 쓴다. ⑶ **⚠ 덫 둘을 코드 경로로 막았다** — `baked_for` 는 판정 입력이 **아니고**(등록 전환 뒤 낡는다 · 그것으로 판정하면 등록분 전건이 불일치로 뜬다), 구판은 「고아」가 **아니라** 「판정 보류」다(없는 필드를 근거로 지우면 오삭제). 둘 다 변이 시험이 증명한다 — 특히 **변이②**는 구판 판정을 떼면 구판이 **고아로 떠 회수 후보가 되는 것**을 재현한다. ⑷ **회수 문을 늘리지 않았다** — `reclaim_plan()` 이 **같은 계산기 `plan()`** 을 부르고 집행은 `apply()` 한 자리다. `tile-` 은 `kept` · 접수분 루트는 `OutOfScope` · **게이트는 아무것도 지우지 않는다.** ⑸ **실측(staging 실물 · 읽기 전용)** — 대상 **49 벌** · 살아 있다 0 · 접수분 0 · **고아 0** · **판정 불가 49** · 지도 타일 1 · 스냅숏 **80 줄** · **삭제 0건**. 자리의 사이드카가 전건 구판이다(판 2 병합 뒤 재굽기 0회). ⑹ ⛔ **완료 정의 ⑺ 의 전제를 실측으로 정정했다** — 「대상 간 재사용」은 두 갈래이고, **`PV-1` ⑵ 가 잰 것은 지도 타일 키(대상 무관)** 쪽이다. **렌더 키는 색범위 단계 토큰 때문에 이미 대상 범위에 딸려 있고**, 그것은 안 ⑷ 의 손실이 아니라 `〈74〉`-㉴ 가 요구한 무효화 장치다. 회귀가 셋을 잠갔다. ⑺ **게이트** `all` **green 37 / red 0**(35 → 37) · viz-render **232** · pipeline-worker 222 · core-api 516 · ai-service 145 · `tsc` 무접촉(frontend 무접촉). ⑻ **하드 룰** — staging DB 쓰기 **0건** · 산출물 삭제 **0건** · 계약·생성물 **무접촉** · 마이그레이션 0 · **배포 0건** · push 0건 · 대장 상태 변경 **0건**(`A-1` 은 `open` 유지). ⑼ ⛔ **남은 것은 ⑻ 하나 — staging 배포 green.** `main` 이 staging 보다 앞서 있다(릴리스 `14d3136aa55a` 는 `A-1` 1→3 병합 이전). **배포는 Ted 승인 자리**이고, 그 전에는 `A-1` 을 닫지 않는다.)

<!-- 원 03-HANDOFF.md L34 -->
／ 같은 날 **최종 갱신** 2026-09-02 (**★ `P7` 연구실 대시보드를 세웠다 — 코드·시험·게이트는 다 섰고 `staging 배포 green` 하나가 남았다.** 워크트리 `lane-p7` · 브랜치 `lane-p7`. ⑴ **D8 실동작 op 0 이 닫혔다** — 501 표 **8 → 5**(`getDashboardSummary`·`getDataMap`·`listActivities`). **계약 개정 0 건**이다: 계약은 처음부터 셋을 들고 있었고 없던 것은 라우트였다(`app/routes/insight.py`). ⑵ **경계를 새로 긋지 않았다** — 지표·맵은 `routes/catalog.py:_compose` 와 `d3_catalog.lineage_state` 를 **그대로 쓰고**(맵의 묶는 기준이 카탈로그 조건과 같아야 한다는 `Policy_홈_대시보드 §5` 축자), 할 일 함의 승인 두 그룹은 **`P6` 이 연 네 op**(`listPendingAccessRequests`·`listPendingVerificationRequests`·`approveAccessRequest`·`rejectAccessRequest`)을 부른다. **새 Port 0 · 새 표 0 · 마이그레이션 0.** ⑶ **최근 활동이 실제로 쌓이게 했다** — `record_activity` 호출 자리가 격자 변경 하나뿐이라 목록이 영원히 비어 있을 자리였다. 등록·계보 확정·Verified 승인·프로젝트 생성/삭제 다섯 곳에서 적는다. **열람은 안 적는다**(§10) — 브라우저에만 두고 음성 시험이 그것을 지킨다. ⑷ **연구실 경계 음성** — `TOKEN_B` 로 세 op 을 다시 불러 A 의 값이 하나도 안 보이는 것을 쟀다(B = 데이터셋 1 · 프로젝트 1 · Verified 0 · 주제 `토지피복·LULC` 1 · 활동에 A 의 대상 0건). **대시보드는 숫자로 접힌 화면이라 누출이 숫자 안에 숨는다** — 그래서 이 시험이 필수다. ⑸ **시험** core-api **519**(＋9 · 501 표가 줄어 파라미터 시험 6건 감소) · frontend **422**(＋23) · viz-render 209 · pipeline-worker 222 · ai-service 145 · 게이트 `all` **green 35 / red 0**(우회·검사 축소 0). ⑹ ⛔ **`P7` 은 🟧 다 — 이유는 하나뿐**: `WORK-UNITS §7` 「각 P 의 완료 판정」 3항 **staging 배포 green**. 이 회차는 **배포 승인 범위 밖**이었다. ⟹ **`P8` 은 아직 열리지 않는다.** ⑺ **정본 안 갈림 하나를 판정 없이 넘기지 않았다** — `Policy_홈_대시보드 §5.3` 은 「맵의 조건이 카탈로그로 전달되는 것」을 Phase 2+ 로 미루는데 같은 문서 `§8`(v1.5)은 「막대는 전부 눌려 그 조건으로 카탈로그를 연다」고 적는다. **뒤에 쓰인 `§8` 을 따랐고** 대장 `P7.note` 에 그 사실을 적었다. ⑻ **기존 시험 하나의 단언을 좁혔다** — `dataset-preview-zoom`·`-tiles` 의 「`setItem` 이 한 번도 안 불린다」는 **확대 상태를 저장하지 않는다**는 규칙보다 넓게 잡혀 있었고, 상세가 「내가 열어 본 것」을 브라우저에 적으면서 걸렸다. **규칙은 그대로 두고 대상만 명시했다** — 확대 관련 키 0건.)

<!-- 원 03-HANDOFF.md L36 -->
／ 이전 **최종 갱신** 2026-09-01 (**★ `bc499c2` 를 staging 에 배포했다 — 배포는 GREEN 이고, `P6` 은 닫혔고, `Y-1` 은 닫히지 않았다.** 워크트리 `lane-deploy2` · 기록 `sessions/DEPLOY-269-20260901.md` · 등재 `PLAN-SoT §9 〈269〉` — **값·근거는 거기 있다**. ⑴ **배포 GREEN · 릴리스 `bc499c2c1ac9`** — **배포 전 백업을 스택 8/8 healthy 인 상태에서 먼저 받았다**(platform·ai 두 프로파일 GREEN · SKIP 0). 마이그레이션은 절차대로 체인별 러너가 돌렸다 — **손으로 DDL 을 치지 않았다.** ⑵ **신원은 본문으로 판정했다 — 루트 200 으로 판정하지 않았다**: 실행 이미지 태그 5종 전부 `bc499c2c1ac9` · 별칭 `:i2` **6/6 이미지 ID 대조** · 플랫폼 체인 head **`0008` → `0010_p6_access_request`** · 헬스 **6종 200 ＋ 본문 `"unit"` 대조** · 컨테이너 **8개 healthy** · 호스트 노출 **0건** · 배포 판정 **GREEN(통과 15건 · SKIP 0)** · **`⑩-b` 엣지 설정 바이트 대조 일치**(`56cc63f04dad443a`). ⑶ **`P6` 을 배포된 스택에서 실동작으로 쟀고 닫았다** — 양성(요청 201 → 목록 1 → 승인 200 · 만료 ＋6개월 → 본문 열림 → 재승인 409) ＋ 음성(남의 연구실 상세·요청·승인·거절 전부 **404** · 목록 누출 0 · Verified 교수 경로 202/1/200). **시험 데이터는 합성만 쓰고 전량 되돌렸다**(잔재 7 표 전부 0건 · 실데이터 무접촉). **`P7` 의 유일한 선행이 풀렸다.** ⑷ ⛔ **`Y-1` 은 닫지 않았다 — 새 블로커 `§4` #59.** 선언은 전부 섰는데(`events` 볼륨 기동 · 두 spool env 선언) **`volume-init` 이 `events` 를 `chown 10001` 하지 않아** `/srv/viz-events` 가 `root:root` 이고 **두 단위(uid 10001) 모두 쓰기 거부**다 ⟹ **트리거 왕복 집행 불가.** **「배선은 있는데 아무 일도 안 난다」이고 헬스는 200 이다.** 고치지 않았다 — 새 커밋 ＋ 두 번째 배포라 **승인 범위 밖**. ⑸ **회귀** — `render-latency` **p95 2.118초 · 최대 2.145초**(눈금 10초 · 상한 60초) · 확대·이동 시험 **3건 전건 통과**(상한 100 ms) · 갈래 기본값 **「한 장」 그대로**(`tileBranch` = `꺼짐`). ⑹ **게이트** `all` **green 35 / red 0** · 우회·검사 축소 **0건** · 이번 배포가 만든 red **0건**. ⑺ ⛔ **배포 스택에서 못 잰 음성 하나 = 「`승인 위임` 꺼진 연구원의 403」** — 주체 표 베어러가 교수 하나뿐. `[미측정]` 로 적었고 푸는 법은 연구원 베어러 1건 추가(**자격 변경 · 별도 승인 사항**).)

<!-- 원 03-HANDOFF.md L37 -->
／ 이전 **최종 갱신** 2026-09-01 (**★ `lane-p6` 를 `main` 에 병합하고, 미발급 결정 하나를 발급·등재하고, 병합 후 계수를 다시 쟀다.** 등재 = `PLAN-SoT §9 〈265〉`·`〈266〉`(`lane-p6` 산출) · `〈267〉`(`lane-s3` 초안을 오케스트레이터가 발급) — **값·근거는 거기 있다.** ⑴ **승인 처리 여덟 op 이 실동작한다** — 접근 요청 넷 ＋ Verified 넷. 승인 권한은 정본대로 갈랐다(접근 = 교수 ＋ `승인 위임` 연구원 / **Verified = 교수만·위임 불가**). 미구현 501 표 **16 → 8**. 마이그레이션 `0010_p6_access_request` ＋ 화면 세 자리. ⑵ **`schema-diff` 의 red 가 닫혔다** — 그 red 는 `0010` 이 공유 적용 DB 에만 올라 선언↔적용이 갈린 것이었고, 병합으로 파일이 `main` 에 들어와 일치했다. 게이트 **green 35 / red(판정) 0 / red(준비) 0**. ⑶ **결정 `〈267〉` 발급·등재** — 사용 전 `§9` 전수 조회로 미사용 확인. `§9` 수록 **218개**(`〈51〉`–`〈268〉` · 중복 0 · 빠진 번호 0). ⑷ **블로커 `#58` 신설** — `NumPy` 헤더 파싱이 멈추면서 「지원 목록 밖」이라는 거짓 메시지를 낸다. ⑸ **병합 후 시험 재실측** — core-api **516** · frontend **399** · viz-render **199** · pipeline-worker **222** · ai-service **145** · `tsc` **0**. **닫힌 WU 0건** — `P6` 은 `staging 배포 green` 하나 때문에 열어 둔다. 배포 0건 · DB 쓰기 0건 · push 0건 · 게이트 우회 0건.)

<!-- 원 03-HANDOFF.md L38 -->
／ 이전 **최종 갱신** 2026-09-01 (**★ 정본↔대장 드리프트 9묶음을 산문 쪽에서 고치고, `CLAUDE.md` 를 게이트의 대조 대상에 넣었다.** 워크트리 `lane-drift` · 등재 `PLAN-SoT §9 〈268〉` — **값·근거는 거기 있다.** ⑴ **대장 변경 0건 · WU 상태 변경 0건 · 원문 삭제 0건** — 어긋난 자리는 전부 산문을 고쳤고 종전 문면은 취소선으로 남겼다. ⑵ **보고보다 넓었다** — `T-1` 도 stage 3 인데 `WORK-UNITS` 가 stage 2 로 세고 있었고, 완주 계수는 `P2`·`P3`·`P5` 에 더해 **`S2`** 까지 잔여로 세고 있었다. ⑶ **진실원 충돌은 역할을 갈라 적어 풀었다** — `CLAUDE.md §1` = 진행 요약의 출발점 · **상태·`stage`·완료 정의의 원본은 대장**(`§6` 무접촉). ⑷ ⭑ **재발 방지 = `work-item-consistency` ㈕ 신설** — 대장 `stage: after_stage2` 집합 ↔ `CLAUDE.md` 표지 블록 양방향 대조. **`CLAUDE.md` 가 대조 대상이 된 것은 처음이다.** fail-closed(표지 부재·미폐쇄·파일 부재 red) · `work-item-selftest` **14 → 17 케이스**로 변이 증명 · 검사 축소 0건. ⑸ 코드·계약·DB·생성물·staging 무접촉 · push 0건. 게이트 `all` before **green 35 / red 0** → after **green 35 / red 0**.)

<!-- 원 03-HANDOFF.md L39 -->
／ 이전 **최종 갱신** 2026-09-01 (**★ 두 레인(`lane-gate-role`·`lane-a1`)을 `main` 으로 병합한 뒤, 미등재분을 등재하고 사각지대 하나를 블로커로 열었다.** ⑴ **결정 `〈262〉` 등재** — 원장 대조 게이트(`autometa-loss`)에 **롤 판정**을 더한 회차. 값·근거는 `PLAN-SoT §9 〈262〉` 에만 적는다(레인이 남긴 등재문 초안 = `sessions/GATE-ROLE-CHECK-20260901.md` · 번호 발급·등재는 오케스트레이터가 직렬로 · 스킬 §1-b-⑶). 중복 검사 실측 = `work-item-consistency` **㈔ 결정 번호 214개 · 중복 0 · 건너뛴 번호 0** · 게이트 green. ⑵ **새 블로커 `#57`** — **`preview-tile-slot` 도 경계 롤에 속는다**(`d5_pipeline_event` 를 롤 확인 없이 센다 → 경계 롤이면 발행 0 → 핵심 판정이 통째로 건너뛰어진다). ⛔ **고치지 않았다 — Ted 판정 사안이다.** ⑶ **`#56`**(등록 전환 시 산출물 자리 이동의 소유)은 `lane-a1` 이 이미 등재했음을 확인했고, **`PR-CHECKLIST §2` 에 `#56`·`#57` 두 행을 더했다**(9건 → **11건**). ⑷ **병합 상태 정합 확인** — 대장·산문 일치(`work-item-consistency` green) · `A-1` 등재(`stage2` · `depends_on: [PV-1]`) · `V-1`·`V-2`·`J-1` 의 `depends_on` 에 `A-1` 있음 · `PV-1` 은 `done` 유지 ＋ `〈264〉`-㉮ 개정 표시 있음 · `〈263〉`·`〈264〉` 각 1행. ⑸ **실측** — 게이트 `all` **green 35 / red(판정) 0 / red(준비) 0**(2회 · 호스트 load average 0.49 와 8.74 에서 **같은 값** · 부하로 갈린 자리 없음) · core-api **511** · frontend **387** · viz-render **194** · pipeline-worker **222** · ai-service **145** · `tsc --noEmit` **exit 0**. ⑹ **문서 전용** — 생산 코드·계약·생성물·`db/**`·`infra/**` 무접촉 · **DB 쓰기 0건** · 배포 0건 · **대장 상태 변경 0건** · 산출물 삭제 0건 · push 0건.)

<!-- 원 03-HANDOFF.md L40 -->
／ 이전 **최종 갱신** 2026-09-01 (**★ 산출물 소유 구조를 대장에 항목으로 세웠다 — `A-1`. 그리고 계약 앞에서 멈췄다.** 워크트리 `lane-a1` · 등재 `PLAN-SoT §9 〈263〉`·`〈264〉` — **값·근거는 거기 있다**. ⑴ **`A-1` 등재** — 설계 초안 6항을 실물에 맞춰 **8항**으로 다듬었다(`title:`→`name:` · `blocks:` 는 스키마에 없어 삭제하고 **후행 셋의 `depends_on`** 으로 옮겼다 · 완료 정의 ⑸ 는 `〈261〉` 실측과 어긋나 폐기·재작성 · **⑹ 사이드카 전 층 · ⑺ `PV-1` 승계** 신설). `stage: stage2`. ⑵ **닫힌 `PV-1` 은 되돌리지 않는다** — 완료 조건 ⑵ 의 **문면도 고치지 않고**, 보증을 `A-1` ⑺ 이 승계한다(`〈264〉`-㉮). 깨지는 것이 무엇인지 갈라 적었다(대상 **간** 재사용만 사라진다 · 실측 손실 **316,201 B**). ⑶ **기준 격자 파일은 예외를 두지 않는다** — 격자는 접수분 루트이고 원장 실측상 **이미 대상별 복제**다(`d3_file` 의 격자 6행 / 데이터셋 3개 · 저장 키 전부 자기 데이터셋 디렉터리). 격자 공유에서 나온 산출물 공유는 **0건**(`〈264〉`-㉯). ⑷ **저장 규약 개정 = 동결 해제 13차 · 등급 ㉯. 그러나 열지 않았다** — 배선이 설 수 없으므로 계약도 열지 않는다(`§5-㉰-4`). **새 블로커 `#56`.** ⑸ **하드 룰** — 산출물 삭제 **0건** · staging DB 쓰기 **0건**(관리자 롤 ＋ `BEGIN READ ONLY`) · 계약·생성물·생산 코드 **무접촉** · 배포 **0건** · red 로 세운 시험 **0건**(구현 0건이라 오라클을 세울 자리가 없었다). ⑹ **게이트** — `all`(-j 2) **green 31 / red 4** 인데 **낮은 병렬도 재현에서 3건이 green 으로 뒤집혔다**(호스트 load average 6.45 · 스킬 §1-b-⑸). **after(부하가 내린 뒤 · 변경 후 트리) = green 35 / red(판정) 0 / red(준비) 0** — 직전 기록이 그대로 재현됐다. **갈린 것은 판정부가 아니라 호스트 부하였고**, 승자를 고르지 않고 기준 차이를 적었다. **이 회차가 만든 red 0건 · 검사 축소 0건.**)

<!-- 원 03-HANDOFF.md L141 -->
／ 이전 **다음 WU** → ⭑ **⟨개정 2026-09-02 · `§9 〈272〉`⟩ `A-1` 은 ⑻ 하나만 남았다 — 그리고 그것은 배포다.** **진입조건 = Ted 의 배포 승인.** `main` 이 staging 보다 앞서 있고(릴리스 `14d3136aa55a` 는 `A-1` 1→3 병합 이전) ⑻ 은 「전 게이트 green ＋ **staging 배포 green**」이라 배포 없이는 잴 수 없다. 게이트 쪽은 이미 섰다(`all` green 37 / red 0). ⚠ **`A-1` 을 부분 완료로 닫지 않는다**(`CLAUDE.md §5`). ⚠ 배포가 서면 `V-1`·`V-2`·`J-1` 의 선행(`depends_on: A-1`)이 걷힌다. ⚠ **`#54`(그 셋의 완료 조건이 초안)는 그대로 열려 있다.** ／ **종전 문면은 지우지 않는다** — 아래가 그것이고, **`#56` 이 소멸하고 갈래 B 가 서면서 낡았다.** ／ 이전 ⭑ **⟨개정 2026-09-01 · `§9 〈263〉`·`〈264〉`⟩ `A-1`(산출물 소유 구조) 착수 2회차 — 그러나 착수 가능이 아니다.** **진입조건 = 블로커 `#56` 판정**(등록 전환 때 미리보기 산출물의 자리를 누가 옮기는가 · 선택지 셋 ⓐⓑⓒ 는 `#56`). 그 판정이 서면 같은 회차에 **계약 동결 해제 13차(등급 ㉯)** ＋ 생성 모듈 3본 재생성 ＋ **D5·D7 소비처 배선을 한 커밋으로** 간다(자리가 갈리면 등록된 데이터셋 전체가 빈 지도가 된다 — `#20` 무늬). ⚠ **`V-1`·`V-2`·`J-1` 은 이제 `A-1` 을 선행으로 든다**(대장 `depends_on`). ⚠ **`#54`(그 셋의 완료 조건이 초안)는 그대로 열려 있다.** ／ **종전 문면은 지우지 않는다** — 아래가 그것이다. ／ 이전 ⭑ **⟨개정 2026-08-31 · `§9 〈253〉`⟩ `Y-1` 의 마지막 조건 하나 — ⓕ staging 배포 green.** ⓐ~ⓔ 가 전부 섰고(발신·수신 배선 포함) 남은 것은 **뜬 스택에서의 트리거 1왕복**이다. **진입조건** = 배포 인가 ＋ `compose.i2.yml` 의 새 버스 볼륨 `events` 가 실제로 뜬 상태 ＋ `verify/verify-deploy.sh` green ＋ 업로드 재처리 → 미리보기 재생성 1건 실측. ⚠ **이 회차는 배포 금지 조건이었다** — 그래서 `Y-1` 은 `partial` 이다. **함께 열려 있는 것** = `S3`·`P6` (선행 0건) · Ted 의 A/B 판정. ／ **종전 문면은 지우지 않는다** — 아래가 그것이다. ／ 이전 ⭑ **⟨개정 2026-08-31 · `§9 〈241〉`⟩ 선행 0건이 된 셋 중 하나 — `S3`(실데이터 4종 E2E 완주) · `P6`(승인 처리) · `Y-1`(자동 무효화).** `P3` 가 닫히면서 셋 다 미충족 선행이 없다. **함께 Ted 에게 남은 것 하나** — **A/B 판정**(기본 갈래를 한 장으로 둘지 타일로 둘지). 자료는 `sessions/P3-AB-DEPLOY-20260831.md §5` 에 있고 **staging 은 기본값(꺼짐)** 이다. ／ **종전 문면은 지우지 않는다** — 아래가 그것이고, **`#51` 이 닫히면서 낡았다**. ／ 이전 ⭑ **⟨개정 2026-08-31 · `§9 〈239〉`⟩ 엣지에 타일 자리를 여는 판정과 그 집행(`§4` #51)**. 타일 갈래는 배포된 스택에서 실제로 나오고 타일 자체도 **200 ＋ PNG** 인데, **공개 엣지에 `/viz/` 가 없어** 브라우저가 SPA 의 `index.html` 을 200 으로 받는다. **`P3` 를 닫는 마지막 조건이고, 어디에·어떤 캐시·어떤 인증으로 여는가가 운영 스택 판정 사항이다.** ／ **종전 문면은 지우지 않는다** — 아래가 그것이고, **재배포가 나면서 낡았다**. ／ 이전 ⭑ **⟨개정 2026-08-31 · `§9 〈237〉`⟩ `P3` 안에서 타일 갈래를 화면에 잇는 회차(`§4` #48)**. `PV-1` 이 닫히면서 그 항이 적어 둔 선행(자리 ＋ 유실 감지)이 전부 섰다 — 남은 것은 렌더 결과에 타일 갈래를 싣고 계약의 `oneOf` 가 갈라 놓은 지도형 주 화면을 갈아끼우는 일이고 **`P3` 소유**다. ／ **종전 문면은 지우지 않는다** — 아래가 그것이고, **`PV-1` 이 닫히면서 낡았다**. ⭑ **⟨개정 2026-08-31 · `§9 〈238〉`⟩ 다음은 이 변경분의 staging 재배포다** — `P3` 를 닫을 마지막 조건이고 **배포 권한자의 자리**다. 재배포가 green 으로 서면 `P3` 의 완료 정의 4항이 전건 충족되고, 그때 `P6`·`V-1`·`V-2`·`Y-1`·`S3` 의 선행이 걷힌다. ／ **종전 문면은 지우지 않는다** — 아래가 그것이고, **타일 갈래가 실리면서 낡았다**.

<!-- 원 03-HANDOFF.md L451 -->
| **43** | ✅ **해소 (2026-09-03 · 단독 창 2 단계 F 집행 · 등재 `§9 〈301〉` · 근거 `sessions/WINDOW-20260903-F2.md`)** — **`rollback.sh --to-last-green` 을 실제로 돌렸고 GREEN 이다.** 서빙 `72408173be50` → **`77813d391f94`** → `deploy.sh --target staging` → **`d9e89a3a074b`**. 판정은 헬스가 아니라 **서빙 이미지 태그**로 났다 — 축자 `서빙 태그 대조 GREEN — 서빙 중인 이미지가 실제로 77813d391f94 다`. 원장에 `rollback … 77813d391f94 … green (직전 배포=72408173be50)` 1행 ＋ 되돌아온 `deploy d9e89a3a074b … green` 1행. 별칭 `:i2` 재부착 6/6(양 국면) · 체인 무변경 · 8 healthy · 데이터 무접촉(단계 E 적재분이 옛 릴리스에서도 200). ⟹ **`I3` 완료 정의 ⑷·⑸ 가 닫혔다.** ⚠ **`I3` 자체는 닫히지 않는다** — 잔여 = ⑴ 자동 트리거 완주 ＋ 부분 3(2-b·⑽·⑾). ／ 이전 ~~⭕ **전제 소멸 (2026-09-03 실측 · 등재 `§9 〈287〉`-㉰)** — **아래 원문의 「green `deploy` 행이 하나뿐」이 더 이상 참이 아니다.** 릴리스 원장 읽기 전용 실측 = `deploy` **15행 · green 12** ⟹ 현재 서빙 태그(`77813d391f94`)를 뺀 **롤백 대상 11건**. `ledger_rollback_target()` 이 대상 0건으로 `die` 하는 조건은 성립하지 않는다. ⚠ **`rollback.sh` 를 실제로 돌려 확인하지는 않았다 `[미확인]`** — 파괴적이라 승인 사안이고, 그것이 `I3` 완료 정의 ⑷(롤백 왕복)의 잔여다. **막는 것은 이제 「대상 부재」가 아니라 「승인 ＋ 단독 창」이다.**~~ (**낡았다 — 실행으로 확인했다. 원문을 지우지 않는다**) ／ 이하 원문 — ~~**스크립트 롤백 경로가 없다**~~ | `ledger_rollback_target()` 은 원장의 green `deploy` 행 중 현재 태그를 뺀 최신을 고르는데 **green `deploy` 행이 `30b3e0a7b3f3` 하나뿐**이라 **대상 0건** → `rollback.sh` 가 `die` 한다. 그리고 `:prev` 는 **직전 릴리스가 아니다** — `deploy.sh` ③ 이 `:i2` 를 그대로 `:prev` 로 태그하고 `:i2` 는 green 배포 말미에만 갱신되므로, 첫 green 배포인 현재 `:prev` 는 **원장에 없는 수동 빌드본**(2026-08-27)이다. `ledger_rollback_target()` 은 `:prev` 를 후보로 보지도 않는다. ⭑ **오케스트레이터가 앞서 「되돌릴 곳은 있다」고 말한 것은 정확하지 않았다**(`§9 〈185〉-㉷`-⑵). **푸는 법 = 다음 배포가 한 번 더 green 으로 서면 롤백 경로가 생긴다.** 막는 것 = `I3` 완료 정의 ⑷⑸(롤백 왕복 · 직전 릴리스 확인) |

<!-- 원 03-HANDOFF.md L462 -->
| **54** | ✅ **해소 (2026-09-03 · `PLAN-SoT §9 〈289〉`-㉰·㉱ · 워크트리 `lane-loop-b` · advisor 권고대로 집행 · Ted 사전 위임 2026-09-03)** — **대상 둘이 모두 확정본으로 올라가 이 블로커가 비었다.** ⑴ **`V-1`** = 초안 ⑴~⑺ 을 글자 그대로 `completion_def` 로 승격(조항 신설·수정 0건). 초안이 못 닫은 둘의 거처 = **ⓐ 값 재사용은 요구에서 제외**(팔레트가 이미 `render_cache_key` 입력이라 키가 갈린다 · 조항 ⑴ 은 재사용 여부와 무관하게 성립 · 성능은 `〈233〉` **p95 2.081 s ≪ 10 s**) · **ⓑ 옛 키 파일 청소는 `V-1` 이 지지 않는다** — 신설 항목 **`RC-1`**(구판 사이드카 재굽기 후 `reclaim_plan` 1회 · `owner: D7 / viz-render` · 완료 조건 = 판정 불가 **49 → 19** · 삭제 **0** · 재굽기 **0회** 세 값의 갱신 관측)로 뗐다. ⑵ **`J-1`** = 9건 초안을 승격하고 **`[미확인]` 5자리를 전부 닫았다** — 넷은 실측 근거(`J-1`⑷ **불허** · RLS `lab_boundary` ＋ 타 연구실 404 / `J-2`⑷ **교수** · 역할 CHECK `(교수, 연구원)` / `J-3`⑶ **고정값 없음** · 쌍마다 계산, 못 구하면 「거리 `[미상]`」 / `J-8`⑴ **표본 없으면 예측을 그리지 않는다** · 음성 시험, 있으면 단계별 중앙값), 하나는 **Ted 판정**(`J-9`⑶ **수치 없는 문면** — 「첫 장 도달이 전량 완료보다 앞서고, 수렴 알림 1회」 · 유일한 후보 수 tif 12:1 이 계산값이라 정본이 측정으로 인용하는 것을 금한다). **지어낸 수 0건.** ⟹ **`V-1`·`J-1` 둘 다 착수 가능이다.** ⛔ **둘의 `status` 는 `open` 그대로 — 열린 것과 착수한 것은 다르다.** ⚠ 마이그레이션 **`0012`** 가 `J-2`(연구실 기본 격자) 몫으로 비어 있다(`〈287〉` §4). ／ **원문은 지우지 않는다** ↓ ~~⛔ **`V-1`·`V-2`·`J-1` 의 완료 조건이 「초안」 상태다 — Ted 확정 전에는 셋 다 착수 가능이 아니다.**~~ (2026-08-31 · Ted RULING ㉓·㉔) **값과 근거는 `PLAN-SoT §9 〈250〉`(`V-1`·`V-2`)·`〈251〉`(`J-1`)에만 적는다.** 대장의 `completion_def` 는 셋 다 **`완료 정의 미작성` 그대로**이고 초안은 `completion_def_draft` 라는 **다른 필드**에 있다 — `〈213〉` 이 세운 「지어낸 완료 정의보다 공백이 싸다」를 어기지 않으려는 자리다. **풀어야 할 것 셋.** ⑴ **`V-1`** — 초안 일곱을 확정으로 올릴지, 그리고 초안이 못 닫은 둘(팔레트만 바꿀 때의 값 재사용 `[미확인]` · 옛 키 파일의 수명·자리 청소 `[미확인]`)을 이 항목이 질지. ⑵ **`V-2`** — 값의 출처를 셋(원본 재조회 / 렌더 부산물 / 자리의 COG) 중 무엇으로 하는지. **정확도와 속도를 맞바꾸는 제품 판단이라 이 회차가 결정하지 않았다.** 물음 넷의 문면이 `〈250〉`-㉲ 에 그대로 있다. ⑶ **`J-1`** — 9건 초안 중 **`[미확인]` 5자리**(`J-1`⑷ 연구실 밖 가져오기 · `J-2`⑷ 기본 격자 지정 역할 · `J-3`⑶ 경고 문구의 거리 수 · `J-8`⑴ 예측의 근거 자체 · `J-9`⑶ 이득의 합격선). **숫자를 지어내 메우지 않았다.** ⚠ **이 회차에 닫은 WU 는 0건이다** — 상태를 하나도 바꾸지 않았다.  ⭑ **⟨범위 축소 2026-09-02 · `§9 〈275〉`-㉰⟩ 대상은 셋이 아니라 둘이다 — `V-1`·`J-1`.** **`V-2` 는 빠진다** — 그 완료 정의는 `〈254〉` 로 **확정본**이고 대장 `work-items.yaml` 의 `V-2.completion_def` 가 그 확정본이다(`completion_def_draft` 는 당시 기록으로 남겨 둔 다른 필드다). ⟹ 위 「대장의 `completion_def` 는 **셋 다** `완료 정의 미작성` 그대로」는 **틀렸다** — 그 문면은 지우지 않고 이 줄로 정정한다. `V-2` 의 초안 말미 「판정 대기 = `03-HANDOFF §4 #54`」에도 해소 표시를 얹었다. **⑵ 항(`V-2`)은 이 블로커에서 내린다.** |

<!-- 원 03-HANDOFF.md L464 -->
| **56** | ⭕ **소멸 (2026-09-02 · Ted 판정 「안 ⑷ 채택」 · 등재 `PLAN-SoT §9 〈270〉`-㉰)** — **해소가 아니다. 물을 것이 없어졌다.** 이 블로커의 질문(「등록 전환 때 미리보기 산출물의 자리를 누가 옮기는가」)은 **자리가 `targetId` 에 종속될 때만** 생기는데, 채택된 **안 ⑷(사이드카 판정)은 경로에 `targetId` 를 넣지 않는다** ⟹ **옮길 것이 없다.** 아래 선택지 ⓐⓑⓒ 는 **전부 고를 필요가 없어졌다** — 어느 것도 집행하지 않았다(core-api 볼륨 무접촉 · 이벤트 계약 무접촉 · 재굽기 트리거 신설 0). ⚠ **안 ⑷ 를 뒤집으면 이 블로커는 되살아난다** — 그래서 「해소」로 적지 않고 종전 등재문을 지우지 않는다. 선례 = `§4` `#28`(전제 소멸) · `§4.2` 6번. ／ 종전 등재문 ↓ ⛔ ~~**안 ⑴(데이터셋 종속 전환)을 배선하려면 판정이 하나 더 필요하다 — 등록 전환 때 미리보기 산출물의 자리를 누가 옮기는가.** (2026-09-01 · 워크트리 `lane-a1` · 등재 `PLAN-SoT §9 〈264〉`-㉳) **실측** — 접수분 바이트는 등록 때 `uploads/{uploadId}/` → `uploads/{datasetId}/` 로 옮겨진다(`core-api` `routes/ingestion.py` `_relocate` · 안 옮겨서 난 사고가 `#20`). 지도 타일을 굽는 시점의 대상은 **`uploadId`** 이고 등록 뒤 그리는 쪽이 보는 대상은 **`datasetId`** 이며, **등록 경로에 타일 재굽기 트리거가 없다.** 그리고 **core-api 는 미리보기 볼륨을 마운트하지 않는다**(`compose.i2.yml` — core-api 는 `uploads` 하나. `previews` 는 nginx `:ro`·pipeline-worker·viz-render). ⚠ **그대로 배선하면 등록된 데이터셋의 지도 미리보기가 에러 없이 빈 채로 뜬다** — `#20` 과 같은 무늬다. **선택지 셋** = ⓐ core-api 에 미리보기 볼륨을 붙여 옮긴다(경계를 넘는다 · `CLAUDE.md §3-1`) ⓑ 등록 사건을 D5·D7 이 받아 옮긴다(이벤트 계약 넷째 종류 = **또 한 번의 동결 해제**) ⓒ 옮기지 않고 다시 굽는다(**재굽기 트리거를 새로 세워야 한다**). **판정 전에는 `A-1` 의 계약 절단을 열지 않는다**(`X2-FREEZE-PROTOCOL §5-㉰-4` — 계약만 열고 배선을 미루는 것은 금지).~~ |

<!-- 원 03-HANDOFF.md L465 -->
| **57** | ✅ **해소 — ⓒ 둘 다 집행 완료 (2026-09-02 · 워크트리 `lane-57` · 판정 `PLAN-SoT §9 〈271〉`-㉮)** — **반쪽으로 닫지 않았다. 두 겹 다 붙였다.** **ⓐ 롤 판정** — `gates/tools/preview-tile-slot.sh` 가 계수 전에 접속 롤을 확인한다: ㉮ `rolsuper`·`rolbypassrls` 가 둘 다 거짓이면 **red**(경계가 거른 값을 세지 않는다) · ㉯ 같은 질의를 경계 롤(`COLAB_PREVIEW_TILE_BOUNDARY_ROLE`)로 한 번 더 돌려 **값이 같으면 red**(경계가 아무것도 가르지 못했다는 뜻) · 그리고 **발행 0건 자체를 red 로 못 박았다**(발행이 0 이면 핵심 판정 「발행＞0 인데 쓸 수 있는 타일 0」이 돌지 않는다 — 안 돈 검사를 통과로 세는 것이 green-by-skip 이다). 읽기 전용 탐침도 함께 붙였다(실물을 보게 됐으므로 한 글자도 쓰지 않음을 매 회차 실측한다). **ⓑ 발행 구조** — 대조 정본을 **스키마 전용 적용 DB → staging 실물 platform DB** 로 옮겼다. 변수도 갈랐다: `COLAB_APPLIED_DB_URL_PLATFORM` → **`COLAB_PREVIEW_TILE_DB_URL`**(읽기 전용). 선례·문면 모두 `autometa-loss`(`〈237〉`·`〈262〉`)와 같다. 값은 홈의 0600 env 파일에만 있다(레포 무기록). ⭑ **음성 증명 — 종전 green 자리가 red 로 갈렸다.** 같은 자리(타일 1건 · 쓸 수 있음 1)·같은 경계 롤 접속(`role=colab_app`)에서 **main `740b8c6` 판 게이트 = `green`(발행 업로드 0 으로 세고 핵심 판정을 건너뛴다) · 새 게이트 = `red`(「접속 롤 'colab_app' 이 관리자 롤이 아니다 · rolsuper=false · rolbypassrls=false · FORCE RLS 표 23개」)**. selftest 변이① 이 종전 게이트를 재현해 같은 상태의 green 을 다시 보인다. **selftest 증보** — 케이스 12 → **20**(ⓙ 경계 롤 미선언 · ⓚ 경계 롤 접속 · ⓛ 경계 롤 = 관리자 롤 · ⓜ 스키마 전용 DB(발행 0건) ＋ 변이 3종). **형제 스윕 재확인** — 같은 무늬 **0건**: `schema-diff`(덤프 대조) · `rls-coverage`(일회용 카탈로그) · `rls-effect`(이미 롤을 본다) · `autometa-loss`(닫혔다) · `e2e-format-coverage`·`render-latency`·`seam-consistency`(대상 0건을 이미 red 로 못 박았다). **실측** — 게이트 `all` **green 35 / red 0** · viz-render 209 · pipeline-worker 222 · core-api 516 · ai-service 145. **배포 0건**(게이트 배선이라 배포가 필요 없다) · staging 쓰기 0건 · 대장 상태 변경 0건. ／ 종전 등재문 ↓ ⛔ ~~**`preview-tile-slot` 이 같은 방식으로 속는다 — `d5_pipeline_event` 를 롤 확인 없이 센다.** (2026-09-01 · 워크트리 `lane-gate-role` 실측 · 등재 `PLAN-SoT §9 〈262〉`-㉳) **실측** — 이 게이트는 `COLAB_APPLIED_DB_URL_PLATFORM` 으로 붙어 발행 건수를 세는데 **접속 롤을 어디서도 확인하지 않는다.** 경계(`FORCE ROW LEVEL SECURITY`)에 걸리는 롤이면 발행이 **0** 이 되고, 그러면 핵심 판정 `[ "$EMITTED" -gt 0 ] && [ "$USABLE" -eq 0 ]`(「발행＞0 인데 쓸 수 있는 타일 0」)이 **통째로 건너뛰어져** 게이트가 green 을 낸다 — `CLAUDE.md §4` green-by-skip 의 정확한 무늬다. ⚠ **결함이 둘 겹쳐 있다** — 지금 배선은 스키마 전용 데이터베이스라 발행이 **구조적으로도 0** 이다. **형제 스윕 결과** = `schema-diff` 해당 없음(`pg_dump` 산출 스키마를 본다) · `rls-coverage` 해당 없음(일회용 postgres 카탈로그) · `rls-effect` **이미 롤을 본다** · `autometa-loss` **닫혔다**(`〈262〉`). ⛔ **이번 회차에 고치지 않았다 — 판정 사항이다.** 붙이는 비용 `[미측정]`. **정할 것** = ⓐ `autometa-loss` 와 같은 롤 판정 한 겹을 붙일 것인가 ⓑ 발행이 구조적으로 0 인 배선(스키마 전용 데이터베이스)을 먼저 바꿀 것인가 ⓒ 둘을 한 회차에 묶을 것인가. **막는 것** = 미리보기 타일 자리의 검사 신뢰도 전체 · `A-1` 착수 회차가 이 게이트를 인용하는 자리.~~  ⭑ **⟨막던 대상 명시 2026-09-02 · `§9 〈275〉`-㉱-⑻⟩ 이 블로커가 막던 것은 `A-1` 갈래 B 였다**(같은 게이트 파일 `preview-tile-slot.sh` 를 만진다). **해소됐고, 막던 것도 풀렸다** — `A-1` 은 `〈272〉` 로 집행되고 `〈273〉` 로 닫혔다. |

<!-- 원 03-HANDOFF.md L466 -->
| **58** | ✅ **해소 (집행 2026-09-02 · 워크트리 `lane-fmt` · 초안 `dev-package/notes/FMT-LEDGER-PENDING.md` ⟨초안 A⟩)** — 파서를 넷 → 여섯으로 채웠고(`NumPy`·`GRIB`), 거짓 문면을 갈랐고(선언 안인데 파서가 없으면 「목록 밖」이 아니라 「구현 결함」), **선언↔구현 대조를 시험이 기계로 지킨다**(`services/pipeline-worker/tests/test_format_declaration_parity.py` 네 자리 · 양방향). 선언 무변경 · 계약 무접촉 · 마이그레이션 0건 · 배포 0건. ⚠ **막는 대상 기재가 틀렸다 — `#58` 은 `S3` 를 막고 있지 않았다**(§9 `〈275〉`-㉱-⑻ 정정 대상 · 근거는 대장 `S3` note 2026-09-02 재판정). ／ 종전 ⛔ **판정됨 · 미집행 (판정 2026-09-02 Ted = **파서 확장 4→6** · 등재 `PLAN-SoT §9 〈271〉`-㉯)** — **선언을 줄이지 않고 처리를 채운다.** 선언(`SUPPORTED_FORMATS`·`RENDERABLE_FORMATS`)은 **여섯 그대로**이고 **처리를 넷 → 여섯**으로 채운다. 되돌리는 비용은 **파서 구현 1회차**다(선언 축소면 문서 1행이었다 — 더 비싼 쪽을 골랐고 기준은 값이 아니라 범위다). ⚠ **거짓 메시지 문면도 같은 회차의 몫이다** — 처리 못 하는 것을 「지원 목록 밖」이라 부르는 자리는 처리를 채워도 남는다. **집행 0건** — D5 파서 무접촉. 아래 「사람이 풀 것」은 **답이 나왔다** — 다시 묻지 않는다. ／ 종전 등재문 ↓ ⛔ ~~**`NumPy` 가 헤더 파싱에서 멈추고, 그 실패 메시지가 「지원 목록 밖」이라고 거짓말한다.** (2026-09-01 · 워크트리 `lane-s3` 실측 · 등재 `PLAN-SoT §9 〈267〉`-㉲) **실측** — `NumPy` 는 `SUPPORTED_FORMATS`·`RENDERABLE_FORMATS` **둘 다에 들어 있다**. **선언 여섯 · 처리 넷** — 선언과 처리가 갈려 있고, 메시지가 그 사실을 감춘다(고장을 「원래 안 되는 것」으로 읽게 만드는 무늬). **소유 = `D5`(인제스트·파이프라인)** — 다만 `D5` 는 2026-08-30 재실측으로 닫혀 있으므로(`sessions/D5-CLOSE-MEASURE-20260830.md`) **닫힌 항목 위의 신규 결함**으로 읽는다. **`S3` 를 막지 않는다** — 화면 경로는 `D7` 이다. **사람이 풀 것 = 처리를 넷에서 여섯으로 채울지, 선언을 넷으로 줄일지의 판정**(범위 결정이라 Ted 판정이다). 되돌리는 비용 — 선언 축소는 문서·정본 1행, 처리 확장은 파서 구현 1회차.~~  ⭑ **⟨막는 대상 명시 2026-09-02 · `§9 〈275〉`-㉱-⑻⟩ 이 블로커가 막는 것은 `S3` 잔여 1행이다.** **판정은 끝났고 집행이 남았다** — 어느 WU 가 멈춰 있는지가 표에 없으면 다음 회차가 그 사실을 다시 찾는다. | ⭑ **⟨등재 2026-09-02 · `PLAN-SoT §9 〈277〉`⟩ 값·근거는 그 회차에 있다.**

<!-- 원 03-HANDOFF.md L467 -->
| **59** | ✅ **해소 (2026-09-02 · Ted 배포 승인 · 워크트리 `lane-59` · 릴리스 `b3c4085ccdf0`)** — `volume-init` 의 `chown -R 10001:10001`·`chmod 0755` 대상에 `/srv/viz-events` 를 넣고 그 볼륨을 이 단위에 마운트했다(**한 줄 · 3줄 변경**). **고치기 전 실측** = 두 단위에서 `id -u`=`10001` · `/srv/viz-events` `drwxr-xr-x 0 0` · `touch` **Permission denied** 양쪽. **고친 뒤 실측** = `drwxr-xr-x 10001 10001` 양쪽 · 쓰기 성공 양쪽 · 한쪽이 쓴 파일이 다른 쪽에 보인다(같은 볼륨 확인). **버스 왕복 실측 1건** = D5 의 `worker.spool_publish('/srv/viz-events')` 로 `preview.grid-changed` 봉투를 발행 → D7 의 `SpoolTriggerPort.poll()` 이 `('격자 변경','upl-59-probe','preview.grid-changed:upl-59-probe')` 로 집었고 → `ack()` 뒤 버스가 비었다. **두 컨테이너의 설치된 코드로 돌렸다** — 시험 하네스가 아니다. 게이트 **35 green / 0 red** · `verify-deploy.sh` **15 PASS · SKIP 0** · 체인 head 2 PASS. ⚠ **그래도 `Y-1` ⓕ 는 닫히지 않는다** — 완료 정의 ⓕ 가 이름으로 건 것은 「트리거 왕복 1건 실측(업로드 재처리 → 미리보기 재생성)」이고, 이번에 선 것은 **버스가 물리적으로 쓰인다**까지다. 막는 것은 **새 블로커 `#60`**(집행 호출자 부재)이다. **이 블로커의 교훈은 그대로다** — 헬스 6종·`docker inspect` 는 결함이 있는 동안에도 전부 정상이었다 | `PLAN-SoT §9 〈269〉`-㉲ · 후속 `#60` |

<!-- 원 03-HANDOFF.md L468 -->
| **60** | ✅ **해소 (2026-09-02 · Ted 판정 「주기 배경 루프」 · 워크트리 `lane-59` · 릴리스 `14d3136aa55a`)** — **집행 호출자를 주기 배경 루프로 세웠다.** 판정 근거(Ted) = 관리 HTTP op 는 D7 의 표면과 **계약 개정**을 부르고, 기동 시 1회는 기동 이후의 봉투를 영영 안 집는다. 실물 = `services/viz-render/src/colab_viz/app/trigger_loop.py`(`TriggerDrainLoop` · 스레드 · `threading.Event` 종료) ＋ `app/main.py` 의 lifespan 이 `app.state.triggers` 가 있을 때만 세운다 ＋ 주기 `COLAB_VIZ_TRIGGER_POLL_SECONDS`(기본 **5초** — pipeline-worker `serve()` 와 같은 값). **모양은 pipeline-worker 를 따랐다** — 새 배관을 짓지 않았고, **멱등·ack 는 기존 `drain`·`SpoolTriggerPort` 것을 그대로 쓴다**(병행 기구 0건). **HTTP 표면 0 · 계약 개정 0 · 마이그레이션 0.** **시험** = viz-render 194 → **204 passed**(새 5건). 핵심 1건은 **집행을 부르지 않는다** — 앱을 띄우고 봉투를 놓고 기다린다. `TriggerDrainLoop.start` 를 무력화해 **#60 이전 상태를 재현하면 그 시험이 red** 다(선확인). 게이트 **35 green / 0 red**(배포 전·후 두 번) · `verify-deploy.sh` **15 PASS · SKIP 0** · 체인 head 2 PASS. **ⓕ 왕복 1건 실측(설치된 코드)** = ⑴ probe 업로드(실원천 GeoTIFF 6,953 B)를 설치된 D5 `IngestionService.process_upload` 로 처리 → `ready=true` ⑵ viz-render 에 `POST /viz/v1/renders` **202** → 미리보기 산출물 5건이 `15:29:47` 에 놓였다 ⑶ **업로드 재처리**(같은 코드로 다시 처리 · `was_ready=true`) → `preview.backend-rerun` 이 버스(`/srv/viz-events`)에 **1건** ⑷ **아무도 부르지 않았는데** 4초 만에 버스가 **0건**이 되고 같은 산출물 5건이 **`15:30:14` 로 다시 쓰였다**(inode·크기 동일 · 내용주소 이름이라 digest 동일 — 바뀐 것은 mtime). probe 자료·원장 행은 **전량 되돌렸다**(업로드 행 0 · 이벤트 0 · 산출물 0 · 버스 0) 뒤 게이트 재측정 **35 green** | `Y-1` ⓕ 해소 · 후속 `#61` |

<!-- 원 03-HANDOFF.md L470 -->
| **62** | ✅ **해소 — 서버를 넓히고 화면을 열었다 (2026-09-02 · 워크트리 `lane-62` · 등재 `§9 〈280〉`).** ⭑ **⟨후속 2026-09-02 · `§9 〈283〉`⟩ 이 항목이 남긴 판정 「기간의 입력 형태」도 닫혔다** — 두 칸 유지 ＋ 끝 조건부 · **계약 동결 해제 14차** · 귀속 `P2`. 남은 것은 표기 문면·스펙 19 문면·기존 행 계수 세 `[미확인]` 이고 **셋 다 이 블로커가 아니라 `§4.5` 판정 대기에 있다.** ／ 이하 원문 — — **Ted 판정 = 등록 화면에서 변수·좌표계·기간 입력을 허용한다.** 그래서 계약을 좁히지 않고(14차 해제 불요 · `〈138〉` 유지) **양쪽을 한 회차에 세웠다.** **ⓐ 서버** — `_ALLOWED_CREATE_FIELDS` **7 → 10** · 저장은 `updateDataset` 이 쓰는 경로 하나(`d3_catalog.update_dataset`)를 그대로 쓴다 · 검사기도 한 벌(`catalog.validate_human_metadata` 를 생성·수정이 같이 부른다) · **계약 개정 0 · 동결 해제 0회 · 마이그레이션 0**(열은 이미 있다 · head `0011`). **ⓑ 순서가 규칙이다** — 사람 값을 먼저 쓰고 보류 사건 반영(`apply_autometa` 「빈 칸만 채운다」)은 그대로 둔다. 시험이 **같은 회차에 만난 사람 값과 `file.header-parsed` 사건**에서 변수·좌표계·기간은 사람 값이 남고 `format`·`grid` 는 사건 값이 들어옴을 못 박는다. **ⓒ 빈 칸은 싣지 않는다** — 빈 배열·빈 문자열을 저장하면 자동 반영이 영영 막힌다(`〈140〉`-㉱). **ⓓ 화면** — `RegisterArea.tsx` 의 세 칸이 읽기 전용 `자동` 에서 입력 칸으로 내려왔다(자동 블록은 **포맷·용량·격자** 셋만 남는다). 근거 = 정본 `POLICY-20260825-001` 핵심규칙 1 · UI 스펙 **18·19·20**. ⚠ **자동완성은 안 붙였다 — 정본에 없다**(스펙 18·20 은 `ui: "Input"` ＋ 예시 안내뿐) ⟹ `listDatasetFieldSuggestions` 는 여전히 프런트 소비자 0. ⭑ **⟨해소 2026-09-02 · 워크트리 `lane-r14` · 14차 해제⟩ 기간의 입력 형태 — 판정 닫혔다.** **Ted 판정 = 기간은 선택이고, 있으면 시작은 있고 끝은 조건부다(없으면 무기한·진행 중).** 계약 `DataPeriod.end` 를 `type: [string, "null"]` 로 열었고(`ProjectPeriod` 와 같은 required-but-nullable 모양) 화면의 **끝 칸은 비워도 된다**. 등재문 초안 = `dev-package/notes/R14-LEDGER-PENDING.md`(`§9` 번호 미발급). ⚠ **레포 밖에 남는 것 1건 — UI 스펙 19 의 문면 갱신**(한 칸 자유 문장 → 두 칸 ＋ 끝 선택). ／ **종전 문면 ↓** ⛔ ~~정본과 어긋나는 자리 1건 — 기간의 입력 형태(스펙 19 = 한 칸 자유 문장 `2025-06 ~ 2025-09` / 계약 `DataPeriod` = `start`·`end` 두 `date-time`). 계약 무변경이 전제라 화면이 **날짜 두 칸**으로 받는다 — **판정 대기**.~~ ⚠ **격자(스펙 21)는 열지 않았다** — 계약에 `grid` 가 없다(`〈74〉`) · 소유·처분 미정. ⭑ **부수 발견 — 수정 경로의 기간이 통째로 죽어 있었다**: `_UPDATABLE` 에 `period` 가 없어 `PATCH` 에 기간을 실으면 `KeyError` → **500**. `fff3034` 이래 열려 있었고 **시험 0건이라 아무도 몰랐다.** 고쳤고 200 을 못 박는 시험을 세웠다. ／ **종전 등재문 ↓** ⛔ ~~**`DatasetCreate` 의 `variables`·`crs`·`period` 가 계약에는 있는데 서버가 받지 않는다 — 실어 보내면 400 이다(판정 필요 · 고치지 않았다).**~~ | (2026-09-02 · 워크트리 `lane-lv1` 실측 · 등재 `PLAN-SoT §9 〈276〉`-㉸) `routes/ingestion.py` 의 `_ALLOWED_CREATE_FIELDS` 에 이 세 필드가 **없다**. `LV-1` 이 닫은 `processingLevel` 과 **같은 무늬의 계약↔런타임 드리프트**이고, **계약이 실물보다 넓은 자리**다. ⛔ **이 회차의 대상이 아니었고 손대지 않았다 — 소유·처분 미정.** **판정 대기(Ted) = ⓐ 계약을 좁히는가 ⓑ 수용 목록을 넓히는가 ⓒ 어느 WU 가 지는가.** ⚠ **어느 게이트도 이것을 말하지 않는다** — 없는 것은 diff 에 안 나온다(`§4 #55` 와 같은 실패형). | ⭑ **⟨번호 발급 2026-09-02⟩ 등재 = `PLAN-SoT §9 〈280〉`.** 초안(`notes/B62-LEDGER-PENDING.md`)이 원장에 실렸다. ⭑ **정체는 「드리프트」가 아니라 「누락」이다** — `869ee64` 축자 「서버 구현 0 … 소비자는 다음 커밋」 ＋ `fff3034` 가 **수정 경로만** 구현. **새로 열린 판정 = 기간의 입력 형태**(스펙 19 = 한 칸 자유 문장 / 계약 `DataPeriod` = 두 `date-time` · 화면은 두 칸으로 받는다). ⭑ **동결 해제 회차표의 결번을 `7-A` 차로 사후 등재했다**(`〈280〉`-㉹ · 정수 회차 번호 무변경 · 다음 해제는 그대로 14차)

<!-- 원 03-HANDOFF.md L471 -->
| **63** | ✅ **해소 (2026-09-02 · Ted 판정 · 워크트리 `lane-st1`)** — **판정 = 「파일 저장처는 지금 볼륨을 그대로 쓴다」.** 객체 저장소는 인프라·비용·운영 경계를 한꺼번에 끌고 오고 이 단계에서 정당화되지 않는다. **붙은 조건 = 내려받기 경로를 이음매 하나 뒤에 둔다** — `services/core-api/src/colab_core/kernel/file_store.py`. ⟹ `ST-1` 완료 정의를 **처음 기재했다**(여섯 조항 ＋ `[미확인]` 2건 · 대장 `work-items.yaml`) 하고 **`downloadDataset` 501 을 실동작으로 갈았다**(501 표 5 → 4 · `NOT_IMPLEMENTED_NO_STORE` **0건**). **마이그레이션 0건**(head `0011` 그대로) · **계약 개정 0건** · **동결 해제 0건.** 실측 = 게이트 `all -j 1` **green 37 / red(판정) 0 / red(준비) 0** · core-api **519 → 524** · frontend **424 → 427**. 값·근거는 등재 대기분 `dev-package/notes/ST1-LEDGER-PENDING.md` 에 있다. ／ 종전 문면(무삭제) — ~~⛔ **파일 저장처의 형태가 미판정이라 `ST-1` 의 완료 정의를 쓸 수 없다.** | (2026-09-02 · Ted 판정으로 항목만 열렸다 · 등재 `PLAN-SoT §9 〈278〉`-㉯) `downloadDataset` 이 **501 `NOT_IMPLEMENTED_NO_STORE`** 이고 저장처를 지는 WU 가 **0건**이었다 ⟹ **신규 항목 `ST-1` 을 열었다**(`CT-1` 이 흡수하면 그 항목의 「새 요건 0건」이 거짓이 되므로 별도 항목). **판정 대기(Ted) = 저장처 형태** — ⓐ 호스트 볼륨 ⓑ 객체 저장소 ⓒ 배치 정본 `contracts/storage/layout.json` 의 종류 승격. **막는 것 = `ST-1` 착수 ＋ `CT-1` 의 남은 한 칸(권한 있는 행의 다운로드).** ⚠ **완료 정의는 「완료 정의 미작성」으로 두었다 — 지어내지 않았다.**~~ | ⭑ **⟨번호 발급 2026-09-02⟩ 등재 = `PLAN-SoT §9 〈279〉`.** 초안(`notes/ST1-LEDGER-PENDING.md`)이 원장에 실렸고 **`ST-1` 이 닫혔다**(`§1 T-P` 에 행 신설 ＋ `WORK-UNITS §11` 에 행 신설)

<!-- 원 03-HANDOFF.md L472 -->
| **64** | ✅ **해소 · 재발 방지 배선 완료 (2026-09-02 · 워크트리 `lane-tsc` · 등재문 초안 `dev-package/notes/TSC-LEDGER-PENDING.md`)** — ⛔ **사고: `main` 이 12:01~22:32 배포 불가였다.** `74deb54` 가 넣은 `frontend/test/e01-apply-points.test.ts` 가 `node:fs`·`node:path`·`__dirname` 을 써 `frontend/Dockerfile:11` 의 `npm run build`(`tsc --noEmit && vite build`) 가 오류 4건으로 죽었고, **staging 배포가 이미지 빌드 단계에서 처음 걸릴 때까지 전 게이트 green** 이었다. **막지 못한 이유 = 프런트 타입 검사가 게이트에 한 줄도 없었다**(`grep -rl 'tsc --noEmit' gates/` = 0 · 검사가 이미지 빌드 **안에만** 있었다). ⚠ **함께 적는다 — 수용 실패**: 그 사이 레인 다섯이 「tsc 오류 4건 = `main` 동일」을 보고했고 **「기존 상태」로 수용**됐다. **아무도 재지 않는 검사는 「원래 그렇다」로 굳는다.** **집행** = ⑴ 시험을 브라우저 API(vite `?raw` ＋ `import.meta.glob`)로 재작성 — `tsconfig` `include` 에서 `test` 를 빼는 **범위 축소는 하지 않았다**(`CLAUDE.md §3`) · 판정 2건·양방향 집합 동일 그대로 · 시험 수 438 무감소. ⑵ **게이트 신설 `frontend-typecheck` ＋ `-selftest`** — 이미지가 도는 그 검사를 같은 tsconfig 로 돌고, `package.json` `build`·`Dockerfile` 대조로 **갈림을 red 로** 잡고, `include` 축소도 red 이며, 도구 부재는 skip 이 아니라 **red(준비 · 78)** 다. 셀프테스트 ⓕ 가 **`74deb54` 결함을 실물로 재현해 red** 를 증명한다. **남은 것** = `ruff`·`mypy` 는 Dockerfile 에도 CI 에도 **없다**(백엔드 린트·타입 검사 미배선 · 판정 대기) · geo 라이브러리 import 스모크 2건(`pipeline-worker/Dockerfile:28`·`viz-render/Dockerfile:32`)은 이미지 안에서만 성립해 후속 · **staging 재배포 확인은 배포 회차 몫**(이 레인은 배포 금지 · 로컬 이미지 빌드로만 증명) | 없음 (배선 완료 · 후속 2건은 판정 대기) |

<!-- 원 03-HANDOFF.md L473 -->
| **65** | ✅ **해소 (2026-09-03 · 워크트리 `lane-window-d4` · 등재 `§9 〈286〉`-㉮㉯ · 회차 기록 `sessions/WINDOW-20260903-D4.md §1`)** — **복원 원장 검사기가 프로파일 합격선을 안 썼다.** `infra/staging/restore/restore-db.sh` 만 전역 `COLAB_BACKUP_MIN_TABLES`(**20** · platform 형상)를 넘겨, 표 6개가 **정상**인 `colab_ai` 덤프가 **구조적 거짓 RED** 로 거부됐다 — **ai 원장이 어느 방향으로도 못 움직였다**(`§9 〈285〉`-㉱ 가 세운 차단). 형제 호출 둘(`backup/latest-check.sh`·`backup/restore-rehearsal.sh`)은 이미 프로파일 값을 썼다. **`〈171〉`-㉯ 스윕이 백업 쪽만 훑고 복원 쪽을 안 본 자리다.** **집행** = `backup/lib.sh` 에 `profile_for_db()` 신설(**미해결이면 기본값으로 안 떨어지고 선다**) ＋ `restore-db.sh` 가 `profile_min_tables`/`profile_min_rows` 사용 ＋ 빨간 픽스처 **4건 증설(34 → 38)** 로 두 방향 고정(온전한 ai 덤프 통과 · **잘린 덤프·끊긴 platform 덤프는 여전히 RED**). ⭑ **부수 발견 1건도 함께 고쳤다** — 행 합격선이 종전 전역 `1`(「행 1건이면 통과」)이었고 프로파일 값(platform 190 · ai 45)으로 올렸다. **검사가 세졌다.** **증명 = ai 원장이 이 회차에 양방향으로 실제 움직였다**(옛 짝으로 복원 → 원상복구) | 없음 (해소 완료) |

<!-- 원 03-HANDOFF.md L474 -->
| **66** | ✅ **해소 (2026-09-02 · 워크트리 `lane-window-d2` · `main` `a9a241c` · 등재 `§9 〈285〉`-㉯ · **이 회차가 실측으로 확인**)** — **복원 검증기가 확인 대상 0건인 채로 「복원 성공」을 낼 수 있었다.** 배선 = 계수 롤 분리(`COLAB_VERIFY_COUNT_ROLE`) ＋ FORCE RLS 가드(0 을 부재로 읽지 않고 `fail`) ＋ 빨간 픽스처 34건. ⭑ **D2·D3 은 이 교정을 「기구 충족」까지만 세웠고 복원된 스택에서 못 돌렸다** — **이 회차가 처음으로 살아 있는 복원 스택 위에서 돌렸고 `--manual-ok` 없이 GREEN 이다**(2회 · `sessions/WINDOW-20260903-D4.md §2.2`·`§4.3`). 축자 「손검사 2건(③-보 읽는 경로 · ⑤ 앱 롤 양성·음성)도 기계가 돌았다」. ⟹ **`R-1` 조건 ⓒ 가 이 실측으로 닫혔다** | 없음 (해소 완료) |

<!-- 원 03-HANDOFF.md L476 -->
| **68** | ✅ **해소 — Ted 판정 2026-09-03 (등재 `§9 〈305〉` · 워크트리 `lane-v2rule`)** — **선택지 ⓑ 채택. 「p95 ≤ 1초 · 상한 ≤ 5초」는 유휴 후 첫 접촉을 포함해 읽는다**(채택된 사실 = 「사용자는 유휴 후 처음 누른다」). **＋ 합산이 아니라 데이터셋별로 읽는다** — 둘 다 더 엄한 쪽이고, 그 읽는 법을 `V-2` 완료 정의 ⑻ 문면에 박았다. ⟹ **`V-2` ⑻ 은 오늘 값으로 미충족**(첫 접촉 p95 **1,164 ms · 2,111 ms** · 상한 5초는 미초과 · 관측 최대 **2,448 ms**)이고 **`V-2` 는 `in_progress` 유지**. ⚠ **판정이 났다고 `V-2` 가 닫히는 것이 아니다 — 남은 것은 사람 판정이 아니라 집행이다.** **그 집행은 신설 항목 `VL-1`(값 조회 초기화 지연 — 유휴 후 첫 접촉 p95 1초 초과의 원인 조사와 개선 · `stage2` · `open` · `owner` D7 / viz-render)이 진다.** `V-2` 를 닫는 순서 = `VL-1` ⑴ 원인 실측 → ⑵ 개선 집행 → ⑶ 유휴 30분 후 첫 접촉 포함 재측정(3 데이터셋 × 첫 접촉 각 5회 이상 · 표본 원값 보존) → ⑷ `V-2` ⑻ 재판정. ／ 이하 원문 — ~~~~ |

<!-- 원 03-HANDOFF.md L483 -->
> ⭑ **⟨2026-09-03 · 코드리뷰 개선 회차 병합 후 · 등재 `§9 〈307〉`⟩ 아래 회차별 블록에 남은 「게이트 39 종」·「43 green」은 낡았다 — 게이트는 50 종이고 병합 트리 판정은 green 50 · red 0 / 0.**

<!-- 원 03-HANDOFF.md L485 -->
> ⭑ **⟨2026-09-03 · 버그 회차 · 등재 `§9 〈310〉`-㉲⟩ ⑷ 재굽기의 순서가 하나 더 묶였다** — `BF-2`(③지도형 리샘플 교체 · 캐시 라벨 `warp+nearest`)를 **배포한 뒤** 재굽기 1회를 돈다. ⭑ ⟨갱신 2026-09-04 · advisor 게이트 ③ · `origin/main` 병합 후 재정정⟩ 재굽기 1회는 2026-09-04 구 알고리즘으로 이미 돌았고(main 〈308〉) `RC-1` 은 〈309〉 로 닫혔다(회수 1회 · 판정 불가 19 유지). `BF-2` 배포 즉시 ③지도형 층 키(`warp+nearest`)가 다시 갈려 **배포 뒤 재굽기 1회 더** ＋ 소유 계수(103/84/19) 재실측이 필요하다 — 판정 불가 19 벌의 처분은 〈309〉-㉶⑴ 이 이미 다음 회차 진입조건으로 잡아 두었다. 지고 가는 열린 항목 = `CR-2` ⑶(`RC-1` 은 done 이라 다시 열지 않는다). ⑸ `BF-6` 의 정본 `§8` 「한 줄 요약」 문구 확인이 Ted 에게 남아 있다 — 위반이면 `git revert 02738e4` 1회.

<!-- 원 03-HANDOFF.md L486 -->
> ⛔ **⟨2026-09-04 · `VL-1` 회차 · 등재 `§9 〈311〉`⟩ 다음 배포 진입조건 1건 — 배포 직후 첫 요청을 반드시 잡는다.** `〈304〉` 의 스파이크(1,164 · 2,111 ms)는 **배포 직후 첫 요청**에서 났다 — 즉 **프로세스 재기동 후 첫 접촉**(후보 ⓓ)이고, `VL-1` 회차는 재시작 0회라 그 갈래를 **한 점도 재지 못했다**(가장 유력한 원인이 정확히 못 잰 자리다). **다음 배포가 그것을 잡을 유일한 창이다** — 배포 green 직후, 다른 요청이 붙기 전에 값 조회 **4건**(GK-2A · U-Net · PV-1 · DEM)을 **첫 요청으로** 부르고 응답의 **`Server-Timing`**(`vizResolve`·`vizFindTile`·`vizReadPoint`·`vizTotal` ＋ `coreAccess`·`coreRelay`·`coreTotal`)과 `curl` 구간을 **원값으로** 남긴다. 같은 배포가 `〈304〉` 의 마지막 `[미확인]`(**서버 단독 p95**)도 함께 닫는다. 놓치면 다음 재기동까지 다시 `[미확인]` 이다. 근거 = `sessions/VL-1-20260904.md` §0·§5.

<!-- 원 03-HANDOFF.md L488 -->
> ⭑ **⟨2026-09-04 · 단독 창 4 집행 · 등재 `§9 〈312〉`⟩ 위 진입조건 두 줄이 **집행됐다** — 무엇이 닫혔고 무엇이 남았는지만 적는다.**

<!-- 원 03-HANDOFF.md L490 -->
> **다음 회차가 지고 갈 것 넷** — ㈎ ~~**`BF-4` 가 아직 안 닫힌다** — 캡션의 「· W × H」가 화면에 없다(Ted Q2 미충족 · `stage1` 미완에 남는다).~~ **⟹ 판정됨 2026-09-05 · `§9 〈315〉`-㉮ — 캡션 값(산출 격자 821×1024)을 그대로 수용, 원본 픽셀 크기는 요구하지 않는다. 계약 변경 0, 완료 정의 ⑶ 은 이미 구현된 문면대로 유효.** ㈏ **`〈304〉` 스파이크 원인은 여전히 `[미확인]`** — 후보 ⓓ(재기동 첫 접촉)를 쟀고 **재현되지 않았다**. 후보가 하나 줄었을 뿐이다. ㈐ ⛔ **키 변경분은 회수 대상이 아니다** — `artifact-ownership` 이 옛 키 산출물도 `살아 있다` 로 세어(사이드카 원천이 살아 있다) **고아 0** 이 나온다. 자리 파일은 `227 → 236` 으로 쌓였다. 지목·회수는 `TL-1` 이 진다. ㈑ **`BF-6` 정본 §8 문구 확인**은 배포 green 과 무관하게 Ted 대기 그대로다. ㈒ ⭑ **⟨신설 2026-09-05 · `§9 〈315〉`-㉯⟩ 판정 불가 19벌 재굽기·재계수** — 걸린 데이터셋을 재굽고 `artifact-ownership` 을 재실측해 판정 불가가 주는지 본다(`tolerate` 기한 연장이 아니다). `RC-1`(`done`)은 다시 열지 않는다 — 이 창의 진입조건/단계다. **대상 데이터셋 목록은 `[미확인]` — 이 창에서 실측한다.**

<!-- 원 03-HANDOFF.md L491 -->
> ⭑ **⟨신설 2026-09-05 · `〈316〉` · 다음 배포의 진입조건⟩ 지도 타일 회수는 **관측 전용으로 배포된다** — `COLAB_VIZ_TILE_RECLAIM_APPLY` 를 선언하지 않으면 루프가 세고 로그에 적기만 하고 **0건 지운다.** **켜기는 계수 증명 뒤 홈 env 한 줄**이다(compose 는 `${COLAB_VIZ_TILE_RECLAIM_APPLY:-}` 로 통과만 시킨다). ⚠ 켜기 전에 볼 것 = 배포 뒤 로그의 「지도 타일 회수 — … 못 닿는다 N · 상한 20 · 관측 전용(0건 지웠다)」 한 줄이고, **N 이 상한(20)을 넘으면 루프가 스스로 멈춘다** — 그것은 고아가 는 것이 아니라 주체 쪽이 무너진 신호다(마운트 소실·자리 이동). staging 의 못 닿는 벌은 `〈313〉` 실측 **0** 이라 켜도 지금 지울 것이 없다.**

<!-- 원 03-HANDOFF.md L493 -->
> ⭑ **⟨2026-09-05 · `V-1` 회차 ＋ 단독 창 5/5b · 등재 `§9 〈317〉`⟩ 창을 막던 red 가 닫혔고, 창이 새 진입조건 하나를 더 얹었다.**

<!-- 원 03-HANDOFF.md L496 -->
> ⑶ ⛔ **새 배포 진입조건 — 소유 계수를 재굽기 *전에* 다시 센다**(근거 = A5b `§0.1`). 자리 파일이 **236 → 285** 로 늘었다(2026-09-05 10:07~10:09 KST 한 덩어리 · `viz_render` 자동 재기동 `ExitCode=0` · 이미지 불변 `f465d83a2ef1`). **쓴 주체 `[미확인]`** — 이 회차가 쓴 것이 아니다. ⟹ **`〈312〉` 의 「판정 불가 19」는 더 이상 실측값이 아니다.** `§6`(iii) `artifact-ownership` 을 먼저 돌려 소유 계수를 **재실측한 뒤에** (iv) 재굽기를 연다. before 기대치도 236 이 아니라 **285** 다.

<!-- 원 03-HANDOFF.md L498 -->
> ⭑ **⟨정정 2026-09-05⟩ 순서는 「`r18` 병합 → 창 5b」가 아니라 「창 5b → `r18` 병합」이다.** ⚠ **직전 인계문에서 그 종전 문면(「`r18` 병합 → 창 5b」)은 `grep` 으로 찾지 못했다 — 지울 원문이 없어 지우지 않았고, 이 줄이 순서를 세우는 자리다.**

<!-- 원 03-HANDOFF.md L500 -->
> ⭑ **⟨2026-09-05 · 단독 창 5b 단계 B · 등재 `§9 〈318〉` · 전문 `sessions/WINDOW-20260905-A5b.md §10`⟩ 창이 닫혔다 — 무엇이 닫혔고 무엇이 다음 회차로 넘어가는지만 적는다.**

<!-- 원 03-HANDOFF.md L505 -->
> ㉢ **`TL-1` 첫 한 바퀴 계수** — 관측 전용으로 섰고 **삭제 0건**은 실측됐으나 **루프는 「못 봤다」**(경과 30분 · 주기 3600초). **기동 60분 뒤 = 2026-09-05 14:22 KST 이후** `docker logs colab_v2_staging_viz_render` 에서 첫 바퀴 계수를 읽는다. `〈313〉` 의 「켜도 지금 지울 것이 없다」는 그때까지 `[미확인]`.

<!-- 원 03-HANDOFF.md L510 -->
> ⭑ **⟨2026-09-05 · 등재 회차 · `§9 〈321〉`~`〈324〉`⟩ 위 ㉠·㉡ 이 닫혔고, 다음 회차의 착수 순서가 정해졌다.**

<!-- 원 03-HANDOFF.md L538 -->
> ⭑ **2026-09-02 (등재 회차) — 진입조건 표를 늘리지 않았다. 다음 행동과 판정 대기만 갱신한다.**

<!-- 원 03-HANDOFF.md L543 -->
> ⭑ **⟨갱신 2026-09-02 · 등재 `〈279〉`·`〈280〉`·`〈281〉`⟩ 위 셋은 그대로 살아 있고, 이 회차가 더한 것은 아래다.** ⑷ **`ST-1`·`P8` 은 닫혔다 — 다음 작업에서 내린다.** ~~⑸ **`CT-1` 을 닫으려면 판정 하나다** — 「승인 처리 도착 전 `Verified` 열에 무엇을 적는가」.~~ ⭑ ⟨해소 2026-09-02 · `§9 〈282〉`-㉮⟩ **판정·집행 완료.** ⛔ **`CT-1` 은 그래도 `open`** — 남은 것은 **헤더 고정 시험 1건**(프런트 시험 도구 제약 · `〈282〉`-㉱). ~~⑹ **기간의 입력 형태 판정**~~ ⭑ ⟨해소 2026-09-02 · `§9 〈283〉`⟩ **판정·집행 완료 — 14차 동결 해제.** 남은 것은 표기 문면·스펙 19 문면 둘뿐이다(아래 판정 대기).

<!-- 원 03-HANDOFF.md L545 -->
> ⭑ **⟨갱신 2026-09-02 · 등재 `〈282〉`·`〈283〉`⟩ 이 회차가 더한 것.** ⑺ **`CT-1` 의 잔여는 이제 판정이 아니라 시험 하나다** — 「표 헤더가 스크롤 중 고정되는가」를 jsdom 이 재지 못한다. **막는 것 = 프런트 시험 도구**(브라우저 층 시험 도입 여부가 착수 조건). ⑻ **기간 변경분은 staging 에 없다** — 계약·서버·화면·시험은 다 섰고(`§9 〈283〉`) 배포 0 이라 **실물은 옛 계약을 든 이미지로 돈다.** ⑼ ⚠ **끝이 NULL 인 기존 행이 화면에 새로 나타날 수 있다 — 건수 미계수**(staging 무접촉).

<!-- 원 03-HANDOFF.md L550 -->
> ・ ~~**파일 저장처(`ST-1`)** = **형태 판정**(호스트 볼륨 / 객체 저장소 / 배치 정본의 종류 승격). **그 전에는 완료 정의를 쓸 수 없다** — 「완료 정의 미작성」으로 두었다(`§4 #63`).~~ ⭑ ⟨해소 2026-09-02⟩ **판정 = 볼륨 그대로 쓴다** ⟹ 완료 정의 기재 ＋ 내려받기 경로 집행 완료(`§4 #63`).

<!-- 원 03-HANDOFF.md L551 -->
> ・ ~~**데이터셋 생성 요청의 계약↔런타임 드리프트** = `variables`·`crs`·`period` 를 **계약에서 빼는가 · 서버가 받게 하는가 · 어느 WU 가 지는가**(`§4 #62`).~~ ⭑ ⟨해소 2026-09-02⟩ **판정 = 등록 화면에서 세 값의 입력을 허용한다** ⟹ **서버가 받게 한다**(계약 무변경) ＋ 화면 세 칸 개방. 집행 완료(`§4 #62`). **새로 열린 판정 둘** = ⓐ ~~**기간 입력 형태**(정본 스펙 19 의 한 칸 자유 문장 ↔ 계약 `DataPeriod` 두 `date-time`)~~ ⭑ ⟨해소 2026-09-02 · 14차 해제⟩ **판정 = 두 칸 ＋ 끝은 조건부**(끝이 없으면 무기한). 집행 완료(`§4 #62`) · ⚠ **UI 스펙 19 문면 갱신은 레포 밖에 남는다** ⓑ **격자 입력**(정본 스펙 21 은 사람 입력인데 계약에 `grid` 가 없다).

<!-- 원 03-HANDOFF.md L552 -->
> ・ ~~⭑ **⟨신규 2026-09-02 · `〈280〉`-㉷⟩ 기간의 입력 형태** = ⓐ UI 스펙 19 대로 한 칸 자유 문장으로 되돌린다(동결 해제 ＋ 마이그레이션) ⓑ 지금의 날짜 두 칸을 정본으로 승격한다.~~ ⭑ **⟨해소 2026-09-02 · Ted 판정 · 등재 `§9 〈283〉`⟩ ⓑ 도 ⓐ 도 아닌 제3안 = 두 칸을 유지하되 끝을 조건부로 연다.** `DataPeriod.end` → `[string, "null"]` · **계약 동결 해제 14차** · 소비자 2건 같은 회차 수정 · 마이그레이션 **0**(DB 열이 처음부터 nullable). ⚠ **가산이 아니다 — `contract-breaking` ERR 7**(`response-property-became-nullable`). **귀속 = `P2`**(등록 화면 입력 칸 · 대장 `P2.note`).

<!-- 원 03-HANDOFF.md L553 -->
> ・ ⭑ **⟨신규 2026-09-02 · `〈280〉`-㉸⟩ 격자(UI 스펙 21)의 소유·처분** = 계약 `DatasetCreate`·`DatasetUpdate` 에 `grid` 가 0건이다(`〈74〉` — 서버가 세는 값). **스펙은 사람이 적는 선택 입력으로 그린다.** 어느 쪽이 정본인가 · 어느 WU 가 지는가 미정.

<!-- 원 03-HANDOFF.md L554 -->
> ・ ⭑ **⟨신규 2026-09-02 · `〈279〉`-㉳⟩ `CT-1` 의 `Verified` 열 문구** = **승인 처리가 도착하기 전** 그 열에 무엇을 적는가. **`CT-1` 을 닫는 유일한 잔여다.** ⭑ **⟨판정·집행 2026-09-02 · 워크트리 `lane-p4x`⟩ Ted 판정 = 승인 처리 도착 전에는 그 열의 글자를 **취소선·회색·비활성** 모양으로 둔다.** 집행 완료(`CatalogTable.tsx` ＋ `catalog.css` `.verified--pending` · 시험 3건) ⟹ `[미확인]` ㈎·㈏ 닫힘. ⛔ **그래도 `CT-1` 은 열려 있다** — 조항 ⑴ 의 **헤더 고정 시험 1건**이 도구 제약으로 `[미확인]` 이다. **등재 = `§9 〈282〉`-㉮·㉱**(초안 `notes/P4X-LEDGER-PENDING.md` ㉯). ⚠ **막는 것은 제품 판정이 아니라 프런트 시험 도구다 — 판정 대기가 아니라 시험 대기로 성격이 바뀌었다.**

<!-- 원 03-HANDOFF.md L555 -->
> ・ ⭑ **⟨신규 2026-09-02 · `〈279〉`-㉲⟩ 내려받기 묶음의 두 값** = ㈎ 묶음에 **기준 격자 파일**을 넣는가(지금은 본체만) ㈏ **큰 파일의 전달 방식**(지금은 메모리 zip · 상한 용량·스트리밍 요건 0건). **`ST-1` 을 닫는 것을 막지는 않았다 — 다음 회차 진입조건이다.** ⭑ **⟨판정·집행 2026-09-02 · 워크트리 `lane-p4x`⟩ 둘 다 판정되고 집행됐다.** ㈎ **격자 파일을 묶음에 넣는다**(`d3_catalog.files_for_download` · `kind IN (본체, 기준 격자 파일)`) ㈏ **용량 상한 없음 ⟹ 스트리밍 zip**(`kernel/file_store.stream_bundle` · 메모리 zip 제거 · **라우트 계약 무변경**). **등재 = `§9 〈282〉`-㉯·㉰.** ⚠ **RSS 로는 재지 않았다 — 증명은 버퍼 상한 ＋ 생성기 ＋ 복원 대조 넷이다.**

<!-- 원 03-HANDOFF.md L556 -->
> ・ ⛔ ⭑ **⟨신규 2026-09-02 · 워크트리 `lane-p4x`⟩ 데이터셋 기간의 끝을 선택으로 여는 것은 계약 동결 해제가 필요하다 — 멈췄다.** Ted 판정 = 기간은 optional 이고 **끝은 조건부**(없으면 무기한). 실물 = `contracts/seams/fe-core.yaml` `DataPeriod`(2162~2171행)가 `required: [start, end]` 이고 `end` 가 nullable 도 아니다 ⟹ **개정 없이는 성립하지 않는다.** 다음 동결 해제 회차는 **14차**. ⭑ **⟨집행 2026-09-02 · 워크트리 `lane-r14` · 등재 `§9 〈283〉`⟩ 14차 해제가 승인·집행됐다** — `required` 는 그대로 두고 `end` 만 `[string, "null"]`(`ProjectPeriod` 와 같은 required-but-nullable · 새 무늬 0건). **회차표 = `sessions/X2-FREEZE-PROTOCOL.md §1` 14행 = `〈283〉`.**

<!-- 원 03-HANDOFF.md L557 -->
> ・ ⚠ ⭑ **⟨신규 2026-09-02 · `〈283〉`-㉸-㈎⟩ 열린 기간의 표기 문면 `[미확인]`** = 정본에 없다. 지금 값은 상세 격자 `2025-06 ~ 진행 중` · 소속 데이터셋 표 `2025-06~` 이고 **레포 안 선례를 따른 것이지 정본 근거가 아니다.** **Ted 판정 대상.**

<!-- 원 03-HANDOFF.md L558 -->
> ・ ~~⚠ ⭑ **⟨신규 2026-09-02 · `〈283〉`-㉸-㈏⟩ UI 스펙 19 문면 갱신** = 「기간 = 한 칸 자유 문장」이 그대로다. 화면은 두 칸 ＋ 끝 선택으로 서 있고 **문면만 낡았다. 레포 밖 문서라 편집하지 않았다** — 다음 회차 진입조건.~~ ⭑ **⟨해소 2026-09-03 · `§9 〈293〉`⟩ 정본 개정 완료** — UI 스펙 19 를 「두 칸 ＋ 끝 조건부」로 고쳤다.

<!-- 원 03-HANDOFF.md L559 -->
> ・ ⭑ **⟨신규 2026-09-03 · `§9 〈293〉`⟩ 정본 미반영 목록 = `dev-package/notes/SPEC-DELTA-PENDING.md` 한 곳이다.** 닫힌 델타 **6**(`Policy_데이터_찾기` v2.0 · `Policy_프로젝트` v1.7 · `Policy_홈_대시보드` v1.6 · 목업 공개 범위 2값 · UI 스펙 19 · 패키지 재동기) · **판정 대기 2** — ⓐ **데이터셋 상세 섹션 순서**(`Policy_데이터셋_상세` v2.8 「계보 → 미리보기」 ↔ `IA_사이트맵` 4장 「미리보기 → 계보」 · **구현은 Policy 를 따른다** · 두 문면 축자 병기) ⓑ **`IA_사이트맵` 의 자연어 검색 서술**(ⓐ 와 같은 파일이라 한 번에 고친다) · **검수 오류 정정 1** — QA `#30` 「`엿보기` = 정본 침묵」은 사실이 아니다(`Policy_데이터_찾기` `§2`·`§5`·`§8` 축자). ⚠ **다만 「미리보기 없는 데이터셋의 엿보기」는 여전히 미정이다**(`_재검토_E02-E03_260826.md` 20행).

<!-- 원 03-HANDOFF.md L560 -->
> ・ ⚠ ⭑ **⟨신규 2026-09-02 · `〈283〉`-㉸-㈐⟩ 끝이 NULL 인 기존 저장 행의 건수 `[미확인]`** = `d3_dataset_autometa` 를 세지 않았다(staging 무접촉). **종전 응답 코드가 그런 행의 기간을 통째로 떨어뜨리고 있었으므로 이 변경으로 화면에 새로 나타나는 기간이 있을 수 있다.** 푸는 법 = 배포 회차에 읽기 전용 계수.

<!-- 원 03-HANDOFF.md L561 -->
> ・ ⭑ **⟨신규 2026-09-02 · `〈280〉`-㉹⟩ 결번 회차의 표기 규약** = 이번에 `7-A` 차라는 **문자 첨자**를 썼는데 **프로토콜에 그 규약이 없다.** 규약으로 굳힐지 판정이 필요하다.

<!-- 원 03-HANDOFF.md L565 -->
> ⚠ ⭑ **⟨갱신 2026-09-02 · 등재 회차 `〈279〉`·`〈281〉` 뒤⟩ `ST-1`·`P8` 이 닫혀 `stage2` 미완이 둘 줄었다** — `deferred` 포함 **11** / 제외 **10** / `PA-G` 제외 **9** · `stage1` 미완 **3** · **총 미완 13**(`PA-G` 포함 · `deferred` 제외). **셈법 규칙은 `〈258〉` 그대로이고 바뀐 것은 값뿐이다.** ⚠ **자동 계수는 여전히 0건 `[미확인]`.** ／ 종전 문면 ↓

<!-- 원 03-HANDOFF.md L566 -->
> ⚠ **계수 기준을 함께 적는다** — 이 회차의 실측(대장 전수 · 2026-09-02) = `stage2` 미완 **12**(`deferred` 포함) / **11**(제외) / **10**(`PA-G` 제외) · `stage1` 미완 **3**. **`ST-1` 신설 뒤에는 각각 13 / 12 / 11 이다.** 종전 `〈275〉` 의 「14 → 13 → 12 · 총 15」는 **2건 과다**였고 `〈278〉`-㉮ 가 정정했다.

<!-- 원 03-HANDOFF.md L568 -->
> ⭑ **2026-09-03 (잔여 갱신 회차 · 등재 `§9 〈287〉`) — 진입조건 표를 늘리지 않았다. 계수와 다음 작업만 다시 잰다.**

<!-- 원 03-HANDOFF.md L571 -->
> **실측 (2026-09-03 · 대장 `8cc54fe` 전수 97항목)** = `stage2` 미완 **10**(`deferred` 포함) / **9**(제외) / **8**(`PA-G` 제외) · `stage1` 미완 **3** · **총 미완 12**(`PA-G` 포함 · `deferred` 제외).

<!-- 원 03-HANDOFF.md L583 -->
> ⭑ **⟨갱신 2026-09-03 · `§9 〈289〉` · 워크트리 `lane-loop-b`⟩ 위 목록에서 넷이 빠진다 — 원문은 지우지 않는다.**

<!-- 원 03-HANDOFF.md L593 -->
> ⭑ **2026-09-03 (집행 루프 마감 회차 · 등재 `§9 〈302〉` · 워크트리 `lane-close`) — 진입조건 표를 늘리지 않았다. 계수·판정·다음 작업만 다시 잰다.**

<!-- 원 03-HANDOFF.md L596 -->
> **실측 (2026-09-03)** = `stage1` 미완 **0**(항목 **44 전건 `done`** ⭐) · `stage2` 미완 **10**(`deferred` 포함) / **9**(제외) / **8**(`PA-G` 제외) · **총 미완 9**(`PA-G` 포함 · `deferred` 제외).

<!-- 원 03-HANDOFF.md L613 -->
> ・ ~~**활용 프로젝트의 「담당」** = **계약에 추가한다**(정본이 이긴다) — **계약 17차 대상 · 레인 `lane-r17`**.~~ ⭑ **⟨대체 2026-09-03 · `§9 〈306〉` · 레인 `lane-spec2`⟩ 「정본에서 내린다」로 바뀌었다**(계약 0 · DB 0) — 생산자가 없어 언제나 null 이고 시험할 수 없다(`〈299〉`). `Policy_데이터셋_상세` **v2.10** 이 `§5` 의 「담당」을 취소선으로 내렸다. **판정 대기에서 내린다.**

<!-- 원 03-HANDOFF.md 「최종 갱신」 — 2026-09-05 병합 회차에 밀려난 문단 (원 L27) -->
**최종 갱신** 2026-09-05 (**★ 이 회차 = `BF-12` 코드 병합 ＋ 기획 정본 폴더 이동 대응 ＋ `TL-2` 착수 보류. 등재 `PLAN-SoT §9 〈325〉`·`〈326〉`.** ⑴ **`BF-12` 코드가 `main` 에 병합됐다**(`14db1a8` — 신규 `kernel/logging_setup.py` · `app/main.py` ＋5줄 · 시험 ＋4건 ⟹ `service-tests-viz-render` 수집 **330** green). **완료 정의 ⑴⑵⑷ 충족 · ⑶(staging 배포 뒤 첫 바퀴 요약 1줄 실물) 미충족** ⟹ 대장 **`open` 무변 · `§1` 표기도 ⬜ 무변**(🟧 는 대장 `partial` 과 짝이라 게이트가 대장 `open` ↔ 🟧 를 red 로 낸다 — 실질은 「코드 병합 · staging ⑶ 대기」이고 그 문면을 `§1` 비고에 적었다). ⑵ **게이트 — 병합 트리 `ed8ceb3` 기준 두 실행에 걸쳐 선언 50 · green 50 · red(판정) 0 · red(준비) 0.** 첫 실행 `all -j 1` = green 43 · red(판정) 1 · red(준비) 6 · 그 **판정 red 1 = `planning-freshness` 이고 원인은 `BF-12` 가 아니라 기획 정본 폴더 이동**(`370ed99` 로 닫힘) · red(준비) 6 은 셸 입력 미선언이라 `source ~/.colab-v2-test.env` 뒤 재실행 10 종 전건 green. ⚠ **「한 번에 50 green」이 아니다 — 두 실행에 걸친 셈이다.** ⑶ **기획 정본 폴더가 `00_기획원본/` 아래로 이동**(`〈326〉`) — 고친 것은 **게이트 상수 `DEFAULT_PLANNING_ROOT` 와 `planning/README §1` 둘뿐**이고 **원장 33개 파일의 옛 경로 인용은 고치지 않는다**(Ted 규칙). 대응표는 `<작업공간>/40 COLAB-기획/README.md` §「경로 대응표」. ⑷ ⛔ **`TL-2` 는 착수하지 않았다** — 진입조건 「`BF-12` 닫힘」 미충족(`CLAUDE.md §4`). 착수 자리는 **다음 배포 창(창 6)**. ⛔ **staging 무접촉 · 배포 0 · 삭제 0 · 소유 계수 무변**(대상 136 · 살아 있다 117 · 접수분 0 · 고아 0 · 판정 불가 19).**)
