# codegen — 계약에서 생성되는 것

| 대상 | 산출물 |
|---|---|
| 백엔드 | pydantic 모델 |
| 프론트엔드 | TS 타입 + 클라이언트 |
| DB | 정규 ID 타입 등 공통 컬럼 정의 |

**생성물은 커밋한다.** `generated-up-to-date` 게이트가 재생성 후 diff로 검증하므로 추적되어야 한다.
**손으로 고치지 않는다.**
