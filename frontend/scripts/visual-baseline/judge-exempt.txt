# 수치 판정 면제 목록 — spec S-DEVICE-WIDTH-INPUT-20260926 「구현 결정 · 수치 판정 스크립트」(judge.mjs 가 읽는다).
# 한 줄 = `지표 · 선택자 · 사유` (구분자 「 · 」). 지표는 44 · 16 · 넘침 중 하나다. 가림은 면제하지 않는다.
# 선택자는 캡처 때 측정 스크립트(measure.js)가 red 후보 요소에 맞춰 본다(el.matches). 목록을 바꾸면 수치를 다시 잰다
# (수치 파일이 찍을 때 받은 목록과 지금 목록이 다르면 78).
# red 이고 아무것도 면제하지 않는 줄: 형식 오류 · 빈 사유 · 모르는 지표 · 맞는 것이 없는 줄(구멍 · 레인 거르기 없는 실행).
# 지금 면제할 것은 없다(L0b).
