# 프리미티브 클래스 목록 — 게이트 frontend-design-lint e 의 정본(spec S-DESIGN-STRUCTURE-P2B-20260924).
# 한 줄에 클래스 하나 · 접두 표기 `.chip--*` 허용. 목록의 클래스는 frontend/src/shell/primitives.css 밖에서
# 맨 정의(문맥 없는 `.btn { }` · `.btn:hover { }` · `:is(.inp, .sel) { }`)를 가질 수 없다.
# 계열을 primitives.css 로 옮긴 커밋마다 그 계열 클래스만 더한다 — 모든 계열 경계에서 green.
# btn
.btn
.btn-primary
.btn-secondary
.btn-ghost
.btn-danger
.btn-sm
# field
.inp
.sel
# chip
.chip
.chip--*
# card
.card
.card-h
.card-b
# table
.tbl
.tblwrap
.table-scroll-hint
