# (픽스처) PLAN-SoT — 결정 로그만 담는다

이 파일은 `work-item-consistency ㈔` 의 **대조군 픽스처**다. 레포의 실제 결정 로그를
들고 오지 않는다 — selftest 가 레포의 진행 상태에 볼모잡히면 안 된다
(`work-item-selftest.sh` 머리말과 같은 이유).

## 9. 결정 로그

### 확정

| # | 결정 | 값 · 근거 |
|---|---|---|
| ① | **동그라미 계열** | 이 계열은 아래 이관 표가 다시 인쇄한다 — 중복이 정상이라 `㈔` 가 세지 않는다 |
| 〈51〉 | **번호가 붙은 결정 하나** | 값과 근거. *픽스처* |

| 〈52〉 | **행 사이에 빈 줄이 있어도 같은 표다** | 실제 문서가 그렇다. *픽스처* |
| 〈53〉 | **셋째 결정** | 값과 근거. *픽스처* |

### 열린 것

| # | 결정 | 막는 것 | 담당 WU |
|---|---|---|---|
| 〈54〉 | **열린 것도 결정 번호를 쓴다** | 이 표도 대상이다 · 본문이 `〈51〉`·`〈51〉`·`〈52〉` 를 인용해도 중복이 아니다 | `X-0` |

#### 확정으로 내려간 것

| # | 결정 | 해소 근거 |
|---|---|---|
| ① | **동그라미 재인쇄** | 이관 기록이다 — 어긋남이 아니다 |
