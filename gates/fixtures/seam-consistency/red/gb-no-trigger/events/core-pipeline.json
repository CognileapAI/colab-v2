{
  "$defs": {
    "EventType": { "enum": ["upload.accepted"] },
    "UploadAccepted": {
      "properties": {
        "type": { "const": "upload.accepted" },
        "source": { "const": "core-api" }
      }
    }
  }
}
