openapi: 3.1.0
info:
  title: red fixture — source const core-api 인데 촉발 op 이 없는 seam (SEAM-AUDIT I-01·I-05)
  version: 0.0.1
paths:
  /datasets:
    get:
      operationId: listDatasets
      description: 목록 조회. 업로드 이벤트를 촉발하지 않는다.
      responses:
        "200": { description: ok }
