# frontend-visual 허용 목록 — 셀렉터 **접두사** 한 줄에 하나. 빈 파일이어도 된다(=전건 판정).
#
# live_probe.js 가 내는 `sel` 은 `<tag>#<id>.<class1>.<class2>` 꼴이다. 여기 적힌 접두사로
# 시작하는 항목만 계수에서 빠진다. 접는 이유를 주석으로 함께 적는다 — **판정은 사람이 한다.**
# 예:
#   svg.legend-tick   # 차트 눈금 라벨 11px — Ted 판정 〈…〉 에서 예외로 승인
#
# (현재 승인된 예외 0건)
