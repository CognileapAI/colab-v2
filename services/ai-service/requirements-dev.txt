# 시험 전용 닫힘. 런타임 이미지에 들어가지 않는다.
#   재생성: .venv/bin/pip install -r requirements.in -r requirements-dev.in && .venv/bin/pip freeze
PyYAML==6.0.2
certifi==2026.7.22
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
pytest==8.4.2
