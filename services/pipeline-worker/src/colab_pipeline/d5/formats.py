"""지원 포맷 — 〈51〉. 숫자가 아니라 목록이다.

PoC 의 4포맷(GRIB·NetCDF·Binary·HDF5)과 수만 같고 구성이 다르다:
GRIB 이 빠지고 GeoTIFF 가 들어오며, HDF 는 실측상 HDF4 다 (SEED-DATA F-2).

⭑ `NumPy` 가 `〈77〉` 로 들어왔다 — Ted 판정: 「npy 도 처리해야 하는 포맷이다만.
추가 포맷으로 한다. nc 랑은 다른 파일이다.」 **격자 파일의 다른 표현형이 아니라
독립 포맷이고, 본체로 올라오면 본체다** — 무엇인지는 `kind` 가 정한다(`§E.3b`).
⚠ 같은 이름의 목록이 `services/viz-render/.../readers.py` 에도 있다. 함께 고친다.

⭑⭑ **`〈51〉` 의 GRIB 제외가 뒤집혔다 (`〈134〉` · 2026-08-26 Ted 판정 ㈎).**

**`〈51〉` 의 원래 근거는 지우지 않는다** — 나중에 「왜 뒤집었나」를 볼 수 있어야 한다:
> S1 실측에서 원천에 GRIB 원파일이 0 건이고(`file_format_1_grib/` 에 `00.Data` 없음,
> 파생 `.npy`·`.png` 만 잔존), 「없는 데이터로 E2E 를 요구하면 완료 조건이 영원히 안
> 닫히거나, 아무도 안 쓸 포맷을 위해 디코더를 짜게 된다」.

**반전 근거** — `POLICY-20260825-001` 이 5종(`nc·grib·tif·HDF·bin`)을 정본으로 두었고
결정 2-3 이 `〈51〉` 을 명시적으로 뒤집었다. 정본 지정은 `〈129〉`. **원파일 0 건이라는
사실은 그대로이나, Ted 가 ㈏(완료 조건에서 제외)가 아니라 ㈎(수급한다)를 택했다** —
레퍼런스 자료를 직접 넣기로 했다. 즉 `〈51〉` 이 든 위험은 **해소된 것이 아니라
수급으로 막는 것**이고, **파일이 들어오기 전까지 E2E 완료 조건은 닫히지 않는다.**

**목록은 6종이다 — 5종이 아니다.** 정본 5종에는 `NumPy` 가 없고 `〈77〉` 이 그것을
이미 독립 포맷으로 들여놨다. **두 목록은 수가 같고 구성이 달랐다.** 합집합으로 적는다.
**숫자로 세지 말고 목록으로 읽는다** — 여기서 실제로 한 번 틀렸다.

⚠ **GRIB 은 지원하되 그릴 수 없다.** 결정 2-3 이 스스로 적었다 — 「5종이어도 grib 은
미리보기 대상이 아니다(미리보기는 bin·nc·tif·HDF)」. 따라서 이 포맷이
`SUPPORTED_FORMATS` 와 `RENDERABLE_FORMATS` 를 **처음으로 가른다**(`renderable.py`).
"""
from __future__ import annotations

#: **숫자가 아니라 목록이다.** 정본 5종(grib 포함) ∪ `〈77〉`(NumPy) = 6종.
SUPPORTED_FORMATS: list[str] = [
    "NetCDF", "Binary", "HDF4", "GeoTIFF", "NumPy", "GRIB"]

#: 못 읽은 값의 표기 — 추정으로 채우지 않는다 (DR-9 · DATA-REFERENCE §0)
UNKNOWN = "[미상]"
