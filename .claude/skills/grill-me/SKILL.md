---
name: grill-me
description: A relentless interview to sharpen a plan or design.
disable-model-invocation: true
---

Call the Skill tool with "grilling".

## 종료 시 — intent.md 초안 (CoLAB v2 개조)

프론티어가 비고 Ted 가 공통 이해 도달을 확인한 뒤, `dev-package/intent/<YYYY-MM-DD>-<주제>.md` 초안을
`dev-package/intent/TEMPLATE.md` 형식 그대로 작성한다.

- **확인 문장은 Ted 가 쓴 원문 그대로** `## 확인` 절에 싣는다. 요약·윤문 금지.
- `## 미해결 질문` 은 **0건**이어야 한다. 남아 있으면 프론티어가 빈 것이 아니므로 라운드를 더 돈다.
- 작성 후 사용자에게 알린다 — 이 초안은 **Ted 의 교정·커밋을 기다린다. 커밋이 곧 승인**이다.
  승인 전에는 후속 작업(spec 합성·구현)을 시작하지 않는다.

Before reporting, check each claim against this session's tool results.
