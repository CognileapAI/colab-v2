# CLAUDE.md — colab-v2 상시 지침

이 파일은 매 세션 자동으로 읽힌다. **여기 있는 규칙은 예외 없이 적용된다.**

---

## 0. 이 레포가 무엇인가

수문학 연구 데이터 협업 플랫폼 **CoLAB v2**. 대학원 연구실의 데이터가 졸업·인수인계와 함께 사라지지 않도록 수집·가공·공유를 하나의 흐름으로 잇는다.

| | |
|---|---|
| 기획 정본 | 이태헌(lth) 1차 마일스톤 **260818** — 에픽 8개(E-00~E-07) |
| 제품 성격 | **역할별 구조화 화면이 정문.** AI는 두 지점에만 얇게 얹힌다 |
| AI 두 지점 | ⭑ **⟨개정 2026-08-29 · `PLAN-SoT §9 〈197〉`-㉲⟩ 이전 표기 ~~① 업로드 시 계보 제안(사람 확인 필수) ② 자연어 검색~~ — 이는 **260818 판**이고 260826 결정(**둘 다 이번 릴리즈 밖**)을 반영하지 않았다.** **현재 위상** — ① **업로드 시 계보 제안** = **사람이 직접 고르는 것이 기본**이고 AI 추천은 **사용자가 버튼을 눌러 받는 보조**다(`〈197〉`). 사람 확인 필수는 그대로다. 서비스는 `K3`(**stage 3** · 2026-08-30 판정 · 등재문 `sessions/STAGE3-AI-SPLIT.md`) · 버튼은 `LV-2`(**stage 1** · `〈203〉`). ／ 종전 ~~`K3`(stage 2) · `LV-2`(stage `[미확인]`)~~ ⚠ **화면은 지금도 자동으로 부른다** — `LineageStep.tsx:90` 이 업로드 1건당 1회 자동 조회를 걸고 있고, 그것을 버튼으로 바꾸는 것이 `LV-2` 다. ② **자연어 검색** = 서비스는 서 있으나 **`K4` 는 열려 있다**(**stage 3** · 2026-08-30 판정 · 등재문 `sessions/STAGE3-AI-SPLIT.md`). **합격선은 퍼센트가 아니라 「미판정 0건 ＋ 판정 후 전건 통과」다**(`〈217〉`) — 평가셋 18건은 `eval/s2b-alayer/` 에 **실재**하고 `S2b` 는 `done` 이다(`〈208〉`). ／ 종전 ~~완료 판정의 오라클(평가셋)이 초안이고 오라클의 자리 자체가 `〈182〉` 로 다시 정해야 한다(`[미확인]`)~~ ⭑ **그래도 「AI 없이도 v2 는 완결된 제품이다」(`§3`)는 바뀌지 않았다** — 두 지점 다 없이 등록·검색이 끝까지 간다 |
| 코드 | **v2 기준 신규 구축.** PoC·v1 코드 미계승 — 도메인 지식·방법론만 참조 |
| 완료 조건 | **staging 배포까지, v2 전체.** 로컬 green은 완료가 아니다. **prod(AWS)는 `PLAN-SoT §9-㊻` 로 보류** — staging 에서 v2 전체가 충분히 동작하고 Ted 가 출시를 결정했을 때 연다 |
| 진행 단위 | 주차 일정 없음. **작업 단위(WU)** |
| **stage** | **v2 는 두 단으로 짓는다**(`PLAN-SoT §9 〈70〉`). **stage 1** = 업로드 · 데이터셋 인포 · AI 검색 · 연구실-프로젝트-데이터셋 · 계보 설정 **＋ 미리보기 3층·기준 격자 파일 수용**(`〈74〉`·`〈75〉`). **stage 2** = **미리보기 뒷단**(헤더 파싱·좌표계 통일·지도용 영상 변환 · `PV-1`) · **확대** · 스크린샷 · S-08 · 값 조회 · 편의 기능 묶음(`〈78〉`). ⭑ **⟨개정 2026-08-29 · `〈189〉`⟩ 이 단계를 「가공」이라 부르지 않는다** ／ 이전 표기 ~~가공(파싱·좌표계·COG)~~ — **「가공」은 새 데이터셋을 만드는 쪽 하나만 가리킨다**(신규 기능 **데이터 프로세스** · 시점 **stage 2 다음**). **미리보기는 데이터셋이 새로 생기지 않는다.** ⭑ **⟨개정 2026-08-31 · `〈240〉`⟩ 「타일 서빙」은 요구가 아니라 선택 갈래다** — 기본값은 「한 장」(`imageUrl`) · 정본 축자 「타일 서버도 바탕 지도도 쓰지 않는다」(POL-021) ／ 종전 ~~타일·확대~~ ⚠ **stage 1 green 은 v2 완료가 아니다** ⭑ **⟨증보 2026-08-30 · 등재문 `sessions/STAGE3-AI-SPLIT.md`⟩ 대장에는 세 번째 단이 있다** — `stage: after_stage2`(＝ Ted 가 이름 붙인 **stage 3**) · 2026-09-01 실측 **15항목** <!-- work-items:after_stage2 -->(`C1`·`C2`·`C3`·`C3·C4`·`C4`·`DP-1`·`G1b`·`K3`·`K4`·`K5`·`LV-3`·`LV-4`·`P4`·`R2`·`T-1`)<!-- /work-items:after_stage2 --> — **이 괄호는 게이트 `work-item-consistency` ㈕ 가 대장과 대조한다**(`〈268〉`). **stage 값의 원본은 대장이다** |

## 1. 세션 시작 — 이 순서로 읽는다

작업을 시작하기 전에 반드시.

> **호스트를 껐다 켠 뒤라면 `dev-package/RESTART.md` 를 먼저 읽는다.** staging 은 자동으로 뜨지 않고,
> 되살리는 명령을 한 글자 빼면 **조용히 I2 이전으로 롤백된다.** 그 상태에서도 루트 헬스는 200 을 낸다.

1. `dev-package/03-HANDOFF.md` — **지금 어디까지 왔는가.** 진행 상황 요약의 출발점이다. ⚠ **항목 상태·`stage`·완료 정의의 원본은 대장 `work-items.yaml` 이고 이 문서는 그 반영본이다**(`§6`) — 갈리면 대장이 이긴다 ／ 종전 ~~여기가 진실원~~
2. `dev-package/DOMAINS.md` — 경계. 무엇을 무엇과 섞지 않는가
3. `dev-package/WORK-UNITS.md` — 완료 정의와 의존 그래프
4. `dev-package/PLAN-SoT.md` — 설계 근거와 결정 로그
5. `dev-package/sessions/<이번 WU>.md` — 이번 세션의 작업지시서

> **데이터·파이프라인을 만지는 작업이면 여기에 하나 더 붙는다 — `dev-package/DATA-REFERENCE.md`.**
> **`§0`(이미 저지른 실수 7건)을 먼저 읽는다.** 확장자로 싸잡아 분류하기 · 한 테이블만 보고 「없다」 하기 ·
> 선례를 「없다」 단정하기 · 측정 안 한 것을 측정된 것처럼 인용하기 · 주석을 실물 확인 없이 믿기 —
> **전부 실제로 일어났고, 일곱 중 여섯이 에러를 안 냈다.**
> 격자·dtype·HSR 스펙·COG 판정 기준·컬러맵·라이브러리 선택도 거기 있다. **다시 재지 말고 찾아 쓴다.**

`03-HANDOFF`와 실제 코드·인프라가 모순되면 **실물을 점검해 HANDOFF를 먼저 바로잡는다.**

## 2. 도메인 — 10개, 3 레이어

```
지식   D9  Ontology & Knowledge Graph      소유: 수문학 도메인
         ↓ 읽기 전용 조회 (Port)
추론   D10 AI Services — 제안만, 기록하지 않음   소유: AI 엔지니어링
         ↓ 제안 → 사람 확인 → 커밋 (단방향)
기록   D1 Identity&Lab · D2 Access&Policy · D3 Catalog · D4 Lineage
       D5 Ingestion&Pipeline · D6 Project · D7 Visualization · D8 Insight
                                          소유: 플랫폼 개발
```

배포 단위 5개 — `core-api`(D1 D2 D3 D4 D6 D8) · `pipeline-worker`(D5) · `viz-render`(D7) · `ai-service`(D9 D10) · `frontend`.

에픽 ↔ 도메인 대응은 `dev-package/DOMAINS.md §6`.

## 3. 불변 규칙 (기계가 강제한다)

1. **도메인은 자기 테이블 + D1(shared kernel)만 참조한다.** 타 도메인 테이블 직접 FK·접근 금지. cross-domain은 Port 경유
2. **D10 → D4(Lineage) 쓰기 경로가 존재하지 않는다.** AI는 제안만 하고, 사람이 확인한 것만 커밋된다
3. **D9·D10 저장소는 D1~D8과 마이그레이션 체인이 분리된다** (`db/ai` vs `db/platform`)
4. **core-api에 geo 라이브러리를 import하지 않는다** (rasterio·GDAL·xarray 등)
5. **모든 조회에 연구실 경계가 자동 주입된다** — 스코프 커널 + RLS + cross-tenant 음성 테스트
6. **정규 ID 타입은 `contracts/schemas/common.json` 에서만 정의된다**
7. **생성된 타입·클라이언트를 손으로 고치지 않는다**
8. **문서에 절대경로를 적지 않는다** (v1의 HANDOFF가 이 규칙이 없어 실제로 틀렸다)

### AI 응답 규격 (정본이 못 박은 것 — 타입으로 지킨다)

- 확신도는 `확실 | 애매 | 모름` **enum**. 숫자·퍼센트 필드 없음
- 근거 필드 **필수**(nullable 아님)
- **[모두 승인] 없음** — 배치 승인 엔드포인트를 노출하지 않는다
- 못 찾으면 **정직한 빈 상태**. 억지 제안 금지
- 검색은 **뒤진 범위를 먼저 밝히고**, 근거는 **한 줄** 고정
- **AI 없이도 v2는 완결된 제품이다.** 이 성질을 잃는 설계는 거부한다

## 4. 작업 규약

### 순서를 바꾸지 않는다

| 단계 | 넘어가는 조건 |
|---|---|
| 진입조건 확인 | `WORK-UNITS.md`의 해당 WU 행. **미충족이면 구현하지 말고 보고한다** |
| 계약 동결 | 계약 게이트 green |
| 실패 테스트 | **red 확인** — green으로 시작하는 테스트는 오라클이 아니다 |
| 구현 | 위 테스트 green |
| 게이트 | 전 게이트 green + (I2 이후) staging 배포 green |

### 경계를 넘어야 할 때

우회하지 말고 **멈추고 보고한다.** 셋 중 하나다.

1. Port를 하나 추가하면 되는가 (경계 유지)
2. 도메인이 잘못 쪼개졌는가 (`DOMAINS.md` 개정 필요)
3. 기획이 애매한가 (정본 확인 필요)

### 게이트 정책

`gates/run.sh`의 **미구현 게이트는 red를 낸다. 이건 버그가 아니다.**
v1 CI가 DB 없이 돌아 RLS 테스트를 green-by-skip 했던 실패를 구조적으로 막는 장치다.

- 게이트를 **우회하거나 임시로 끄지 않는다**
- 게이트를 green으로 만들려고 **검사 대상을 줄이지 않는다**
- 각 게이트는 red fixture로 **자신이 fail-closed임을 증명**해야 한다 (`selftest`)

## 5. 절대 하지 않는 것

- **PoC·v1 코드를 복사해 오기** — 참조는 *지식*이지 코드가 아니다 (`PLAN-SoT.md §6`)
- 게이트 우회·비활성화
- 생성물 손수정
- 문서에 절대경로 적기
- **"나중에"로 남기기** — 차단이면 차단 해제를 WU로 만든다. WU는 부분 완료로 닫지 않는다
- **범위 늘리기** — P1·백로그 B-01~B-09·대화 정문은 v2 범위 밖이다. 늘리려면 정본을 먼저 고친다
- **대화형 UI 도입** — v2 정본이 명시적으로 뺐다. 그 방향은 v3다

## 6. 세션 종료 규약 (예외 없음)

> ⭑ **상태 변경은 대장을 먼저 고친다.** 항목 상태의 유일한 원본은 `dev-package/work-items.yaml` 이고,
> `03-HANDOFF §1`·`WORK-UNITS §10~§11` 은 **그 반영본**이다. 순서를 뒤집으면 산문이 다시 갈라진다.
> **판정은 사람 기억이 아니라 게이트 `work-item-consistency` 가 한다.**

끝내기 전에 반드시:

1. `dev-package/03-HANDOFF.md §1` 해당 WU 상태 갱신 — ✅ / 🟧+사유 / ⛔+원인
2. 같은 파일 상단 **최종 갱신 · 현재 단계 · 다음 WU** 갱신
3. 새 결정은 `dev-package/PLAN-SoT.md §9`에 **값과 근거**를 적는다 (HANDOFF에는 링크만 — 두 곳에 적으면 갈라진다)
4. 새 블로커는 `03-HANDOFF §4`
5. 다음 세션이 확인할 **진입조건을 명시**한다

> 갱신하지 않고 끝낸 세션은 다음 세션의 시간을 두 배로 쓰게 만든다. v1에서 이 규약이 지켜진 구간만 인계가 성립했다.

## 7. 커밋

- 한 커밋 = 한 WU의 한 논리적 단계. 계약과 그 소비자는 **같은 커밋**에 (모노레포의 이점을 버리지 않는다)
- 메시지는 한국어. 첫 줄에 무엇을, 본문에 왜
- 게이트가 red인 상태로 `main`에 직접 push하지 않는다 (스캐폴드 단계 제외)

## 8. 자주 쓰는 것

```bash
./gates/run.sh <gate>        # 개별 게이트 — 목록은 gates/README.md
git log --oneline -10
```

기획 정본은 이 레포에 없다. 위치와 상태는 `planning/README.md`.

## 업로드(S3) — 고칠 때 알아야 할 것

업로드 바이트 저장이 **로컬/S3 로 갈린다** (`PLAN-SoT §9 〈173〉·〈174〉` · 운영 정본 `dev-package/S3.md`).
로컬 개발은 local 모드(form-data→디스크)가 기본이라 AWS 없이 그대로 돈다.

- 분기점은 저장 Port(`ports/storage.py`)와 전송 라우트(`routes/upload_transfers.py`) 둘뿐이다.
  s3 모드는 `COLAB_CORE_STORAGE_MODE=s3` + 버킷·리전 — 반쪽 설정은 기동이 거부된다
- **SigV4 는 표준 라이브러리 자작이다** (`kernel/sigv4.py`) — boto3 없음. S3 API 호출에 본문이
  있으면 `content-type: application/xml` 을 명시해야 한다 (urllib 기본값이 서명을 깨뜨린다).
  그래서 **에뮬레이터(MinIO 등) 검증 금지** — 관대한 통과가 진짜 S3 의 403 을 숨긴다
- **파트의 정본은 S3 ListParts 다** — 재개·완료 검증 어디서도 클라이언트 자기 보고를 믿지 않는다
- **완결이 곧 접수다** — `completeUploadTransfer` 전에는 `d5_upload` 도 `upload.accepted` 도 없다
- 만료 전송 정리는 **원장이 아는 것만** 지운다. 버킷 루트 스캔 금지 — 시드 lab_id 가 겹치는
  버킷에서 남의 데이터를 지운다. 최후 백스톱은 라이프사이클 abort-7d
- 폴더 업로드의 경로는 저장 키가 아니라 원장 메타(`relative_path`, d5→d3 승계)다 — 키 규약(생성물)은 불변
- **다운로드는 302 가 아니라 200 티켓이다** — 브라우저 `<a href>` 는 Bearer 를 못 싣는다. 바이트 op
  (`/downloads/{ticket}`)는 `security: []` 지만 티켓 클레임으로 `apply_scope` 를 심어 RLS 가 다시 판정한다.
  `session_secret` 이 없으면 500 `DOWNLOAD_UNAVAILABLE`(`createSession` 과 같은 자리) — 조용한 폴백을 두지 않는다
- AWS 검증은 콘솔 눈이 아니라 `services/core-api` 에서 `.venv/bin/python ops/s3_doctor.py` / `ops/s3_smoke.py`
- 시크릿 키는 채팅·커밋에 절대 넣지 않는다

## 배포 — 고칠 때 알아야 할 것

운영 문서 `docs/DEPLOY.md` · 변경 요약 `docs/DEPLOY_HANDOVER.md` · 기계적 절차 `infra/dev/README.md`.
**지금 서 있는 것은 dev 하나**(`d31zgpff2091oh.cloudfront.net`). prod 는 정본 `㊻` 가 ⏸ 다.

### 깨뜨리면 안 되는 것 (전부 이유가 있다)

1. **NAT 게이트웨이를 만들지 않는다.** 월 $45 이고 크레딧이 $120 뿐이다. EC2 가 퍼블릭 서브넷에
   있어 IGW 로 직접 나가고, RDS 는 밖으로 나갈 일이 없다.
2. **EC2 env 에 AWS 액세스 키를 넣지 않는다.** IAM 역할이 IMDSv2 로 임시 자격증명을 준다.
   키를 넣으면 공급자 사슬이 **그것을 먼저 집어** 역할이 무의미해지고 프리사인드 TTL 클램프도
   함께 죽는다. **IMDSv2 홉 제한은 `2`** 다 — 앱이 컨테이너 안에서 돌아 한 번 더 건넌다.
3. **CloudFront 를 걷어내지 않는다.** 도메인이 없어 HTTPS 를 주는 유일한 수단이다. HTTPS 가
   없으면 폴더 드롭이 보안 컨텍스트 밖이라 **폴더가 낱개 파일로 조용히 접힌다.** 로컬은
   `localhost` 라 보안 컨텍스트가 잡혀 **개발 중엔 안 보인다.**
4. **`/api/*` 동작의 원본 요청 정책을 `AllViewer` 에서 내리지 않는다.** `Authorization` 헤더가
   잘려 전량 401 이 된다. **캐시도 켜지 않는다** — API 응답이 남에게 나간다.
5. **`index.html` 에 긴 캐시를 걸지 않는다.** 배포해도 옛 화면이 계속 보인다. 해시가 붙은
   `assets/*` 만 `immutable` 이다.
6. **설정에 관대한 기본값을 도입하지 않는다.** 필수값이 없으면 **기동에 실패하는 것이 의도된
   동작**이다. 기본값이 있으면 설정을 빠뜨린 채 뜨고, 그게 제일 찾기 어렵다.
7. **배포 환경의 저장 모드를 로컬 파일로 바꾸지 않는다.** 업로드가 성공한 것처럼 보이면서
   바이트가 **EC2 디스크에만 쌓인다** — 재배포로 사라지고 `pg_dump` 에도 스냅샷에도 안 들어간다.
   **로컬은 local 이 정상이고 배포는 s3 가 정상이다.**
8. **DB 와 저장 백엔드를 따로 바꾸지 않는다.** 환경을 옮길 때는 env 파일을 **통째로 바꿔 끼운다.**
   한쪽만 바꾸면 dev 작업이 prod 버킷을 오염시키거나 DB 가 없는 객체를 가리킨다.
9. **정리 잡·백업을 앱 안의 백그라운드 태스크로 옮기지 않는다.** 워커가 여럿이면 중복 실행된다.
   cron 이 맞다. `pg_dump` 클라이언트는 서버 버전(16) **이상**이어야 한다.
10. **백업은 `colab_backup` 롤로 뜬다.** RLS 가 FORCE 라 소유자도 정책에 걸리고, 경계가 없으면
    `current_lab_id()` 가 NULL 이라 **어떤 롤도 전수를 못 읽는다**(RDS 마스터조차).
    ⚠ 그 자격이 새면 연구실 경계가 통째로 뚫린다.

### 고치기 전에 돌릴 것

`cd services/core-api && .venv/bin/python ops/deploy_doctor.py --env dev …` — 14 항목 중 어디가
`✗` 인지가 원인의 절반이다. 인자는 `infra/dev/README.md`. **부분 실행 둘을 합쳐 green 이라 하지
않는다** — `─ 0` 이 나온 한 번의 결과만 근거다. 증상별 진단은 `docs/DEPLOY.md §3`.

### 확장하려면

도메인 연결(**ACM 인증서는 반드시 us-east-1**) · prod(`docs/DEPLOY.md §5` 를 `-prod` 이름으로
다시 실행) · CI 자동 배포(ECR 부터 — 지금은 `scp` 다). 자리는 `docs/DEPLOY.md §10` 에 있다.
